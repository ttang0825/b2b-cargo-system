> 🔴 이 파일은 `CLAUDE.md` 에서 옮겨온 세션 기록입니다. **요약하거나 줄이지 마십시오** —
> 되돌리면 안 되는 결정의 근거가 여기에만 있습니다.
> 담는 범위: 60차 #107 2026-08-31  →  65차 #112 2026-09-02   (6건)
> 입구는 `HANDOFF.md`, 규칙과 로드맵은 `CLAUDE.md` 입니다.

---

**작성일: 2026-07-23** (최종 갱신: 2026-09-02, Claude Code 세션에서 직접 갱신 — 65차
세션: "랜딩 2차 개편 + 법적 문서" — 🔴 **차수를 쓰지 않았다**(41·47·59·63·64차와 같은
처리). 신규 파일 **1개**(`components/BusinessInfoModal.tsx`).
커밋 15개(본작업 10 + **실사용 리뷰 4라운드 5**). **PR #112 merge됨**(squash `284c776`).
🟢 **DB 변경 0**(운임기준표 값 0) · 🟢 **신규 의존성 0** · 🟢 **`globals.css` 0** ·
🟢 **`lib/legal/` 0** · 🟢 **`SiteFooter` 0** · 🟢 **화주포털 0** ·
🟢 **관리자 회귀 0**(9화면 실측, `/admin/rates` 만 의도적 변경).
🔴 **64차가 답을 기다리던 것 4건이 전부 확정됐다 — 아래 ① 을 먼저 읽을 것.**

**═══ ① 🔴 확정 4건 (2026-09-02, 사용자) — 64차의 대기 항목이 전부 닫혔다 ═══**
```
  1  🔴 운임기준표 ×0.833 인하는 **의도다**        → 64차 ① 의 답. 되돌리지 말 것
  2  🔴 「이사」를 고객 접점에서 **다시 뺀다**       → 30차 확정을 뒤집고 34차로 복귀
  3  🔴 「최저가」는 빼고 **「월별 이벤트」는 남긴다** → 64차 ⑥ 의 답
  4  🔴 전자상거래법 표시는 **「사업자정보」 모달**    → 64차 ⑦ 의 답
```
🔴 **1번 — 「인하됐네」 하고 45차 값으로 되돌리지 말 것.** 64차 ① 이 *"의도인지 사고인지
아직 안 정해졌다"* 며 손대지 않고 남겨둔 것인데, **의도한 인하로 확정됐다.** 그래서
1톤 10km = **40,000원**이 지금의 정본이고 45차 마이그레이션(48,000)은 **옛 값**이다.
⚠️ **작업지시서 0-2 가 경고한 5톤급 역마진(−3.7%)은 그대로 남아 있다** — 마진을 다시
볼지는 별도 판단이고, 이 차수는 DB 를 한 원도 건드리지 않았다.

**═══ ② 🔴 「이사」를 다시 뺐다 — 30차 확정을 뒤집은 것이다 ═══**
30차가 *"이사서비스를 노출한다"* 로 확정했던 것을 **34차 방침(고객 접점에서 이사를 쓰지
않는다)으로 되돌린 것**이다. 🔴 **30차 기록을 근거로 되살리지 말 것.**
```
  왜   이사는 wecarry24.co.kr 로 분리한다(사용자 확정 2026-09-02)
  결과 서비스 카드가 「고정화물 배차」 · 개인화물 · 전국화물 · 혼적화물 4장
  검증 공개 9화면 렌더링 텍스트에 「이사」 **0건**(대표이사·특이사항 제외 후)
```
🔴 **`public/landing/service-04-moving.jpg` 는 지우지 않았다** — `wecarry24.co.kr` 이
그 파일을 쓴다. **"쓰이지 않는 이미지"로 보고 정리하지 말 것.**
🔴 **금지 표현 자동검증에 「이사」를 다시 넣었다** — 30차에 빠졌던 것이다.

**═══ ③ 🔴 「고정화물 배차」가 첫 서비스다 — 문구가 실제 기능에 근거한다 ═══**
```
  매주·매월 반복되는 운송은 노선과 조건을 등록해 두고 배차합니다.
  매번 견적을 다시 받지 않아도 됩니다.
```
🔴 **이 문구는 지어낸 것이 아니라 화주포털 프리셋 기능(`customer_presets`, 20차·25차)에
근거한다** — 배송지·화물·요청사항을 저장해 두고 불러 쓰는 그 기능이다. 🔴 **「전담 차량을
확보한다」는 취지로 바꾸지 말 것** — 그런 약속을 할 수 있는 상태가 아니다(작업지시서 금지 5).
🟢 **사진은 68차에 들어와 채워졌다**(2026-09-04). ⚠️ **여기 적어둔 「`img` 한 줄만 채우면
된다」는 부정확했다** — 경로를 `data.ts` 에 직접 박지 않는 것이 규칙이라 `lib/landingImages.ts`
에 상수를 먼저 만들어야 했고, 원본이 3:2 로 와서 4:3 크롭도 필요했다(68차 ①·③).
🔴 **`img` 를 다시 비우면** 화면이 **회색 상자만 남기고 `<img>` 를 아예 그리지 않는다**(깨진
이미지 아이콘 방지) — 그 분기는 `app/page.tsx` 에 그대로 살아 있다. **자리표시자 문구를
넣지 말 것.**

**═══ ④ 🔴 「최저가」는 뺐고 「월별 이벤트」는 남겼다 ═══**
64차 ⑥ 이 *"표시광고법 제3조 실증 자료가 없으면 부당표시"* 라며 물어둔 것의 답이다.
```
  WHY 01 제목  합리적인 운임가 및 월별 이벤트          ← 그대로
  WHY 01 본문  최저가 운임을 산정하고 →  **거리·차량·상하차 조건을 기준으로 운임을 산정하고**
```
🔴 **금지 표현 검증의 랜딩 예외(`ALLOW = { '/': ['최저가'] }`)도 같이 제거했다** — 예외를
남겨두면 다음에 누가 다시 넣어도 검증이 안 잡는다. 공개 9화면 실측 **0건**.
⚠️ **「월별 이벤트」는 남았으므로 실제로 운영해야 하고, 적용 조건과 기간을 함께 알려야
한다**(표시광고법). 지금 화면에는 조건·기간이 없다 — **운영이 시작되면 그때 함께 적을 것.**

**═══ ⑤ 🔴 법적 문서 모달이 랜딩에서만 비어 보였다 — 상속 색이 원인이다 ═══**
사용자 신고 *"랜딩 푸터의 이용약관·개인정보처리방침·이메일무단수집거부에 내용이 안 나온다"*.
🔴 **내용이 없던 것이 아니라 글자색이 배경과 같았다.** `LegalModal` 은 자기를 부른 곳의
DOM 안에 그려지는데, 랜딩의 법적 링크가 어두운 CTA 띠 안(`div.landing-cta-links`,
**인라인** `color: rgba(255,255,255,0.6)`)에 있어서 **모달이 그 흰색을 상속**했다. 모달
배경은 흰색이라 글자가 통째로 사라진 것이다(`/about`·`/vehicles` 는 밝은 배경이라 멀쩡했다).
🔴 **해법은 오버레이에 인라인 `color: var(--text)` 를 선언한 것이다.**
```
  왜 인라인인가   조상의 인라인 style 을 이겨야 한다 — 클래스 규칙으로는 못 이긴다
  왜 globally 아닌가  `globals.css` 변경 0 이 완료조건이었다(관리자 31화면 공유)
  실측            랜딩 대비 15:1 · /about 16.3:1 · 모달 color = rgb(25,31,40)
```
🔴 **이 한 줄을 지우지 말 것** — 지우면 랜딩에서만 다시 안 보인다. 🔴 **어두운 배경 안에서
모달을 여는 새 화면을 만들 때도 같은 함정이 있다**(모달은 자기 글자색을 스스로 선언해야 한다).
🟢 `BusinessInfoModal` 도 같은 이유로 처음부터 이 선언을 갖고 있다.

**═══ ⑥ 🔴 「사업자정보」 모달 — 푸터 한 줄로 되돌리지 말 것 ═══**
64차 ⑦ 이 *"랜딩 푸터에서 호스팅사업자를 빼서 전자상거래법 제10조 1항 요건이 지금
미충족이다"* 라고 남긴 것의 답이다. 사용자가 **(b) 법적 문서 링크 옆에 「사업자정보」**
로 확정했다(2026-09-02).
```
  자리   CTA 하단 법적 문서 3종 옆(네 번째)
  내용   상호(CompanyNameMark) · 대표 · 사업자등록번호 · 통신판매업 신고 ·
         운송주선사업 허가 · 주소 · 개인정보 보호책임자 · **호스팅 제공 Vercel Inc.** ·
         대표전화(tel:) · 이메일(ObfuscatedEmail)
```
🔴 **값을 모달에 직접 적지 않았다** — `lib/companyInfo.ts` 의 `COMPANY_BUSINESS_INFO`
배열을 읽는다. **모달에 값을 하드코딩하지 말 것.**
⚠️ **`components/SiteFooter.tsx` 에 같은 목록(`BUSINESS_INFO`)이 따로 있다** — 완료조건
25(SiteFooter 변경 0) 때문에 이번에 합치지 못했다. 🔴 **다음에 그 파일을 열 때
`COMPANY_BUSINESS_INFO` 로 합칠 것** — 두 벌이면 조용히 갈린다.
🔴 **호스팅사업자를 다시 빼지 말 것** — 이것이 이번에 요건을 충족시킨 항목이다.

**═══ ⑦ 🔴 보험 상수를 채웠다 — 값은 화면에 나가지 않는다 ═══**
2026-09-01 에 DB손해보험 **일반화물** 증권을 받았다.
```
  증권번호     120261161557
  담보         화물자동차운송주선위험(대물)
  1사고당 한도  100,000,000원
  자기부담금    200,000원
  보험기간     2026-09-02 ~ 2027-09-01
```
🔴 **이 값들을 화면에 렌더링하지 말 것 — 내부 값이다.** 약관 제19조 2항이 *「요청이 있는
경우 보험 가입 사실과 보상 한도를 안내합니다」* 라고만 적고 있고, 34차가 제19조를 **금액을
쓰지 않는 설계**로 개정했다(상한은 3항 가액 신고제 + 4항 고가물 특칙). 실측으로 참조 지점
**0건** 확인했다.
⚠️ **자기부담금이 20만원이라 소액 사고에는 실질적으로 보험이 작동하지 않는다.** 파손 문의에
「보험으로 다 처리된다」는 취지로 답하지 말 것.
🔴 **`INSURANCE_ENABLED` 는 건드리지 않았다**(30차 리뷰에 이미 `true`). 내린 것은
`INSURANCE_INFO_IS_PLACEHOLDER` 뿐이고, 그 결과 **빌드 경고가 0건**이 됐다(자리표시자 5개
전부 false). 🔴 **증권 만기(2027-09-01) 뒤 갱신이 확인되지 않으면 값을 비우고 플래그를
다시 true 로 올릴 것.**
⚠️ **이사화물 증권(120260427278)은 상수에 넣지 않았다** — 이사를 고객 접점에서 뺐기
때문이며, 🔴 **그 증권 자체는 살아 있으므로 「해지됐다」고 적지 말 것.**
🟢 **음성 시험을 했다** — `INSURANCE_ENABLED` 를 잠시 false 로 두고 다시 빌드하니
WHY 가 **4장 · 번호 01~04** 로 이어지고 「보험」 0건이었다. 되돌린 뒤 md5 로 원본 대조했다.

**═══ ⑧ 🔴 히어로에서 견적 버튼을 뺐다 — 34차 순서를 바꾼 것이다 ═══**
34차가 *"히어로·마감 CTA 둘 다 견적 → 전화 순서로 통일"* 이라고 확정했는데, **히어로에서는
전화 하나만 남겼다**(사용자 지시). 🔴 **34차 기록을 근거로 견적 버튼을 되살리지 말 것.**
🟢 **마감 CTA 는 그대로 견적·계정신청 두 개다** — 견적으로 가는 길이 사라진 것이 아니다.
🔴 **사진 교차는 4초 유지 + 1초 교차 = 한 바퀴 10초다**(사용자 확정 2026-09-02, 지시서의
3.5초가 아니다). ⚠️ **`@keyframes` 의 % 가 유지/교차 비율을 담고 있어서 `animation-duration`
만 바꾸면 비율이 깨진다** — 둘을 같이 고칠 것(`0%,40%{0} 50%,90%{1} 100%{0}` + `10s`).
🟢 `prefers-reduced-motion` 에서 `animation: none` 인 것을 실측했다.

**═══ ⑨ 🔴 WHY 5종이 배타 아코디언이 됐다 ═══**
`<details name="wecarry-why">` — 브라우저가 배타를 처리한다(FAQ 가 쓰는 방식과 같다).
🔴 **접힌 상태에 지금 보이던 제목·본문이 그대로 남는다** — 펼치면 **2문장이 더** 나온다.
정보를 접어 감춘 것이 아니다. 🔴 **`<div>` 로 되돌리지 말 것.**
⚠️ **`open=true` 를 한꺼번에 걸면 마지막 하나만 열린다**(62차 함정) — 검증은 하나씩 열 것.
🟢 `.landing-reason-detail` 은 `white-space: pre-line` 이고 `detail` 문자열의 `\n` 이 줄을
가른다 — **둘이 같이 있어야 성립한다.**

**═══ ⑩ 🔴 약관은 한 글자도 고치지 않았다 — 출력해 보고만 했다 ═══**
사용자가 *"이용약관을 정리해서 코드에 전달하자"* 고 했으나 **지시서에 수정안이 없었고**,
🔴 **`lib/legal/` 변경 0** 이 완료조건이었다. Code 가 한 것은 조사와 보고뿐이다.
```
  🔴 약관 본문에 「이사」 조문은 **0건**이다 — `lib/legal/terms.ts` 의 주석 2줄뿐
     (42·43행). 즉 **이사를 빼면서 약관에서 고칠 것이 애초에 없었다**
  🟢 제19조(손해배상) 7항 · 제20조 5항 전문을 그대로 출력해 사용자에게 보고했다
     🔴 「제20조(면책)」는 **오기다** — 제20조는 「클레임의 접수 및 처리」다(66차 ⑧②).
        구 제20조(면책)는 34차에 제19조로 통합되어 사라졌다
```
⚠️ **보고한 것 하나 — 제19조 4항(고가물은 명시한 경우에만 배상)과 증권의 면책 품목
(원고·설계서·미술품·귀금속·유가증권)이 어긋난다.** 「수탁화물확장 특별약관」이 없으면
그 간극은 회사가 떠안는다. 🔴 **보험사에 확인할 사항이며 약관 개정은 별도 지시서다.**

**═══ ⑪ 🟢 검증 ═══**
```
  법적 문서      모달 3종 전부 전문 렌더링(약관 6,522자 · 방침 7,446자 · 이메일 600자) ·
                「전문 준비 중」 0건 · 모달 color = rgb(25,31,40)
  「이사」        공개 9화면 렌더링 텍스트 **0건**(대표이사·특이사항 제외 후) ·
                service-04-moving.jpg 파일은 남아 있음
  서비스 카드    4장 · 고정화물 배차 → 개인화물 → 전국화물 → 혼적화물 순 ·
                첫 카드에 `<img>` 0개(회색 상자만)
  히어로        animationDuration 10s · reduced-motion 에서 `none` · 「무료 견적 문의」 0건
  WHY           details 5장 · name 하나(`wecarry-why`) · 처음엔 전부 접힘 ·
                하나 열고 다른 것을 누르면 **앞의 것이 닫힌다** · detail pre-line ·
                🟢 보험을 끄면 **4장 · 01~04** (실제로 빌드해서 확인)
  설명글        🔄 리뷰① 뒤 **NOTE 3줄 + 표 아래 각주 1줄** · 팝업과 /vehicles 가
                **문자 단위로 같다** · pre-line · 필수 넷 유지 · 「할인」·「더 저렴」 0건
  기준가        게시 6행 · DB 값 6종 렌더링 · 8~25톤 금액 0건
  「최저가」      공개 9화면 0건 · 랜딩 예외 제거 · 「월별 이벤트」 유지
  사업자정보     모달에 표시사항 9종 + 호스팅 + 대표전화 + 이메일 · 상호는 CompanyNameMark
  보험          렌더링에 금액 0건 · 상수 참조 지점 0건 · 자리표시자 경고 **0건**
  /admin/rates  🟢 **실제로 눌러서 확인** — 「10km 이내」 × 1톤 을 41,000 → 42,000 으로
                고치니 확인 창(차급 · 전 → 후) · 취소하면 41,000 유지 ·
                **「60km 이내」 는 창 없이 저장**(비게시 행)
  관리자 회귀    9화면 실측 — overflow 0 · container 1100px · card #fff · body #f2f4f6 ·
                SUIT Variable · nav chips 4(guide 만 14) — **64차 기준선과 동일**
  오버플로우     랜딩·/vehicles·팝업 × 1280·430·390·360px **전부 0**
  npx tsc       통과(파일로 받아 종료코드 확인)
  npm run build 프리렌더 실패 **44개** — 🔴 **항목 단위로 64차 기준선과 완전히 동일**
  변경 없음      DB · 운임기준표 · globals.css · lib/legal/ · SiteFooter · 화주포털 · package*
```
🔴 **프리렌더 기준선은 44다**(64차 ③ — `/vehicles` 가 동적 라우트가 되어 45 → 44 로
줄었다). **45를 기대하고 「하나 빠졌다」 하지 말 것.**

**═══ ⑫ ⚠️ 지시서 전제와 달랐던 것 · 겪은 함정 ═══**
🔴 **지시서 전제 셋이 실제와 달랐다.**
```
  ① 「히어로 교차가 5.5초라 느리다」   실제 **14.4초**였다(그래서 더 느렸다)
  ② 「마감 CTA 번호를 가운데로」       이미 가운데였다 — 섹션이 `textAlign: center` 다
  ③ 「약관에서 이사 조문을 찾아라」     본문에 **0건**(주석 2줄뿐) — 고칠 것이 없었다
```
⚠️ **완료조건 10 의 「3.5초」는 사용자 확정으로 10초가 됐다**(위 ⑧). 완료조건만 보고
3.5초로 되돌리지 말 것.
⚠️ **거짓 ❌ 가 다섯 번 났고 전부 시험 자산 결함이었다**(27·59·60·61·62·64차와 같다) —
① 법적 문서를 `button` 으로 찾았는데 실제로는 `<a class="landing-legal-link">` 다
(30차 하이브리드 — 링크지만 평클릭을 가로채 모달을 연다) ② 서비스 카드 제목을 `h3` 로
찾았는데 `<div>` 다 ③ 카드 제목에 공백을 넣어 비교했는데 시안 원문은 「개인화물서비스」로
붙어 있다 ④ 요금 가이드 트리거를 「차량·요금 가이드」로 찾았는데 실제는 **「차량 · 요금
가이드」**(공백 있음) ⑤ `/admin/rates` 셀을 `<td>` 로 클릭했는데 클릭 핸들러는 안쪽
`<span>` 에 있다. 🔴 **"❌ 가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 말 것.**
🔴 **목이 실제 DB 보다 헐거워 한 번 막혔다**(원칙 56번) — 목의 `rate_distance_tiers` 에
`distance_from_km` 이 없어서 `/admin/rates` 가 42703 으로 표를 통째로 못 그렸다. 목을
고치고서야 확인 창을 실제로 눌러볼 수 있었다.
🔴 **옛 `next-server` 가 포트를 잡아 낡은 빌드를 두 번 쟀다 — 여섯 번째다**(57·60·63차와
같다). ⚠️ **이 환경에는 `ss` 가 없다** — `fuser -n tcp <포트>` 로 확인할 것. 그리고
**PID 파일에는 `npx` 래퍼 PID 가 들어가서 자식 `next-server` 가 안 죽는다**(39차 함정 d) —
`/proc/*/comm` 으로 실제 프로세스를 찾아 죽일 것.

**═══ ⑭ 🔴 PR #112 실사용 리뷰 4라운드 — 내 판단 둘이 뒤집혔다 ═══**
사용자가 Preview 를 눌러 보고 지적한 것이다(2026-09-02~03). 커밋 5개로 반영했다.

**1라운드 — 지적 6건**
```
  ① 히어로 문장       한 줄 구성으로 맞추자
  ② WHY 펼침         카드나 칸 디자인으로 「부가 설명」처럼 · 줄바꿈도 보기 좋게
  ③ 「어디서 얼마가 붙었는지 확인하실 수 있습니다.」 빼자
  ④ 「정확한 금액은 견적으로…」  기준가 **표 아래 첨부글**로 옮기자
  ⑤ 「기준가」        금액 **위**에 표시하고 글씨톤을 다르게
  ⑥ FAQ 회신 시간     「평일 업무 시간에 접수된 건은 확인 후 신속히 안내드립니다.」
```
🔴 **④ 로 설명글이 두 상수로 갈렸다** — `START_PRICE_NOTE`(3줄, 제목 아래)와
**`START_PRICE_FOOTNOTE`**(1줄, 기준가 표 **바로 아래**). 🔴 **필수 넷은 그대로 화면에
다 있지만 두 곳에 나뉘어 있으니 한쪽만 보고 「빠졌다」 하지 말 것** — ①·② 는 NOTE 에,
③·④ 는 각주에 있다. 두 상수는 팝업·`/vehicles` 가 **같이** 읽는다(한쪽만 고치면 갈린다).
🔴 **⑤ 「기준가」는 이제 금액 옆 라벨이 아니라 칼럼 머리다**(`/vehicles` `<th>` 12px,
팝업 머리 행 13px `#8B8A85`). 금액 옆으로 되돌리지 말 것.
🔴 **진짜 회귀를 하나 만들었다가 같은 라운드에서 잡았다** — ⑨ 에서 WHY 행을
`<details>` 로 바꾸면서 **`app/landing.css` 의 모바일 규칙 6개가 빗나갔다**(그리드가
`<summary>` 로 옮겨갔는데 규칙은 `.landing-reason-row` 를 겨누고 있었다). 그래서 펼침
카드가 번호 칼럼 스타일(14px `#A8A79F`)을 받고 모바일 2열이 무너져 있었다.
`.landing-reason-head` 로 다시 겨눠 고쳤다(390px 에서 번호 14px · 이미지 92px ·
칼럼 `244px 92px` 실측). ⚠️ **본작업 검증이 오버플로우만 재고 모바일 레이아웃 구조를
안 재서 놓친 것이다** — 구조를 바꾸면 그 요소를 겨누는 CSS 를 같이 훑을 것.

**2라운드 — WHY 펼침 카드에서 왼쪽 옐로 막대 제거**
1라운드에 「부가 설명임을 표시」하려고 세운 3px 옐로 막대를 뺐다 — *"깔끔하게 흰 카드로만"*.
🔴 **다시 넣지 말 것**(주석에도 남겼다). 네 변 전부 `1px #E8E7E2` · 흰 배경 · radius 12 ·
76px 들여쓰기 · `pre-line` 은 그대로다.

**3·4라운드 — 모바일 히어로 전화 버튼**
```
  본작업   350×83 · 26px · padding 23px 45px · min-width 330   ← 데스크탑 시안 값 그대로
  3라운드  350×56 · 18px · padding 15px 28px · min-width 0     「너무 크다」
  4라운드  **225×56**  (width:auto + 부모 align-items:flex-start)  「글자 폭에 맞춰」
```
🔴 **`width: auto` 는 부모의 `align-items: flex-start` 와 쌍이다** — `stretch` 로 되돌리면
`width: auto` 여도 다시 꽉 찬다. 둘을 같이 볼 것.
🔴 **`min-width: 0` 을 빼지 말 것** — 330px 이 남아 있으면 360px 화면에서 버튼이 본문
칼럼(320px)보다 넓어진다(실측으로 그 상태였다).
⚠️ **세로가 15px 인 것은 이 버튼만 1px 테두리가 있어서다** — 15+15+24+2 = 56 으로 마감
CTA(16+16+24, 테두리 없음)와 실측 높이가 같아진다.
🟢 **마감 CTA · 계정신청 버튼은 그대로 꽉 채운다**(별도 규칙) — 거기는 선택지가 둘이라
나란히 꽉 차는 편이 누르기 쉽다. 🔴 **데스크탑은 건드리지 않았다**(334×83 · 26px).
🟢 **`app/landing.css` 의 ≤700px 블록에만 걸었다** — `app/page.tsx` 의 `heroBtn` 은 그대로다.

**리뷰 검증** — 라운드마다 프로덕션 빌드 + 실제 렌더링으로 쟀다(21 · 27 · 32항목, 실패 0).
카드 네 변 1px 균일 · 옐로 0건 · 모바일 3폭 전부 225×56 이고 글자 폭 + 58 과 일치 ·
데스크탑 3폭 334×83 유지 · 오버플로우 전부 0 · 매 라운드 `tsc` 통과 · 프리렌더 실패 0 ·
자리표시자 경고 0건.

**═══ ⑬ ⚠️ 답을 기다리는 것 · 다음 차수 ═══**
🔴 **다음 지시서는 그대로 32차(내부 정합 ① 견적·오더)다** — 이번 차수도 번호를 쓰지 않았다.
```
  ⚠️ ① 「월별 이벤트」의 적용 조건·기간 (위 ④, 표시광고법)
  ⚠️ ② 제19조 4항 ↔ 증권 면책 품목의 간극 (위 ⑩) — 보험사 확인 후 약관 개정 여부
  ⚠️ ③ 5톤급 역마진 (위 ①) — 운임 인하가 확정됐으므로 마진을 다시 볼지
  ⚠️ ④ 혼적(합짐) 문장을 요금 설명글에 되살릴지 (64차 ⑤ 에서 그대로)
```
⚠️ **사용자가 코드 밖에서 할 일** — 🟢 고정화물 배차 카드 사진은 **68차에 끝났다** ·
🔴 카카오톡 OG 캐시
초기화(63차 ⑩) · ⚠️ 원격 임시 브랜치 정리(`claude/*`, 이 환경은 삭제가 막힌다) ·
⚠️ 검색엔진 등록.
⚠️ **답을 못 받은 것 2건**(그대로다) — 자주 쓰는 화물 목록 이름 표시(26차부터) · 무료 대기
`"초과 시 30분당"` 단위(25차부터).
🟢 **리뷰 4라운드에서 위 4건에 대한 답은 나오지 않았다** — 전부 그대로 대기 중이다.

**작성일: 2026-07-23** (최종 갱신: 2026-09-02, Claude Code 세션에서 직접 갱신 — 64차
세션: "랜딩 소수정 + 차량·요금 가이드 개편" — 🔴 **차수를 쓰지 않았다**(41·47·59·63차와
같은 처리). 신규 파일 **2개**(`lib/startPrices.ts` · `app/api/public/start-prices/route.ts`).
커밋 3개. **PR #111 merge됨**(squash `83691b1`).
🟢 **DB 변경 0** · 🟢 **신규 의존성 0** · 🟢 **`globals.css` 0** · 🟢 **`lib/legal/` 0** ·
🟢 **화주포털·관리자 변경 0**.
🔴 **확정 셋이 사용자 지시로 뒤집혔다** — 금지 표현 「최저가」·「월별 이벤트」 · 게시
차급 11행 · 푸터 호스팅사업자. **아래 ②·④·⑦ 을 근거 없이 되돌리지 말 것.**

**═══ ① 🔴 운영 DB 운임기준표가 이미 ×0.833 인하돼 있다 — 아직 답을 못 받았다 ═══**
「운임기준표를 고쳤는데 랜딩 금액이 안 바뀐다」는 신고를 조사하다 `verify`(읽기 전용)로
운영 DB 를 읽었더니 **전 구간이 이미 내려가 있었다.**
```
                     코드에 적혀 있던 값   운영 DB 실측(2026-09-01)
  1톤 10km 이내            48,000              **40,000**
  1톤 60km 이내            84,000              **70,000**
  8톤 10km 이내           151,000             **125,800**
  18톤 200km 이내         341,000             **284,200**
  25톤 400km 초과         954,000             **795,000**
```
🟢 **2026-09-02 에 「의도한 인하」로 확정됐다**(65차 ①) — 40,000 이 정본이고 45차 값은
옛 값이다. 🔴 **되돌리지 말 것.** ⚠️ 5톤급 역마진은 그대로 남아 있다.
🔴 **전부 정확히 ×0.833 — 45차가 마진 17%를 얹기 *전*의 v8 원본값이다.** 사용자가 말한
「기준가 40,000원」은 예시가 아니라 **이미 DB 에 넣어둔 실제 값**이었다.
🔴 **작업지시서 0-2 가 경고한 「5톤급 역마진(−3.7%)」이 지금 실제 상태다**(45차가 마진
17%를 얹은 이유가 장거리 역마진이었다). **DB 는 한 원도 손대지 않았고 답을 기다리는 중이다.**
⚠️ **다음 세션이 이 값을 보고 「인하됐네」 하고 되돌리지 말 것** — 의도인지 사고인지가
아직 안 정해졌다. 되돌린다면 45차 마이그레이션 값(`migrations/2026-08-25_rate_base_fare_v8.sql`)이 근거다.

**═══ ② 🔴 기준가가 이제 `rate_distance_tiers` 를 실시간으로 읽는다 ═══**
30차까지는 `components/landing/data.ts` 의 `startPrices` 배열이 화면에 나가는 값이었고,
그래서 **`/admin/rates` 에서 고쳐도 랜딩·`/vehicles` 금액이 안 바뀌었다**(45차 (6)·61차 ⑥ 이
「손으로 맞춘 값」이라고 적어둔 그것이다). 이제 `lib/startPrices.ts` 가 유일 정의처이고
「10km 이내」 행을 매번 읽는다.
```
  랜딩 팝업     /api/public/start-prices (신규, service_role)
  /vehicles     서버 컴포넌트에서 직접 조회 (force-dynamic)
```
🔴 **반드시 service_role 서버 경로를 거친다** — `rate_distance_tiers` 는 21차에 잠겨
있어 anon 은 **0행**을 받는다(에러도 안 난다). 화면에서
`supabase.from("rate_distance_tiers")` 로 직접 읽으려 하지 말 것.
🔴 **공개 범위는 「10km 이내」 한 칸뿐이다**(28차 확정) — 다른 구간·가산 값을 얹지 말 것.
🔴 **원칙 21번대로 `force-dynamic` 과 `createServiceClient()` 를 둘 다 걸었다.**
🟢 **캐시를 실측으로 뚫었다** — 목 DB 를 40,000 → 41,000 으로 바꾸고 **Next 서버를
재시작하지 않은 채** API·`/vehicles` 둘 다 새 값이 나오는 것을 확인했다.
🔴 **폴백 금액을 코드에서 없앴다 — 지시서에 없던 판단이다.** 코드 폴백(48,000)과 실제
DB(40,000)가 **이미 8,000원 어긋나 있었고**, 폴백이 뜨면 **게시가 ≠ 견적가**가 되어 그대로
표시가격 분쟁이다. 그래서 **표를 아예 그리지 않고** 「기준가를 불러오지 못했습니다. 정확한
금액은 견적으로 안내드립니다.」만 남긴다(팝업·`/vehicles` 둘 다).
🟢 **실패 경로를 실제로 태워 확인했다** — 목을 죽이니 표 0개 · 낡은 숫자 0건 · 안내 문구
표시, 되살리니 정상. ⚠️ **폴백 배열을 다시 만들지 말 것** — 두면 반드시 낡는다.
⚠️ **대가가 있다** — 이제 `/admin/rates` 에서 숫자를 고치는 **순간** 공개 게시가가 바뀐다.
그 화면은 클릭이 곧 저장이고 되돌리기가 없어서 **오타 하나가 그대로 게시된다.**
45차 (6)이 「자동 연동을 하려면 저장 전 확인 단계를 같이 넣으라」고 적어둔 것이 이 위험이고,
🟢 **65차 ⑨ 에 확인 창을 만들어 해소했다** — 「10km 이내」 × 게시 차급을 고칠 때만
차급·전→후 금액을 보여주고 「이 값이 랜딩과 차량안내에 바로 게시됩니다」라고 묻는다.
🔴 **전 구간에 걸지 말 것**(매번 눌러야 하면 실무가 막힌다). ⚠️ 아래 「아직 안 만들었다」는
그 이전 시점 기록이다.

**═══ ③ 🔴 프리렌더 기준선이 45 → 44 로 줄었다 — 신규 실패가 아니다 ═══**
`/vehicles` 가 서버에서 DB 를 읽게 되면서 **동적 라우트가 되어 프리렌더 대상에서 빠졌다.**
줄어든 1건이 정확히 그 페이지이고 **신규 실패는 0**이다. 🔴 **다음 세션의 기준선은 44다** —
「45였는데 44면 뭔가 빠진 것 아닌가」 하고 찾지 말 것.

**═══ ④ 🔴 게시 차급이 11행 → 6행이다 (사용자 결정) ═══**
1톤 · 1.4톤 · 2.5톤 · 3.5톤 · 5톤 · 5톤 플러스/축.
🔴 **30차 리뷰 1라운드의 「운임기준표 기준으로 세분화」 지시로 6 → 11행이 됐던 것을 다시
6행으로 되돌린 것이다**(61차 ⑥·⑭①). **30차·61차 기록을 근거로 11행으로 되돌리지 말 것.**
🟢 **8~25톤을 지운 것이 아니라 게시할 때만 자른다**(`PUBLISHED_START_PRICE_TONS`) — 다시
게시하려면 그 배열에 차급을 더하기만 하면 된다(값은 DB 가 갖고 있다).
⚠️ **25톤 게시가가 화면에서 사라졌다.** 랜딩 문구 「1톤부터 5톤 이상, 특수차량까지」와의
간극은 설명글의 **「5톤보다 큰 차량도 문의」** 한 문장이 메운다 — 🔴 **그 문장을 빼지 말 것.**
🟢 근거는 FAQ 3번이 「5톤 플러스/축 차량까지 배차가 가능하며」라고 답하는 것과 맞는 범위다.
🟢 **「시작가」 → 「기준가」로 바꿨다**(렌더링 0건) — 오르내리는 값이라는 뜻을 담는다.

**═══ ⑤ 🔴 설명글은 3줄이고 `\n` + `pre-line` 둘이 같이 있어야 성립한다 ═══**
표 아래 안내 4줄 → 제목 아래 설명글로 합쳤고(2026-09-01), **62자 3줄로 줄였다**(2026-09-02,
*"글이 너무 길다"*).
🔄 **65차 리뷰① 에 구조가 한 번 더 바뀌었다** — 셋째 줄(「정확한 금액은 견적으로…」)이
`START_PRICE_FOOTNOTE` 로 떨어져 나가 **기준가 표 바로 아래 각주**가 됐고, 그 자리에
고정물량 한 줄이 들어왔다. 🔴 **필수 넷은 두 상수에 나뉘어 여전히 화면에 다 있다** —
한쪽만 보고 「빠졌다」 하지 말 것(65차 ⑭). 아래 3줄 표기는 그 이전 시점 기록이다.
```
  10km 이내 기준 금액이며 부가세는 별도입니다.
  실제 운임은 조건에 따라 기준가에서 ± 됩니다.
  정확한 금액은 견적으로 안내드리며, 5톤보다 큰 차량도 문의 주세요.
```
🔴 **`START_PRICE_NOTE` 가 유일 정의처**이고 팝업·`/vehicles` 가 같이 읽는다 — 화면에 직접
적으면 두 곳이 조용히 갈린다(30차가 두 벌로 뒀다가 실제로 갈렸다).
🔴 **줄바꿈은 상수의 `\n` + 읽는 쪽 `<p>` 의 `white-space: pre-line` 두 개가 같이 있어야
성립한다.** **두 화면 다** 걸려 있고 한쪽만 빼면 그 화면만 한 문단으로 붙는다.
🔴 **필수 넷을 빼지 말 것** — ① 「10km 이내」(이 금액이 성립하는 조건) ② 「부가세는 별도」
③ 「견적으로 안내」 ④ 「5톤보다 큰 차량도 문의」. ④ 는 **6행으로 줄면서 더 중요해졌다**(위 ④).
⚠️ **혼적(합짐) 문장을 뺐다** — 「±」가 오르내림을 다 말하므로 필수 넷은 살아 있지만,
**내려가는 경로가 무엇인지는 화면에서 더 이상 말하지 않는다.** 실제로 내려가는 길은 혼적
할인 하나뿐이고 견적 자동계산이 `applyMixedDiscount()` 로 최종금액에서 차감한다
(`app/admin/quotes/page.tsx:585` 실측). 🔴 **되살리려면 둘째 줄에 붙일 것.**
⚠️ 사용자 초안은 2·3번째 줄에 「정확한 금액은 견적으로 안내드립니다」가 겹쳐 있었고 **3번째
줄에만 남겼다** — 견적 안내와 5톤 초과 문의가 둘 다 「그래서 어떻게 하나」에 대한 답이다.

**═══ ⑥ 🔴 「최저가」·「월별 이벤트」 금지가 풀렸다 — 30차 기준이 또 바뀌었다 ═══**
랜딩 WHY 05 를 시안 이미지 그대로 되돌렸다(사용자 지시 2026-09-01).
```
  제목  투명한 운임 산정 → **합리적인 운임가 및 월별 이벤트**
  본문  …투명하게 산정하고… → **…최저가 운임을 산정하고, 월별 이벤트를 통해 추가 혜택을…**
```
🔴 **30차가 이 두 낱말을 금지 표현으로 묶었던 근거는 그대로 유효하다** — 코드 주석에
남겼고 PR 에도 보고했다.
🟢 **2026-09-02 에 답이 왔다**(65차 ④) — **「최저가」는 빼고 「월별 이벤트」는 남긴다.**
랜딩 예외도 같이 제거했으므로 **지금은 공개 화면 어디에도 「최저가」가 없다.**
```
  「최저가」      최상급 표현 — **실증 자료**(비교 근거)가 없으면 표시광고법 제3조 부당표시
  「월별 이벤트」  실제로 운영돼야 하고 **적용 조건과 기간을 함께** 알려야 한다
```
🔴 **그래서 금지 표현 자동검증에서 「최저가」는 랜딩(`/`)만 예외로 갈랐다** — 다른 공개
화면에 새로 생기면 여전히 걸린다. **예외를 넓히지 말 것.**
🟢 **WHY 순서가 확정됐다** — 01 운임가 · 02 운송관리 시스템 · 03 정산 · 04 빠른 배차 ·
**05 보험**. 🔴 **보험이 맨 뒤인 데는 이유가 있다** — 미가입이면 그 카드가 배열에서 빠지는데
(61차 ④), **뒤에 있어야 남는 번호가 01~04 로 이어진다.** 가운데면 그때부터 번호가 비어 보인다.
⚠️ **61차 기록의 「WHY 02」·「WHY 03」 표기는 이제 낡았다** — 재배열로 번호가 밀렸다.

**═══ ⑦ 🔴 랜딩 푸터에서 호스팅사업자를 뺐다 — 법 요건이 지금 미충족이다 ═══**
운영시간(`평일 09:00 ~ 18:00`) · 개인정보 보호책임자 · **호스팅 제공 Vercel Inc.** 셋을 뺐다.
⚠️ **호스팅은 내가 한 번 「전자상거래법 제10조 1항이 초기화면 표시를 요구한다」며 남겼고,
사용자가 재확인해서 뺀 것이다**(2026-09-01). 🔴 **그래서 그 요건은 지금 충족되지 않는다.**
🟢 값이 사라진 것은 아니다 — `SiteFooter`(`/about`·`/vehicles`·법적 문서 3종)와 개인정보
처리방침 제5조 위탁 표·제6조 국외이전 표에 그대로 있다.
🔴 **되살리는 길은 둘이다** — (a) 랜딩 푸터에 다시 한 줄, (b) 법적 문서 링크 옆에
「사업자정보」. 사유를 코드 주석에 남겼다.
🟢 **2026-09-02 에 (b) 로 확정·구현됐다**(65차 ⑥) — `components/BusinessInfoModal.tsx`.
**요건이 이제 충족된다.** 🔴 호스팅사업자를 다시 빼지 말 것.
🟢 개인정보 보호책임자는 처리방침 제9조가 같은 값을 이미 고지하므로 푸터에서 빼도 고지
의무가 유지된다. 🟢 운영시간 값도 `/about`·화주포털·로그인 화면에 그대로 있다.

**═══ ⑧ 🔴 모바일 캐러셀 「덜덜거림」 — 원인이 둘이었다 ═══**
**(a) 터치에서 스크롤이 두 번 걸렸다.** 브라우저 자체 스크롤이 이미 도는데 마우스
드래그용 핸들러가 `scrollLeft` 를 같이 밀어 두 힘이 겹쳤다 → **`e.pointerType !== "mouse"`
면 걸지 않는다.** 🔴 **그 가드를 빼지 말 것.**
**(b) 카드 애니메이션이 매 스크롤마다 처음부터 다시 시작됐다.** 스크롤 이벤트는 초당
수십 번 오는데 그때마다 새 `transform` 을 써서 320ms transition 이 계속 재시작됐고 같은
값으로도 리렌더가 돌았다 → **rAF 로 프레임당 한 번만 그리고**, transition 을 없애 위치를
그대로 반영하고, state 는 값이 실제로 바뀔 때만 갱신한다.
🔴 **카드에 `transition` 을 다시 넣지 말 것** — 그것이 덜덜거림의 절반이었다.
🔴 **마우스로 끌어 넘기기는 원래부터 브라우저가 되돌린다 — 이번 변경 탓이 아니다.**
트랙의 `scroll-snap-type: x mandatory` 가 스냅 지점이 아닌 `scrollLeft` 대입을 **즉시
원위치시킨다**(실측: 스냅을 끄면 300, 켜면 0). 꺽쇠·아이콘 클릭은 `scrollTo` 라 정상이다.
고치려면 스냅 규칙을 바꿔야 해서 손대지 않았다.

**═══ ⑨ 모바일 차량 형태 — 한 줄 가로 스와이프 ═══**
12장을 2열로 쌓으면 6줄이 되어 화면을 통째로 먹고 있었다. ≤700px 에서 `display:flex` 한 줄
가로 스크롤로 바꿨고, **카드 폭 44%** 로 둬서 **세 번째 카드가 오른쪽 끝에 살짝 걸치게** 했다
— 🔴 **그 걸침이 "옆으로 넘길 수 있다"는 유일한 신호다.** 폭을 50% 이상으로 올리지 말 것.

**═══ ⑩ 🟢 검증 ═══**
```
  설명글        팝업·/vehicles 가 **문자 단위로 같고 실제로 3줄로 끊긴다**(개행 2개) ·
                computed white-space = pre-line · 필수 4가지 포함 · 옛 문구 5종 0건
  기준가        **6행** · 1톤 → 5톤 플러스/축 순서 · 8·11·15·18·25톤 **0건** ·
                「시작가」 0건 · 「차종·적재 용량」 0건 · 표 아래 ul/li 0개
  조회 실패      실제로 태웠다 — 표 0개 · 낡은 숫자 0건 · 안내 문구 · 복구하면 정상
  캐시          목을 40,000 → 41,000 으로 바꾸고 **서버 재시작 없이** 새 값 확인
  WHY           5장 순서·번호 01~05 · 푸터 운영시간·보호책임자·호스팅 0건 ·
                법정 표시사항 나머지 유지
  캐러셀        카드 transition 0s · 트랙 scroll-behavior ≠ smooth · 오버스크롤 contain ·
                **터치 pointer 는 트랙을 밀지 않는다** · TMS 카드 6장 회귀
  차량 형태      360·390·430px display:flex · **1줄** · 12장 전부 · 3번째가 화면에 걸침
  오버플로우     랜딩·/vehicles·팝업 × 1280·430·390·360px **전부 0** · 페이지 에러 0
  금지 표현      공개 6화면 0건(랜딩 「최저가」·약관 「기사 정보」는 예외로 갈랐다) ·
                「특수차량」은 전부 「…까지」
  npx tsc       통과 · 프리렌더 실패 **44개**(위 ③) · 보험 자리표시자 경고 그대로
  변경 없음      DB · rate_distance_tiers · globals.css · lib/legal/ · package*
```
⚠️ **거짓 ❌ 가 또 났고 셋 다 시험 자산·기존 예외였다**(27·59·60·61·62차와 같다) —
① **`/원\b/` 가 한글 뒤에서 안 걸린다**(`원` 이 `\w` 가 아니라 경계 판정이 뒤집힌다).
표는 실제로 6행이었다. ② 랜딩 「최저가」는 사용자 지시로 넣은 시안 문구다(위 ⑥).
③ 약관 「기사 정보」는 제2·8조가 배차 확정을 정의하는 말로 **34차가 정한 예외**이고
`origin/main` 에도 있다. 🔴 **"❌ 가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 말 것.**

**═══ ⑪ ⚠️ 겪은 함정 ═══**
🔴 **`git checkout -b` 가 이미 있는 브랜치라 실패했는데 그대로 진행해서 첫 커밋이 로컬
`main` 에 얹혔다.** 그 상태로 `git push -u origin <branch>` 를 했더니 **낡은 PR #110
브랜치가 올라갔다.** `git branch -f` 로 브랜치를 커밋에 맞추고 `main` 을 `origin/main` 으로
되돌린 뒤, **원격 브랜치 내용이 이미 main 에 들어가 있는 것을 트리 비교로 확인하고**
`--force-with-lease` 로 정리했다. 🔴 **브랜치를 만들 때 종료코드를 볼 것.**
⚠️ `PUBLISHED_START_PRICE_TONS.includes` 의 타입 술어가 TS2677 을 냈다 —
`as readonly string[]` 로 캐스팅해서 해소.
⚠️ **PR 본문이 커밋 1 시점 내용이라 세 군데가 사실과 달랐다**(11행 · 「기준가 48,000원」 ·
「호스팅은 남겼습니다」). merge 전에 **세 커밋 기준으로 본문을 다시 썼다** — 뒤 커밋이 앞
커밋을 덮는 PR 에서는 본문을 마지막에 손볼 것.

**═══ ⑫ ⚠️ 답을 기다리는 것 3건 · 다음 차수 ═══**
🔴 **다음 지시서는 그대로 32차(내부 정합 ① 견적·오더)다** — 이번 차수는 번호를 쓰지 않았다.
```
  🟢 ① 운임 인하가 의도인지 — **의도로 확정**(2026-09-02, 65차 ①)
  🟢 ② 「최저가」·「월별 이벤트」 — **최저가는 빼고 이벤트는 유지**로 확정(65차 ④)
  🟢 ③-2 푸터 호스팅 — **「사업자정보」 모달로 해소**(65차 ⑥)
  ⚠️ ③-1 혼적(합짐) 문장을 되살릴지 — **그대로 대기 중**
```
⚠️ **사용자가 코드 밖에서 할 일** — 🟢 **보험 증권은 2026-09-01 에 받았다**(65차 ⑦) ·
🔴 카카오톡 OG 캐시 초기화(63차 ⑩) · ⚠️ 원격 임시 브랜치 정리(`claude/*`, 이 환경은 삭제가
막힌다) · ⚠️ 검색엔진 등록(급하지 않다).
⚠️ **답을 못 받은 것 2건**(그대로다) — 자주 쓰는 화물 목록 이름 표시(26차부터) · 무료 대기
`"초과 시 30분당"` 단위(25차부터).

**작성일: 2026-07-23** (최종 갱신: 2026-09-01, Claude Code 세션에서 직접 갱신 — 63차
세션: "도메인 확정 반영" — 🔴 **차수를 쓰지 않았다**(41·47·59차와 같은 처리).
신규 파일 **3개**(`lib/pageMetadata.ts` · `app/sitemap.ts` ·
`app/customer/login/layout.tsx`) + 이미지 1장. 커밋 3개.
🟢 **DB 변경 0** · 🟢 **신규 의존성 0** · 🟢 **화면 변경 0**(메타데이터만) ·
🟢 **`globals.css` 0** · 🟢 **`lib/legal/` 0** · 🟢 **관리자 회귀 0**.

**═══ ① 🔴 도메인이 확정됐다 — `www` 없는 쪽이 메인이다 ═══**
```
  wecarrylogis.co.kr        Production · 🔴 **메인**
  www.wecarrylogis.co.kr    308 → wecarrylogis.co.kr
  b2b-cargo-system.vercel.app   그대로 살아 있다 (죽이지 말 것 — 도메인 장애 시 들어갈 길)
```
🔴 **메인이 `www` 없는 짧은 쪽인 것은 사용자 확정이다**(2026-09-01) — 명함·문자·전화
안내에 쓰기 위해서다. Vercel 기본값은 반대였고 그것을 뒤집었다. **되돌리지 말 것** —
`www` 를 붙이면 canonical 과 308 리다이렉트가 서로 싸운다.
⚠️ 네임서버는 가비아에 두고 DNS 레코드만 걸었다 — A `216.198.79.1` /
CNAME `6ddf062e71f2c5be.vercel-dns-017.com.`

**═══ ② 🔴 주소 정의처는 `lib/siteUrl.ts` 하나다 — 환경변수로 빼지 않았다 ═══**
⚠️ **예전에는 `VERCEL_PROJECT_PRODUCTION_URL` 을 읽었다**(23차). 그 파일 주석이
*"커스텀 도메인이 붙으면 자동으로 따라온다 — 코드 수정 불필요"* 라고 적어뒀는데,
🔴 **그 값이 실제로 무엇인지 배포 바깥에서는 확인할 수 없다.** 추측 위에 문자 링크가
얹히는 구조라 상수로 못박았다.
🔴 **환경변수로 빼지 않은 이유** — Vercel 에서 Production 만 체크하고 Preview 를
빠뜨리는 함정(7절)을 새로 만들지 않기 위해서다. **주소가 또 바뀌면 이 한 줄만 고친다.**

**═══ ③ 🔴 `vercel.app` 하드코딩은 0건이었다 — 무엇을 찾아봤는지 남긴다 ═══**
지시서가 「가장 위험한 항목」으로 꼽은 3-5 의 전제가 실제와 달랐다. **다음 세션이 같은
조사를 반복하지 않게** 찾아본 곳을 적어둔다.
```
  lib/sms/templates.ts     URL 문자열 0건 — `params.portalUrl` 을 받아 쓴다
  견적서 print 2종 · 엑셀   0건 (`origin` 은 상차지 컬럼명이라 오탐이다)
  이메일 템플릿 2종         0건 (URL 은 Resend API 주소뿐)
  API·리다이렉트            `lib/siteUrl.ts` 한 곳만 주소를 만든다
  lib/legal/               0건 → 🟢 **법적 문서를 손댈 일이 없었다**
  저장소 전역 grep          코드 0건 · `CLAUDE.md` 뿐
```
🔴 **SMS 링크는 지시서가 적은 4종이 아니라 계정 관련 3종이다.**
```
  링크 있음   승인+계정발급 318 · 계정발급 245 · 비밀번호 재발급 259 byte
  링크 없음   배차확정 · 상차완료 · 하차완료 · 견적안내 · 등록반려
```
🟢 실제로 실행해서 쟀다 — **셋 다 이미 LMS 였고 9byte 씩 짧아졌다**(`b2b-cargo-system
.vercel.app` 27자 → `wecarrylogis.co.kr` 18자). SMS↔LMS 경계 변화 없음.

**═══ ④ 🔴 OG 정의처가 `<title>`·description 과 같다 — 두 벌로 나누지 말 것 ═══**
`lib/pageMetadata.ts` 의 `buildPageMetadata({ title, description, path })` 가 **하나의
값**을 `<title>` · description · `og:title` · `og:description` · `twitter:*` · canonical
에 함께 넣는다. 화면은 셋만 넘긴다.
🔴 **`og:` 쪽에 문구를 따로 적지 말 것** — 두 벌이 되면 조용히 갈린다(59차
`lib/arrivalType.ts` 와 같은 판단).
🔴 **금지 표현 기준이 OG description 에도 그대로 적용된다** — 검색결과·카카오톡에
그대로 노출되는 문장이다(12·13차가 메타를 두 번 연속 놓쳤고 30차에 또 걸렸다).

**═══ ⑤ 🔴 `og:image` 는 반드시 절대 URL 이다 ═══**
상대경로(`/og.jpg`)로 두면 **카카오톡이 이미지를 못 읽는다.** 루트 layout 의
`metadataBase` 가 상대경로를 절대화하지만, `OG_IMAGE_URL` 은 **처음부터 절대 URL**로
만들어 둔다(자동 절대화에 기대지 않는다).
🟢 실측 — `https://wecarrylogis.co.kr/og-wecarry-1200x630.jpg` 가 **200 · `image/jpeg` ·
1200×630 · 125,731 byte**. 파일은 사용자가 `public/` 에 직접 올렸다(039f777).
⚠️ **이 이미지는 `lib/landingImages.ts` 에 넣지 않았다** — 화면에 그리는 것이 아니라
메타데이터 전용이라 성격이 다르다. 정의처는 `lib/siteUrl.ts` 다.

**═══ ⑥ 🔴 `/admin`·`/customer` 는 OG 를 **지워야** 한다 ═══**
Next 는 metadata 를 **병합**하므로, 루트에 OG 를 넣으면 하위가 그대로 물려받는다.
그대로 두면 내부 주소를 카톡·슬랙에 붙였을 때 **공개 사이트 미리보기가 뜬다.**
그래서 두 layout 에 `openGraph: null` · `twitter: null` 을 넣었다.
🔴 **noindex 와 OG 는 별개 축이다** — 검색 색인 차단과 링크 미리보기는 서로 무관하다.
둘 다 유지할 것.
🔴 **`/customer/login` 만 예외로 OG 를 넣었다** — 랜딩에서 넘어오는 공개 진입 화면이라
미리보기가 필요하다. `page.tsx` 가 클라이언트 컴포넌트라 얇은 서버 레이아웃을 하나
뒀다(원칙 10번). **noindex 는 부모에서 물려받아 그대로다 — 여기서 풀지 말 것.**
⚠️ **완료조건 6(로그인에 OG)과 7(`/customer` 에 OG 없음)이 서로 부딪혔고**, 위 근거로
갈랐다. 그래서 `app/admin/layout.tsx`·`app/customer/layout.tsx` 가 각 5~6줄 바뀌었다
(「관리자 변경 0」과 어긋나 보이지만 **metadata 뿐이고 렌더링은 그대로**다 — 아래 ⑧).

**═══ ⑦ 🔴 sitemap 은 라우트에서 자동으로 뽑는다 — 손으로 적지 말 것 ═══**
`app/sitemap.ts` 가 `app/` 아래를 훑어 `page.tsx` 가 있는 폴더를 모은다. 손으로 적으면
화면이 늘 때마다 어긋나고, `/status` 처럼 **지워진 라우트가 목록에 남는다.**
```
  잘라내는 것   PRIVATE = admin · customer · api
                라우트 그룹 `(...)` · 동적 세그먼트 `[...]` · private 폴더 `_...`
  실측          8 URL · /admin·/customer·/api·/status **0건** · www 붙은 loc **0건**
                8개 전부 200
```
⚠️ **`/customer/login` 은 sitemap 에 없다** — noindex 라 넣지 않는 것이 맞다(OG 는 있다).
🟢 `robots.ts` 의 `sitemap` 필드에 절대 URL 을 넣었다. Allow·Disallow 목록은 그대로다.

**═══ ⑧ 🟢 검증 ═══**
```
  OG 6종          공개 9화면 전부 6/6 · twitter:card summary_large_image 9/9
  og:image        절대 URL · 200 · image/jpeg · 1200×630 (실제로 내려받아 파싱)
  비공개          /admin/login · /customer/dispatches · /customer/invoices —
                  og 0건 · noindex 유지
  canonical       9화면 전부 www 없는 절대 URL (어긋남 0)
  og:title        `<title>` 과 문자 단위 동일
  금지 표현        OG description 9개 — 0건 · 「특수차량」은 「…까지」 형태
  robots.txt      Sitemap: https://wecarrylogis.co.kr/sitemap.xml
  sitemap.xml     8 URL · 비공개 0건 · 전부 200
  SMS 8종         실제 실행해서 바이트 측정 · URL 있는 것 3종 · 전부 LMS 유지
  화면 변경 0      공개 9화면의 **보이는 DOM** 을 origin/main 과 md5 대조 → 불일치 0
  관리자 회귀 0    9화면 × 12지표를 origin/main 을 같은 목으로 띄워 대조 → 불일치 0
  npx tsc         통과 · 프리렌더 실패 45개가 기준선과 항목 단위 동일
  변경 없음        page.tsx · globals.css · lib/legal/ · package* · DB
```
⚠️ **거짓 ❌ 를 또 밟을 뻔했다** — 화면 동일성 비교가 **9/9 실패**로 나왔는데 차이가
전부 **빌드 ID·청크 해시와 RSC 에 직렬화된 `<head>` 메타**였다. script·head 를 걷어내고
다시 재니 0건이다. 🔴 **"❌ 가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 말 것.**
⚠️ **이 환경은 프록시가 새 도메인도 403 으로 막는다** — `wecarrylogis.co.kr` 도
`vercel.app` 과 똑같이 못 닿는다(29·30·43차와 같다). **로컬 프로덕션 빌드로 쟀다.**
⚠️ **옛 `next-server` 가 포트를 잡아 낡은 빌드를 잴 뻔했다 — 다섯 번째다.** 이번엔
`og:image` 가 404 로 나와서 알아챘다. 🔴 **죽이고 → 포트가 빈 것을 확인하고 → 새로
띄울 것.** `npm exec next s` 로 뜬 래퍼까지 같이 죽여야 한다.

**═══ ⑧-2 🔴 Preview 로는 링크 미리보기를 확인할 수 없다 — 내가 틀리게 안내했다 ═══**
사용자가 merge 전에 **Preview 링크를 카카오톡에 보내** 확인했는데 미리보기가
**「Protected Deployment – Vercel / 여기를 눌러 링크를 확인하세요」** 로 떴다.
🔴 **우리 페이지를 못 읽은 것이 아니라 Vercel 보호 화면을 읽은 것이다.** Preview 배포는
Vercel 이 로그인 벽으로 막아두고, 카톡 크롤러는 로그인이 안 되니 그 벽 앞에서 멈춰
**Vercel 자체 문구**를 미리보기로 가져간다. `og:` 태그는 읽히지도 않았다.
🔴 **그래서 「Preview 링크를 카톡에 보내 확인해 주세요」는 원리상 성립하지 않는 지시다** —
내가 PR 확인 목록에 그렇게 적었고 틀렸다. **확인은 merge 후 실제 도메인으로만 된다.**
🟢 **Preview 에서 확인하려면 브라우저로 직접 열고 페이지 소스를 보면 된다**(사람은
로그인돼 있어서 벽을 통과한다). 크롤러만 못 지나간다.
⚠️ Vercel 설정에서 Preview 보호를 끄면 카톡도 읽지만 **공개 전 사이트가 통째로 열린다** —
권하지 않았다.

**═══ ⑨ ⚠️ 보고만 하고 안 고친 것 ═══**
🟢 **65차에 정정했다**(그 파일을 열었을 때 같이 고쳤다).
🔴 **`app/page.tsx` 의 주석 2줄이 낡았다** — `/status` 가 「`/quote`·`/apply` 헤더 칩과
두 접수완료 화면에 있다」고 적혀 있는데 **62차에 통째로 지워졌다.** 지시서 금지
10·14번(화면 파일 손대기 · 다른 부분 정리)에 걸려 손대지 않았다. **다음에 그 파일을
열 때 같이 고칠 것.**

**═══ ⑩ ⚠️ 사용자가 코드 밖에서 할 일 ═══**
🔴 **카카오톡 캐시 초기화** — 반영 후 **반드시**. 카톡은 한 번 읽은 주소의 미리보기를
며칠씩 캐시해서, 코드를 고쳐도 다시 보내면 옛 화면이 그대로 나온다(「안 고쳐졌다」로
오해하기 딱 좋다). `https://developers.kakao.com/tool/clear/og` 에 주소를 넣고 초기화할 것
(카카오 계정 로그인 필요). ⚠️ 페이스북·슬랙도 캐시하지만 대개 더 짧다.
⚠️ **검색엔진 등록은 급하지 않다** — 구글 서치콘솔 · 네이버 서치어드바이저에 소유확인
TXT 를 가비아 DNS 에 넣고 사이트맵을 제출하면 된다. 공개 후 며칠 안에 해도 된다.
🟢 **→ 네이버는 2026-09-04 에 끝났다**(사이트 등록 · 소유확인 · 사이트맵 제출 · 수집요청).
🔴 **소유확인은 DNS TXT 가 아니라 `public/` 의 HTML 파일 방식이었다** — 위 「차수 없음:
네이버 서치어드바이저 등록」 참고. **구글 서치콘솔은 아직이다.**
🟢 **보험 증권은 2026-09-01 에 받았고 65차 ⑦ 에서 상수를 채웠다** — 이 항목은 끝났다.
🔴 **접수 안내 문자를 만들지 여부** — `/status` 가 없어져 비회원이 접수 결과를 확인할
방법이 지금 없다.
⚠️ **원격 임시 브랜치 정리**(`claude/*`) — 이 환경은 브랜치 삭제가 막힌다(58차 ⑪c).

**═══ ⑪ ⚠️ 다음 차수 ═══**
🔴 **차수를 쓰지 않았다 — 다음 지시서는 그대로 32차(내부 정합 ① 견적·오더)다.**

**작성일: 2026-07-23** (최종 갱신: 2026-09-01, Claude Code 세션에서 직접 갱신 — 62차
세션: "폼 3화면 교체(31차)" — 디자인팀 Next 변환본으로 `/quote` · `/apply` ·
`/customer/login` 세 화면을 통째로 교체. 신규 파일 **3개**
(`components/landing/form/Fields.tsx` · `components/landing/SubmitDone.tsx` ·
그 폴더). 커밋 17개(본작업 7 + **실사용 리뷰 3라운드 10**).
**PR #109 merge됨**(squash `7d88930`). 🟢 **DB 변경 0** · 🟢 **신규 의존성 0** ·
🟢 **`globals.css` 변경 0** · 🟢 **관리자 파일 변경 0개** ·
🟢 **랜딩은 리뷰 3라운드에 여백만 조정**(구조 변경 0).
🔴 **리뷰에서 내 판단 둘이 뒤집혔다** — `/status` 존치와 현장 3항목 미도입.
아래 ⑬ 을 ①·⑨·⑫ 보다 **먼저** 읽을 것.

**═══ ① 🔴 이 차수의 핵심은 겉모습이 아니라 `/apply` 동의 기록이다 ═══**
셋이 **한 벌로** 움직인다(18차·49차) — 하나만 건드려도 나머지가 조용히 무너진다.
```
  ① 화면 체크박스 **2개**        개인정보보호법 제22조 1항 — 항목별로 구분해 받는다
  ② 서버 게이트 **각각**         `termsAgreed !== true` · `agreed !== true` (원칙 25번)
  ③ `recordConsents` **2행**     privacy + terms · 실패하면 신청서를 지운다(롤백)
```
🔄 **리뷰 3라운드에 네 번째 조각이 붙었다** — 현장 담당자 연락처를 적으면 `third_party`
동의가 **조건부로** 활성화되어 **3행**이 된다(아래 ⑬-3). 안 적으면 그대로 2행이다.
🔴 **동의 블록을 화면에 새로 그리지 말 것.** 시안은 동의 카드를 화면 코드에 직접
그렸고 **약관 초안까지 통째로 들고 있었다.** 그대로 옮기면 「체크박스 2개」·「문구는
상수」 규칙이 화면마다 갈린다 — `PublicConsentFields` 에 **`variant="landing"`** 을
더해 **모양만** 분기했다. 새 화면이 또 생겨도 그 파일에 variant 를 더할 것.
🔴 **「전문 보기」는 30차 `LegalLinks` 모달이다** — 조문은 `lib/legal/` 하나만 본다.
그래서 `LegalLinks` 에 **`only` · `label`** prop 을 더했다(문서 정의·모달은 그대로 공유).
🟢 **왕복 시험 통과** — 제출 → `/api/apply-submit` → `customer_applications` 1행 +
`consents` **2행**(privacy/privacy-v1 · terms/terms-v1)이 **그 신청서 id 를 가리키는
것**까지 확인했다. **고아 0.** 14차가 *"행이 생겼다만 보지 말고 join 까지 볼 것"* 이라
못박은 지점이다.
🟢 **🔴 롤백 경로를 처음으로 실제로 태웠다 — 43차 이후 계속 「미검증」이던 것이다.**
목에 강제 실패 스위치를 넣어 `consents` insert 를 400 으로 만들자 **400 응답 + 방금
만든 신청서가 삭제**됐다(before 0 / after 0). ⚠️ 정상 흐름에서는 안 타는 길이라
재현이 안 됐던 것이고, 이제 재현 방법이 있다(아래 ⑦).

**═══ ② 🔴 DB 컬럼이 없는 값은 특이사항(`notes`)으로 잇는다 ═══**
`public_quote_requests` 에는 **물품특성 · 왕복편도 · 대기시간 · 경유지수 ·
희망 하차 일시 · 운송시간** 컬럼이 **없다**(코드 전수 확인). 31차는 **DB 변경 0이
조건**이라, 27차가 「당착/내착」을 특이사항 한 줄로 이은 것과 **같은 방식**을 썼다.
```
  ※ 상세 정보
  · 물품특성: 파손주의
  · 왕복/편도: 편도
  · 희망 하차 일시: 2026-09-08 14:00
```
🟢 **이 값들은 공개문의 상세와 견적 전환 프리필까지 그대로 따라간다**
(`app/admin/quotes/page.tsx` 가 `notes: reqData.notes` 로 받는다).
⚠️ **컬럼을 만들자는 제안이 나오면** 관리자 목록·상세·프리필까지 함께 봐야 한다.

**═══ ③ 🔴 공개 폼 톤수가 6종 → 11종이 됐다 ═══**
30차가 *"곧 지워질 화면만 바꾸는 셈"* 이라며 미뤄두고 **「31차가 폼을 그릴 때 함께
정할 것」** 이라고 주석에 남긴 그 결정이다.
```
  46차   "25톤" 이 금지 표현 → 공개 4개 화면을 6종으로 묶음
  30차   사용자가 취급 범위를 시안 문구로 확정 → "25톤" 이 금지에서 풀림
  31차   6종으로 두면 18톤이 필요한 화주가 견적 문의로 못 넣고 전화를 해야 한다
```
🔴 **그래도 `VEHICLE_TYPES_ALL` 과 합치지 말 것.** 값이 같은 것은 우연이고, 이 배열은
「**공개 화면에 무엇을 노출할지**」라는 다른 질문의 답이다. 합치면 다음에 관리자 전용
차급을 `ALL` 에 넣는 순간 공개 폼에도 새어 나간다 — 이름이 갈라져 있어야 `tsc` 가
사용처를 드러낸다(46차가 나눈 이유).
🟢 **46차가 배열을 나눈 직접적인 이유였던 `/apply` 의 `join(", ")` 툴팁은 없어졌다** —
21차의 hover 「!」 툴팁을 캡션으로 바꿨다(톤수 목록은 드롭다운을 열면 보이므로 잃는
정보가 없고, 터치에는 hover 가 없다). 🔴 **그 툴팁을 다시 만들지 말 것.**

**═══ ④ 🔴 「특수차량」 금지가 풀렸다 — 61차 기록이 자기모순이었다 ═══**
30차 리뷰 2라운드에 사용자가 시안 문구로 통일하라고 해서 풀린 것인데, **61차 ② 가
같은 기록 안에서 「여전히 금지」로 적어두어 앞뒤가 어긋나 있었다.** 31차에 정정했다.
🔴 **허용 범위는 시안 확정 문구 안뿐이다** — 「1톤부터 5톤 이상, **특수차량까지**」와
WHY 02 의 「1~5톤 기본 차량에 **특수차량까지**」. 🔴 **홀로 쓴 「특수차량」과
「전 차종」·「모든 차량」은 여전히 금지다.** 🟢 검증이 공개 10개 화면에서 **「특수차량」이
전부 「…까지」 형태**임을 실측했다(홀로 쓴 것 0건).
⚠️ **차량형태는 21종이 아니라 22종이다**(`받침` 포함) — 옛 기록 세 곳을 함께 고쳤다.

**═══ ⑤ 🔴 입력창 글자를 16px 로 올렸다 — 시안의 15px 을 그대로 쓰지 말 것 ═══**
iOS Safari 는 **16px 미만 입력창에 포커스하면 화면을 자동 확대**하고, 그러면 뷰포트가
페이지보다 좁아져 **좌우로 밀리는 것처럼 보인다** — 26차에 사용자가 「커지고 밀린다」로
신고한 것이 이것이다.
🔴 **전역 `.public-form` 규칙(16px)이 있는데도 안 먹는다** — 시안이 인라인 `style` 로
15px 을 주고 **인라인이 CSS 를 이기기 때문**이다. 그래서 `fieldStyle`(Fields.tsx)과
로그인 `inputStyle` 의 **값 자체**를 16으로 올렸다. 🟢 세 화면 최상위에 `public-form`
클래스도 함께 붙였다(`/quote` 는 처음에 빠져 있었다).
⚠️ **검증이 이걸 잡아냈다** — 「login 입력창 16px → 15px」 ❌ 한 줄이 세 화면 공통
문제의 실마리였다.

**═══ ⑥ 🔴 공유 컴포넌트는 고치지 않고 prop 을 더했다 ═══**
```
  AddressSearch      한 줄도 안 고쳤다 — `className="landing-addr"` 를 넘겨 모양만 바꿨다
  MultiSelectTags    `variant` 추가. 🔴 저장 형식("서울, 경기" 콤마)은 그대로다
  PublicConsentFields `variant="landing"` 추가 (위 ①)
  LegalLinks         `only` · `label` 추가 (위 ①)
  BrandLogo          `style?` 추가 · PasswordInput `inputStyle?` 추가
```
🔴 **`AddressSearch` 원본을 고치면 관리자 6개 화면이 같이 바뀐다**(원칙 37번). 라벨은
화면이 직접 그리므로(출발지 줄에 「⇄ 바꾸기」 버튼이 함께 있다) **컴포넌트가 그리는 빈
라벨은 `label:empty` 로 숨긴다.**
🔴 **`MultiSelectTags` 는 한 번 잘못 고쳤다가 되돌렸다** — `whiteSpace`·`fontFamily` 를
공통 style 에 넣었는데 이 컴포넌트를 **관리자 4개 화면**(화주 관리 2 · 차주 관리 2)이
같이 쓴다. 두 속성을 landing 팔레트로 옮겨 **portal 쪽 렌더링 값을 31차 이전과 한 글자도
다르지 않게** 되돌렸다(별도 보정 커밋). ⚠️ **공유 컴포넌트에 variant 를 더할 때는 기존
쪽 렌더링 값이 안 바뀌는지부터 볼 것.**

**═══ ⑦ 🟢 검증 도구가 늘었다 — 다음 차수가 그대로 쓸 것 ═══**
```
  목 POST 에 OBJECT_STRICT   Accept: vnd.pgrst.object+json 이면 **단일 객체**를 준다
                             🔴 안 그러면 `.single()` 뒤 `inserted.id` 가 undefined 가 되어
                             `consents.subject_id` 가 안 붙은 것을 검증이 못 잡는다(원칙 56번)
  목 FAIL_SWITCH             `/__fail?table=consents` 로 그 표의 POST 를 400 으로 만든다
                             → 롤백 경로 시험(위 ①). `/__fail?table=` 로 끈다
```
⚠️ **거짓 ❌ 가 또 시험 자산 결함이었다**(27·59·60·61차와 같다) — ① 동의 체크박스를
`.check()` 로 못 눌렀다(체크마크 SVG 가 입력을 가린다 — 🟢 **이건 진짜 결함이라
`pointer-events:none` 으로 고쳤다**, 시안도 같은 상태였다) ② 스티키 헤더가 클릭을
가로챘다(요소를 화면 **가운데로** 스크롤한 뒤 누르면 된다) ③ 금지 표현 검사가
**법적 문서 3종**에 걸렸다(「기사 정보」는 약관 제2·8조가 배차 확정을 정의하는 말이고
「이벤트」는 처리방침의 마케팅 동의 항목이다 — 34차가 정한 예외 범위다).
🔴 **`origin/main` 과 나란히 재서 세 건 다 그쪽에도 똑같이 있는 것을 확인한 뒤** 시험
자산을 고쳤다. **"❌ 가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 말 것.**

**═══ ⑧ 🟢 검증 ═══**
```
  왕복             /apply 제출 → consents 가 그 신청서 id · 고아 0
                   🔄 리뷰 3라운드 후 — 현장 연락처 **있으면 3행** / **없으면 2행**
  롤백             consents 강제 실패 → 400 + 신청서 삭제 (before/after 동일)
  서버 게이트       필수 미입력 / 동의 미체크 / 약관만 체크 / 문자열 `"true"` /
                   🔄 조건부 동의 미체크 — 전부 막힘
  동의 문구         약관 라벨 · 요약 · 거부권 · 개인정보 문구 전부 상수에서 렌더링
  상세 접이식       접힘 상태에 해당 문구 0 · 펼치면 4블록 · aria-expanded 토글
  선택지            /quote 톤수 **11종** · /apply 주 이용 차량 **11종** · 25톤 선택지 있음
                   /apply 본문에 톤수 join 노출 **0건**
  원칙 57번         네이티브 select **0** · input[type=date] **0** (세 화면)
  금지 표현         공개 10개 화면 렌더링 텍스트 — 「전 차종」·「모든 차량」·「최저가」·
                   「이벤트」·「기사 정보」·「예비 배차처」·「계약 없이」 **0건**
                   「특수차량」은 전부 「…까지」 형태(홀로 쓴 것 0)
  가로 오버플로우    🔄 랜딩 포함 **4화면** × 1280·430·390·360px **전부 0**
  입력창            16px (iOS 자동확대 방지)
  관리자 회귀        🔴 **9화면 × 12지표를 `origin/main` 과 같은 목으로 나란히 띄워 대조 →
                   불일치 0** · 오버플로우 9화면 전부 0
  npx tsc --noEmit  통과 · package* 변경 0
  npm run build     본작업 때는 **48개가 `origin/main` 과 항목 단위로 동일**했고,
                   🔄 리뷰 3라운드에 `/status` 3개를 지워 **45개**가 됐다 —
                   🔴 **줄어든 3건이 지운 라우트와 정확히 일치하고 신규 0**
  변경 범위         globals.css **0** · 랜딩 **0** · app/customer 는 login 하나만
```

**═══ ⑨ ⚠️ 지시서와 다르게 판단한 것 ═══**
**(a) 회신 시간 문구를 시안대로 쓰지 않았다** — 시안 「평일 기준 평균 30분 이내」인데
랜딩 FAQ 는 「평일 업무 시간에 접수된 건은 당일 안에 회신」이다. **두 화면이 다른 시간을
약속하면 안 되므로** FAQ 문구로 맞췄다. 로그인·`/apply` 의 「2~3시간 이내 발급」도 같은
이유로 「{운영시간} 접수 기준 당일 발급」으로 바꿨다.
**(b) 무료 대기시간 안내는 「20분」이다**(시안 30분) — 25차에 30 → 20분으로 바뀐
**가격 변경**이다.
**(c) 접수번호를 만들지 않았다** — 우리는 발급하지 않고 `/status` 조회는 **전화번호**로
한다. 쓸 수 없는 번호를 보여주면 그걸로 조회하려다 막힌다. 대신 `SubmitDone` 이
**조회 경로를 안내**한다.
**(d) 🔄 세 화면에 `/status` 링크를 남겼다 — 리뷰에서 뒤집혔다.** 본작업 때는 원칙
42번(정보 손실 금지)을 근거로 남겼으나, **1라운드에 진입 경로를 전부 빼고 3라운드에
라우트·API·법적 문서 행까지 지웠다**(아래 ⑬-2). 🔴 **이 문단을 근거로 되살리지 말 것.**
**(e) 접수완료 팝업에 계정 신청 유도 카드를 넣었다**(`/quote`) — 옛 화면에 있던 안내다.
**(f) `format.ts` 를 만들지 않았다** — 시안이 `formatPhone`/`formatBiz` 를 새로 들고
왔는데 우리에겐 `formatPhoneNumber`·`formatBizRegNo` 가 이미 있다(원칙 35번).
**(g) 시간 라벨은 `HH:mm` 이다** — 시안은 「오전 06:00」이지만 관리자 화면이 전부
`HH:mm` 이라 여기만 바꾸면 같은 건이 화면마다 다르게 보인다(26차와 같은 판단).

**═══ ⑩ 🔴 보고만 하고 안 고친 것 — 사용자 판단이 필요하다 ═══**
🟢 **이 항목은 2026-09-08 에 「두 화면을 통째로 삭제」로 닫혔다**(PR #132) — 진입 경로를
새로 만들지 말 것. 아래는 그 결정에 이른 배경이라 그대로 둔다.
🔴 **랜딩에서 `/about` · `/vehicles` 로 갈 경로가 하나도 없다.**
30차 리뷰 ④(*"푸터, CTA 하단에는 법적 문서 3종 모달만 빼고 모두 삭제"*)로 그 링크들이
지워졌는데, **시안 헤더에도 그 메뉴가 없다.** 그래서 지금 두 화면은 **주소를 직접 치거나
그 화면 자신의 `SiteFooter` 로만** 닿는다(랜딩·폼 3화면은 `LandingFooter` 를 쓰고 거기엔
링크가 없다).
⚠️ **되살리면 사용자 본인의 리뷰 ④ 결정을 뒤집는 것**이라 손대지 않았다 — 랜딩 푸터에
두 줄을 되살릴지, 헤더에 넣을지, 그대로 둘지가 결정 사항이다.
🟢 **`SiteFooter` 에는 `/about`·`/vehicles`·`/status` 가 이미 다 있다** — 고칠 것이 없어
「푸터 링크」 커밋은 만들지 않았다.

**═══ ⑪ 신규 파일 3개 ═══**
```
  components/landing/form/Fields.tsx   폼 공용 부품 — 커스텀 드롭다운(role=listbox·
                                       ↑↓·Enter·Esc·Home·End) · 달력 · 시간 슬롯 ·
                                       스타일 토큰(`fieldStyle` 16px · `cardStyle` …)
  components/landing/SubmitDone.tsx    접수 완료 팝업 (견적문의 · 계정신청 공용)
                                       🔴 접수번호 없음 · `/status` 안내 · `extra` 슬롯
  (components/landing/form/ 폴더)
```

**═══ ⑫ ⚠️ 다음 차수가 알아야 할 것 ═══**
🔴 **다음은 32차(내부 정합 ① 견적·오더)다.** 전제(월정산 (A)안)는 58차에 풀렸다.
```
  32차   내부 정합 ① 견적·오더 — P1-3(견적→오더 5필드) · P2-3
  33차   내부 정합 ② 배차·정산 — P2-1
  34차   내부 정합 ③ 화주·차주 — P1-4 · P2-1 나머지
  미정   ~~담당자 정보(`customer/profile`)~~ **완료(PR #121)** · admin 공개문의 배지 · 보험 문구 정합 ·
         접수 안내 문자 신설 · ~~랜딩→`/about`·`/vehicles` 진입 경로(위 ⑩)~~
         → 🟢 **두 화면 삭제로 닫혔다**(PR #132)
```
🟢 **admin 공개문의 배지에 딸려 있던 「랜딩 「문의·신청 현황 조회」 삭제」는 끝났다**
(55차 ⑮ 가 한 차수로 묶자고 한 것 중 절반) — 이제 그 항목은 **배지 하나만 남았다.**
🔴 **32~34차는 파일 단위로 안 겹친다** — `dispatches/[id]`(2520줄)와 `quotes`(1821줄)를
같은 차수에 넣지 말 것.
⚠️ **사용자가 코드 밖에서 할 일**
```
  🟢 보험 증권 — **2026-09-01 수령, 65차 ⑦ 에서 상수 반영**. 끝난 항목이다
  🔴 원격 임시 브랜치 정리(`claude/*`) — 이 환경은 브랜치 삭제가 막힌다(58차 ⑪c)
  🟢 위 ⑩ — **두 화면을 삭제하기로 확정해 닫혔다**(2026-09-08, PR #132)
```
⚠️ **답을 못 받은 것 2건**(그대로다) — ① 자주 쓰는 화물 목록 이름 표시(26차부터)
② 무료 대기 `"초과 시 30분당"` 단위(25차부터).
🔄 **처리방침 제2조 건은 리뷰 3라운드에 해소됐다** — 본작업 때는 방침에 그 항목이
없어서 세 칸을 넣지 않았는데, 사용자가 「선택 항목이라 안 넣어도 되나」 물어와서
**선택 항목이어도 넣어야 한다**고 답하고 방침과 화면을 함께 고쳤다(아래 ⑬-3).
🔴 **「31차는 `lib/legal/` 금지」를 근거로 되돌리지 말 것** — 사용자 지시로 풀렸다.

**═══ ⑬ 🔴 PR #109 실사용 리뷰 3라운드 — 내 판단 둘이 뒤집혔다 ═══**
사용자가 Preview 를 눌러 보고 지적한 것이다(2026-09-01). 커밋 10개로 반영했다.

**1라운드 — 지적 13건 + 질문 3건**
```
  ①  /status 진입 경로     🔴 **모든 화면에서 없앨 것 · 내부시스템 설정도**
  ②  모바일 헤더           버튼 두 개가 겹치고 로고가 너무 작다
  ③④ 접이식·표시          🟢 정상 — 코드 변경 0
  ⑤  회사소개·차량요금      🟢 **회사소개는 아예 빼기로 확정** · 차량요금은 그대로
      현장 3항목           🔴 **시안대로 선택 항목으로 넣을 것**
  /quote                  항목칸 컬러 · 운송물건 예시 제거 · 이메일 제거 ·
                          동의 블록을 **제목 먼저 · 개인정보처리방침만** · 「선택」 칩 선명도
  로그인                   계정신청 버튼 위치
  /apply                  가로 사이즈 · 항목칸 컬러 · 약관 설명 간략 ·
                          주 이용 차량 **톤수+형태** · 주요 출발지·도착지 · 운송 규모
```
🔴 **②의 원인이 셋이었다** — (a) 인라인 `margin-left: -22` 와 미디어쿼리 `gap: 14` 가
겹쳐 **-8px 겹침**, (b) 로고 링크가 `flex: 0 1 auto` 라 좁은 화면에서 **폭 0 까지 찌그러짐**
(430px 61 · 390px 21 · **360px 0** 실측), (c) `flex-wrap` 은 **줄이기 전에 줄바꿈한다** —
기본 폭 합이 넘으면 한쪽이 줄어들 수 있어도 그냥 다음 줄로 내려간다.
🟢 로고를 `flex: 0 0 auto` 로 고정하고 **520px 이하에서 라벨을 짧게**(「무료 견적 문의」→
「견적 문의」) 바꿔 해소했다. 🔴 **`!important` 로 건 것은 인라인 style 을 이기기
위해서다** — 빼면 되돌아간다.
🔴 **주소칸 `font-size` 가 15px 이었다** — `.landing-page .landing-addr input`(0,0,2,1)
이 `.public-form input`(0,0,1,1)을 이겨서 **iOS 자동확대 방어가 조용히 뚫려 있었다.**
16px 로 올렸다(원칙 58번과 같은 특성도 함정).

**2라운드 — 지적 2건**
```
  로그인 안내    「평일 09:00 ~ 18:00」 / 「접수 기준 당일 발급」 **두 줄**
  운송 규모      4칸 폭을 똑같이 (`repeat(4, minmax(0,1fr))` — 실측 272px × 4)
```

**3라운드 — 지적 6건**
```
  1 주 이용 차량   🟢 **톤수 1개 + 형태 1개로 확정**(사용자 재확인) — 코드 변경 0
  2 /status       🔴 **라우트까지 삭제** · "기존 나간 안내는 테스트라 상관없다"
  3 처리방침 제2조  현장 3항목을 넣어야 하나? + **연락처 적을 때만 동의 활성화** 제안
  4 랜딩 여백      「이렇게 진행합니다」·「궁금하실 것들」이 벙벙하다
  5 푸터          위 여백 축소 + 회사 정보가 너무 작고 희미하다
  6 엑셀          월별 통계 엑셀에 화주에게 필요한 정보를 더 (예: 운송 품목)
```

**⑬-1 🔴 주 이용 차량은 톤수 1개 + 형태 1개다 — 중복 선택으로 바꾸지 말 것**
저장은 `preferred_vehicle` 에 **`"5톤 윙바디"` 한 문자열**(`join(" ")`)이고 승계 대상인
`companies.recommended_vehicle` 이 **단일 text** 다. 중복 선택으로 바꾸면 그 칸이 목록이
되어 담당자가 배차 판단에 바로 못 쓴다(539건이 "1톤 탑차" 형태다). 여러 차종을 쓰는
화주는 **요청사항으로 받는다.** 사용자가 2026-09-01 에 「지금 방향」으로 재확인했다.

**⑬-2 🔴 `/status` 는 라우트·API·법적 문서까지 통째로 없앴다**
1라운드에 링크를 빼고 3라운드에 나머지를 지웠다. 🔴 **되살리지 말 것.**
```
  지운 것   app/status/{page,layout}.tsx · app/quote/status · app/apply/status
            app/api/status-lookup · app/api/apply-status-lookup
  곁들여    app/robots.ts 의 disallow 한 줄 · TopNav 의 isPublicPath
  법적 문서  처리방침 제2조에서 「문의·신청 현황 조회 (/status) · 전화번호」 행 삭제
```
🟢 배포본에서 **세 경로 다 404** 를 확인했고, 프리렌더 실패가 48 → 45 로 **정확히 3개**
줄었다. ⚠️ **접수 안내 문자에 그 링크가 들어간 적은 없다** — SMS 8종에 접수 안내 문자
자체가 없다(61차 ⑨). 사용자가 말한 "기존 나간 안내"는 화면 문구였다.
🔴 **`PublicPageHeader` 의 `showStatusLink` prop 도 없앴다** — 쓰는 곳이 사라졌다.

**⑬-3 🔴 현장 3항목 — 처리방침이 먼저이고, 동의는 조건부다**
🔴 **사용자 질문에 대한 답: 선택 항목이어도 처리방침 제2조에 넣어야 한다.**
개인정보보호법 제15조 2항이 요구하는 것은 **"실제로 수집하는 항목"** 이고 필수/선택은
고지 의무를 가르지 않는다. 화면과 대조해 보니 현장 3항목 말고도 빠진 것이 많아서
**`/quote`·`/apply` 두 행을 통째로 다시 썼다**(품목·차량·상하차 조건·희망 일시·물품
특성·왕복편도·운송 시간대·대기시간·경유지 수·특이사항 / 사업자등록번호·이메일·주요
구간·현장 3항목·월 운송건수·업종·이용 지역·주 이용 차량·요청사항).
⚠️ **제4조와 어긋나지 않는다** — 제4조는 "신청 단계에서는 제3자에게 **제공**하지 않는다"
는 뜻이고 그건 지금도 사실이다. `/apply` 는 **수집만** 하고 실제 제공은 발주·배차
시점에 47차가 만든 포털 동의를 거친다. **이 구분을 놓치면 두 조가 모순돼 보인다.**
🔴 **제3자 동의는 조건부다 — 항상 뜨게 바꾸지 말 것**(사용자가 먼저 제안했고 법적으로도
옳다). 연락처를 안 적으면 **제3자의 개인정보 자체가 없어서** 그때 동의 행을 남기면
그 기록이 거짓이 된다(14차 "문구에 없는 동의를 기록하지 않는다"와 같은 결).
```
  연락처 비었을 때   카드 없음 · 체크박스 2개 · consents 2행
  연락처 적었을 때   카드 등장 · 체크박스 3개 · consents 3행 (+ third_party)
```
🔴 **서버가 화면이 보낸 boolean 하나를 믿지 않는다**(원칙 25번) — `origin_contact_phone`·
`destination_contact_phone` **값 자체를 받아** 조건을 직접 판단한다. 그 두 값은
**저장하지 않는다**(컬럼이 없어 `notes` 로 접혀서 따로 온다) — 게이트 판단에만 쓴다.
🔴 **`lib/consent.ts` 에 `CONDITIONAL_CONSENT_TYPES_BY_SOURCE` 를 새로 뒀다** —
`recordConsents` 가 `extraTypes` 를 받되 **그 표에 있는 값만** 통과시킨다. 호출부가
아무 동의나 끼워 넣지 못하게 하는 장치이니 지우지 말 것.
🟢 실측 — 연락처 있음+동의 없음 400 / 문자열 `"true"` 400 / `false` 400 /
**연락처 없음+동의 없음 200**(third_party 행 없이 정상 접수).

**⑬-4 ⚠️ 여백은 "섹션 리듬"이 아니라 그 섹션의 내용 높이에 맞춘다**
40차가 ⑤ 진행 단계에서 같은 지적을 받았던 것과 같은 문제다.
```
  이렇게 진행합니다   150/160px → 110/118px · 단계 행 40 → 30px
  궁금하실 것들       150/170px → 110/120px · 문답 간격 64 → 52px
  푸터 위 여백        130px → 76px
  푸터 회사 정보      12px → 14px · 줄간격 1.8 → 1.95 · #9C9B95 → #6C6B65
                     상호 줄 15.5px/700 #2F2E2B · © 줄 12.5px #9C9B95 (위계를 갈랐다)
```

**⑬-5 🔴 월별 통계 엑셀 — 곁다리로 어긋나 있던 것 둘을 같이 고쳤다**
운송내역 **5열 → 12열** · 정산내역 **8열 → 19열**.
🔴 **엑셀이 배차상태 원본 6종을 그대로 찍고 있었다.** 29차가 화주 화면을 **3단계 +
「문제 발생」 별도**로 확정했는데(`lib/dispatchStage.ts`) 엑셀만 안 바뀌어서, 같은 건이
화면에는 「배차완료」인데 엑셀에는 「상차완료」로 보이는 상태였다. **원본 6종으로
되돌리지 말 것** — 화면과 엑셀이 갈린다(원칙 13·42번과 같은 결).
🔴 **`pickup_confirmed`·`delivery_confirmed` 를 조회에서 빼지 말 것** — 문제발생 건은
상태값이 덮여 있어 이 둘로만 단계를 복원한다.
🔴 **현장 추가비를 표시 시점에 합산한다**(원칙 47번) — `customer_charge_total` 은 정산 건
생성 시점 스냅샷이라 그 뒤에 등록된 추가비가 없다. 정산확인 화면은 더해서 보여주므로
엑셀만 빼면 화면과 금액이 갈린다. **19차 `attributeActiveExtraCharges()` 를 그대로
재사용했다**(active만 / `correction_invoice_id` 없는 것만 / 가장 최근 invoice 이후 것만).
열은 **확정분 / 추가비 / 합계** 로 나눠 어디서 온 금액인지 보이게 했다.
🔴 **차주 정보·차주 지급액은 어떤 열로도 넣지 말 것**(29차 확정 9번 · 원칙 3·9번) —
`dispatch_extra_charges` 는 `PORTAL_DISPATCH_EXTRA_CHARGE_FIELDS` 로만 읽는다
(`driver_payout_amount` 는 컬럼 GRANT 로 DB 가 막아서 넣으면 쿼리가 죽는다).
🟢 **실제 `.xlsx` 를 브라우저에서 내려받아 파싱해 확인했다** — 하차완료 → 「운송완료」 ·
문제발생+상차확인 → 「배차완료」+「예」 · 현장 추가비 50,000(취소된 90,000 제외) ·
합계 248,000 · 부가세 24,800 · **금액 6열 전부 `number` 타입** · 원본 6종 문자열 0건.

**⑬-6 ⚠️ 겪은 함정**
🔴 **`floorMonth` 때문에 통계 조회 하한이 이번 달이라** 지난달 데이터로는 운송내역
시트를 시험할 수 없다(60차 ⑨(a)가 만든 정상 동작이다) — 목에 **이번 달 배차**를 넣어야
잰다. 🔴 **포털 세션 주입은 supabase-js v2 형태**여야 한다(`{access_token, user, ...}`
평평한 객체) — v1 의 `{currentSession:...}` 로 넣으면 로그인 화면으로 튕긴다. 그리고
목의 `/auth/v1/user` 가 `bearer === "t"` 일 때만 화주 계정 id 를 주므로
**`access_token` 을 `"t"` 로** 넣어야 한다.
⚠️ **거짓 ❌ 가 라운드마다 또 났고 전부 시험 자산 결함이었다**(27·59·60·61차와 같다) —
① `:has-text("5톤")` 이 **「2.5톤」에**, `:has-text("윙바디")` 가 **「윙바디/탑」에**
걸려 `preferred_vehicle` 이 `"2.5톤 윙바디/탑"` 으로 나왔다(`exact: true` 로 고쳤다)
② 기존 자동검증이 「동의 2행」을 기대하는데 3라운드로 3행이 됐고, 현장 연락처를 채우는
시나리오라 제3자 동의를 안 눌러 **제출이 정상적으로 막힌 것**이었다
③ dev 서버에서만 360px 오버플로우 3px 이 잡혔는데 **Next 개발용 오버레이 SVG** 였다
(프로덕션 빌드는 0). 🔴 **"❌ 가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 말 것.**
⚠️ `pkill -f` 가 또 자기 자신을 죽였다(종료코드 144) — `/proc/*/comm` 으로 거를 것.
⚠️ **라우트 파일을 지운 뒤 `.next/types` 가 낡아 `tsc` 가 없는 모듈을 찾는다** —
`rm -rf .next` 로 해소된다.

**작성일: 2026-07-23** (최종 갱신: 2026-08-31, Claude Code 세션에서 직접 갱신 — 61차
세션: "랜딩페이지 교체(30차)" — 디자인팀 Next 변환본을 받아 `/` 를 통째로 교체.
신규 파일 **8개**(`app/landing.css` · `components/landing/` 7개) + **이미지 32종**.
커밋 7개(본작업 5 + **실사용 리뷰 2**). **PR #108 merge됨**(squash `29f849b`, 리뷰 **2라운드**).
🟢 **DB 변경 0** · 🟢 **신규 의존성 0** · 🟢 **`globals.css` 변경 0** ·
🟢 **관리자 회귀 0**(9화면 × 12지표, `origin/main` 을 같은 목으로 나란히 띄워 대조) ·
🟢 **`app/customer/**` · `/quote` · `/apply` · `/customer/login` 변경 0**(31차 범위).
🟢 **Go-Live 차단이던 「랜딩 이미지」가 이것으로 해소됐다.**

**═══ ① 🔴 확정 다섯 개가 사용자 결정으로 뒤집혔다 (2026-08-31) ═══**
```
  1 취급 범위    「1톤부터 5톤 이상까지」(34차)  → 「1톤부터 25톤까지」(본작업)
               → 🔴 **「1톤부터 5톤 이상, 특수차량까지」**(리뷰 2라운드, 시안 문구)
  2 「이사」      고객 접점에서 쓰지 않음(34차)   → 🔴 **이사서비스를 노출한다**
               → 🟢 **2026-09-02 에 다시 뺐다**(65차 ② — wecarry24.co.kr 로 분리).
                 🔴 **이 줄을 근거로 되살리지 말 것.**
  3 차량 형태    랜딩 4종 / `/vehicles` 4종      → 🔴 **12종 · 이름도 시안 그대로**
  4 폼 3화면     미변경                          → 🔴 **새 디자인으로 교체 — 31차**
  5 보험         스위치로 켜고 끄기 → 꺼진 채 배포(본작업)
               → 🔴 **리뷰 1라운드에 켰다**(`INSURANCE_ENABLED = true`) — 아래 ④
```
🔴 **34차·합의 v3.0 을 근거로 되돌리지 말 것** — 27차 5라운드(선착불 입금 계좌)·
60차 ⑬(사진·인수증)과 같은 구조다. 옛 확정은 그 시점 기록이고 뒤에 뒤집혔다.

**═══ ② 🔴 「25톤」은 금지어에서 풀렸다 — 「전 차종」은 아니다 ═══**
🔴 **⚠️ 리뷰 2라운드에 화면 문구는 다시 시안(「1톤부터 5톤 이상, 특수차량까지」)으로
돌아갔다** — 아래는 **왜 25톤이 금지어에서 풀렸는지의 근거**로 계속 유효하다(요금
가이드가 25톤 시작가를 게시하고 FAQ 가 「그 이상 대형 차량도 문의 주시면」이라고
답하는 것이 그 결과다). 🔴 **「특수차량」도 사용자 지시로 풀렸다** — 아래 금지 목록에서
그 낱말만 빠졌다. 「전 차종」·「모든 차량」은 그대로 금지다.
🟢 **근거** — `rate_distance_tiers` 에 **25톤 행이 실재**하고(52차: 25톤 400km 초과
954,000) 차급이 11종이라 **견적이 실제로 산출된다.** 32차·12차가 금지했던 이유
("배차망으로 가능하긴 하나 아직 수월하지 않다")가 22차 이후로 성립하지 않는다.
🔴 **여전히 금지: 「전 차종」 · 「모든 차량」.** ⚠️ **「특수차량」은 30차 리뷰 2라운드에
사용자가 시안 문구로 통일하라고 해서 풀렸다** — 이 문단이 한동안 「특수차량」도 금지로
적어두어 같은 기록 안에서 앞뒤가 어긋나 있었고, **31차에 정정했다.**
⚠️ **「최저가」·「월별 이벤트」도 64차에 사용자 지시로 풀렸다** — **랜딩 WHY 05 한 곳
한정**이고(시안 원문), 다른 공개 화면에 새로 생기면 여전히 금지다. 🔴 **30차가 이 둘을
묶었던 근거(표시광고법 제3조 실증 자료 · 적용 조건·기간 고지)는 그대로 유효하고 아직
답을 못 받았다** — 64차 ⑥ 참고.
🔴 **다만 허용 범위는 시안 확정 문구 안뿐이다** — 「1톤부터 5톤 이상, **특수차량까지**」
(랜딩 차량 섹션 h2 · 요금 가이드 모달 · `/vehicles`)와 WHY 02 의 「1~5톤 기본 차량에
**특수차량까지**」. 홀로 쓴 「특수차량」이나 「전 차종」·「모든 차량」은 여전히 안 된다
— **25톤은 차급이고 특수차량(트레일러·크레인)은 차종**이라 다른 축이며, 차종 쪽은
취급 범위 밖이다. 🟢 31차 검증이 공개 10개 화면에서 **「특수차량」이 전부 「…까지」
형태**임을 실측했다(홀로 쓴 것 0건).
🔴 **한 곳만 고치면 말이 갈린다 — 이번에 맞춘 곳 전부:**
```
  랜딩 차량 섹션 제목 · WHY 02 · FAQ 「큰 차량」 · 요금 가이드 모달 안내
  /vehicles 부제 · 안내 문구 · PRICING_NOTES
  /about 인사말 원칙 1 (`lib/companyInfo.ts`)
  🔴 메타 description — `/` 와 `/vehicles`. **12·13차에 두 번 연속 놓친 자리다**
  lib/constants.ts 주석(근거 갱신)
```
⚠️ **`/about` 메타는 손댈 것이 없었다** — 차량 범위를 말하지 않는 문장이다.
🔴 **`VEHICLE_TYPES_PUBLIC`(6종)은 안 늘렸다** — 쓰는 곳이 `/quote`·`/apply` 뿐이고
그 둘은 **31차에 통째로 교체**된다(완료조건 34 가 변경 0을 요구한다). 곧 지워질
화면만 바꾸는 셈이라 미뤘고 **31차가 폼을 그릴 때 함께 정할 것**을 주석에 남겼다.

**═══ ③ 🔴 차량 12종 이름이 발주 폼과 다르다 — 알고 그대로 뒀다 ═══**
```
  랜딩·/vehicles 에서 보는 이름   5톤 탑차 · 5톤 윙바디 · 중형 윙바디
  발주 폼에 있는 것               차급 11종  +  형태 22종  (두 축을 각각 고른다)
                                  「5톤 탑차」라는 선택지는 없다
```
🔴 **이름은 시안대로 두는 것이 사용자 확정이다.** 대신 그 간극을 **안내 한 줄**로
메웠다 — 「발주 요청에서는 **차량 크기와 형태를 각각 선택**합니다. 예를 들어
「5톤 윙바디」는 **5톤 + 윙바디**로 고르시면 됩니다.」 랜딩과 `/vehicles` 두 곳에
같은 문장이 있다. 🔴 **지우지 말 것** — 없으면 화주가 폼에서 그 이름을 찾다 헤맨다.
🔴 **`/vehicles` 는 배열을 다시 적지 않고 `components/landing/data.ts` 의 `vehicles`
를 그대로 읽는다** — 두 곳에 각각 적으면 순서·이름·이미지가 조용히 갈린다.

**═══ ④ 🔴 보험 — 꺼진 채로 만들었다가 리뷰에서 켰다 ═══**
🔴 **디자인팀이 `INSURANCE_ENABLED` 스위치를 제거했길래 되살렸다.**
🔴 **그리고 리뷰 1라운드에 사용자 지시로 `true` 가 됐다** — *"위캐리를 선택해야 하는
이유에서 03. 적재물배상책임보험은 추가해줘. 나중에 이용약관만 수정하면 된다."*
같은 날 증권이 "오늘·내일 중 전달 예정"으로 확인됐고 공개일은 2026-09-07 이다.
⚠️ **그래서 이 플래그는 이제 "가입 완료"가 아니라 "공개일에는 가입돼 있다"는 전제다.**
🔴 **담보가 확인되지 않은 채 공개일이 오면 다시 `false` 로 내려야 한다.**
🟢 **카드 문구는 시안 원문과 글자 단위로 같았다** — 본작업에서 쓴 문장이 시안 `desc`
와 동일해서 리뷰에서 고칠 것이 없었다.
🔴 **FAQ 파손 문항에도 「2,000만원」은 넣지 않았다** — 보험 가입 사실만 적었다.
**왜** — 현 증권은 **이사화물 운송주선업자 특별약관 · 1사고당 1,000만원**이고 일반화물
담보로 변경 진행 중이다. 시안의 **「사고 건당 2,000만원 한도로 보장」은 근거가 없는
수치**다. 🔴 **약관 제19조는 금액을 쓰지 않는 설계다**(34차 — 상한은 3항 가액 신고제와
4항 고가물 특칙으로 만든다). **금액을 넣지 말 것.**
```
  꺼짐(false)          WHY 카드 **4개**(번호를 01~04 로 다시 매긴다) · FAQ **5문항**
  켜짐(true, 현재)     WHY **5개**(01~05) · FAQ **6문항**
```
🔴 **숨기는 것이 아니라 배열에서 빼서 DOM 에 렌더링하지 않는다** — `display:none` 은
페이지 소스에 남고, 미가입 상태의 노출은 표시광고 문제다.
🔴 **번호를 다시 매기는 것이 핵심이다** — 그냥 필터만 하면 02 다음이 04 가 되어
화주가 "빠진 항목이 있다"고 읽는다.
🟢 **2026-09-01 에 일반화물 증권을 받아 65차 ⑦ 에서 값 5개를 채우고 플래그를 내렸다** —
빌드 경고가 **0건**이 됐다. 🔴 **값은 화면에 나가지 않는다**(내부 값).
⚠️ **아래는 그 전 시점 기록이다** — `INSURANCE_INFO_IS_PLACEHOLDER` 가 `true` 라 상수
5개(상품명·보험사·한도·개시일·면책)가 비어 있어 빌드 경고가 계속 뜨던 상태였다. 🟢 **그 값을 렌더링하는 화면은
없다**(랜딩은 정적 문구만 쓴다). 증권을 받으면 값을 채우고 이 플래그만 내리면 된다.
⚠️ **켤 때 함께 볼 곳** — WHY 03 문구 · FAQ 파손 문항 · `/apply` 제5조(31차) ·
약관 제19조. 34차 기록이 *"담보가 정리되어도 **다시 넣을지는 별도 판단**"* 이라고
적어둔 것도 그대로 유효하다.

**═══ ⑤ ⚠️ 「이사」를 노출하면서 확인해야 할 것 둘 ═══**
```
  ① 현 증권이 **이사화물 특약**이라 이사는 오히려 담보된다.
     🔴 일반화물 담보로 바꾸면 **이사 담보가 빠질 수 있다** — 증권 받을 때 둘 다 볼 것
  ② 이사화물은 **별도 표준약관**이 있다. 현 이용약관이 이사를 커버하는지
     🔴 **변호사 검토 항목**이다
```

**═══ ⑥ 🔴 시작가는 DB 실측값이다 — 시안 값은 전부 틀렸다 ═══**
```
  시안 rateRows[0]   1톤 「40,000원부터」
  실제 DB            1톤 10km 이내 = **48,000**
```
`verify` 워크플로(읽기 전용)로 「10km 이내」 행을 직접 읽어 맞췄다. 🔴 **리뷰 1라운드에
11차급 전부로 늘렸다** — 48,000 / 60,000 / 84,000 / 96,000 / 108,000 / 133,000 /
151,000 / 169,000 / **176,000(15톤)** / 182,000 / **217,000(25톤)**. 뒤 둘은 이때
처음 실측한 값이다(CLAUDE.md 에는 15톤 60km · 25톤 400km 만 있었다).
🔴 **`app/vehicles/page.tsx` 가 이제 이 배열(`components/landing/data.ts` 의
`startPrices`)을 그대로 읽는다** — 본작업 때는 두 곳에 각각 적혀 있었다(리뷰 1라운드에
구조로 없앴다). **다시 갈라 적지 말 것.**
⚠️ **자동 연동이 아니다** — 손으로 맞춘 값이고, **운임기준표를 바꾸는 마이그레이션과
항상 같은 PR 에서 함께 움직인다**(주석을 두 곳에 달았다). 안 고치면 게시가 40,000 ·
실제 견적 48,000 이 되어 그대로 표시가격 분쟁이다.
🟢 **→ 64차에 자동 연동으로 바뀌었고 `data.ts` 의 `startPrices` 배열은 없어졌다**
(정의처는 `lib/startPrices.ts`, DB 실시간 조회). ⚠️ **게시 차급도 11 → 6종이 됐다**
(1톤 ~ 5톤 플러스/축) — 🔴 **이 문단의 11차급 기록을 근거로 되돌리지 말 것**(64차 ④).

**═══ ⑦ 🔴 법적 문서는 기존 모달을 쓴다 — `lib/legal/` 은 한 줄도 안 고쳤다 ═══**
시안 `components/landing/LegalModal.tsx` 는 약관·방침이 **「전문 준비 중」 자리표시자**
였다. 그 파일을 지우고 기존 `components/LegalLinks.tsx`(→ `lib/legal/*` 전문)로 바꿨다.
🔴 **`LegalLinks` 에 `linkClassName` prop 을 더했다** — 어두운 CTA 띠 위라 색이 달라야
하는데, 전역 `site-footer-link` 를 랜딩 스코프에서 덮으면 **전역 클래스 재정의**가 된다.
문서 목록·모달은 그대로 공유하므로 두 곳이 갈리지 않는다.
⚠️ **지시서 3-6 의 전제가 틀렸다** — *"이메일 무단수집 거부는 시안 것이 더 낫다(법조문
인용). 그 내용을 `lib/legal/` 로 옮기라"* 고 했는데, 대조해 보니 **우리 `emailPolicy.ts`
에 제50조의2·제74조가 같은 문안으로 이미 있다.** 옮길 것이 없었다.

**═══ ⑧ 🔴 서체는 `.landing-page` 스코프에만 걸었다 ═══**
시안은 Pretendard Variable 기준인데 `font-family` 를 지정하지 않고 왔다(디자인팀
README: *"상위 스코프에서 처리"*). 🔴 **전역 서체 변수를 건드리면 관리자 31개 화면과
다른 공개 화면이 함께 바뀐다** — 26차가 `.portal-v2` 에 쓴 방법 그대로 `.landing-page`
에만 걸었다. 🟢 **CDN link 는 `app/layout.tsx` 에 이미 있었다**(26차에 넣은 것) —
새로 넣지 않았다. ⚠️ **이 환경은 프록시가 폰트 CDN 을 막아 실제 서체를 확인할 수 없다**
— 선언 스택까지만 확인했다(56차와 같다).

**═══ ⑨ ⚠️ 착수 전 확인 14항목 — X 셋 ═══**
🟢 11항목은 지시서 전제와 같았다. 다른 셋:
```
  13 「`/status` 진입 링크가 푸터 + 모바일 드롭다운에 있다」  🔴 **X**
     실제로는 **푸터(`SiteFooter`) · `PublicPageHeader` 칩 · `/quote`·`/apply` 접수완료
     화면 2곳** 이다. 헤더 드롭다운에는 13차가 이미 뺐다.
     → 🟢 **접수완료 화면 2곳에는 조회 링크가 이미 있다**(3-9 가 요구한 것이 이미 되어
       있었다). 새 랜딩에는 `/status` 링크가 하나도 없어서 **CTA 하단에 넣었다.**
  14 「`/quote` placeholder 가 무료 **30분**」                🔴 **X — 그 문구가 없다**
     `/quote` 에는 대기시간 문구 자체가 없다. 20분 문구는 `customer/request` 와
     `admin/quotes` 에 있고 **이미 20분**이다(25차). → 3-3(f) 는 해당 없음.
  ——  3-9 「접수 안내 문자 2종에 조회 경로 포함」            🔴 **X — 그 문자가 없다**
     SMS 8종 어디에도 **접수 안내 문자가 없다**(견적문의·계정신청 접수 시 자동 문자가
     나가지 않는다). 새로 만드는 일이고 발송 트리거가 `/quote`·`/apply` API 에
     붙어야 하는데 **완료조건 34 가 그 두 화면 변경 0을 요구한다.** → 하지 않았고
     🔴 **별도 차수 후보로 남긴다.**
```

**═══ ⑩ ⚠️ 지시서와 다르게 판단한 것 ═══**
**(a) 🔴 커밋을 ②~⑥ 으로 나누지 않고 하나로 묶었다.** 문구 정합(③)·상수(④)·법적
모달(⑤)·보험(⑥)이 전부 **이번에 새로 들어오는 같은 파일들**에 대한 수정이라, 나눠
커밋하면 「최저가」·「월별 이벤트」·「차량·기사 정보를 전달합니다」·「사고 건당
2,000만원」 같은 **표시광고·확정 위반 문구를 `main` 이력에 한 번 넣었다가 빼는** 모양이
된다. 처음부터 고친 상태로 넣었다.
**(b) 푸터에 두 줄을 더했다** — **호스팅사업자(Vercel Inc.)** 와 **개인정보 보호책임자**.
시안 푸터에 없지만 **전자상거래법 제10조 표시사항**이고 기존 `SiteFooter` 가 같은
이유로 담고 있다. 랜딩에서 빠지면 표시사항이 주 화면에서 사라진다.
**(c) CTA 하단에 `/vehicles`·`/about`·`/status` 링크를 더했다** — 시안의 `#vehicles`
는 **페이지 내부 앵커**라 그 세 화면이 고아가 된다.
🔴 **리뷰 1라운드에 사용자가 이 판단을 뒤집었다** — 이동 링크 8개를 전부 뺐다(아래 ⑭①④).
**(d) FAQ 「5톤보다 큰 차량도 가능한가요?」 질문 자체를 고쳤다** — 답을 25톤 기준으로
바꾸면 질문("5톤보다 큰")과 어긋나서 「큰 차량도 가능한가요?」로 바꿨다.
🔴 **리뷰 2라운드에 질문·답 둘 다 시안 원문으로 되돌아갔다**(아래 ⑭②).
**(e) `components/LandingImage.tsx` 를 지우지 않았다** — 자리표시자 상자를 그리던
컴포넌트라 이제 쓰이지 않는다. 50차가 죽은 파일을 남긴 것과 같은 처리다.
**(f) `app/landing.css` 에 폼 3화면용 규칙이 함께 들어 있다** — 그 세 화면은 31차
범위라 안 바꿨고, 해당 클래스가 아직 없어 무해하다. **31차가 그대로 쓴다.**

**═══ ⑪ 신규 파일 8개 ═══**
```
  app/landing.css                        @keyframes · 미디어쿼리 · 30차 추가분
                                         (서체 스코프 · 법적 링크 · 차량 안내)
  components/landing/LandingHeader.tsx   헤더 — 스크롤 배경 전환 · BrandLogo · 버튼 2개
  components/landing/LandingFooter.tsx   푸터 — 고객센터 + 법정 표시사항(전부 상수 참조)
  components/landing/TmsShowcase.tsx     TMS 6아이콘 + 가로 캐러셀(드래그·꺽쇠)
  components/landing/FaqList.tsx         FAQ — `<details name>` 배타 아코디언 + 보험 분기
  components/landing/RatesModal.tsx      차량·요금 가이드 팝업(시작가 11차급 = DB 실측값)
  components/landing/data.ts             콘텐츠 데이터 전체 + `buildReasons`/`buildFaqs`
  components/landing/useReveal.ts        스크롤 리빌 훅(reduced-motion 이면 아무것도 안 함)
```
⚠️ **`components/landing/LandingHeader.tsx` 는 기존 `components/LandingHeader.tsx`(37차)
와 다른 파일이다** — 후자는 `/about`·`/vehicles`·법적 문서 페이지가 그대로 쓴다.
🔴 **공개 화면 헤더가 당분간 두 종류다**(랜딩 = 새 헤더 / 나머지 = 37차 옐로 헤더).
31차가 폼 3화면을 교체하면 좁혀진다.

**═══ ⑫ 🟢 검증 ═══**
```
  금지 표현        렌더링 텍스트 기준 — `/` · `/vehicles` · `/about` 전부 **0건**
                   (전 차종 · 모든 차량 · 최저가 · 이벤트 · 기사 정보 · 기사님 ·
                    예비 배차처 · 계약 없이). 🔴 「대표이사」·「특이사항」은 걸러냈다
                   ⚠️ **본작업 때는 「특수차량」·「보험」·「2,000만원」·「5톤 이상」도
                   0건이었다** — 리뷰에서 앞 둘이 사용자 지시로 풀렸다(위 ②·④)
  이미지           32종 · 깨짐 **0** · 페이지 에러 0 · 4xx/5xx 0
                   정의처 우회(직접 경로) **0건** — 전 파일 실측
  보험 켜짐        WHY 카드 **5개** · 번호 01~05 · FAQ **6문항** · 🔴 금액 0건
                   (본작업 때는 4개·5문항이었다 — 리뷰 1라운드에 켰다)
  시작가           **11행**이 `verify` 실측값과 일치 · 시안 옛 값(40,000) 0건 ·
                   `/vehicles` 도 같은 배열을 읽어 11행 · 「차종·적재 용량」 0건
  법적 문서        모달이 **전문**(제1조 …) · 「전문 준비 중」 0건 · 링크 3개
  푸터             호스팅·보호책임자 있음 · `tel:` 2곳 · **서버 HTML 에 평문 이메일 0**
  반응형           1320·1150·1120·900·700·620·430·390·360px 가로 오버플로우 **전부 0**
  메타             `/`·`/vehicles` 가 **시안 문구** 기준 · 금지 표현 0
  CTA 하단         링크 **3개**(법적 문서만) · 이동 링크 0 · **오른쪽 여백 0**(1440·
                   1120·900px) · 푸터 법정 표시사항 5종 유지
  reduced-motion   히어로 페이드 `animation: none` · 리빌 미적용(opacity 1)
  FAQ              한 번에 하나만 열림 · TMS 캐러셀 카드 6장
  관리자 회귀      🔴 **9화면 × 12지표를 `origin/main` 과 같은 목으로 나란히 띄워 대조 →
                   불일치 0** · 오버플로우 9화면 전부 0
  npx tsc --noEmit 통과 · `npm run build` 프리렌더 실패 **48개가 `origin/main` 과
                   항목 단위 동일** · `globals.css` 변경 0 · package* 변경 0
  자동검증 항목수   본작업 43 → 리뷰 2라운드 **77** · 실패 0
```
⚠️ **거짓 ❌ 하나가 또 시험 자산 결함이었다**(27·59·60차와 같다) — 「이메일 평문 아님」을
`page.content()`(= **하이드레이션 후 DOM**)로 쟀는데, `ObfuscatedEmail` 은 마운트 뒤에
주소를 드러내는 것이 **정상 동작**이다. **서버 HTML 을 `curl` 로 받아 재야 맞다**(0건 확인).

**═══ ⑬ ⚠️ 겪은 함정 ═══**
🔴 **`grep -c` 가 0을 찾으면 종료코드 1이라 배경 작업이 "실패"로 뜬다** — 빌드는 성공인데
프리렌더 실패 수를 세는 마지막 명령 때문에 실패로 보였다. **로그를 직접 볼 것.**
⚠️ **lazy 이미지를 "깨짐"으로 오판할 뻔했다** — `loading="lazy"` 라 화면 밖 12장이
`complete=false` 였다. **전체를 훑어 로드시킨 뒤 재야 한다**(4xx 는 0이었다).

**═══ ⑭ 🔴 PR #108 실사용 리뷰 2라운드 — 내 판단 셋이 뒤집혔다 ═══**
사용자가 Preview 를 눌러 보고 지적한 것이다(2026-08-31). 커밋 2개로 반영했다.

**1라운드 — 지적 5건**
```
  ①⑤ WHY 5종      🔴 **보험을 켜라**(위 ④). 「4장만 보인다 → 시안대로 5장」
  ②③ 요금 가이드   🔴 「차종·적재 용량」 삭제 · **시작가만** · 운임기준표 기준으로 세분화
  ④  CTA 하단     🔴 **이동 링크 8개 삭제**, 법적 문서 3종만
  ⑥  1440px 겹침  🟢 「일단 시안대로」 — 코드 변경 0
  ⑦  서체         🟢 「잘 들어온 것 같다」 — Preview 에서 Pretendard 확인됨
  추가 차량 문구   🔴 시안 문구(「1톤부터 5톤 이상, 특수차량까지」)로 되돌려라
  추가 폼 3화면    시안 HTML 3종 첨부 (아래 ⑮)
```
🔴 **②③ 은 「차종·적재 용량」(CBM·적재 예시)을 화면에서 없앤 것이다** — 원칙 42(정보
손실 금지)를 사용자 지시로 넘긴 것이니 되살리지 말 것. 되살리려면 **8~25톤 5개 차급의
CBM 을 먼저 정해야 한다**(기존 6종 값은 커밋 이력에 있다).
🔴 **④ 로 `/status` 가 랜딩에서 사라졌다** — `/quote`·`/apply` 헤더 칩과 두 접수완료
화면에는 그대로다. 55차 ⑮ 가 예고한 방향과 같으니 "고아가 됐다"며 되살리지 말 것.
🔴 **푸터의 고객센터·법정 표시사항은 남겼다** — 지시는 「푸터, CTA 하단에는 법적 문서
3종 모달만 빼고 모두 삭제」였지만, 그 값들(상호·대표자·사업자등록번호·주소·전화·
이메일·통신판매업 신고번호·호스팅사업자·개인정보 보호책임자)은 **전자상거래법 제10조
표시사항**이라 빼면 위법 소지가 있고 **화면에 남는 곳이 푸터뿐**이다. 지우라는 뜻이었는지
되물었고 그 뒤 지적이 없었다.

**2라운드 — 지적 2건**
```
  ① 다섯 곳도 시안 문구로 통일   WHY 02 · FAQ · 요금 가이드 · /vehicles 부제 · 메타 2곳
                                (+ /about 원칙 1 — 세지 않았지만 같은 기준이라 함께)
  ② 법적 문서 모달을 오른쪽 하단  "배경이미지의 밝은 부분과 겹쳐서 잘 안 보인다"
```
🔴 **②의 원인은 내 1라운드 수정이었다** — CTA 하단 줄이 `space-between` 인데 이동
링크를 빼면서 **자식이 하나만 남아 왼쪽으로 붙었다.** `flex-end` 로 고쳤고 실측
1440·1120·900px 전부 오른쪽 여백 0이다(모바일은 `app/landing.css` 가 가운데로 돌린다).
🔴 **①에서 운송 유형 「전국화물서비스」의 「1톤부터 25톤까지」는 그대로 뒀다** —
**그 줄은 시안 원문 자체가 25톤**이다. 통일한다며 이 줄까지 바꾸지 말 것(주석을 달았다).
⚠️ **FAQ 는 질문까지 시안으로 돌아갔다**(「5톤보다 큰 차량도 가능한가요?」). 답이
「5톤 플러스/축 차량까지 배차가 가능하며, 그 이상 대형 차량도 문의 주시면」인데 요금
가이드에는 25톤 시작가가 실려 있다 — **어긋나지 않는다**(게시가는 있고 가능 여부는
건별 확인이다). 이 조합을 "모순"으로 보고 한쪽만 고치지 말 것.
🟢 곁들여 `next.config.mjs` 의 보험 자리표시자 경고 문구도 고쳤다 — *"INSURANCE_ENABLED
가 false 라 관련 UI 는 렌더링되지 않음"* 이 1라운드 이후로 사실과 달라졌다.
🔴 **검증 중 함정을 또 밟았다** — 옛 `next-server` 가 포트를 잡고 있어 `next start` 가
`EADDRINUSE` 로 죽었는데 `curl` 이 200 을 돌려줘서 **낡은 빌드를 쟀다**(57차 ⑩(j)·
60차 ⑫ 와 같다). **죽이고 → 포트가 빈 것을 확인하고 → 새로 띄울 것.**
⚠️ 거짓 ❌ 둘도 시험 자산 결함이었다 — 시작가 행을 `.landing-rate-price > div` 로 셌더니
「시작가」 제목 div 까지 12개로 잡혔고, FAQ 답을 접힌 채로 쟀다(`<details name>` 배타
아코디언이라 `open=true` 를 한꺼번에 걸면 **마지막 하나만** 열린다 — 하나씩 열어야 한다).

**═══ ⑮ ⚠️ 다음 차수가 알아야 할 것 ═══**
🔴 **내부 정합이 한 칸씩 밀려 31~34차가 됐다.**
```
  31차   폼 3화면 — /quote · /apply · /customer/login 새 디자인 ✅ **62차 세션 완료**
         (2026-08-31 — "31차로 따로 가자". 이 PR 에 얹지 않기로 했다)
         🔴 **시안 HTML 3종은 PR #108 코멘트에 첨부돼 있다** — 저장소에는 안 넣었다
            (자체 압축해제 번들이라 3개 합쳐 20MB 다). 다시 받으려면 **WebFetch 로
            302 Location 을 받아 5분 안에 `curl`**(57차 ① 방법). 다운로드 링크는
            `github.com/user-attachments/files/31628074`(견적문의) ·
            `…/31628099`(운송관리 로그인) · `…/31628118`(운송관리 계정신청)
         🔴 `/apply` 동의 기록(`consents`, 49차) 연속성이 핵심 — 체크박스 2개 분리와
            항목별 서버 게이트가 화면·API·DB 한 벌이다(개인정보보호법 제22조 1항)
         🔴 `/quote` 도 서버 API(`quote-submit`)를 거쳐 동의 1행을 남긴다
         🟢 `VEHICLE_TYPES_PUBLIC` 은 **11종으로 늘렸다**(62차 ③) · 주소검색은 공용
            `AddressSearch` 를 `.landing-addr` 로 그대로 썼다(원본 변경 0)
         🟢 `app/landing.css` 에 폼 3화면용 규칙이 이미 들어 있다(위 ⑩(f))
  미정   보험 값 채우기 — 증권 수령 후. 상수 5개 + `INSURANCE_INFO_IS_PLACEHOLDER`.
         🔴 **화면은 이미 켜져 있다**(위 ④) — 문구 정합은 FAQ · WHY 03 ·
         /apply 제5조(31차) · 약관 제19조가 **함께 움직인다**
  32차   내부 정합 ① 견적·오더 — P1-3 · P2-3
  33차   내부 정합 ② 배차·정산 — P2-1
  34차   내부 정합 ③ 화주·차주 — P1-4 · P2-1 나머지
  미정   ~~담당자 정보(`customer/profile`)~~ **완료(PR #121)** · admin 공개문의 배지 · P1-5 · P2-2 ·
         상차 시각 지정 가산 · anon ③ · **접수 안내 문자 신설**(위 ⑨)
```
⚠️ **사용자가 코드 밖에서 할 일**
```
  🟢 `service-01-personal.jpg` — **zip 에 들어온 것이 이미 교체본이다**(사용자 확인
     2026-08-31). 나중에 또 바뀌면 같은 파일명으로 덮어쓰면 된다
  🟢 보험 증권 — **2026-09-01 수령**(일반화물, 1사고당 1억). 65차 ⑦ 에서 상수 반영.
     🔴 아래 「내려야 한다」는 그 전 시점 기록이다 — 지금은 내릴 이유가 없다
  🔴 원격 임시 브랜치 정리(`claude/*`) — 이 환경은 브랜치 삭제가 막힌다(58차 ⑪c)
```
⚠️ **답을 못 받은 것 2건**(그대로다) — ① 자주 쓰는 화물 목록 이름 표시(26차부터)
② 무료 대기 `"초과 시 30분당"` 단위(25차부터).
⚠️ **보고만 하고 안 고친 것 1건** — **1440px 안팎에서 히어로 둘째 줄이 트럭 위로
올라온다**(헤드라인이 `white-space: nowrap` + `clamp` 라 44% 폭 칼럼을 넘는다.
1280 이하는 세로로 쌓이고 1920 은 여유가 있다). 🔴 **지시서 3-2 가 레이아웃·반응형은
시안 그대로 받고 검수에서 걸지 않는다고 해서 손대지 않았다** — 고칠지는 사용자 판단이다.

**작성일: 2026-07-23** (최종 갱신: 2026-08-31, Claude Code 세션에서 직접 갱신 — 60차
세션: "화주포털 조회 흐름(29차)" — 신규 파일 2개(`lib/dispatchStage.ts` ·
`components/pv2/Pv2DispatchCalendar.tsx`). 커밋 11개(본작업 7 + **실사용 리뷰 4**).
**PR #107 merge됨**(squash `62eca7b`, 리뷰 **2라운드**). 🟢 **DB 변경 0** ·
🟢 **신규 의존성 0** · 🟢 **관리자 화면 변경 0** · 🟢 **`globals.css` 1095 추가 / 0 삭제**.

**═══ ① 🔴 시안은 처음부터 있었다 — 「새 시안」이 없었을 뿐이다 ═══**
28차·59차가 *"29차는 시안 대기로 막혀 있다"* 고 적었는데 **틀렸다.**
`고객운송관리 리디자인 v4.dc.html` 에 **세 화면이 다 있었다**(배차·정산·통계 + 캘린더).
없던 것은 클로드디자인이 새로 줄 「신규 시안」이었고, **사용자가 v4 로 진행하기로
확정했다**(2026-08-29). 🔴 **"시안이 없어서 못 한다"를 다시 쓰기 전에 저장된 시안
파일부터 열어볼 것.**

**═══ ② 🔴 시안과 확정이 갈리는 넷 — "시안에 있으니까/없으니까"로 되돌리지 말 것 ═══**
```
  (a) 차주 정보      시안에 있다   → 🔴 **통째로 뺐다** (확정 9번). 마스킹도 안 한다
  (b) 정산방식       시안은 담당자 말 → 🔴 **화주 말 두 줄** (P1-1, 아래 ④)
  (c) 문제발생·혼적·추가비   시안에 0건 → 🔴 **그대로 뒀다** (원칙 42번)
      ⚠️ **사진·인수증만 예외** — PR 리뷰에서 사용자가 확정 10번을 뒤집었다 ↓
  (d) PDF 전체 다운로드  시안에 버튼 있다 → 🔴 **안 만들었다** (확정 12번)
```
🔴 **(a) 는 정보 손실이 아니다** — 화주가 알아야 할 것은 "배차됐는가"이고 그건 알약
보조 텍스트가 「배차 대기」/「차량 배차됨」으로 이미 보여준다.
🔴 **(c) 는 시안이 안 그린 것이지 없애라는 뜻이 아니다**(27차 ⑧(c) 와 같은 판단).

**═══ ③ 🔴 배차 3단계 매핑 표 — 정의처는 `lib/dispatchStage.ts` 하나다 ═══**
```
  DB 값        단계            비고
  접수중       0 접수
  배차확정     1 배차완료
  상차완료     1 배차완료
  하차완료     2 운송완료      🔴 진행 중에 두지 말 것 — 화물은 이미 도착했다(53차 ⑦)
  운송완료     2 운송완료
  문제발생     ✗ 단계 없음     상태값을 덮어써서 단계를 알 수 없다 ↓
```
🔴 **문제발생 건은 `pickup_confirmed`·`delivery_confirmed` 두 boolean 으로 복원한다** —
조회에서 그 둘을 빼면 문제발생 건이 전부 「접수」로 보인다.
🔴 **배차 화면과 홈이 이 파일을 같이 쓴다** — 두 곳에 각각 적으면 "홈에는 배차완료인데
조회에는 접수"처럼 조용히 갈린다(59차가 `lib/arrivalType.ts` 로 한 것과 같은 방식).
🔴 **`lib/dispatchStatusColors.ts`(6종)를 이 매핑으로 바꾸지 말 것** — 관리자 3화면이
그대로 쓰고, 담당자에게는 6단계가 정확한 말이다.
🔴 **「문제 발생」은 4번째 알약이 아니라 알약 3개 위의 빨간 배지다**(확정 6번).
색은 27차가 견적 「취소」에 쓴 `#B4423A`/`#FDF3F2` 그대로이고 **새 색을 만들지 않았다.**
⚠️ **홈 목록 필터를 `neq("운송완료")` → `not in (운송완료,하차완료)` 로 바꿨다** —
안 바꾸면 "진행 중인 운송" 블록에 「운송완료」 배지가 달린 행이 남는다.

**═══ ④ 🔴 정산방식 2축 분리는 확정 8번과 충돌하지 않는다 ═══**
```
  운임 수금방식   getCustomerCollectionMethodLabel()   위캐리 수금 / 선착불(차주 직접수금)
  청구            getCustomerBillingCycleLabel()       건별 / 월정산       ← **별도 줄**
```
🔴 **줄이는 것이 아니라 나누는 것이다** — 4종을 2종으로 줄이면 확정 8번 위반이지만,
2축으로 나누면 조합 4가지가 그대로 표현되고 **「월정산」이 화면에서 사라지지 않는다**
(사라지면 월정산 묶음 청구가 그 값으로 도는데 화주는 그 사실을 모른다, 원칙 42번).
🔴 **청구 축을 `getCustomerCollectionMethodLabel()` 안에 넣지 말 것** — 그것이 27차가
기각한 (C)안이다. **별도 함수 · 별도 줄**이다.
🔴 **`getSettlementDisplayLabel()` 은 한 글자도 안 고쳤다** — 관리자 5개 파일이 쓴다.
🟢 실측 — `app/customer/**` 에서 담당자 말 함수 호출 **0건**(27차가 남겨둔 두 화면 해소).
⚠️ **주석에도 그 함수 이름을 문자로 쓰지 않았다** — 완료조건 grep 에 주석 자신이 걸린다
(12·51·54차에 이어 **네 번째**로 피한 오탐이다).

**═══ ⑤ 🔴 완료 알약에 일시를 못 넣는 이유 ═══**
`dispatches` 에 **완료 시각 컬럼이 없다** — `created_at`·`updated_at` 뿐이고 단계는
boolean 두 개뿐이다(코드 전수 확인: `confirmed_at`·`pickup_at`·`delivery_at` 0건).
그래서 완료 알약 보조는 **「완료」 한 단어**다. 🔴 **일시를 넣자는 제안이 나오면 컬럼부터
확인할 것** — 없는 값을 그럴듯하게 채우면 화주가 그 시각을 실제 완료 시각으로 읽는다.
⚠️ 완료 **전**의 3번째 알약 보조는 「{상차일} 예정」이라 날짜가 정상적으로 들어간다.

**═══ ⑥ 🔴 캘린더는 월별 통계 하단이다 — 홈이 아니다 ═══**
24·25차에 진입 경로가 없던 **의도된 공백**이 이번에 메워졌다.
🔴 **`/customer/calendar` 라우트는 그대로 살아 있다**(53차·54차 확정 — "안 쓰는 라우트"로
보고 지우지 말 것). 캘린더를 `components/pv2/Pv2DispatchCalendar.tsx` 로 떼어내
**통계 화면과 그 라우트가 같은 컴포넌트를 쓴다.**
🔴 **날짜 클릭 상세는 34차 기능이라 지우지 않았다.** 🔴 **그리고 그것은 팝오버다**
(PR 리뷰 — 아래 ⑬). ⚠️ 마지막 줄을 빈 칸으로 채워 7의 배수로 맞춘다(안 하면
`:nth-child(7n)` 세로선 규칙이 어긋난다).
🔴 **`.pv2-calcell` 의 `border: none` 을 빼지 말 것** — 이 칸은 `<button>` 이라
`border-bottom`·`border-right` 만 주면 위·왼쪽에 **브라우저 기본 버튼 테두리(진한
회색)** 가 남는다. 사용자가 「지난간 날짜에 회색 진한 라인」으로 지적한 것이 이것이다.

**═══ ⑦ 신규 파일 2개 ═══**
```
  lib/dispatchStage.ts                  배차 6종 → 화주 3단계 매핑 + 문제발생 판정
                                        (배차 화면 · 홈 공용, 위 ③)
  components/pv2/Pv2DispatchCalendar.tsx  배차 캘린더 (통계 하단 · /customer/calendar 공용)
```

**═══ ⑧ ⚠️ 착수 전 확인 14항목 — X 둘 ═══**
🟢 12항목은 지시서 전제와 같았다. 다른 둘:
```
  2  「문제발생이 별도 boolean 인가」  🔴 **X — 둘 다 있다.**
     `dispatch_status='문제발생'`(6종 중 하나, 포털이 이미 조회)과
     `dispatches.issue_occurred`(관리자 배차 상세 체크박스)가 **별개**이고 서로
     자동으로 맞추지 않는다. 포털은 그동안 후자를 조회조차 안 했다.
     → `hasDispatchIssue()` 가 **둘의 합집합**으로 판정한다. 한쪽만 보면 담당자가
       체크만 하고 상태를 안 바꾼 건(또는 그 반대)이 새어 나간다.
  14 「세 화면 네이티브 요소」      부분 X — `input[type=date]` 0 은 맞지만
     `<select>` 가 **각 1개씩 총 3개** 남아 있었다(26차 범위 밖이던 것).
     → 셋 다 `Pv2Select` 로 바꿔 **0개**가 됐다(원칙 57번).
```
⚠️ **6번(`dispatchStatusColors` 사용처)을 한 번 X 로 오판했다** — `lib/smsLogLabels.ts`
가 **주석에만** 그 이름을 적어둔 것을 사용처로 셌다. 실제로는 admin 3 · portal 2 로
지시서 전제가 맞다. **grep 결과를 사용처로 곧장 읽지 말 것.**

**═══ ⑨ ⚠️ 지시서에 없어서 판단한 것 4가지 ═══**
(a) 🔴 **통계 조회 하한을 「이번 달을 넘지 않게」 한 번 더 눌렀다** — 시행일(2026-09-07)이
아직 안 와서 하한을 그대로 두면 **조회 구간이 거꾸로(시작 > 끝) 잡혀 화면이 통째로 빈다.**
55차 ⑩ 이 발주 폼에서 겪은 것과 같은 함정이다(`max(오늘, 시행일)` 로 두었더니 시행일
전까지 발주가 아예 안 됐다). 🔴 **날짜는 여전히 하드코딩하지 않았다.**
(b) 정산 표에 **「상태」 열이 시안 6열에 없다** — 그냥 빼면 「지연」·「거래중단」·
「정산확정」이 사라진다(세금계산서·입금 배지로 표현되지 않는 값이다). 열을 7개로 늘리면
시안 grid 비율이 깨지므로 **오더번호 칸 아래 작은 배지**로 남겼다(원칙 42번).
(c) 통계에서 **「정산대기 중」 카드와 「자주 이용하는 구간 TOP 3」을 없앴다** — 시안 요약은
3개 고정이고, 미결제는 정산 화면의 「미결제 잔액」이 더 정확히(건수+금액) 보여준다.
구간 TOP 3 은 시안의 「하차지별 운송현황」이 같은 자리에서 금액까지 함께 답한다.
(d) 캘린더의 「배차·운송 조회에서 전체 보기」를 **데스크탑에도** 뒀다(시안은 모바일 전용) —
없으면 캘린더에서 목록으로 갈 경로가 사이드바뿐이다.

**═══ ⑩ 🔴 새로 계산하는 것 — 지역별·하차지별 ═══**
```
  지역별    도착지의 **시·도**(첫 어절)   「경기 성남시 …」 → 「경기」
  하차지별  도착지의 **시·군·구까지**(앞 3어절) · 운송비 상위 4곳
```
🔴 **하차지를 전체 주소로 묶지 말 것** — 상세주소가 달라 같은 곳이 여러 줄로 갈린다.
🔴 **하차지 막대에 `margin-left:32px` 가 있다**(순위 배지 폭) — 빼면 이름 줄과 어긋난다.
⚠️ **집계 기준은 그대로다** — 입금이 확인된 정산 건(`payment_received`)만 실적으로 센다.

**═══ ⑪ 🟢 검증 76항목 — 실패 0 ═══**
```
  배차       차주 이름·연락처·차량번호 **0건**(마스킹 형태도 0) · 카드 20px/22px 26px/20px ·
             알약 3개 · 현재 단계 #FFD833 · 연결선 radial-gradient · 「문제 발생」 별도 배지 ·
             알약 라벨 3종뿐 · 혼적·사진 유지 · 화살표만 #FFD833 · 모바일 세로+연결선 없음
  3단계 매핑  6종 전부 실측 — 접수중→0 · 배차확정→1 · 상차완료→1 · 하차완료→**2** ·
             운송완료→2 · 문제발생(상차확인)→**1**(boolean 복원)
  정산       요약 3카드 · 미결제 #B4423A · 안내 띠 전화번호가 상수 · 열 6개와 이름 ·
             배지 4종 색 전부 · **정산방식 두 줄 화주 말** · 「청구 월정산」 유지 ·
             현장 추가비 펼침 + **취소 항목 미노출** · 담당자 말 0건 · select 0
  통계       PDF 버튼 0·엑셀 1 · 그래프 236px · 막대 40px/8-8-2-2 · 평균선 dashed +
             라벨 흰 배경 · 두 카드 · 하차지 margin-left 32px · 프리셋 6개 ·
             데스크탑 달력 = Pv2DatePicker · **모바일은 드롭다운**
  캘린더     통계 하단 · 날짜 칸 92px · 요일 일 #C05B54 토 #4C6FBF · 건수 버튼 #FFF9D6/#7A5F00
  홈         배지가 3단계 라벨만 · **6종 원본값 0건**
  가로 스크롤 포털 8화면 × 360·390·430px = 24항목 전부 0
  npx tsc --noEmit  통과 · package.json·package-lock 변경 0
  npm run build     프리렌더 실패 48개가 origin/main 별도 worktree 빌드와 **항목 단위 동일**
                    (목 환경변수를 넣고 빌드하면 실패 0)
  globals.css       **1095 추가 / 0 삭제**
```

**═══ ⑫ ⚠️ 겪은 함정 ═══**
🔴 **거짓 ❌ 하나가 또 시험 자산 결함이었다**(27·59차와 같다) — 「완료 알약에 일시 없음」을
`/완료\s*\d{2}\.\s*\d{2}/` 로 쟀는데, **완료 전 3번째 알약의 「{상차일} 예정」**에 걸린 것이다.
**완료된 건(stage 2)의 보조가 「완료」 한 단어인가**로 다시 재야 맞다.
🟢 **진짜 결함도 하나 있었다** — 구간 화살표가 `#7A5F00`(24차가 홈에 쓴 값)이었다.
`.pv2-droute .pv2-arrow-glyph` **두 클래스로 특성도를 올려** 시안 `#FFD833` 로 덮었고,
🔴 **홈의 `.pv2-arrow-glyph` 는 되돌리지 않았다**(24차가 의도적으로 둔 값이다, 원칙 58번).
⚠️ **목 결함 셋** — ① `dispatch_extra_charges` 키를 파일 뒤쪽에서 빈 배열로 덮어써서
추가비가 0건이었다 ② `dispatches` 에 `order_id` 를 안 넣어 정산↔추가비 연결이 끊겼다
③ 카테고리 키를 `manual_labor` 로 잘못 썼다(실제는 `manual_loading_fee`) — 라벨 함수가
모르는 값을 원문 그대로 돌려주는 **정상 동작**이 화면에 그대로 보였다.
🟢 **목에 `not.` 연산자(PostgREST `not.in`·`not.is`)를 새로 넣었다** — 없으면 홈의
`not in (운송완료,하차완료)` 와 캘린더의 `not.is.null` 이 통째로 무시되어 실제 DB 보다
헐거워진다(원칙 56번).
🔴 **옛 `next-server` 가 포트를 잡고 있어 `next start` 가 죽었는데 `curl` 은 200 을 줬다**
(57차 ⑩(j) 와 같다) — `comm` 이 `node` 가 아니라 `next-server (v14.2.5)` 라 `node` 만
훑는 정리에서 빠진다. **죽이고 → 포트가 빈 것을 확인하고 → 새로 띄울 것.**

**═══ ⑬ 🔴 PR #107 실사용 리뷰 — 내 판단 하나가 뒤집혔고 확정 하나가 뒤집혔다 ═══**
사용자가 시안 이미지 2장과 함께 지적한 3건이다(2026-08-31). 커밋 2개로 반영했다.
```
  ① 사진·인수증   🔴 **확정 10번(존치)을 사용자 본인이 뒤집었다**
                  *"화주에게 인수증 전달은 여기서 안함."*
  ② 캘린더        🔴 **내 커밋⑤ 판단(표 아래 카드)이 뒤집혔다 — 팝오버가 맞다**
                  + 칸의 「회색 진한 라인」 제거
  ③ 통계 카드 2개  🟢 없어도 된다고 확인 — 코드 변경 0 (위 ⑨(c) 가 맞았다)
```
🔴 **①은 27차 5라운드가 선착불 입금 계좌를 뒤집은 것과 같은 구조다** — 옛 확정을
근거로 되살리지 말 것. `components/DispatchPhotosPanel.tsx` 와 관리자 업로드·열람,
화주포털 서버 API 는 **그대로 살아 있다**(로드맵④ 인프라를 지운 것이 아니다).
⚠️ **`dispatches` 조회의 `delivery_confirmed` 는 남겼다** — 사진 게이트가 아니라
**문제발생 건의 3단계 복원**에 쓰는 값이다(빼면 그 건이 「접수」로 보인다).
🔴 **②의 팝오버는 grid 좌표를 계산하지 않는다** — 클릭한 칸의 실제
`offsetLeft`/`offsetTop` 에서 읽고 캘린더 폭 안에 가둔다. 닫는 길은 셋(✕ · Esc ·
바깥 클릭)이고 월을 넘기면 같이 닫힌다.
⚠️ **팝오버 배지는 오더 상태 그대로다 — 배차 3단계로 바꾸지 말 것.** 캘린더는
`orders` 를 읽고 오더 상태에는 「운송중」처럼 3단계로 표현되지 않는 값이 있다.
🟢 **리뷰 반영분도 `globals.css` 삭제 줄 0 을 유지했다** — 지운 `.pv2-dphoto-btn`·
`.pv2-caldetail*` 는 이 PR 에서 내가 넣은 줄이라 `origin/main` 기준으로는 없던 줄이다.
🔴 **리뷰 2라운드 — 캘린더 칸 hover 에 「상세 보기」 안내를 넣었다**(`.pv2-calhint`).
*"클릭을 유도할 수 있게 커서를 올렸을 때 클릭안내가 있으면 좋겠다."*
🔴 **네이티브 `title` 로 바꾸지 말 것** — 브라우저 기본 지연(약 1초)이 있어 21차에
사용자가 그 늦음을 지적했고 그때도 커스텀 표시로 교체했다.
🔴 **자리는 날짜 숫자 오른쪽(칸 위쪽)이고 문구가 짧은 데는 이유가 있다** — 아래로
내리면 「N건 상차」 배지와 겹친다(실측: 칸 **130×92px** 에서 배지가 47~77px 을 쓰고
남는 세로가 **5px** 뿐이다. 처음에 아래에 뒀다가 10px 겹치는 것을 실측으로 잡았다).
문구도 그래서 「눌러서 상세 보기」(87px)가 아니라 「상세 보기」다(남는 가로 76px).
🔴 **건수가 있는 날짜에만 붙인다** — 빈 날짜에 띄우면 볼 것이 있다는 뜻이 되어
눌렀을 때 빈 팝오버가 뜬다. ⚠️ **모바일(≤700px)에서는 숨긴다** — 터치에는 hover 가
없고, 탭하면 `:hover` 가 남아 안내가 계속 떠 있는다.

**═══ ⑭ ⚠️ 다음 차수가 알아야 할 것 ═══**
🔴 **다음은 차수 없는 소수정(담당자 정보 `customer/profile`)이고, 그 뒤가 30~32차(내부 정합)다.**
```
  차수 없음  담당자 정보 — 29차에서 뺐다(조회가 아니라 계정 화면이고 파일이 안 겹친다)
  30차       내부 정합 ① 견적·오더 — P1-3(견적→오더 5필드) · P2-3
  31차       내부 정합 ② 배차·정산 — P2-1
  32차       내부 정합 ③ 화주·차주 — P1-4 · P2-1 나머지
  미정       랜딩 교체 · 현황조회 대체 경로 · admin 공개문의 배지 · P1-5 · P2-2 ·
             상차 시각 지정 가산 · 5톤급 재검토 · anon ③
```
🔴 **30~32차는 파일 단위로 안 겹친다** — `dispatches/[id]`(2520줄)와 `quotes`(1821줄)를
같은 차수에 넣지 말 것.
🟢 **P1-1(포털 정산방식 화주 말)은 이번에 끝났다** — 30~32차 지시서에서 빼도 된다.
⚠️ **답을 못 받은 것 2건**(그대로 남아 있다) — ① 자주 쓰는 화물을 불러오면 그 목록 이름을
어딘가 표시할지(26차부터) ② 무료 대기 `"초과 시 30분당"` 단위도 20분으로 바꿀지(25차부터).
~~⚠️ **Go-Live 차단은 「랜딩 이미지」 1건**이다.~~ → 🟢 **30차(61차 세션)에 해소됐다** —
디자인팀이 이미지 32종을 줬고 `public/landing/` 에 들어갔다.
⚠️ **사용자가 코드 밖에서 할 일** — 🔴 **원격 임시 브랜치 정리**(`claude/*`, GitHub 웹에서).
이 환경은 브랜치 삭제가 막힌다(58차 ⑪c). 🟢 **이번 차수는 새 원격 브랜치를 만들지 않았고
DB 도 건드리지 않았다.**

