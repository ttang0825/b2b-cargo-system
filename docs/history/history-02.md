> 🔴 이 파일은 `CLAUDE.md` 에서 옮겨온 세션 기록입니다. **요약하거나 줄이지 마십시오** —
> 되돌리면 안 되는 결정의 근거가 여기에만 있습니다.
> 담는 범위: 37차  →  52차 #101   (14건)
> 입구는 `HANDOFF.md`, 규칙과 로드맵은 `CLAUDE.md` 입니다.

---

52차
세션: "운임 매트릭스 보정 + 차급 11종(22차)" — 신규 파일 3개(전부 마이그레이션 SQL).
커밋 5개. **PR #101 merge됨 — 리뷰 지적은 없었다.**
🔴 **DB 변경 있음** — `rate_distance_tiers` 15행 UPDATE + **30행 INSERT** ·
`rate_vehicle_extra_fees` 2행 INSERT · `rate_surcharges` 2행 UPDATE. **총 165행 11차급.**
🟢 **착수 전 확인에서 지시서 전제가 하나도 안 틀린 첫 차수다**(16·17·19·20·21차는 다섯 번
연속으로 틀렸다). 그래도 절차는 그대로 밟았고, 그게 맞았다 — 아래 ②.

**═══ ① 🔴 왜 지금인가 — 21차가 전제를 깼다 ═══**
18톤 견적이 **실제보다 약 13% 비싸다**는 건 46차부터 알고 있었다. 미뤄온 근거는
*"대형 오더는 드물고, 나오더라도 담당자가 최종금액을 수동 조정할 수 있다"*였는데,
**51차 merge 전 리뷰에서 `/customer/request` 희망 톤수가 6종 → 9종이 되면서 그 전제가
깨졌다.** 이제 **화주가 스스로 18톤을 요청하고 자동견적이 그대로 나간다.**
그리고 그 목록에 **8톤·15톤이 없어서** 15톤이 필요한 화주가 11톤(견적이 낮아 마진이
깎인다)이나 18톤(비싸다)으로 고르고 있었다. 2026-08-25 실거래 114건 중 **36건(32%)**이
표에 없는 차급이었다(8톤 13 · 9.5톤 5 · 14톤 12 · 15톤 6).

**═══ ② 착수 전 확인 — 전부 맞았다. 그래도 건너뛰지 말 것 ═══**
`verify` 워크플로로 운영 DB 를 읽어 7건을 대조했고 **어긋난 것이 없었다.**
135행 · 9차급 · 15구간 / 18톤 15칸 현재값이 지시서 "현행" 열과 전부 일치 /
파손주의 `0.1 / 30,000` · 장척/중량 `0.2 / 80,000` / `rate_vehicle_extra_fees` 9행 null 0 /
`VEHICLE_TYPES_ALL` 9종에 정의처 1곳 / `rate_surcharges` 에 상차 시각 지정 카테고리 없음.
🟢 **거기에 더해 보간의 근거가 되는 5톤+ · 11톤 · 25톤 45칸도 직접 재계산해 대조했다** —
지시서 표가 실제 DB 값과 한 칸도 안 틀렸다. **이 대조를 안 했으면 틀린 근거 위에 30행을
쌓을 뻔했다**(값이 틀려도 단언은 통과한다 — 단언은 "일관성"을 보지 "정확성"을 못 본다).
⚠️ 조사 방법은 48·49·51차와 같다 — 브랜치에 조사용 SQL 을 올려 워크플로를 **그 브랜치 ref**
로 돌렸고, 확인 후 `_verify.sql` 을 원본으로 되돌리고 임시 커밋은 PR 전에 정리했다.

**═══ ③ 🔴 18톤 — `200km 이내` 한 칸만 −5.5%다. 되돌리지 말 것 ═══**
전 구간 **×0.89**(천원 반올림)인데 **`200km 이내`만 341,000**이다.
```
  361,000 × 0.89 = 321,000   그런데 11톤이 325,000 → 차급 역전
  → 11톤 대비 +5% 바닥을 걸어 341,000
```
v8 원본에서 그 구간만 11톤과의 격차가 11%로 좁았기 때문이다.
🔴 **"왜 이 칸만 다르지" 하고 ×0.89 로 맞추지 말 것** — 역전이 되살아난다. 이유를
마이그레이션 파일과 `lib/constants.ts` 주석 **두 곳에** 못박아뒀다.
🟢 **음성 시험으로 확인했다** — 로컬에서 321,000 을 넣으니 단언이
`18톤이 11톤 이하인 구간이 있다: 1개` 로 정확히 멈췄다.
🟢 **11톤은 손대지 않았다**(완료 36건, 마진 중앙값 +19.6% — 검증됨).
⚠️ **25톤도 손대지 않았다**(완료 3건, 판단 불가 — 관찰 대상).

**═══ ④ 🔴 8톤·15톤은 보간값(근거 등급 D)이다 ═══**
`8톤 = (5톤 플러스/축 + 11톤) / 2` · `15톤 = (11톤 + 18톤 조정후) / 2`, 천원 반올림.
⚠️ **15톤은 같은 날 조정한 18톤을 근거로 삼는다** — 파일명 순서상 18톤 파일이 먼저 실행되고,
15톤 파일 ①에 `18톤 10km 이 182,000 이 아니면 멈춤` 단언을 넣어 순서를 강제했다.
```
  8톤   완료 5건   🟢 실거래 역산 중앙값 193,000, 표의 30~110km 가 187,000~211,000 — 잘 맞는다
  15톤  완료 1건   🔴 사실상 미검증. 투입 후 관찰 대상이다
```
🔴 **이 파일을 둘로 쪼개지 말 것.** 차급만 넣고 `rate_vehicle_extra_fees` 를 빠뜨리면
그 차급 대기료가 **예외도 경고도 없이 0원**이 된다(16차에 5톤급이 그 상태였다).
🟢 음성 시험 — 대기료 행을 빼니 `가산기준이 11행이 아니다: 10행` 으로 멈춘다.

**═══ ⑤ 물품특성 2행 — 장척/중량은 경계선이다 ═══**
파손주의 `10% + 30,000 → 10% + 15,000`(14건) / 장척/중량 `20% + 80,000 → 15% + 40,000`(5건).
⚠️ 🔴 **장척/중량은 표본 5건이고 v8 자신이 "5건 미만은 금액을 바꾸지 말 것"이라 했다** —
경계선이다. **관찰 대상**이며 실거래가 쌓이면 다시 볼 것.
🟢 **화주 화면에는 영향이 없다** — 21차가 `/api/customer/surcharge-options` 를 **이름만**
내려주도록 만들어서 화주는 금액을 보지 않는다. **바뀌는 것은 관리자 견적 계산뿐이다.**
🔴 **그 API 에 `rate_pct`·`flat_amount` 를 추가하지 말 것**(28차 비공개 확정).
⚠️ 이미 저장된 견적은 소급 재계산되지 않는다(저장 시점 금액 그대로).

**═══ ⑥ 🔴 적용 순서가 21차와 또 반대다 — 이번은 DB 가 먼저 ═══**
21차는 **지우는** 마이그레이션이라 코드가 먼저였다. 이번은 **더하기**다.
```
  ① DB 먼저   값이 들어가도 배열에 없으면 선택 자체가 안 되므로 무해
  ② 코드 나중  배열에만 먼저 넣으면 드롭다운에 8·15톤이 뜨는데 DB 에 값이 없어
               "선택하신 톤수에 해당하는 운임기준이 없습니다"(15차가 만든 문구)가 뜬다
```
🔴 **매번 어느 쪽이 먼저인지 따질 것 — 한 방향으로 굳어진 규칙이 아니다.**
PR 브랜치 ref 로 `dry-run` → `apply` → `verify` 를 돌렸고 `_migrations` 가 **16행**이 됐다.

**═══ ⑦ 🟢 운영 반영 완료(2026-08-26) — 실측치다 ═══**
```
  ① 구조         차급 11 · 구간 15 · 총 165행 · 가산기준 11행
  ⑤ 거리 역전 0 · ⑥-b 차급 역전 0     ← 8톤·15톤이 중간에 끼어든 뒤에도 0
  ⑥ 표본         1톤 10km 48,000 · 11톤 10km 169,000 · 25톤 400km 초과 954,000  (무변경)
                 8톤 10km 151,000 · 8톤 400km 초과 636,000
                 15톤 60km 215,000 · 15톤 400km 초과 740,000
                 18톤 10km 182,000 · 🔴 18톤 200km 이내 341,000
  ⑦ 가산기준     8톤 30분/30,000/55,000 · 15톤 30분/35,000/65,000 · 없는 차급 0
  ⑦-b 물품특성   파손주의 0.1 / 15,000 · 장척/중량 0.15 / 40,000
  ⑧-h 직원 롤 실측  운임구간 165 (anon 은 전부 0 — 21차 회귀 그대로)
```
⚠️ `_verify.sql` 에 **⑥-b(11차급 차급 역전)** 과 **⑦ 가산기준 없는 차급** 검사를 새로 넣었다.
🔴 **차급을 더 추가하면 ⑥-b 의 `ord(vt, rk)` 목록도 같이 갱신할 것** — 안 하면 새 차급이
검사에서 조용히 빠진다.

**═══ ⑧ 🟢 곁다리 — 18차 잔여 확인 항목이 해소됐다 ═══**
같은 `verify` 로그에 **약관 동의가 실제로 저장된 것**이 찍혔다(49차가 "다음 세션이 확인할
항목"으로 남긴 것). `⑧-c terms / terms-v1 / 1건` · `⑧-b application/terms → 동의행 1 ·
원본 있음 1 · **고아 0**`. 14차에 사용자가 *"행이 생겼다만 보지 말고 join 까지 볼 것"*
이라고 못박은 지점까지 확인됐다.

**(9) 검증 — 로컬 PostgreSQL 16 에 운영과 같은 모양을 만들어 먼저 돌렸다.**
135행 9차급 · 가산기준 9행 · 물품특성 8행을 재현한 뒤 세 파일을 실행했다.
🟢 정상: `UPDATE 15` / `INSERT 30` / `INSERT 2` / `UPDATE 1 + 1` — 단언 전부 통과 /
🟢 **재실행 안전**: ②는 `이미 들어간 8/15톤 행이 있다: 30행`, ③은 `파손주의 현재값이 …가
아니다` 로 멈추고 총행수 165 유지(①은 UPDATE 라 멱등이며 그대로 통과하는 것이 정상) /
🔴 **음성 시험 3건**: 200km 를 321,000 으로 → 차급 역전 검출 / 대기료 행 제거 → 10행 검출 /
15톤 한 칸을 18톤보다 크게 → `차급 역전이 있다: 1개`.
**165조합 회귀** — 마이그레이션 SQL 에서 값을 파싱해 15차 매칭 로직에 태웠다(46·48차 방식):
`165조합 미매칭 0` / `0.0~450.0 step 0.1 × 11차급 49,511건 미매칭 0` /
`15톤 90분 대기료 70,000원` · `8톤 60,000원` · 5톤 50,000 · 25톤 80,000(16·17차 회귀) /
**기존 9차급 135칸을 전수 대조해 18톤 15칸 외에는 한 칸도 안 바뀐 것**을 확인.
**렌더링**: 공개 4개 화면에 `"8톤"`·`"15톤"`·`"11톤"`·`"18톤"`·`"25톤"` **각 0건**,
`/quote` 옵션 6개 · `/apply` 옵션 7개, 🔴 **`/apply` 캡션은 hover 해야 뜨는 툴팁이라**(46차)
실제로 hover 해서 `예: 1톤, 1.4톤, 2.5톤, 3.5톤, 5톤, 5톤 플러스/축` 6종만 나오는 것을 봤다.
**`/admin/rates` 표**는 로그인 제약이 있어 같은 구조의 스텁으로 쟀다 — 열 12개(거리구간 +
차급 11), 순서가 배열과 일치, 1280px 에서 **표 1050px / 카드 1052px**, 카드 내부 가로
스크롤 없음, 페이지 오버플로우 0px. ⚠️ 46차의 9열 실측(1050px)과 같은 폭이다.
**회귀**: `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일** + 보험 자리표시자 경고 그대로 +
`package.json`·`package-lock.json` 변경 **0건**.
⚠️ **`/admin/*` 과 화주포털은 미들웨어·로그인 때문에 렌더링으로 확인하지 못했다**
(46·48·50·51차와 같은 제약).

**(10) ⚠️ 겪은 함정 2가지.** (a) 🔴 **`git checkout lib/constants.ts` 로 음성 시험을
되돌렸다가 커밋하지 않은 본 작업 편집까지 통째로 날렸다.** 음성 시험은 **원본을 따로 복사해
두고 그 사본으로 되돌릴 것**(`cp` → 수정 → `cp` 복원). (b) 🔴 **`pkill -f "next dev -p 3111"`
가 또 자기 자신을 죽였다**(종료코드 144, 44·50차와 같음) — 그 바람에 같은 명령의 뒷부분이
실행되지 않았다. **`kill $(ps aux | grep '[n]ext dev …' | awk '{print $2}')` 처럼 대괄호로
자기 자신을 걸러낼 것.**

**(11) 🔴 하지 않은 것 — 다음 차수가 헤매지 않게.**
**상차 시각 지정 가산 신설**(새 카테고리 설계 + `calc` 의 `selections` 배열 변경이 따라온다) ·
**5톤급 `base_fare` 재검토**(실거래 42건, 마진 중앙값 13.6%로 목표 17% 미달. 표본 8건이라
관찰 대상 — **한 번에 하나씩**) · **anon ③(GRANT 회수)** · **화주포털 화면**(24·25차).
⚠️ **`rate_surcharges` 에 상차 시각 지정 카테고리가 아직 없는 것을 확인했다** — 만들지 않았다.

51차
세션: "anon RLS 정리 ②+④ — 실제로 잠갔다(21차)" — 신규 파일 4개(마이그레이션 3 + 서버
API 1). 커밋 5개. **PR #100 merge됨.**
🔴 **DB 변경 큼 — 이번은 "더하기"가 아니라 "지우기"다.** 정책 21개 삭제 + RLS 13개 켜기.
🟢 **이 차수로 실질적으로 잠겼다** — anon 이 개인정보를 읽지도 쓰지도 못한다.
🔴 **merge 전 리뷰에서 화주포털 희망 톤수가 9종으로 바뀌었다(아래 ⑤).**

**═══ ① 🔴 무엇이 잠겼는가 — 남은 anon 경로는 INSERT 2개뿐이다 ═══**
```
  지운 것 21개
    anon ALL true/true  13개   announcements claims companies customer_accounts
                               customer_locations customer_presets dispatches
                               external_networks invoices orders portal_order_requests
                               quote_items quotes
    anon SELECT true     4개   customer_billing_batches customer_billing_batch_items
                               dispatch_extra_charges staff_accounts
    {public} using(true) 4개   insurance_rate_settings mixed_loading_discount_settings
                               settlement_type_change_logs invoice_amendment_logs
  남긴 것
    🔴 anon INSERT 2개        public_quote_requests · customer_applications
                              → 지우면 /quote · /apply 가 죽는다(원칙 3)
    화주 정책 14개            화주포털
    직원 정책 staff_all_*     19차 16개 + 이번 10개 = 26개
    RLS on + 정책 0개         consents sms_logs dispatch_photos support_access_logs
                              settlement_field_change_logs _migrations + 백업 4
                              → service_role 전용이 **정상**이다. 고치지 말 것
```
🔴 **되돌리는 SQL 이 세 마이그레이션 파일 맨 아래 주석에 있다.** 지운 정책 21개를 **원래
이름 그대로** 다시 만드는 문장이다. 이번은 지우기라 이것이 없으면 복구가 어렵다.
**(1-1) 🟢 운영 반영 완료(2026-08-26) — 실측치다.** `_verify.sql` ⑧-g·⑧-h 로 언제든
재확인할 수 있다. `_migrations` 는 **13행**이 됐다.
```
  ⑧-g  RLS 꺼진 표 0 / anon 정책 2(전부 INSERT 전용) / 직원 26 / 화주 14
  ⑧-h  롤        companies quotes drivers 개인고객 운임구간 직원
       anon          0       0       0       0       0      0   ← 운영 실측
       직원        539       4       2       0     135      4
  ⑧-i  뷰 security_invoker = on
```
⚠️ **`anon` 이 `companies` 539행을 읽던 것이 0이 됐다** — 이 숫자가 이번 차수의 요점이다.

**═══ ② 🔴 착수 전 확인에서 지시서 전제가 또 틀렸다 (3건) ═══**
16·17·19·20차에 이어 **다섯 번째**다. 이 절차를 건너뛰지 말 것.
**(2-1) 🔴 대체 정책이 없는 표가 "1개 더"가 아니라 3개였다.** 그중 문제는
**`customer_presets`** — 48차가 만들었는데 **19차 대상 목록에서 빠져 있었다**(쓰는 화면이
아직 없어 눈에 안 띄었다). 직원 정책 없이 anon 을 지웠으면 22·23차 화면이 붙는 순간
관리자 쪽에서 못 읽는다. 이번에 만들었다.
⚠️ 나머지 둘은 문제 아님 — `customer_applications`(INSERT 전용, 의도된 것) ·
`dispatch_extra_charges`(관리자는 19차에 서버 API 로 옮겼고 화주는 자체 정책 + 컬럼 GRANT).
**(2-2) `{public}` 정책은 4개가 아니라 5개였다.** 다섯째는 `staff_accounts` 의
**"staff can read own row"**(`auth.uid() = id`)인데 **조건이 좁아 anon 에게 열리지 않는다**
(anon 은 `auth.uid()` 가 null). 그래서 **일부러 남겼다** — 지우기 대상은 `using(true)` 인
4개뿐이다.
**(2-3) 🔴 `rate_surcharges` 를 화주포털이 읽고 있었다** — 아래 ④.

**═══ ③ 정책 설계에서 되돌리면 안 되는 것 ═══**
**(3-1) 🔴 RLS 켜기와 정책은 반드시 같은 파일·같은 트랜잭션에.** RLS 를 켜는 순간 정책이
없으면 **전부 거부**다. 파일 사이는 원자적이지 않아서(47차 (F)②) 쪼개면 그 사이에 화면이
죽는다.
**(3-2) 🔴 쓰이지 않는 6개는 RLS 만 켜고 정책을 안 만들었다**(= service_role 전용) —
`activity_logs contacts files profiles rates sales_activities`. 코드 참조 0건 + 행 0건을
둘 다 확인했고, **표를 지우지는 않았다**(금지 9). 마이그레이션 안에 "비어 있지 않으면 멈춤"
단언이 있으니 전제가 틀리면 실행이 중단된다. ⚠️ 나중에 쓸 일이 생기면 정책만 만들면 된다.
⚠️ `profiles` 는 27차가 남긴 레거시다(`quotes.created_by` 가 한때 이 표를 참조했다).
**(3-3) 🔴 삭제 전에 "대체 정책이 있는가"를 단언으로 확인한다.** 50차의 `staff_accounts`
사례처럼 이 장치가 실제로 사고를 막는다. **지우는 마이그레이션에는 반드시 넣을 것.**

**═══ ④ 🔴 `rate_surcharges` — 화주포털이 읽고 있었다 ═══**
`app/customer/request/page.tsx` 가 **차량형태·물품특성·운송시간·왕복/편도** 드롭다운을
채우려고 이 표를 직접 읽고 있었다. 직원 전용 정책만 주면 **발주요청 화면의 드롭다운이 빈다.**
🔴 **그런데 같은 표에 가산 금액(`rate_pct`·`flat_amount`)이 들어 있고 28차가
"전체 운임표·추가비 기준은 비공개"로 확정했다.** 화주에게 SELECT 정책을 주면 금액까지
읽히고, **컬럼 GRANT 로 가르려 해도 직원도 같은 `authenticated` 롤이라 구분되지 않는다**
(16차 `dispatch_extra_charges` 와 똑같은 문제). 그래서 **이름만** 내려주는
`app/api/customer/surcharge-options/route.ts`(service_role)로 옮겼다(사용자 결정 2026-08-26).
🔴 **그 API 에 `rate_pct`·`flat_amount` 를 추가하지 말 것.**
🔴 **관리자 화면은 이 API 를 쓰지 않는다** — 직원 정책으로 표를 직접 읽는다(금액 포함).

**═══ ⑤ 🔴 적용 순서가 19차와 반대다 — 코드가 먼저다 ═══**
19차 마이그레이션은 정책을 **더하기만** 해서 코드보다 먼저 넣어도 무해했다. 이번은
**지우고 RLS 를 켜는 것**이라, 화주포털의 `rate_surcharges` 조회가 서버 API 로 바뀐
**코드가 먼저 배포된 뒤** 넣어야 한다. 그래서 **merge → Vercel 배포 확인 → apply** 로 했다.
🔴 **다음에 "지우는" 마이그레이션을 쓸 때도 어느 쪽이 먼저인지 매번 따질 것** — 한 방향으로
굳어진 규칙이 아니다.

**═══ ⑥ 발주요청 희망 톤수 6종 → 9종 (merge 전 리뷰) ═══**
사용자가 merge 전에 *"운임기준표를 보완하면서 5톤 이상도 조사했는데 발주요청에서 빠지면
안 되는 것 아닌가"* 라고 짚었고 **맞는 지적이었다.**
17차(46차 세션)가 `/customer/request` 를 "화주 입력"으로 묶어 공개 화면과 같은 6종을 줬는데:
① **로그인한 화주만 보는 화면**이라 불특정 다수 대상 광고가 아니다 — 금지 표현 기준
(32차·12차)의 적용 대상이 아니다 ② 운임기준표(135행 9차급)·화주포털 조회·견적서에는 **이미
9종이 나온다** — 18톤으로 운송한 화주가 **결과는 보는데 다음 요청은 그 톤수로 못 넣었다**
③ 32차 확정 문구 자체가 **"1톤부터 5톤 이상까지"** 로 상한을 닫지 않는다.
🔴 **공개 4개 화면(`/`·`/vehicles`·`/quote`·`/apply`)은 그대로 6종이다. 되돌리지 말 것.**
특히 **`/apply` 는 배열을 `join(", ")` 으로 안내 문구 본문에 그대로 뿌린다** — 46차가 배열을
둘로 나눈 직접적인 이유이고, 뚫리면 공개 화면 본문에 `"25톤"` 이 찍힌다.
🟢 **렌더링으로 확인했다** — `/quote`·`/apply` 옵션 6개, 본문 대형 3종 0건, `/apply` 캡션은
**hover 해야 뜨는 툴팁이라**(46차 기록) 실제로 hover 해서 6종만 나오는 것을 봤다.
⚠️ **18톤 견적은 지금 실제보다 약 13% 비싸다**(46차 실측). 가산기준 조정(×0.89)이 미정
차수라, **화주가 스스로 18톤을 요청하면 비싼 견적을 받는다** — 노출 빈도가 올라간 만큼
가산기준 조정을 앞당길지 판단이 필요하다(사용자에게 보고함).
⚠️ 46차 기록의 4갈래 표에서 **"화주 입력 → 6종" 행만 바뀐 것**이다. 나머지 3행은 그대로.

**═══ ⑦ 뷰 — ② 이후에는 유일하게 열린 문이 된다 ═══**
`customer_billing_batch_candidates` 는 owner 가 `postgres` 라 **하위 표의 RLS 를 우회**한다.
②로 `invoices` 를 잠가도 anon 이 뷰에 GRANT 를 갖고 있으면 그대로 읽힌다. 그래서
**`security_invoker = on`** 으로 켜서 호출자 권한으로 실행되게 했다(운영 PostgreSQL **17.6**).
⚠️ **저장되는 값은 `on` 이지 `true` 가 아니다** — 단언에서 `option_value::boolean` 으로 캐스팅할 것.
⚠️ **anon GRANT 자체는 회수하지 않았다** — ③ 차수 범위(금지 7). RLS 가 막으므로 실질적으로 닫혔다.

**(8) 검증 — 로컬 PostgreSQL 16 에 운영과 같은 모양을 만들어 먼저 돌렸다.**
운영과 같은 정책 구성(anon 19 / 화주 14 / 직원 16 / public 5)을 재현한 뒤 세 롤을 각각 쟀다.
```
  롤        companies quotes drivers 운임구간 staff_accounts
  anon          0       0       0       0         0        ← 개인정보 전부 0
  직원          2       2       1       1         4
  화주      자기회사  자기회사    0       0         0
```
🟢 **anon INSERT 2개는 실제로 넣어봐서 되는 것을 확인**(확인 후 rollback) /
🔴 **anon 쓰기 차단은 `DELETE` 로 8개 표에 실제로 시도해 영향 행수 0** — ⚠️ **컬럼 이름을
몰라도 되고 영향 행수가 그대로 증거가 되므로 INSERT 보다 DELETE 로 재는 편이 낫다**
(처음에 `insert into … (vehicle_type)` 로 썼다가 로컬 스키마 차이로 헛돌았다) /
🟢 뷰는 anon 0행·직원 전부 / 🔴 **음성 시험 3건** — anon 정책을 다시 주면
`anon 이 아직 rate_distance_tiers 를 읽는다: 1행`, 직원 정책을 빼면
`직원 세션에서 drivers 가 안 보인다: 전체 1행 / 직원에게 0행` 으로 정확히 멈춘다.
**회귀**: `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일** + `package.json`·
`package-lock.json` 변경 **0건**.
⚠️ **`/admin/*` 과 화주포털은 미들웨어·로그인 때문에 렌더링으로 확인하지 못했다**
(46·48·50차와 같은 제약). **DB 는 단언과 롤별 실측으로, 공개 화면은 렌더링으로 증명했고
남은 것은 로그인이 필요한 화면이 "어떻게 보이는가"뿐이다.**

**(9) ⚠️ 겪은 함정 2가지.** (a) 🔴 **주석에 쓴 상수 이름이 내 단언에 걸렸다** —
`VEHICLE_TYPES_PUBLIC` 을 경고 주석에 언급했더니 `s.count(...) == 2` 단언이 3으로 실패했다.
**12차가 기록한 "주의 표현을 경고하는 주석 자체가 매치되는 오탐"과 같은 것**이다.
(b) `reloptions` 의 `security_invoker` 값은 `on` 이다(위 ⑦).

**(10) 🔴 남은 것 — ③(anon GRANT 회수).** RLS 가 이미 막고 있으므로 **추가 방어이며 급하지
않다.** 함께 정할 것: 뷰의 anon GRANT 회수 / `dispatch_extra_charges` 의 컬럼 GRANT 정리.
⚠️ **`dispatch_extra_charges` 에는 여전히 직원 정책이 없다**(19차 그대로) — `authenticated`
전체 SELECT 를 주면 화주가 차주 지급액을 읽는다.

50차
세션: "관리자 질의를 직원 세션으로 — anon RLS 정리 ①(19차)" — 신규 파일 3개
(마이그레이션 SQL · 서버 API · 조회 헬퍼). 커밋 2개. **PR #99 merge됨 — 리뷰 지적은 없었다.**
🔴 **DB 변경 있음**(정책 16개 + 함수 1개 신설. **기존 정책은 하나도 안 지웠다**).
🟢 **세션이 직접 반영했다** — dry-run → apply(1회 실패 후 통과) → verify.
🔴 **지시서 전제가 틀려서 범위를 바꿨다 — 아래 (1)을 반드시 읽을 것.**

**(0) 🔴 차수 이동.** 49차 조사 결과를 보고 **사용자가 anon 정리를 가산기준보다 먼저 하기로
확정(2026-08-26)**했다. 그래서 **19차 = anon 정리 ①**이고 **가산기준은 미정으로 밀렸다**
(21·22차 화주포털 화면 예고는 그대로). ⚠️ **랜딩 이미지 5종도 보류됐다** — 랜딩페이지를
Claude Design 에서 다시 잡은 뒤 넣기로 했다(2026-08-26). Go-Live 차단 목록에서 빠진 것이
아니라 **순서가 뒤로 간 것**이다.

**═══ ① 🔴 지시서의 핵심 전제가 틀렸다. 착수 전 조사가 아니었으면 관리자 화면이 통째로 막혔다 ═══**
지시서 0장은 *"anon 정책이 `USING(true)` 로 열려 있으므로 질의가 `authenticated` 롤로
바뀌어도 그대로 통과한다. 즉 화면이 깨질 수가 없다"*고 했다. **아니다.**
🔴 **RLS 정책은 롤별로 갈린다.** anon 정책은 `TO anon` 이라 `authenticated` 요청에는
**아예 적용되지 않는다.** 실제 운영 DB 를 읽어 확인한 것:
```
  화주 전용 정책만 있음 (10개 테이블)  → 조건이 "화주 본인 회사"라 직원은 0행.
    companies quotes orders dispatches invoices quote_items customer_locations
    customer_accounts portal_order_requests announcements
    게다가 대부분 SELECT 전용이라 저장도 안 된다.
  anon 정책만 있음 (5개)              → 맞는 정책이 없어 RLS 기본값인 거부.
    claims external_networks customer_billing_batches customer_billing_batch_items
    public_quote_requests
  컬럼 GRANT 제한 (1개)               → dispatch_extra_charges. 권한 오류.
```
🔴 **그래서 ①(클라이언트 전환)과 ②(정책)는 분리할 수 없다.** 사용자 확인 후 한 차수로
합쳤다. **다음에 "지시서대로 ①만 하자"는 말이 나오면 이 문단을 먼저 볼 것.**
⚠️ 조사 방법은 48·49차와 같다 — 브랜치에 조사용 SQL 을 올려 워크플로를 **그 브랜치 ref**
로 돌렸다. 확인 후 `_verify.sql` 을 원본으로 되돌리고 임시 커밋은 PR 전에 정리했다.

**═══ ② 직원 전용 정책 16개 ═══**
**(2-1) 🔴 기존 정책을 하나도 지우지 않았다.** RLS 정책은 permissive 라 **OR 로 합쳐진다.**
anon 정책 19개(현행 동작)와 화주 정책 14개(화주포털)는 그대로 두고 직원용을 **한 겹 더한
것**이다. 그래서 이 마이그레이션만으로는 **노출이 넓어지지도 좁아지지도 않고**, 되돌릴 때도
`staff_all_*` 만 지우면 된다. 🔴 **다음 차수에서 anon 정책을 교체할 때도 "지우기 전에
대체 정책이 이미 있는가"를 먼저 확인할 것.**
**(2-2) 🔴 조건은 반드시 "재직 직원"이어야 한다.** `authenticated` 롤은 **화주포털 계정도
같이 쓴다.** 단순 `authenticated` 로 두면 화주가 관리자 데이터를 읽는다.
```sql
  exists (select 1 from staff_accounts where id = auth.uid() and status = 'active')
```
🔴 **`public.is_active_staff()` 는 security definer 다. 빼지 말 것** — `staff_accounts`
자체에도 RLS 가 걸려 있어서, 정책 안에서 그냥 서브쿼리로 조회하면 그 조회에 다시 RLS 가
걸려 **조용히 false** 가 될 수 있다.
**(2-3) 🔴 `staff_accounts` 도 대상에 넣었다 — 운영 apply 가 실제로 이걸 잡아냈다.**
첫 apply 가 `staff_accounts 가 안 보인다: 전체 4행 / 직원에게 1행` 으로 멈췄다. 이 표의
`{public}` 정책이 `using(true)` 가 아니라 **본인 행만**(`id = auth.uid()`)이었기 때문이다.
관리자 화면은 **다른 직원의 이름**을 읽어야 한다(등록/최종수정 표시 `ProcessedByFooter` ·
직원 계정 관리 · 문자 이력의 보낸 사람). 🟢 **단언 덕분에 아무것도 반영되지 않고 통째로
롤백됐다** — 47차가 정한 "안전장치는 `select` 가 아니라 `raise exception` 으로"가 실제로
값어치를 한 첫 사례다.
**(2-4) ⚠️ `customer_billing_batch_candidates` 는 뷰라 정책을 걸 수 없다.** GRANT 로만
통제되어서 anon 과 같은 수준을 authenticated 에도 줬다. 🔴 **③(anon GRANT 회수) 차수에서
`security_invoker` 로 바꾸거나 서버 API 로 옮길지 함께 정할 것** — 지금은 화주도 읽을 수
있는 상태다. ⚠️ **`dispatch_extra_charges` 에는 일부러 정책을 안 만들었다**(아래 ③).

**═══ ③ dispatch_extra_charges — 16차 보호가 무너질 뻔했다 ═══**
🔴 16차가 차주 지급액(`driver_payout_amount`)을 화주에게 감추려고 **`authenticated` 롤의
전체 SELECT 를 회수하고 안전한 8개 컬럼만 다시 GRANT** 해뒀다. **직원도 이제 authenticated
가 되므로 컬럼 GRANT 로는 둘을 구분할 수 없다.**
🔴 **`authenticated` 에 전체 SELECT 를 되돌려주지 말 것** — 그 순간 화주가 자기 건의 차주
지급액을 **DB 수준에서** 읽는다(프론트 화이트리스트만 남는 방어가 된다). 그래서 **관리자
조회만** service_role 서버 API 로 옮겼다(사용자 결정 2026-08-26).
신설 `app/api/admin/dispatch-extra-charges/list/route.ts` + `lib/fetchDispatchExtraCharges.ts`.
🔴 **관리자 화면에서 `supabase.from("dispatch_extra_charges")` 로 직접 조회하지 말 것** —
호출부 6곳을 전부 헬퍼로 바꿨다. ⚠️ 등록·취소는 이미 서버 API(register/cancel)라 무변경.
⚠️ **화주포털 조회는 손대지 않았다** — 화주는 지금도 컬럼 GRANT 로 8개만 본다(16차 그대로).
⚠️ **`lib/dispatchExtraCharges.ts`(라벨 상수)와 `lib/fetchDispatchExtraCharges.ts`(조회)는
다른 파일이다.** 내가 처음에 전자를 덮어써서 날렸다가 복구했다 — **이름이 비슷하니 주의.**

**═══ ④ 클라이언트 전환은 파일 하나였다 ═══**
🟢 **지시서는 40여 개 화면을 하나씩 옮기는 것을 전제했지만, 30개 파일이 전부
`lib/supabaseClient.ts` 싱글턴 하나를 공유**하고 있어서 그 파일만 바꾸면 30개가 함께
전환된다. 🟢 그리고 `@supabase/ssr` 의 `createBrowserClient` 는 **브라우저에서 모듈
싱글턴**이라(소스 확인), `supabaseAdminAuth`(로그인용)와 **literally 같은 인스턴스**가 된다
— GoTrue 가 두 개 생기지 않는다(실측 `supabase === supabaseAdminAuth` → `true`).
🔴 **원칙 24번("두 클라이언트를 섞지 말 것")의 전제가 이번에 바뀌었다.** 예전엔
`supabaseClient` 가 anon·localStorage 였지만 이제 둘 다 같은 쿠키 세션이다. 로그인·로그아웃은
여전히 `supabaseAdminAuth` 이름으로만 부른다(이름이 곧 용도 표시).
**(4-1) 대상 수 — 지시서가 요구한 전수 집계.** 정본 import 30개 =
관리자 화면 20 + 관리자 공용 컴포넌트 3(`TopNav`·`ProcessedByFooter`·`MonthlyBillingBatchPanel`)
+ 관리자용 lib 헬퍼 7. **공개 화면 0개 · 화주포털 0개.**
🔴 **공개 화면은 애초에 이 클라이언트를 안 쓴다** — 세션이 없어 anon 으로 붙는 것이
**의도된 동작**이고 "정리" 대상이 아니다. ⚠️ `TopNav` 는 공개 화면에도 렌더링되지만
`isPublicPath` 일 때 질의를 건너뛴다.
**(4-2) 🟢 화주포털은 범위 밖이었다** — `lib/supabaseCustomerClient.ts` 는 별도이고,
로그인(`signInWithPassword`)과 데이터 조회가 **같은 클라이언트**라 **예전부터 화주 세션이
실려 이미 `authenticated` 로 붙고 있었다.** 지시서 2장 6번이 분기라고 한 지점인데 조사
결과 손댈 것이 없었다.
**(4-3) ⚠️ `lib/settlementTypeChangeLog.ts` 는 죽은 파일이다** — 이 클라이언트를 import
하지만 **어디서도 안 쓰인다**(13차에 `settlementFieldChangeLog.ts` 로 대체됨).
금지 15번대로 "정리"하지 않고 뒀다.

**═══ ⑤ 곁다리로 드러난 것 ═══**
⚠️ **`app/admin/quotes/page.tsx` 의 "공개문의 → 견적 전환" UPDATE 가 지금까지 조용히
0행이었다.** `public_quote_requests` 의 anon 정책이 INSERT 전용이라, `연락완료` + `quote_id`
연결이 **한 번도 반영된 적이 없다**(원칙 22번이 경고한 "에러 없이 조용히 실패"). 직원 정책이
생기면서 **이제 실제로 반영된다.** 고치려고 한 것이 아니라 부수 효과다.

**(6) 검증 — 실제 요청 헤더를 봤다.** "코드를 바꿨으니 됐다"로 넘기지 않았다.
목 Supabase 서버(node http)를 띄우고 임시 스텁 페이지 + Playwright 로 나가는
`Authorization` 헤더를 디코딩했다.
🟢 로그인 전 = **anon key 그대로** / 로그인 후 = **`role: authenticated`, `sub` = 직원 uuid**
/ 새로고침 후 유지 / 🔴 **만료된 토큰으로 시작해도 갱신 요청이 실제로 나가고 새 토큰의
`exp` 가 미래**였다. middleware 에 **만료된 쿠키**를 직접 보내니 토큰 갱신을 호출하고
`Set-Cookie` 로 갱신본을 돌려줬다(지시서 3-1이 "가장 놓치기 쉬운 지점"이라 한 것).
**DB 쪽은 마이그레이션 안의 단언이 증명한다** — 재직 직원을 흉내내어
(`set_config('request.jwt.claims', …)` + `set local role authenticated`) 관리자 화면이 읽는
**26개 테이블 전부**의 행수를 소유자 기준과 비교한다. 하나라도 다르면 통째로 롤백된다.
🔴 **이 흉내내기 기법을 기억할 것** — Supabase RLS 를 psql 에서 시험하는 표준 방법이고,
②·④ 차수에서 계속 쓴다. ⚠️ **`set local` 은 트랜잭션 안에서만 듣는다** — psql 자동커밋
상태로 한 줄씩 실행하면 조용히 postgres 권한으로 돌아 **시험이 통째로 무의미해진다**
(실제로 한 번 속았다. `begin; … commit;` 으로 감쌀 것).
**로컬 PostgreSQL 16** 에 운영과 같은 모양(anon 19 / 화주 14 / public 5)을 만들어 세 롤을
각각 쟀다 — anon 은 전과 동일 / **화주는 자기 회사 1건, claims·staff_accounts 는 0** /
직원은 전부. **음성 시험**도 했다(정책 하나를 지우면 `전체 2행 / 직원에게 0행` 으로 정확히 멈춤).
**회귀**: `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일** + 보험 경고 그대로 +
**`package.json`·`package-lock.json` 변경 0건**(`@supabase/ssr` 이 **이미 설치돼 있었다** —
관리자 로그인이 쓰고 있었으므로 지시서 완료조건 16의 "추가 하나뿐"이 아니라 **0건**이다).
⚠️ **`/admin/*` 은 미들웨어·로그인 때문에 이 환경에서 렌더링으로 확인하지 못했다**
(46·48차와 같은 제약). **DB 는 단언으로 증명됐고 남은 것은 "화면에 어떻게 보이는가"뿐이다.**

**(7) 🔴 오해하지 말 것 — 아직 잠기지 않았다.** 이번 차수는 **권한을 조이지 않았다.**
anon 정책 19개가 그대로 열려 있고 `anon` 은 여전히 47개 테이블 전부에 DML GRANT 를 갖고
있다. **화면이 도는 것은 anon 정책 덕분이 아니라 새로 만든 직원 정책 덕분이다.**
남은 순서와 이유:
```
  ② anon 정책 → authenticated + 직원 조건 으로 교체 (~30개)
  ④ RLS 꺼진 13개 켜고 직원 전용 정책
  ③ anon GRANT 회수   ← 마지막. 공개 사이트가 진짜 필요한 것은
                        public_quote_requests · customer_applications INSERT 둘뿐이라,
                        앞의 둘을 끝낸 뒤 한 번에 회수하는 편이 안전하다.
```
⚠️ **④ 대상 13개**(`activity_logs contacts drivers files individual_customer_addresses
individual_customers profiles rate_distance_tiers rate_surcharges rate_vehicle_extra_fees
rates sales_activities vehicles`)는 RLS 를 켜는 순간 **정책이 없으면 전부 거부**가 되므로
**정책과 반드시 같은 파일에** 넣을 것.

**(8) ⚠️ 검증 중 겪은 함정 3가지.** (a) 🔴 **`pkill -f "node /tmp/mock.js"` 가 또 자기 자신을
죽였다**(44차와 같음, 종료코드 144) — 그 바람에 같은 명령의 뒷부분(heredoc)이 실행되지 않아
파일이 안 만들어졌고 원인을 한참 찾았다. **포트를 바꿔 새로 띄울 것.**
(b) 🔴 **`raise exception` 의 `%%` 는 리터럴 `%` 다** — 인자 3개에 자리표시자 2개가 되어
`too many parameters specified for RAISE` 가 났다. 로컬 시험이 아니었으면 운영 apply 에서
났을 것이다. (c) **파일을 새로 만들기 전에 같은 이름이 있는지 확인할 것**(28차와 같은 사고
재발 — 위 ③ 참고).

49차
세션: "이용약관 동의 + anon RLS 조사(18차)" — 신규 파일 1개
(`components/PublicConsentFields.tsx`). 커밋 4개(그중 1개는 사용자 결정에 따른 되돌리기).
**DB 스키마 변경 없음 · 마이그레이션 없음.** **PR #98 merge됨.**
🔴 **Go-Live 차단이던 이용약관 동의가 해소됐다.** 🔴 **그리고 조사에서 지시서가 예상한
것보다 큰 것이 나왔다 — 아래 (1)을 반드시 읽을 것.**

**═══ ① 🔴 anon RLS 조사 — 개인정보가 anon 으로 열려 있다. 예. ═══**
**(1-0) 고치지 않았다. 조사만 했고 정책은 하나도 안 바꿨다**(지시서 금지사항 1번,
완료조건 4). 잘못 조이면 **관리자 화면 전체가 막힌다** — 원칙 2번대로 관리자 화면이
anon key 위에서 돌기 때문이다.
**(1-1) 🔴 지시서가 물은 것보다 구멍이 크다.** 지시서는 "anon 에 `USING(true)` 가 걸린
정책"을 물었는데, **더 큰 것은 RLS 자체가 꺼진 테이블 13개**였다.
```
  activity_logs  contacts  drivers  files  individual_customer_addresses
  individual_customers  profiles  rate_distance_tiers  rate_surcharges
  rate_vehicle_extra_fees  rates  sales_activities  vehicles
```
🔴 **RLS 가 꺼져 있으면 정책이 아니라 GRANT 가 유일한 관문**인데, `anon` 은 **47개 테이블
전부에 `SELECT·INSERT·UPDATE·DELETE·TRUNCATE`** 를 갖고 있다. 즉 이 13개는 **anon key
만으로 읽기도 쓰기도 삭제도 된다.** 그중 `drivers`(**차주 성명·연락처·차량번호**)와
`individual_customers`(**개인고객 성명·연락처·이메일**)가 들어 있다.
⚠️ `rate_*` 는 개인정보는 아니지만 **운임 원가표가 열려 있고 쓰기까지 가능**하다.
**(1-2) anon 무조건 허용(`true`) 정책 19개 / 17개 테이블.**
`announcements`·`claims`·`companies`·`customer_accounts`·`customer_locations`·
`customer_presets`·`dispatches`·`external_networks`·`invoices`·`orders`·
`portal_order_requests`·`quote_items`·`quotes` 가 전부 `ALL / true / true`.
읽기만 true: `customer_billing_batches`·`customer_billing_batch_items`·
`dispatch_extra_charges`·`staff_accounts`. INSERT 만 true(**의도된 것**, 원칙 3):
`customer_applications`·`public_quote_requests`.
⚠️ **`{public}` 롤 정책이 4개 더 있다**(`insurance_rate_settings`·
`mixed_loading_discount_settings`·`settlement_type_change_logs`·`invoice_amendment_logs`).
`public` 은 anon·authenticated 를 모두 포함해 **더 넓다.**
**(1-3) 그 테이블들의 개인정보 컬럼 75개.** `companies`(담당자명·이메일·주소·사업자번호)·
`customer_accounts`·`customer_applications`(신청자 전원)·`quotes`/`orders`/`dispatches`/
`portal_order_requests`(**상·하차지 담당자 성명·연락처**, 게스트 정보, 외부차주 성명·연락처)·
`customer_locations`(**20차가 추가한 `contact_name`·`contact_phone` 포함**)·
`public_quote_requests`·`staff_accounts`(**직원 이름·이메일·발신번호**).
**(1-4) 🟢 올바른 상태인 것 10개** — RLS on + 정책 0개(service_role 전용):
`consents`·`sms_logs`·`dispatch_photos`·`support_access_logs`·
`settlement_field_change_logs`·`_migrations` + 백업 스냅샷 4개.
**(1-5) 🔴 권장안은 ③(authenticated + 재직 직원 조건), 2~3 세션.**
**이유는 관리자 인증이 이미 있기 때문이다.** 직원 계정 재구조화 2단계에서 관리자 로그인이
**Supabase Auth 개별 계정**으로 이미 전환됐다. 다만 **인증이 페이지 단(`middleware.ts`)에서만
쓰이고 화면의 DB 질의는 여전히 anon key 로 나간다**(`lib/supabaseClient.ts`). 그래서 ③은
"인증을 새로 만드는 일"이 아니라 **이미 있는 세션을 질의에도 쓰게 하는 일**이다.
① 관리자 화면을 전부 서버 API/service_role 로 옮기기는 40+ 화면·API 수십 개라 과하고,
② 민감 컬럼 분리는 침습적인 데다 **RLS 꺼진 테이블 문제를 아예 못 고친다.**
🔴 **③의 함정** — `authenticated` 롤은 **화주포털 계정도 같이 쓴다.** 조건이 단순
`authenticated` 면 안 되고 `exists (select 1 from staff_accounts where id = auth.uid()
and status = 'active')` 처럼 **직원임을 확인**해야 한다.
단계: ① 관리자 데이터 클라이언트를 쿠키 세션 클라이언트로 전환 → ② anon 정책을
authenticated + 직원 조건으로 교체(~30개) → ③ **anon GRANT 회수**(공개 사이트가 진짜
필요한 것은 `public_quote_requests`·`customer_applications` **INSERT 둘뿐**) →
④ RLS 꺼진 13개에 RLS 를 켜고 직원 전용 정책.
⚠️ **지금은 노출량이 작다**(신청 6·계정 3·문의 1). **공개 후에는 계속 늘어난다.**

**═══ ② 이용약관 동의 (18차 본작업) ═══**
**(2-1) 🔴 왜 5차부터 미사용이었나 — 문구가 원인이었다.** `TERMS_VERSION` 은 5차에 정의된
뒤 계속 안 쓰였는데, 원인은 화면 동의 문구가 **"개인정보 수집·이용"으로만 끝나고 이용약관을
언급하지 않은 것**이었다. 14차 원칙("문구에 없는 동의를 기록하지 않는다")대로 **문구를 먼저
넣고 나서** `CONSENT_TYPES_BY_SOURCE` 에 `terms` 를 추가했다. 🔴 **순서를 뒤집지 말 것** —
먼저 넣으면 받지 않은 동의를 기록하게 되어 그 기록이 거짓이 된다.
**(2-2) 🔴 받는 화면은 `/apply` 하나뿐이다. `/quote` 에는 넣지 않는다(사용자 결정 2026-08-26).**
⚠️ **내가 처음엔 넣었다가 되돌렸다.** 지시서가 "넣는 쪽을 권장하되 변호사 검토 항목으로
표시하라"고 했고 나도 그렇게 했는데, 사용자가 **빼는 것으로 확정**했다. 견적 문의 자체는
계약이 아니기 때문이다. 🔴 **다시 넣지 말 것** — 넣으려면 그 화면 문구부터 고쳐야 하고
그건 별도 판단이다. 근거를 `lib/legalInfo.ts`·`lib/consent.ts` 주석에 남겼다.
⚠️ 되돌릴 때 **`git checkout origin/main -- <두 파일>` 로 바이트 단위로** 되돌렸다 —
손으로 지우면 14차가 고친 `/quote` 의 **"1년간 보관 후 파기"** 문구가 미세하게 달라질 수 있다.
**(2-3) 🔴 체크박스는 두 개다. 합치지 말 것.** 개인정보보호법 제22조 1항이 **각각의 동의
사항을 구분해 알리고 각각 동의를 받도록** 하고 있다. 나중에 "UI 정리"로 묶으면 법적 요건이
깨진다. **서버 게이트도 항목별로 둘**이고 `!== true` **엄격 비교**다(`!x` 로 쓰면 문자열
`"true"` 가 통과한다). 신설 `components/PublicConsentFields.tsx` 가 이 규칙을 붙들고 있다.
⚠️ 그 컴포넌트를 쓰는 곳은 **`/apply` 하나뿐**이다(원래 두 화면이었다가 위 (2-2)로 하나가 됨).
공용화 명분은 약해졌지만 **규칙과 문구를 한 곳에 두려고 남겼다** — 화면 JSX 로 흩으면
다음에 조용히 어긋난다.
**(2-4) 🔴 약관 요약 문장은 `lib/legal/terms.ts` 의 실제 조 제목에서 파생됐다.**
> 약관에는 운송계약의 성립, 운임의 산정과 추가비, 화물의 상·하차와 인도, 손해배상과
> 면책에 관한 내용이 있습니다.
근거: 제8조(운송계약의 성립)·제10조(운임의 산정)·제11조(추가비)·제15조(상·하차)·
제17조(인도 및 검수)·제19조(손해배상, **면책은 34차에 6항으로 통합됨**).
⚠️ **지시서 초안을 조문과 대조해 두 군데를 조였다** — `추가 비용`→**`추가비`**,
`화물의 인수·인도`→**`화물의 상·하차와 인도`**. 🔴 **조문이 개정되면 이 요약도 같이 볼 것.
어긋나면 요약을 고치고 조문은 두라**(지시서 금지사항 2번). `lib/legal/` 은 한 글자도 안 건드렸다.
⚠️ **문구는 초안이며 변호사 검토 대상이다.**
**(2-5) 저장 — 스키마 변경 없음.** `consent_type='terms'` / `version=`**`TERMS_VERSION`**
(🔴 `PRIVACY_POLICY_VERSION` 을 재사용하지 말 것 — 5차가 개정 주기가 달라 일부러 나눈 상수다) /
`subject_type='application'` · `source='/apply'` 는 기존 그대로. `recordConsents` 가
**privacy + terms 두 행**을 남긴다. 동의 기록 실패 시 접수 건 삭제(14·47차와 같은 롤백).
**(2-6) 🔴 소급은 하지 않았다.** 기존 신청 **6건**·계정 **3건**에 약관 동의 기록이 없다.
실제로 받지 않았으므로 **소급 INSERT 는 거짓 기록**이 된다. 행 수만 남기고 처리 방법은
사용자가 정한다. (참고: `public_quote_requests` 1건 · `consents` 3건.)

**═══ ③ `_verify.sql` 보강 ═══**
47차·48차가 남긴 후속 제안을 반영했다. 🔴 **읽기 전용을 유지했다.**
`⑧-b` **`consents` 를 세 원본 테이블에 join — 고아 행 수**(14차에 사용자가 "행이 생겼다만
보면 안 되고 join 까지 볼 것"이라고 못박은 지점) / `⑧-c` 동의 항목별 건수 /
`⑧-d` `customer_locations` 컬럼(**`name`·`note` 중복 0 확인 포함**) / `⑧-e`
`customer_presets` RLS·정책·CHECK / `⑧-f` `상하차방식` 8행·`도크`.

**(4) 검증.** 🟢 **로컬 PostgreSQL 16 에 운영과 같은 모양을 만들고 `_verify.sql` 을 실제로
돌렸고, ⑧-b 가 일부러 심어둔 고아 행을 잡아냈다**(`application/terms → 고아_행 1`) —
형식만 도는 검사가 아니라는 증거다.
**서버 게이트 실측**(공개 라우트라 우회 사본이 필요 없다 — 47차와 다른 점): `/apply` 는
둘 다 없음·약관만·개인정보만·**문자열 `"true"`** 전부 400, 둘 다 true 면 통과해 DB 까지 감.
`/quote` 는 되돌린 뒤 개인정보만으로 통과하는 것을 재확인.
**화면단 실측**(Playwright): 세 경우 모두 **API 요청 0건**으로 막히고 각각 맞는 메시지.
⚠️ **처음엔 브라우저 기본 검증(`required`)이 먼저 걸려 React 핸들러까지 가지 않았다** —
`.error-box` 가 안 뜨고 요청도 0이라 "막혔다"로 착각하기 쉽다. **`form.noValidate = true`
로 기본 검증을 끄고** 다시 재서 **동의 검증이 막는 것**을 정확히 겨냥했다. 다음에도 쓸 것.
**렌더링**: `/apply` 체크박스 **2개**(약관 라벨·요약·거부권·「약관 보기」·「처리방침 보기」) /
`/quote` **1개**(약관 문구 0건, "1년간 보관 후 파기" 그대로).
**회귀**: `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일** + `lib/legal/`·`next-env.d.ts`·
`tsconfig.json`·`package*.json` 변경 **0건** + 기존 개인정보 문구 삭제된 줄 **0**.
⚠️ **동의 2행이 실제로 저장되는 것은 아직 확인하지 못했다** — 배포본에서 `/apply` 신청을
한 건 넣으면 `verify` ⑧-c 에 `terms / terms-v1` 이 뜬다. **다음 세션이 확인할 항목.**
**(5) ⚠️ 조사 방법은 48차와 같다** — 브랜치에 조사용 SQL 을 올려 워크플로를 **그 브랜치
ref** 로 돌렸다. 확인 후 `_verify.sql` 을 원본으로 되돌리고 임시 커밋은 PR 전에 정리했다.

48차
세션: "화주포털 저장소 기반(20차 3-1·3-2·3-3)" — 신규 파일 3개(마이그레이션 SQL) +
`lib/loadingMethods.ts` 7종 → 8종. 커밋 3개. **PR #97 merge됨 — 리뷰 지적은 없었다.**
🔴 **DB 변경 있음**(컬럼 3개 · 테이블 1개 · 행 1개). 🟢 **SQL을 세션이 직접 돌렸다** —
47차에 만든 워크플로의 첫 실사용이며 `dry-run` → `apply` → `verify`까지 전부 확인했다.
**이것으로 20차가 전부 끝났다**(3-4는 47차).

**(0) 🟢 47차 자동화가 실제로 동작했다 — 이제 이것이 기본 절차다.**
`dry-run`이 대기 3건을 잡고 → `apply`가 22초에 3파일을 반영하고 → `verify`가 결과를 보여줬다.
**사용자는 아무것도 하지 않았다.** 마이그레이션 파일만 쓰면 나머지는 세션이 한다.
⚠️ `apply`가 2분 넘게 `in_progress`로 보일 수 있으나 실제 실행은 22초였다 — GitHub Actions의
큐 대기 시간이다. 조급하게 두 번 돌리지 말 것(이력 표가 막아주긴 한다).

**(1) 🔴 착수 전 확인에서 지시서 전제가 틀린 것이 나왔다 — 멈추고 사용자 확인을 받았다.**
지시서 2장이 "다르면 임의로 판단하지 말고 보고 후 대기"라고 못박은 바로 그 경우다.
`customer_locations`는 지시서가 적은 6컬럼이 아니라 **8컬럼**이었고, 추가하려던 5개 중
**둘이 이미 있었다.**
```
  지시서 name → 🔴 location_name 이 이미 있음   (재사용)
  지시서 note → 🔴 notes 가 이미 있음           (재사용)
  address_detail / contact_name / contact_phone → 없음 (추가)
```
🔴 **`location_name`은 죽은 컬럼이 아니다** — `app/admin/companies/[id]/page.tsx:412`와
`app/admin/quotes/page.tsx:380`이 **이미 조회하고 있다.** `name`을 새로 만들었으면 관리자
화면은 `location_name`을, 화주포털은 `name`을 쓰게 되어 **같은 배송지에 이름이 두 개**
생겼을 것이다(원칙 27번이 경고한 사고). **사용자가 "기존 컬럼 재사용"으로 확정(2026-08-26).**
🔴 **다음에 지시서 표만 보고 `name`·`note`를 추가하지 말 것** — 마이그레이션 파일 안에
**그 두 이름이 있으면 실행이 멈추는 단언**을 넣어뒀다. 단언을 지우지 말 것.

**(2) 나머지 확인은 전부 지시서 전제와 일치했다(다시 조사하지 말 것).**
`portal_order_requests` **34컬럼** — 시안이 요구한 `waiting_minutes`·`waypoint_count`·
`transport_time`·`trip_type`이 **전부 이미 있다**(추가 불필요) / `rate_surcharges`
`상하차방식` 7행이고 `도크` 없음 / `lib/loadingMethods.ts` 7종 / `invoices`에
`payment_received`(boolean)+`payment_received_date` 있음(22차 대비 확인만) /
`customer_presets` 없음. ⚠️ `portal_order_requests`는 **1행**이었다(지시서는 0행) —
47차에 사용자가 넣은 테스트 발주요청이며 이번 3건은 이 테이블을 안 건드린다.
🟢 **RLS 정책 전문을 실제 DB에서 읽어 `customer_presets`에 그대로 옮겼다** —
`anon ALL true/true` + `authenticated ALL (company_id IN … is_active = true)` 두 개다.

**(3) `customer_presets` — 되돌리면 안 되는 설계 3가지.**
① 🔴 **테이블을 둘로 나누지 말 것.** "자주 쓰는 화물"과 "자주 쓰는 요청"을 `preset_type`
한 컬럼(`cargo`/`request`, CHECK 제약)으로 가른다. ② 🔴 **`payload`(jsonb)를 정규화된
컬럼으로 펼치지 말 것** — 프리셋은 **폼에 채워넣기 용도**이지 집계 대상이 아니다. 이렇게
두면 프리셋 종류가 늘어도 테이블을 안 늘린다. ③ 🔴 **`is_active = true` 조건을 빼지 말 것**
— 빼면 **비활성화된 계정이 프리셋을 읽고 쓸 수 있게 된다.**
⚠️ payload 안의 `vehicle_type`·`item_condition` 등은 **저장 시점 문자열 스냅샷**이다.
옵션 이름이 바뀌면 안 맞을 수 있으니 **불러올 때 현재 목록에 없는 값은 조용히 버리고
나머지만 채우도록** 21차에서 처리할 것. ⚠️ **이번엔 구조만 만들었고 쓰는 화면은 없다.**

**(4) 🔴 `도크`는 DB 행이 없으면 화면에만 뜨고 값이 안 붙는다.**
`option_name`은 앱에서 바꿀 수 없고(운임기준표 API가 `rate_pct`/`flat_amount` 두 필드만
허용), 견적 계산이 **문자열 완전일치**로 매칭한 뒤 못 찾으면 `continue` 한다 —
**예외도 경고도 없이 가산만 빠진다.** 그래서 코드(8종)와 DB(1행)를 **같은 커밋**에 넣었다.
🔴 **앞으로 상하차 옵션을 추가할 때도 반드시 둘을 같이 할 것**(마이그레이션 파일 ④번
단언이 코드 8종과 DB 라벨의 완전일치를 검사한다). 금액은 **`0 / 0`**으로 넣었다 —
신규 옵션이라 기존 견적에 영향이 없어야 하기 때문이며, 실제 금액은 담당자가
`/admin/rates` "가산기준" 탭에서 정한다. ⚠️ 배열에서 `도크`는 **`지게차` 다음**이다
(이 순서가 곧 화면 순서다).

**(5) 🟢 운영 반영 완료(2026-08-26). 무엇이 어떻게 확인됐는지 구분해서 적는다.**
`apply`가 **22초에 3파일을 반영**했고 `_migrations`가 **9행**이 됐다(baseline 6 + 이번 3,
`applied_by = ttang0825`). `verify`로 회귀 없음도 확인했다 — 가산기준 9행·동의 3행·
운임 135행 전부 그대로.
🟢 **새 스키마 자체는 마이그레이션 파일 안의 단언이 운영 DB에서 통과한 것으로 증명된다** —
컬럼 3개 생성·전부 nullable·**중복 컬럼(`name`/`note`) 0**·`location_name`/`notes` 보존 /
`customer_presets` RLS on·정책 2개·`preset_type` CHECK·`company_id` FK가 `companies` /
`도크` 1행·`상하차방식` 8행·금액 `0/0`·코드 8종과 라벨 완전일치. 하나라도 어긋났으면
**트랜잭션이 통째로 되돌아가고 워크플로가 빨간불로 멈췄을 것**이다.
⚠️ **다만 `_verify.sql`은 아직 이 항목들을 다루지 않는다**(운임·동의·이력만 본다).
다음에 손볼 때 `customer_locations` 컬럼 · `customer_presets` · `상하차방식` 8행을
**⑧⑨ 사이에 추가하는 것이 좋다** — 그러면 언제든 버튼 하나로 재확인할 수 있다.

**(6) 검증 — 로컬 PostgreSQL 16에 워크플로 실행기를 그대로 돌렸다.**
운영과 같은 모양(`customer_locations` 8컬럼 · `상하차방식` 7행)을 만들고 시험했다:
완료조건 1~7 전부 통과 / **조건 4는 실제로 `'이상한값'`을 넣어봐서 `check constraint`
위반으로 차단되고 `'cargo'`는 통과하는 것을 확인** / **조건 6 재실행 시 `반영된_행수 = 0`,
행수 8 그대로** / 3-1·3-2도 재실행 안전 / `apply` 재실행은 "없습니다".
🔴 **로컬에 Supabase 전용 롤(`anon`·`authenticated`)과 `auth.uid()`가 없어서 3-2가 한 번
멈췄다** — 코드 결함이 아니라 환경 차이다. 다음에 RLS가 있는 마이그레이션을 로컬에서
시험하려면 그 셋을 스텁으로 먼저 만들 것.
코드 쪽은 `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일** + `package.json`·
`package-lock.json`·`next-env.d.ts`·`tsconfig.json` 변경 **0건**.
**완료조건 9는 `/quote`를 렌더링해 확인했다** — 버튼 **16개**(상차 8 + 하차 8), 순서가
배열과 정확히 일치. ⚠️ **나머지 4개 화면**(`admin/quotes`·`admin/orders`·
`admin/orders/[id]`·`customer/request`)은 미들웨어·로그인 때문에 렌더링으로 못 봤다
(46차와 같은 제약). 같은 상수를 `.map()`으로 쓰는 것을 전수 확인했고, **원칙 50번 함정
(상수를 순회하며 state를 인덱싱)은 해당 없음**을 확인했다 — 전부 `<option>`·버튼 렌더링용.
⚠️ **완료조건 14(기존 견적 계산 무변경)는 논증으로 갈음했다** — `도크`는 신규 옵션이고
금액이 `0/0`이라 기존 저장 건에는 이 옵션 자체가 없다.

**(7) ⚠️ 조사 방법 — 다음에도 쓸 것.** 스키마를 봐야 할 때 사용자에게 SQL 결과를 받지
않고, **브랜치에 조사용 SQL을 올려 워크플로를 그 브랜치 ref로 돌렸다**(`actions_run_trigger`의
`ref` 파라미터). 읽기 전용이라 안전하고 결과가 로그에 그대로 남는다. 확인 후 `_verify.sql`을
원본으로 되돌리고 그 커밋 2개는 PR 전에 정리했다.
47차
세션: **두 건**. ① "SQL 마이그레이션을 GitHub Actions로"(차수 없음, PR #94·#95) +
② "제3자 제공 동의(20차 3-4)"(PR #96). **DB 스키마 변경 없음.** 신규 파일 6개.
커밋 4+1+3 = 8개. 세 PR 모두 리뷰 지적 없이 merge됨.
🟢 **이 세션부터 SQL을 사람이 손으로 실행하지 않는다** — 아래 (A)를 반드시 읽을 것.

**═══ ① SQL 마이그레이션 자동화 (차수 없음) ═══**
**(A) 🔴 이제 마이그레이션은 GitHub Actions 버튼으로 실행한다. Supabase SQL Editor에
붙여넣지 말 것.** 사용자가 "매번 SQL을 실행하고 확인하고 전달하는 과정이 번거롭다"고 해서
만든 것이고, **실제로 부담이 큰 쪽은 실행이 아니라 검증이었다**(결과를 눈으로 읽어 옮겨 적기).
```
  저장소 → Actions 탭 → "DB 마이그레이션" → Run workflow → mode 선택
    dry-run  실행할 파일 목록만 보여주고 DB를 안 건드림   ← 항상 이것부터
    apply    실제로 반영
    verify   운임·동의·스키마 점검 (읽기 전용, 49차에 ⑧-b~⑧-f 추가)
```
🟢 **세션이 직접 돌리고 로그까지 읽을 수 있다**(`mcp__github__actions_run_trigger` +
`get_job_logs`). 47차에 dry-run·verify를 실제로 돌려 결과를 확인했다 — **사용자가 버튼을
누를 필요도 없다.** 신규 파일: `.github/workflows/migrate.yml` · `scripts/run-migrations.sh` ·
`migrations/_bootstrap.sql`(`_migrations` 이력 표) · `migrations/_baseline.txt` ·
`migrations/_verify.sql` · `migrations/README.md`(설정 절차와 작성 규칙 전문).
**(B) 🔴 `migrations/_baseline.txt`를 건드리지 말 것 — 이번 작업의 가장 중요한 안전장치다.**
거기 적힌 6개 파일은 **이미 사람이 실행한 것**이라 워크플로가 **실행하지 않고 "적용됨"으로
등록만** 한다. 이 장치가 없으면 첫 실행에 6개가 전부 다시 돌고, 17차 두 파일은 INSERT인데
**이 테이블들에 유니크 제약이 없어서** `rate_distance_tiers` 45행 / `rate_vehicle_extra_fees`
3행이 **중복으로 더 생긴다.** 견적은 매칭된 첫 행을 쓰므로 겉으로는 멀쩡해 보이고 그래서 더
늦게 발견된다. ⚠️ **새로 만드는 마이그레이션은 이 목록에 넣지 말 것.**
**(C) 🔴 앞으로 쓰는 마이그레이션 파일은 "한 번에 통째로" 실행된다.** 기존 6개처럼
"0단계 백업 / 1단계 확인 / 2단계 실행"으로 나눠 사람이 하나씩 돌리는 구조로 쓰면 안 된다.
지킬 것 셋: ① `begin;`/`commit;`을 쓰지 말 것(워크플로가 이미 `--single-transaction`으로
파일 전체를 감싼다 — 안에서 커밋하면 그 원자성이 깨진다) ② 안전장치를 **사람이 읽는
`select`가 아니라 `do $$ … raise exception … end $$;`**로 쓸 것(값이 어긋나면 아무것도
반영되지 않고 워크플로가 빨간불로 멈춘다) ③ **적용이 끝난 파일의 SQL 문을 나중에 고치지
말 것**(DB엔 옛 내용, 파일엔 새 내용이 되어 어긋난다. 값을 바꾸려면 새 파일).
⚠️ 16차가 정한 `with … as (… returning 1) select count(*)` 형태는 **사람이 SQL Editor에서
돌릴 때의 규칙**이었다. 워크플로는 psql이라 모든 문장의 결과를 보여주므로 이제 필수가 아니다
(써도 무해하다). **파일명은 `YYYY-MM-DD_무엇.sql`** — 실행 순서가 파일명 오름차순이다.
**(D) 🔴 접속 문자열은 `Session pooler`다. `Direct connection`이 아니다.**
첫 실행이 `Network is unreachable`로 실패했고 원인은 **Direct(`db.<ID>.supabase.co`)가
IPv6 전용인데 GitHub Actions 러너는 IPv4만 쓰기 때문**이었다(내가 처음에 Direct를 쓰라고
잘못 안내했다). Supabase 화면 맨 위 **초록색 `Connect` 버튼** → `Session pooler` 탭이며
(예전의 Settings → Database 자리가 여기로 옮겨졌다), Direct와 두 군데가 다르다 —
**사용자 이름이 `postgres`가 아니라 `postgres.<프로젝트ID>`**, **호스트가
`aws-0-<리전>.pooler.supabase.com`**. 포트는 **5432(Session)**이고 6543(Transaction)이 아니다.
🟢 PR #95에서 **접속 전에 Direct 형태를 걸러내고 안내를 출력하는 가드**를 넣었으므로 다음에
같은 실수를 하면 원인이 바로 뜬다(URL 자체는 출력하지 않는다 — 비밀번호가 들어 있다).
**(E) ⚠️ 사용자가 코드 밖에서 한 일 — 이미 끝났다. 다시 시키지 말 것.**
GitHub 저장소 Secret **`SUPABASE_DB_URL`** 등록(Settings → Secrets and variables → Actions).
🔴 **이 값은 RLS를 우회하는 최고 권한이라 `service_role` 키보다 강하다. 채팅·이슈·PR
어디에도 붙여넣게 하지 말 것.** 실행 로그에는 `***`로 가려진다.
**(F) 판단해서 정한 것 3가지.** ① **merge 시 자동 실행이 아니라 수동 버튼**이다 — 운임
테이블에는 되돌리기가 없어서(운임 이력 테이블이 없다) 코드 배포와 DB 반영 시점을 붙여두면
사고 시 되돌릴 여지가 사라진다. `Settings → Environments → production`에 리뷰어를 지정하면
승인 단계도 붙는다(지금은 미지정이라 그냥 통과). ② **파일 하나 = 트랜잭션 하나이고 이력
기록 INSERT를 같은 트랜잭션에 넣었다** — "반영은 됐는데 기록이 안 된" 상태가 원천적으로
생기지 않는다. ⚠️ **파일 사이는 원자적이지 않다**(3개 중 3번째가 실패하면 1·2번은 반영된 채
남는다). ③ **드리프트는 경고만 하고 멈추지 않는다** — 주석만 고친 경우까지 막으면 이후
마이그레이션이 통째로 발이 묶인다(17차 tiers 파일이 실제로 실행 후 주석이 추가됐다).
**(G) 검증 — 로컬 PostgreSQL 16을 실제로 띄워 전부 돌렸다.** 운영과 같은 모양
(135행/9행/consents 2행)을 만들고 스크립트를 그대로 실행: 빈 DB dry-run → baseline 6건
등록·**대기 0건**·데이터 무변경 / 새 파일 추가 → 대기 1건 / apply 1회차 실행 → **2회차는
"없습니다"**(중복 방지) / 중간 실패 마이그레이션 → **테이블 0·행수 원복·이력 0**·종료코드 3 /
적용 파일 수정 → `::warning::` 후 계속 / verify 9항목 출력. **운영 DB에서도 재확인**했다 —
dry-run은 baseline 6건·대기 0건, verify는 **차급 9/구간 15/총 135행/가산기준 9행 · 거리 역전
0건 · 1톤 10km 48,000·60km 84,000·400km 이내 289,000**. ⚠️ 시험용 파일과 로컬 클러스터는 삭제했다.

**═══ ② 제3자 제공 동의 (20차 3-4) ═══**
🔴 **20차 지시서에서 3-4만 떼어내 단독으로 처리했다(사용자 결정).** 3-1(`customer_locations`
컬럼)·3-2(`customer_presets`)·3-3(`도크` 1행 + `loadingMethods` 8종)은 **아직 안 했다** —
"왜 안 되어 있지" 하고 헤매지 말 것. 아래 5번 로드맵 참고.
**(0) 왜 이것부터인가 — 지시서 2장 3번의 🔴 분기에 걸렸다.** `/customer/request`가 상·하차지
담당자의 성명·연락처를 **이미 필수로 받고 있었다**(`:227`·`:231`). 제3자 개인정보인데 동의
절차가 없었고, **처리방침 제4조는 "실제 운송 접수(발주) 시점에 별도의 동의를 받은 후에만
이루어집니다 / 운송관리 화면을 통해 발주하시는 경우: 발주 요청 화면에서 동의 절차를 거칩니다"
라고 약속하고 있었다.** 문서와 실제가 어긋난 상태였고 이 작업이 그것을 맞춘 것이다.
**(1) 🔴 접수를 서버 API로 옮겼다 — `/quote`(14차)와 원인이 다르다.**
`consents`는 **RLS를 켜고 정책이 0개**라 service_role로만 쓸 수 있다. 로그인한 화주라도
`authenticated` 롤로는 동의 기록을 남길 수 없다. `/quote`는 "방금 넣은 행의 id를 못 받는
것"이 문제였지만 **여기는 id는 받을 수 있는데 `consents`에 쓸 권한이 없다.** 결론만 같다.
🔴 **`consents`에 `authenticated` INSERT 정책을 만드는 방식으로 되돌리지 말 것** — 그 순간
로그인한 화주가 **아무 내용의 동의 행이나 직접 써넣을 수 있게 되어** 이 표의 존재 이유
(입증 자료)가 사라진다. 신설 `app/api/customer/order-request/route.ts`.
🔴 **`company_id`·`requested_by`를 요청 바디에서 받지 않는다**(원칙 30번) — 받으면 콘솔에서
회사 id만 바꿔 **남의 회사 이름으로 발주요청을 넣을 수 있다.** 세션에서만 유도한다.
🔴 **동의 기록이 실패하면 접수 건을 삭제한다**(14차와 같은 롤백). 트랜잭션이 없어서 그냥
두면 **동의 없이 제3자 정보를 받아둔 상태**가 되고, 그게 이 작업이 없애려던 바로 그 상태다.
⚠️ 접수 뒤의 "배송지 저장"(`customer_locations`)은 **화면에 그대로 뒀다** — RLS 정책이 있어
화주 본인이 쓸 수 있고 실패해도 접수와 무관하다. 서버로 옮기지 말 것(범위가 커진다).
**(2) 저장되는 값 — DB 스키마 변경 없음.** `subject_type='portal_order_request'` /
`consent_type='third_party'` / `version=PRIVACY_POLICY_VERSION`(`privacy-v1`) / `agreed=true` /
`source='customer_portal'`. 🔴 **`third_party`의 버전으로 별도 상수를 만들지 말 것** —
근거 문서가 처리방침 제4조 하나뿐이라 버전이 갈라지면 어느 방침에 동의한 것인지 되짚을 수 없다.
⚠️ `source`만 경로가 아니라 이름이라 형태가 섞였다(앞의 둘은 `/quote`·`/apply`). 지시서가
정한 값이며 **21차가 화면을 갈아엎으며 경로가 바뀔 수 있어서** 경로보다 안정적인 이름을 쓴
것으로 읽는다. 🔴 이미 저장된 행이 이 값을 가지므로 나중에 바꾸지 말 것.
⚠️ **마이그레이션이 필요 없는 이유** — 14차가 `consents`에 CHECK 제약을 **일부러 안 걸었다.**
그래서 새 값 3개가 그대로 들어간다. 반대로 **`lib/consent.ts`가 유일한 방어선**이다.
**(3) 판단해서 정한 것 3가지.** ① 🔴 **접수 성공 후 체크를 해제한다** — 동의는 **발주 건별**
이고, 체크가 남아 있으면 다음 건에 대해 화주가 의식적으로 동의하지 않았는데도 동의 행이
기록되어 **기록 자체가 거짓이 된다.** ② **이 화면은 `third_party` 하나만 받는다** — 화주
본인의 개인정보는 `/apply`에서 이미 동의를 받았고 화면 문구도 제3자 제공만 말한다("문구에
없는 동의를 기록하지 않는다"는 14차 원칙 그대로). ③ `accountId` state를 제거했다(서버가
세션에서 직접 구하게 되어 화면이 들고 있을 이유가 없어졌다).
**(4) 🔴 문구는 `lib/legalInfo.ts`의 `PORTAL_ORDER_THIRD_PARTY_CONSENT`에 있다 — 화면 JSX에
다시 적지 말 것.** 21차가 이 화면을 새 디자인으로 갈아엎을 때 **문구·저장 로직·DB는 그대로
재사용**한다. 상수로 두어야 옮기면서 어긋나지 않는다. ⚠️ **이 문구는 초안이며 변호사 검토
대상이다** — 개인정보보호법 제15조 2항의 고지 4항목과 거부권을 형식상 갖췄으나 법적 완결성은
별개이고, 마지막 확인 문장("제3자의 개인정보를 입력하시는 경우…")은 실무 관행이지 법적
면책이 아니다. 🔴 **디자인은 21차 범위라 기존 폼 톤에 맞춘 최소 구성으로 뒀다.**
**(5) 🔴 보고만 하고 고치지 않은 것 — 처리방침 제4조에 하지 않기로 한 일이 적혀 있다.**
지시서 금지사항대로 **`lib/legal/`은 한 글자도 안 건드렸다**(완료조건 13, `git diff` 0건).
`lib/legal/privacy.ts:211`에 이 행이 있다:
```
  ["**운송을 의뢰한 고객**", "배차 확정 정보 안내",
   "차주 성명, 연락처, 차량번호, 차량 종류", "해당 운송 건 종료 시까지"]
```
**사용자 결정(2026-08-25)은 차주 정보를 화주에게 노출하지 않는 것**이다. 위반은 아니지만
사실과 다르다. 🔴 **변호사 검토 항목이며 임의로 고치지 말 것**(법적 문서다).
**(6) 검증** — `npx tsc --noEmit` 통과(출력 0줄) + `npm run build` 프리렌더 실패 **48개가
`origin/main` 별도 worktree 빌드와 항목 단위로 완전히 동일**(`diff` 빈 출력) + 보험 경고 1건
그대로 + `package.json`/`package-lock.json` 변경 0건 + **서버 게이트 실측**(검증이 전부 DB
접근 전에 있어서 **인증만 우회한 임시 사본**으로 실제 응답을 받았다 — `agreed` 없음·`false`·
**문자열 `"true"`** 전부 400(`!== true` 엄격 비교), 연락처 누락·출발지 누락 400, 정상 요청은
검증을 통과해 DB까지 감. 확인 후 삭제) + **실제 라우트 인증**(토큰 없음·위조 토큰 둘 다 401)
+ **렌더링**(로그인이 필요한 화면이라 동의 블록만 스텁으로 — 문구 8줄 전부·체크박스 정확히
1개, Playwright로 **라벨 글자만 클릭해도 토글·해제**되는 것 확인, 페이지 에러 0. 확인 후 삭제).
🟢 **동의 행이 실제로 저장되는 것까지 확인됐다(2026-08-26)** — 사용자가 Preview에서 발주요청을
한 건 넣었고, `verify` 워크플로 ⑧번에 **`customer_portal / third_party / privacy-v1 / t / 1건`**
이 떴다(완료조건 11·12). ⚠️ **그 건은 운영 DB의 진짜 행**이라 관리자 「화주요청」에 떠 있다.
⚠️ **`subject_id`가 어느 발주요청에 붙었는지는 아직 확인하지 않았다** — 14차에 사용자가
"행이 생겼다만 보면 안 되고 join까지 볼 것"이라고 한 지점이다. `_verify.sql` ⑧번에 join을
더하는 것이 다음 차수 후보다(48차의 `customer_locations`·`customer_presets`·`상하차방식`
점검 추가와 **같이 하면 한 번에 끝난다**). ⚠️ 발주요청을 지워도 `consents` 행은 남는다(FK 없음, 14차 의도).
**(7) ⚠️ 검증 중 겪은 함정 2가지 — 둘 다 시험 자산 결함이었다.**
(a) 🔴 **`grep`의 대소문자 함정에 한 세션에 두 번 걸렸다.** `setAccountId`·`setThirdPartyAgreed`는
`set` + **대문자**로 이어져서 **소문자 `accountId`·`thirdPartyAgreed` 패턴에 안 걸린다.**
이걸 보고 "`requested_by`가 계속 null이었다"고 **오진해서 사용자에게 잘못 보고했다** — 값은
정상적으로 채워지고 있었다. 스텁 생성 스크립트도 같은 이유로 치환에 실패해 토글 시험이
헛돌았다. **`set` 접두사가 붙는 이름을 grep할 때는 대소문자 경계를 의심할 것.**
(b) `_`로 시작하는 폴더는 App Router의 **private folder라 라우팅되지 않는다**(43차와 같은
함정 — `app/api/__gatetest` 404 → `gatetest`로 해결). `pkill -f "next dev …"`가 **자기 자신을
죽이는 것**(44차)과 `networkidle`이 CDN 폰트 때문에 안 오는 것도 그대로 재현됐다.
46차
세션: "차급 9종 확장(17차)" — 코드 3개 파일군 + 🔴 **DB 변경 있음**(`rate_distance_tiers`
**45행 INSERT**, `rate_vehicle_extra_fees` **3행 INSERT**). 신규 파일 2개(마이그레이션 SQL).
커밋 6개. **PR #93 merge됨 — 리뷰 지적은 없었다.** 🟢 **사용자가 같은 세션에서 SQL을 실행해
135행 / 9행을 확인했다**(아래 (6)번). 🔴 **작업 중에 새 실측 114건이 들어와 값 변경 없이
기록만 반영했다**(아래 (7)번 — 다음 차수에 직접 영향이 있으니 반드시 읽을 것).
**(0) 왜 단순히 배열을 합치지 않았나 — 🔴 `VEHICLE_TYPES`는 관리자 전용이 아니었다.**
`/quote`(완전공개 견적문의) · `/apply`(완전공개 계정 신청) · `/customer/request`(화주포털
발주요청) 세 화면이 이 배열을 그대로 쓰고 있었다. 특히 **`/apply`는 드롭다운뿐 아니라
안내 캡션이 `VEHICLE_TYPES.join(", ")`으로 배열 전체를 본문에 뿌린다.** 그대로 9종으로
늘렸으면 **`"25톤"`이 공개 화면 본문에 문자열로 출력**됐을 것이다 — 32차·12차가 정한
금지 표현이다. 그래서 합치지 않고 **`VEHICLE_TYPES_ALL`(9종) / `VEHICLE_TYPES_PUBLIC`(6종)
으로 나눴다**(`BODY_TYPES` 11종을 `/vehicles`에서 4종으로 좁힌 것과 같은 취지, 원칙 50번).
**(1) 🔴 이번 차수가 확정한 것 — 금지 표현의 적용 범위.** 완료조건 문구가 예전의
*"공개 12개 화면 렌더링 텍스트 0건"*에서 아래로 바뀌었다. **되돌리지 말 것.**
```
  광고·유입 (불특정 다수)   /, /vehicles, /quote, /apply       → 6종 VEHICLE_TYPES_PUBLIC
  화주 입력 (로그인 화주)    /customer/request 희망 톤수         → 11종 VEHICLE_TYPES_ALL
  표시 (이미 확정된 건)      화주포털 조회·견적서 PDF 2종·엑셀    → 저장값 그대로 (11종 가능)
  관리자 입력·계산          /admin/*, rate_distance_tiers      → 11종 VEHICLE_TYPES_ALL
```
⚠️ **아래 두 행은 23차(53차 세션)에 현재 사실로 고친 것이다.** 46차 원문은 "화주 입력 →
6종 PUBLIC" · "관리자 → 9종 ALL" 이었는데, **21차가 `/customer/request` 를 6종 → 9종으로
(로그인한 화주만 보는 화면이라 광고가 아니다), 22차가 차급을 9종 → 11종으로** 바꾸면서
둘 다 낡았다. 정정 내용이 21차 ⑥ 끝줄·22차 ③·로드맵에 흩어져 있을 뿐 **이 표에는 표시가
없어서**, 로드맵 17차 항목이 이 표를 정본으로 가리키는 동안 계속 틀린 값을 가리키고 있었다.
🔴 **`/customer/request` 를 6종으로 되돌리지 말 것** — 대형이 나와야 맞는 화면이다.
🔴 **공개 4개 화면(첫 행)은 6종 그대로다.** 특히 `/apply` 는 배열을 `join(", ")` 으로
안내 문구 본문에 뿌려서, 뚫리면 공개 화면에 `"25톤"` 이 찍힌다.
🔴 **"표시" 계열은 배열을 참조하지 않는다** — `quotes.vehicle_type` 저장 문자열을 그대로
인쇄한다. **대형 오더를 이미 의뢰한 화주에게 나가는 견적서는 광고가 아니다**(그 화주는
우리가 그 건을 처리한다는 걸 이미 안다). 금지되는 것은 **취급 범위를 과장하는 광고**다.
`/vehicles`의 32차 확정 문구 "5톤 이상 대형 차량도 문의 주시면 확인해 안내드립니다."가
정확히 이 구조를 말하고 있다 — **문의 경로는 열려 있고 목록에 대형을 나열하지는 않는다.**
**(2) 🔴 `VEHICLE_TYPES`라는 이름을 남기지 않은 것이 이 리팩터링의 핵심이다.**
남겨뒀으면 기존 import 5곳이 **아무 신호 없이 9종을 받았을 것**이다. 이름을 없애니
`npx tsc --noEmit`이 **정확히 5곳을 컴파일 에러로 드러냈고**(companies·companies/[id]·
apply·customer/request·quote) 하나씩 성격을 보고 판단할 수 있었다. **이름을 되살리지 말 것.**
**(3) 전수 확인 결과 13곳이었다** — 정본 1 + import 사용처 5 + 로컬 재정의·인라인 5 +
`/vehicles` 배열 2. ⚠️ **45차 기록의 "8곳"은 배열 정의 지점만 센 것**이고 import 사용처를
포함하면 13곳이다(둘 다 맞는 숫자이며 세는 기준이 다르다).
⚠️ **`app/page.tsx`의 `const VEHICLES`는 대상이 아니다** — 톤수가 아니라 **차량 형태**
(카고/탑차/윙바디/리프트)다. 지시서의 `VEHICLES\b` grep에 걸리는 **오탐**이니 다음에도
속지 말 것.
**(4) 🔴 로컬 재정의 5곳을 없앤 결과 — 앞으로 차급 추가는 배열 한 곳만 고치면 된다.**
`admin/rates`(표의 **열 머리글**) · `admin/quotes`(등록 드롭다운) · `admin/quotes/[id]`
(**인라인 배열**, 상세 수정 폼) · `admin/drivers` · `admin/drivers/[id]` 다섯 곳이 각자
사본을 갖고 있었다. 안 고쳤으면 값을 넣어도 **그 화면에서만 신규 차급이 안 보였다.**
🔴 **`/vehicles`의 `VEHICLE_SPECS`·`START_PRICES`는 그대로 뒀다** — 공개 문구 기준이
"1톤부터 5톤 이상까지"이고, 적재용량·시작가가 붙은 객체 배열이라 단순 치환도 안 된다.
**"정리" 대상이 아니다.** `PRICING_NOTES` 4줄도 무변경(`git diff`에 파일이 아예 없다).
**(5) 🟢 5-3의 가로 스크롤 대응은 필요 없었다** — 지시서가 "열이 3개 늘어 표가 잘릴 수
있으니 가로 스크롤 컨테이너를 붙이라"고 했으나, `/admin/rates` 표는 **이미
`<div className="card" style={{ overflowX: "auto" }}>` 안에 있다.** 1280px에서 9열이
**표 1050px / 카드 1050px**로 여유 있게 들어가고 페이지 가로 오버플로우 0px이다.
**(6) 🟢 실행 결과 — 전부 확인됐다(2026-08-25, 사용자가 Supabase에서 직접).**
`반영된_행수` **45** / **3**, 총행수 **135**(90+45), 천원 단위 아닌 행 0, 표본 5개
(169,000 / 694,000 / 253,000 / 217,000 / 954,000) 일치, **1톤 10km이 48,000 그대로**
(완료조건 20 — 신규 차급이 기존을 안 건드림), `400km 초과` **9행 전부 상한 `null`**,
`rate_vehicle_extra_fees` **9행 · `null` 0건**. ⚠️ 백업 스냅샷 이름은
`_bak_rate_distance_tiers_before17` · `_bak_rate_vehicle_extra_fees_before17`이다
(16차의 `_20260825`와 별개로 남아 있다). ⚠️ 기존 4행이 `10000.0`으로 보이는 것은
16차와 같은 **표시 차이**일 뿐이다.
🔴 **INSERT라 두 번 돌리면 중복 행이 생긴다** — 이 테이블에는 유니크 제약이 없다.
그래서 마이그레이션 파일에 "이미_들어간_대형행수 = 0" 재실행 안전장치를 넣었다.
**(7) 🔴 작업 중에 새 실측이 들어왔다 — 값은 안 바꾸고 기록만 반영했다(사용자 지시).**
2026-08-25 중대형 실거래 **114건**(24시콜)을 받아 대조한 결과다. 세 가지 전부
`lib/constants.ts`와 마이그레이션 파일 주석에 남겨뒀다.
① 🔴 **18톤은 잠정값이다 — 우리 표가 약 13% 높다.** 완료 8건 역산. 원인은 v8 보간식
`11톤 + 0.70 × (25톤 − 11톤)`의 **위치계수 0.70이 과대평가**였던 것이고, 실측에서
18톤은 11톤과 거의 같은 가격대다(**차주지급 중앙값이 둘 다 180,000원**).
🔴 **18차에서 전 구간 ×0.89 조정을 검토한다.** ⚠️ **그때까지 18톤 견적은 실제보다
비싸게 나간다** — 배차가 안 붙거나 화주가 다른 곳과 비교할 위험이 있으니 담당자 확인 필요.
🟢 **→ 22차(52차 세션)에 실제로 조정됐다.** 차수가 밀려 "18차"가 아니라 22차가 됐고,
🔴 **`200km 이내` 한 칸만 −5.5%(341,000)** 다(×0.89 면 11톤보다 낮아진다 — 위 52차 기록 ③).
② 🟢 **11톤은 검증됐다 — 그대로 둘 것**(완료 36건, 마진 중앙값 **+19.6%**).
⚠️ **25톤은 완료 3건이라 판단 불가** — 관찰 대상.
③ 🔴 **차급은 9종으로 끝나지 않는다.**(🟢 **22차에 8톤·15톤이 들어가 11종이 됐다** —
아래 목록은 그때 실제로 그대로 쓰였고, 다음 차급 추가 때도 유효하다.)
같은 데이터에 **8톤 13건 · 9.5톤 5건 · 14톤 12건 ·
15톤 6건**이 있었다 — **114건 중 36건(32%)이 우리 표에 없는 차급**이다. 한국 화물시장
표준 차급은 `1 / 1.4 / 2.5 / 3.5 / 5 / 5톤축 / 8 / 11 / 15 / 18 / 25`이고 **v8이 8톤과
15톤을 빠뜨렸다.** 이 둘을 넣으면 커버율 **100%**가 된다(9.5톤 → 8톤, 14톤 → 15톤 흡수).
⚠️ **"구조를 차급이 더 늘어난다는 전제로 만들라"는 지시에 구조는 손대지 않았다** —
(4)번 덕분에 **이미 그렇게 되어 있어서 바꿀 것이 없었다.** 대신 `lib/constants.ts`에
**"차급을 더 추가할 때 고칠 곳" 목록**을 남겼다. 핵심 두 가지:
🔴 **톤수 순서대로 끼워 넣을 것 — 끝에 붙이지 말 것**(이 배열 순서가 곧 `/admin/rates`
표의 **열 순서**이자 드롭다운 순서다. `… "5톤 플러스/축", "8톤", "11톤", "15톤", "18톤", "25톤"`).
🔴 **`rate_vehicle_extra_fees`에도 차급당 1행을 같이 넣을 것** — 빠뜨리면 그 차급
대기료가 조용히 0원이 되고 견적서에 흔적조차 안 남는다(16차에 5톤급이 그 상태였다).
그리고 **새 차급이 중간에 끼어들므로 차급 역전 검증을 다시 돌릴 것.**
**(8) 검증** — `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과
항목 단위로 완전히 동일** + 빌드 경고 보험 1건 그대로 + **기계 대조**(지시서 표 45칸 전사
오류 0 · 16차 90칸과 합친 **9차급 통합 역전 0건** · 구간 경계가 16차와 동일 · `400km 초과`
`distance_to_km` 전부 null · 코드 배열과 문자열 완전일치 · PUBLIC에 대형 없음) +
**임시 스크립트**(마이그레이션 SQL에서 값을 파싱 — **135조합 미매칭 0건**, 0.0~450.0
step 0.1 × 9차급 **40,509건 미매칭 0건**으로 15차 회귀 유지, 25톤 대기 90분 **80,000원**,
기존 6차급 90칸이 16차 값과 동일) + **dev 서버 렌더링 텍스트**(광고·유입 4개 화면에
대형 3종 0건 · 금지 표현 8종 0건 · `/vehicles` `PRICING_NOTES` 4줄) + **브라우저 실측**
(🔴 **`/apply` 캡션은 hover해야 뜨는 툴팁이라 텍스트 덤프로는 안 잡힌다** — Playwright로
실제 hover해 6종만 나오는 것을 확인했다. `/quote` 옵션 6개, `/apply` 7개(선택 안 함 포함))
+ **임시 스텁**(`/admin/*`은 미들웨어가 막아 `/admin/rates` 표 구조를 그대로 뜬 스텁으로
9열·오버플로우 0px 확인 후 삭제).
⚠️ **완료조건 13(`/customer/request` 옵션 6개)만 렌더링으로 확인하지 못했다** — 이 화면은
로그인 세션이 필요해 자격증명 없이는 `"확인 중..."`에서 멈춘다(`<select>` 0개). 소스로는
확인했다(PUBLIC을 그대로 `.map()`). **사용자가 배포본에서 볼 항목이다.**
**(9) ⚠️ 지시서와 다른 것 1건(값 영향 없음)** — 3-3의 "5톤 플러스/축 대비 11톤 최소 격차
+15.3%(**170km 이내**)"에서 구간 표기가 다르다. 비율 15.3%는 맞지만 최소 지점은
**350km 이내**다(170km 이내는 +19.9%). 마이그레이션 파일 주석에 정정해뒀다.
45차
세션: "운임 기준 교체(16차)" — 🔴 **DB 변경 있음**(`rate_distance_tiers` 90행 UPDATE,
`rate_vehicle_extra_fees` 2행 UPDATE). 신규 파일 2개(마이그레이션 SQL). 커밋 5개.
**PR #92 merge됨 — 리뷰 지적은 없었다.** 🟢 **사용자가 같은 세션에서 SQL을 실제로 실행했고
결과를 전부 확인했다**(아래 (5)번). 사용자 질문 1건(`/vehicles` 자동 연동)에 코드 변경 없이
답했고 그 결정을 아래 5번 "다음에 정할 것"에 남겼다.
**(0) 왜 했나 — 🔴 장거리가 역마진이었다.** `rate_distance_tiers.base_fare`는 **화주 청구가**
이고(차주 지급가가 아니다) 견적 계산에 **마진율 파라미터가 아예 없다**(`calc`는
`기본운임 + 가산 − 혼적할인`이 전부). 그래서 마진을 확보하는 길은 `base_fare` 자체를 올리는
것뿐이었다. 1톤 351~400km가 화주 청구 200,000 / 차주 지급 실측 240,000으로 **건당 약 4만원
손해**였다. `quotes` 2건 · `portal_order_requests` 0건이라 인상을 통보할 기존 화주가 없는
지금이 가장 싼 시점이었다.
**(1) 값 — v8 3장 × 1/0.83(마진 17%), 천원 반올림.** 인상 폭은 10~300km +20~21% /
350km 이내 +32~33% / 400km 이내·초과 **+45%**. 장거리가 큰 것은 거기가 역마진이었기 때문이다.
마진율 17%는 **사용자 결정(2026-08-25)**이며 v8 4장 "화주 청구 참고표"와 일치한다.
🔴 **1톤 10km = 48,000 / 60km = 84,000 / 400km 이내 = 289,000이 기준점**이다.
**(2) 🔴 SQL은 세션이 실행하지 않았다 — 파일로 남기고 사용자가 Supabase에서 실행했다.**
이 저장소의 관례이고 지시서가 못박은 조건이다. 🔴 **그리고 값을 파일로 남긴 것이 이번의 핵심
개선이다** — 그전까지 운임 값이 **DB에만 존재하고 저장소에 기록이 없어서**, v8을 만들 때
v7 엑셀을 대용으로 써야 했다. 앞으로 운임을 바꿀 때도 `/admin/rates` 화면 클릭이 아니라
**마이그레이션 파일**로 남길 것.
**(3) ⚠️ SQL 파일 구조를 중간에 한 번 고쳤다(커밋 5번째).** 처음엔 `begin; … commit;` 한
덩어리였는데, **Supabase SQL Editor가 여러 문장을 한 번에 돌리면 마지막 결과만 보여줘서
"UPDATE 90"을 놓친다.** 되돌리기가 없는 테이블인데 몇 행이 반영됐는지 못 보면 안 되므로,
`with upd as (update … returning 1) select count(*) as 반영된_행수 from upd;` 로 감쌌다 —
**단일 문장이라 그 자체로 원자적이고**(중간 실패 시 아무것도 반영 안 됨) 결과 표에 90이
그대로 뜬다. 1단계 백업 / 2단계 교체 / 3단계 검증으로 나눠 하나씩 실행하게 했다.
🔴 **다음에 운임 SQL을 쓸 때도 이 형태를 쓸 것.**
**(4) 🔴 `distance_label` 문자열로 매칭했다**(`distance_from_km` 숫자가 아니라). 사람이 읽을
수 있어 검토가 쉽기 때문이고, 라벨이 하나라도 다르면 그 행이 **조용히 안 바뀌므로** 반영 행수
확인이 그만큼 중요하다. ⚠️ **차급 문자열은 `5톤 플러스/축`(슬래시)이며 가운뎃점이 아니다** —
견적 자동산출이 완전일치로 매칭해서 한 글자만 달라도 그 차급 계산 패널이 통째로 사라진다.
지시서 표 90칸을 SQL과 **기계로 대조**했다(전사 오류 0 · 차급/거리 역전 0 · 차급 문자열이
`lib/constants.ts`의 `VEHICLE_TYPES`와 완전일치 · 라벨 15종 일치).
**(5) 🟢 실행 결과 — 전부 확인됐다(2026-08-25, 사용자가 Supabase에서 직접).**
백업 스냅샷 2개 생성 후 `_bak_rate_distance_tiers_20260825` **90행** ·
`_bak_rate_vehicle_extra_fees_20260825` **6행**을 실측했다 — 🟢 **이것으로 "15구간 × 6차급 =
90행" 구조가 처음으로 실측 확인됐다**(그전까지는 지시서 기록으로만 알던 값이다).
`반영된_행수` = **90**, 총행수 90, 천원 단위 아닌 행 0, 표본 6개(48,000 / 84,000 / 289,000 /
337,000 / 133,000 / 578,000) 전부 일치. `rate_vehicle_extra_fees`도 2행 반영되고 6차급
전부 `null` 0건. ⚠️ **테이블 전체가 90행인데 90행이 갱신됐으므로 누락 행은 원천적으로 없다** —
이 논리 때문에 100줄짜리 full outer join 검증은 생략하고 표본 확인으로 갈음했다.
⚠️ Supabase가 `create table as`에 **RLS 경고**를 띄운다 — 백업 테이블은 앱이 읽지 않으므로
**"Run and enable RLS"**를 눌렀다(정책 0개면 anon 차단, SQL Editor는 postgres 권한이라 되돌리기
SQL이 그대로 동작한다). ⚠️ 기존 4행이 `10000.0`처럼 소수로 보이는 것은 **표시 차이일 뿐**이다.
**(6) 🔴 `/vehicles` 공개 시작가는 DB에서 자동으로 오지 않는다 — 손으로 맞춰야 한다.**
`START_PRICES` 6개는 `rate_distance_tiers`의 "10km 이내" 행과 **1원도 다르면 안 되는데**
하드코딩이다. 안 고치면 **게시가 40,000 · 실제 견적 48,000**이 되어 표시가격 분쟁이 된다.
🔴 **운임기준표를 바꾸는 마이그레이션과 이 배열은 항상 같은 PR에서 함께 움직일 것**(파일
주석에도 못박았다). 자동 연동은 아래 5번 "다음에 정할 것" 참고.
🟢 **→ 64차에 해소됐다** — `lib/startPrices.ts` 가 `rate_distance_tiers` 를 실시간으로
읽는다. **`START_PRICES` 하드코딩 배열 자체가 없어졌으니 이 문단의 "손으로 맞춰야 한다"를
근거로 되살리지 말 것.**
⚠️ **시작가 숫자 전수 grep 결과 렌더링되는 곳은 이 파일 1곳뿐이었다** — 메타 description에는
금액이 없고(`"…시작가를 안내합니다"`) 랜딩에도 없다. `PRICING_NOTES` 4줄은 한 글자도 안 바꿨다.
**(7) 첫거래지원 할인 제거 — 🔴 지시서가 지목하지 않은 곳이 1군데 더 있었고 일부러 남겼다.**
지시서 범위는 `admin/quotes/page.tsx`·`admin/guide/page.tsx` 2개였는데 grep 결과 **3개 파일**
이었다. 🔴 **`app/admin/quotes/[id]/page.tsx`의 `첫거래지원할인:` 줄은 제거하면 안 된다** —
견적 상세 수정이 `selected_options` **전체를 새 객체로 교체**하므로, 그 줄을 빼면 그 견적을
한 번 수정하는 것만으로 **과거에 기록된 값이 사라진다.** 지시서가 "기존 키를 손대지 말 것"
이라고 한 것이 이 경우다. 이유를 주석으로 못박았으니 **"정리" 대상으로 오해하지 말 것.**
렌더링에는 영향이 없다 — 견적서 print 2종과 엑셀이 `selected_options`를 **generic하게 순회하며
`v !== false`로 걸러내므로** 값이 false인 한 인쇄에 안 나온다. `/admin/rates`에는 `특별할인`을
`HIDDEN_CATEGORIES`에 넣어 화면에서만 숨겼다(`긴급여부`와 같은 방식, 원칙 32번).
🔴 **DB의 `rate_surcharges` `특별할인` 행은 삭제하지 않았다.**
⚠️ 가이드 문구가 틀려 있었다 — "최종금액에서 10% 할인"이라 적혀 있었으나 실제로는
`base * rate_pct` = **기본운임의 10%**였다. 행을 지우면서 같이 없어졌다.
**(8) 검증** — `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목
단위로 완전히 동일** + 빌드 경고 보험 1건 그대로 + **임시 스크립트**(마이그레이션 SQL에서 값을
파싱해 15차 매칭 로직에 태움 — 90조합 미매칭·구간오류 **0건**, 0.0~450.0 step 0.1 × 6차급
27,006건 미매칭 0건으로 15차 회귀 유지, `10.4km → 60,000원`, 5톤 대기 90분 **50,000원**) +
**dev 서버 렌더링 텍스트 검증**(`/vehicles` 구값 6종 0건·신값 6종 각 1건, `PRICING_NOTES`
4줄 전부, 금지 표현 8종 공개 4개 화면 0건, 가로 오버플로우 1280·360px 모두 0px) +
**스크린샷 육안 확인**(데스크탑·모바일). ⚠️ `/admin/*`은 미들웨어가 막아 렌더링 확인이 안 되므로
`첫거래지원`은 소스로 확인했다(남은 매치는 주석과 위 (7)의 보존 코드뿐, UI 렌더링 0건).
**(9) ⚠️ 45차에 확인한 부록 C 정정 — 차급 배열은 3곳이 아니라 6곳이다.** 44차 기록 (7)번이
사용자 질문에 답하며 지나가듯 세느라 3곳으로 적게 셌고, 16차 지시서 부록 C가 이를 정정했다.
grep 전수로 확인한 결과가 맞다: `lib/constants.ts:24`(정본) · `admin/quotes/page.tsx:26` ·
`admin/rates/page.tsx:19` · **`admin/drivers/page.tsx:10`** · **`admin/drivers/[id]/page.tsx:11`** ·
**`admin/quotes/[id]/page.tsx`의 인라인 배열**(지시서의 `:795`는 이번 PR에서 `:800`으로 밀렸으니
**행 번호가 아니라 grep으로 찾을 것**). 🔴 **17차 목록에는 `/vehicles`의 배열 2개
(`VEHICLE_SPECS`·`START_PRICES`)도 함께 넣어야 한다** — 이 둘도 차급 이름을 담고 있으나
**공개 문구 기준상 9종으로 늘리면 안 되는 곳**이다(32차·12차 확정 "1톤부터 5톤 이상까지",
`"25톤"` 금지). 부록 C가 예고한 `VEHICLE_TYPES_ALL` / `VEHICLE_TYPES_PUBLIC` 분리가 정확히
이 지점에서 필요하다.
44차
세션: "거리 구간 매칭 버그 수정(15차)" — 코드 1개 파일만. **DB 변경 없음**(운임 3개
테이블에 UPDATE·INSERT·DELETE 0건, 마이그레이션 파일 없음 — 지시서가 못박은 조건이다).
신규 파일 없음. 커밋 2개. **PR #91 merge됨 — 리뷰 지적은 없었다.** 사용자 질문 1건
("운임기준표에 11톤·18톤·25톤이 빠져있나")에 코드 변경 없이 답했다(내용은 아래 (7)번).
**(0) 무엇이 고장나 있었나 — 🔴 견적 자동계산이 소수 거리에서 조용히 실패하고 있었다.**
거리 API(`app/api/distance/route.ts`)가 `Math.round(m/1000*10)/10`으로 **소수 첫째
자리까지** 돌려주는데(`10.4`), `rate_distance_tiers`는 `0~10` / `11~20`처럼 **정수로
끊겨 있어 사이가 비어 있다.** `10.4`는 `10.4<=10` 거짓 · `10.4>=11` 거짓이라 어느 구간에도
안 걸리고 `calc`가 null이 됐다. 빈틈 14개 — `(10,11) (20,21) (30,31) (60,61) (90,91)
(110,111) (130,131) (150,151) (170,171) (200,201) (250,251) (300,301) (350,351) (400,401)`.
주소 두 개가 채워지면 800ms 뒤 거리 자동계산이 도는 것이 기본 동작이라 실제로 마주치는
버그였다(시내 배송 구간에서 약 6%).
**(1) 🔴 고친 방식 — "상한 이하 첫 구간"이며 `distance_from_km`을 보지 않는다.**
`.filter(vehicle_type)` → `.sort(distance_to_km 오름차순, null은 Infinity)` →
`.find(to === null || distance <= to)`. 🔴 **`distance_from_km`을 조건에 다시 넣지 말 것** —
그 순간 빈틈이 되살아난다. 구간을 추가·삭제해도 자동으로 대응되는 것이 이 방식의 핵심이다.
⚠️ `.filter()`가 새 배열을 만들기 때문에 `.sort()`가 `tiers` state를 훼손하지 않는다
(정렬을 `tiers`에 직접 걸면 React state를 제자리 변경하게 된다).
🔴 **반올림 방식(거리를 정수로 만든 뒤 매칭)으로 바꾸지 말 것** — `10.4 → 10`이 되어
10km를 넘은 운송에 10km 이내 요금이 적용된다(과소청구). 34차 개정 약관 제11조 2항이 추가
청구를 "사유와 금액을 안내하고 협의한 후"로 묶어놨기 때문에 **처음부터 덜 부른 금액을
나중에 올리기가 어렵다.** 지시서 부록 A가 이 판단을 남겨둔 이유다.
**(2) 🔴 매칭 로직을 쓰는 파일은 `app/admin/quotes/page.tsx` 한 곳뿐이다(전수 확인함).**
지시서가 "최근 세 차수 연속으로 대상 수를 적게 셌다"며 직접 세라고 요구한 항목이라 6개
후보를 전부 열어 봤다. **지시서에 적힌 6개 파일 목록은 `distance_*_km`이 아니라
`rate_surcharges|rate_vehicle_extra_fees`로 grep했을 때 나오는 목록이었다**(지시서의 grep을
그대로 돌리면 2개 파일만 나온다). 결과: `admin/quotes/[id]`(견적 상세 수정)와
`customer/request`(발주요청)는 **`rate_surcharges`에서 `category,option_name`만 가져와
드롭다운을 채울 뿐 운임을 계산하지 않는다**, `admin/rates`는 `distance_from_km`을
`.order()` 정렬키로만 쓰고 표는 `distance_label`로 그린다, `api/admin/rates`는 `base_fare`
UPDATE 전용, `lib/legal/terms.ts`는 **주석 한 줄**(법적 문서라 무변경). 🔴 **견적 상세가
기본운임을 재계산하지 않는 것은 5차 세션(PR #49)이 의도적으로 그렇게 둔 것**이니
"거기도 고쳐야 하는 것 아닌가" 하고 손대지 말 것(최종금액만 직접 조정하는 구조).
**(3) 🔴 지시서 2-2와 실제 코드가 달라 착수 전에 보고하고 확정받았다.** 지시서는 계산
패널 문구를 「거리 또는 톤수가 비어 있음」과 「둘 다 입력됐는데 null」로 가르라고 했는데,
**이 화면에 "비어 있음" 상태가 존재하지 않는다** — `vehicle_type`은 빈 옵션이 없는
`<select>`에 초기값 `"1톤"`이라 비워질 수 없고, `distance_km`이 비면 `Number("") || 0`으로
`0`이 되어 첫 구간에 걸린다(아무것도 입력 안 해도 40,000원이 표시된다). 지시서 표대로
만들면 「현행 문구」 쪽이 **렌더링되지 않는 죽은 코드**가 되므로, 사용자 확인을 받아
**「로딩 중(`ratesLoading`)」 / 「그 외」**로 갈랐다. 후자가 지시서가 요구한
"선택하신 톤수에 해당하는 운임기준이 없습니다. 운임기준표를 확인해주세요."다.
🔴 **이 문구는 17차(차급 9종 확장)를 위한 안전장치다** — 드롭다운 배열에만 차급을 추가하고
`rate_distance_tiers` INSERT를 빠뜨리면 예전에는 "거리와 톤수를 입력하면 계산됩니다"라는
엉뚱한 안내가 떠서 담당자가 자기 잘못인 줄 알았다. **되돌리지 말 것.**
⚠️ 저장 시 오류 문구(`:665`)와 「운임기준 데이터가 아직 없습니다」 배너(`:900`, 톤수 무관
전체가 0건일 때)는 **성격이 달라 그대로 뒀다** — 셋이 각각 다른 상황을 가리킨다.
**(4) 검증** — `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과
항목 단위로 완전히 동일**(origin/main을 별도 worktree로 빌드해 diff) + 빌드 경고 보험 1건
그대로 + **임시 스크립트 순회**(0.0~450.0 step 0.1 × 6차급 **27,006건에서 미매칭 0건**,
수정 전은 756건 = 빈틈 14 × 소수 9 × 차급 6으로 정확히 떨어진다 / 정수 0~450 × 6차급
**2,706건에서 수정 전후 `base_fare` 전부 동일**해 회귀 없음 / 경계값 12개 12/12) +
**임시 스텁 페이지로 두 문구 분기를 실제 렌더링해 확인**(확인 후 삭제, 커밋 안 함).
🔴 **순회 스크립트가 쓴 구간 데이터는 실제 DB가 아니라 재구성본이다** — 이 환경에 Supabase
자격증명이 없다. 경계는 지시서가 명시한 빈틈 14개에서 복원했고 **15구간 × 6차급 = 90행**으로
지시서의 "90행"과 맞는다. 완료조건 1·2는 `base_fare` 값과 무관한 구조 검사라 재구성본으로
충분하지만, **실제 값으로 다시 재고 싶으면 사용자에게 SQL 결과를 받아야 한다.**
⚠️ **화면 확인(지시서 4-2·4-3)은 이 환경에서 못 했다** — `/admin/quotes`는 로그인과 실제
운임 데이터가 필요한데 자격증명이 없고 프록시가 `vercel.app`을 차단한다(29·30·43차와 같은
제약). **Preview에서 거리 `10.4` · 톤수 `1톤`을 사용자가 확인할 항목이다.**
⚠️ **당시 기대값은 50,000원이었으나 16차(45차 세션)가 운임을 올려 지금은 60,000원이다** —
이 문단은 15차 시점의 기록이므로 숫자만 여기서 정정해둔다.
**(5) ⚠️ 검증 중 겪은 함정 3가지**: (a) **`node_modules`가 설치돼 있지 않은 상태로 세션이
시작된다** — 이걸 모르고 `npx tsc --noEmit 2>&1 | tail -5`로 확인했다가 `$?`가 tsc가 아니라
**`tail`의 종료코드**를 읽어서 "통과"로 잘못 판단했다(실제로는 "Cannot find module 'react'"가
전부 잘려 있었다). **`npm ci` 먼저 돌리고, tsc 결과는 파이프를 걸지 말고 파일로 받아
종료코드를 볼 것.** (b) `pkill -f "next dev"`가 **자기 자신을 죽인다**(39·40차와 같은 함정) —
포트를 바꿔 새로 띄울 것. (c) 스텁 페이지를 클라이언트 컴포넌트로 만들면 `searchParams`가
안 들어온다 — **서버 컴포넌트로 두면 그대로 동작한다.** 그리고 루트 레이아웃의 `TopNav`가
Supabase 환경변수 없이는 던지므로 **더미 `NEXT_PUBLIC_SUPABASE_URL`/`ANON_KEY`를 넣고
dev 서버를 띄워야** 스텁이 200으로 뜬다(더미라 실제 DB에 닿지 않는다).
**(6) 🔴 왜 16차보다 먼저 했나** — 16차가 `base_fare` 90칸을 교체한 뒤 회귀 테스트를 돌릴 때
이 버그가 살아 있으면 **계산이 안 되는 것이 데이터 탓인지 로직 탓인지 가려낼 수 없다.**
순서를 바꾸지 말 것.
**(7) 사용자 질문에 답한 것(코드 변경 없음) — 운임기준표에 11톤·18톤·25톤은 없다.**
차급은 6종(`["1톤","1.4톤","2.5톤","3.5톤","5톤","5톤 플러스/축"]`)이고, 이 배열이
**서로 참조하지 않는 사본 3개**로 하드코딩돼 있다 — `lib/constants.ts:24`(`VEHICLE_TYPES`)·
`app/admin/quotes/page.tsx:26`·`app/admin/rates/page.tsx:19`. 지시서 부록 B의 **17차
"차급 9종 확장(배열 분리 + 로컬 재정의 정리 + 45행 INSERT)"**이 정확히 이 3종을 더하는
작업이다(6+3=9, 45행 = 15구간 × 3차급). 🔴 **내부 운임기준표에 대형을 넣는 것과 랜딩에
대형을 약속하는 것은 다른 결정이다** — 고객 노출 문구는 32차·12차 확정대로
**"1톤부터 5톤 이상까지"**이고 `"25톤"`·`"전 차종"`은 금지다(`/vehicles`는
"5톤 이상 대형 차량도 문의 주시면 확인해 안내드립니다."로만 적혀 있다). 17차에서 차급을
늘리더라도 **공개 문구는 건드리지 말 것.**
43차
세션: "동의 절차(14차)" — 동의 기록 저장 구조 신설. 🔴 **DB 변경 있음**(`consents` 테이블).
신규 파일 4개(`migrations/2026-08-21_consents.sql`·`lib/consent.ts`·
`app/api/quote-submit/route.ts`·`components/ConsentInfo.tsx`). 커밋 6개.
**PR #90 merge됨 — 리뷰 지적은 없었다.** 사용자 질문 1건("Supabase에서 어떻게 확인하나")에
코드 변경 없이 조회용 SQL로 답했고, 그 SQL은 PR #90 코멘트에 남아 있다(다시 짜지 말고 가져다 쓸 것).
⚠️ **42차는 이 작업의 사전조사 전용 세션**(코드·DB 무변경)이었고 그 결과가 지시서의 근거다.
**동의 절차는 두 번 밀려 14차가 됐다**(11차→12차→14차).
**(0) 왜 했나 — 🔴 동의 체크박스가 어느 화면에서도 저장되지 않고 있었다.** 42차 사전조사에서
코드·DB 양쪽으로 확정된 사실이다. 체크박스는 제출 버튼을 막기만 했고 `agreed`는 `form` state와
분리돼 **서버로 가지도 않았다.** 법적 동의는 입증 책임이 있어 기록이 없으면 받지 않은 것과 같다.
**(1) B안(이력 테이블) 채택 — 🔴 A안(각 테이블에 컬럼 추가)은 마케팅 동의에서 막힌다.**
연락처가 3개 테이블에 흩어져 있고 실측 결과 **고유 번호 4개 중 3개(75%)가 두 개 이상 테이블에
걸쳐** 있어, A안이면 같은 사람의 동의가 갈라지고 값이 다를 때 어느 쪽을 따를지 정할 수 없다.
`sms_logs`·`settlement_field_change_logs`·`support_access_logs`라는 폴리모픽 이력 관례가 이미 셋 있다.
**(2) 🔴 `consents` 설계 — 바꾸기 전에 이유부터 볼 것.** FK 없음(폴리모픽이라 불가능 + 원본이
지워져도 동의는 남아야 함) / **INSERT만, UPDATE 없음**(철회는 `agreed=false` 행을 **추가**) /
**RLS on + 정책 0개**(정책을 하나라도 만들면 그 순간 anon에게 열린다 — anon GRANT는 Supabase
기본값이고 실제 게이트가 RLS다) / **CHECK 제약 없음**(값 목록은 `lib/consent.ts`가 관리.
`dispatch_status` CHECK 때문에 배차 등록이 막힌 전례 회피 — 대신 **그 파일이 유일한 방어선**) /
`subject_id`가 text(마케팅 동의를 연락처 단위로 붙일 때 정규화된 번호를 담기 위함).
**(3) 🔴 `/quote` 제출을 서버 API로 옮겼다**(`app/api/quote-submit/route.ts` 신설).
`public_quote_requests`에 **INSERT 정책만 있고 SELECT 정책이 없어**(원칙 3) anon 직접 insert로는
**방금 넣은 행의 id를 못 받고**, 그러면 `consents.subject_id`를 채울 수 없기 때문이다.
⚠️ **anon INSERT 정책은 제거하지 않았다** — 실사용 검증 후 지우는 것이 안전(다음 차수 항목).
**(4) 🔴 롤백** — Supabase JS에 트랜잭션이 없어 원본 insert 성공 + `consents` 실패면 **동의 없는
접수 건이 남는다.** 그래서 원본 행을 삭제하고 에러를 반환한다(`approve-application`이 계정 발급
실패 시 회사를 되돌리는 것과 같은 패턴). `lib/sendSms.ts`처럼 조용히 넘기는 부가기능과 **성격이
반대**이니 `recordConsents` 실패를 삼키지 말 것.
**(5) 🔴 저장되는 동의는 `privacy` 1행뿐이다**(양쪽 화면 동일). 두 문구가 "개인정보 수집·이용에
동의합니다"로 끝나고 **이용약관을 언급하지 않기 때문** — 받지 않은 동의를 기록하면 그 기록이
거짓이 되어 입증 자료로 쓸 수 없다. **그 결과 `terms` 동의를 받는 화면이 하나도 없고
`TERMS_VERSION`은 계속 미사용이다.** 약관 동의를 받으려면 **먼저 화면 문구에 약관을 넣어야** 하며
그건 법적 문구 개편이라 별도 차수다. `CONSENT_TYPES_BY_SOURCE`에 `"terms"`를 먼저 넣지 말 것.
**(6) 동의 문구를 `lib/legalInfo.ts` 상수로 옮겼다**(화면 JSX 하드코딩 0건). 버전 상수가 같은
파일에 있어 문구를 고칠 때 버전을 같이 보게 된다. `/quote` 문구의 **사실 오류 1건만** 고쳤다 —
예전 문구는 보관하지 않는 것처럼 읽혔으나 실제로는 저장·조회되고 파기 로직도 없어서, 처리방침
제3조에 맞춰 **"문의일로부터 1년간 보관 후 파기됩니다"**로 교체했다(새 법적 판단이 아니라 원문과의
정합성 수정). 🔴 **버전은 올리지 않았다** — 처리방침·약관 **본문이 안 바뀌었고** 기존 동의
데이터가 0건이라 "같은 v1인데 다른 문구에 동의한 사람"이 생기지 않는다. **다음에 본문을 고칠 때는
반드시 올릴 것**(이유를 상수 파일 주석에 못박아뒀다).
⚠️ `/apply` 문구는 **내용 그대로** 상수로만 옮겼다(기간 미기재이지만 사실과 어긋나지는 않음).
**(7) 관리자 표시** — 신청 상세 모달(「처리 이력」 위)·공개문의 상세 모달(「접수정보」).
`consents`는 anon 조회 불가라 **각 서버 API가 원본 행에 붙여 내려준다**(id 목록으로 한 번만 조회).
⚠️ **14차 이전 접수 건에는 기록이 없다** — "동의하지 않았다"가 아니라 저장 기능이 없던 것이라
그렇게 읽히지 않도록 안내 문구를 따로 뒀다. 🔴 **화주 상세는 범위 밖**(승인 시 담당자 정보가
`companies`로 복사돼 동의는 신청서 id에 붙는데 화면은 화주 상세 — 연결은 별도 판단).
검증은 `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목 단위로
동일** + 빌드 경고 보험 1건 그대로 + **Playwright 71개 항목 전체 통과**(양쪽 폼이 서버 API로
POST하고 **payload에 `agreed: true`가 실제로 들어가는지 네트워크 인터셉트로 확인** / 문구 교체와
옛 문구 0건 / ConsentInfo 4가지 경우 렌더링 / 공개 12개 화면 × 360·390·430px 가로스크롤 0 /
금지 표현 8종 0건 / 13차·12차·31차·41차 회귀) + **서버 이중 방어 실측**(`agreed` 누락·false로
직접 POST → 양쪽 API가 400. 원칙 25번대로 화면 검증은 콘솔에서 우회되므로 서버에서도 막는다).
🟢 **실제 저장까지 확인됐다(2026-08-25 — 사용자가 마이그레이션을 실행하고 양쪽 폼을 실사용).**
`consents` 존재 · RLS on · **정책 0개** · 동의 2행을 확인했고, 원본 테이블에
**`subject_id = id::text`로 left join**한 결과 `/quote` 1건과 `/apply` 1건에 각각
`privacy`/`privacy-v1`/`true`가 **정확히 한 행씩** 붙어 있었다(중복 저장 없음).
총 동의행수(2)와 join된 행수(2)가 같아 **어디에도 안 붙은 고아 행도 없다.**
⚠️ **다만 롤백 경로는 아직 한 번도 실행되지 않았다** — `consents` insert가 실패해야 타는 길이라
정상 흐름에서는 재현되지 않는다. 코드가 단순하고 서버 게이트(400)는 실측했지만 **미검증은 맞다.**
⚠️ **마이그레이션 전에는 두 폼의 제출이 전부 실패한다** — 버그가 아니라 롤백이 매번 걸리는
설계된 동작이다. "문의가 접수되지 않는다"는 신고를 받으면 **테이블 존재 여부부터** 볼 것.
⚠️ 확인용 SQL은 **PR #90 코멘트**에 그대로 있다(다시 짜지 말 것). 🔴 **`select * from consents`로
"행이 생겼다"만 보면 안 된다** — 어느 접수 건에 붙었는지가 확인되지 않으므로 **join까지 볼 것.**
⚠️ **이 실행 환경에서는 확인할 방법이 없다** — 컨테이너에 Supabase 자격증명이 없고,
프록시 정책이 `vercel.app`을 차단해 배포된 사이트에도 닿지 못한다(29·30차와 같은 제약).
**사용자에게 SQL 결과를 받는 것이 유일한 경로다.**
⚠️ **검증 중 겪은 함정 2가지**: (a) 임시 스텁 페이지를 `app/__consent_stub/`에 만들었더니 404 —
**`_`로 시작하는 폴더는 App Router의 private folder라 라우팅되지 않는다.** `app/consent-stub/`로
바꿔 해결(확인 후 삭제). (b) 컨테이너 TZ가 UTC라 09:30(KST)이 00:30으로 렌더돼 시험이 실패했다 —
코드 문제가 아니라 **시각을 보는 사람의 로컬 TZ로 표시하는 저장소 관례**대로 동작한 것이다.
41차
세션: "랜딩 진입 경로 소수정" — **지시서 없는 요청 2건**(36차와 같은 방식, 차수를 쓰지 않음).
**DB 변경 없음.** 신규 파일 없음. 커밋 1개. **PR #89 merge됨**(리뷰 지적 없이 1라운드 통과).
⚠️ **로드맵 14차(동의 절차)와는 별개다** — 차수를 소비하지 않았으니 다음 지시서는 여전히 14차.
**(1) 히어로 CTA 3개 → 2개.** 세 번째 보조 링크 "운송관리 계정 신청 →"을 제거하고
`[무료 견적 문의] [전화 문의 1661-2403]`만 남겼다. 🔴 **첫 화면에 선택지가 셋이면 결정이
늦어진다**(9차 시안 검수 결과) — **히어로에 세 번째 선택지를 다시 넣지 말 것.**
⚠️ 32차 기록의 "히어로 CTA 2개 + 보조 링크"와 28차의 "랜딩 Hero의 계정 신청 CTA로 유도"는
**이 변경으로 무효가 된 서술**이다(그 문단들은 당시 기록이라 그대로 뒀다).
쓰이지 않게 된 `.hero-secondary-link` CSS 2블록(데스크탑·모바일)도 지웠고, 히어로 **모바일
순서를 설명하던 주석 2곳**을 "텍스트 → CTA 2개 → 신뢰 3줄 → 이미지"로 정정했다 —
안 고치면 다음 세션이 존재하지 않는 링크를 찾는다.
**(2) `/customer/login` 폼 하단에 "계정이 없으신가요? 운송관리 계정 신청 →"**(→ `/apply`,
`BackToHomeLink` **위**). (1)로 **데스크탑 랜딩의 계정 신청 진입점이 ⑥ 하나만 남기 때문**에
그 손실을 메우는 것이다(두 건은 하나의 결정이니 한쪽만 되돌리지 말 것).
🔴 **`/customer/support-verify`에는 붙이지 않았다** — 같은 `PUBLIC_PATHS` 분기라 헤더는
함께 붙지만(12차), 지원 접속 인증 화면에 계정 신청 링크는 맞지 않는다.
🔴 **`BackToHomeLink`는 그대로 뒀다**(왔던 자리로 복귀라 역할이 다름). 위계는
**계정 신청(`--brand-yellow-text`·굵게) > 홈으로(회색 보조)**.
⚠️ **모바일 진입점은 원래대로다** — 햄버거 드롭다운 4개에 "운송관리 계정 신청"이 그대로 있다.
검증은 `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목 단위로
동일** + 빌드 경고 보험 1건 그대로 + **Playwright 66개 항목 전체 통과**(히어로 CTA가 **정확히
2개**·히어로 안 `/apply` 0건·`.hero-secondary-link` 0건 / ⑥ 버튼과 드롭다운 4개 유지 /
로그인 링크 위치·이동·`BackToHomeLink` 존치 / **`/customer/support-verify` 0건** /
`/customer/login` noindex·`public-form`·16px·헤더 회귀 / 공개 12개 화면 × 360·390·430px
가로스크롤 0 / 13차 구조 회귀) + **`"고객 등록"` grep 8건으로 변동 없음**(전부 법적 문서·
`/apply` 동의 문구·라벨 이력 주석).
⚠️ **검증 중 겪은 함정 2가지**: (a) 처음 쓴 주석에 "고객 등록"이 들어가 grep이 8 → 10건이
됐다 — 12차와 같은 **"주의 표현을 경고하는 주석 자체가 매치되는" 오탐**이라 문구를 바꿔
되돌렸다. (b) **서버가 뜬 채로 `npm run build`를 다시 돌리면** 청크 해시가 어긋나 화면이
`Application error: a client-side exception has occurred`로 나온다 — 코드 문제가 아니라
재빌드 타이밍 문제다. 포트를 바꿔 새로 띄우고 응답으로 확인할 것(13차에 적어둔 것과 같은 결).
40차
세션: "랜딩 섹션 신설 — 운송관리·안전책임(13차)" — 섹션 2개 신설 + 헤더 개편 + 문구 정합.
**DB 변경 없음.** 신규 파일 1개(`components/BackToHomeLink.tsx` — 리뷰 반영분).
커밋 7개 + 리뷰 반영 1개 = 8개. 사용자가 Preview에서 실사용 확인 후 **PR #88 merge됨**.
🔴 **근거 시안 HTML(`WeCarry 랜딩 9차 시안 단일파일 검수본.html`)이 전달되지 않아** 지시서
본문의 확정 문구·순서·색·완료조건만 근거로 구현했다 — 여백·카드 기하 같은 세부는 판단으로
채웠고 아래에 전부 적어뒀다.
**(1) 섹션이 6개 → 8개가 됐다.** 배경 교차는 ① 다크 → ② 흰 → ③ 옅은 노랑 → ④ 흰 →
⑤ 회색 → **⑥ 옅은 노랑 → ⑦ 흰** → ⑧ 다크. 🔴 **임의로 바꾸면 리듬이 깨진다.**
**(2) ⑤에서 두 블록을 뺐다.** 「첫 거래 혜택」 칩 4개는 **전체 제거** — "첫 운송 운임 지원"에
적용 조건·기간이 없어 표시광고 문제가 되고(32차에 금액·비율만 뺐고 조건 미기재는 남아
있었음), 나머지 칩 2개는 혜택이 아니라 ⑥·⑦과 겹치는 기능 설명이라 혜택을 빼면 중복만
남았다. 🟢 **이 결정으로 Go-Live 차단 항목 "첫 거래 혜택 조건·기간 병기"가 해소됐다** —
다시 쓰려면 조건·기간을 확정한 뒤 별도 차수로 설계할 것. 운송관리 곁다리 카드는 ⑥으로
승격했고 🔴 **본문 "전화와 문자로만 오가다 나중에 확인할 방법이 없는 일을 만들지
않겠습니다."는 버리지 않고 ⑦-1 설명문 둘째 문장으로 이관**했다.
⚠️ **`app/admin/companies/[id]`의 "첫 운송오더"는 그대로 뒀다** — 관리자 내부 문장이고
혜택과 무관하다(전수 grep에 걸리지만 제거 대상이 아님).
**(3) ⑥ 운송관리 — 🔴 모바일 순서는 `grid-template-areas`가 정한다**(기능 4줄 → 캡처 →
버튼 → 배지). DOM 순서가 아니라 영역 이름으로 배치하므로 데스크탑 2열과 모바일 1열을
각각 정확히 지정할 수 있다. **캡처를 모바일 맨 위에 두면 버튼이 한참 아래로 밀린다.**
🔴 **옅은 노랑 배경 위에 `--text-muted`를 직접 올릴 수 없어** 설명문을 흰 카드 안에 넣었다.
🔴 **버튼은 전용 클래스를 새로 만들었다** — `.btn`은 `.portal-theme`에서 검정 배경 +
**옐로 글자**라 흰 카드 위에서 흐리고, `.btn-ghost`는 `border: none`이라 테두리 버튼이
성립하지 않는다. "2~3시간 이내"는 차별점이라 큰 숫자로 강조했고 접수 시간 단서는 접지
않았다. 운영시간은 `COMPANY_SUPPORT_HOURS` 상수 참조.
⚠️ **시안 모바일의 "캡처가 카드 경계를 1px씩 넘침"은 따르지 않았다** — 좌우 1px씩이라
풀블리드 의도가 아니라 반올림 잔재로 보였고, 카드 안에 정확히 맞추는 편이 안전하다(보고함).
**(4) ⑦ 안전·책임 — 🔴 열 수가 항목 수를 따라간다**(2항목 2열 / 3항목 3열, `.landing-safety-grid-2`
/`-3`). **9/7 공개 시점의 실제 출시본은 보험이 꺼진 2항목**이다. 하단 회색 띠에 허가번호와
`tel:` 링크(색 `#8a6d00`, 번호는 상수 참조).
**(5) 🔴 보험은 `INSURANCE_ENABLED` 하나가 두 곳을 제어한다** — 히어로 신뢰줄 4번째와
⑦-3 카드. 별도 노출 플래그를 만들지 않았고, false면 **배열에 항목을 넣지 않아 DOM에 아예
없다**(display:none이면 페이지 소스에 남는다). 🔴 **히어로에 한도 숫자를 넣지 않았다** —
금액이 나오는 곳은 ⑦ 카드 한 곳뿐이다. 히어로 신뢰줄 그리드는 `li:nth-child(3):last-child`
한 줄로 처리했다(OFF 3줄이면 3번째가 전체 폭, ON 4줄이면 저절로 풀려 2×2).
🔴 **플래그 값 2개는 바꾸지 않았다**(`ENABLED=false`/`IS_PLACEHOLDER=true`) — 켜는 것은
가입 완료 후 별도 작업이고 **그때 둘을 같이 바꿔야 한다**(상수 파일 주석에 명시).
⚠️ 시안의 "…가입 확정 후 이 자리에 표기됩니다(INSURANCE_PRODUCT_NAME · …)" 문장은
시안 설명용이라 구현하지 않았다 — 실제로는 상수를 렌더링한다.
**(6) 「선택 이유」 3·4번을 한 문장으로 줄이고 앵커 링크를 붙였다**(`#safety`·`#management`).
계층이 나뉜다 — **「선택 이유」는 판단 기준(질문)과 약속, ⑥·⑦은 방법과 절차.** 여기에 방법
설명을 다시 늘리면 두 곳이 같은 말을 한다. 🔴 **4문항 유지**(늘리면 2×2가 깨짐). 앵커
오프셋은 31차의 `.portal-theme :target`이 이미 처리하므로 섹션에 `id`만 붙였다.
**(7) 헤더** — 「문의·신청 현황」을 데스크탑·모바일 **양쪽에서** 제거(12차에 푸터에 넣어둬서
중복 해소). "로그인" → **"운송관리 로그인" 테두리 버튼**이며 `.public-header-status-link`와
같은 방식(흰 칩 + 옅은 검정 테두리)으로 전용 클래스를 만들었다. 🔴 **데스크탑 전용** —
360px 헤더는 로고 156 + 햄버거 44 + 견적 문의 87 = 287px을 쓰고 **여유가 15px뿐**이라
보이는 요소를 더 넣을 자리가 없다(38차 실측). 🔴 **`PublicPageHeader`는 건드리지 않았다.**
**(8) "품목만" → "연락처만" — 🔴 지시서 표는 3곳이었지만 실제로는 5곳이었다.**
히어로·마감 CTA·`/` 메타 외에 **⑤ 1단계 설명**과 **`/quote` 메타 description**에도 같은
문장이 있었다. 완료조건이 `"품목만"` grep 0건이라 함께 고쳐야 했고, 특히 `/quote` 메타는
**견적 폼 자신의 검색결과 문장**이라 그대로 두면 "화면은 연락처만, 검색결과는 품목만"이
된다(12차 `/vehicles` 때 정한 원칙 그대로). 🔴 **폼을 줄여 맞추지 않았다** — 필수 항목을
줄이면 상담 품질이 떨어진다.
**(9) ⑤ 여백 — 🔴 처음 판단이 틀렸고 리뷰에서 뒤집혔다(아래 리뷰 라운드 참고).** 3단계만
남아 콘텐츠가 227px(1280 기준)로 줄었는데 다른 섹션과 같은 88/56px 패딩을 그대로 두는 편이
낫다고 보고했으나, 사용자가 "여백이 어색하다"고 지적해 **데스크탑 60/64px · 모바일 40/44px로
줄였다**(전용 클래스 `.landing-section-steps`, 섹션 403 → 345px). 🔴 **이 섹션에 블록을 다시
넣으면 88px로 되돌릴 것.**
⚠️ **360px 헤드라인은 3줄이다**(완료조건 16은 2줄). 37차 확정값 28px에서 둘째 줄이 394px를
요구하는데 가용 폭이 320px라 **물리적으로 2줄이 안 되고**, 2줄로 만들려면 23px 이하로
줄여야 해서 임의로 바꾸지 않았다. **13차에서 생긴 문제가 아니라 37차부터의 상태**이며
어절 중간에서 끊기지는 않는다(`word-break: keep-all`).
**사용자가 리뷰에서 이 상태를 보고 "ok"로 확인했으므로 지금 상태가 의도된 것이다** —
다음 세션이 완료조건 16만 보고 다시 줄이려 하지 말 것.
검증은 `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목 단위로
완전히 동일**(origin/main을 별도 worktree로 빌드해 diff) + **빌드 경고는 보험 1건 그대로** +
**Playwright 149개 항목 전체 통과**(⑤ 3단계·제거 2건 / 이관 문장 생존 / 섹션 8개와 배경
교차 8색 / ⑥ 버튼 2개·링크·기능 4줄·배지 강조·흰 카드 / ⑥ 모바일 순서 4단계와 카드 경계 /
⑦ 2항목 2열·허가번호·tel·색 / **공개 12개 화면 "보험" 0건** / 신뢰줄 3줄과 3번째 전체 폭 /
선택 이유 4문항·앵커 2개·**클릭 후 제목이 헤더에 안 가림** / 헤더 데스크탑·모바일 4개·
`PublicPageHeader` 회귀·`--text-muted` 0건 / `"품목만"` 본문·메타 0건 / 공개 12개 화면 ×
360·390·430px 가로스크롤 0 / 금지 표현 8종 0건 / 31차 스티키·모달, 12차 진입경로·상수 회귀)
+ **보험 토글 실측 24개 항목**(임시로 `true`로 바꿔 빌드 — 히어로 4번째 줄과 ⑦-3이 **함께**
켜지고, 신뢰줄이 2×2가 되고, ⑦이 3열로 바뀌고, 상수값이 렌더링되는 것까지 확인 후 되돌림).
⚠️ **「배상」은 약관 제19조(손해배상)·처리방침의 법령 인용이라 법적 문서에는 남아 있어야
한다** — 34차가 정한 "약관 외 화면에 보험·배상 0건" 기준을 그대로 따랐다.
⚠️ **검증 중 겪은 함정 2가지**: (a) `<picture>` 테스트에 **data URI를 쓰면 안 된다** —
data URI에 콤마가 있어서 `srcSet` 후보 구분자로 해석돼 모바일 자산이 안 골라진다(코드
문제가 아니라 시험 자산 문제였다). 임시 PNG 파일을 `public/`에 두고 다시 재니 760·390px에서
정상적으로 모바일 자산을 골랐다(확인 후 삭제). (b) `pkill -f "next start"`가 **자기 자신을
죽인다**(명령줄 문자열이 패턴에 매치됨) — 포트를 바꿔 새로 띄우고 **응답 본문으로 새 빌드가
맞는지 확인**하는 방식으로 진행했다.
**PR #88 실사용 리뷰 라운드 2건 — 둘 다 반영 후 merge됨**(확인 항목 10개 중 8개는 "ok"):
**(a) ⑤ 여백이 어색하다** — 위 (9)번. **내가 "그대로 두는 편이 낫다"고 보고한 판단이
틀렸다.** 콘텐츠가 227px로 줄었는데 88px 패딩을 그대로 두니 **내용보다 여백이 더 큰** 띠가
됐다. 제목↔진행선 간격도 44 → 38px로 함께 조였다. ⚠️ **다음에 섹션에서 블록을 빼면 여백을
같이 보라** — 패딩은 "섹션 리듬"이 아니라 **그 섹션의 내용 높이**에 맞춰야 한다.
**(b) 운송관리 로그인·계정 신청 화면에 홈으로 가는 버튼 + 왔던 자리로 복귀** —
`components/BackToHomeLink.tsx` 신설(`/customer/login`·`/apply` 폼 하단).
🔴 **헤더 로고와 동작이 다르다**: 로고는 홈 맨 위로, 이 링크는 **왔던 자리로**. ⑥ 운송관리가
페이지 한참 아래라 맨 위로 튕기면 스크롤을 다시 내려야 했다. **12차에 "헤더 로고와 중복"이라며
뺐던 링크를 이 동작 차이 때문에 되살린 것**이니 다시 중복으로 보고 지우지 말 것.
구현은 `href="/"`를 두고 **평클릭만 가로채 `router.back()`** — 브라우저 뒤로가기가 **스크롤
위치까지 복원**한다(실측 3434 → 3434, 2200 → 2200). sessionStorage에 위치를 저장하는 방식보다
정확하고 값이 낡을 일도 없다. 랜딩에서 왔는지는 **`?from=landing`**으로 판정하며 랜딩이 보내는
링크에만 붙는다(⑥ 버튼 2개 + `LandingHeader`가 **pathname이 `/`일 때만**) — `/about`·`/vehicles`
에서 눌러 들어온 경우엔 안 붙어서 평범하게 `/`로 간다.
🔴 **`document.referrer`로 판정하면 안 된다** — 랜딩에서 넘어오는 경로가 `next/link` 클라이언트
전환이라 **referrer가 갱신되지 않고 빈 값으로 남는다.** 처음에 그렇게 만들었다가 복원이 전혀
동작하지 않는 것을 실측으로 발견해 교체했다. **이 저장소의 화면 간 이동은 대부분 클라이언트
전환이므로 referrer 기반 판정을 다른 곳에도 쓰지 말 것.**
⚠️ `useSearchParams()` 대신 `window.location.search`를 효과 안에서 읽는다(원칙 38번의 Suspense
경계 요구를 건드리지 않으려는 것). `history.length <= 1`(새 탭)이면 가로채지 않는다.
⚠️ **`/apply` 접수 완료 화면의 기존 "홈으로 돌아가기" 버튼은 그대로 뒀다** — 신청을 마치고
떠나는 자리라 홈 맨 위가 맞다고 판단했고 보고함(바꿔달라는 요청은 없었음).
39차
세션: "정합성·상수·진입경로·이미지 구조(12차)" — 시안이 필요 없는 정리 작업 8건.
**DB 변경 없음.** 신규 파일 1개(`lib/insuranceInfo.ts`). 커밋 5개로 분리했고
리뷰 반영 1개가 더해져 총 6개.
⚠️ **38차는 이 작업의 사전조사 전용 세션**(코드·DB 무변경)이었고, 그 조사 결과가 이번
지시서의 근거가 됐다. **동의 절차는 두 번 밀려 14차가 됐다**(11차→12차→14차).
**(1) 차량 범위 표기에서 단종 소형 차종을 뺐음** — 2021년 5월 단종이라 신차가 없고 전부
중고 운행분이어서 배차 확보가 불확실함. **못 잡는 차를 광고하면 첫 통화에서 신뢰를 잃음.**
표현 기준을 **"1톤부터 5톤 이상까지"**로 확정. 🔴 **"이상" 한 단어가 금지와 허용을 가른다**
— "1톤부터 5톤까지"는 상한을 못 박는 금지 표현이고, 검증은 정규식
`1톤(부터|~)\s*5톤(?!\s*이상)` 0건으로 한다. ⚠️ **고객 노출 지점이 지시서 표(2곳)보다
많은 4곳이었음**(38차 조사에서 발견) — 랜딩 히어로 신뢰 3줄·랜딩 ④ 차량 형태 부제·
**`/vehicles` 「차량 형태」**·**`lib/companyInfo.ts`의 `/about` 인사말 원칙 1**. 뒤 2곳이
지시서에 없었고, 그대로 뒀으면 랜딩에서만 지우고 다른 페이지엔 남을 뻔했음. 같은 파일들의
"차량 범위 표현 기준" 주석 3곳도 함께 고쳤고(안 고치면 다음 세션이 옛 기준을 보고 되돌림),
**주석에도 차종명을 쓰지 않아** 저장소 전체 grep이 0건이 되게 했음. 🟢 **`/vehicles`의 표
2개(`VEHICLE_SPECS`·`START_PRICES`)·`lib/constants.ts`의 `VEHICLE_TYPES`·`PRICING_NOTES`는
손대지 않았음** — 이미 1톤부터 시작해서 그대로 두는 편이 정합성이 맞음.
**(2) 법적 문서 시행일 2026-09-07 확정**(목표 공개일) + 플래그 false. 🔴 **값과 플래그는
항상 같이 움직인다** — 값만 바꾸면 빌드 경고가 계속 남음. ⚠️ **소스 주석의 "5곳"은 문서
본문만 센 숫자였고 실제 표시 지점은 11곳**(본문 5개 항목 + 페이지 헤더 3 + **법적 문서
모달 3**). `legalInfo.ts` 주석과 `next.config.mjs` 경고 문구를 둘 다 정정했음.
🔴 **이메일무단수집거부 게시일은 `lib/legal/emailPolicy.ts`에 참조가 0건**이고
`app/email-policy/page.tsx`가 `effectiveLabel` prop으로 넘김 — **내용 파일만 보고 찾으면
놓친다.** **(3) Vercel 문의처** `privacy@vercel.com` 기입 + 플래그 false(29·30차에 네트워크
차단으로 못 채웠던 값을 사용자가 확인해 전달). `lib/legal/privacy.ts:41`의
`VERCEL_PRIVACY_CONTACT || "확인 중"` 폴백은 그대로 뒀으므로 **값을 비우면 다시 "확인 중"**.
**(4) `/apply` 라벨을 "운송관리 계정 신청"으로 통일(화면 6곳)** — "고객 등록"/"고객 등록
신청"/"고객 등록 신청하기" 3가지로 갈려 있었음. 화주가 운송관리로 들어오는 경로가
"로그인"과 "계정 신청" 두 갈래인데 **둘 다 운송관리로 가는 문이라는 표시가 없어서** 용어로
동선을 이은 것. 같은 화면(`/status`) 안내 문구의 "고객 등록신청"도 함께 맞췄음(바로 아래
링크와 용어가 갈리면 통일한 의미가 없음). 🔴 **`lib/legal/privacy.ts` 5곳·`lib/legal/terms.ts`
제·목적 문구·`/apply` 동의 문구의 "고객 등록 심사"는 그대로 뒀음** — 법적 문서 본문이거나
**처리방침의 수집 목적 표와 짝을 이루는 문구**라 한쪽만 바꾸면 오히려 어긋남.
`/apply` 본문의 "검토 후 계정을 발급해드립니다"도 유지(라벨만 바꾸고 관리자 검토 절차를
숨기지 않음). **(5) `/customer/login`에 공개 화면 헤더 적용** ⚠️ **— PR #128 에 되돌렸다.
31차가 그 화면에 자체 헤더를 준 뒤로 같은 로고가 두 번 그려지고 있었고(노란 띠 81px +
흰 띠 66px), 브랜드 연속성은 자체 헤더가 그대로 지킨다. `/customer/support-verify` 는
자체 헤더가 없어 계속 공용 헤더를 받는다. 아래는 그 시점 기록이다** — 랜딩의 옐로 헤더를 보고
넘어오면 헤더가 사라지고 회색 배경에 카드만 남아 브랜드 연속성이 끊겼음.
`CustomerPortalShell`의 `PUBLIC_PATHS` 분기가 `PublicPageHeader`를 `showStatusLink={false}`로
렌더링함(로그인하러 온 사람에게 비회원 조회로 새는 링크를 주지 않으려고 칩은 끔).
🟢 **noindex에 영향 없음** — 색인 차단은 `app/customer/layout.tsx`의 metadata가 결정하고
이 경로엔 자체 layout이 없어 부모 설정을 상속함(헤더는 렌더 트리에 컴포넌트를 더할 뿐).
헤더 로고와 겹치게 된 폼 하단 **"← 홈으로" 링크는 제거**했고 🔴 **헤더를 다시 떼면 그
링크도 되살릴 것**(26차 PR #75에서 "들어오면 홈으로 갈 방법이 없다"는 신고로 넣었던 것).
`public-form` 클래스는 유지(입력창 16px, iOS 자동확대 방지 — 실측으로 확인).
⚠️ **같은 분기를 타는 `/customer/support-verify`에도 헤더가 함께 붙음.**
**(6) 푸터에 「문의·신청 현황」 추가** — 13차에서 헤더의 같은 항목을 뺄 예정이라 푸터가 그
자리를 대신하도록 미리 넣은 것. 12차만 배포된 기간엔 헤더·푸터 양쪽에 있지만 중복일 뿐임.
`SiteFooter`가 **서버 컴포넌트**라는 구조는 그대로 뒀음(법적 문서만 모달이라 `LegalLinks`로
분리해둔 것).
**(7) `lib/insuranceInfo.ts` 신설 — 참조하는 UI는 0건**(의도된 것, 13차 범위). 곧 가입할
예정이라 상수 파일을 먼저 만들어 13차가 UI에만 집중하게 한 것. 🔴 **아직 미가입이라
`INSURANCE_ENABLED = false`** — 미가입 상태에서 "보험 가입"이 화면에 보이면 허위·과장
광고임. 13차용 규칙 3가지를 파일 주석에 못박아뒀음: ① 플래그가 false면 **DOM에 아예
렌더링하지 않는다**(`display:none`이 아니라 조건부 렌더링 — 숨기기만 하면 페이지 소스에
남음), ② 보험 문구를 컴포넌트에 하드코딩하지 않는다, ③ **히어로 신뢰줄 4번째와 안전·책임
섹션을 이 플래그 하나로** 제어한다(별도 플래그 금지 — 한쪽만 켜지는 사고 방지).
⚠️ **지시서 템플릿에 없던 `INSURANCE_INFO_IS_PLACEHOLDER`를 추가했음(보고함)** — 빌드 경고
로직이 `= true` 문자열을 정규식으로 찾는데 `INSURANCE_ENABLED`는 미가입 상태에서 false라
검사에 안 걸림. 이게 없으면 완료조건("보험 경고 1건이 새로 나타난다")을 만족할 수 없음.
🔴 **플래그는 반드시 리터럴로 둘 것**(계산식으로 유도하면 경고가 조용히 죽음).
**(8) 이미지 구조 — 🔴 모바일 전용 자산의 기준은 "글자를 읽어야 하는가"** 하나다.
38차 조사에서 **선언만 되고 쓰이지 않던 키 2개**(`hero.mobile`·`portal.mobile`)가 나왔고,
둘을 다르게 처리했음: **`hero.mobile`은 키 자체를 제거**(트럭 사진에는 판독할 글자가 없어
데스크탑 자산 하나로 충분, 비율만 CSS가 바꿈 — **다시 만들지 말 것**), **`portal.mobile`은
실제로 연결**(캡처 안에 글자가 있음. 제작 1240px 자산을 모바일 320px에 표시하면 축소율이
**26%**라 13px 글자가 **6.7px로 뭉개짐**). `components/LandingImage.tsx`에 `mobileSrc` prop을
추가하고 `<picture>`로 렌더함. 🔴 **`.desktop-only`/`.mobile-only`로 두 `<img>`를 렌더링한
뒤 CSS로 숨기지 말 것 — 양쪽 다 내려받을 수 있음.** `<picture>`는 브라우저가 하나만 받음.
분기점 **760px**은 헤더가 데스크탑 메뉴 → 햄버거로 바뀌는 지점과 같게 맞춤(38차 실측).
⚠️ **`.landing-media picture { display: contents }`가 필요함** — 없으면 크기 없는 인라인
요소라 안의 `<img>`가 접힘. ⚠️ **모바일 캡처는 카드 뷰를 찍는다**(640px 폭에 목록 표가
안 들어가고 이 시스템은 모바일에서 표를 카드로 전환함 — `.mobile-row-card`).
검증은 `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목 단위로
동일** + **빌드 경고가 시행일·Vercel 2건 사라지고 보험 1건 신규**(실행 확인) +
**Playwright 197개 항목 전체 통과**(금지 표현 8종 × 공개 12개 화면 0건 / 단종 차종명 렌더링
0건 / 시행일 11곳 — **모달 3곳 포함** / 처리방침 "확인 중" 0건 / 라벨 6곳 통일과 "검토 후"
유지 / 약관 외 화면에 "보험"·"배상" 0건 / 푸터 링크 / `/customer/login` 헤더·옐로 배경·
noindex·`public-form`·입력창 16px·"← 홈으로" 중복 제거·현황조회 칩 미노출 / 공개 12개 화면 ×
360·390·430px 가로스크롤 0 / 31차 스티키·법적 문서 모달·26차 햄버거·드롭다운 5항목 회귀).
⚠️ **`<picture>`는 임시 값을 넣어 실제로 확인했음**(원칙 54번 방식) — 세 상태 전부
(둘 다 null → 자리표시자 9곳 / desktop만 → 단일 `<img>` / 둘 다 → `<picture>`+`<source>`)와
**브라우저가 고르는 자산**(1280·800px → desktop, 760·390px → mobile)까지 실측한 뒤 되돌림.
⚠️ **검증 중 겪은 함정**: `pkill`이 실패해 **임시 값으로 빌드된 예전 서버가 계속 떠 있는
채로** 1차 검증이 돌아 자리표시자 항목이 실패로 나왔음. 포트를 바꿔 새로 띄우니 통과.
**서버를 재시작했다고 가정하지 말고 응답에서 실제로 확인할 것.**
**PR #87 실사용 리뷰 라운드 1건 — 메타 description도 같은 기준으로**: 작업 중
`app/vehicles/layout.tsx`의 메타 description이 **"1톤부터 5톤 플러스/축까지"**인 것을
발견했으나, (a) 화면 본문이 아니라 검색결과용이고 (b) `/vehicles` 표의 실제 상한을 정확히
서술한 문장이며 (c) 그 표는 지시서 금지사항이라 손댈 수 없어서 **그대로 두고 판단을
요청**했음. 사용자가 "메타도 바꿔달라"고 확정해 `"1톤부터 5톤 이상까지"`로 교체함.
🔴 **검색결과에 그대로 노출되는 문장은 화면 본문과 같은 기준을 적용한다** — 다음에 이
자리를 고칠 때 헷갈리지 않도록 사유를 파일 주석에 남겼음. 표와 `PRICING_NOTES`는 여전히
무변경. ⚠️ **금지 표현 검증은 소스가 아니라 렌더링 텍스트에 적용해야 한다** — 소스에
걸면 `app/page.tsx`의 "쓰지 않기로 한 표현" 목록처럼 **금지어를 경고하는 주석 자체**가
매치됨(38차 조사에서 "옛 스냅샷 의혹"의 정체로 밝혀진 것과 같은 오탐). 현재 소스에 남은
매치 2건은 둘 다 그런 주석이다.
사용자가 Preview에서 실사용 확인 후 **PR #87 merge됨**(리뷰 라운드 1건 반영).
37차
세션: "랜딩페이지 레이아웃 전면 개편 + 공개 화면 헤더 옐로 전환(11차)" — 레이아웃·색상
전면 개편. **DB 변경 없음.**
⚠️ **작업지시서 차수가 겹쳤음** — 전달된 지시서 제목이 "11차"인데 이 문서 로드맵에는
"11차 = 동의 절차"가 예약돼 있었음. 이번 것을 11차로 확정하고 **동의 절차는 12차로 밀었음.**
지시서 머리말의 "세션 구분: 36차(예정)"도 실제로는 37차임(36차는 문자 이력 화면).
**(1) 헤더 옐로 전환 — `.public-header` 한 곳만 고치면 공개 10개 화면에 전부 반영됨**
(31차에 `LandingHeader`·`PublicPageHeader`가 이 클래스를 공유하도록 정리해둔 덕분).
관리자·운송관리 `TopNav`는 이 클래스를 안 써서 영향 없음(자동검증으로 확인).
🔴 **옐로(#ffd833) 위에 `--text-muted`(#5f6b78)를 쓰지 말 것** — 대비가 부족하다. 헤더 안
텍스트는 예외 없이 `--text`(#191f28) 계열. **배경색만 바꾸면 안 되는 것이 5개 더 있었음**:
메뉴 링크(`.guide-link`가 `--text-muted`+`--border`), **헤더 CTA**(기존 `.btn`이
`.portal-theme`에서 검정 배경 + **옐로 글자**라 옐로 헤더 위에서는 글자가 배경과 같은 색이
됨 → 검정 배경 + 흰 글자로 덮어씀), 햄버거 버튼, 현황조회 칩(`--accent-soft`가 옅은 노랑
이라 묻힘 → 흰 칩), 스크롤 그림자(`--shadow-sm`이 옐로 위에서 거의 안 보이고 탁하게 번짐
→ 강도 재조정).
**(2) 색상 토큰 5종 신규** — `--brand-yellow`(#ffd833, 27차에 등록만 해두고 "아직 어디에도
적용하지 않음"이던 것이 이번에 실제로 쓰이기 시작함) + `-soft`(#fffdf0 섹션 배경)/
`-border`(#f0e7c4 카드·칩 테두리)/`-tint`(#f5f2e3 아이콘 자리)/`-text`(#8a6d00 옅은 배경 위
진한 텍스트). ⚠️ **파랑 `--accent-soft`(#e8f3ff)를 랜딩에 재사용하지 말 것** — 랜딩은
`.portal-theme` 안이라 그 값이 #fff9d6으로 덮이긴 하지만 포털 배지용이라 색이 다름.
**(3) 🔴 섹션 배경 교차 순서가 시안 리듬의 핵심** — ① 히어로 다크 `#1a1a1a` → ② 운송 유형
흰색 → ③ 선택 이유 옅은 노랑 → ④ 차량 형태 흰색 → ⑤ 진행 단계 회색 `#f2f4f6` → ⑥ 마감
CTA 다크. **임의로 바꾸면 의도가 깨짐.** ⚠️ **회색에 `var(--bg)`를 쓸 수 없음** — 랜딩은
`.portal-theme` 안이고 거기서 `--bg`가 `#fffdf6`(옅은 노랑)으로 덮여 있어 회색이 안 나옴.
그래서 `#f2f4f6`을 직접 적었고, 흰색 섹션도 같은 이유로 배경을 명시함.
**(4) 신규 섹션 2개** — 「필요한 차량 형태로 배차해드립니다」(카고/탑차/윙바디/리프트 4종,
`/vehicles`의 `BODY_TYPES_SHOWN`과 같은 4종으로 맞춤)와 진행 단계 아래 「운송관리 화면」
카드. 🔴 **차량 형태는 모바일에서도 2×2를 유지** — 4종을 한눈에 비교하는 것이 목적이라
가로 스크롤로 바꾸면 안 됨. 운송 유형 01~06의 번호 배지는 **노란 원 + 검정 숫자**이며,
흰 배경에 노란 글씨만 올리면 대비가 부족해서 반전한 것이니 **색을 다시 뒤집지 말 것.**
**(5) 이미지 5종은 자리만 확보 — 🔴 Go-Live 차단 항목.** 경로는 `lib/landingImages.ts`
**한 곳에만** 두었고(`components/LandingImage.tsx`가 null이면 "이미지 준비 중" 회색 상자를
그림, 상자 크기는 사진 유무와 무관하게 같아서 넣어도 레이아웃이 안 밀림).
**컴포넌트에 경로를 직접 적지 말 것.** 규격은 히어로 **870×716**/360×170(시안 실측으로
확정 — **규격서의 520은 폐기**), 운송 유형 456×304/모바일 없음, 차량 4장 272×200/154×104,
아이콘 4종 64×64/48×48, 운송관리 캡처 560×240/284×130.
**(6) 지시서와 다르게 판단한 것 6건**(전부 PR 본문에 보고함): (a) **선택 이유를 5문항 →
4문항으로 병합**(사용자 확정) — 지시서 완료조건이 "2×2 / 1×4"인데 코드엔 5개였고, 시안이
「정산이 편한가?」와 「다음 거래가 더 편한가?」를 「반복 거래가 편한가?」 한 장으로 합쳐
적고 있었음. **32차 문구가 한 곳 바뀐 것이며, 다시 5개로 늘리면 그리드가 깨짐.**
(b) 신규 섹션 2곳의 문구는 기존 코드에 아예 없어서 **시안에서 읽은 것을 그대로 씀**(사용자
확정). (c) **`<br />`로 줄을 고정하지 않음** — 아래 (7) 참고. (d) 차량 설명을 데스크탑/모바일
두 벌(`desc`/`descShort`)로 둠 — 2×2 칸이 좁아 긴 설명이 3줄로 늘어짐. (e) 히어로
자리표시자만 어둡게(#262b31) — 다크 히어로 위에 밝은 회색 상자를 놓으면 화면이 반으로
갈라져 보임(나머지 4곳은 지시서대로 회색). (f) 지시서 4-1의 본문("무료 견적 문의")과 표
("무료 견적 받기")가 엇갈려 있어 기존 코드·시안대로 "문의"를 유지함.
**(7) 🔴 한글이 어절 중간에서 끊기던 문제 → 섹션 단위 `word-break: keep-all`**
("화물운송을 확실하게 관리합니 / 다"처럼 잘렸음). 본문 폰트(SUIT)를 **CDN에서 받아오므로**
폰트가 늦게 뜨거나 못 뜨면 글자 폭이 달라져 줄바꿈 위치가 바뀜 — **특정 폰트 폭에 맞춰
`<br />`로 줄을 고정하지 말 것.** 시안이 의도한 줄바꿈(히어로 H1·섹션 제목)만 남기고
운송관리 카드 제목의 `<br />`는 뺐음(고정하면 모바일에서 "내역이"만 남는 어색한 3줄이 됨).
**PR #86 실사용 리뷰 라운드 1건 — 히어로 폭·이미지 균형**: "데스크탑 히어로 헤드라인 창이
좁게 느껴지고, 이미지가 들어가면 왼쪽으로 쏠려 보일 것 같다"는 지적. **시안의 트럭 사진을
임시로 넣고 실측**해 보니 지적대로 두 가지가 실제로 있었음(임시 이미지는 커밋 전 삭제 —
자리표시자 상태에서는 눈에 안 띄는 종류의 문제라 이 방법이 유효했음).
(a) **콘텐츠 폭이 시안보다 좁았음** — 랜딩이 사이트 공용 `.container`(1100)를 그대로 써서
1280 기준 x=114에서 시작했는데 시안은 x=56(폭 약 1170)이었음. 랜딩 전용으로 1220으로 넓혀
x=54로 맞춤. ⚠️ **`.container` 자체는 건드리지 않았음**(관리자·운송관리·다른 공개 화면이
함께 씀). 헤더는 본문과 좌측이 어긋나면 안 되므로 `.landing-page` 안으로 범위를 좁혀 함께
넓혔고, **`/about`·`/vehicles`·`/quote` 등 다른 공개 화면 헤더는 1100 그대로**임.
(b) 🔴 **그라데이션을 히어로 전체 → 사진 위에만 얹도록 바꿈** — 덮개를 히어로 전체에 깔고
정지점을 %로 주면 **정지점이 뷰포트 폭 기준**이라 화면이 넓어질수록 텍스트와 사진의 관계가
달라짐(1920에서 본문 끝과 사진 시작이 맞물려 트럭이 제목 위로 올라오고 차체 로고가 글자
뒤로 비쳤음). 사진 위에만 얹으면 본문은 항상 단색 배경 위라 **어떤 사진이 와도 읽힘** —
**전체 그라데이션으로 되돌리지 말 것.** 사진 폭도 68% → 58%로 줄여 시안 비율에 맞춤.
(c) 800px에서 트럭이 본문·CTA 위로 올라와 **태블릿 구간(≤1000px)을 따로 보정**함(사진 52%
+ 덮개 강화 + 텍스트 560px). 겹침을 아예 없애지 않은 이유는 사진을 더 줄이면 히어로에서
차량이 아예 안 보이기 때문.
검증은 `npx tsc --noEmit` 통과 + `npm run build` 프리렌더 실패 **48개가 기준선과 항목 단위로
완전히 동일**(임시 환경변수를 넣고 빌드하면 **실패 0**이라 기존과 같은 사유임을 재확인) +
자리표시자 경고 2건 유지 + **Playwright 259개 항목 전체 통과**(공개 11개 화면 헤더 배경·
스티키·로고 색·**헤더 내 `--text-muted` 0건** / 공개 11개 화면 × 360·390·430px 가로스크롤 0 /
드롭다운 열기·닫기·`aria-expanded`·44px·바깥클릭·스크롤 닫힘 / 법적 문서 모달·페이지 직접
접근 200 / **관리자·운송관리 헤더 미변경** / 배경 교차 순서·번호 배지·카드 열 수·차량 그리드·
진행선 방향·캡처 페이드·CTA 순서·이미지 자리 수·목업 문구 0건 / 32·34차 **금지 표현 0건**·
신뢰 3줄 유지·대표번호 상수 참조) + 1200~1920 여섯 폭 × 공개 4개 화면 가로스크롤 0 +
랜딩 헤더·본문 좌측 정렬 일치. ⚠️ 자동검증에서 `/terms`의 "화주"는 **약관 제2조 병기 문장
한 곳만 예외**로 뒀음(30차가 "지우면 안 됨"으로 못박은 문장이며, 그 외에 늘어나면 걸림).
사용자가 Preview에서 실사용 확인 후 **PR #86 merge됨**(리뷰 라운드 1건 반영).
