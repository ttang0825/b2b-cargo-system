> 🔴 이 파일은 `docs/history/current.md` 에서 넘어온 세션 기록입니다. **요약하거나 줄이지 마십시오** —
> 되돌리면 안 되는 결정의 근거가 여기에만 있습니다.
> 담는 범위: 랜딩 히어로 계정 신청 #147 2026-09-14  →  36차 A·B장 #150 2026-09-15   (5건)
> 입구는 `HANDOFF.md`, 규칙과 로드맵은 `CLAUDE.md` 입니다.
---

**작성일: 2026-07-23** (최종 갱신: 2026-09-15, Claude Code 세션에서 직접 갱신 —
**36차: 화주 거래조건 + 발주요청 청구주기 「요청」 (A·B장)** — **PR #150 merge됨**(squash
`f338721`, 커밋 8개 · 파일 20개 · +1,211 / −65). 🔴 **DB 변경 있음** — 마이그레이션 **3건** ·
`_migrations` **29 → 32행**. 🟢 화주포털 표시 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `lib/legal/` 0줄 ·
실사용 리뷰 **3라운드**.
🔴 **36차는 다섯 장(A~E)이고 이 PR 은 A·B장뿐이다** — C·D·E장은 **PR 2**(DB 0)로 따로 간다.

**═══ ① 🔴 merge 직후에 발견한 누락 1건 — 세금계산서 발행 방식이 화면에 없다 ═══**

🔴 **이것부터 읽을 것.** 사용자가 36차 착수 전에 **⭕ 로 확정한 항목**인데
(*"세금계산서 발행 방식 (정발행 / 역발행 / 발행 안 함)"*) **화면에 입력칸이 없는 채로
merge 됐다.**

```
  companies.tax_invoice_method        컬럼      ✅ 있다 (CHECK 제약도 있다)
  TAX_INVOICE_METHOD_OPTIONS          상수      ✅ 있다 (lib/companyFields.ts)
  COMPANY_FIELDS 의 field 항목        입력칸    ❌ **없다**
```

🔴 **원인은 리뷰 2라운드 커밋(`53b205d`)이다.** 사용자가 *"여신한도는 지금 뺀다"* 라고 해서
`credit_limit` 을 지웠는데, **`tax_invoice_method` 가 바로 그 아래 붙어 있어서 같이 지워졌다.**
커밋별로 세어 보면 분명하다 — `98c4fe1` 1 · `e3d9c5e` 1 · **`53b205d` 0** · `8363979` 0 · `5b1c143` 0.

⚠️ **그리고 내가 PR 본문에 「칸이 넷(청구주기 · 결제일 · 세금계산서 · 특이사항)」이라고
적었다 — 그것이 틀렸다.** 실제로 merge 된 것은 **셋**이다(+ 읽기 전용 「정산 마감」 줄).
사용자 확인 목록 1번도 같은 문장이라 **「넷이 맞나」로 보면 안 맞는다.**

🟢 **복구는 한 항목 추가로 끝난다** — 컬럼·상수·CHECK 가 전부 남아 있어 **DB 변경이 없다.**
PR 2 의 첫 커밋으로 처리했다(아래 ⑨).

🔴 **교훈** — **인접한 항목을 함께 지우지 않았는지 지운 뒤에 세어 볼 것.** 「무엇을 지웠나」가
아니라 **「무엇이 남았나」**를 세야 잡힌다. 이번에는 `grep -c 'key: "..."'` 한 줄이면 됐다.
🔴 **그리고 PR 본문의 목록은 코드에서 뽑아 쓸 것** — 기억으로 적으면 이렇게 어긋난다.

**═══ ② A장 — 화주 상세의 「거래 조건」 ═══**

사용자 원문(2026-09-14):
> 화주정보 상세 수정에서 거래조건에서 건별인지 월별정산인지 구분하는 드롭다운 방식의
> 옵션이 있으면 좋겠고, 정산마감일과 결제일등 상세한 정보들도 기입이 되어야한다.

merge 된 모습:

```
  청구주기       건별 / 월정산
  정산 마감      🔴 **읽기 전용 안내** — 입력칸이 아니다
                   월정산 → 「월말 기준 (모든 화주 공통)」
                   건별   → 「해당 없음 — 운송 완료 시 즉시지급」
                   미정   → 안 그림
  결제일         익월 말일 · 익월 1일 … 익월 31일 · 협의   ← **월정산일 때만**
  (세금계산서)   ❌ 위 ① — 빠졌다
  특이사항       자유 서술 (구 `payment_terms` 를 이름만 바꿔 계속 쓴다)
```

🔴 **전부 내부 전용이다**(사용자 확정) — 화주포털·견적서·엑셀 **0줄**. 포털은 청구주기·결제일을
**기본값으로만** 읽고 화면에 글자로 그리지 않는다.

**═══ ③ 🔴 지시서 전제 넷이 틀렸다 ═══**

```
  1  「정산마감일을 신설한다」   →  `companies.billing_cutoff_day` 가 **이미 있었고 화면에도 있었다**
                                  (540행 중 1행 채워짐). 두 벌을 만들면 월정산 묶음이 갈린다
                                  — 원칙 46번이 기록한 바로 그 사고 자리다
  2  「결제조건 칸이 없다」       →  `payment_terms` 자유 텍스트가 **이미 있었다**(1행 채워짐)
  3  조정 계산식(E장)            →  `기본운임 + 가산 − 혼적할인` 이 아니다. 견적서가 그리는 것은
                                  `quote_items`(혼적할인이 이미 음수 행으로 들어 있다) + `discount_amount`
                                  → 실제 식은 아래 ⑨
  4  `CLAUDE.md` §7 의 죽은 컬럼  →  「`receivable_amount`/`payable_amount` 는 죽은 컬럼」이 **낡았다**
                                  (35차가 `invoices/[id]` 에서 읽게 만들었다)
```

**═══ ④ 🔴 정산 마감일 — 입력칸과 값을 둘 다 없앴다 ═══**

리뷰 2·3라운드에서 **월말 고정**으로 확정됐다.
> *"거래조건이 월정산인 경우 정산 마감일은 자동 월말기준으로 두자.
>   업체마다 정산마감일이 상이하면 복잡해질것 같다."* (2라운드)
> *"정산 마감일은 구설정은 필요없다. 월정산을 했을때 모두 월말기준으로 바꾸자.
>   정산마감은 월말기준이라는 표시는 두자."* (3라운드)

🟢 **화면 쪽은 DB 변경이 필요 없었다** — 월정산 묶음 계산이 원래 「마감일이 비어 있으면
달력월(1일~말일) 기준」이라 **`null` 이 곧 월말**이다.

🔴 **저장 경로도 막았다** — `buildCompanyPayload` 가 이 키를 payload 에서 **아예 지운다.**
화면만 숨기면 다음 세션이 칸을 되살려 다시 화주마다 달라진다.

🔴 **컬럼은 남겼다** — `null` 이 「월말」이라는 **뜻**이라 컬럼을 지우면 그 계산이 통째로 깨진다.

**🔴 이 저장소에서 드문 「값을 지우는 마이그레이션」이다** — `2026-09-14_billing_cutoff_month_end.sql`.
지우기 전에 **`_bak_billing_cutoff_day_20260914`** 로 백업했고, **백업이 비면 멈추도록 단언**을
걸었다(되돌릴 근거가 조용히 사라지는 것을 막는다). 🔴 **그 표를 먼저 지우지 말 것.**

🟢 **과거 월정산 묶음은 안전하다 — 돌리기 전에 코드로 확인했다.** `MonthlyBillingBatchPanel` 이
① 기존 묶음을 **`period_end` 가 그 달력월 안에 있는지**로 찾고(마감일로 역산하지 않는다)
② 찾은 묶음은 **저장된 `period_start`/`period_end` 를 그대로** 쓴다.
마감일은 **새 묶음을 만들 때의 기간 계산에만** 쓰인다.
🔴 **이 성질이 깨지면(= 묶음을 마감일로 역산해 찾도록 되돌리면) 이 마이그레이션이 소급 사고가
된다.** 원칙 46번이 그래서 있는 것이니 되돌리지 말 것.

**═══ ⑤ 결제일 — 「정산 익월 며칠」 한 칸 ═══**

처음에 「기준 + 값」 두 칸으로 만들었는데, 마감이 월말 고정이 되면서 기준이 하나뿐이 되어
**4지선다를 화면에서 뺐다**(*"결제일은 (정산 다음달 기준)"*).

🔴 **화면 값과 DB 값이 1:1 이 아니다** — 「협의」를 고르면 값 칸이 아니라 `payment_due_basis` 가
바뀐다. 변환은 **`paymentDueToForm`/`paymentDueFromForm` 두 함수만** 한다. 화면에서 직접
컬럼을 읽고 쓰지 말 것.
🔴 **`payment_due_basis` 컬럼과 `PAYMENT_DUE_BASIS_OPTIONS` 를 지우지 말 것** — 화면에서 안 고를
뿐 저장은 계속 하고, 나중에 기준이 여럿이 되면 **화면만** 되살리면 된다.
🔴 **건별로 바꾸면 결제일 값을 비운다**(`applyCompanyFieldChange`) — 숨기기만 하면 나중에
월정산으로 되돌렸을 때 **옛 날짜가 되살아난다.**

**═══ ⑥ 여신 한도 — 화면에서 뺐다 (컬럼은 남겼다) ═══**

1라운드에 *"여신 한도는 무엇인가? 이 옵션은 필요한가?"* 로 물어서, 지금 구조로는 **특이사항에
「여신 500만」이라 적는 것과 동작이 똑같다**고 보고했고(값만 받고 아무것도 막지 않는다 ·
비교 대상인 미수금이 C장 전까지 틀리다) 사용자가 *"여신한도는 지금 뺀다"* 로 확정했다.

🔴 **컬럼(`credit_limit`)은 남겼다** — 채워진 행이 0건이라 남겨 두는 비용이 없고, 되살릴 때
마이그레이션이 또 필요해진다. 두 번째 마이그레이션의 단언이 이 칸이 그대로 있는지 확인한다.
⚠️ **이 칸을 지우다가 바로 아래 세금계산서 칸까지 같이 지운 것이 위 ① 이다.**

**═══ ⑦ B장 — 발주요청의 청구주기·결제일 「요청」 ═══**

```
  청구   ○ 건별   ● 월정산
         정산 마감은 월말 기준입니다. 계약하신 조건이 기본으로 선택되어 있고,
         바꾸어 요청하시면 담당자가 확인 후 안내드립니다.
  결제일  [ 익월 25일 ▾ ]     ← 월정산일 때만 · 기본값은 화주 상세의 결제일
```

🔴 **칸 이름이 `requested_*` 인 것은 의도다** — 27차가 `portal_order_requests` 에 확정 정산값을
넣지 말라고 못박았고 사유(*「월정산은 화주별 계약이라 담당자가 정한다」*)가 그대로 살아 있다.
**요청값이고 확정은 담당자가 한다.** `requested_` 를 떼지 말 것.
🔴 **화면이 칸을 숨겨도 서버가 월정산 여부를 다시 본다**(원칙 25번) — 콘솔로 아무 값이나 보낼 수
있다. 서버는 잘못된 값을 400 으로 막지 않고 **`null` 로 떨어뜨린다**(발주 자체는 통과시킨다).
🔴 **계약이 「협의」인 화주에게는 결제일 칸을 안 그린다** — 숫자로 정해 둔 날이 없고 담당자와
이미 따로 정한 형태다.
⚠️ 항목이 32개라 라디오로 그리면 화면을 덮어서 **여기만** 포털 드롭다운(`Pv2Select`)을 썼다.

**「계약과 다름」 배지** — 계약과 다르게 요청하면 견적·오더의 정산방식 칸에 뜬다.
🔴 **알리기만 하고 막지 않는다** — 막으면 계약과 다르게 합의한 건을 아예 못 넣는다.
🔴 **`contractBillingCycle` prop 을 안 넘기는 화면(배차·정산·사유 모달)은 한 글자도 안 바뀐다** —
그것이 회귀 0 의 근거다.

**═══ ⑧ 곁들여 고친 것 (A·B장 밖 · 리뷰 2라운드) ═══**

```
  활성 화주 목록 「결제조건」 → 청구주기   🔴 라벨을 목록에 다시 적지 않고 정의처 함수를 쓴다.
                                        자유 텍스트를 되돌리지 말 것(길이가 제각각이라 옆 칸을 민다)
  상단 메뉴바 고정                        🔴 **`fixed` 가 아니라 `sticky`** — `fixed` 면 본문 맨 위가
                                        메뉴 뒤로 숨고, 메우려면 헤더 높이를 두 곳에 적게 된다
                                        (데스크탑 18px · 모바일 12px 로 이미 다르다).
                                        🔴 **`z-index: 30` 을 올리지 말 것** — 관리자 모달이 전부
                                        50 이상이라(실측 50·60·100·200·1000) 넘기면 모달이 뒤로 깔린다
  운임기준표를 「운송 운영」 맨 아래로      🔴 **원칙 14번과 `HANDOFF` §3 도 같은 커밋에서 고쳤다** —
                                        코드를 뒤집었으면 그것을 적어 둔 글도 같이 고친다(35차 교훈)
```

**═══ ⑨ 🔴 PR 2 (C·D·E장) — 아직 안 했다 ═══**

**C장 — 활성 화주 목록의 선착불 미수금.** 사용자 원문:
> *"활성화주 목록에서 선착불건의 금액은 미수금으로 표시되면 안된다."*

🟢 **원인을 먼저 갈랐고 「코드 결함」 하나였다**(데이터는 이미 맞다). `verify` 실측:

```
  선착불 정산 3건   화주청구액   받을돈(receivable_amount)   주선수수료   화주입금완료
                    50,000       5,000                       5,000        f
                    50,000       5,000                       5,000        f
                    80,000      15,000                      15,000        f

  화주별 미수금     저장값     식A(청구액)   식B(받을돈)   식C(선착불 제외)
                    180,000    180,000       25,000        0
```

🔴 **`receivable_amount` 는 3건 다 이미 맞다 — 옛 정산 건을 SQL 로 일괄 수정하지 말 것.**
틀린 것은 **읽는 쪽 두 곳**이다:
```
  식A  app/admin/invoices/page.tsx:500        `customer_charge_total` 을 직접 합산 → 180,000
  식B  app/admin/invoices/[id]/page.tsx:327   선착불 수수료를 화주 미수금에 넣고, 게다가
                                             `collection_method === "broker"` 게이트 때문에
                                             **선착불 건에서는 아예 안 돈다**(그래서 영영 안 고쳐진다)
```
→ **정의처 하나로 합칠 것**(35차 `lib/marginCalc.ts` 와 같은 결).

**D장 — 견적관리 하차 일시 = 상차 +30분부터.**
🔴 **상차 하한·기본값은 건드리지 않는다**(35차 확정: 지나간 날도 고를 수 있어야 한다).
막는 곳 2군데. 🔴 **`quotes` 에 `dropoff_arrival_type` 컬럼을 만들지 말 것.**
🔴 **같은 커밋에서 원칙 6번 본문도 고칠 것** — 견적관리가 「거리기반 2~5h」가 아니라 **고정 30분**이
되고, 그 원칙의 「발주요청 2시간」도 **이미 낡았다**(실제 30분이다).

**E장 — 「최종금액 직접 입력」을 견적서에 「조정」 줄로.** 사용자 원문:
> *"견적관리에서 「최종금액 직접 입력」을 수정했을 때 기본운임에서 + - 되어서 해당 최종금액이
>   맞춰 줘야 한다. 그래서 화주가 보는 견적서상에 오해가 없어야 한다."*

🔴 **식은 지시서가 적은 것과 다르다**(위 ③-3):
```
  조정 = final_amount − (base_fare + Σ quote_items.amount − discount_amount)
```
🔴 **기본운임을 덮어쓰지 않고 줄을 하나 더한다**(사용자 확정) · 0이면 안 그림 ·
증액 `+` / 감액 `−` · **혼적할인과 별개 줄**(혼적할인은 이미 `quote_items` 안에 음수 행이다).
실측: `quote_items` **0행** · 조정 **−2,000** 한 건 · `discount_amount` 는 전부 0이라
**할인 줄 불일치는 지금은 잠복 상태**다.

다섯 산출물이 그리는 줄(조사 완료):
```
                      관리자 상세  화주 상세  admin PDF  화주 PDF  엑셀
  기본운임 base_fare       ✓          ✓         ✓         ✓       ✓
  가산 quote_items         ✓          ✓         ✓         ✓       ✓
  할인 −discount_amount    ✗          ✗         ✓         ✓       ✓  ⚠️
  공급가액 final_amount    ✗          ✓         ✓         ✓       ✓  ⚠️
```
🔴 **커밋 7(정의처 신설)과 커밋 8(다섯 산출물이 그것을 쓰게)을 합치지 말 것** — 커밋 7 직후
**조정 0 견적의 출력이 한 글자도 안 달라져야** 회귀 0 의 근거가 된다.

**═══ ⑩ 검증·기준선 ═══**

```
  npx tsc --noEmit         통과 (출력 0줄)
  npm run build            프리렌더 실패 **46건** — 기준선과 **항목 단위로 동일**(diff 0)
  마이그레이션 dry-run      셋 다 「실행 대기 1건」 · 「적용된 파일 내용 변경 없음」
  마이그레이션 apply        단언 전부 통과 · `_migrations` **32행**
```
🔴 프리렌더는 **개수가 아니라 항목을 `diff`** 로 가른다(35차 교훈).
⚠️ **`wc -m` 로 자수를 세지 말 것** — 이 환경은 locale 이 UTF-8 이 아니라 **바이트가 나온다**
(한글 섞인 글은 실제 자수의 약 1.9배). `python3` 의 `len()` 으로 셀 것. §0 표의 자수가
「틀려 보이는」 것은 대부분 이 때문이다.

**═══ ⑪ ⚠️ 사용자 확인이 안 된 채로 merge 됐다 ═══**

3라운드 반영분(`5b1c143`) Preview 를 올리고 **15시간 동안 열다섯 번 점검했으나 응답이 없었고**,
사용자가 확인 없이 *"merge하고 다음으로 진행하자"* 로 지시했다. 그래서 **아래는 아직 눈으로
확인되지 않았다**:
```
  ① 거래 조건 칸 구성과 「정산 마감」 안내 줄        ← 🔴 위 ① 때문에 실제로 셋이다
  ② 「수정」을 안 눌러도 그 줄이 보이는지(`5b1c143` 이 고친 것)
  ③ 건별 ↔ 월정산 왕복 시 결제일이 비는지
  ④ 화주포털 발주요청의 안내·드롭다운·기본값
  ⑤ 기존 확정 월정산 묶음의 기간이 그대로인지     ← 값을 지운 마이그레이션의 영향
  ⑥ 상단바 고정 + 모달이 헤더 위로 뜨는지
```
🔴 **⑤ 는 값을 지우는 마이그레이션의 영향이라 가장 먼저 볼 것.** 코드로는 안전을 확인했지만
(위 ④) **실물 확인은 아직이다.**

**작성일: 2026-07-23** (최종 갱신: 2026-09-14, Claude Code 세션에서 직접 갱신 —
**차수 없음: 화주포털 이용가이드 `/customer/guide` 신설** — **PR #149 merge됨**(squash `45e3179`,
커밋 4개 · 파일 7개 · +1,270 / −312). 🟢 **DB 0** · `_migrations` **29행 그대로** ·
마이그레이션 0 · 신규 의존성 0 · **신규 이미지 0** · `lib/legal/` 0줄 · 관리자 0줄 ·
랜딩 0줄 · **포털 로그인 화면 0줄** · 실사용 리뷰 라운드 **0**.
🔴 **차수를 소비하지 않았다** — 다음 지시서는 그대로 **36차**다(PR #148·PWA PR #126 과 같은 취급).

**═══ ① 🔴 하루 만에 뒤집힌 확정 셋 — 사유는 「로고」다 ═══**

바로 앞 차수(PR #148, 같은 날 오전)가 **명시적으로 금지한 것 셋**을 사용자가 뒤집었다.

```
  PR #148 이 적은 것                          이번
  ─────────────────────────────────────────────────────────
  🔴 /customer/guide 를 따로 만들지 말 것   →  만들었다
  🔴 포털 메뉴 8종에 안내 항목 추가 금지     →  9종이 됐다
  🔴 포털에 별도 안내 화면이 없는 것이 의도  →  의도가 바뀌었다
```

🔴 **사용자 원문(2026-09-14) — 요약하지 말 것**

```
"운송관리 이용안내 보기"는 화주포털에서는 빼자. 여기서 들어가게 되면 로고 클릭시
다시 랜딩페이지로 가는 문제점이 있다. 대신에 화주포털 이용가이드를 따로 메뉴를
만들어서 화주포털을 처음 사용하는 고객이 쉽게 이용할수 있는 메뉴 페이지가 있으면
좋겠다. 이 이용가이드는 보기 편하게 구성되면 좋겠다. 한장에 쭉 나열해서 길게
스크롤 하면서 메뉴얼을 보는 방식은 불편하다. 이용가이드내에서 항목선택하면서
보고 싶은 메뉴가이드를 선택해서 볼수 있으면 좋겠다.
```

🔴 **진단이 정확했다.** `/guide` 는 `PublicPageHeader`(로고만)를 쓰는 **공개 화면**이라,
로그인한 화주가 포털 홈에서 들어가면 **로고를 눌렀을 때 포털이 아니라 랜딩으로 나간다.**
포털 셸 밖으로 나간 것이므로 돌아올 길이 없다. ⚠️ `BackToHomeLink` 의 「왔던 자리로」는
30차 이후 **한 번도 동작한 적이 없어서**(`?from=landing` 을 붙이는 곳이 0곳) 그것으로는 못 고친다.

🔴 **「페이지는 하나」를 근거로 되돌리지 말 것.** PR #148 의 그 문장은 **「두 벌이면 한쪽만
낡는다」**는 걱정이었고, 그 걱정은 **없앤 것이 아니라 다른 방법으로 푼 것**이다 —
`lib/guideContent.ts` 한 정의처 + 노출범위(`scope`) 표시.

**═══ ② 🔴 본문 정의처 하나, 화면 둘 — `lib/guideContent.ts` ═══**

```
  항목 하나 = { id, title, titleSub?, scope, group?, step?, blocks }
  scope     "public"(공개 전용) · "portal"(포털 전용) · "both"(양쪽)

  /guide          public + both  →  10항목 · 한 장 스크롤(그대로)
  /customer/guide portal + both  →  11항목 · 항목 선택
```

🔴 **`lib/legal/*.ts` 모양을 따랐다** — 순수 데이터 + 블록 유니온 + `**...**` 강조 +
`dangerouslySetInnerHTML` 미사용. **한 가지만 다르다:**

🔴 **블록 타입이 렌더러가 아니라 정의처에 있다.** 법적 문서는 **한 정의처 → 렌더러 하나**
(`components/LegalDoc.tsx` 를 페이지와 모달이 공유)라 타입을 그 컴포넌트에 둘 수 있었다.
이용가이드는 **렌더러가 둘이고 CSS 스코프가 서로 다르다**(`.guide-*` / `.pv2-*`).
타입을 한쪽 렌더러에 두면 **반대쪽 화면이 그 컴포넌트를 import 하게 되어 두 화면이 한 파일에
묶인다.** 🔴 **「법적 문서처럼 렌더러로 옮기자」로 되돌리지 말 것.**

🔴 **`pills` 블록은 글자를 담지 않는다** — `{ type:"pills", set:"quote"|"dispatch" }` 뿐이고
라벨·색은 **화면이** `lib/quoteStatusLabels.ts`·`lib/dispatchStage.ts` 에서 가져온다.
정의처에 적으면 **DB 값(`보류`·`실패`)이 그 파일에 박히고** 라벨이 바뀔 때 안내만 조용히 낡는다.

🔴 **강조를 `<b>` 로 그린다**(법적 문서는 `<strong>`) — 옮기기 전 화면의 태그 그대로다.
빈 조각을 버리는 것도 같은 이유다(문장이 `**` 로 시작하면 빈 조각이 하나 생긴다).

🔴 **항목 `id` 는 URL 이다**(`?topic=<id>`) — 바꾸면 밖에서 보낸 링크가 깨진다. 영문 소문자·하이픈.

🔴 **정의처에 JSX·className 을 넣지 말 것** — 넣는 순간 공개 화면 CSS 와 `.pv2-*` 가 한
파일에서 섞이고 두 화면이 같은 마크업을 강제로 쓰게 된다. **그리는 일은 각 화면이 한다.**

**═══ ③ 🔴 항목 구성표 — 지시서 제안 13개 → 실제 14개 ═══**

**조정 사유는 하나다 — 제안대로 쪼개면 `/guide` 의 섹션·제목 구조가 바뀌어 완료조건 3
(렌더링 동일)이 깨진다.** 그래서 **항목 제목을 지금 `/guide` 의 h2·h3 문자열과 한 글자도
다르지 않게** 잡았다.

```
  id              제목 (= 화면 문자열)        범위      /guide 의 자리
  ────────────────────────────────────────────────────────────────────
  apply           계정 신청                  public    시작하기·네 단계 01
  first-login     첫 로그인                  public    〃 02
  install         앱으로 설치하기             both      〃 03
  usage           발주부터 정산까지           public    〃 04
  menus           메뉴 한눈에 보기            both      자체 섹션
  order           발주 요청하기               portal    — (신규)
  address         배송지·화물 저장해 쓰기      portal    — (신규)
  quote-status    견적 상태                  both      진행 상태 보는 법
  dispatch-status 배차·운송 상태              both      〃
  amount          금액 표시                  both      〃
  stats           월별 통계와 엑셀            portal    — (신규)
  profile         담당자 정보                portal    — (신규)
  faq             자주 묻는 질문              both      자체 섹션
  contact         문의                       both      자체 섹션
```

🔴 **제안과 달라진 것 넷**

```
  제안 quote/dispatch/invoice  →  quote-status/dispatch-status/amount
     제안 제목(「견적 확인과 승인」 등)을 쓰면 /guide ④ 섹션의 <h3> 셋이 바뀐다
  제안이 쪼갠 04 카드          →  usage 로 살렸다(public)
     쪼개면 /guide 의 04 카드가 사라진다. 🟢 포털은 쪼갠 쪽(order·address·stats·profile)을 본다
     — 「범위가 갈리는 것이 요점」이 여기서 실현된다
  invoice 항목                →  안 만들었다
     amount(금액 표시) + menus 표의 「정산·결제내역」 행이 이미 답한다.
     ⚠️ 사용자가 「정산을 찾는 사람에게 금액 표시가 읽히는가」를 보고 원하면 넣기로 남겨뒀다
  배열 순서 하나로 두 화면     🟢 public 필터도 portal 필터도 각자 맞는 순서가 나오게 짰다
```

🔴 **`public` 항목(apply · first-login · usage)의 문장은 PR #148 원고 그대로다 — 다듬지 말 것.**
각 문장이 PR #148 이 막으려던 문의 다섯 개에 대응한다.

**═══ ④ 🔴 `/customer/guide` — 라우트 하나 · 쿼리스트링 ═══**

```
  라우트     /customer/guide       🔴 한 개다. 항목마다 만들지 말 것
  항목 선택  ?topic=<id>           새로고침·링크 공유·뒤로가기가 전부 그대로 동작한다
  셸        포털 셸 안            → 로고가 포털 홈으로 간다 = 이번 작업의 목적
  스코프     .pv2-*                공개 화면 CSS 를 끌어오지 않았다
  데스크탑   왼쪽 목차(sticky) + 오른쪽 본문 · topic 없으면 첫 항목
  ≤700px    목록 → 상세 두 화면
```

🔴 **항목 이동은 `<Link>`(history push)다** — `replace` 로 바꾸면 뒤로가기가 목록이 아니라
**포털 홈으로** 튄다.

🔴 **모바일에서 목록과 본문을 같이 그리지 않는다** — 합치면 **이번에 없애려던 긴 스크롤로
돌아간다.** 🔴 **가르는 일은 CSS 가 한다**(`.pv2-guide-open`, ≤700px). 화면 폭을 JS 로 재면
첫 그림에서 잘못된 쪽이 한 번 번쩍인다. 본문은 항상 DOM 에 있고 CSS 가 한쪽만 보여준다.

🔴 **알 수 없는 `topic` 에 빈 화면을 내지 않는다** — 첫 항목(데스크탑)·목록(모바일)으로 떨어뜨린다.

🔴 **분기점은 700px 이고 새 값을 만들지 않았다** — 포털이 이미 쓰는 값이다
(`.pv2-page-head`·`.pv2-sonly-*`). 1023px(사이드바 접힘)·430px 과 헷갈리지 말 것.

🔴 **항목 고르기를 네이티브 `<select>` 로 만들지 말 것**(원칙 57번).

**═══ ⑤ 🟢 상단 「←」가 이미 있었다 — 버튼을 새로 만들지 않았다 ═══**

착수 전 확인 4번(보고 후 대기 항목)의 답:

```
  그리는 조건   pathname !== "/customer"       → /customer/guide 에 그려진다
  보이는 폭     .pv2-mobile-header (≤1023px)   → 360px 에서 나온다 · 데스크탑은 CSS 로 숨김
  누르면        history.length > 1 ? router.back() : router.push("/customer")
```

🔴 **그래서 「목록으로」 버튼을 따로 만들지 않았다** — 만들면 같은 줄에 뒤로가기가 둘이 된다.
항목 이동이 `push` 라 「←」가 그대로 목록으로 돌아간다(실측 확인).
⚠️ 밖에서 받은 `?topic=` 링크로 **바로 들어온 경우**에는 「←」가 목록이 아니라 그 이전
페이지로 간다 — 브라우저 뒤로가기와 같은 동작이라 정상으로 본다.

**═══ ⑥ 🔴 포털 메뉴 배열은 둘인데, 고칠 곳은 하나였다 ═══**

착수 전 확인 3번:

```
  NAV_GROUPS   app/customer/CustomerPortalShell.tsx:42
               데스크탑 사이드바 **+ 모바일 「전체」 바텀시트**가 같은 NavList 를 쓴다
  MOBILE_TABS  같은 파일:72 — 하단 **고정 4탭**(홈/발주/견적/운송) + 「전체」 토글.
               🔴 전체 메뉴가 아니다. 여기에 항목을 더하지 말 것
```

🟢 **그래서 `NAV_GROUPS` 한 곳만 고쳐 데스크탑·모바일이 함께 9종이 됐다.**

🔴 **자리는 맨 아래 혼자 한 그룹**(5그룹 9항목) — 매일 쓰는 메뉴가 아니다.
🔴 **라벨은 「이용가이드」**이고 공개 화면의 「운송관리 이용안내」와 **일부러 다르다** —
같은 말이면 같은 화면으로 오해한다.
🔴 **캘린더·공지사항·비밀번호 변경을 이 김에 넣지 않았다** — 메뉴 밖인 것이 확정이다.
⚠️ 배지(`NavCountBadge`)를 붙이지 않았다 — 셀 것이 없다.

**🔴 아이콘을 10번째로 새로 그렸다**(지시서에 없던 것). `PortalIconName` 이 **9종뿐**이고
`NavItem.icon` 이 **필수**인데 가이드에 맞는 것이 없었다(`menu` 는 모바일 「전체」 탭의
햄버거다). 펼친 책이고 `invoices`·`stats`·`locations`·`profile` 과 같이 **line/fill 사양이
동일한 선 아이콘**이다(채움 변형이 없다). **이미지 파일 0건 — 인라인 path 다.**

**═══ ⑦ 🔴 포털 홈 링크 제거 — `margin-left:auto` 는 안 건드렸다 ═══**

착수 전 확인 5번에서 **접점이 0** 임을 먼저 확인했다 — `/guide` 링크는 `.pv2-page-head`
**밖의 별도 `<div className="pv2-guide-link">`** 였고, `margin-left: auto` 는
`.pv2-page-head > .pv2-install-btn` 에 걸려 있다.

```
  전후 실측   설치 버튼 오른쪽 끝 = 제목 줄 오른쪽 끝 (1280px 1244=1244 · 390px 374=374)
             버튼 top 도 동일(80 · 89) · 아래 블록만 그 줄 높이만큼 올라옴(173→141 · 178→143)
```

🔴 **링크를 다른 자리로 옮기지 않았다** — 메뉴가 그 일을 한다. 두 자리가 되면 다시 정리 대상이 된다.
🟢 **포털 로그인 화면의 링크는 일부러 남겼다**(사용자 확정) — 로그인 전이라 로고가 랜딩으로
가는 것이 자연스럽고, **계정을 막 받은 사람이 가장 안내가 필요하다.**

**═══ ⑧ ⚠️ 완료조건 셋이 현실과 부딪혔다 — 전부 보고 후 확정받고 진행 ═══**

```
  조건 3   「/guide 렌더링 HTML 이 이전과 같다」
           🔴 **바이트 동일은 불가능하다** — 지금 화면에 `고객센터{" "}` 처럼 `{" "}` 를 쓴
           자리가 셋 있어 React 가 텍스트 노드를 쪼개고 사이에 `<!-- -->` 를 넣는다.
           데이터에서 한 문자열로 오면 그 구분자가 사라진다.
           🟢 **`<!-- -->` 만 지운 정규화본이 md5 완전 일치**(0ed3370f67439f4dba904213ce3c8535).
           구분자 9개 → 6개, 그 외 한 글자도 다르지 않다
  조건 23  「globals.css 삭제된 줄 0」
           🔴 커밋 4 에서 죽은 `.pv2-guide-link` 규칙 3개(**12줄**)가 삭제된다.
           사용자 확정으로 **지웠다** — 지워진 줄에 다른 선택자 **0건**, 그 클래스 참조도
           저장소 전체에 **0건**. 나머지 342줄은 순수 추가다
  조건 2   항목 구성표 → ③ 참고(13개 → 14개)
```

**═══ ⑨ 검증 — 목 Supabase + 화주 세션으로 실제 화면을 그려서 쟀다 ═══**

```
  tsc                통과(파일로 받음, 0줄)
  프리렌더           45 → 46 · 🔴 개수가 아니라 **항목을 diff** 했고 늘어난 것은
                     /customer/guide 하나다(32a33)
  /guide 동일         정규화본 md5 일치
  /customer/guide    55항목 전부 통과(실패 0)
                     포털 셸 로고 → /customer · 목차 11개 · 비로그인 → /customer/login
                     360px 목록/상세 갈림 · 클릭 후 뒤로가기 → 목록 · 1280px 2열 236+706 sticky
                     알 수 없는 topic → 첫 항목/목록 · 메뉴 표가 1280 표 / 360 카드
                     네이티브 select 0건 · 금지어 0건(보류·실패·차량번호·주선수수료·마진·
                     지급운임·차주 성명·연락처) · 360/768/1280/1920px 가로 넘침 0px
                     본문 영역 클래스 전수가 전부 pv2- · 공개 /guide 에 .pv2- 0건
  포털 메뉴           9항목 통과(사이드바 9 · 바텀시트 9 · 하단 탭 5 그대로)
  포털 홈             전후 실측(⑦)
```

🔴 **목은 실제 DB 보다 헐거우면 안 된다**(원칙 56번) — 이 화면이 실제로 부르는 것만 답하고
**그 밖의 경로는 명시적으로 404** 로 떨어뜨려 조용히 통과하지 않게 했다.
⚠️ **`waitUntil: "networkidle"` 을 쓰면 안 된다** — 루트 레이아웃의 `TopNav` 가 목 Supabase 를
계속 부르느라 idle 이 오지 않아 30초 타임아웃이 났다. `domcontentloaded` + `waitForSelector` 로 쟀다.
⚠️ **`waitForSelector` 의 기본값은 `visible` 이다** — 360px 에서 `.pv2-nav-item` 은 DOM 에는
있지만 사이드바가 숨겨져 있어 **거짓 타임아웃**이 났다. `state: "attached"` 로 고쳤다.

**═══ ⑩ ⚠️ 남은 것 · 일부러 안 한 것 ═══**

```
  🔴 사용자 확인 대기  ① 「금액 표시」가 정산을 찾는 사람에게 읽히는지 —
                        원하면 포털 전용 `invoice` 항목을 하나 더 넣는다(지금은 없다)
                      ② 새 portal 항목 넷의 설명이 실제 화면과 맞는지
                      ③ 책 아이콘이 나머지 여덟과 어울리는지
  🔴 안 한 것          공개 /guide 의 형태(한 장 스크롤)는 **그대로다** — 바꾸지 말 것.
                      그 화면은 처음 오는 사람이 네 단계를 순서대로 읽는 자리다
  🔴 안 한 것          포털 로그인 화면 **0줄**(693px 기준선 그대로) · 관리자 0줄 · 랜딩 0줄
  ⚠️ 코드 밖 작업       **없다**
```

---

**작성일: 2026-07-23** (최종 갱신: 2026-09-14, Claude Code 세션에서 직접 갱신 —
**차수 없음: 거래처 온보딩 안내 페이지 `/guide` 신설** — **PR #148 merge됨**(squash `931ec79`,
커밋 5개 · 파일 9개 · +804 / −13). 🟢 **DB 0** · `_migrations` **29행 그대로** ·
마이그레이션 0 · 신규 의존성 0 · **신규 이미지 0** · `lib/legal/` 0줄 · `middleware.ts` 0줄 ·
실사용 리뷰 **1라운드(지적 2건)**.
🔴 **차수를 소비하지 않았다** — 다음 지시서는 그대로 **36차**다(PWA 전환 PR #126 과 같은 취급).

**═══ ① 왜 만들었나 — 문의 다섯 개가 이미 정해져 있었다 ═══**

계정을 발급받은 거래처가 **첫 로그인에서 멈춘다.** 지금 안내할 수 있는 것이 담당자
전화뿐이라 담당자가 없으면 그대로 방치된다. 지시서가 짚은 다섯 개는 전부 **「고객이
잘못한 것」이 아니라 「안내가 없는 것」**이다.

```
  1  아이디를 이메일로 착각한다        포털 아이디는 이메일이 아니다
  2  아이폰에서 설치 버튼을 못 찾는다   beforeinstallprompt 가 없다 — 버그가 아니다
  3  카톡 링크로 열어서 설치가 안 된다   카톡 자체 브라우저에 설치 메뉴가 없다
  4  비밀번호를 잊고 화면에서 찾는다     화면에 재설정 경로가 없다(위캐리 재발급)
  5  「견적 도착」이 무슨 뜻인지 모른다  상태 라벨이 화주 전용 표기다
```

🔴 **본문 문장을 줄이면 그 문의가 그대로 돌아온다** — 각 문장이 이 다섯 개에 하나씩
대응한다. 「간결하게 다듬자」로 지우지 말 것.

**═══ ② 🔴 착수 전 확인 13건 — 지시서 전제 셋이 틀렸다 ═══**

```
  1  로그인 실패 문구    🟢 갈리지 않는다 — 화면이 Supabase 응답을 보지 않고 모든 실패를
                        한 문구(「아이디 또는 비밀번호가 올바르지 않습니다.」)로 뭉갠다.
                        분기 자체가 없다(app/customer/login/page.tsx `if (error)` 하나)
  5  프리렌더 기준선     🔴 **44** (직접 셌다. 문서 숫자를 베끼지 않았다) → 이 차수로 **45**
  6  로그인 모바일 높이  🔴 **693px** — ⚠️ **지시서의 726px 과 다르다.** PR #129 가
                        「홈으로」를 헤더로 올린 뒤의 값으로 보인다. 실측을 기준으로 썼다
  4  PATHS_WITH_OWN_HEADER  `app/customer/CustomerPortalShell.tsx:27` = ["/customer/login"].
                        🔴 **포털 셸 안에서만 쓰여서 `/guide` 는 해당 없다**(셸을 안 거친다)
  8  랜딩 <a> 진입 링크  ⚠️ **랜딩 파일에는 한 곳이다** — `components/landing/LandingHeader.tsx`
                        의 `<a href="/customer/login">` 하나이고, 그 헤더를 **세 화면**
                        (`/`·`/quote`·`/apply`)이 공유하는 것이 「3곳」의 정체다
 11  메뉴 8종 실제 문자열  홈 / 발주 요청 / 견적 확인 / **배차·운송 조회** / **정산·결제내역** /
                        월별 통계 / **배송지·화물 관리** / 담당자 정보
                        ⚠️ 원고는 「배차 · 운송 조회」처럼 가운뎃점 양옆에 공백이 있었다
 12  상태 라벨 실제값     견적 — 상담 중 · 협의 중 · **견적 도착** · **운송 확정** · 취소
                        (⚠️ 원고의 「확정」이 아니라 **「운송 확정」**)
                        배차 — 접수 · 배차완료 · 운송완료 + **「문제 발생」**
                        (⚠️ 원고의 「문제발생」과 달리 **띄어쓰기가 있다**)
```

🔴 **원고(부록 A)를 그대로 옮기면 화면과 갈리는 자리가 셋이었다** — 메뉴 라벨 공백,
「확정」, 「문제발생」. **원고가 확정본이어도 화면 문자열은 실측해서 맞출 것.**

**═══ ③ 🔴 되돌리면 안 되는 것 ═══**

🔴 **`/guide` 는 하나다. `/customer/guide` 를 따로 만들지 말 것**(사용자 확정) —
두 벌이면 한쪽만 낡는다. **포털에 별도 안내 화면이 없는 것이 의도다.**
「포털 안에도 만들자」가 다시 나오면 이 줄을 먼저 볼 것.

🔴 **`components/TopNav.tsx` 숨김 조건에 `/guide` 가 등록돼 있다**(원칙 11번) — 빼면
관리자 메뉴가 이 공개 화면 위에 얹힌다. 28차(회사소개·차량안내)·30차(법적 문서 3종)에
**두 번 겪은 버그**다. 🟢 `TopNav` 가 `isPublicPath` 에서 **가장 먼저 `null` 을 돌려주므로**
로그인 상태와 무관하다(완료조건 2를 이 구조로 증명했다).

🔴 **`/guide` 에서 `/customer/login` 으로 가는 링크만 `<a href>` 다 — 의도다**(PR #130).
서비스워커 구역이 `/customer` 인데 `/guide` 는 그 **밖**이라, 클라이언트 전환으로
넘어가면 그 문서가 영영 워커의 제어를 못 받고 「홈 화면에 추가」가 앱이 아니라
**바로가기**를 만든다. **화면은 멀쩡해 보이고 설치만 조용히 망가진다.**
🟢 「계정 신청하기」(`/apply`)는 공개 화면끼리라 `next/link` 다 — **왜 한쪽만 다른지
코드 주석에 적어 뒀다.** 안 적으면 다음 세션이 「통일」한다.

🔴 **포털 메뉴는 여전히 8종이다** — 「이용안내」를 메뉴 배열에 넣지 않은 것이 확정이다
(확정 13번 — 캘린더·공지를 뺀 판단과 부딪힌다). **홈의 링크 하나로 끝낸다.**

🔴 **캡처 이미지를 일부러 넣지 않았다**(사용자 확정) — ① 화면을 고칠 때마다 낡고
② 무겁고(랜딩이 10.9MB → 5.5MB 였던 전례, PR #119) ③ **실계정으로 찍으면 화주 상호·
담당자 연락처가 public 저장소에 박힌다.** 빈 자리표시자도 두지 않았다(미완성으로 보인다).
🟢 필요한 곳만 **CSS 도식**으로 대신했다 — 03 앱 설치의 창 틀(`.guide-mock`)이 그것이고,
`margin-left: auto` **한 줄이 그 도식의 전부**다(「버튼이 오른쪽 위에 있다」만 말한다).

🔴 **아이디 형식(접두어·자릿수)을 공개 화면에 적지 말 것** — 리뷰 확정(아래 ⑤).
기술적으로 안전한 값이라는 판단과 **적지 않기로 한 결정은 별개**라는 것을 주석에 남겼다.

🔴 **담당 영업자 실명·휴대폰·이메일을 넣지 말 것** — 이 저장소는 public 이다.
화면 본문에는 **고객센터 번호만** 있다. ⚠️ 공용 푸터의 법정 표시사항(대표자·사업자
이메일)은 **전자상거래법 제10조** 항목이라 다른 공개 화면과 똑같이 나온다 — 그것은
이 화면이 더한 것이 아니다.

🔴 **상태 알약의 글자·색은 `lib/quoteStatusLabels.ts`·`lib/dispatchStage.ts` 에서 가져온다** —
문자열을 안내에 다시 적으면 라벨이 바뀔 때 **안내만 조용히 낡는다.** DB 값(`보류`·`실패`)은
노출되지 않는다(`quoteStatusStyle` 이 화주용 글자로 바꿔 준다).

**═══ ④ 로그인 화면 — 링크 한 줄과 여백 조정은 한 벌이다 ═══**

링크 한 줄이 **+34px** 이라 그대로 두면 모바일 한 화면을 넘는다. 지시서대로 **문구는
하나도 지우지 않고 여백만** 되찾았다.

```
  본문 아래 14 → 8   ·  카드 위 22 → 18  ·  카드 아래 20 → 16
  「아이디 저장」 줄 위 14 → 12  ·  로그인 단추 위 18 → 16
  구분선 위 20 → 16 / 아래 18 → 14   ·   설치 버튼 위 14 → 10
```

🔴 결과 — **360px 문서 높이 693px 로, 그 줄을 넣기 전과 같다**(390px 도 같은 값).
🔴 **링크만 빼면 화면이 성기게 남고, 여백만 되돌리면 한 화면에서 넘친다 — 한 벌이다.**
⚠️ `app/landing.css` 의 그 블록에 *「여백은 더 뺄 곳이 없다」*(PR #128)고 적혀 있던 것을
이번에 **다시 줄인 것**이다. **이제는 정말 없다** — 다음에 무언가를 더하려면 문구를
지워야 한다. 그 사정을 같은 주석에 남겼다.

⚠️ **지시서 3-3 의 전제가 틀렸다** — 이 화면에 *「아이디·비밀번호는 위캐리에서
발급합니다」* 라는 문구는 **없다.** 가장 가까운 자리인 「계정이 없으신가요?」 블록
바로 아래에 붙였다.

**═══ ⑤ 실사용 리뷰 1라운드 — 지적 2건 ═══**

```
  1  "「화주 등록신청」 대신 「운송관리 계정신청」으로 얘기하자"
     → 🔴 **지적은 「계정신청」(붙여쓰기)이었지만 「운송관리 계정 신청」(띄어쓰기)으로 넣었다.**
       12차에 화면 6곳을 통일한 라벨이 그쪽이다(`/apply` 제목 · 랜딩 3곳 · 로그인 · 견적).
       안내에만 다른 표기가 있으면 **화주가 찾는 화면과 이름이 갈린다.** 보고했다
  2  "「we + 발급일 6자리 + 순번 2자리」 형태를 공개 화면에 적지 말자"
     → 문장과 `<code>` 표시를 통째로 뺐고 **쓰이지 않게 된 `.guide-code` CSS 도 같이 지웠다**
       (죽은 규칙을 남기지 않는다). 지금은 「아이디는 이메일이 아닙니다 … 발급받으신
       아이디를 그대로 입력해 주세요」까지만 — **원래 막으려던 문의 1번은 그대로 막는다**
```

🔴 **2번은 「기술적으로 안전한가」와 다른 축의 결정이다** — 착수 전 확인 1번이
「갈리지 않는다」로 나와 기술적으로는 넣어도 됐고(형식을 알아도 계정 유무를 가릴 수
없다) 형식 자체가 이미 저장소 소스 주석에 있다. **그래도 적지 않기로 한 것**이니
「안전하다」를 근거로 되살리지 말 것.

**═══ ⑥ 판단 — 포털 홈은 「제목 줄 아래」다 ═══**

지시서 3-4 는 「제목 줄에 여유가 있으면 설치 버튼 왼쪽, 없으면 아래 한 줄」이었다.
🔴 **아래를 골랐다** — `.pv2-page-head` 가 `align-items: flex-end` 라 **왼쪽 덩이가 한 줄
길어지면 설치 버튼이 그만큼 아래로 내려앉는다.** 별도 줄로 두면 그 줄의 두 자식이
그대로라 버튼 위치가 변하지 않는다(PR #127 이 맞춰 둔 자리다).
🟢 실측 — 설치 버튼 오른쪽 끝이 제목 줄 오른쪽 끝과 **같다**(390px·1280px 둘 다).

랜딩은 **운송관리 소개 마무리 카드**(「운송 관리 시스템, 어떻게 시작하나요?」)의 계정
신청 단추 아래다. 🔴 **두 링크를 한 덩이로 감쌌다** — 바깥 컨테이너가 `gap: 36` 이라
그냥 형제로 두면 단추와 이 줄 사이가 36px 이 되어 서로 딴 블록으로 읽힌다.
🟢 **리빌 클래스를 따로 붙이지 않았다** — 그 카드가 통째로 하나의 리빌 대상이라
형제 단추와 **같은 타이밍**에 나타난다(실측: 카드에 `landing-reveal` 이 있고 자손에는 0개).
여기에 또 붙이면 **이 줄만 늦게 떠서 눈에 띈다.**

**═══ ⑦ 검증 — 완료조건 15개 전부 통과 ═══**

```
  프리렌더   44 → 45 · `diff` 로 **늘어난 항목이 `/guide` 하나**임을 확인
             🔴 개수만 세지 말 것(35차 교훈) — 항목 단위로 비교했다
  렌더링     `/guide` 200 · pv2- 클래스 0건 · 관리자 메뉴 문자열 9종 전수 0건
  링크       `/guide` 로 가는 링크 정확히 3곳 · `/customer/login` 링크는 `<a href>`
  치수       360·768·1280·1920px 가로 오버플로우 0 · 표 8행 = 모바일 카드 8장
  로그인     360px 693px(변경 전과 동일) · 설치 버튼 오른쪽 끝 유지
  메타       description **62자**(상한 80) · 빌드 로그 길이 경고 0
  회귀       `globals.css` **삭제 줄 0**(순수 추가) · 기능 파일은 링크 한 줄씩만
```

⚠️ **검증 방법** — 포털 홈은 로그인이 필요해 화면에서 직접 못 잰다. `app/pgcheck/` 에
제목 줄 마크업을 **그대로 옮긴 임시 라우트**를 만들어 재고 **커밋 전에 지웠다**
(`.next/types/app/pgcheck` 까지). ⚠️ 서체는 이 환경에서 CDN 이 막혀 대체 서체로 잰 값이다 —
**절대 높이가 아니라 전후 비교**가 근거다.

**═══ ⑧ 안 한 것 · 남은 것 ═══**

```
  🔴 부록 B(확정 대기)  이용료 「무료」 표기 · 등록신청 승인 소요시간 · 계정 추가 발급 상한
                      — 정해지면 한 줄씩 더하면 된다. **지금 적지 않은 것이 맞다**
  ⚠️ 화면 캡처         나중에 넣기로 하면 23차 방식(Playwright + 목 Supabase)으로 찍는다 —
                      목 데이터라 개인정보 문제가 없다
  🔴 PDF 다운로드      사용자 확정으로 제외(파일이 구버전으로 남는다)
  ⚠️ Preview 실기기    사용자 확인 항목이다 — 이 환경은 프록시가 배포 도메인을 막는다
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-14, Claude Code 세션에서 직접 갱신 —
**차수 없음: 문서 정합 정리 3차** — 🔴 **코드 0 · DB 0 · `migrations/` 0.**
`CLAUDE.md`·`HANDOFF.md`·`docs/history/` 만. PR 없이 `main` 직접.
🔴 **위 `/guide` 차수와 한 커밋으로 묶었다** — 두 지시서가 **같은 두 파일을 PR 없이 `main`
직접**으로 고치는데, 따로 하면 뒤에 하는 쪽이 앞 커밋을 다시 읽고 자리를 옮겨야 한다.
지시서 §0-1 이 *「병행 세션이 아직 안 끝났으면 기다리십시오」* 라고 적어 둔 그 상황이다.

**═══ ① 🔴 지시서 §0-1 의 전제가 틀렸다 ═══**

지시서는 병행 세션(= `/guide`)을 *"코드 0 · 산출물은 Artifact + PDF"* 로 보고
**「§3 화면 구성과 프리렌더 개수는 안 바뀔 것」**이라고 적었다. **둘 다 바뀌었다.**

```
  공개 화면   6개 → **7개** (`/guide` 신설)
  프리렌더    44  → **45**
  sitemap     6   → **7 URL**(라우트에서 자동으로 뽑으므로 코드 변경 0)
```

🟢 지시서 자신이 *「실측으로 확인하십시오」* 라고 적어 둔 자리라 그대로 재서 갈랐다.

**═══ ② 실측 — 착수 전 확인 2·3·4번(내가 코드를 안 보고 쓴 자리) ═══**

**2번 · 관리자 「운송 운영」 메뉴 실제 순서** — 코드가 정본이다.

```
  운임기준표 · 견적 관리 · 운송오더 · 배차 관리 · 정산 관리 · 차주 관리 · 외부 정보망 관리
```

⚠️ **원칙 14번은 「외부정보망」을 적고 HANDOFF §3 에는 없었다** — 코드에는 **있다**
(`/admin/settings/external-networks`). 원칙 쪽이 맞았고 HANDOFF 가 빠뜨린 것이다.

**3번 · 오더 화면 4가지** — 실측이 지시서 표와 **한 항목 달랐다.**

```
  ① 들어가면 등록 폼이 먼저 열리는가   🔴 **아니다.** `showForm` 초기값이 `false` 이고
                                    「+ 신규 오더 등록」을 눌러야 열린다. 견적에서
                                    넘어온 경우(`from_quote`)만 자동으로 열린다
                                    🔴 **이것이 의도다** — 사용자 13건 중 1번(「바로
                                    등록창으로 시작하게 하자」)이 **리뷰 3라운드에
                                    사용자 지시로 뒤집혔다**(`59b0076`:
                                    *"전처럼 새 운송오더가 펼쳐저 있지 말고 운송목록이 보이게"*)
  ② 폼이 열려도 목록이 보이는가        🟢 **보인다.** `showForm &&` 게이트는 **폼 블록에만**
                                    걸려 있고(한 곳) 검색·필터·목록은 조건 없이 그린다
  ③ 회사명 자동 기입이 빈 칸만 채우나  🟢 **그렇다.** `prefillFromLastOrder()` 의 `put()` 이
                                    `next[key]` 가 비어 있을 때만 넣는다.
                                    ⚠️ 차량만 예외 — 기본값(`1톤 카고`)일 때만 갈아끼운다
  ④ 금액도 채우나                     🔴 **안 채운다.** select 목록에 금액이 아예 없고
                                    안내 문구가 *「금액과 일정은 채우지 않습니다」* 다
```

**4번 · A-5 세금계산서가 화면에서 어떻게 갈리는가**

```
  선착불(driver_direct)   화주 세금계산서 칸을 **감추지 않는다** — 자리는 남기고
                         「해당 없음 — 선착불은 화주가 차주에게 직접 지급합니다」로
                         **문구를 바꿔 그린다**(빈 칸이 아니다)
  차주 수수료분           정산 상세의 **선착불 블록 안**에 「차주 세금계산서 발행완료
                         (주선수수료분)」 체크 + 발행일.
                         🔴 컬럼이 **다르다** — `driver_tax_invoice_issued` /
                            `driver_tax_invoice_date` (화주쪽은 `tax_invoice_issued`)
  주선사정산(broker)      화주 세금계산서 · 화주 입금완료 · 차주 지급완료 그대로
```

🔴 **「빈 항목이니 살리자」로 화주 세금계산서 칸을 선착불에 되돌리지 말 것** — 위캐리가
화주에게 발행할 것이 없다(운임이 화주에서 차주로 바로 간다).

**═══ ③ 고친 것 여섯 + 곁다리 하나 ═══**

```
  ①  HANDOFF §3 「운송 운영」 메뉴 순서를 코드 실측값으로 · 언제 왜 바뀌었는지 한 줄
     🔴 원칙 14번 본문에 **「HANDOFF §3 도 같이」**를 덧붙였다 — 35차가 원칙은 고쳤는데
        HANDOFF 가 그 「여기」에 안 들어가서 갈렸다
  ②  HANDOFF §5-12 「선착불 정산 모델」 신설 + §4-3 에 한 줄
     🔴 **가장 급했던 것** — §5 확정 사항을 다 읽어도 「선착불의 받을 돈이 수수료」라는
        것을 알 수 없었다. 그 사실이 §6 진행 현황 표의 긴 한 칸에만 있었고,
        §6 은 「무엇을 했나」이지 「무엇이 확정인가」가 아니다
  ③  원칙 47번 본문 끝에 A-7 예외 한 줄
     🔴 원칙 본문은 「스냅샷을 직접 UPDATE 하지 말 것」이고 A-7 이 하는 일이 정확히
        그것인데, 「위반이 아니다」가 §5 로드맵에만 있었다 — 다음 세션이 지운다
  ④  §5 「35차 이후 요약」에 B장(오더 화면) 네 줄
  ⑤  HANDOFF §6 24시콜 행에 `lib/autoCreateInvoice.ts` · §0 표 자수 실측
  ⑥  CLAUDE.md 안의 모순 한 쌍(남은 「차수 없는 소수정」) 통일 · 프리렌더 기준선
  곁  🔴 §0 표의 `history-07`·`history-08` 자수가 **처음부터 틀려 있었다**(아래 ④)
```

**═══ ④ 🔴 곁다리 — §0 표의 자수 둘이 애초에 틀린 값이었다 ═══**

전수 실측에서 **두 행이 크게 어긋났다.**

```
  history-07   표 71,022자  →  실측 **38,232자**
  history-08   표 32,637자  →  실측 **18,221자**
  (나머지 여섯 행은 전부 실측과 ±200자 안이다)
```

🟢 **데이터 손실이 아니다.** 그 숫자를 처음 써 넣은 커밋(`c37d5b4`, 33차 기록)에서
파일을 꺼내 재 봤더니 **그때도 38,232자**였다 — 쓰는 순간부터 틀린 값이다.
🔴 **「줄어들었다」로 읽고 아카이브를 복원하려 들지 말 것.** 실측값으로 고쳤다.

⚠️ **두 파일은 PR #134 가 만든 것이 아니다** — `6b70a5a`(PR #134) 시점에는
`history-01~06` 뿐이었고, 07·08 은 `c37d5b4`(33차 기록, current.md 분리) 때 생겼다.

**═══ ⑤ current.md 를 또 갈랐다 — `history-09.md` 신설 ═══**

`current.md` 가 75,365자였고 이번 기록을 더하면 **80,000자 상한을 넘는다**(`/merge` 규칙).
가장 오래된 두 건을 `docs/history/history-09.md` 로 옮겼다.

```
  history-09.md   32차 #137 2026-09-09  →  이용약관 terms-v5 #138 2026-09-09   (2건)  14,012자
  current.md      33차 #139 2026-09-10  →  거래처 온보딩 안내 #148 2026-09-14
```

🔴 **한 글자도 안 지웠고 md5 로 대조했다**(`45ee5738…` — 자른 뒤 두 조각을 도로 이으면
원본과 같다). 🔴 **파일 머리의 「요약하지 마십시오」 경고를 지우지 말 것.**

**═══ ⑥ 36차 착수 전 입력을 받아 적었다 — 화주 거래조건 ═══**

`CLAUDE.md` §5 의 「36차」 줄 아래에 **「35차 착수 전」과 같은 형식**으로 블록을 넣었다.
🔴 **사용자 원문과 확정(4-2)은 그대로 옮겼고, 기획 세션의 조사(4-3)와 설계 제안(4-4)은
「확정이 아니다」로 갈라 적었다** — 섞으면 36차가 제안을 확정으로 읽는다.

🔴 **핵심 발견은 `companies.billing_cutoff_day`(정산 마감일)가 이미 있다는 것이다** —
원칙 46번 본문이 그 이름을 그대로 적고 있다. **신설하면 두 벌이 되어 월정산 묶음이
갈린다.** 이번 일은 「만드는 것」이 아니라 **화주 상세에 없다면 노출하는 것**일 가능성이
크다 — 🔴 **36차가 코드로 다시 재서 확인할 것.**

```
  🔴 다음은 36차   내부 정합 ② 화주·차주 (CRM 항목 신설 + 거래조건이 여기다)
  ⚠️ 그 뒤        24시콜 가져오기 ⓐ → ⓑ → ⓒ 세 차수(설계 확정 · 35차 merge 를 기다리던 것)
  ⚠️ 남은 소수정   admin 공개문의 배지 하나뿐이다(아래 ⑦ 에서 통일했다)
```

**═══ ⑧ 🔴 이번에 배운 규칙 — 새 확정은 §6 표가 아니라 §5 에 절로 만든다 ═══**

선착불 정산 모델이 **HANDOFF §6(진행 현황 표)의 긴 한 칸에만** 있었다. §6 은
「무엇을 했나」이고 §5 는 「무엇이 확정인가」다. 새 세션은 §5 를 읽고 판단하므로,
**확정을 §6 칸에만 적으면 그 확정이 없는 것과 같다.**
🔴 **앞으로 확정이 생기면 §6 에 이력 한 줄 + §5 에 절 하나를 같이 만들 것.**

⚠️ **완료조건 5(「`docs/history/` 기존 파일 diff 0줄」)를 지키지 못했다** — `current.md`
가 80,000자 상한을 넘어 `/merge` 규칙대로 **갈라야 했다**(위 ⑤). 지시서가 그 크기를
예상하지 못한 자리이고, **한 글자도 안 지웠음을 md5 로 증명했다.**

**═══ ⑦ CLAUDE.md 안의 모순을 어느 쪽으로 통일했나 ═══**

```
  §5 로드맵 줄    "admin 공개문의 배지 · 보험 문구 정합 · 접수 안내 문자"        ← 셋
  §5 아래쪽 줄    "남은 「차수 없는 소수정」은 admin 공개문의 배지 하나뿐이다"   ← 하나
```

🔴 **「하나」로 통일했다.** 근거 — **접수 안내 문자는 애초에 존재하지 않는 기능**이고
(61차 ⑨ 가 「지시서 3-9 의 전제가 틀렸다」로 확인), 만들려면 `/quote`·`/apply` API 에
발송 트리거를 붙이는 **별도 차수**다. **보험 문구 정합**은 30차에 `INSURANCE_ENABLED` 를
켜면서 랜딩·FAQ 가 이미 맞춰졌다. 둘 다 「소수정」이 아니다.
⚠️ **HANDOFF §6 「미정」에는 다섯이 있다** — 그쪽은 「소수정」이 아니라 「미정」 목록이라
범위가 다르다. 같은 값으로 맞추지 않았다.

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 랜딩 히어로에 「운송관리 계정 신청」 버튼** — **PR #147 merge됨**(squash `2ac879a`,
커밋 1개 · 파일 2개 · +36 / −4). 🟢 **DB 0** · `_migrations` **29행 그대로** ·
`globals.css` 0줄 · 관리자 0줄 · 화주포털 0줄 · `lib/` 0줄 · `components/` 0줄 ·
실사용 리뷰 라운드 0. 사용자 지시 한 줄에서 시작했다 —
*"상단 히어로 페이지에 전화문의 버튼 아래에 운송관리 계정 신청 버튼을 추가하자."*

**═══ ① 🔴 이 버튼은 **옛 결정 둘을 뒤집은 것**이다 ═══**

착수 전 이 자리에 두 줄이 있었다.

```
  app/page.tsx 주석  "히어로에 남는 유일한 행동이 전화다 — **새 버튼을 만들지 말 것**"
                     (사용자 지시 2026-09-02, 견적 버튼을 빼면서)
  CLAUDE.md 41차     "**히어로에 세 번째 선택지를 다시 넣지 말 것**"
```

🔴 **그 두 줄을 근거로 이 버튼을 빼지 말 것 — 지시로 뒤집힌 것이다.**
🔴 **주석도 같이 고쳤다.** 안 고치면 다음 세션이 주석을 근거로 조용히 되돌린다 —
   35차가 원칙 14번에서 **실제로 그 상태**를 발견했다(코드는 바뀌었는데 원칙이 옛
   순서를 적고 있었다). **코드를 뒤집었으면 그것을 금지하던 글도 같은 커밋에서 고칠 것.**
🔴 **넷째는 넣지 않는다** — 견적으로 가는 길은 헤더 CTA·마감 CTA 에 있다.

**═══ ② 무엇을 어떻게 넣었나 ═══**

- 🔴 **세로로 쌓는다**(지시가 「아래에」다). 가로로 두면 히어로 본문이 `min(760px, 44%)` 라
  `minWidth: 330` 짜리 둘이 1440px 에서 안 들어가 줄이 접힌다.
- 🔴 **옐로 `#FFD834`** — 랜딩의 다른 세 곳(⑥ 운송관리 섹션 · 마감 CTA · FAQ)이 전부
  이 색이라 **여기만 다르면 같은 행동으로 안 읽힌다.** 전화가 검정 채움 **+ 그림자**로
  여전히 1순위이고 이 버튼에는 그림자가 없다. 크기는 같은 `heroBtn`(330×83 / 모바일 212×56).
- 🔴 **`?from=landing` 을 붙이지 않았다** — 그 파라미터는 「페이지 한참 아래 ⑥ 섹션에서
  왔을 때 뒤로가기가 맨 위로 튕기지 않게」 하려는 것이다(`BackToHomeLink`).
  **히어로가 곧 맨 위**라 붙여도 얻는 것이 없다.
- 🟢 **모바일 CSS 는 한 줄도 안 썼다** — `landing.css` 가 **이미** 히어로 버튼을 세로로
  쌓고 있었다(`flex-direction: column` · `width: auto` · `min-width: 0`). 견적 버튼이
  있던 시절 규칙이 남아 있던 것이고, 그래서 모바일은 손댈 것이 없었다.

**═══ ③ 🔴 렌더링이 잡은 회귀 — 같이 고쳤다 ═══**

버튼만 넣고 끝냈더니 **1366~1440px 에서 제목 마지막 글자 「다.」가 차체의 WeCarry 로고에
정확히 겹쳐 안 읽혔다.** 원인은 히어로 본문이 `justify-content: center` 라
**버튼이 한 줄 늘면 제목이 그 절반만큼 위로 올라가기** 때문이다(83 + 간격 12 = 95 →
47px 상승). 하필 그 높이에 로고가 있었다.

⚠️ **가로 겹침은 내가 만든 것이 아니다 — 재서 갈랐다.** 제목은 전부터 사진 위로
266~432px 걸쳐 있었고 **그 폭은 전후가 완전히 동일**했다(1440에서 288px).
바뀐 것은 **세로 47px 뿐**이다. 🔴 **스크린샷만 보면 「내가 겹치게 만들었다」로
오판하기 딱 좋은 자리다** — 원인을 가르기 전에 숫자를 볼 것.

고친 방법 — `app/landing.css` 에 **1321px 이상 전용**으로 히어로 위 여백 120 → **215px**
(버튼 83 + 간격 12 = 95, 가운데 정렬이라 절반인 47이 정확히 상쇄된다).
🟢 결과로 **제목 위치가 모든 폭에서 기준선과 한 픽셀도 다르지 않다**
(1920·1600 = 277 / 1440 = 284 / 1366 = 288 / 1280 = 120).

🔴 **히어로 버튼을 더하거나 빼거나 크기를 바꾸면 이 215px 도 같이 고칠 것** — 안 고치면
   제목이 조용히 위아래로 움직이고, 그 증상이 **「글자가 트럭에 묻힌다」**로 나타난다.
🔴 **1321px 이상에만 건다** — `max-width: 1320px` 부터는 `height: auto` 라 가운데 정렬
   자체가 없어서, 거기 걸면 그냥 95px 밀려 내려간다.

**═══ ④ 검증 · 브랜치 ═══**

- 임시 dev 서버 + **실제 렌더링**으로 1920 / 1600 / 1440 / 1366 / 1280 / 390 / 360px 측정.
  세로 쌓임 ✅ 전 폭 · 가로 오버플로우 0 · 버튼 잘림 0(1440 기준 본문 바닥 695 < 820) ·
  모바일 전화 버튼 **225×56 유지**(HANDOFF §5 가 못박은 값)
- tsc 통과 · 프리렌더 실패 **44건** 기준선과 **항목 단위 동일**(변경 전 트리를 stash 로
  따로 빌드해 `diff`)
- ⚠️ **원격 브랜치가 merge 때 삭제돼 있었다** — 35차 merge 후 `--force-with-lease` 가
  *"stale info"* 로 거부됐는데, 원인은 충돌이 아니라 **원격 ref 자체가 없는 것**이었다
  (`git fetch origin <branch>` 가 `couldn't find remote ref`). 🔴 **강제로 밀어붙이기 전에
  원격에 그 브랜치가 실제로 있는지부터 볼 것** — `git remote prune origin` 뒤 평범한
  push 로 끝났다.

**═══ ⑤ ⚠️ 사용자 확인을 못 받고 merge 했다 ═══**

merge 지시가 Preview 확인보다 먼저 왔다. 🔴 **다음 둘은 아직 눈으로 확인되지 않았다** —
① **옐로 톤이 적당한지**(더 조용한 대안: 테두리만 있는 흰 버튼. 한 줄이면 바뀐다)
② **노트북 폭 1366~1440px 에서 제목 끝 글자가 트럭에 안 가리는지**(③ 에서 고친 자리다.
숫자로는 기준선과 동일하지만 실기기 확인은 아직이다).
⚠️ 둘 다 **되돌리기 쉬운 표면 변경**이라 merge 를 막을 이유는 아니었다.
