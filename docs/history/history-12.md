> 🔴 이 파일은 `CLAUDE.md` 에서 옮겨온 세션 기록입니다. **요약하거나 줄이지 마십시오** —
> 되돌리면 안 되는 결정의 근거가 여기에만 있습니다.
> 담는 범위: 차수 없음 35차 착수 전 입력 정리 2026-09-11  →  35차 #146 2026-09-14   (2건)
> 입구는 `HANDOFF.md`, 규칙과 로드맵은 `CLAUDE.md` 입니다.
---


**35차: 내부 정합 ① 오더·배차·정산 + 운영 대시보드** — **PR #146 merge됨**(squash `9abca84`,
커밋 17개 · 파일 17개 · +2,496 / −416). 🔴 **DB 변경 있음**(`_migrations` **29행**) ·
🟢 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `globals.css` 0줄 · `lib/legal/` 0줄 ·
🔴 **실사용 리뷰 8라운드**(본작업 커밋 7 + 리뷰 반영 커밋 8 + 진단 4).

사용자가 배포본을 쓰며 짚은 **13건**(2026-09-11, `CLAUDE.md` §5 에 원문 보존)을 A·B·C 세
장으로 묶어 처리했고, merge 전에 실사용 리뷰가 **여덟 번** 붙었다.

**═══ ① A장 — 선착불 정산 모델이 틀려 있었다 (본체) ═══**

```
[선착불 driver_direct]                      [주선사정산 broker]
  화주 ──합계──▶ 차주                          화주 ──청구액──▶ 위캐리 ──지급액──▶ 차주
  차주 ──수수료─▶ 위캐리                        받을 돈 = 청구액 · 줄 돈 = 지급액
  🔴 받을 돈 = 수수료 · 줄 돈 = 0
```

종전 코드는 **수금방식과 무관하게** 「받을 돈 = 화주 청구액 · 줄 돈 = 차주 지급액」으로
정산 건을 만들었다. 선착불은 둘 다 틀린다 — **위캐리를 거치지도 않는 운임이 화주
미수금으로 잡혀 있었다.**

🔴 **되돌리면 안 되는 것** — `receivable = isDirect ? fee : charge` · `payable = isDirect ? 0 : payout`.
「화주 청구액으로 되돌리자」가 나오면 위 그림을 볼 것. 🔴 **받을 수수료를 입금 완료와
한 칸으로 뭉개지 말 것** — `brokerage_fee_paid` 가 따로 있고 **그것이 선착불 정산 건의
유일한 미수금**이다.

**═══ ② A-6 — 정산 자동등록이 화면에 두 벌이었고 실제로 갈려 있었다 ═══**

같은 함수가 `admin/dispatches/[id]/page.tsx` 와 `admin/dispatches/page.tsx` 에 **각각**
있었고, 목록 쪽에는 *"예전부터 settlement_type 자체가 누락돼있던 버그가 있었음"* 이라는
주석이 남아 있었다. `lib/autoCreateInvoice.ts` 하나로 뺐다.
🔴 **화면 파일에 본체를 다시 만들지 말 것** — 24시콜 가져오기 차수(ⓒ)가 이 함수를 부른다.

**═══ ③ 🔴 A-7 — 옛 건을 고칠 경로가 **아예 없었다** (리뷰 1라운드) ═══**

사용자 지적: *"예전 오더에서 마진부분이나 수수료부분이 잘못된 부분이 있는데 수정이 필요하다."*

맞았고 **내가 빠뜨린 것**이다. `invoices` 금액은 생성 시점 스냅샷인데(원칙 47번) 정산
상세의 금액 칸은 전부 읽기 전용이고 `invoices/save` 화이트리스트에도 금액이 없어
**화면에서도 못 고치고 우회할 길도 없었다.** ⚠️ 더 나쁜 것은 `lib/autoCreateInvoice.ts`
주석이 *"뒤늦게 맞추는 것은 담당자가 화면에서 하는 별도 경로다"* 라고 적고 있었는데
**그 경로가 실재하지 않았다** — 2026-09-11 사용자 확정(「스냅샷 갱신을 35차에」)을
A·B·C장을 만들면서 흘린 것이다.

`app/api/admin/invoices/resync/route.ts` 신설(service_role). 🔴 **원칙 47번 위반이 아니다** —
그 원칙이 막는 것은 「나중에 생긴 **추가** 금액을 스냅샷에 더하는 것」이고 그건 그대로
금지다. 이건 **처음부터 틀리게 얼려진 기준 금액을 바로잡는 것**이다.
🔴 **추가비는 `created_at <= invoice.created_at` 인 것만 넣는다** — 정산 상세가 그 뒤
추가비를 표시 시점에 더하므로(원칙 51번) 이 필터를 빼면 **두 번 청구**된다.
🔴 `invoice.created_at` 을 건드리지 말 것(그 필터의 기준선이다) · 🔴 입금·지급 완료
플래그와 청구주기는 안 건드린다 · 🔴 금액 칸을 직접 편집하게 만들지 말 것(정본은 배차다).

**═══ ④ 🔴 A-8 — 마진 공식이 한 화면 안에 **셋**이었다 (리뷰 2라운드) ═══**

사용자 지적: *"정산관리에서 배차관리에서 정해진 지급운임 및 마진이 제대로 적용이 안되는 것 같다."*

```
  신규 정산 등록 폼 미리보기 :  청구액 − 지급액
  저장되는 commission_total :  청구액 × 1.1 − 지급액   ← 부가세 구분을 아예 안 봄
  운영 대시보드             :  lib/marginCalc.ts
```

**등록할 때 뜬 숫자와 저장된 숫자와 대시보드 숫자가 셋 다 달랐다.** 게다가 가운데 식은
청구액을 「부가세 포함」으로 적은 건에 ×1.1 을 한 번 더 걸어 **마진을 부풀리고 있었다.**
목록 조회에는 부가세 구분 컬럼 자체가 없어 「부가세 별도」를 **글자로 박아뒀고**,
배차에서 「포함」으로 정해도 목록은 계속 「별도」라고 썼다.

🔴 **마진은 `lib/marginCalc.ts` 하나다. 화면에 공식을 다시 적지 말 것.**
🔴 **저장값(`commission_total`)에서 읽지 말 것 — 표시 시점 계산이라 옛 건도 자동으로 맞는다.**
🟢 그래서 **재동기화를 안 눌러도 마진은 바로 맞는다** — 재동기화가 필요한 것은 **금액
자체가 배차와 다른 건**뿐이다(둘은 다른 문제다).

**═══ ⑤ A-9 — 「주선수수료 면제」 제거, 그러나 게이트는 남겼다 (리뷰 2라운드) ═══**

사용자 지시: *"「주선수수료 면제」 옵션은 따로 있을 필요가 없을 것 같다."*

화면·저장 화이트리스트·자동등록·재동기화에서 뺐다. 컬럼은 읽기 전용으로 얼렸다(원칙 45번).
🔴 **게이트까지 없애지는 않았다 — 사용자 지시를 그대로 따르지 않은 자리다.** 그 체크가
정산확정 게이트의 판정 근거였고 같이 없애면 **수수료 입력을 빠뜨린 건이 미수금 없이
조용히 확정된다.** 그래서 별도 컬럼·체크박스 없이 **확정 직전 `window.confirm` 한 번**으로
옮겼다(*"주선수수료가 0원입니다… 입력을 빠뜨린 것이라면 취소하고 금액을 먼저 적어주세요"*).
🔴 **이 확인 창을 지우지 말 것** — 지우면 「정말 0원」과 「아직 안 적었다」를 가릴 장치가
하나도 없어진다.

**═══ ⑥ B장 — 오더 폼 · C장 — 대시보드 ═══**

B장: 블록을 **① 구간·현장 ② 일정 ③ 화물·차량 ④ 요청사항 ⑤ 정산·금액**으로 다시 묶었다
(그전엔 주소 → 차량 → 정산방식 → 일정 순이라 **일정이 차량·정산 뒤**였다).
🔴 **오더에 금액 칸이 하나도 없었다**(실측) — 금액은 배차 등록 때 처음 들어갔다.
`orders.customer_charge` + 부가세 구분 신설. 🔴 **`orders.tonnage` 는 안 썼다** — 컬럼은
있지만 **관리자 어디서도 읽지 않는다**(실측). 나눠 담으면 두 곳이 갈린다.
🔴 **⑤ 정산·금액은 포털에 없는 블록이다**(화주 비노출 정보).
🔴 **직전 오더 제안이 금액·일정을 채우지 않는다** — 지난 금액은 잘못된 청구가 되고 지난
날짜는 원칙 6번과 부딪힌다.

C장: 마진 정의를 `lib/marginCalc.ts` 로. **「위캐리가 버는 돈, 부가세 제외」 하나**이고
수금방식이 계산식을 정한다(선착불은 **운임을 매출에도 원가에도 안 잡는다** — 취급고가
매출로 둔갑한다). 🔴 **대시보드에 마진 셋을 섞지 말 것** — `dispatches.margin`(DB 생성
컬럼, 부가세 환산 없음) · `settlementCalc.realMargin`(**`driver_base_fare` 입력 실측 0건**이라
켜면 숫자가 틀린다) · 이 파일.

**═══ ⑦ 리뷰 3~6라운드 — 화면 정리 · 조회기간 · 엑셀 ═══**

```
  3  59b0076  "운송오더에서 전처럼 새 운송오더가 펼쳐저 있지 말고 운송목록이 보이게"
     → 목록으로 열고 「+ 신규 오더 등록」을 제목 아래로.
     🔴 PR #144 가 없앤 「폼 열림 시 목록 미렌더」 분기를 되살린 것이 아니다
  4  1d0b14b  "차주관리는 정산관리 아래에" → 운임기준표·견적·오더·배차·정산·차주·외부정보망
     🔴 **원칙 14번 본문의 메뉴 순서를 이때 같이 고쳤다**(안 고치면 원칙이 코드와 어긋난다)
  5  fd19ae3  "조회기간 검색 + 월별 마진추이" → 프리셋 5개 + 월 직접 선택 · 세로 막대 그래프 ·
     담당자별을 맨 아래로. 🔴 **조회기간은 서버까지 간다**(`?months=`, 상한 120) —
     화면에서만 자르면 24개월을 골라도 12개월치밖에 없다
  6  62465e3  "엑셀로도 다운받을 수 있게" → 시트 4개. 🔴 금액은 **숫자**(문자열이면 SUM 이
     안 되어 엑셀로 주는 의미가 없다) · 🔴 차주 성명·연락처·지급운임은 어떤 시트에도 없다
```

**═══ ⑧ 🔴 리뷰 7~8라운드 — 부가세. 실측이 결함 넷을 잡았다 ═══**

7라운드(*"마진추이와 수익성 금액에서 부가세 포함가도 표시… 가로를 치거나 약한 글씨로"*)
는 다섯 자리에 괄호+흐린 글씨로 병기한 것인데, **화면을 실제로 그려 보고서야** 둘이 나왔다.

- 🔴 **담당자별 8자리 금액의 「원)」이 아랫줄로 떨어졌다** → 칸 120 → **150px + `nowrap`**
- 🔴 **평균선 라벨 「평균 148만」이 첫 달 막대 라벨과 겹쳐** 바로 아래 「(141만)」이 놓여
  **포함가가 더 작은 것처럼** 읽혔다 → **선 위의 숫자를 없앴다.** 리뷰 5라운드가 그 라벨을
  왼쪽 끝에 둔 것은 오른쪽이 24개월에서 화면 밖으로 밀려서인데, **왼쪽 끝이 첫 달 막대의
  라벨 자리**다. 첫 달 실적이 평균 근처면 언제든 겹치므로 **자리를 옮겨도 재발한다** —
  값은 머리줄·안내문이 말한다. 🔴 **다시 얹지 말 것**

8라운드(*"세금계산서 처리에서 부가세 금액에 살짝 차이가 있다. 이부분을 어떻게 잡을 수
있나?"* + *"25,000원이어야 하는데 24,999원"*) — **두 신고가 같은 뿌리였고 결함이 둘이었다.**

```
  (가) 합계에 ×1.1 을 했다            25,000원 3건 → 74,999  (정답 75,000)
       부가세는 **세금계산서 단위로 원 단위가 확정**되므로 합계의 부가세가 아니라
       **건별 부가세의 합**이 맞다. 건당 최대 0.5원이라 **1건이면 안 보인다**(10건 −3원 · 37건 −11원)

  (나) 포함가로 적은 금액을 포함가로 **되계산**했다
       round(x ÷ 1.1) × 1.1 은 원래 값으로 안 돌아온다 — **11,000원마다** 1원
       (6,000 → 6,001 · 17,000 → 17,001 …)
       🔴 **주선수수료는 포함가로 기입하므로 이것이 24시콜 대조의 원인이다**
```

`lib/vat.ts` 에 **`splitVat(금액, 포함여부)`**, `lib/marginCalc.ts` 에 **`calcMarginInclusive()`** 신설.
🔴 **`inclusive` 를 `supply × 1.1` 로 되돌리지 말 것**(입력값이 보존되지 않는다) ·
🔴 **부가세는 `공급가액 × 0.1` 이 아니라 차액이다**(따로 반올림하면 `공급 + 부가세 ≠ 합계` 인
줄이 생긴다) · 🔴 **포함가는 건별로 구해서 더한다** — 그래서 집계마다 `marginIncl` 을 **따로
이고 다닌다.** 마진 합계에서 되계산하면 (가)가 그대로 돌아온다.
🟢 **`calcMargin`(공급가액)은 한 줄도 안 바뀌었다** — `lib/vat.ts` 는 **순수 추가**(+34/−0).

**═══ ⑨ 지시서·전제와 달랐던 것 ═══**

1. 🔴 **착수 전 확인 6번의 전제가 틀렸다** — 지시서는 *"차주쪽 `driver_vat_included` 는 있는
   것으로 보입니다"* 라고 했는데 **양쪽 다 없었다.** 7차 세션에 부가세 컬럼 **4개를 전부
   삭제**하고 `toSupplyAmount()` 도 같이 지웠다. 이번 작업은 신설이 아니라 **되살리기**다.
2. **마진은 둘이 아니라 셋이었다** — 대시보드가 쓰던 `청구 × 1.1 − 지급` 이 셋째였고,
   리뷰 2라운드에 **정산 화면 안에서만 또 셋**이었다는 것이 드러났다.
3. **7번(정산확정 안 됨)의 원인은 「지나간 날짜」가 아니었다** — 신고 한 줄이 서로 다른 둘이었다.
   ① 확정이 막힌 것은 2026-09 건이 **선착불 · 수수료 0원 · 지급자 빈칸**이라 게이트에 걸린 것
   (같은 조건이 DB 전체에 **딱 1건**) ② 금액이 안 맞는 것은 **스냅샷 문제**(정산 50,000 vs
   배차 45,000, 정산이 먼저 만들어짐). 🔴 **한 줄의 신고가 둘일 수 있다 — 먼저 가를 것.**
4. **오더 폼이 견적 폼과 같은 스코프(`req-marks quote-form`)를 쓴다.** 이름이 「quote」인 채로
   공유하는 이유는 같은 규칙을 두 벌로 복사하면 한쪽만 고쳐져 갈리기 때문이다. 이름을 바꾸려면
   `globals.css` 를 건드려야 하고 그러면 견적 폼이 같이 움직인다.
5. **C-3(포털 월별통계 참고)은 본작업에 안 넣었다** — 🟢 **리뷰 5라운드에 사용자가 직접 지시해
   그때 들어갔다.** 🔴 **`--pv2-*` 는 `.portal-v2` 안에만 있어 포털 부품을 끌어올 수 없다** —
   관리자 어휘(네이티브 `select`)로 새로 그렸다(원칙 57번은 **포털 한정**이다).

**═══ ⑩ 검증 · 아직 안 한 것 ═══**

- 프리렌더 실패 **44** — 기준선과 **개수뿐 아니라 항목까지** 동일. 🔴 **매 라운드 변경 전
  트리를 `git stash` 로 따로 빌드해 두 목록을 `diff` 했다**(개수만 맞춰보면 맞바꿔치기를 못 잡는다)
- 부가세 산술은 **1원~500,000원 전수**로 왕복 어긋남 0 · `공급 + 부가세 = 합계` 어긋남 0
- 엑셀은 **목이 아니라 실제 `lib/exportExcel.ts`** 로 써서 되읽어 확인(원칙 56번).
  ⚠️ **`XLSX.readFile` 은 `cellStyles: true` 없이는 스타일을 통째로 버린다** — 그걸 모르고
  재서 「머리글 굵게 ❌」라는 **거짓 실패**가 났다
- 화면은 임시 라우트 `app/pgcheck/` + 목으로 렌더링해 쟀다(커밋 전 삭제, `.next/types` 포함)
- ⚠️ **마이그레이션 단언 숫자를 11로 잘못 세서 apply 가 멈췄다** — 파일 전체가 한 트랜잭션이라
  **DB 에는 아무것도 반영되지 않았다**(안전장치가 제 몫을 했다). 10으로 고쳐 다시 돌렸다
- ⚠️ **390px 가로 스크롤(516px)은 대시보드에 원래 있던 것이다** — 기준선을 stash 로 다시 재서
  같은 값임을 확인했다(내 변경이 더한 것 0). **휴대폰 대응은 별도 차수**
- ⚠️ **기존 정산 건을 SQL 로 일괄 수정하지 않았다** — 잘못 잡힌 건은 **선착불 3건**이고,
  원칙 47번대로 **화면에서 담당자가 고치는 경로(A-7)만 열었다**
- ⚠️ **DB 에 수수료 기준이 섞여 있다** — 5,000(포함가)과 4,545(공급가액). 라벨이 「공급가액」이던
  시절 것이다. 🔴 **금액이 맞아도 부가세가 안 맞는 원인이 될 수 있다**

**═══ ⑪ ⚠️ 사용자가 코드 밖에서 해야 하는 것 ═══**

🔴 **배차 담당자에게 운영 규칙 전달** — 선착불 **주선수수료는 이제 「부가세 포함가」로 기입**한다
(그전 라벨이 「공급가액」이라 두 기준이 섞여 있다). 코드가 강제하지 못하는 부분이다.

**═══ ⑫ 24시콜 가져오기 설계서와의 관계 ═══**

설계서 §8 「35차 지시서에 먼저 넣을 것」 **2건이 이 차수에 들어갔다** — ① 선착불 받을 돈(A-1)
② 정산 자동등록 공용 함수화(A-6). ⓐ·ⓑ·ⓒ 본체는 착수 전이고 `dispatches.external_order_no` 도
아직 없다.
🔴 **그러나 35차가 설계서 §5 매핑표를 낡게 만들었다** — 설계서는 *"margin(생성컬럼) = 24시콜
수수료 → 자동 일치"* 라고 적었는데, **35차가 선착불 마진의 출처를 `brokerage_fee ÷ 1.1` 로
바꿨다.** ⓐ가 `brokerage_fee` 를 안 쓰면 배차 목록의 단순마진만 맞고 **정산 미수금·대시보드
마진은 0**이 되며 정산확정에서 0원 확인창에 걸린다. **§5 에 `brokerage_fee` 행을 더해야 한다.**
⚠️ **그 행의 부가세 기준이 미결이다** — 설계서 자신의 실측(*"송금할 운임(부가세포함) = 합계 ×
1.1 − 산재"*)이 **합계 80,000 이 공급가액**임을 뜻하므로 수수료 15,000 도 공급가액인데,
35차는 `brokerage_fee` 를 **포함가 기입**으로 확정했다. 그대로 넣으면 마진이 10% 적게 잡힌다.
🟢 **`customer_charge_vat_included` 는 `false`(기본값)가 맞다** — 마이그레이션의 「합계가 포함가면
true 를 넣으라」 경고는 이 근거로 닫힌다.
🟢 **§7 착수 전 확인 하나는 답이 나왔다** — 차주 운임은 **`driver_base_fare`** 에 넣는다.
`handlePayoutCalcSave()` 가 그 값으로 산재·부가세를 계산해 **`driver_payout` 에 최종값을
덮어쓴다.** `driver_payout` 에 직접 넣으면 계산기를 여는 순간 덮어써진다.

---

**차수 없음: 35차 착수 전 입력 정리** — 🔴 **코드 0 · DB 0 · 화면 0.**
`CLAUDE.md`·`HANDOFF.md` 둘만. 사용자가 배포본을 쓰면서 파악한 수정 항목 **13건**을
받아 적고, 그중 전제가 틀릴 만한 것을 코드에서 확인했다.

**═══ ① merge 할 것은 없었다 ═══**

사용자 지시가 *"앞서 마무리된 내용을 merge하고 문서를 업데이트"* 였는데 **열린 PR 0건 ·
작업트리 깨끗 · `main` 이 이미 `1837ca3`** 이었다. PR #145 가 마지막이고 그 뒤 문서
커밋 둘(`a24fb50` · `1837ca3`)까지 끝나 있었다. 🔴 **없는 것을 만들어내지 않고 그대로
보고했고**, 대신 전달된 목록을 기록하는 것으로 이 차수의 값어치를 만들었다.

**═══ ② 🔴 사용자 원문 13건은 `CLAUDE.md` §5 에 그대로 있다 ═══**

여기에 다시 옮기지 않는다(두 곳에 적으면 갈린다). **지시서를 쓸 세션은 그 블록을
읽을 것.** 🔴 **원문을 요약·정리하지 말 것** — 지시서가 범위를 정할 근거다.

갈래만: 운송오더 **6** · 배차관리 **1** · 정산관리 **4** · 운영대시보드 **2**.

**═══ ③ 🔴 조사 — 7번(정산확정이 안 된다)은 날짜 문제가 아닐 가능성이 크다 ═══**

사용자가 *"지나간 날짜에 운임료 정보 수정을 해서 그런가?"* 로 의심했는데, 코드를 보면
**날짜와 무관한 구조적 이유**가 먼저 걸린다.

```
  차주지급액이 안 보인다   invoices 금액은 정산 건 **생성 시점 스냅샷**이다(원칙 47번).
                          신규 정산 등록이 배차 값을 `insert` 로 복사하고, 그 뒤 배차에서
                          운임을 고쳐도 **따라오지 않는다.**
                          → 정산 건이 배차 운임을 적기 전에 먼저 만들어졌을 가능성
                          🔴 그렇다면 「버그」가 아니라 **갱신 경로가 없는 것**이고,
                             원칙 47번을 건드리는 **설계 결정**이 된다

  정산확정이 안 된다      막는 자리가 따로 둘이다(invoices/[id] handleConfirmSettlement) —
                          선착불에서 ① 수수료 > 0 인데 입금완료 미체크
                                      ② 수수료 = 0 인데 지급자가 「면제」가 아님
                          그리고 **확정 버튼 자체가 admin 롤에게만 보인다**
```
🔴 **셋 중 무엇인지 먼저 가릴 것** — 답에 따라 35차 범위인지 별건 수정인지가 갈린다.
⚠️ 그래서 `HANDOFF` §7 「미결·위험」에도 한 줄 올렸다(운영에서 지금 아픈 항목이라).

**═══ ④ 🔴 조사 — 10번(수수료 지급자 항목 불필요)은 「화면에서 빼기」로 안 끝난다 ═══**

`brokerage_fee_payer` 는 `dispatches`·`invoices` **컬럼**이고 화면 셋이 쓴다(배차 상세 ·
정산 상세 · 정산 목록 계산). 🔴 **값 중 `waived`(면제)가 ③ 의 정산확정 게이트에 실제로
쓰이고 있다** — 지우면 그 게이트가 같이 무너진다.
**화면에서 감추는 것 / 컬럼·게이트까지 없애는 것은 다른 작업이고, 지시서가 정해야 한다.**

**═══ ⑤ 🟢 조사 — 27차 확정과 충돌하지 않는다 (5·6·9번) ═══**

「선착불이면 견적서에 입금 계좌를 그리지 않는다」(27차 5라운드 확답 — *"주선수수료는
화주가 주는 경우는 없다"*)는 **그대로 유효**하다. 다만 **부가세 포함/별도 축이 하나 더
생기므로** 이미 있는 `customer_charge_vat_included` 와 어떻게 맞물리는지를 지시서가
짚어야 한다(새 컬럼을 또 만들면 원칙 45번의 「두 값이 서로를 덮어쓰는」 자리가 된다).

**═══ ⑥ 🔴 조사 — 대시보드(12·13번)는 「마진이 둘」인 것이 핵심이다 ═══**
```
  단순마진   dispatches.margin — DB **생성 컬럼**(customer_charge − driver_payout).
             직접 UPDATE 안 된다
  실질마진   lib/settlementCalc.ts — 공급가액 기준 + 주선사부담 산재보험료 반영
```
🔴 **어느 쪽으로 통계를 낼지 지시서가 정해야 한다 — 섞으면 숫자가 안 맞는다.**
⚠️ 포털 월별통계를 참고하더라도 **`--pv2-*` 33개는 `.portal-v2` 스코프 안에만 있다**
(28차 조사 §7, PR #145 에서 다시 실측). 끌어오려면 `.pv2-tokens` 를 **추가**해야 하고,
**옮기면 `globals.css` 삭제 줄이 생긴다**(24차부터의 무회귀 근거가 깨진다).

**═══ ⑦ 안 한 것 ═══**
```
  🔴 코드·DB 를 한 줄도 안 고쳤다 — 지시서를 받기로 한 것이 사용자 결정이다
  🔴 7번을 실제로 재현해 보지 않았다 — 운영 데이터가 필요하고, 어느 정산 건인지
     모른다. **지시서 착수 전 확인에 「그 건의 invoice created_at 과 배차 updated_at
     을 비교」를 넣으면 ③ 의 가설이 바로 판가름난다**
  ⚠️ 13건 중 코드·DB 를 안 건드리고 되는 것은 **하나도 없다** — 전부 35차(또는 분할) 범위다
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 혼적 31~250km 할인율 확정** — 🔴 **코드 0 · DB 0 · 화면 0.**
`CLAUDE.md`·`HANDOFF.md` 둘만. PR #136 부터 열려 있던 미결 하나를 닫았다.

**═══ ① 사용자 확정 — (A) 현행 유지 ═══**

바로 앞 차수(문서 정합 정리 2차)가 「담당자 확인이 여전히 필요하다」로 남긴 항목을
사용자에게 셋 중 하나로 물었고 **(A) 를 골랐다**(2026-09-11).
```
  (A) 35% 가 대충 맞다 → 아무것도 안 한다. 실거래가 쌓이면 그때 다시 본다   ✅ 확정
  (B) 다른 숫자가 맞다 → 한 줄
  (C) 거리로 나누는 것 자체가 어색하다 → 별도 차수
```

🔴 **「검증됐다」가 아니다.** (A) 는 **「지금은 안 바꾸고 실거래를 모은다」**는 뜻이고,
값의 근거는 **여전히 0쌍 보간값 그대로**다. 다음 세션이 이 확정을 근거로
「35% 는 확인된 값」이라고 적으면 안 된다 — 그러면 나중에 실측이 나왔을 때
그것을 근거 있는 값과 비교하는 셈이 된다.

**═══ ② 근거 상태 (안 바뀌었다) ═══**
```
  ~30km       2쌍   서울 0.85 · 경기 0.88            → 지금 DB 20%
  31~250km    0쌍   🔴 보간값(위아래 중간)           → 지금 DB 35%   ← 이 항목
  251km 이상  5쌍   0.33~0.67 · 중앙 0.46            → 지금 DB 55%
```
⚠️ **국내 화물의 대부분이 가운데 구간이다** — 근거가 가장 약한 값이 가장 자주 쓰인다.
🟢 다만 **이것은 계산식이 아니라 견적 입력칸의 기본값**이고 담당자가 건별로 고치면
그 값이 이긴다. 그래서 즉시 사고가 나는 구조가 아니고, (A) 가 성립한다.

**═══ ③ 🔴 언제 다시 보는가 ═══**
```
  🔴 신호 1   31~250km 구간에서 혼적 실적이 쌓여 **같은 구간 독차 건과 비교 쌍이 생겼을 때**
  🔴 신호 2   담당자가 그 기본값을 **매번 손으로 고치고 있을 때** — 기본값이 안 맞는다는 뜻이다
```
🔴 **바꾸는 자리는 `/admin/rates` → 「가산기준」 탭 → 「표준 혼적 할인율」이다**
(마이그레이션이 아니다 — 화면에서 고치는 값이다).
⚠️ **화면에서 고치면 저장소 문서에는 안 남는다** — 30km 가 실제로 그렇게 15 → 20 이
됐고, 그 때문에 문서와 DB 가 갈려 앞 차수가 그것을 잡는 데 매달렸다.
🔴 **바꾸면 그 사실을 문서에 남길 것.**

**═══ ④ 안 한 것 ═══**
- **값 변경 0** — (A) 라서 DB 도 코드도 손대지 않았다. `_migrations` **28행 그대로**.
- **근거 보강 0** — 비교 쌍을 만들 실적이 아직 없다. 만들려면 그 구간의 혼적 완료
  건이 필요하고, 그것은 운영이 쌓아야 하는 것이지 코드가 만들 수 있는 것이 아니다.

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 문서 정합 정리 2차** — 🔴 **코드 0 · DB 0 · `migrations/` 0 ·
`docs/history/` 의 옛 기록 0.** 고친 것은 `CLAUDE.md`·`HANDOFF.md` 둘뿐이고
이 기록만 새로 쌓았다. PR 없이 `main` 직접(문서만 바뀌는 커밋 규칙).

**═══ ① 🔴 혼적 30km 구간이 두 문서에서 갈려 있었다 — DB 는 20% 다 ═══**

이 차수가 급했던 이유다. 혼적 할인율은 담당자가 **매일 보는 입력창 기본값**이라
어느 문서를 보느냐로 견적이 달라진다.

```
  HANDOFF §5-1        30km 이내 15%      ← 낡았다
  CLAUDE.md §2·PR#136 15 / 35 / 55       ← 낡았다
  CLAUDE.md PR#142 요약  20 / 35 / 55    ← 맞았다
  ─────────────────────────────────────
  DB 실측 (verify ⑩-e)  20 / 35 / 55     🔴 정본
```

🔴 **왜 갈렸는지까지 짚었다** — PR #136 마이그레이션은 **15 로 넣었고 단언까지
15/35/55 로 걸어 뒀다**(파일 61~63행 · 124행). 그런데 DB 세 행의 `updated_at` 이
`2026-09-09 03:10:47.965` 로 **모두 같고** `updated_by` 가 직원 계정이다.
`/api/admin/mixed-loading-discount-settings` 가 세 행을 **한 `now` 로 묶어
UPDATE** 하므로, 이것은 **담당자가 `/admin/rates` 에서 세 칸을 한 번에 저장하며
30km 만 20 으로 올린 흔적**이다. 마이그레이션 insert 흔적이 아니다.

🔴 **그래서 마이그레이션 파일의 15 를 근거로 되돌리지 말 것.** 적용이 끝난 SQL 은
「그때 넣은 값」이고 그 뒤 화면에서 바뀔 수 있다 — **DB 가 정본이다.**
🔴 **`migrations/2026-09-09_mixed_discount_tiers.sql` 은 한 글자도 안 고쳤다**
(값을 바꾸려면 새 파일이다).

⚠️ **PR #136 로드맵 기록의 「15 / 35 / 55」는 지우지 않고 남겼다** — 그 차수가
실제로 넣은 값이라 지우면 이력이 틀려진다. 대신 **「지금 DB 는 20/35/55 다 ·
이 줄의 15 를 현재 값으로 읽지 말 것」**을 덧붙였다.

**═══ ② 전수 grep 결과 — 아카이브는 안 고쳤다 ═══**
```
  루트 두 문서   4군데   CLAUDE.md 3(§2 · PR#136 기록 · PR#142 요약) · HANDOFF 1(§5-1)
                        → 셋을 고쳤고 PR#142 요약은 원래 맞아서 근거만 덧붙였다
  docs/history/  2군데   current.md(PR#136 기록) · history-08.md
                        → 🔴 **안 고쳤다.** 틀린 것이 아니라 「그때 그렇게 적었다」는
                          기록이고, 고치면 PR #134 의 md5 대조 근거가 무효가 된다
  migrations/    1파일   값 3곳 + 단언 1곳 → 🔴 **안 고쳤다**(적용이 끝난 SQL)
  HANDOFF §6     0군데   PR #136 행에는 값이 안 적혀 있었다
```

**═══ ③ 🔴 다마스·라보 적재 한도 — 무게가 아니라 부피·형태로 갈린다 ═══**

사용자 확정(2026-09-11). `HANDOFF` §5-1 차급 칸에 넣었다.
```
  무게      둘 다 500kg 이내          ← 🔴 **여기서는 둘이 같다**
  다마스    높이 110cm 이내 · 비를 맞으면 안 되는 짐 (밀폐형 밴 · 160×110×110cm)
  라보      높이 160cm 까지 · 길거나 높은 짐 (개방형 · 190×130×160cm)
  파렛트    🔴 **둘 다 안 받는다** — 표준 파렛트(1,100×1,100)가 다마스 폭에 안 들어가고
            라보는 폭은 되지만 파렛트 무게까지 더하면 500kg 를 넘긴다. **1톤부터**다
```
🔴 **「다마스가 더 작은 차」로 요약하지 말 것** — 무게 한도가 같아서 그 요약은 틀린다.
접수에서 갈라야 하는 것은 **높이와 비 맞음 여부**다.

⚠️ 「미정」에 있던 **약 400~550kg / 약 500~600kg**(모델별 추정치)는 이 확정으로
대체됐다 — `HANDOFF` §6 「미정」에서 뺐고 🟢 해소 줄을 더했다.

**═══ ④ 🔴 다마스·라보는 화주포털에서 화주가 직접 고른다 ═══**

`HANDOFF` §5-1 의 **「🔴 내부 전용이라 공개 폼에는 넣지 않습니다」** 한 줄을
세 갈래로 바꿨다(사용자 확정 2026-09-11).
```
  화주포털 발주요청 /customer/request   ✅ 화주가 직접 고른다
  공개 폼 /quote · /apply               ❌ VEHICLE_TYPES_PUBLIC 에 없다
  랜딩 「차량 · 요금 가이드」            ❌ PUBLISHED_START_PRICE_TONS 에 없다
```
🔴 **「내부 전용」이라는 말을 남기지 말 것** — 그 표현을 근거로 다음 세션이 포털에서
빼면 사용자 확정과 정반대가 된다. **지금 동작이 확정된 동작이고 코드는 0줄 바뀌었다**
(문서가 따라잡은 것뿐이다). §5-3 범위 표에도 「사용자 확정 2026-09-11」을 붙였다.

**═══ ⑤ 나머지 셋 ═══**
```
  §5-1 행 수   총 165행 → **195행 (13차급 × 15구간)**
               차급만 13종으로 고치고 행 수를 빠뜨려 **같은 표 안에서 산술이 안 맞았다**
  §8 횟수      착수 전 확인 「열 번」 → **열한 번** (+ PR #144 — 지시서 전제 다섯 개)
  §6 미정      적재 한도 제거 + 🟢 해소 줄
```

**═══ ⑥ ⚠️ 다음 세션이 알아야 할 것 ═══**
```
  🔴 값이 갈리면 DB 부터   적용이 끝난 마이그레이션 SQL 은 「그때 넣은 값」이다.
                          화면에서 바꿀 수 있는 값(요율·설정)은 반드시 verify 로 잴 것
  🔴 고치면 사유 한 줄     「왜 갈렸는지」가 없으면 다음 세션이 옛 값을 근거로 되돌린다.
                          이번에 세 자리 전부에 사유를 붙였다
  🔴 아카이브는 읽기만     grep 하면 옛 값이 걸리지만 그건 기록이다(md5 대조 근거)
  ⚠️ 31~250km 35%         여전히 직접 비교 쌍 0쌍인 보간값이고 가장 자주 걸린다 —
                          담당자 확인이 아직 필요하다(PR #136 부터의 미결)
```
