> 🔴 이 파일은 `CLAUDE.md` 에서 옮겨온 세션 기록입니다. **요약하거나 줄이지 마십시오** —
> 되돌리면 안 되는 결정의 근거가 여기에만 있습니다.
> 담는 범위: 32차 #137 2026-09-09  →  35차 착수 전 입력 정리 2026-09-11   (12건)
> 입구는 `HANDOFF.md`, 규칙과 로드맵은 `CLAUDE.md` 입니다.
---

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 35차 착수 전 입력 정리** — 🔴 **코드 0 · DB 0 · 화면 0.**
`CLAUDE.md`·`HANDOFF.md` 둘만. 사용자가 배포본을 쓰면서 파악한 수정 항목 **13건**을
받아 적고, 그중 전제가 틀릴 만한 것을 코드에서 확인했다.

**═══ ① merge 할 것은 없었다 ═══**

사용자 지시가 *"앞서 마무리된 내용을 merge하고 문서를 업데이트"* 였는데 **열린 PR 0건 ·
작업트리 깨끗 · `main` 이 이미 `1837ca3`** 이었다. PR #145 가 마지막이고 그 뒤 문서
커밋 둘(`a24fb50` · `1837ca3`)까지 끝나 있었다. 🔴 **없는 것을 만들어내지 않고 그대로
보고했고**, 대신 전달된 목록을 기록하는 것으로 이 차수의 값어치를 만들었다.

**═══ ② 🔴 사용자 원문 13건은 `CLAUDE.md` §5 에 그대로 있다 ═══**

여기에 다시 옮기지 않는다(두 곳에 적으면 갈린다). **지시서를 쓸 세션은 그 블록을
읽을 것.** 🔴 **원문을 요약·정리하지 말 것** — 지시서가 범위를 정할 근거다.

갈래만: 운송오더 **6** · 배차관리 **1** · 정산관리 **4** · 운영대시보드 **2**.

**═══ ③ 🔴 조사 — 7번(정산확정이 안 된다)은 날짜 문제가 아닐 가능성이 크다 ═══**

사용자가 *"지나간 날짜에 운임료 정보 수정을 해서 그런가?"* 로 의심했는데, 코드를 보면
**날짜와 무관한 구조적 이유**가 먼저 걸린다.

```
  차주지급액이 안 보인다   invoices 금액은 정산 건 **생성 시점 스냅샷**이다(원칙 47번).
                          신규 정산 등록이 배차 값을 `insert` 로 복사하고, 그 뒤 배차에서
                          운임을 고쳐도 **따라오지 않는다.**
                          → 정산 건이 배차 운임을 적기 전에 먼저 만들어졌을 가능성
                          🔴 그렇다면 「버그」가 아니라 **갱신 경로가 없는 것**이고,
                             원칙 47번을 건드리는 **설계 결정**이 된다

  정산확정이 안 된다      막는 자리가 따로 둘이다(invoices/[id] handleConfirmSettlement) —
                          선착불에서 ① 수수료 > 0 인데 입금완료 미체크
                                      ② 수수료 = 0 인데 지급자가 「면제」가 아님
                          그리고 **확정 버튼 자체가 admin 롤에게만 보인다**
```
🔴 **셋 중 무엇인지 먼저 가릴 것** — 답에 따라 35차 범위인지 별건 수정인지가 갈린다.
⚠️ 그래서 `HANDOFF` §7 「미결·위험」에도 한 줄 올렸다(운영에서 지금 아픈 항목이라).

**═══ ④ 🔴 조사 — 10번(수수료 지급자 항목 불필요)은 「화면에서 빼기」로 안 끝난다 ═══**

`brokerage_fee_payer` 는 `dispatches`·`invoices` **컬럼**이고 화면 셋이 쓴다(배차 상세 ·
정산 상세 · 정산 목록 계산). 🔴 **값 중 `waived`(면제)가 ③ 의 정산확정 게이트에 실제로
쓰이고 있다** — 지우면 그 게이트가 같이 무너진다.
**화면에서 감추는 것 / 컬럼·게이트까지 없애는 것은 다른 작업이고, 지시서가 정해야 한다.**

**═══ ⑤ 🟢 조사 — 27차 확정과 충돌하지 않는다 (5·6·9번) ═══**

「선착불이면 견적서에 입금 계좌를 그리지 않는다」(27차 5라운드 확답 — *"주선수수료는
화주가 주는 경우는 없다"*)는 **그대로 유효**하다. 다만 **부가세 포함/별도 축이 하나 더
생기므로** 이미 있는 `customer_charge_vat_included` 와 어떻게 맞물리는지를 지시서가
짚어야 한다(새 컬럼을 또 만들면 원칙 45번의 「두 값이 서로를 덮어쓰는」 자리가 된다).

**═══ ⑥ 🔴 조사 — 대시보드(12·13번)는 「마진이 둘」인 것이 핵심이다 ═══**
```
  단순마진   dispatches.margin — DB **생성 컬럼**(customer_charge − driver_payout).
             직접 UPDATE 안 된다
  실질마진   lib/settlementCalc.ts — 공급가액 기준 + 주선사부담 산재보험료 반영
```
🔴 **어느 쪽으로 통계를 낼지 지시서가 정해야 한다 — 섞으면 숫자가 안 맞는다.**
⚠️ 포털 월별통계를 참고하더라도 **`--pv2-*` 33개는 `.portal-v2` 스코프 안에만 있다**
(28차 조사 §7, PR #145 에서 다시 실측). 끌어오려면 `.pv2-tokens` 를 **추가**해야 하고,
**옮기면 `globals.css` 삭제 줄이 생긴다**(24차부터의 무회귀 근거가 깨진다).

**═══ ⑦ 안 한 것 ═══**
```
  🔴 코드·DB 를 한 줄도 안 고쳤다 — 지시서를 받기로 한 것이 사용자 결정이다
  🔴 7번을 실제로 재현해 보지 않았다 — 운영 데이터가 필요하고, 어느 정산 건인지
     모른다. **지시서 착수 전 확인에 「그 건의 invoice created_at 과 배차 updated_at
     을 비교」를 넣으면 ③ 의 가설이 바로 판가름난다**
  ⚠️ 13건 중 코드·DB 를 안 건드리고 되는 것은 **하나도 없다** — 전부 35차(또는 분할) 범위다
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 혼적 31~250km 할인율 확정** — 🔴 **코드 0 · DB 0 · 화면 0.**
`CLAUDE.md`·`HANDOFF.md` 둘만. PR #136 부터 열려 있던 미결 하나를 닫았다.

**═══ ① 사용자 확정 — (A) 현행 유지 ═══**

바로 앞 차수(문서 정합 정리 2차)가 「담당자 확인이 여전히 필요하다」로 남긴 항목을
사용자에게 셋 중 하나로 물었고 **(A) 를 골랐다**(2026-09-11).
```
  (A) 35% 가 대충 맞다 → 아무것도 안 한다. 실거래가 쌓이면 그때 다시 본다   ✅ 확정
  (B) 다른 숫자가 맞다 → 한 줄
  (C) 거리로 나누는 것 자체가 어색하다 → 별도 차수
```

🔴 **「검증됐다」가 아니다.** (A) 는 **「지금은 안 바꾸고 실거래를 모은다」**는 뜻이고,
값의 근거는 **여전히 0쌍 보간값 그대로**다. 다음 세션이 이 확정을 근거로
「35% 는 확인된 값」이라고 적으면 안 된다 — 그러면 나중에 실측이 나왔을 때
그것을 근거 있는 값과 비교하는 셈이 된다.

**═══ ② 근거 상태 (안 바뀌었다) ═══**
```
  ~30km       2쌍   서울 0.85 · 경기 0.88            → 지금 DB 20%
  31~250km    0쌍   🔴 보간값(위아래 중간)           → 지금 DB 35%   ← 이 항목
  251km 이상  5쌍   0.33~0.67 · 중앙 0.46            → 지금 DB 55%
```
⚠️ **국내 화물의 대부분이 가운데 구간이다** — 근거가 가장 약한 값이 가장 자주 쓰인다.
🟢 다만 **이것은 계산식이 아니라 견적 입력칸의 기본값**이고 담당자가 건별로 고치면
그 값이 이긴다. 그래서 즉시 사고가 나는 구조가 아니고, (A) 가 성립한다.

**═══ ③ 🔴 언제 다시 보는가 ═══**
```
  🔴 신호 1   31~250km 구간에서 혼적 실적이 쌓여 **같은 구간 독차 건과 비교 쌍이 생겼을 때**
  🔴 신호 2   담당자가 그 기본값을 **매번 손으로 고치고 있을 때** — 기본값이 안 맞는다는 뜻이다
```
🔴 **바꾸는 자리는 `/admin/rates` → 「가산기준」 탭 → 「표준 혼적 할인율」이다**
(마이그레이션이 아니다 — 화면에서 고치는 값이다).
⚠️ **화면에서 고치면 저장소 문서에는 안 남는다** — 30km 가 실제로 그렇게 15 → 20 이
됐고, 그 때문에 문서와 DB 가 갈려 앞 차수가 그것을 잡는 데 매달렸다.
🔴 **바꾸면 그 사실을 문서에 남길 것.**

**═══ ④ 안 한 것 ═══**
- **값 변경 0** — (A) 라서 DB 도 코드도 손대지 않았다. `_migrations` **28행 그대로**.
- **근거 보강 0** — 비교 쌍을 만들 실적이 아직 없다. 만들려면 그 구간의 혼적 완료
  건이 필요하고, 그것은 운영이 쌓아야 하는 것이지 코드가 만들 수 있는 것이 아니다.

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 문서 정합 정리 2차** — 🔴 **코드 0 · DB 0 · `migrations/` 0 ·
`docs/history/` 의 옛 기록 0.** 고친 것은 `CLAUDE.md`·`HANDOFF.md` 둘뿐이고
이 기록만 새로 쌓았다. PR 없이 `main` 직접(문서만 바뀌는 커밋 규칙).

**═══ ① 🔴 혼적 30km 구간이 두 문서에서 갈려 있었다 — DB 는 20% 다 ═══**

이 차수가 급했던 이유다. 혼적 할인율은 담당자가 **매일 보는 입력창 기본값**이라
어느 문서를 보느냐로 견적이 달라진다.

```
  HANDOFF §5-1        30km 이내 15%      ← 낡았다
  CLAUDE.md §2·PR#136 15 / 35 / 55       ← 낡았다
  CLAUDE.md PR#142 요약  20 / 35 / 55    ← 맞았다
  ─────────────────────────────────────
  DB 실측 (verify ⑩-e)  20 / 35 / 55     🔴 정본
```

🔴 **왜 갈렸는지까지 짚었다** — PR #136 마이그레이션은 **15 로 넣었고 단언까지
15/35/55 로 걸어 뒀다**(파일 61~63행 · 124행). 그런데 DB 세 행의 `updated_at` 이
`2026-09-09 03:10:47.965` 로 **모두 같고** `updated_by` 가 직원 계정이다.
`/api/admin/mixed-loading-discount-settings` 가 세 행을 **한 `now` 로 묶어
UPDATE** 하므로, 이것은 **담당자가 `/admin/rates` 에서 세 칸을 한 번에 저장하며
30km 만 20 으로 올린 흔적**이다. 마이그레이션 insert 흔적이 아니다.

🔴 **그래서 마이그레이션 파일의 15 를 근거로 되돌리지 말 것.** 적용이 끝난 SQL 은
「그때 넣은 값」이고 그 뒤 화면에서 바뀔 수 있다 — **DB 가 정본이다.**
🔴 **`migrations/2026-09-09_mixed_discount_tiers.sql` 은 한 글자도 안 고쳤다**
(값을 바꾸려면 새 파일이다).

⚠️ **PR #136 로드맵 기록의 「15 / 35 / 55」는 지우지 않고 남겼다** — 그 차수가
실제로 넣은 값이라 지우면 이력이 틀려진다. 대신 **「지금 DB 는 20/35/55 다 ·
이 줄의 15 를 현재 값으로 읽지 말 것」**을 덧붙였다.

**═══ ② 전수 grep 결과 — 아카이브는 안 고쳤다 ═══**
```
  루트 두 문서   4군데   CLAUDE.md 3(§2 · PR#136 기록 · PR#142 요약) · HANDOFF 1(§5-1)
                        → 셋을 고쳤고 PR#142 요약은 원래 맞아서 근거만 덧붙였다
  docs/history/  2군데   current.md(PR#136 기록) · history-08.md
                        → 🔴 **안 고쳤다.** 틀린 것이 아니라 「그때 그렇게 적었다」는
                          기록이고, 고치면 PR #134 의 md5 대조 근거가 무효가 된다
  migrations/    1파일   값 3곳 + 단언 1곳 → 🔴 **안 고쳤다**(적용이 끝난 SQL)
  HANDOFF §6     0군데   PR #136 행에는 값이 안 적혀 있었다
```

**═══ ③ 🔴 다마스·라보 적재 한도 — 무게가 아니라 부피·형태로 갈린다 ═══**

사용자 확정(2026-09-11). `HANDOFF` §5-1 차급 칸에 넣었다.
```
  무게      둘 다 500kg 이내          ← 🔴 **여기서는 둘이 같다**
  다마스    높이 110cm 이내 · 비를 맞으면 안 되는 짐 (밀폐형 밴 · 160×110×110cm)
  라보      높이 160cm 까지 · 길거나 높은 짐 (개방형 · 190×130×160cm)
  파렛트    🔴 **둘 다 안 받는다** — 표준 파렛트(1,100×1,100)가 다마스 폭에 안 들어가고
            라보는 폭은 되지만 파렛트 무게까지 더하면 500kg 를 넘긴다. **1톤부터**다
```
🔴 **「다마스가 더 작은 차」로 요약하지 말 것** — 무게 한도가 같아서 그 요약은 틀린다.
접수에서 갈라야 하는 것은 **높이와 비 맞음 여부**다.

⚠️ 「미정」에 있던 **약 400~550kg / 약 500~600kg**(모델별 추정치)는 이 확정으로
대체됐다 — `HANDOFF` §6 「미정」에서 뺐고 🟢 해소 줄을 더했다.

**═══ ④ 🔴 다마스·라보는 화주포털에서 화주가 직접 고른다 ═══**

`HANDOFF` §5-1 의 **「🔴 내부 전용이라 공개 폼에는 넣지 않습니다」** 한 줄을
세 갈래로 바꿨다(사용자 확정 2026-09-11).
```
  화주포털 발주요청 /customer/request   ✅ 화주가 직접 고른다
  공개 폼 /quote · /apply               ❌ VEHICLE_TYPES_PUBLIC 에 없다
  랜딩 「차량 · 요금 가이드」            ❌ PUBLISHED_START_PRICE_TONS 에 없다
```
🔴 **「내부 전용」이라는 말을 남기지 말 것** — 그 표현을 근거로 다음 세션이 포털에서
빼면 사용자 확정과 정반대가 된다. **지금 동작이 확정된 동작이고 코드는 0줄 바뀌었다**
(문서가 따라잡은 것뿐이다). §5-3 범위 표에도 「사용자 확정 2026-09-11」을 붙였다.

**═══ ⑤ 나머지 셋 ═══**
```
  §5-1 행 수   총 165행 → **195행 (13차급 × 15구간)**
               차급만 13종으로 고치고 행 수를 빠뜨려 **같은 표 안에서 산술이 안 맞았다**
  §8 횟수      착수 전 확인 「열 번」 → **열한 번** (+ PR #144 — 지시서 전제 다섯 개)
  §6 미정      적재 한도 제거 + 🟢 해소 줄
```

**═══ ⑥ ⚠️ 다음 세션이 알아야 할 것 ═══**
```
  🔴 값이 갈리면 DB 부터   적용이 끝난 마이그레이션 SQL 은 「그때 넣은 값」이다.
                          화면에서 바꿀 수 있는 값(요율·설정)은 반드시 verify 로 잴 것
  🔴 고치면 사유 한 줄     「왜 갈렸는지」가 없으면 다음 세션이 옛 값을 근거로 되돌린다.
                          이번에 세 자리 전부에 사유를 붙였다
  🔴 아카이브는 읽기만     grep 하면 옛 값이 걸리지만 그건 기록이다(md5 대조 근거)
  ⚠️ 31~250km 35%         여전히 직접 비교 쌍 0쌍인 보간값이고 가장 자주 걸린다 —
                          담당자 확인이 아직 필요하다(PR #136 부터의 미결)
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 견적 폼 블록 번호 배지 옐로** — **PR #145 merge됨**(squash `b0c3f2d`).
🟢 **DB 0** · `_migrations` **28행 그대로** · 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 ·
`globals.css` **0줄** · `lib/legal/` 0. 파일 **1개**(`app/admin/quotes/page.tsx`).
실사용 리뷰 라운드 0. **+14 / −4.**

**═══ ① 이것은 PR #143 이 답을 못 받아 남겨둔 한 줄이다 ═══**

바로 앞 차수(견적관리 폼 시안 정합)가 블록 번호 배지를 **파랑**(`var(--accent)`)으로
남겨두고 *"바꾸는 것은 한 줄이다"* 라고 적어뒀다. 그 줄에 답이 왔다 —

> 사용자 지시(2026-09-11) — *"배지 색도 화주포털처럼 옐로우로 바꿔줘."*

🔴 **그래서 CLAUDE.md·HANDOFF.md·`current.md` 의 「답을 못 받아 파랑으로 남았다」는
전부 낡았다.** 이 차수에 같이 고쳤지만, 옛 커밋에서 그 문장을 보면 **PR #143 시점의
글**이라는 것을 기억할 것.

**═══ ② 바꾼 것은 배경 한 값뿐이다 ═══**
```
  app/admin/quotes/page.tsx   블록 머리 배지 4개
     ① 운송 구간 · 현장 정보   ② 일정   ③ 화물 · 차량   ④ 요청사항

  background:  var(--accent)  →  var(--brand-yellow)
  color:       #1a1a1a        그대로 (원래부터 포털 `--pv2-text` 와 같은 값이었다)
  크기:        22 × 22         그대로
```
🟢 **글자색을 안 건드려도 된 것이 이 작업이 한 줄인 이유다** — 43차가 배지를 만들 때
포털 규격(`.pv2-step-num`)을 그대로 베껴 쓰면서 색만 관리자 강조색으로 뒀기 때문에,
배경만 맞추면 포털과 완전히 같아진다.

**═══ ③ 🔴 `var(--pv2-yellow)` 를 쓰지 말 것 — 실측으로 확인했다 ═══**

포털 배지는 `--pv2-yellow`(#ffd833)를 쓴다. 값이 같으니 그것을 쓰면 될 것 같지만
**관리자 화면에서는 옐로가 나오지 않는다.** `--pv2-*` 33개는 `.portal-v2` 스코프
**안에만** 선언돼 있어서(28차 조사 그대로) 관리자 화면에서는 그 변수가 없는 것과 같다.

```
  실측(브라우저, 관리자 화면 안에서)
     var(--brand-yellow)                    → rgb(255, 216, 51)   ✅ 옐로
     var(--pv2-yellow)  (.portal-v2 안)      → rgb(255, 216, 51)   ✅ 같은 값
     var(--pv2-yellow)  (관리자 스코프)       → rgb(25, 31, 40)     ❌ 옐로가 아니다
```
⚠️ **마지막 줄이 「앞 선언으로 되돌아간 값」이 아니다.** 무효한 `var()` 는 계산값 시점에
무효가 되어 **상속값**이 된다(그래서 글자색 `--text` 가 나왔다). 검증 스크립트에서 내가
「앞 선언이 남는다」로 단언했다가 거짓 ❌ 를 한 번 냈다 — 🔴 **어느 쪽이든 요점은
「옐로가 아니다」이고, 그것이 이 토큰을 쓰면 안 되는 근거다.**

🔴 **`--pv2-*` 를 관리자에서 쓰고 싶으면 `.pv2-tokens` 를 따로 **추가**하는 것이지
(28차 조사 §7), 이 한 줄 때문에 그것을 하지 않았다.**

**═══ ④ 🔴 색은 토큰으로 쓴다 — `#FFD833` 을 다시 적지 말 것 ═══**

34차가 계산창 손잡이에서 정한 규칙과 같다. `--brand-yellow` 는 `:root` 선언이라
관리자·공개 화면 어디서나 해석되고, 랜딩 헤더·히어로 CTA 와 **같은 노랑**임이
이름으로 드러난다. 리터럴로 적으면 브랜드 노랑이 바뀔 때 이 배지만 남는다.

⚠️ **옐로 배경 위 글자는 `--text` 계열만** — `globals.css` 머리 주석이 이미
*"옐로(#ffd833) 위에 `--text-muted`(#5f6b78)를 올리지 말 것"* 이라고 못박고 있다.
지금 `#1a1a1a` 는 그 규칙 안이다.

**═══ ⑤ 검증 ═══**

`/admin/*` 은 middleware 가 막으므로 **임시 라우트 `app/preview37-quotes/`** 를 만들어
견적 페이지를 re-export 하고, 43차가 쓰던 목 서버(`mock34b.js`, 포트 4466)를 그대로
재사용해 dev 서버(3196)를 띄웠다. 🔴 **커밋 전에 임시 라우트와 `.next/types` 를
지웠다**(PR #144 가 `app/pgcheck/*` 에서 한 것과 같다).

```
  ✅ 블록 번호 배지 4개 · 순서 1 2 3 4
  ✅ 배경 네 개 전부 rgb(255, 216, 51)
  ✅ 글자색 네 개 전부 rgb(26, 26, 26)
  ✅ 크기 22×22 유지
  ✅ 파일 안 var(--accent) 잔여 0 · 콘솔 에러 0
  ✅ --brand-yellow == --pv2-yellow(.portal-v2)
  ✅ tsc 0건 · 프리렌더 실패 44 (기준선과 diff 0 — 항목까지 동일)
```
⚠️ **화주포털 `.pv2-step-num` 을 화면에서 직접 재지는 못했다** — `/customer/request` 는
로그인이 필요해 목 환경에서 안 뜬다. 대신 **두 토큰의 계산값을 같은 페이지에서
나란히 재서** 같은 값임을 확인했다(값 대조가 목적이라 이것으로 충분하다).

**═══ ⑥ 일부러 안 한 것 ═══**
```
  🔴 같은 화면의 다른 파랑은 그대로 두었다 — 「기존 화주 / 개인·신규 고객」 탭 버튼,
     라디오, 자동계산 결과 카드 등은 여전히 var(--accent) 다. 지시가 **배지**였고,
     관리자 전체 강조색을 바꾸는 것은 전혀 다른 크기의 작업이다
  🔴 다른 관리자 화면의 번호 배지는 손대지 않았다 — 견적 폼 외에 같은 모양의 배지를
     쓰는 화면이 없다(이 배지는 34차가 이 폼에만 만든 것이다)
  ⚠️ `.pv2-tokens` 신설(28차 조사 §7) — 이 한 줄에는 과한 작업이라 안 했다.
     관리자가 포털 부품을 여러 개 끌어와야 할 때가 오면 그때 할 것
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-11, Claude Code 세션에서 직접 갱신 —
**차수 없음: 화주 목록 페이지 나누기 + 모바일 카드** — **PR #144 merge됨**(squash `143e55d`).
🟢 **DB 0** · `_migrations` **28행 그대로** · 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 ·
`lib/legal/` 0. 파일 **6개**. 실사용 리뷰 **2라운드**.

**═══ ① 🔴 지시서 전제 다섯 개가 틀렸다 — 3번이 「멈추고 보고」 조건이었다 ═══**

지시서(「화주목록 페이지네이션」)는 저장소를 안 보고 쓴 것이라 §1 에 착수 전 확인 9건을
달아 두었고, 실측에서 **다섯 개가 달랐다.**

```
  2  `/admin/crm`                → 🔴 **그 경로가 없다.** 활성 화주는 `/admin/customers`
  3  필터가 서버인가              → 🔴 **두 화면 다 100% 클라이언트** ← 「보고 후 대기」 조건
  4  검색                         → 있고, 클라이언트
  5  정렬                         → 있고, 클라이언트(companies 6키)
  6  표 + 모바일 카드 별개 JSX    → 🔴 **두 화면 다 모바일 카드가 없었다**
```

🔴 **3번에서 실제로 멈추고 보고했고, 사용자가 (C)안 + 모바일 카드로 확정했다**
(2026-09-11). 「보고 후 대기」가 적힌 항목은 **진짜로 멈출 것** — 이번엔 그게 설계를
통째로 바꿨다.

**═══ ② 🔴 왜 서버 전환이 아니라 클라이언트 나누기인가 — 되돌리지 말 것 ═══**

정렬 키 6개 중 **셋이 DB 컬럼이 아니라 JS 계산값**이다.

```
  지역   formatRegion()   = metro_region + district → metro_region → district → region
  업종   formatIndustry() = 같은 3단 폴백
  상태   STATUS_OPTIONS.indexOf(status)   ← 배열 순서지 가나다순이 아니다
```

PostgREST 의 `.order()` 는 **컬럼 이름만** 받는다. 이 셋을 서버에서 정렬하려면 생성
컬럼이나 뷰를 만들어야 하고 그건 지시서가 못박은 「DB 변경 없음」과 부딪힌다. 검색도
`formatRegion(c).includes(q)` 라 `ilike` 하나로 옮기면 **결과가 달라진다**(하지 말 것 2번).

🟢 **그래도 느림은 사라진다** — 조회 응답은 수십 KB인데 느린 것은 그 **539행을 DOM 에
그리는 것**이었다(행마다 드롭다운·배지·버튼). 그리는 수를 줄이면 그 비용이 없어진다.
⚠️ 그래서 **완료조건 2(페이지마다 새 네트워크 요청)만 설계상 불충족**이다 — 요청이 안
나가는 것이 목적이다.

**═══ ③ 🔴 실측이 계획 하나를 지웠다 — 재고 나서 줄인 첫 사례 ═══**

착수 전에 나는 `/admin/customers` 의 `dispatches` 전수조회(`limit` 없음)를
「페이지네이션으로도 안 줄어드는 무게」로 보고 (C)안에 고칠 계획을 넣었다. `_verify.sql`
에 ⑮ 를 더해 재 봤더니:

```
  전체화주 539  ·  활성화주 7
  배차_전체 6   ·  오더_전체 10  ·  포털계정_활성 2
```

**배차가 6행**이라 무게가 아니다 — **안 고쳤고, 그 사실을 사용자에게 바로 보고했다.**
🔴 「무거워 보인다」로 착수하지 말 것. `verify` 모드가 있으니 **재고 시작하면 된다.**
🔴 그 조회에 `limit` 이 없는 것은 사실이니 배차가 수천 건이 되면 그때 다시 볼 것.

같은 실측이 `/admin/customers` 를 **7건**으로 밝혔다 — 지금 이 화면은 페이지가 하나다.
그래도 같은 훅을 붙인 것은 활성 화주가 늘어나는 쪽이고, 한 페이지면 번호 줄을 안 그려서
**보이는 변화가 건수 줄 하나**뿐이라 비용이 없기 때문이다.

**═══ ④ 공용 부품 둘 — 새 목록 화면은 이걸 쓸 것 ═══**

`lib/useListPagination.ts` · `components/ListPagination.tsx`(원칙 43번
`useListSearchSort` 와 같은 결이다. **화면마다 새로 만들지 말 것**).

- 🔴 **걸러지고 정렬까지 끝난 배열**을 받아서 자른다. 조회를 나누지 않는다
- 🔴 `resetKey` 에 **필터·검색·정렬을 전부** 넣을 것 — 하나라도 빠지면 「3페이지를 보다가
  조건을 바꿨더니 결과는 2건인데 3페이지라 빈 화면」이 된다(완료조건 3)
- 🔴 **구분자를 빈 문자열로 두지 말 것** — 탭 `전체`+검색 `""` 과 탭 `전`+검색 `체` 가
  같은 키가 된다. **`" "`**(검색창에 입력할 수 없는 문자)를 쓴다
- 목록이 줄어 현재 페이지가 사라지면 마지막 페이지로 당긴다(빈 화면 방지)
- 🔴 **로딩 스피너를 만들지 말 것** — 네트워크 요청이 없어 기다림 자체가 없다.
  지시서 2-2 가 막으려던 「빈 화면 깜빡임」은 이 구조에서 애초에 생기지 않는다
- 🔴 번호는 **최대 5개 + 양 끝 고정 + `…`** — 페이지가 36개가 돼도 버튼은 9개 그대로다.
  🔴 **무한 스크롤로 바꾸지 말 것**(지시서 하지 말 것 3번 — 담당자가 「몇 페이지에 있던
  그 화주」로 돌아가야 한다)

**═══ ⑤ 모바일 카드 — 두 화면 다 없었다 ═══**

`AdminMobileList`(34차)는 **견적·오더·배차·정산 네 화면에만** 있었다. 화주 두 화면은
표만 있어서 390px 에 안 들어갔다(companies 8열 · customers `minWidth: 880` 못박힘).

```
  companies 에서 뺀 것   출처 배지(좁은 칸에서 두 줄로 쌓여 카드를 민다) · 등록일
  customers 에서 뺀 것   결제조건 · 미수금 · 등급 · 배차상태 (고른 **뒤에** 보는 값들)
  남긴 것                정기계약 배지 · 영업상태 · CRM 전환 / 🔑 포털계정 · 누적오더 ·
                        거래상태 · 제외
```

🔴 **드롭다운·버튼은 전부 `action` 에 넣었다** — `AdminMobileList` 가 거기에만
`stopPropagation` 을 건다. `lines` 로 옮기면 누르는 순간 상세로 튄다.

**═══ ⑥ 🔴 「폼 열림 시 목록 미렌더」 분기를 없앴다 — 다시 만들지 말 것 ═══**

`companies/page.tsx` 의 `{!showForm && (` 는 539건을 한 번에 그리던 시절의 증상
완화였고(34차 실측 300행 121ms → 감추면 79ms), 주석이 스스로 「페이지네이션되면 없애도
된다」고 적어두고 있었다. 이제 15행이라 감출 이유가 없다.

🔴 **그 분기는 「신규업체등록을 눌렀는데 아무것도 안 뜬다」를 한 번 만들었다**(폼까지 같이
감쌌다. 34차 리뷰 3라운드). 되살리지 말 것.
🟢 `contain: layout style` 은 **남겼다** — 폼 구획 토글 시 아래 목록이 다시 배치되는 것을
막는 것은 행 수와 별개로 유효하다.

**═══ ⑦ 실사용 리뷰 1라운드 — 「50행은 너무 많다」 ═══**

*"한페이지에 50행은 너무 많다. 15개 업체만 한페이지에 보이게 하자."*

`LIST_PAGE_SIZE` **한 줄**이다(값을 한 곳에 모아둔 이유가 이것이다).
🟢 15행이면 탭·검색·정렬·목록·페이지 줄이 **1440×950 한 화면에 스크롤 없이** 들어간다.
⚠️ 11 → **36페이지**가 됐지만 ④ 의 창 구조 덕에 버튼은 9개 그대로다(실측).

**═══ ⑧ 🔴 실사용 리뷰 2라운드 — 「카드 외곽에 너무 붙어 있다」 ═══**

*"목록 창 아래 [539건 중 1–50] 과 번호가 카드의 외곽에 너무 붙어 있어 레이아웃이 좋아
보이지 않는다. 이런부분은 무언가 새로 만들때 반복적으로 레이아웃이 일반적이지 않고
러프하게 붙이는 경우가 있는데 알아서 신경써줘."*

**맞는 지적이고 원인이 명확하다 — 내가 그 자리의 기존 값을 안 재고 붙였다**
(`padding: 12px 4px 2px`).

재 보니 `.card` 는 **패딩이 0**이고 표가 카드를 꽉 채운다(`tbody td` 가 13px 16px).
게다가 `tbody tr:last-child td` 가 아래 테두리를 지운다 — 그래서 이 줄이 마지막 행과
경계 없이 이어지고 좌우로는 카드 모서리에서 4px 떨어진 곳에 얹혀 있었다.

🔴 **고친 방향: 떠 있는 글이 아니라 「카드의 푸터」다.**
- `border-top` 신설 — `tr:last-child` 가 지운 경계를 되살린다. **빼지 말 것**
- 좌우를 **표의 칸 여백과 같게** 맞춰 건수 글자가 첫 칸과 같은 세로선에서 시작한다
- 🔴 **표마다 칸 여백이 다르다** — `tbody td` 16px · `.table-compact td` **8px** ·
  모바일 `.mobile-row-card` 18px. `--lp-pad-x` 하나를 세 규칙이 갈아끼운다
  (padding 을 통째로 다시 선언하면 위아래 값까지 복사되어 한쪽만 고칠 때 갈린다)
- 🔴 `.table-compact` 를 쓰는 화면은 **`<ListPagination compact />`** 로 켤 것 —
  안 켜면 8px 오른쪽에서 시작한다(활성 화주가 그랬다)

```
  실측   화주 관리  줄 왼쪽 211 = 첫 칸 글자 211
        활성 화주  줄 왼쪽 203 = 첫 칸 글자 203
        모바일     줄 18px = 카드 18px
```

🔴 **다음에 새 UI 를 붙일 때는 그 자리의 기존 값(카드 패딩 · 칸 여백 · 행 리듬)을 먼저
재고 거기에 맞출 것.** 사용자가 「반복적으로」라고 한 것은 이것이 처음이 아니라는 뜻이다.

**═══ ⑨ 곁다리 — 여백을 넓히자 모바일 한 줄이 깨졌다 ═══**

좌우가 4 → 18px 이 되면서 쓸 폭이 28px 줄었고, 390px 에서 **정확히 6px 모자라** 「다음」이
아랫줄로 내려갔다. 모바일에서만 `min-width` 28→26 · 버튼 padding 6→5 · nav gap 4→3 으로
조여 **21px 여유**로 한 줄에 넣었다.
⚠️ **360px 은 그대로 두 줄**이다 — 가로 넘침 0이고, 한 줄로 만들려면 누르기 어려운 크기가
된다. 되감기는 것이 맞다.

**═══ ⑩ 검증 — 실제 렌더링 24항목 ═══**

🔴 목은 **실제 DB 만큼 엄격하게** 만들었다(원칙 56번) — 모르는 컬럼으로 정렬하면 **42703**,
`select` 투영 강제, `status=in.(…)` 실제 반영. 데이터도 실측과 같은 **539건 / 활성 7건**.

⚠️ **`/admin/*` 은 middleware 가 막아서** 같은 컴포넌트를 `app/pgcheck/*` 에 re-export 해
그 바깥에서 쟀다(커밋 전 삭제). `.next/types` 에 그 라우트의 타입이 남아 tsc 가 4줄
에러를 내므로 **지운 뒤 `.next/types/app/pgcheck` 도 같이 지울 것.**

```
  최초 DOM 행수 15 · "539건 중 1–15" · 2p "16–30" · 마지막 "526–539"
  번호 버튼 9개(1p · 마지막 p 둘 다) · 필터 후 "135건 중 1–15" 현재 페이지 1
  검색 "가나" → "77건 중 1–15" · 검색어 바꾸면 1페이지
  430·390·360px 가로 넘침 0 · 카드 렌더 · 표 display:none
  폼 열어도 목록 15행 그대로 · 활성 화주 7건은 번호 없이 "7건 중 1–7"
  tsc 0줄 · 프리렌더 실패 44(기준선과 항목까지 동일) · globals.css 순수 추가(+119 / −0)
```

⚠️ **거짓 ❌ 가 두 번** 났고 둘 다 시험 자산 결함이다(함정 3, 열다섯 차수째) —
(1) 목이 활성 상태를 고르게 뿌려 **활성 화주가 360행**이 되는 바람에 「7건이라 페이지가
하나」를 못 쟀다. (2) 「줄 수」를 자식들의 `top` **Set** 으로 셌는데 `…` 가 `<span>` 이라
top 이 달라 **한 줄을 2줄로** 잡았다(PR #142 와 같은 자리다 — **세로로 겹치지 않는 띠**로
셀 것).

**═══ ⑪ 일부러 안 한 것 ═══**

- 🔴 **`dispatches` 전수조회**(③) — 6행이라 무게가 아니다
- 🔴 **정렬·검색의 서버 이전**(②) — 순서·검색 결과가 달라진다
- **페이지당 건수 선택 UI**(25/50/100) · 무한 스크롤 — 지시서 범위 밖
- **`/admin/dashboard` 의 최근 12개월 화주 조회** — 하지 말 것 4번

**차수 없음: 견적관리 폼 시안 정합 (화주포털 발주요청과 같은 결로)** — **PR #143 merge됨**
(squash `5fcb9fa`). 🟢 **DB 0** · `_migrations` **28행 그대로** · 화주포털 0줄 ·
공개 화면 0줄 · 랜딩 0줄 · `lib/legal/` 0. 파일 **2개**(`app/admin/quotes/page.tsx` ·
`app/globals.css`). 실사용 리뷰 **1라운드(지적 3건)**.

**═══ ① 왜 했나 — 34차는 「배치」만 맞췄다 ═══**

사용자 지적: *"내부시스템 견적관리 폼 구성이 아직 마음에 들지 않는다. 최대한 화주포털
발주요청 폼에 맞추자. 글씨의 가독성, 항목 레이아웃 구성 등. 그리고 「회사명 입력」
「거리」의 입력칸이 폭이 너무 넓다. 지금보다 절반 사이즈여도 될 것 같고."*

34차(PR #141)가 맞춘 것은 **네 블록 순서와 출발지/도착지 열 배치**였고, **입력칸의
생김새 자체는 손대지 않았다.** 그래서 배치는 같은데 읽는 느낌이 다른 상태가 남았다.

**═══ ② 🟢 실측이 범위를 절반 이하로 줄였다 ═══**

두 폼을 브라우저에 나란히 띄워 `getComputedStyle` 로 재 봤더니 **이미 거의 같았다.**

```
                  화주포털 발주요청        관리자 견적 폼(전)
  본문 서체        SUIT Variable          SUIT Variable      같음
  높이            38px                   40px               거의 같음
  모서리          12px                   12px               같음
  테두리          1px #ebeae7            1px #e5e8eb        거의 같음
  안쪽여백        10px 11px              11px 13px          거의 같음
  🔴 라벨         14px / 400 / #888378   12.5px / 600 / #5f6b78   ← 다름
  🔴 입력 배경     #ffffff                #f9fafb                  ← 다름
```

🔴 **「폼이 다르다」는 지적을 받으면 먼저 두 화면을 나란히 재라** — 안 재고 착수했으면
포털 부품(`Pv2Select`·`.pv2-*`)을 통째로 끌어오는 큰 작업이 됐을 것이다. 실제로 다른
것은 **라벨과 배경 둘뿐**이었다.

**═══ ③ 🔴 (가) 관리자 CSS 로만 「닮게」 vs (나) 토큰 공유 — (가)로 갔다 ═══**

(나)는 `.portal-v2` 안에만 있는 `--pv2-*` 33개를 `.pv2-tokens` 로 빼서 관리자도 쓰게
하는 안이다(28차 조사가 권장했던 길). 실측 결과 차이가 둘뿐이라 **토큰 공유 없이
해결되고**, (나)는 `globals.css` 의 구조를 건드려 범위가 커진다.

🔴 **그래서 이 차수의 CSS 는 색이 리터럴이다**(`#888378`). 34차가 「포털 부품을 하나도
안 가져왔다」고 적은 것과 같은 이유이고, **값은 포털 실측치를 옮겨 적은 것**이다.
포털 토큰이 바뀌면 이쪽은 따라가지 않는다 — 알고 둔 것이다.

**═══ ④ 🔴 `.quote-form` 스코프 신설 — `.field` 를 전역으로 고치면 안 된다 ═══**

`.field` 는 관리자 **31개 화면**이 공유한다. 그래서 폼 여는 태그를
`className="req-marks quote-form"` 으로 바꾸고 **그 안에서만** 덮어썼다.

```css
.admin-wide .quote-form .field label { font-size: 14px; font-weight: 500; color: #888378; }
.admin-wide .quote-form .field input { background: var(--surface); font-size: 14.5px; }
```

34차의 `.admin-wide`(폭)와 같은 결의 장치다. 🔴 **`.field` 자체를 고치는 유혹을 막는
것이 이 클래스의 존재 이유이니 없애지 말 것.**

**═══ ⑤ 넓던 칸 두 개 — 원인이 서로 달랐다 ═══**

```
  「화주 업체 검색」  910 → 448px   이 칸은 `.form-grid` **밖**이라 폼 전체 폭을 먹고 있었다
                                  → `.quote-form-half`(max-width 448px)
  「거리(km)」        836 → 448px   `gridColumn: "1 / -1"` 이 걸려 있었다 → 제거(2열 한 칸)
```

🔴 **`max-width` 를 `width` 로 바꾸지 말 것** — 좁은 화면에서 칸이 줄어들지 않게 된다.
🔴 **「품목」·「특이사항」의 전체 폭은 일부러 남겼다** — 긴 문장이 들어가는 칸이다.

**═══ ⑥ 실사용 리뷰 1라운드 — 지적 3건 ═══**

*"블록제목 크기하고 간격도 포털에 맞춰줘 / 견적관리 필수항목칸에 옅은 컬러로 강조 표시 /
개인, 신규고객은 고객명은 필수로 두지 말고 연락처만 필수 항목"*

**⑥-1 블록제목** — 포털을 재서 값을 그대로 옮겼다.

```
           포털            관리자(전)          관리자(후)
  배지     22×22 · 13.5px  20×20 · 11.5px     22×22 · 13.5px
  제목     16.5px          13.5px             16.5px
  정렬     gap 10 · center gap 8 · baseline   gap 10 · center
  아래여백 (다음까지 18px)  margin 6 0 -4px    margin 6 0 4px → 18px
```

🔴 **배지 색은 일부러 안 바꿨다** — 포털은 옐로(`#FFD833`)인데 관리자는
`var(--accent)` 파랑이고, **파랑은 관리자 화면 전체의 강조색**이다. 지시가 「크기하고
간격」이었고 이 폼만 옐로로 바꾸면 오히려 어긋난다. 물어봤으나 답 대신 merge 지시가
왔다 — **답을 못 받은 채 파랑으로 남은 것이니, 다음에 「왜 포털과 색이 다르지」가
나오면 이 문단을 볼 것.** 바꾸는 것은 한 줄이다.

**⑥-2 🔴 필수항목 옅은 강조 — 「없던 기능」이 아니라 내가 없앤 것이었다**

강조(`#fffbfa`)는 **원래부터 있던 기능**인데, ④ 에서 넣은 규칙이 그것을 덮어써서
**조용히 흰색이 돼 있었다.** 실측으로 확인했다(필수 칸 배경 = `rgb(255,255,255)`).

```
  .admin-wide .quote-form .field input     (0,0,3,1)  ← 내가 넣은 것
  .req-marks .field-required input         (0,0,2,1)  ← 원래 있던 것
```

🔴 **규칙 순서를 바꿔 고치지 않았다**(원칙 58번) — 다음에 누가 `globals.css` 를
정렬하면 조용히 되돌아간다. `.quote-form.req-marks` **두 클래스로 특성도를 올려서**
(0,0,4,1) 이기게 했다. 폼 태그가 `className="req-marks quote-form"` 이라 둘이 같은
요소다. 🔴 **포커스 시 흰색 복귀 규칙도 같이 올려야 한다** — 한쪽만 올리면 포커스해도
분홍이 남는다.

🔴 **교훈: 스코프 클래스를 새로 만들 때는 그 스코프가 무엇을 덮어쓰는지 먼저 볼 것.**
「이 화면에만 적용된다」는 안전해 보이지만, **특성도가 올라간 만큼 그 화면의 기존
규칙들을 이긴다.** 이번에는 지적을 받아서 알았지, 안 그랬으면 계속 사라진 채였다.

**⑥-3 개인·신규 고객은 고객명이 아니라 연락처가 필수**

별표 · `field-required` 클래스 · 제출 검증을 통째로 맞바꿨다.

🔴 **이름이 빈 값이면 `guest_name` 에 빈 문자열이 아니라 `null` 을 넣는다** —
목록·상세가 `guest_name || …` 로 대체 표기하는 구조라, 빈 문자열이 들어가면 그 대체가
**안 걸려서** 이름 칸이 그냥 빈 채로 보인다(`""` 는 falsy 지만 `.trim()` 을 안 하면
공백만 있는 값이 통과한다 — 그래서 `form.guest_name.trim() || null`).

🔴 **되돌리지 말 것** — 개인 고객은 이름을 안 밝히는 경우가 흔하고, 나중에 다시 연락할
수 있어야 하는 쪽은 연락처다(사용자 확정 2026-09-11).

**═══ ⑦ ⚠️ 검증 함정 — 회귀를 견적 화면에서 재면 안 된다 ═══**

「스코프 밖 `.field` 가 그대로인가」를 재려고 했는데, **견적 폼이 열리면 그 화면에
스코프 밖 `.field` 가 0개**라 비교 대상이 없어 **거짓 ❌** 가 났다. 다른 관리자
화면(차주 관리)에서 재야 한다 — 🔴 **그 화면도 「+ 차주 등록」을 눌러 폼을 열어야
`.field` 가 생긴다.**

⚠️ **목(mock) 가드도 헐거웠다** — `".admin-wide .quote-form" not in s` 로 CSS 존재를
확인했더니 **34차의 `.admin-wide .quote-form-layout` 이 부분문자열로 걸려** 통과했다.
정확한 선택자(`".admin-wide .quote-form .field"`)로 고쳐야 의미가 있다(원칙 56번).

**═══ ⑧ 검증 결과 ═══**

```
  1600·1440·1280·700 네 폭   라벨 14px/#888378 · 입력 배경 흰색 · 화주검색 448px 이하 ·
                             거리 칸 전체폭 아님 · 페이지 가로 넘침 0 · 예외 0
  블록제목                    배지 22×22/13.5px · 제목 16.5px · gap 10 · center ·
                             다음 칸까지 18px (포털과 같은 값)
  필수 강조                   필수 칸 5개 전부 rgb(255,251,250) · 포커스 시 흰색 복귀
  고객명/연락처               고객명 별표 없음·흰 배경 / 연락처 별표 있음·분홍 배경 ·
                             이름만 넣고 제출 → 「개인/신규 고객 연락처를 입력해주세요.」
  🔴 스코프 밖 회귀 0         차주 관리 화면 라벨 12.5px/600/#5f6b78 · 배경 #f9fafb 그대로
  빌드                        tsc 종료코드 0(파일로 받아서) · 프리렌더 실패 44(기준선과
                             항목까지 동일) · globals.css 삭제 줄 0
```

**═══ ⑨ 일부러 안 한 것 ═══**

- 🔴 **배지 색**(⑥-1) — 답을 못 받았다. 파랑으로 남아 있다
- 🔴 **`won()` 함수는 이 파일에서 원래부터 사용처 0** 이었다(`origin/main` 에서도).
  내 변경이 만든 죽은 코드가 아니라 **손대지 않았다**(손댄 파일의 다른 부분 「정리」 금지)
- **포털 부품 도입**(`Pv2Select`·`Pv2DatePicker`) — ③ 의 이유로 안 가져왔다.
  관리자에 네이티브 `select` 가 남는 것은 **원칙 57번대로 정상**이다(그 원칙은 포털 한정)
- **화주 목록 페이지네이션**(540건을 한 번에 그린다) · `/api/customer/update-contact`
  서버 검증 — 34차부터 남아 있는 것이고 이번에도 범위 밖이다

**차수 없음: 다마스·라보 차급 신설 (11차급 → 13차급)** — **PR #142 merge됨**(squash `8c0f251`).
🔴 **DB 변경 있음** — `rate_distance_tiers` **165 → 195행** · `rate_vehicle_extra_fees`
**11 → 13행** · `_migrations` **27 → 28행**. 🟢 화주포털 화면 0 · 공개 화면 0 ·
`globals.css` 0 · 랜딩 0 · `lib/legal/` 0. 실사용 리뷰 **1라운드**.

**═══ ① 왜 넣었나 ═══**

지금은 다마스·라보로 실을 짐도 담당자가 **1톤으로 견적**을 냈다. 근거리 시내에서 두 차급의
실측 지급이 1톤의 **0.63~0.80** 이라, 1톤 값으로 받으면 화주에게 비싸고 차주에게는 그 값으로
가지 않는다 — 그 차이가 통째로 협상으로 넘어간다.

**═══ ② 🔴 값은 표가 정본이다 — 수식으로 재생성하지 말 것 ═══**

화주 청구가(부가세 별도 · 독차 · 주간 · 지게차 상하차), 단위 원

```
  구간         다마스    라보    (참고) 1톤        구간         다마스    라보    1톤
  10km 이내     34,000   35,000     40,000       170km 이내   112,000  118,000  124,000
  20km 이내     45,000   46,000     52,000       200km 이내   122,000  128,000  135,000
  30km 이내     54,000   56,000     62,000       250km 이내   136,000  144,000  155,000
  60km 이내     64,000   67,000     72,000       300km 이내   153,000  162,000  176,000
  90km 이내     75,000   79,000     83,000       350km 이내   186,000  197,000  216,000
 110km 이내     84,000   88,000     93,000       400km 이내   213,000  226,000  248,000
 130km 이내     94,000   99,000    104,000       400km 초과   242,000  256,000  285,000
 150km 이내    103,000  108,000    114,000
```

🔴 **라보 `400km 초과` = 256,000 이다.** `285,000 × 0.90 = 256,500` 이라 반올림 방식에 따라
**256,000 / 257,000** 으로 갈린다 — 마이그레이션 ⑥ 단언이 256,000 을 못박는다.
**`round(one_ton * 0.90, -3)` 같은 SQL 로 생성하면 그 한 칸이 어긋난다.**
⚠️ 계수는 값이 어떻게 나왔는지를 설명할 뿐 **계산식이 아니다**(다마스 0.849~0.904 ·
라보 0.875~0.952 로 구간마다 다르다).
🔴 **10km → 20km 의 +32%/+31% 급등은 정상이다** — 1톤의 같은 칸이 +30%(40,000 → 52,000)라
그대로 물려받은 모양이고 원인은 **1톤 10km 40,000 정책 고정값**이다. 「펴자」고 하지 말 것.

**═══ ③ ⚠️ 두 차급은 「싼 차」가 아니다 — 차이는 운영 규칙으로 넘겼다 ═══**

거리 통제 비교(같은 시도·같은 거리구간·1톤 완료·독차), 1톤 대비

```
  근거리 ~30km    표본 29건   다마스 0.63   라보 0.80   🟢 여기가 두 차급의 진짜 강점
  중거리 31~200km 표본 12건   다마스 1.00   라보 1.14   🔴 1톤과 동가이거나 더 비싸다
  장거리 201km+   표본  2건   다마스 0.77   라보 0.75   🟡 근거 등급 D
```

🔴 **중거리 실측(1.00·1.14)에 맞춰 값을 올리면 차급 역전**이 된다(마이그레이션 ⑤ 단언이 막는다).
근거리 실측(0.63)에 맞춰 내리면 그 구간 배차가 막힌다. 그래서 **1톤 아래로 눌러 잡았고**
차이는 배차 담당자 **운영 규칙**으로 넘겼다 — 🔴 **코드로 만들지 말 것**:

```
  지급 상한      청구가 ÷ 1.15
  🔴 중거리 예외  60~200km 는 1톤 수준 지급을 각오할 것
  🔴 10km 이내    마진이 거의 없거나 음수일 수 있다
                 (실측 지급 중앙 다마스 32,500 · 라보 35,000 vs 상한 29,565 · 30,435)
```

🔴 **`base_fare` 는 화주 청구가다.** 마진율 컬럼·설정 화면을 만들면 그 순간 의미가 뒤집혀
공개 게시가에 원가가 나간다.

**═══ ④ 🔴 착수 전 확인 16항목 — 지시서 전제 둘이 틀렸고 사고 하나를 찾았다 ═══**

| # | 지시서 전제 | 실제 |
|---|---|---|
| 4 | `VEHICLE_TYPES_PUBLIC` 은 6종(또는 11종)이다 | 🔴 **11종이다.** `HANDOFF` §5-3 의 「차급 6종만」이 낡았다. 지시대로 **diff 0줄**로 뒀다 |
| 0-2 근거 | 늘리면 `/apply` 가 `join(", ")` 으로 본문에 25톤을 찍는다 | 🟢 **31차에 그 툴팁을 없앴다** — 근거는 낡았지만 지시는 그대로 따랐다 |
| 10·11 | 가산·산재보험료가 차급별일 수 있다 | 🟢 **둘 다 차급 무관**(컬럼에 `vehicle_type` 이 없다). 혼적도 마찬가지 — 이 차수에 손댈 것이 없었다 |
| 첨부 | 엑셀 `…다마스라보_추가안_v1.xlsx` 로 전수 대조 | 🔴 **전달되지 않았다**(첨부된 xlsx 는 v11 것). 지시서 2-1 표를 정본으로 쓰고 **산술 전수 검사**로 대체했다 |

🔴 **혼적 30km 구간의 실제 DB 값은 20% 다** — `CLAUDE.md` 의 「15/35/55%」가 낡았다.
실측은 **20 / 35 / 55** 이고 목도 그렇게 맞췄다.

**═══ ⑤ 🔴 `DEFAULT_VEHICLE_TYPE` 신설 — 지시서에 없던 것이고, 없으면 바로 사고 ═══**

맨 앞에 넣으면 `VEHICLE_TYPES_ALL[0]` 이 `"1톤"` → `"다마스"` 로 바뀐다. 그 자리를
**기본값으로 쓰던 곳이 일곱 군데**였고 **그중 둘이 화주포털**이다 —
`/customer/request` 발주요청 **희망 톤수 초기값**과 `/customer/locations` 화물 프리셋.
그대로 뒀으면 **톤수를 안 건드린 화주 발주가 전부 다마스로 접수**됐다.

```
  components/CompanyFieldInput.tsx   화주 추천차량 톤수
  app/customer/request/page.tsx      🔴 발주요청 희망 톤수 초기값
  app/customer/locations/page.tsx    🔴 화물 프리셋 (3곳)
  lib/companyFields.ts               화주 등록 기본값 + recommended_vehicle 파싱 fallback (2곳)
```

🔴 **`DEFAULT_VEHICLE_TYPE = "1톤"` 을 `VEHICLE_TYPES_ALL[0]` 로 되돌리지 말 것.**
값이 「1톤」이라 **동작 변화는 0** 이다. 배열은 「무엇이 있는가」의 답이지 「기본이
무엇인가」의 답이 아니다 — 지금까지 우연히 같았을 뿐이다.
⚠️ 완료조건 29(화주포털 0)를 두 파일에서 어긴 것이고, **안 고치는 쪽이 화주포털을 실제로
망가뜨린다**. 사용자가 *"기본설정은 기존처럼 1톤에 맞춰지게"* 로 확정했다(2026-09-11).

**═══ ⑥ 🔴 두 배열의 값이 처음으로 갈렸다 ═══**

```
  VEHICLE_TYPES_ALL      관리자·견적·운임기준표 · 화주포털   13종  ← 다마스·라보가 맨 앞
  VEHICLE_TYPES_PUBLIC   공개 폼 /quote · /apply             11종  ← diff 0줄
```

🔴 **같은 값으로 맞추지 말 것.** 46차가 두 배열을 나눈 이유가 실현된 **첫 사례**다.
`lib/constants.ts` 의 「차급을 더 추가할 때 고칠 곳」 2번이 「`PUBLIC` 도 같이 늘릴 것」이라고
말하고 있어서, **그 자리에 이번 예외와 `DEFAULT_VEHICLE_TYPE`(2-b)을 적어 뒀다.**
🔴 **배열 끝에 붙이지 말 것** — 배열 순서가 곧 `/admin/rates` 열 순서이자 드롭다운 순서다.

🟢 **화주포털 노출은 사용자 확정이다**(2026-09-11 — *"화주포털 발주요청에도 노출하는데,
기본설정은 기존처럼 1톤에 맞춰지게, 선택 클릭시 드롭다운에 다마스, 라보는 1톤 위에 배치"*).
실측으로 셋 다 확인했다 — 기본 「1톤」 · 순서 다마스 → 라보 → 1톤 → … → 25톤 · 13종.

**═══ ⑦ 마이그레이션 ═══**

`migrations/2026-09-11_rate_damas_labo.sql` — 운임 30행 + 대기료 2행을 **한 파일**에.
🔴 **쪼개지 말 것** — 대기료를 빠뜨리면 그 차급 견적에서 **조용히 0원**이 된다(16차).

```
  ① 재실행 방지   다마스/라보 0건 · 165행 · extra_fees 11행 · 1톤 첫·끝 칸이 v11 값인가
  ④ 구조         195행 · 13차급 · 15구간 · null 0 · 천원단위 · 400km 초과 상한 null
                 + 🔴 신설 30행의 `distance_from_km` 이 기존과 같은 규칙인가
  ⑤ 역전         차급 역전 0 · 거리 역전 0 + 🔴 **ord 목록에 없는 차급이 있으면 멈춘다**
  ⑥ 표본         라보 400km 초과 = 256,000 + 🔴 **게시 6칸 대조로 기존 165행 무변경 확인**
```

🔴 **`rate_distance_tiers` 에는 유니크 제약이 없다**(실측: `PRIMARY KEY (id)` 뿐) — 두 번
돌면 중복 INSERT 라 ①이 막는다. 🟢 반면 `rate_vehicle_extra_fees` 는 **UNIQUE (vehicle_type)**
이 있다(이번에 `_verify.sql` ⑭-a 를 새로 만들어 확인했다 — ⑩-g 가 그 표를 빠뜨리고 있었다).
🔴 **⑤ 의 `ord` 목록을 차급 추가 때마다 갱신할 것** — 안 하면 새 차급이 검사에서 **조용히**
빠진다. 그래서 「목록에 없는 차급이 있으면 멈추는」 단언을 이번에 새로 넣었다.
🟢 **되돌리기 SQL 은 파일 맨 아래 주석**에 있다. 🔴 되돌리면 `VEHICLE_TYPES_ALL` 도 11종으로
같이 되돌려야 한다(배열에만 남으면 그 차급을 고르는 순간 계산 패널이 통째로 사라진다).
🟢 **기존 165행은 안 건드리므로 v11 공개 게시가는 되돌리기와 무관**하다.

**═══ ⑧ 실사용 리뷰 1라운드 — 운임기준표 줄바꿈 ═══**

지적: *"「거리구간」에서 「이내」가 아래줄로 내려가지 않게(한줄 구성), 금액에서 「원」이
아래줄로 내려가지 않게. 폭이 좁으면 「원」 글씨를 더 작게 가거나 아예 없어도 된다."*

```
  고치기 전   거리구간 **15구간 전부** 2줄 · 금액 **154칸**(1440px) / **180칸**(1280px) 2줄
              차급 머리(「5톤 플러스/축」 포함)는 **0개** — 신고 범위가 정확했다
  고친 뒤     1920·1440·1280·390 네 폭에서 **전부 0**
```

원인은 13차급이 되면서 거리구간 칸이 **99px**, 금액 칸이 **92px** 로 좁아진 것.
고친 것은 거리구간 `<td>` 에 `whiteSpace: nowrap` + 금액을 `UnitAmount` 로 뽑아
**숫자와 단위를 한 줄로 묶고 단위를 0.85em + 흐린 색**으로.

🔴 **「원」을 지우지 않았다** — 작게 하는 것만으로 한 줄에 들어갔고, 이 부품은 기본운임뿐
아니라 **가산기준의 `분`·`%`** 도 그린다(지우면 금액인지 분·퍼센트인지 구분이 사라진다).
사용자가 준 선택지 중 「더 작게」 쪽을 고른 것이다.
🔴 **`whiteSpace: nowrap` 두 곳을 빼지 말 것** — 빼면 그대로 되돌아간다.
🟢 화면에 읽히는 글자는 그대로 「34,000원」이다(태그만 나뉜다).
⚠️ 표폭이 1310 → **1357** 이 되어 감싸개(1312)보다 커졌다 — **표 안에서만** 가로 스크롤되고
**페이지 자체 가로 넘침은 네 폭 모두 0** 이다.
⚠️ `won()` 은 그 파일에서 **원래부터 사용처가 0** 이었다(`origin/main` 에서도). 내 변경이
만든 죽은 코드가 아니라 손대지 않았다(금지 20번 — 손댄 파일의 다른 부분 정리 금지).

**═══ ⑨ 검증 ═══**

```
산술 전수 30칸   1톤 열이 DB v11 실측과 완전일치 · 차급 역전 0 · 거리 역전 0 ·
                천원 단위 30/30 · 지급상한 = 청구가÷1.15 30/30 · 라보 400km 초과 256,000
반영 후 실측     13차급 · 15구간 · 195행 · 가산기준 13행 · 이상값 0 · 역전 0 · 미매칭 0 ·
                `_migrations` 28행 · 🟢 기존 165칸 무변경(게시 6칸 그대로)
렌더링(목+PW)    /admin/rates 14열 195칸 「-」 0 · 견적 자동계산 **195칸 전부** 일치 ·
                대기료 30,000 · 가산 10%+15,000 · 혼적 20/35/55% · 랜딩 요금 가이드 6줄 ·
                /quote·/apply 11종 · 화주포털 기본 1톤 + 다마스·라보가 위 ·
                34차 회귀(네 블록 · 2열 · sticky · 표+카드) · 견적서 4종은 저장 문자열 그대로
빌드            tsc 0 · 프리렌더 실패 **44**(기준선과 동일, 항목까지 일치) · globals.css 0
```

🔴 **거짓 ❌ 가 아홉 번 났고 전부 내 시험 자산 결함이었다**(함정 3, 열세 차수째 — **코드
결함 0건**). 라디오 라벨이 「율(%)」이 아니라 **「할인율(%)」** · 표준 할인율은 **빈칸일 때
한 번만 채우는 기본값**이라 거리를 바꿔도 다시 안 채워지는 것이 정상 · 모달 버튼 문구가
**「차량 · 요금 가이드」** · `/apply` 톤수 칸은 **접힌 「상세 정보 입력하기」 안** · 펼침
선택자에 `div` 를 넣어 엉뚱한 요소 클릭 · 트리거 문구가 `"1톤▼"` ·
🔴 **줄 수를 칸 높이 ÷ line-height 로 재면 패딩이 섞여 「1톤」까지 2줄로 나온다** ·
🔴 **`Range` rect 의 `top` 을 Set 으로 세면 「원」을 작은 span 으로 뺀 순간 같은 줄인데도
2줄로 잡힌다**(줄이 진짜 나뉘었는지는 **rect 가 세로로 분리됐는지**로 봐야 한다) ·
산재보험료 탭을 표로 가정(**입력 폼이다** — `EditableNumber` 를 안 쓴다).

**═══ ⑩ ⚠️ 사용자가 코드 밖에서 할 일 · 안 한 것 ═══**

- ⚠️ **적재 한도 미확정** — 다마스 약 400~550kg · 라보 약 500~600kg(모델별로 다르다).
  표에는 없어도 상담에 필요하다
- ⚠️ **배차 담당자에게 ③ 운영 규칙 전달** — 특히 중거리 예외와 10km 이내 마진
- 🟡 **표본 43건**(다마스 18 · 라보 25)이고 **장거리는 2건뿐(근거 등급 D)** — 8톤·15톤과
  같은 성격이다. 실거래가 쌓이면 **중·장거리**를 다시 잡아야 한다
- 🔴 **`PUBLISHED_START_PRICE_TONS`(6종)를 늘리지 않았다** — 사용자가 명시적으로 반대한
  것이다. 다마스·라보는 랜딩 「차량 · 요금 가이드」에 나오지 않는다(렌더링으로 확인)
- 🔴 **상차 시각 지정 가산 신설은 여전히 미착수**다(22차가 미룬 것) — `rate_surcharges` 에
  그 카테고리 자체가 없다

**작성일: 2026-07-23** (최종 갱신: 2026-09-10, Claude Code 세션에서 직접 갱신 —
**34차: 견적관리 개편 — 화주포털 발주요청과 정합** — **PR #141 merge됨**(squash `8ddb59a`).
🔴 **DB 0 · 마이그레이션 0 · `_migrations` 27행 유지 · 화주포털 0줄 · 공개 화면 0줄 ·
`globals.css` 순수 추가(+371 / −0).** 지시서 6건 + **실사용 리뷰 5라운드**(라운드마다
지적이 달라져서, 본작업보다 리뷰 반영분이 더 크다).

**═══ ① 착수 전 확인에서 지시서 전제 넷이 틀렸다 ═══**

| # | 지시서 전제 | 실제 |
|---|---|---|
| 4·5 | 발주요청 ↔ 견적 연결 컬럼이 없을 수 있다 | 🟢 `portal_order_requests.quote_id` 가 **이미 있고 이미 쓰인다.** 「발주요청 → 견적」 프리필 경로(`?from_request=`)도 이미 있고 저장 시 `status:"승인됨" + quote_id` 까지 쓴다. **이번에 만든 것은 「진입점」이지 새 경로가 아니다** |
| 6 | 알림이 없으면 신설은 범위 밖 | 🟢 이미 있다 — TopNav 「화주요청」 배지 + Realtime 구독 + 15초 폴링 |
| 8 | 인쇄 화면에 공용 컴포넌트가 없으면 보고 | 공용 컴포넌트는 없지만 🟢 관리자 print 라우트에 **`?embed=1` 분기가 이미 준비돼 있었다**(앞 세션이 「관리자에 모달이 붙을 때를 대비」해 넣어둔 것) |
| 11 | 과거 일시 금지가 어디에 걸려 있는가 | 🔴 **한 곳이 아니라 넷** — 화면 `minDateTime` × 2 + 제출 검증 × 2. 서버 검증은 **0건** |

🟢 **그래서 DB 마이그레이션이 아예 필요 없어졌다.** 지시서는 「있을 수 있다」였다.

**═══ ② 본작업 여섯 가지 ═══**

`RequiredMark` 신규(별표 + `aria-label`) · 담당자 연락처 필수 해제(**여섯 화면 전부**) ·
과거 일시 허용(견적 화면 넷) · 견적 입력을 포털 순서 네 블록으로 · 견적서 출력을
화면 안 모달(`PrintModal`)로 · 대기 중 발주요청을 견적목록 **맨 위 고정** + 중복 전환 차단.

🔴 **맞춘 것은 배치이지 부품이 아니다** — 포털 부품(`Pv2Select`·`Pv2DatePicker`·`.pv2-*`)을
**하나도 안 가져왔다.** `.portal-v2` 스코프 전용이라 끌어오면 관리자 31화면이 공유하는
CSS 가 딸려온다. 블록 제목·번호 배지는 관리자 쪽으로 새로 그렸다.
🔴 **화주포털의 과거 일시 금지는 일부러 남겼다** — 화주가 지난 날짜로 발주하면 안 된다.
🔴 **견적서 내용은 한 곳뿐이다** — 모달은 `iframe` 껍데기고 기존 print 라우트를 띄운다
(실측: `PrintModal` 안에 견적서 필드 문자열 **0건**). 모달용으로 따로 그리면 조용히 갈린다.
🔴 **부모에서 `window.print()` 를 부르지 말 것** — 관리자 화면 전체가 인쇄된다.
   반드시 `iframe.contentWindow` 에.
🔴 **중복 차단을 `handleSubmit` 맨 앞에 뒀다** — 뒤에 두면 거리 자동계산 같은 다른 검증에
   먼저 막혀서, 이미 처리된 건이라는 사실을 나중에야 알게 된다.
🔴 **「당착/내착」은 DB 컬럼을 만들지 않았다** — `quotes.notes` → `orders.special_notes` 로
   이미 끝까지 이어지는데 `quotes` 에만 컬럼을 만들면 오더 전환(35차 범위)이 그걸 안 읽어
   **지금보다 값이 일찍 끊긴다.**

**═══ ③ 리뷰 1라운드 — 「견적관리 알림 4건이 계속 남아 있다」 ═══**

이 라운드의 본체다. 지적은 짧았지만 **원인이 둘이었고 둘 다 구조 문제였다.**

`_verify.sql` 에 읽기 전용 진단 ⑬ 을 넣고 실측했다(2026-09-10):

```
  배지 4건 · 견적은 전부 `수주` 7건 · 오더 6건 중 **3건이 quote_id 없음**
```

배지는 27차가 만든 것으로 「`status='수주'` 인데 `orders.quote_id` 로 이어진 오더가
없는 견적」을 센다. 안 없어지는 길이 둘이었다.

- **(가) 그 4건이 어느 건인지 볼 화면이 없었다.** 계산식이 `TopNav.tsx` 안에만 있어서
  배지는 숫자만 말하고 담당자는 목록에서 그 건을 찾을 방법이 없었다.
- **(나) 오더를 `?from_quote=` 를 거치지 않고 직접 등록하면 `quote_id` 가 빈다.**
  그러면 오더가 실재해도 그 견적은 영영 「오더 없음」으로 남는다.
  🔴 **이 길이 없으면 담당자가 배지를 지우려고 같은 건의 오더를 또 만든다** —
  실측에 고아 오더가 **3건** 있었으므로 가상의 위험이 아니다.

고친 것 — `lib/unlinkedWonQuotes.ts`(세는 규칙의 **유일한 정의처**) 신규, 화면 넷이 같은
함수를 쓴다: TopNav 배지 · 견적 목록(「운송오더 생성 필요」 + 「+ 운송오더」) ·
오더 목록(맨 위 안내 + 건별 만들기) · **오더 상세의 「견적 연결」**.

🔴 **「견적 연결」을 지우지 말 것** — **이미 만든 오더를 뒤늦게 잇는 유일한 길**이다.
🔴 후보는 **그 화주의** 「수주인데 오더 없는 견적」뿐이고 **게스트 오더에는 안 띄운다**
   (회사가 없으면 어느 견적과 한 건인지 판단할 근거가 없다).
🔴 **이미 이어진 오더에는 후보 드롭다운을 안 띄운다**(갈아 끼우기는 이 화면 일이 아니다).
   해제만 가능하고, 해제하면 그 견적이 알림에 다시 나타난다고 확인 창에 적어 뒀다.
🔴 연결은 한 필드짜리 즉시 저장이라 낙관적 잠금을 안 쓰지만 **끝에서 반드시 `load()`**
   (원칙 36번) + `notifyBadgeRefresh()`(원칙 23번). 둘 중 하나만 빠져도 「고쳤는데
   그대로다」로 읽힌다.

같은 라운드의 나머지 둘 — 필수칸 **왼쪽 빨간 선 제거**(별표 + 옅은 바탕은 남겼다.
색만으로 구분하지 않는다는 전제는 그대로 성립한다) · 화면 폭 확대(아래 ④).

**═══ ④ 리뷰 2라운드 — 폭을 넓히면 오히려 좁아진다 ═══**

1560px 로 넓혔더니 사용자가 *"너무 넓어 졌다 … 1300~1400사이"* 라 **1360px** 로.
값은 `--admin-max` **한 곳**에 둔다(상단바와 본문이 어긋나면 안 된다).
🔴 1100(원래)도 1560(1라운드)도 사용자가 물린 값이니 그 범위 밖으로 되돌리지 말 것.

🔴 **`.container` 자체를 고치지 않았다** — 쓰는 곳 39개 중 **7개가 화주포털**이고
`SiteFooter`·`PublicPageHeader`·`LegalDoc` 을 통해 **공개 화면 6개**도 쓴다.
`app/admin/layout.tsx` 가 자식을 `.admin-wide` 로 감싸고 그 안에서만 넓힌다.
상단바는 그 래퍼 밖(루트 레이아웃)이라 `.top-nav .top-nav-inner` 로 따로 넓혔다 —
`TopNav` 는 `/customer` 와 공개 경로에서 `null` 을 돌려주므로 관리자 전용이 맞다.

🔴 **폭만 넓히면 지적이 더 나빠진다.** `.form-grid` 가 `auto-fill minmax(200px, 1fr)` 이라
넓힌 폭이 칸 폭이 아니라 **열 수**로 간다 — 실측 견적 폼이 2열 282px → **4열 203px** 로
오히려 좁아졌다. 최소 폭을 260px 로 올려 열이 덜 늘게 하고 **견적 폼은 2열로 못박았다**
(출발지·도착지가 좌우 한 쌍이어야 하고 상·하차 담당자 블록도 2열 기준이다).
결과: 견적 폼 282 → **420px**(뒤에 340px 패널 도입으로 466px).

**견적 폼을 포털 배치로** — 출발지 열 / 도착지 열로 갈랐다. 전에는 주소 둘이 위에
나란히, 담당자 여섯 칸이 아래에 따로 있어서 「이 담당자가 상차인가 하차인가」를
라벨로만 알 수 있었다. 🔴 **`PickupDropoffContactFields` 에 `only` 를 더했다**(선택 prop) —
주지 않으면 지금까지와 똑같아서 **나머지 다섯 화면은 한 줄도 안 바뀐다.** 이 모드는
자기 레이아웃을 스스로 그린다(`grid-column` span 을 컴포넌트에 심으면 그 다섯 화면이
같이 망가진다).
🔴 계산 패널을 `0.8fr` → **고정 340px** — 비율로 두면 넓힐수록 패널만 커지고 정작
입력칸이 안 넓어진다. `minmax(0, 1fr)` 의 `0` 을 빼지 말 것(긴 주소가 칸을 민다).

⚠️ **34차 본작업이 「완전히 갈리지는 않는다 — 범위 밖」으로 남겨둔 항목이 이 지시로
범위 안에 들어온 것**이다.

**═══ ⑤ 리뷰 3라운드 — 모바일이 통째로 깨져 있었다 ═══**

*"모바일에서 견적관리, 운송오더, 배차관리, 정산관리의 레이아웃이 엉망이다."*
실측(390px) — 표가 **880 / 610 / 914 / 1056px**, 셋은 **페이지 자체가 옆으로 밀려**
(scrollWidth 627·931·1073) 제목·필터까지 잘렸다.

`components/AdminMobileList.tsx` 신규 — 🔴 **원칙 13번 그대로 데스크탑 표와 모바일 카드는
별개 JSX** 다. CSS 로 표를 접는 방법(`display:block` + `td::before`)을 안 쓴 이유는
**모든 `<td>` 에 `data-label` 을 달아야 하고 「생략 가능한 부분은 생략」이 안 되기 때문**
이다. 🟢 스타일은 포털이 쓰던 `.mobile-row-*` 재사용 — **CSS 를 한 줄도 안 더했다.**
🔴 **생략한 것**(상세에 가면 다 있다): 오더=등록일 · 배차=마진율 · 정산=금액 밑 잔주석 ·
견적=삭제 버튼. 🔴 **배차의 청구·지급·마진은 안 뺐다**(목록에서 먼저 보는 숫자다).
🔴 **상태 드롭다운은 카드에도 남겼고, 배차는 데스크탑과 같은 목록이라야 해서
「접수중」을 걸러낸다**(상세에서만 확정 — 한쪽만 고치면 모바일에서만 접수중으로 되돌릴
수 있게 된다).
곁다리로 머리줄 버튼이 「오 / 늘」로 두 줄 접히던 것도 고쳤다(🔴 `.btn` 자체에 `nowrap`
을 주지 말 것 — 공개 화면까지 딸려간다).

홈 카드는 **4칸 2줄**로 못박았다 — 🔴 **폭을 넓혔더니 깨진 것이 원인**이다
(`auto-fill` 이라 1100 에서 4열이던 것이 1560 에서 6열). 칸 수를 못박아 남는 폭이
**카드 폭**으로 가게 했다(254 → **316px**). 모바일은 `≤460px` 에서 1열로 떨어지던 것을
2열로 눌렀고 설명글은 `-webkit-line-clamp: 2`(🔴 `display: -webkit-box` 와 한 벌).

**═══ ⑥ 계산창을 떠 있는 창으로 — 3·4·5라운드에 걸쳐 세 번 고쳤다 ═══**

`components/DraggablePanel.tsx` 신규. 라운드마다 지적이 이어졌다.

```
  3라운드  "팝업으로 기본 아래에 배치되는데 끌어다 자유롭게 위치조정을 할수 있게"
  4라운드  "좌우로는 안움직였으면 좋겠다" · "헤더 컬러(옐로우)가 달라서 구분감이"
  5라운드  "약간 왼쪽으로 쏠려 있다. 가운데 정렬이 되어야 한다"
```

🔴 **데스크탑에는 한 겹도 안 씌운다** — `isMobile` 이 아니면 `children` 을 그대로
돌려준다. 감싸개를 하나라도 끼우면 그 카드의 `position: sticky` 기준(가장 가까운 스크롤
조상)이 바뀌어 **따라다니기가 조용히 멈춘다.**
🔴 `touch-action: none`(브라우저가 스크롤로 가로챈다) · `setPointerCapture`(손가락이
손잡이를 벗어나면 멈춘다) — 둘 다 빼지 말 것.
🔴 **가로는 CSS 가 잡는다**(`left: 50%` + `translateX(-50%)`). 5라운드의 「왼쪽 쏠림」은
`left: 8px` + 폭 상한 360px 조합이 원인이었다(390px 에서 왼쪽 8 / 오른쪽 22).
**컴포넌트에서 `left` 를 인라인으로 주지 말 것** — 그래서 state 를 `top` 하나로 줄였다.
🔴 **세로만 움직인다** — `dragRef` 에서 `dx` 를 되살리면 좌우가 그대로 풀린다.
🔴 손잡이는 `--brand-yellow`(#ffd833). **값을 리터럴로 다시 적지 말 것**(랜딩·포털과
같은 노랑이어야 한다). **옐로 위에 `--text-muted` 를 올리지 말 것**(11차 헤더 옐로에서
겪은 자리) — 제목 `#1a1a1a`(대비 **12.53**) · 안내글 `#5c4a00`(대비 **6.21**).
🔴 **떠 있는 창은 흐름에서 빠지므로 견적 화면 2열 격자를 모바일에서 1열로 접었다** —
안 접으면 340px 빈 칸이 남아 입력칸이 반쪽이 된다(실측: 라벨이 세로로 쪼개져 있었다.
**이번에 같이 해소된 문제다**).
⚠️ **위치는 기억하지 않는다** — 저장하려면 어디에 저장할지부터 정해야 하고 화면 크기가
바뀌면 그 좌표가 화면 밖일 수 있다(범위 밖으로 뒀다. 요청이 오면 그때 판단).

**═══ ⑦ 검증 — 거짓 ❌ 가 다섯 번 났고 전부 내 시험 자산 결함이었다 ═══**

🔴 **함정 18 이 말하는 그 자리다.** 이번 다섯 건:
목이 `select=*` 를 400 으로 돌려줌 / 목 표본에 `created_at` 없음 / 중괄호 추출기가 옆
코드를 쓸어담음 / **목이 PATCH 를 막고 있었다**(`Access-Control-Allow-Methods` 누락 —
실제 PostgREST 는 보낸다. 🔴 원칙 56번의 **반대 방향** 사례: 목이 실제보다 **엄격해도**
검증이 무의미해진다) / 폭 임계값·숨은 DOM 세기·2라운드 순서를 그대로 둔 기대값.
🟢 그때 화면에 뜬 「견적 연결에 실패했습니다」가 오히려 **오류 분리(원칙 33번)가
제대로 됐다는 확인**이 됐다.

측정한 것 — 화주포털·공개 화면 **diff 0줄** · 견적 계산 로직 **diff 0줄**(계산 블록을
전후 **내용으로** 대조) · 항목 집합 **23 → 23**(실제 렌더링 DOM 에서) ·
인쇄는 **스크린샷이 아니라 인쇄 미디어로**(모달 밖 0개) · 견적서 엑셀 **md5 동일** ·
모바일 목록 네 화면 × 390·360px 가로 넘침 0 · 데스크탑 1440px 에서 표가 보이고 카드 0 ·
끌기 실측(706 → 356, 좌우 8 → 8) · 좌우 여백 다섯 폭에서 정확히 같음 ·
`tsc` 통과 · **프리렌더 실패 44개로 기준선 동일**.

**═══ ⑧ 아직 안 한 것 / 사용자 확인 대기 ═══**

- ⚠️ **PR 본문 검증표 4번이 낡았다** — 「왼쪽 선 3px」이 그대로 적혀 있다. 본문 전체를
  다시 쓰는 대신 뒀고, 정정은 코멘트 세 건과 **`globals.css` 주석**에 있다(코드 쪽이
  진짜 방어선이라 판단).
- ⚠️ **계산창 위치 기억** · **`/api/customer/update-contact` 서버 검증** ·
  **화주 목록 페이지네이션**(540건을 한 번에 그린다) — 전부 범위 밖으로 남겼다.
- ⚠️ **`_verify.sql` ⑬ 은 진단용이라 남겨 뒀다** — 다음에 같은 신고가 오면 그대로 쓴다.

---

**작성일: 2026-07-23** (최종 갱신: 2026-09-10, Claude Code 세션에서 직접 갱신 —
**차수 없음: 정기계약 배지 — 활성 화주 목록 추가 · 회사명 위 줄로 · 필터** — **PR #140
merge됨**(squash `079dd3b`). 🔴 **DB 0 · 마이그레이션 0 · 화주포털 0줄 · 공개 화면 0줄.**
33차 직후 사용자 지적 3건으로 이어진 소수정이다(33차와 같은 날).

**═══ ① 33차가 활성 화주 목록을 빠뜨렸다 ═══**

33차 B장이 견적·오더·배차에는 배지를 붙였는데 **`/admin/customers`(활성 화주 CRM)가
빠져 있었다.** 화주 관리(`/admin/companies`)와 나란히 쓰이는 화면이라 한쪽에만 있으면
담당자가 「이 화면에는 정기계약이 없다」로 읽는다.
🔴 **B장을 지시서대로 「견적·오더·배차」만 보고 끝낸 것이 원인이다** — 다음에 표시를
확산할 때는 **그 값을 보여줄 만한 화면을 전수로 훑을 것**(화주 관련 목록은 넷이다:
화주 관리 · 활성 화주 · 개인고객 · 화주요청).

🔴 **여기서도 별도 집합 조회를 만들지 않았다** — 이 화면은 이미 `companies` 를 직접
조회하므로 select 에 컬럼만 더하면 되고, 그래야 조회가 실패할 때 **기존 오류 경로
(`setError`)가 그대로 잡는다.** 따로 떼면 「목록은 떴는데 배지만 조용히 사라지는」
실패 모드가 새로 생긴다(B장과 같은 판단).

**═══ ② 배지는 회사명 **위 줄**이다 — 다섯 목록 전부 ═══**

사용자 지적: *"정기계약 배지 위치를 회사명 위에 붙이는 게 좋겠다."*

🔴 **활성 화주 목록만 고치지 않고 다섯 목록 전부 바꿨다**(화주 관리 · 활성 화주 ·
견적 · 오더 · 배차). 근거 둘 —
```
  ① 같은 배지가 형제 화면에서 자리가 다르면 담당자가 다른 것으로 읽는다
  ② 이름 옆에 붙으면 이름 칸이 그만큼 넓어져 다른 칸을 민다
     (이름 칸이 `cell-nowrap` 이라 줄바꿈으로 흡수되지도 않는다)
```
🔴 **넓힌 판단이라 사용자에게 명시적으로 알리고 되돌리기 쉽다고 밝혔다.**

🔴 **호출부에서 `<div>` 로 감싸 올리지 말 것 — 이것이 이 차수의 함정이다.**
배지가 안 붙는 화주는 `RecurringContractBadge` 가 `null` 을 돌려주는데, 바깥 `<div>` 는
그대로 남아 **빈 줄로 칸이 벌어진다.** 그래서 감싸는 일까지 컴포넌트가 하도록
`block` prop 을 뒀다(`display: block` + `width: fit-content` + `marginBottom: 2`).
**5곳에 각자 `<div>` 를 쓰는 방식으로 되돌리지 말 것.**

**═══ ③ 활성 화주 목록에 「정기계약만 보기」 ═══**

🔴 **판정은 배지와 같은 함수(`isRecurringContractActive`)를 쓴다** — 따로 적으면
「배지는 없는데 필터에는 걸리는」 건이 생긴다. 종료일이 지난 계약은 여기서도 걸러진다.
🔴 **칩의 모양·자리·문구를 화주 관리 목록의 같은 칩과 맞췄다** — 두 화면이 나란히
쓰여서 한쪽만 다르면 다른 기능으로 읽힌다. 자리도 검색칸 **앞**으로 같게 뒀다.

**═══ ④ 검증 — 위치는 눈으로 짐작하지 않고 좌표로 쟀다 ═══**
```
  「배지의 아래끝 ≤ 이름의 위끝」인가 + computed display 가 block 인가
  이름은 `<td>` 안의 **텍스트 노드**를 Range 로 잡아 좌표를 얻는다
  (이름이 요소로 감싸여 있지 않아 `querySelector` 로는 못 잡는다)

  활성 화주 3행 · 화주 관리 3 · 견적 2 · 오더 2 · 배차 2   전부 통과 · 예외 0
  필터: 활성 화주 5 → 3 → 5, 걸러진 둘이 정확히 배지 없는 행(종료됨·미체크)
        화주 관리도 5 → 3
  🔴 음성 대조 — select 에서 두 컬럼을 빼니 배지 3건이 사라져 시험이 실제로 실패했다
```
⚠️ **검증 중 두 번 헛짚을 뻔했다 — 둘 다 내 시험 쪽 문제였다.**
```
  ⓐ 화주 관리 목록이 통째로 비었다 → 목 표본에 `created_at` 이 없어서 42703 이 났고
     화면은 그 오류를 **제대로 띄우고 있었다**(원칙 55번이 지켜지는 것을 확인한 셈).
     표본을 채우고 다시 쟀다
  ⓑ 화주 관리 필터가 「❌」로 나왔다 → 그 표는 **첫 칸이 회사명이 아니라 「출처」**라
     행 구분이 안 된 것이었다. 행 전체 텍스트로 다시 재니 통과.
     🔴 **목록 검증에서 「첫 번째 `<td>` = 이름」이라고 가정하지 말 것**
```

**═══ ⑤ 안 한 것 ═══**
```
  🔴 개인고객(individual_customers) · 화주요청 목록에는 안 붙였다 — 정기계약은
     `companies` 컬럼이라 개인고객에는 애초에 없고(33차 범위 밖 결정 그대로),
     화주요청은 요청 단위 화면이라 화주 속성을 얹을 자리가 아니다
  ⚠️ 활성 화주 목록 화면 설명글에 「정기계약」 안내를 넣지 않았다 — 🔑 안내처럼
     한 줄 넣을 수 있으나 요청 밖이라 두었다
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-10, Claude Code 세션에서 직접 갱신 —
**33차: 화주 항목 정합 + 정기계약 (A장 + B장)** — **PR #139 merge됨**(squash `5fef0d1`).
🔴 **DB 변경 있음**(`companies` 에 정기계약 5개 컬럼 + 부분 인덱스 · `_migrations` **27행**) ·
🟢 **화주포털 0줄 · 공개 화면 0줄 · `globals.css` 0줄.** 실사용 리뷰 **4라운드**.
🔴 **지시서는 A장·B장을 두 PR 로 나눴는데 사용자 확정으로 한 PR 이 됐다**(2026-09-10).

**═══ ① 이 차수의 본체는 「정기계약」이 아니라 「입구를 하나로 모은 것」이다 ═══**

화주(`companies`)가 만들어지는 입구가 **셋**인데 화면마다 항목 목록을 각자 들고 있었다 —
**등록 25개 · 수정 54개 · 승인 19개.** 값만 손으로 맞추면 다음에 컬럼이 하나 늘 때 또
갈린다. 그래서 정의를 한 파일로 모으고 세 입구가 **같은 정의를 읽게** 했다.

```
  lib/companyFields.ts        🔴 유일한 정의처 — 항목 정의 · payload 조립 · 정기계약 판정
  components/CompanyFieldInput.tsx   위젯 렌더링 (등록·수정 폼 공용)
  components/RecurringContractBadge.tsx   「정기계약」 배지
  app/admin/companies/page.tsx        등록 폼 317줄 → 83줄
  app/admin/companies/[id]/page.tsx   섹션 렌더링 290줄 → 98줄 (지역 배열 5개 제거)
  app/api/admin/approve-application/route.ts   같은 payload 함수 사용
```
🔴 **화면 파일에 필드 배열을 다시 만들지 말 것** — 만드는 순간 정의 파일과 갈린다.
   이 차수가 없앤 것이 정확히 그 상태다.

**═══ ② 지시서 전제 다섯 개가 실제와 달랐다 ═══**
```
  1  승인 API 는 이미 고쳐져 있었다 — manual_source_note 가 아니라 notes 에 넣고
     주소도 제대로 된 컬럼에 들어간다. 2-4 의 할 일이 크게 줄었다
  2  `companies_id_page_final.tsx` 는 존재하지 않는다(CLAUDE.md §7 안내가 낡았다).
     배열도 2개가 아니라 **5개**였다
  3  화주 목록은 이중 구조가 아니다 — <table> 단일이라 한 곳만 고쳤다(원칙 13번 해당 없음)
  4  `repeat_customer`(재거래 여부)가 이미 있었다
  5  `status` 값 목록에 「반복화주」·「월정산화주」가 이미 있었다 — 관계 개념이 섞여 있다
```

**═══ ③ 되돌리면 안 되는 결정 ═══**

🔴 **`companies.status` 에 「정기계약」을 값으로 넣지 말 것 — 축이 다르다.**
   상태는 「지금 어느 단계인가」이고 정기계약은 「어떤 관계인가」다. 같은 칸에 넣으면
   **「정기계약 화주인데 지금 새 견적을 협의 중」** 을 표현할 수 없고, 상태 필터·이력·
   `STATUS_OPTIONS`(lib/statusColors.ts)를 쓰는 화면 5곳이 전부 걸린다.
   🟢 **마이그레이션 안의 단언이 이걸 막는다** — `status = '정기계약'` 행이 있으면 실패한다.

🔴 **`repeat_customer` 를 재사용하지 말 것.** 실측하니 그 컬럼은 정산 저장
   (`app/admin/invoices/page.tsx`)이 **`총 오더수 > 1` 이면 자동으로 덮어쓰는 실적
   파생값**이다. 계약 여부를 거기 담으면 담당자가 켠 값이 다음 정산에서 조용히 사라진다.
   **두 항목이 화면에 나란히 보이는 것이 정상이다**(「재거래 여부」 ≠ 「정기계약」).

🔴 **종료일이 지난 계약에는 배지를 붙이지 않는다**(`isRecurringContractActive`).
   체크는 기록으로 남고 상세에는 「종료됨」이 뜬다. **「체크가 켜져 있는데 왜 배지가
   없지」는 의도된 동작이다.** 🔴 **종료일 당일은 유효**로 센다(날짜만 비교 — 시각이
   섞이면 종료일 당일이 빠진다).

🔴 **정기계약은 화주에게 보이지 않는다** — 포털·견적서·엑셀 **0줄**. 배차 담당자가
   우선순위를 판단하는 내부 정보다. `RecurringContractBadge` 를 `app/customer/**` 에
   쓰지 말 것(컴포넌트 주석에도 적어뒀다).

**═══ ④ A장에서 내가 판단한 것 ═══**
```
  접이식 경계   지시서는 「필수 항목만 펼침」이라 했으나 실측하니 **필수는 회사명 하나뿐**
                이라 그대로 하면 등록 화면이 **입력칸 한 개**가 된다. 전화 상담 중 바로
                받아 적는 「기본 정보」·「담당자」까지 펼치고 나머지를 접었다.
                🔴 **접힌 구획의 값도 그대로 저장된다**(CSS 로만 감춘다)
  실적 6개      등록 폼에서 뺐다(`inForm: false`) — 신규 화주는 0이고 정산이 덮어쓴다.
                완료조건 2의 「등록 == 수정」은 이 플래그를 제외한 집합으로 읽었다
  회사명        상세 수정에 **넣었다** — 지금까지 회사명을 고칠 방법이 아예 없었다
  요약 카드     상단 요약 카드의 편집 UI 9개를 제거해 **표시 전용**으로. 같은 항목을 두
                자리에서 편집하면 어느 쪽이 맞는지 헷갈리고, 정합의 취지가 입구를 모으는 것이다
  월 예상 건수  숫자로 읽히면 `monthly_expected_orders` 컬럼에, 「30건 정도」처럼 못 읽히면
                `notes` 에 원문을 남긴다(값을 잃지 않는 쪽)
  showWhen      「출처 설명」이 등록 폼에서는 항상 보이고 수정 폼에서는 「기타」일 때만
                보이던 것을 정의(`showWhen`)로 옮겼다 — 이번 차수가 없애려던 바로 그 형태였다
```

**═══ ⑤ B장 — 착수 전 확인 10의 답이 설계를 결정했다 ═══**

🟢 **견적·오더·배차가 전부 `company_id` 로 조인하고 이미 `companies(...)` 를 select 한다**
(배차는 `orders(...companies(name)...)` 중첩). 그래서 지시서가 걱정한 *"조인이 하나 늘어
느려지는"* 일이 **없다** — 별도 집합 조회 API 없이 **기존 select 에 컬럼 두 개만** 더했다.

🔴 **집합 조회 API 를 안 쓴 진짜 이유는 성능이 아니라 실패 모드다.** 배지 값이 목록과
   **같은 질의**로 오면 그 질의가 실패할 때 **기존 오류 경로가 그대로 잡는다.** 별도
   조회로 떼면 「목록은 떴는데 배지만 조용히 사라지는」 새 실패 모드가 생기는데,
   그게 지시서가 경고한 형태다.

🔴 **배차 등록의 오더 선택 `<select>` 에는 안 붙였다** — `<option>` 안의 내용은 브라우저가
   **평문으로** 그려서 배지 컴포넌트가 렌더링되지 않는다. 그 select 문자열(`dispatches`
   위쪽 2칸 들여쓰기 타입)은 **구형 그대로 뒀다.** 아래쪽 4칸 들여쓰기 타입만 넓혔다.

**═══ ⑥ 대시보드 카운트에서 조심한 것 셋 ═══**

🔴 **기존 `companies` 조회를 재사용하지 말 것** — 그 조회는 `.in("id", companyIds)` 라
   **최근 12개월에 거래가 있는 화주만** 담는다. 계약만 맺고 아직 첫 운송이 없는 화주가
   빠져 실제보다 적게 세어진다. 그래서 `is_recurring_contract` 로만 좁힌 별도 조회를 둔다
   (부분 인덱스가 정확히 그 조건이다).

🔴 **「활성」 판정을 SQL 로 다시 쓰지 말 것** — 종료일 당일 포함 규칙이 두 벌이 되어
   조용히 갈린다. `isRecurringContractActive()` **한 함수**로 센다. 대상이 소수라 값이 싸다.

🔴 **조회 실패를 0 으로 내려보내지 말 것** — 화면이 「정기계약이 하나도 없다」로 읽는다.
   `null` + 「조회 실패」로 그린다. ⚠️ **같은 라우트의 다른 6개 조회처럼 400 으로 끊지
   않은 것은 의도다** — 부차 지표 하나 때문에 대시보드 전체가 안 뜨면 손해가 더 크다.
   나중에 「일관성」을 이유로 400 으로 통일하려 들지 말 것(주석에 사유를 적어뒀다).

**═══ ⑦ 실사용 리뷰 4라운드 — 2라운드가 3라운드를 만들었다 ═══**
```
  1  「펼치고 닫을 때 버벅거림 · 레이아웃이 어색」
     🔴 첫 가설(마운트/언마운트)은 **틀렸다** — CSS 감추기로 바꿔도 218ms 로 그대로였다.
     진짜 원인은 **`form` 객체를 통째로 prop 으로 넘겨 입력칸 51개가 매번 다시 그려지는 것**.
     `React.memo` + **값만 넘기기** + `useCallback` **셋이 한 벌**이다(하나만 어겨도 무력).
     159 → 99ms. 레이아웃은 카드에 `padding` 이 아예 없던 것(구획 제목이 가장자리에 붙음)
  2  「여전히 버벅이고 **윈도우도 같이 버벅댄다**」
     🔴 **1라운드 진단이 일부였다. 「윈도우도」가 결정적 단서였다** — 메인 스레드가 오래 잡힌다는 뜻.
     🔴 **내 미리보기에 화주 목록이 없었다.** 실제 화면은 등록 폼과 목록이 같은 컴포넌트에
     있고 화주가 **540건**인데 페이지네이션이 없다. 폼 높이가 바뀔 때마다 그 아래 수천 DOM 이
     다시 배치됐다. 목록 카드에 `contain: layout style` + 폼이 열리면 목록·검색·탭을 안 그림.
     DOM **3,819 → 408** · 평균 **64ms**(반복 30~45) · 스로틀 없이 11~20ms.
     ⚠️ 1라운드에 「데스크탑 5~17ms 라 괜찮다」고 보고한 것은 **목록 없는 화면을 잰 값**이라 틀렸다
  3  🔴 **「신규업체등록을 누르면 아무것도 안 뜬다」 — 2라운드 수정이 만든 회귀.**
     목록·검색·탭을 `{!showForm && (…)}` 로 감싸면서 **그 괄호 안에 등록 폼 블록까지** 넣었다.
     두 조건이 배타적이라 **폼이 영영 렌더링될 수 없었다.** 폼을 래퍼 **바깥 앞쪽**으로 옮기고
     조회 실패 배너도 바깥으로 뺐다(안에 있으면 폼이 열린 동안 오류가 안 보인다)
  4  「대시보드에 수만 나오는데 클릭시 명단이 나오면 좋겠다」
     숫자를 눌러 펼치는 명단 + 이름 → 화주 상세(`next/link`, 원칙 31번) + 종료일 병기.
     🔴 **API 가 개수를 따로 주지 않는다 — 명단 길이가 곧 개수다**(두 값을 나눠 보내면
     「3개인데 명단은 2줄」로 조용히 어긋난다). 0개·조회 실패일 때는 **버튼으로 만들지 않는다**
     (눌러도 아무 일이 없으면 고장으로 읽힌다)
```

**═══ ⑧ 검증 — 목이 헐거우면 시험이 무의미하다(원칙 56번을 두 번 겪었다) ═══**

🔴 **기존 목은 `select` 투영을 흉내내지 않았다.** 그대로 썼으면 **select 에 컬럼을 안
   더해도 배지가 그려져** 시험이 통과했을 것이다. 투영을 강제하고 없는 컬럼에는 실제
   PostgREST 처럼 `42703` 400 을 내는 목을 새로 만들었다.

🟢 **음성 대조를 실제로 돌렸다** — `quotes` select 를 구형(`companies(name)`)으로 되돌리니
   그 시험이 **정말로 실패했다**(배지 2건 소실). 「내 시험이 통과했다」가 「동작한다」를
   뜻한다는 근거가 이것이다. **앞으로도 새 시험을 만들면 음성 대조를 한 번 돌려볼 것.**
```
  세 목록 × 화주 5종(유효 / 기한없음 / 종료됨 / 미체크 / 개인) 배지 판정   전부 통과 · 예외 0
  카운트: 미체크는 조회 단계에서 제외(4행) · 종료된 계약은 카운트에서 제외(3개)
          🟢 **종료일 == 오늘은 유효**로 센다(경계 실측)
  대시보드 카드 3상태(3개 / 0개 / 조회 실패) 실제 페이지 JSX 렌더링 — 실패가 0개와 눈으로 구분
  명단: 기본 접힘 → 클릭 펼침 → 링크가 /admin/companies/<id> → 재클릭 닫힘
```
⚠️ **검증 중 내 하네스가 원칙 21번에 걸렸다** — `force-dynamic` 만으로는 supabase-js
   내부 `fetch` 가 Next Data Cache 를 타서 **낡은 3행**을 돌려줬다. `createServiceClient()`
   로 고치고 다시 쟀다(실제 대시보드 라우트는 원래 그것을 써서 면역이다).
⚠️ **이전 라운드의 dev 서버 둘이 낡은 빌드를 물고 살아 있었다** — 전부 종료하고 새 포트에서
   다시 쟀다. 안 그랬으면 3라운드 검증이 무의미했다. 🔴 **검증 전에 포트를 실측할 것.**

**═══ ⑨ 완료조건 ═══**
```
   1  _migrations 27행                            ✅ Actions apply 로그
   2  등록 == 수정 항목 집합                       ✅ **실제 모듈 평가** 21항목(정규식 아님)
   3  화면에 자체 필드 배열 0건                    ✅ 세 입구가 전부 lib/companyFields.ts import
   4  companies.status 값 목록 무변경              ✅ diff 0줄 + 마이그레이션 단언
   5  승인 화주에 빈칸 안 남음                     ⚠️ **사용자 확인 항목**(아래 ⑩)
   6·7 필터·배지 판정                             ✅ 브라우저 실측
   8  화주포털·공개 화면 diff                      ✅ **0줄**
   9  견적서 print 2종·엑셀에 정기계약 문자열      ✅ **0건**
  10  프리렌더 실패 **44**                         ✅ 기준선 동일
  11  globals.css 삭제된 줄 0                      ✅ 파일 변경 자체가 0
  13  낙관적 잠금 유지                             ✅ optimisticUpdate + ConflictWarning 그대로
```

**═══ ⑩ 아직 안 한 것 / 사용자 확인 대기 ═══**
```
  ⚠️ 완료조건 5   `/apply` 신청 → 승인 시 빈칸이 안 남는지(특히 「월 예상 운송건수」에
                  `30` 처럼 숫자만 넣었을 때). **이 환경에서 확인할 수 없는 유일한 항목**
  ⚠️ staff 롤     `staff` 계정으로도 화주 등록·수정이 되는지(권한 회귀)
  🔴 안 만든 것   정기계약 전용 운임 · 자동 반복 발주 · 계약 만료 알림 — **범위 밖**(지시서 §5-12)
  🔴 안 건드린 것 개인고객(`individual_customers`)에는 같은 필드를 만들지 않았다(범위 밖)
  🔴 CRM 항목     업종·잠재등급·주요 노선 등 **새 항목을 늘리지 않았다 — 36차 범위**다.
                  이번은 **있는 항목을 맞춘 것**이다
  ⚠️ 페이지네이션 화주 540건을 한 번에 그리는 구조가 이 화면의 진짜 무게다. 2라운드 수정은
                  증상 완화이고 근본 해결은 페이지네이션(§5 「유료 플랜 전환 / 페이지네이션」).
                  그게 되면 **폼 열림 시 목록 미렌더는 없애도 된다**
```

**작성일: 2026-07-23** (최종 갱신: 2026-09-09, Claude Code 세션에서 직접 갱신 —
**차수 없음: 이용약관 제19조 1·2·7항 개정 (terms-v4 → terms-v5)** — PR #138 merge
`a9b5ccc`. 🔴 **DB 0 · 화면 코드 0 · 마이그레이션 0.** 고친 파일은
`lib/legal/terms.ts` · `lib/legalInfo.ts` **둘뿐**이다. 커밋 2개(첫 커밋 = 사용자 픽스
문안 그대로 / 둘째 커밋 = 그때 보고한 우려 셋을 사용자 확정대로 반영).

**═══ ① 무엇이 바뀌었나 ═══**
```
       전                                        후
  1항  차주·보험자 + 도착지 가격 + 보험 안내   →  가액 신고제 + **미신고는 법령에 따름**
  2항  가액 신고제                             →  차주·보험자 + 한도는 **제1항** + 보험 안내
  7항  「제2항부터 제6항까지」                  →  「**제1항부터** 제6항까지」
  3~6항 고가물 · 취급 협의 · 간접손해 · 면책    →  한 글자도 안 건드렸다
```
**최종 문안(정본은 `lib/legal/terms.ts`, 아래는 대조용)**
- **1항** 「고객이 운송 의뢰 시 화물의 가액을 신고하고 회사가 이를 승낙한 경우에는 그
  신고 가액을 배상 한도로 합니다. 가액을 신고하지 아니한 경우에는 상법 등 관계 법령이
  정하는 바에 따릅니다.」
- **2항** 「화물의 멸실·훼손 또는 연착에 대한 배상책임은 **실제 운송을 수행한 차주 및 그
  보험자에게 있으며**, 그럼에도 상법 등 관계 법령이 정하는 바에 따라 회사가 배상책임을
  지게 되는 경우 그 배상한도는 제1항을 따릅니다. 회사는 고객의 요청이 있는 경우
  적재물배상책임보험의 가입 사실과 보상 한도를 안내합니다.」
- **7항** 「**제1항부터 제6항까지**는 회사의 고의 또는 중대한 과실로 인한 손해에는
  적용되지 않습니다.」

**═══ ② 🔴 1·2항의 순서를 되돌리지 말 것 ═══**
2항이 「그 배상한도는 **제1항**을 따릅니다」로 1항을 가리킨다. 순서를 되돌리면 그 참조가
**자기 자신을 가리키게** 된다. 🔴 「중복이니 정리하자」로 한쪽을 지우는 것도 안 된다 —
제18조 1항·제19조 2항·제19조 1항이 각각 **누가 지는가 / 그럼에도 회사가 질 때 /
얼마까지인가**를 나눠 맡는다.

**═══ ③ 🔴 「인도할 날의 도착지 가격」을 되살리지 말 것 ═══**
옛 1항은 「그 배상액은 상법 등 관계 법령이 정하는 바에 따르되 **인도할 날의 도착지
가격**을 기준으로 산정합니다」였다. **상법 제137조 1항의 법문 그대로**이고 내가 지어낸
말이 아니다 — 그런데 **사용자 판단으로 뺐다**(*"일반적이지 않고 애매하다"*, 2026-09-09).
화주가 읽고 금액을 가늠할 수 없다는 것이 이유다.
🟢 **빼도 회사 지위는 같다** — 상법 제137조는 약관에 없어도 그대로 적용된다. 적고 안
적고의 차이는 「약관이 그 기준을 안내하느냐」뿐이고, 1항 둘째 문장이 「법이 정한다」로
그 자리를 가리켜 준다.
🔴 **거래명세서·세금계산서 기준으로 바꾸는 안은 일부러 안 골랐다**(선택지로 제시했고
사용자가 (A)를 골랐다) — 도착지 시가보다 낮아질 수 있어 **법정 기준을 약관으로 깎는
것**이 되고, 약관규제법 제7조 검수 항목이 하나 늘어난다.
⚠️ 1항과 2항에 「상법 등 관계 법령이 정하는 바에 따라/따릅니다」가 **두 번** 나온다 —
뜻이 겹치는 것은 아니다(2항 = 회사가 지게 되는 경우 / 1항 = 그 한도). 거슬리면 1항을
「법령이 정하는 바에 따릅니다」로 줄여도 뜻은 같다(보고했고 그대로 뒀다).

**═══ ④ 🔴 7항의 「제1항부터」를 「제2항부터」로 되돌리지 말 것 ═══**
사용자 픽스 지시는 1·2항만이었고 7항은 손대지 말라는 뜻이었는데, **숫자를 그대로 두면
덮는 대상이 뒤바뀐다는 것을 보고**했고 사용자가 고치기로 확정했다.
```
  전  1항 차주·보험자 (예외 밖)   ·  2항 가액 신고 한도 (예외 안)
  후  1항 가액 신고 한도          ·  2항 차주·보험자      → 둘 다 예외 안
```
🔴 **가액 신고 한도는 전형적인 책임제한 조항**이라 고의·중과실 예외 밖에 두면
*"회사가 고의로 훼손해도 신고 가액까지만"* 으로 읽혀 **그 항만 약관규제법 제7조로
무효**가 될 수 있다. 🟢 2항(책임 귀속)이 함께 덮이는 것은 오히려 안전한 쪽이다.
🔴 **항을 더하거나 빼면 이 범위를 반드시 같이 고칠 것** — 66차가 실제로 겪은 자리다
(「제3항부터 제7항까지」가 항 삭제로 조용히 어긋났다).

**═══ ⑤ 🔴 2항 앞 문장의 굵게 표시 · 「제1항」 표기 ═══**
- 🔴 **「실제 운송을 수행한 차주 및 그 보험자에게 있으며」의 굵게 표시를 지우지 말 것** —
  화주가 약관을 훑을 때 **책임 귀속이 눈에 들어오는 자리**다. 사용자가 준 픽스 문안에는
  강조가 없어서 첫 커밋에 평문으로 넣었다가, 보고 후 확정으로 되살렸다.
- 🔴 **뒤의 조건절(「그럼에도 …지게 되는 경우」)에는 강조를 붙이지 않는다**(2026-09-07
  확정 그대로). 강조는 앞 문장 하나뿐이다.
- ⚠️ 처음 받은 문안의 표기가 **「19조 1항」**이었고(문서의 다른 참조는 전부 「제19조」·
  「제1항」 형식), 사용자 확정으로 **「제1항」**으로 맞췄다.

**═══ ⑥ 🔴 판본은 terms-v5 다 ═══**
`lib/legalInfo.ts` 의 `TERMS_VERSION` 을 **`terms-v4` → `terms-v5`** 로 올렸다.
배상 한도의 근거가 **상법 제137조 → 1항(신고 가액)** 으로 옮겨진 실질 변경이고, 이
저장소 규칙이 *"판본은 파일 diff 가 아니라 **게시되는 문서 내용**을 기준으로 올린다"*
이기 때문이다(69차가 `privacy-v2` 에서 세운 규칙).
🔴 **merge = 게시가 아니다.** `terms-v1` 로 저장된 동의가 **실제로 3건** 있고, 제3조
3항이 「고객에게 불리한 변경」에 **적용일 30일 전 공지**를 요구한다. **이번 변경이
「불리한 변경」인지는 변호사 검수 항목으로 남겼다** — 그렇게 판단되면 30일 공지는
**v5 기준**으로 계산된다(v3·v4 가 게시 전이라 중간 판본은 따로 공지하지 않는다).
🟢 **기존 동의 3건의 기록은 손대지 않았다** — 판본만 올리면 그분들이 본 문서가 특정된다.

**═══ ⑦ ⚠️ 항 번호를 쓴 옛 기록이 또 낡았다 ═══**
```
  옛 기록 표기                        지금
  「제19조 1항의 조건절」              → **2항**이다(1·2항이 자리를 바꿨다)
  「제19조 7항 … 제2항부터 제6항까지」  → 「**제1항부터** 제6항까지」
  「제19조 2항이 보험 안내」            → 그 문장은 **2항 끝**에 그대로 있다(우연히 맞다)
```
🔴 **번호가 아니라 내용으로 찾을 것.** 66차·PR #133 에 이어 **세 번째**로 밀린 것이다.
🟢 화면에 렌더링되는 상호참조는 **2항의 「제1항」 하나뿐**이고, 코드의 「제19조 N항」은
전부 주석이다(`lib/insuranceInfo.ts` · `components/landing/data.ts` — 둘 다 이미
「낡았다」고 표시돼 있다). 그래서 이번 이동으로 깨진 참조는 없다.

**═══ ⑧ 🟢 검증 ═══**
```
  npx tsc --noEmit                    ✅ exit 0
  npm run build (더미 환경변수)         ✅ exit 0 · 프리렌더 실패 0
  /terms 실제 렌더링                   ✅ 1항·2항·7항 모두 최종 문안대로
     🔴 배열 순서만 보고 끝내지 않았다 — `next start` 로 띄워 HTML 을 받아 확인했다.
        2항은 굵게 표시 때문에 텍스트가 세 조각으로 쪼개져 나오는데 그게 정상이다.
  「도착지 가격」·「19조 1항」·「제2항부터」 잔존   ✅ 전부 0건
  다른 조문 diff                       ✅ 0줄 (제18조·제20조 포함)
  「제19조 N항」 상호참조                ✅ 코드에는 주석 3곳뿐 · 렌더링되는 참조 0
```
⚠️ **프리렌더 기준선은 44 그대로다**(32차에 `/admin/change-password` 하나가 늘었다).
이번 차수는 화면을 더하지 않았다.

**═══ ⑨ ⚠️ 사용자가 코드 밖에서 할 일 ═══**
```
  🔴 게시 시점·30일 공지 결정        merge 는 게시가 아니다(위 ⑥)
  🔴 변호사 검수                     지점이 **여섯 → 일곱**이 됐다 —
                                    ⑦ 「미신고 건의 한도를 약관이 법에 위임한 것」이
                                    약관규제법상 문제가 없는지(우리 판단은 문제 없음:
                                    법정 기준을 그대로 따르는 것이라 깎지 않았다)
```

**═══ ⑩ ⚠️ 일부러 안 한 것 ═══**
- **거래명세서·세금계산서 기준 한도**(위 ③) — 선택지로 제시했고 사용자가 안 골랐다.
- **부록(「v20 이후 무엇이 바뀌었나」) 갱신** — `/merge` 규칙상 매 차수에 손대지 않는다.
  ⚠️ 그래서 부록의 약관 행은 **여전히 terms-v4** 라고 적혀 있다. 🔴 **본문(§5-5)이
  정본이고 부록은 전면 갱신 때만 다시 쓴다** — 부록을 근거로 판본을 되돌리지 말 것.
- **제18조·제20조** — 한 글자도 안 건드렸다.
)

**작성일: 2026-07-23** (최종 갱신: 2026-09-09, Claude Code 세션에서 직접 갱신 —
**32차: 내부관리 아이디 로그인 + 직원 비밀번호 재발급** — PR #137 merge `1f97fc7`.
🔴 **DB 변경 있음**(`staff_accounts` 에 `login_id`·`must_change_password` + 소문자 유니크
인덱스, `_migrations` **26행**) · 🟢 **화주포털 0줄 · 공개 화면 0줄 · `globals.css` 0줄.**
실사용 리뷰 라운드 0.

**═══ ① 🔴 차수가 밀렸다 — 32차가 「내부 정합 ① 견적·오더」가 아니다 ═══**
`CLAUDE.md`·`HANDOFF.md` 는 32차를 「내부 정합 ① 견적·오더」로 예고했는데, **사용자가
내부시스템 개편을 먼저 하기로 확정**(2026-09-09)해서 내부 정합 3건이 통째로 한 칸씩
밀렸다(**33·34·35차**). 🔴 **「차수는 예고가 아니라 실행 순서로 붙인다」는 원칙 그대로다** —
아홉 번째 밀림이다.

**═══ ② 이 차수의 진짜 이유는 「1번」이 아니라 「2번」이다 ═══**
```
  1  이메일을 매번 다 쳐야 한다                         (불편)
  2  🔴 비밀번호를 잊으면 아무도 풀어줄 수 없다          ← 이것 때문에 했다
  3  화주포털은 이미 아이디 로그인이라 방식이 다르다      (일관성)
```
`RESEND_API_KEY` 미등록 · SMTP 미연결이라 **재설정 메일을 보낼 방법이 없었다.** 직원 한
명이 비밀번호를 잊으면 회사가 그 계정을 영영 못 쓰는 상태였다. 🔴 **「아이디가 편해서
했다」로 기억하지 말 것** — 그러면 재발급 기능을 곁다리로 여기게 된다.

**═══ ③ 🔴 화주포털 방식을 일부러 안 베꼈다 — 되돌리지 말 것 ═══**
포털은 아이디를 **합성 이메일**(`{login_id}@wecarry-portal.internal`)로 바꿔 Auth 에
등록한다. 화주는 실제 이메일이 없어도 계정이 나가야 하기 때문이다.
🔴 **직원은 사정이 정반대다** — 실제 회사 메일이 있고, 그것이 **나중에 Resend 를 켰을 때의
유일한 복구 경로**다. 합성 이메일로 갈아치우면 이번 차수를 하게 만든 그 구멍을 스스로
다시 판다. 그래서 **매핑 방식**이다.
```
  직원이 입력           wejjy + 비밀번호
  서버가 하는 일         login_id 로 행을 찾아 → 그 행의 email 로 로그인시킨다
  Auth 에 저장된 이메일   ✅ 그대로 (한 글자도 안 바꿈)
```
🔴 **`staff_accounts.email` 은 이제 로그인 자격이 아니라 연락처다.** 화면 라벨도 세 곳에서
「담당자 이메일(연락용)」로 바꿨다(발급 폼 · 수정 모달 · 내 계정). **「포털과 통일하자」가
나오면 이 항목을 먼저 읽을 것.**

**═══ ④ 🟢 착수 전 확인 — 2-0 갈림길이 실측으로 사라졌다 ═══**
지시서는 `staff_accounts` 에 `anon` 전체 조회 정책이 살아 있으면 설계가 갈린다고 했다
(가: 뷰로 대체 / 나: 별도 표 `staff_login_ids`). **실측 결과 그 정책은 없다**(21차에 정리됨).
```
  staff can read own row     {public}         SELECT  (auth.uid() = id)
  staff_all_staff_accounts   {authenticated}  ALL     is_active_staff()
  🔴 set local role anon 으로 실제 조회 → 0행
```
🔴 **「정책을 안 건드렸으니 괜찮다」로 넘기지 않고 실제로 재봤다**(완료조건 4). `login_id` 도
같은 정책 아래라 그대로 0행이다. 그래서 **별도 표를 만들지 않고 `staff_accounts.login_id`
로 갔고 정책은 한 줄도 안 건드렸다.**
⚠️ **`anon` 은 여전히 이 표에 DML GRANT 를 다 갖고 있다**(RLS 가 막는 구조 — ③ 차수 미정).
⚠️ **확인 4번만 지시서 전제와 달랐다** — `/admin/my-account` 에 비밀번호 변경이 **있다.**
다만 **현재 비밀번호를 알아야** 하는 화면이라 임시 비밀번호를 받은 직원을 가둘 수 없어서
`/admin/change-password` 를 따로 만들었다(포털과 같이 **메뉴에는 안 넣는다**).

**═══ ⑤ 🔴 저장소가 public 이다 — 이것이 이번 차수의 설계를 두 번 바꿨다 ═══**
`visibility: public` 을 API 로 확인했다. **Actions 로그는 로그인 없이 누구나 읽는다.**
1. 🔴 **아이디 값을 마이그레이션에 넣지 않았다**(지시서 2-2 와 다름). 커밋하면 **직원 실명
   4명과 로그인 아이디 4개가 git 이력에 영구 공개**되고, 로그인 화면이 공개돼 있으니
   **유효한 아이디 목록을 그대로 넘기는 셈**이다. 값 채우기는 Supabase SQL Editor 에서
   한 번 했고(사용자), `_verify.sql` ⑪ 로 확인했다.
2. 🔴 **`_verify.sql` ⑪ 의 조회를 전부 마스킹했다** — `홍**` / `eg***@d***` / `we***`.
   **이 마스킹을 풀지 말 것.** 사람이 누구인지 아는 것은 사용자이지 이 로그가 아니다.
🔴 **앞으로도 직원 실명·이메일·아이디·전화번호를 마이그레이션이나 verify 출력에 그대로
쓰지 말 것.** 이 저장소는 private 가 아니다.

**═══ ⑥ ⚠️ 아이디 넷이 전부 `we` 로 시작한다 — 화면 문구를 지시서와 다르게 적었다 ═══**
```
  전준영 admin  wejjy      김도연 admin  wekdy
  정제원 admin  wejjw      홍길동 staff  wehhh
```
지시서는 화면 도움말에 「🔴 we 로 시작하지 마십시오」를 적으라고 했는데, **실제 아이디가
전부 `we` 로 시작한다**(사용자 확정). 화면이 반대로 안내하면 다음 사람이 헷갈린다.
🔴 **그래서 이렇게 적었다** — 「영문 소문자와 숫자만 · 4~20자 · 첫 글자는 영문. 화주포털
아이디(we+숫자 8자리)와 같은 형태로는 만들지 마세요.」 `LOGIN_ID_HELP` 상수 한 곳이다.
🟢 실제 충돌 위험은 낮다 — 포털은 `we`+**숫자 8자리**, 직원은 `we`+**영문**이고 표도
로그인 화면도 완전히 별개다.
🟢 **아이디는 언제든 바꿀 수 있다**(사용자 질문에 답한 내용) — Auth 이메일과 무관한 평범한
컬럼이라 바꿔도 비밀번호는 그대로다. 화면에도 그렇게 적어 뒀다. **포털 아이디는 반대로
바꾸기 어렵다**(아이디가 곧 Auth 이메일이다).

**═══ ⑦ 🔴 실패 응답을 한 종류로 뭉갠 것은 의도다 ═══**
「없는 아이디」·「비밀번호 틀림」·「퇴사 처리됨」이 **문구·상태코드·응답시간 모두 같다.**
갈라 보여주면 아이디를 넣어보는 것만으로 **누가 재직 중인지가 새어 나간다.**
🔴 **「사용자에게 친절하게」를 이유로 나누지 말 것.** 형식 위반(`we_jjy` 등)도 DB 조회조차
하지 않고 같은 실패로 떨어진다 — 알려주면 규칙을 모르는 공격자에게 규칙을 가르쳐 준다.
🔴 **없는 아이디일 때도 비밀번호 확인을 한 번 태운다**(`no-such-staff@wecarry-admin.invalid`).
안 태우면 「빨리 실패했다」는 것만으로 그 아이디가 없다는 게 드러난다. `.invalid` 는 표준이
예약한 TLD 라 누구도 그 주소로 계정을 만들 수 없다.
🟢 **잠금 문구만 다르다** — 판정 근거가 「방금 그 사람이 입력한 문자열」이라 계정 존재를
말해주지 않는다(없는 아이디로 10번 틀려도 똑같이 잠긴다. 시험으로 확인함).

**═══ ⑧ 🔴 실패 횟수 제한에 표를 만들지 않았다 — 한계를 알고 쓸 것 ═══**
같은 아이디로 **10분에 10번 실패 → 5분 잠금**, 값은 **프로세스 메모리**(`LoginAttemptTracker`).
```
  🔴 IP 를 저장하지 않는다   IP 는 개인정보라 저장하면 처리방침 제2조를 고쳐야 한다(범위 밖)
  🔴 표를 안 만든 둘째 이유   완료조건 1이 _migrations 26행이라, 표를 만들면 27행이 된다
  ⚠️ 한계                   Vercel 은 인스턴스가 여럿이라 완벽히 못 막는다 —
                            한 연결로 반복해 두드리는 경우를 막는 과속방지턱이지 벽이 아니다
  ⚠️ 대가                   아이디를 아는 사람이 남의 계정을 5분간 잠글 수 있다
                            (관리자 계정이 3개라 감당 가능하다고 봤다)
```
🔴 **제대로 된 지속 저장이 필요해지면 표와 함께 별도 차수다.** 그때도 **IP 를 넣으려면
처리방침을 같이 고쳐야 한다**는 것을 잊지 말 것.

**═══ ⑨ 🔴 로그인은 목으로 흉내내기 어렵다 — 어떻게 쟀는가 ═══**
판정만 **순수 함수**(`lib/staffLogin.ts` 의 `resolveStaffLogin`)로 떼어내고 라우트는 결과를
응답으로 옮기기만 하게 했다. 그래서 다섯 갈래를 주입으로 전수 시험할 수 있다.
🔴 **목이 실제보다 헐거우면 검증이 통째로 무의미해진다**(원칙 56번). 그래서 목 Auth 는
**「이메일과 비밀번호가 정확히 맞을 때만」** 성공을 돌려준다 — 존재하지 않는 이메일에는
비밀번호가 무엇이든 실패한다.
```
  다섯 갈래         없는 아이디 / 비번 틀림 / 퇴사 / 성공 / 성공(임시 비번)
  완료조건 7        ①=②=③ **바이트 단위** 일치 · 셋 다 401                     ✅
  완료조건 6        응답 전체에 '@' 0건 · 도메인 문자열 0건                     ✅
  응답시간          없는 아이디·퇴사에도 signIn 1회씩 · 퇴사자 이메일로는 안 씀 ✅
  형식 위반 7종     전부 같은 실패 · DB 조회조차 안 함                          ✅
  정규화            "  WEJJY  " → 성공 (소문자 유니크와 짝)                     ✅
  잠금 6항목        상한·해제·기록 초기화·구간 만료·없는 아이디도 잠김          ✅
  통과 26 · 실패 0
```
⚠️ **진짜 Supabase Auth 로 되는지는 이 환경에서 잴 수 없다** — 사용자 확인 항목이다.

**═══ ⑩ 🔴 되돌리면 안 되는 구현 지점 ═══**
- 🔴 **재설정 API 는 서버에서 `role === 'admin'` 을 다시 확인한다**(원칙 25번). 화면에서
  버튼을 숨기는 것만으로는 콘솔에서 직접 부르는 것을 못 막는다. **대상이 실제 직원
  계정인지도 확인한다** — 안 하면 화주포털 계정 비밀번호까지 바꿀 수 있다.
- 🔴 **재설정은 비밀번호와 `must_change_password` 를 `updateUserById` **한 호출로** 함께
  바꾼다.** 따로 돌다 한쪽만 성공하면 「임시 비밀번호인데 아무 화면이나 들어가지는」
  상태가 생긴다. ⚠️ `updateUserById` 는 `user_metadata` 를 **병합이 아니라 교체**하므로
  `role`·`status` 미러링을 되살리는 보정이 뒤따른다 — 그 블록을 지우지 말 것.
- 🔴 **middleware 의 강제변경 검사는 재직 확인 *뒤*에 있다.** 앞에 두면 퇴사자가 차단
  대신 변경 화면으로 간다. 판정은 `user_metadata.must_change_password === true` 이고
  값이 없으면 막지 않는다(켜는 곳이 재설정 API 한 곳뿐이라 그게 맞다).
- 🔴 **`/admin/change-password` 라우트를 지우지 말 것** — middleware 가 여기로 보내는데
  화면이 없으면 404 에 갇힌다. **`PUBLIC_PATHS` 에 넣지도 말 것**(로그인이 필요한 화면).
  `PUBLIC_PATHS` 3개는 그대로다.
- 🔴 **해제는 클라이언트가 직접 안 한다** — `/api/admin/confirm-password-change` 가 토큰을
  **서버에서 직접 검증**한 뒤 끈다(원칙 30번. 화주쪽과 같은 구조, **표는 다르다**).
  DB 와 `user_metadata` 를 **둘 다** 끈다 — 한쪽만 끄면 계속 되돌아온다.
- 🔴 **판별자를 `kind: "ok"|"failed"|"locked"` 문자열로 뒀다** — 이 저장소는 `strict: false`
  라 `if (!result.ok)` 로는 타입이 **안 좁혀진다**(실제로 컴파일 에러가 났다). boolean
  판별자로 되돌리지 말 것.
- ⚠️ **`.ilike("login_id", id)` 를 쓴다** — supabase-js 에 `lower(col) = ?` 를 거는 방법이
  없다. `_`·`%` 가 와일드카드가 되지만 `LOGIN_ID_PATTERN` 이 그 앞에서 이미 걸러낸다.
  **패턴 검사를 빼면 이 자리가 구멍이 된다.**
- 🔴 **성공 경로에서 `setLoading(false)` 를 부르지 않는다**(PR #129 와 같은 이유).
  로그인 화면의 `LEGACY_SAVED_EMAIL_KEY` 제거도 지우지 말 것 — 옛 키에는 **이메일**이
  들어 있어서 그대로 읽으면 아이디 칸에 이메일이 채워진다.

**═══ ⑪ 🟢 검증 · 기준선 ═══**
```
  npx tsc --noEmit                     ✅ exit 0
  npm run build (더미 환경변수)          ✅ exit 0 · 프리렌더 실패 0
  npm run build (환경변수 없음)          프리렌더 실패 44
     ⚠️ 기준선 43 에서 **+1**. 늘어난 항목이 정확히 `/admin/change-password` 하나이고
        다른 43개와 원인이 같다(로컬에 Supabase 환경변수 없음). 회귀가 아니다.
     🔴 **새 기준선은 44 다** — 다음 차수는 이 값과 비교할 것.
  로그인 판정 전수 시험                  ✅ 26/26
  _migrations                          26행
  화주포털·공개 화면·globals.css         diff 0줄
  app/admin/login/page.tsx 의 Auth 직접 호출   0건 (완료조건 5)
  anon 이 읽는 staff_accounts 행수        0 (완료조건 4, 실측)
  아이디 없는 재직 직원                   0명 · 규칙위반 0 · 서로다른 아이디 4 (완료조건 3)
```

**═══ ⑫ ⚠️ 이 환경의 제약 — CLAUDE.md 기록이 한 번 틀렸다 ═══**
`CLAUDE.md` 5장은 *"세션이 직접 돌리고 로그까지 읽을 수 있다"* 고 적고 있는데,
**MCP 서버가 끊긴 동안에는 성립하지 않는다.** 그때 실측한 것:
```
  workflow_dispatch (REST)   403 Resource not accessible by integration — 토큰에 actions:write 없음
  Actions 로그 (REST)         차단 — results-receiver.actions.githubusercontent.com 을
                              egress 정책이 CONNECT 403 으로 막는다
  읽히는 것                    실행 목록·단계별 성공/실패 결론만
```
🟢 **`mcp__github__actions_run_trigger` · `get_job_logs` 가 붙어 있으면 둘 다 된다** — 이번
차수도 그것으로 verify·dry-run·apply 를 전부 직접 돌렸다. 🔴 **MCP 가 끊겼을 때 「못 한다」로
결론내지 말고 ToolSearch 로 다시 붙었는지 먼저 볼 것**(이번에 실제로 다시 붙었다).
⚠️ **로그가 길면 `tail_lines` 로는 중간이 묻힌다** — `_verify.sql` 의 새 항목을 **파일 맨
끝**으로 옮겨서 해결했다(⑩ 운임 165칸 덤프가 그 앞에 있다).

**═══ ⑬ ⚠️ 사용자가 코드 밖에서 할 일 ═══**
```
  🔴 admin 롤 계정을 최소 두 개 유지     서로 비밀번호를 풀어줄 수 있다. 하나뿐이면
                                        그 사람이 잊는 순간 회사가 잠긴다 (지금 3개)
  🔴 최후 수단                          Supabase 대시보드 → Authentication → 해당 사용자
                                        → 비밀번호 재설정. **이 경로를 아는 것 자체가 대비책**
  ⚠️ 운영 확인 (배포 후)                아이디 로그인 · 실패 문구 동일 · 아이디 변경 후 로그인 ·
                                        재설정 후 강제 변경 · staff 롤은 /admin/staff 안 보임
```

**═══ ⑭ ⚠️ 일부러 안 한 것 ═══**
- **SMS 비밀번호 찾기 · Resend 활성화 · 화면별 권한 세분화** — 지시서 범위 밖.
- **재발급 안내 문자** — 화주쪽 재발급은 SMS 미리보기를 만드는데 **직원쪽은 안 만들었다**
  (관리자가 직접 알려주는 구조). 🔴 「화주쪽에 있는데 왜 없지」 하지 말 것.
- **`/admin/my-account` 의 비밀번호 변경** — 그대로 뒀다. 강제 변경만 새 화면이다.
- **지속 저장 실패 제한 · IP 기반 제한** — ⑧ 참고.
)
