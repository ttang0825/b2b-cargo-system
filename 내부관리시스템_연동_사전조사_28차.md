# WeCarry — 28차 사전조사 보고서 (내부관리시스템 연동)

| | |
|---|---|
| 차수 | **28차** — 조사 전용 |
| 선행 | 27차 merge 완료 (PR #105, squash `5dbead2`) |
| 기준 커밋 | `19c1c27` (main) |
| 코드 변경 | **0** — `CLAUDE.md` 정정 4건만 (1장 허용 예외) |
| DB 변경 | **0** — `verify`(읽기 전용) 2회만 실행 |

---

## 0. 착수 전 확인 결과표 (완료조건 1)

🔴 **지시서 전제 중 X 가 셋이다.** 결과를 그대로 남긴다.

| # | 지시서 전제 | 결과 | 실측 |
|---|---|---|---|
| 1 | `portal_order_requests` 38컬럼 | **O** | 38 (`verify` 직접 카운트) |
| 2 | 신규 3컬럼은 `collection_method`·`direct_collection_point`·`dropoff_arrival_type` | **O** | 36·37·38번, 전부 nullable text |
| 3 | `CLAUDE.md` §5 28차 항목이 「35컬럼」으로 낡음 | **O** | 정정함 |
| 4 | `getSettlementDisplayLabel()` 을 **admin 8개 파일**이 쓴다 | 🔴 **X** | **총 8개 = admin 6 + 포털 2.** 게다가 admin 중 `orders/page.tsx` 는 **import 만 하고 호출하지 않는다**(죽은 import) → 실제 호출 admin **5곳** |
| 5 | 화주요청 목록·견적 프리필에 27차 3컬럼이 들어갔다 | **O** | 렌더링 확인 (§2) |
| 6 | `23:59` 자리 채움을 그대로 그리는 화면이 있을 것 | 🔴 **X** | **전 화면 0건.** 다른 문제가 대신 있다 (§3) |
| 7 | `?embed=1` 분기가 관리자 print 에 있다 | **O** | `app/admin/quotes/[id]/print/page.tsx:68` |
| 8 | 배송지 화면 2곳이 `location_name` 을 조회만 하고 안 그린다 | 🔴 **X(부분)** | 조회는 함. 그런데 **더 큰 문제는 `contact_name`·`contact_phone`·`address_detail`·`notes` 를 조회조차 안 하는 것** |
| 9 | `customer_presets` 를 관리자가 볼 수 있는가 | **X** | 읽는 관리자 화면 **0개** |
| 10 | `HANDOFF.md` 가 저장소 루트에 있다 | **O** | 20,768 bytes |
| 11 | 54차 「9화면 × 12지표」 스크립트가 남아 있다 | **O** | 스크래치패드 `admin_cmp.mjs` |

### 운영 DB 실측 (완료조건 2)

```
  portal_order_requests   38컬럼        customer_locations  11컬럼
  quotes                  53            orders              44
  dispatches              45            invoices            39
  customer_presets         7컬럼 · cargo 1 · request 1 (실사용 시작됨)
  _migrations             21행
```

🔴 **`billing_cycle` 이 전 테이블에서 `per_order` 100% — `monthly` 는 0건이다.**
`quotes` 6 · `orders` 3 · `dispatches` 3 · `invoices` 3 전부. 마감일(`billing_cutoff_day`)을
설정한 화주는 **1명**. → **월정산은 아직 한 건도 돌지 않았다**(D-1 권장안의 근거).

⚠️ **`portal_order_requests` 3행이 전부 `승인됨` 이고 27차 3컬럼이 전부 NULL 이다** —
그 3건은 4라운드 이전에 들어온 것이다. 23차 조사의 「대기중 2 · 승인됨 1」에서 바뀌었다.
🔴 **그래서 새 컬럼의 렌더링은 실데이터로 확인할 수 없고 목으로 해야 한다.**

---

## 1. 🔴 이 조사의 결론 세 줄

1. **승계는 거의 안 끊긴다.** 견적 전환 프리필이 32컬럼을 읽어 대부분 이어받는다.
2. 🔴 **끊기는 곳은 「승인 전」과 「배차」 두 군데다.** 담당자가 **승인·반려를 판단하는 화면**에
   품목·상하차조건·담당자 연락처가 없고, **배차 화면**에 「당착/내착」이 도달하지 않는다.
3. 🔴 **월정산은 아직 0건이라 지금이 정하기에 가장 싼 시점이다.**

---

## 2. 화주 입력 → 관리자 표시 전수 대조 (완료조건 3·4·5)

### 2-1. `portal_order_requests` 38컬럼

관리자 화주요청 목록(`/admin/portal-requests`)은 **19컬럼을 조회하고 9개 열을 그린다.**
🔴 **이 화면에는 상세 모달이 없다** — 담당자는 이 한 화면에서 승인/반려를 판단한다.

| # | 컬럼 | 판정 | 근거 |
|---|---|---|---|
| 4·5 | `origin`·`destination` | 🟢 보인다 | 구간 열 |
| 6 | `vehicle_type` | 🟢 | 차량 열 |
| 15 | `body_type` | 🟢 | 차량 열 |
| 35 | `loading_type` | 🟢 | 「혼적가능」 배지 |
| 8 | `requested_pickup_at` | 🟢 | 희망 상차일 |
| 14 | `requested_dropoff_at` | 🟢 | 희망 하차일 |
| 36·37 | `collection_method`·`direct_collection_point` | 🟢 **27차 4라운드** | 정산방식 열 (렌더링 확인) |
| 38 | `dropoff_arrival_type` | 🟢 **27차 4라운드** | 「내착 · 시각 무관」 (렌더링 확인) |
| 9 | `notes` | 🟢 | 특이사항 열 |
| 10·11 | `status`·`staff_note` | 🟢 | 상태 배지 · 반려 사유 |
| **7** | **`item` (품목)** | 🔴 **안 보인다** | **조회는 하는데 그리지 않는다** |
| **17·18** | **`load_condition`·`unload_condition`** | 🔴 **안 보인다** | 조회조차 안 함 |
| **19** | **`item_condition` (물품특성)** | 🔴 **안 보인다** | 조회조차 안 함 |
| **20** | **`transport_time`** | 🔴 안 보인다 | 조회조차 안 함 |
| **22** | **`trip_type` (왕복/편도)** | 🔴 안 보인다 | 조회조차 안 함 |
| **23·24** | **`waiting_minutes`·`waypoint_count`** | 🔴 안 보인다 | 조회조차 안 함 |
| **29~34** | **상·하차지 담당자 6필드** | 🔴 **안 보인다** | 조회조차 안 함 — 🔴 **47차가 제3자 제공 동의까지 받은 값이다** |
| 25~28 | `*_sido`·`*_sigungu` | ⚠️ 볼 필요 없다 | 주소 문자열에 이미 포함 |
| 21 | `urgency` | ⚠️ 볼 필요 없다 | 폐지된 항목(과거 데이터 보존용) |
| 1·2·3·12·13·16 | id·company_id·requested_by·order_id·created_at·quote_id | ⚠️ 볼 필요 없다 | 내부 연결값 |

🔴 **넣을 자리 제안** — 표에 열을 더하면 이미 `minWidth:1030` 인 표가 무너진다.
**행 펼치기(27차 견적 카드와 같은 방식) 또는 상세 모달**을 신설하고 거기에
품목 · 상하차조건 · 물품특성 · 왕복/편도 · 대기/경유 · **상하차지 담당자 6필드**를 넣는다.
원칙 18번(목록+상세모달)이 이 저장소의 기존 패턴이다.

### 2-2. `customer_locations` 11컬럼

관리자에서 읽는 곳은 **화주 상세**(`companies/[id]`)와 **견적 프리필** 둘뿐이고,
화주 상세의 select 는 `id, location_name, address, location_type, sido, sigungu` **6개**다.

| 컬럼 | 판정 |
|---|---|
| `location_name`·`address`·`location_type` | 🟢 보인다 |
| **`address_detail`** | 🔴 **안 보인다** — 조회 안 함 |
| **`contact_name`·`contact_phone`** | 🔴 **안 보인다** — 조회 안 함. 25차가 화주에게 입력받기 시작한 값 |
| **`notes`** | 🔴 **안 보인다** — 조회 안 함 |
| `sido`·`sigungu` | ⚠️ 볼 필요 없다 |

### 2-3. `customer_presets`

🔴 **관리자에서 읽는 화면 0개.** `lib/customerPresets.ts` 는 포털 전용이다.
⚠️ **볼 필요 없다로 판정한다** — 프리셋은 화주가 폼을 빨리 채우는 도구이고, 그 결과값
(품목·요청사항)은 발주요청에 이미 담겨 넘어온다. 담당자가 프리셋 자체를 볼 실익이 없다.
🔴 다만 26차부터 대기 중인 *"불러온 프리셋 이름을 어딘가 표시"* 는 **화주 화면 문제**이지
관리자 문제가 아니다.

### 2-4. 적재구분 (A-4)

🟢 **관리자 견적·오더·배차 폼에 전부 있다.** `loading_type` 이 4개 표에 모두 있고
`MixableBadge` 가 관리자 7곳·포털 5곳에서 쓰인다. **끊기는 곳 없음.**

---

## 3. 🔴 A-1c — 23:59 는 안 보인다. 대신 다른 문제가 있다 (완료조건 6)

목의 「내착」 건을 **저장값 23:59** 로 맞춰 다시 재었다.

```
  관리자 화주요청 목록      🟢 23:59 0건 — 「내착」 + 날짜 + 「시각 무관」 을 그린다
  견적 전환 프리필          🟢 23:59 0건
  견적 폼 전체 본문         🟢 23:59 0건
```

🔴 **그런데 23:59 가 안 보이는 이유가 「잘 처리해서」가 아니다.**
`DateTimePicker` 의 시간 드롭다운이 **30분 단위**라 `23:59` 가 선택지에 없어서
**시간이 빈 값으로 떨어진다.** 실측: 날짜 입력창 2개에 `2026-08-30`·`2026-08-31` 만 들어가고
시간 드롭다운은 비어 있다.

⇒ 담당자가 견적 폼에서 보는 것은 **날짜만 채워지고 시각이 빈 하차 일시**이고,
「내착」이라는 사실은 **특이사항 textarea 안의 한 줄**에만 있다:
`※ 화주 요청: 내착 (상차 다음 날 도착 · 시각 무관)`

🟢 **오독 위험은 없다**(23:59 를 요청 시각으로 읽을 일이 없다).
🔴 **대신 담당자가 특이사항을 안 읽으면 빈 시각을 임의로 채운다.**

---

## 4. 승계 경로 (완료조건 7·8·9)

```
  발주요청 ──▶ 견적 ──▶ 오더 ──▶ 배차 ──▶ 정산
           32컬럼   34필드   6필드+정산방식
           프리필    프리필    프리필
```

### 4-1. 단계별로 떨어지는 필드

| 단계 | 떨어지는 것 | 담당자가 다시 입력? |
|---|---|---|
| 발주요청 → 견적 | (거의 없음) | — |
| **견적 → 오더** | **`물품특성`·`운송시간`·`왕복/편도`·`대기시간`·`경유지수`** | 🔴 **불가** — `orders` 에 **컬럼 자체가 없다** |
| **오더 → 배차** | **`special_notes`(특이사항)** | 🔴 배차 상세가 오더에서 안 읽는다 |

🔴 **견적 → 오더에서 5개가 떨어지는 것은 "다시 입력"의 문제가 아니라 저장할 곳이 없는 것이다.**
`quotes` 는 그 값들을 `selected_options`(jsonb) 에 담는데, `orders` 에는 대응 컬럼이 없고
프리필도 `상차조건`·`하차조건` 둘만 꺼낸다.

⚠️ **금액에는 이미 녹아 있다**(견적 계산의 가산 항목). 문제는 **현장 정보**다 —
「파손주의」·「장척/중량」은 차주에게 알려야 하고, 「경유지 2곳」·「대기 60분」은 배차 조건이다.

### 4-2. 🔴 「당착/내착」은 배차에서 끊긴다 (B-4)

```
  포털          dropoff_arrival_type          🟢 정본 컬럼
  견적          quotes.notes 한 줄            🟢 이어짐 (27차)
  오더          orders.special_notes          🟢 이어짐 (프리필 q.notes → special_notes)
  배차          ✗                              🔴 끊김
```

배차 상세가 오더에서 읽는 것은
`order_no, origin, destination, item, vehicle_type, loading_type, mixed_*` 뿐이다 —
**`special_notes` 가 없다.** 배차 화면에서 차주에게 「당착」을 알려줄 근거가 없고,
「연결된 운송오더 보기」로 넘어가야만 보인다.

🔴 **제안 — 컬럼을 새로 만들지 말고 배차 상세의 조회에 `special_notes` 를 더한다.**
`orders(... , special_notes)` 한 항목이고, 화면에 「운송오더 특이사항」 읽기 전용 줄을 그린다.
⚠️ **`dispatches` 에 도착구분 컬럼을 만드는 안은 권하지 않는다** — 특이사항 한 줄이 이미
오더까지 살아 있고, 컬럼을 만들면 `quotes`·`orders` 에도 만들어야 승계가 성립한다(3개 표).
**필요성이 확인되면 그때 세 표에 함께 넣을 것.**

### 4-3. B-5 조용한 끊김 재점검

50차가 잡은 「공개문의 → 견적 전환 UPDATE 가 0행」 과 같은 종류를 찾았다.
🟢 **같은 패턴은 발견되지 않았다** — 21차가 anon 정책을 정리하면서 직원 정책이 생겼고,
승계 UPDATE 는 전부 직원 세션으로 돈다.

---

## 5. 🔴 화주 「견적 승인」 (완료조건 10·11)

### 5-1. C-1 구분할 수 있는가 → **없다** (실측)

`quotes` 에서 승인 흔적이 될 만한 컬럼은 `created_by`·`updated_by` 둘뿐이고 **둘 다 uuid** 다.
이력 표 7개(`activity_logs`·`consents`·`invoice_amendment_logs`·`settlement_field_change_logs`·
`settlement_type_change_logs`·`sms_logs`·`support_access_logs`) 어디에도 화주 상태 변경을
남기는 곳이 없다.

### 5-2. C-2 필요한가 → **필요하다. P1.**

**왜** — 화주가 승인한 건은 **담당자가 오더를 만들어야 하는 건**이고, 담당자가 손으로 바꾼 건은
**이미 무언가 진행 중인 건**이다. 지금은 배지가 둘을 같이 세어 할 일 목록이 부정확해진다.

🔴 **제안: `quotes` 에 nullable 컬럼 2개.**

```
  approved_by_customer_at   timestamptz   화주가 승인한 시각 (null = 담당자 변경)
  approved_by_account_id    uuid          customer_accounts(id) 참조
```

🔴 **`updated_by` 를 재사용하지 않는다** — `staff_accounts` 참조라 화주 계정 id 를 넣으면
**FK 위반**이다(지시서 금지 11번, 27차가 이미 확인).
⚠️ 컬럼 2개면 배지를 「화주 승인」과 「담당자 변경」으로 갈라 셀 수 있다.

### 5-3. C-3 배지는 정확한가 → **정확하다** (실측)

```
  로직        quotes.status = '수주' AND 그 견적을 참조하는 orders 없음   (TopNav.tsx:199)
  운영 DB     수주 5건 중 오더 없음 3건  →  배지 3
  목 렌더링   해당 1건                  →  「운송 운영 ▼ 1」  ✅ 일치
```

🟢 **배지 수 = 실제 건수**가 확인됐다. 다만 5-2 때문에 **화주 승인분과 담당자 변경분이 섞여 있다.**

⚠️ **자동 오더 생성은 제안하지 않는다**(27차 확정 · 지시서 금지 9번).

---

## 6. 🔴 용어 — 「월정산」 두 칸 (완료조건 12~16)

### 6-1. D-1a 실제 사용처 (지시서 전제와 다름)

| 파일 | 호출 | 자리 |
|---|---|---|
| `app/admin/quotes/[id]/page.tsx` | 1 | 견적 상세 정산방식 표시 |
| `app/admin/orders/[id]/page.tsx` | 1 | 오더 상세 |
| `app/admin/dispatches/[id]/page.tsx` | 1 | 배차 상세 |
| `app/admin/invoices/page.tsx` | 1 | 정산 목록 배지 |
| `app/admin/invoices/[id]/page.tsx` | 1 | 정산 상세 |
| `app/admin/orders/page.tsx` | **0** | 🔴 **import 만 — 죽은 import** |
| `app/customer/dispatches/page.tsx` | 2 | 포털 배차·운송 조회 |
| `app/customer/invoices/page.tsx` | 3 | 포털 정산·결제내역 |

⇒ **admin 실제 호출 5곳 + 포털 2곳.** 지시서의 「admin 8개」는 틀렸다.

### 6-2. D-1b `is_billing_batch_candidate` 에 걸린 것

```sql
  collection_method = 'broker' AND billing_cycle = 'monthly'
  AND status = '정산대기' AND customer_side_locked = false AND locked = false
  AND customer_charge_total > 0 AND company_id is not null
```

이 함수를 쓰는 것: 뷰 `customer_billing_batch_candidates` + 함수
`add_item_to_billing_batch` · `validate_billing_batch` · `confirm_billing_batch`
(15차가 한 함수로 통합).
🔴 **화면에서 「월정산」이라는 말이 사라져도 이 로직은 계속 돈다** — 데이터는 남고 표시만
사라지는 종류다(원칙 42번).

### 6-3. D-1c 포털 2화면이 지금 보여주는 것 (렌더링 실측)

| | `/customer/dispatches` | `/customer/invoices` |
|---|---|---|
| 「정산방식」(축 이름) | 🟢 | 🟢 |
| 「주선사 정산」 | 🟢 | 🟢 |
| **「월정산」** | 🟢 | 🟢 |
| 「선착불」 | 🟢 | 🟢 |
| 「운임 수금방식」·「위캐리 수금」 | — | — |

🔴 **확인됐다 — 화주가 견적 확인에서는 「위캐리 수금」을, 이 두 화면에서는 「주선사 정산」을 본다.**

### 6-4. 🔴 D-1 선택지 비교와 권장안

| | 내용 | 잃는 것 | 범위 |
|---|---|---|---|
| **(A)** | 담당자 화면은 그대로, 화주 화면만 화주 말로 통일 | 두 시스템의 말이 계속 다름 | 포털 2파일 |
| **(B)** | 관리자에도 화주 말 + 청구주기를 **별도 열/배지**로 분리 | 없음 | admin 5파일 + 정산관리 레이아웃 + 포털 2파일 |
| **(C)** | 화주 말 함수에 청구주기 축을 더해 2축 4값 | 🔴 화주가 「월정산」을 계약 확정 조건으로 읽음 | 포털 전체 + 발주 폼 재검토 |

🔴 **권장은 (A) 다.**

**왜** —
1. **(C) 는 27차가 이미 기각한 설계다.** 청구주기는 `companies.billing_cutoff_day` 에 딸린
   **화주별 계약 사항**이라 화주에게 보여주면 확정 조건처럼 읽힌다. 4라운드에 발주 폼에서
   `billing_cycle` 축을 뺀 것과 같은 이유이고, 지시서 금지 8번이기도 하다.
2. **(B) 는 지금 비용을 낼 이유가 없다.** 🔴 **월정산이 운영에 0건**이다(전 테이블 `per_order`
   100%). 담당자가 「월정산」이라는 말을 화면에서 실제로 읽는 상황이 아직 오지 않았다.
   레이아웃까지 건드리면서 지금 할 일이 아니다.
3. **(A) 는 아무것도 잃지 않는다.** 담당자 말은 2축 4값 그대로 남고, 화주 말은 1축 2값로
   일관된다. 🟢 **두 시스템의 말이 다른 것은 사고가 아니라 27차 ④ 가 확정한 구조다** —
   견적 상태(`보류` ↔ 「협의 중」)와 같은 결이다.

⚠️ **(A) 를 고르면 남는 숙제 하나** — 포털 2화면에서 「월정산」을 화주에게 뭐라고 부를지.
🔴 **권장: 축을 쪼갠다.** 「운임 수금방식: 위캐리 수금」 + 「청구: 월정산」 **두 줄**로 나눈다.
그러면 화주 말 함수를 건드리지 않고도 월정산이라는 사실이 살아남는다.
⚠️ **월정산이 실제로 돌기 시작하면 (B) 를 다시 볼 것.**

### 6-5. D-1d 29차가 두 화면에 써야 하는 것

```
  운임 수금방식   getCustomerCollectionMethodLabel()   위캐리 수금 / 선착불(차주 직접수금)
  청구            별도 줄                               건별 / 월정산
```
🔴 **`getSettlementDisplayLabel()` 을 포털에서 걷어내되 `billing_cycle` 값 자체는 계속 읽을 것.**

### 6-6. 용어 불일치 전수 (완료조건 16)

| 대상 | 상태 | 판정 |
|---|---|---|
| 견적 상태 | DB `보류`·`실패` / 화주 「협의 중」·「취소」 / 관리자 그대로 | 🟢 **의도된 것** |
| 정산방식 | 담당자 2축 4값 / 화주 1축 2값 | 🟢 **의도된 것** — 단 포털 2화면이 미정합 (6-3) |
| 상하차방식 | `lib/loadingMethods.ts` 8종 단일 정의처, 관리자·포털 공용 | 🟢 일치 |
| 차량형태 | `QUOTE_BODY_TYPES` 21 / `DRIVER_BODY_TYPES` 24 | 🟢 **의도된 것**(25차 확정) |
| 차급 | `VEHICLE_TYPES_ALL` 11 / `_PUBLIC` 6 | 🟢 **의도된 것**(17차 확정) |
| 배차 상태 | DB 6종 / 화주 3단계는 **아직 미구현** | ⚠️ 29차가 공용 매핑을 만들 것 |

🔴 **사고는 하나도 발견되지 않았다** — 라벨이 달라 문자열 매칭에서 빠지는 곳은 없다.
유일한 미정합이 6-3(포털 2화면)이고 그것은 27차가 **의도적으로 남긴 것**이다.

---

## 7. 공유 부품 (완료조건 17~20)

### 7-1. 26·27차 컴포넌트 실제 목록

```
  components/pv2/Pv2Select.tsx        188줄   커스텀 드롭다운
  components/pv2/Pv2DatePicker.tsx    189줄   커스텀 달력
  components/pv2/Pv2DateTimeField.tsx 233줄   날짜+시간+칩
  components/pv2/Pv2AddressField.tsx   72줄
  components/pv2/Pv2PrintModal.tsx     87줄   27차
  components/pv2/Pv2PromptModal.tsx   100줄
  components/pv2/pv2Popover.ts         54줄   열림 레지스트리(전역 1개)
```

### 7-2. 🔴 E-3 관리자 재사용 — **가능하다. 단 한 가지를 풀어야 한다**

```
  컴포넌트 파일에 "portal-v2" 문자열   0건        🟢 컴포넌트는 스코프에 안 묶여 있다
  최상위 .pv2-* 선택자                371개      🟢 클래스도 안 묶여 있다
  .portal-v2 로 시작하는 규칙          10개
  🔴 --pv2-* 변수 33개                 .portal-v2 { } 안에서만 정의된다
```

🔴 **그래서 관리자에서 `Pv2Select` 를 그냥 쓰면 CSS 변수가 상속되지 않아 색·배경이 전부
무효가 된다**(브라우저 기본값으로 그려진다).

🔴 **권장 해법 — 관리자 폼 래퍼에 토큰 클래스 하나.**
`globals.css` 에 `.pv2-tokens { --pv2-…: … }` 를 **추가**하고 관리자 폼 컨테이너에 붙인다.
⚠️ **`.portal-v2` 블록에서 변수를 옮기지 말 것** — 옮기면 `globals.css` 에 **삭제 줄이 생겨**
관리자 무회귀의 근거(1045 추가 / 0 삭제)가 깨진다. **추가만 한다.**
⚠️ 관리자 전체(`body`·`:root`)에 붙이는 안은 권하지 않는다 — 포털 토큰이 관리자 31개 화면에
새고, 0-6 확정(디자인은 공유 부품까지만)과 어긋난다.

### 7-3. E-2 관리자 native 요소 화면별 개수

```
  합계   <select> 67개 · type="date" 9개 · datetime-local 1개   (15개 화면)

  companies/[id]      select 8  date 1        quotes/page         select 7
  companies/page      select 8  date 1        quotes/[id]         select 8
  dispatches/[id]     select 8  datetime 1    orders/page         select 5
  invoices/page       select 3  date 2        drivers/page        select 4
  invoices/[id]       select 1  date 4        drivers/[id]        select 3  date 1
  dispatches/page     select 2                sms-logs            select 3
  orders/[id]         select 3                staff               select 3
  customers/page      select 1
```

🔴 **한 차수에 67개를 다 바꾸지 말 것.** 화면 단위로 쪼갠다(§9 분할안).

### 7-4. E-4 `?embed=1` — **이미 있다**

`app/admin/quotes/[id]/print/page.tsx:68` 에 `useSearchParams().get("embed") === "1"` 이
있고 `:142` 에서 툴바를 숨긴다. 화주용과 **쌍으로** 들어가 있다(27차 3라운드).
🔴 **관리자에 `Pv2PrintModal` 을 붙일 때 print 쪽은 손댈 필요가 없다.**

### 7-5. E-5 공유 자산 전수

| 컴포넌트 | admin | portal |
|---|---|---|
| `DateRangeFilter` | 8 | 3 |
| `MixableBadge` | 7 | 5 |
| `CompanyNameMark` | 1 | 2 |
| `PasswordInput` | 2 | 2 |

| lib | admin | portal | | lib | admin | portal |
|---|---|---|---|---|---|---|
| `constants` | 12 | 4 | | `settlementLabels` | 9 | 6 |
| `preventEnterSubmit` | 13 | 3 | | `vat` | 8 | 5 |
| `localDateTime` | 5 | 1 | | `loadingMethods` | 4 | 2 |
| `vehicleBodyTypes` | 4 | 2 | | `dispatchStatusColors` | 3 | 2 |
| `dispatchExtraCharges` | 3 | 1 | | `shortAddress` | 2 | 1 |
| `orderStatusColors` | 2 | 1 | | `quoteExcel` | 1 | 2 |
| `companyInfo` | 1 | 2 | | `dispatchPhotos` | 1 | 1 |

🔴 **`settlementLabels` 가 가장 넓다**(15곳) — 6-4의 결정이 여기로 번진다.

---

## 8. 관리자 규모·결합도 (완료조건 21~24)

### 8-1. F-1·F-2 회귀 기준선 (완료조건 21)

**9화면**: orders · invoices · dispatches · rates · portal-requests · quotes · guide · drivers · companies
**12지표**: overflow · cards · tables · btns · fields · badges · rows · containerW · cardBg · bodyBg · fontFamily · navChips

🟢 **`origin/main 19c1c27` 기준으로 다시 쟀다**(목 데이터·1280×900):

```
  전 화면    overflow 0 · containerW 1100 · cardBg rgb(255,255,255) · bodyBg rgb(242,244,246)
             fontFamily "SUIT Variable" · navChips 4 (guide 만 14)
  orders     cards 1 tables 0 btns  6 rows  0      invoices   cards 1 tables 1 btns  7 rows 3 badges 4
  dispatches cards 1 tables 1 btns  5 rows  5      rates      cards 0 tables 0 btns  3 rows 0
  portal-req cards 1 tables 1 btns 10 rows  1      quotes     cards 3 tables 1 btns 14 rows 7 fields 25
  guide      cards 11 tables 8 badges 11 rows 35   drivers    cards 1 btns 2 · companies cards 1 btns 7
```
⚠️ **행수(rows)는 목 데이터에 따라 달라진다** — 회귀 비교는 **같은 목으로 origin/main 과
브랜치를 나란히 띄워** 할 것(55차 ⑬).

### 8-2. G-1·G-2 화면 전수 (33개 파일 · `app/api/admin` 라우트 49개)

| 그룹 | 화면 (줄 수) |
|---|---|
| **화주 확보** | companies 793 · companies/[id] **1521** · applications 309 · public-quotes 202 · individual-customers 172 · [id] 393 |
| **화주 관리** | customers 397 · portal-requests 366 · account-cleanup 160 |
| **운송 운영** | rates 785 · quotes **1821** · quotes/[id] **1297** · quotes/[id]/print 428 · orders 911 · orders/[id] 997 · drivers 507 · drivers/[id] 623 · dispatches 922 · dispatches/[id] **2520** · invoices 876 · invoices/[id] 962 · settings/external-networks 315 |
| **시스템** | dashboard 422 · staff 487 · sms-logs 622 · support-logs 106 · my-account 188 · guide 486 |
| 기타 | login 137 · page 81 · layout 11 · loading 7 |

🔴 **큰 것 넷이 전체의 절반이다** — `dispatches/[id]` 2520 · `quotes` 1821 · `companies/[id]` 1521 ·
`quotes/[id]` 1297. **한 차수에 두 개 이상 넣지 말 것.**

### 8-3. G-3·G-4 겹침 (완료조건 23)

```
  🔴 29차와 겹치는 것
     견적서 print 2종        admin/quotes/[id]/print  ↔  customer/quotes/[id]/print   (31차 쌍 규칙)
     DispatchPhotosPanel     customer/dispatches 가 씀 — 29차가 다시 그리는 화면
     lib/settlementLabels    admin 9 · portal 6 — 6-4 결정이 양쪽에 동시에 닿는다
     lib/dispatchStatusColors admin 3 · portal 2 — 29차 3단계 매핑이 여기 들어간다
```
🔴 **내부 정합 차수와 29차가 `settlementLabels`·`dispatchStatusColors` 에서 충돌한다.**
**29차를 먼저 하고 내부 정합을 뒤에 둘 것**(29차가 포털 쪽 라벨을 확정한 뒤 관리자를 맞춘다).

### 8-4. G-5 0-3 지시를 적용할 화면 (완료조건 24)

사용자 지시: *"메뉴 항목이나 순서들이나 구성 등이 화주포털 발주요청·견적과 같이"*

| 포털 | 대응 관리자 화면 | 🟢 옮길 수 있는 것 | 🔴 옮기면 안 되는 것 |
|---|---|---|---|
| 발주 요청 | `/admin/quotes` 등록 폼 | 블록 4구획(① 구간 ② 일정 ③ 화물·차량 ④ 요청사항) · 번호 배지 · 입력 순서 | 색·레이아웃 |
| 발주 요청 | `/admin/portal-requests` | 상세 펼침(§2-1 제안) | — |
| 견적 확인 | `/admin/quotes` 목록 | 상태 칩 · 기간 필터 배치 | 🔴 **상태 라벨**(담당자는 `보류`·`실패`) |
| 견적서 상세 | `/admin/quotes/[id]` | 금액 줄 구성(공급가액·부가세·총액) | 🔴 **정산방식 말** |

🔴 **결론: 순서·구성·번호 배지는 그대로 옮길 수 있고, 말(라벨)과 색은 옮기면 안 된다.**

---

## 9. P0 / P1 / P2 (완료조건 25·26·27)

### 🔴 P0 — 9/7 전 (2건만)

| # | 무엇 | 왜 P0 | 어디에 |
|---|---|---|---|
| **P0-1** | **관리자 화주요청에 상세 펼침** — 품목 · 상하차조건 · 물품특성 · 왕복/편도 · 대기/경유 · **상하차지 담당자 6필드** | 🔴 담당자가 **품목도 모르는 채 승인/반려를 판단**한다. 되물으면 포털을 만든 이유가 무너진다. 담당자 연락처는 **제3자 동의까지 받은 값**이다 | 29차 PR |
| **P0-2** | **배차 상세에 오더 특이사항** — `orders(special_notes)` 조회 + 읽기 전용 줄 | 🔴 「당착/내착」이 배차에서 끊긴다. **배차가 틀어지는 종류**다 | 29차 PR |

🔴 **P0 를 둘로 묶은 이유** — 둘 다 **관리자 화면 1개씩 · 조회 필드 추가 + 표시**라
29차 PR 에 얹어도 그 차수가 흔들리지 않는다. **DB 변경 0.**

⚠️ **P0 로 고르지 않은 것과 사유**
- `customer_locations` 담당자·상세주소 → 발주요청에 같은 값이 함께 넘어오므로 **운영이 막히지 않는다**
- 견적 → 오더 5필드 → `orders` 에 컬럼이 필요해 **DB 변경**이고, 금액에는 이미 반영돼 있다
- 화주 승인 흔적 → 배지가 「오더를 만들어야 하는 건」으로는 이미 정확하다
- 정산방식 말 통일 → 화주가 헷갈릴 뿐 **운영이 막히지 않는다**

### P1

| # | 무엇 | 비고 |
|---|---|---|
| P1-1 | 포털 2화면 정산방식 라벨 (6-4 (A) + 청구 별도 줄) | **29차** |
| P1-2 | `quotes` 에 화주 승인 흔적 2컬럼 + 배지 분리 | DB 변경 |
| P1-3 | 견적 → 오더 5필드 (`orders` 컬럼 5개 + 프리필 + 표시) | DB 변경 |
| P1-4 | 화주 상세 배송지에 담당자·상세주소·비고 표시 | 조회 4필드 추가 |
| P1-5 | `orders/page.tsx` 죽은 import 정리 | 1줄 |

### P2

| # | 무엇 |
|---|---|
| P2-1 | 관리자 폼에 `Pv2Select`·`Pv2DatePicker` 도입 — **화면 단위로 쪼갠다** |
| P2-2 | 관리자 `Pv2PrintModal` (print 쪽은 준비 완료) |
| P2-3 | 0-3 구성·순서 정합 (§8-4) |

### 차수 분할안 (파일이 안 겹치게)

```
  29차   포털 조회 흐름 + P0-1 + P0-2 + P1-1        🔴 시안 필요
  30차   내부 정합 ① — 견적·오더 (P1-3 · P2-3 견적/오더)   quotes*, orders*
  31차   내부 정합 ② — 배차·정산 (P2-1 배차/정산)          dispatches*, invoices*
  32차   내부 정합 ③ — 화주·차주 (P1-4 · P2-1 나머지)      companies*, drivers*
  미정   P1-2 (DB 변경) · P2-2
```
🔴 **30~32차가 파일 단위로 안 겹치게 잘랐다.** `dispatches/[id]`(2520줄)와 `quotes`(1821줄)를
같은 차수에 넣지 않았다.

---

## 10. 사용자 결정이 필요한 항목 (완료조건 28)

| # | 물음 | 권장안 |
|---|---|---|
| **1** | 🔴 **「월정산」 두 칸** — (A) 담당자 말 유지 / (B) 관리자에도 화주 말 + 청구주기 분리 / (C) 화주 말에 청구주기 축 추가 | 🟢 **(A)** + 포털에서 「청구: 월정산」을 **별도 줄**로. 월정산이 실제로 돌기 시작하면 (B) 재검토 |
| **2** | 「당착/내착」을 `dispatches` 컬럼으로 만들지, 특이사항 승계로 둘지 | 🟢 **특이사항 승계**(P0-2). 컬럼은 3개 표에 함께 넣어야 성립 |
| **3** | 화주 승인 흔적 컬럼 2개를 만들지 | 🟢 **만든다(P1-2)** — 배지를 갈라 세려면 필요 |
| 4 | 견적서 머리 금액을 부가세 별도로 내릴지 | ⚠️ 10차·53차 위계를 뒤집는 일 — **그대로 두기 권장** |
| 5 | 자주 쓰는 화물 프리셋 이름 표시 (26차부터) | 화주 화면 문제 · 29차에서 |
| 6 | 무료 대기 `"초과 시 30분당"` 단위 (25차부터) | DB 값이 정본이라 문구만 |

---

## 11. 검증·한계

```
  DB          verify(읽기 전용) 2회 — 1회는 aggregate 오류로 마지막 쿼리 실패, 고쳐 재실행
  렌더링      관리자 9화면 + 화주요청 + 견적 프리필 + 포털 2화면 (54차 목 로그인 방식)
  목          27차가 겪은 함정 3종이 이미 반영된 mock-27b.js 사용 + 23:59 자리 채움 추가
  git         CLAUDE.md 외 변경 0 · _verify.sql md5 원본 복원 확인
```

⚠️ **못 본 것 · 한계**
- 🔴 **운영 DB 에 27차 3컬럼 실데이터가 0건**이라 새 컬럼 표시는 **목으로만** 확인했다
- 🔴 **원격 임시 브랜치 `claude/28-survey-tmp` 를 지우지 못했다** — 이 환경의 git 프록시가
  브랜치 삭제를 막는다(`send-pack: unexpected disconnect`). 기존 `claude/*` 브랜치 10개도
  같은 이유로 남아 있다. **GitHub 웹에서 지울 수 있고, 내용은 읽기 전용 SQL 뿐이다**
- 서체는 이 환경에서 확인 불가(프록시가 폰트 CDN 차단, 56차부터 계속)
- 화주 계정 실제 로그인은 하지 않았다(목 세션 주입)
