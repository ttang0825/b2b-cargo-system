// 화주포털 목록의 **페이지 내 업데이트 표시** (2026-09-16 · 사용자 지시)
//
// 🔴 **배너와 별개다.** 배너는 「견적서 도착 · 배차확정 · 운송완료」 셋에만 뜨도록
//    좁혔고(`lib/portalAlert.ts`), 이 표시는 **마지막으로 본 뒤 바뀐 건 전부**에 붙는다.
//    🔴 배너와 같은 조건으로 좁히지 말 것 — 사용자가 「알림배너와 별개로」라고 했다.
//
// 🔴 **글자를 「새」로 바꾸지 말 것** — 새로 생긴 것과 고쳐진 것을 가리지 않는다
//    (판정이 `updated_at` 하나다). 「업데이트」가 정확한 말이다.
//
// 🔴 **색은 포털 토큰이 아니라 리터럴이다** — `--pv2-*` 는 `.portal-v2` 스코프 안에만
//    있어서 그 밖(관리자·공개 화면)에서 쓰면 색이 통째로 안 나온다. 값은 시안 팔레트의
//    파랑(`#1D57C6` / `#E8EFFC` — 「견적 도착」 알약과 같은 쌍)이라 새 색이 아니다.
export default function Pv2UpdatedMark() {
  return (
    <span className="pv2-updated" title="마지막으로 확인하신 뒤에 바뀐 건입니다">
      업데이트
    </span>
  );
}
