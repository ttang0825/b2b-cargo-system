# WeCarry 운송 통합 운영 시스템 — 인수인계 문서

**버전** v20 · 2026-08-27
**기준** 세션 55차 / 지시서 26차 진행 중 / 저장소 `ttang0825/b2b-cargo-system`

> 🔴 **이 파일은 이름을 고정해서 씁니다.**
> 갱신할 때 `HANDOFF_v21.md` 같은 새 파일을 만들지 말고 **이 파일의 내용만 교체**한 뒤 위 버전 줄을 올리십시오.
> 옛 버전은 `git log -- HANDOFF.md`로 언제든 꺼내 볼 수 있고, 파일명을 바꾸면 `CLAUDE.md`의 안내가 깨집니다.
>
> 직전 버전 **v19**(2026-07-23)는 claude.ai 채팅에서 Claude Code로 전환하며 저장소 `CLAUDE.md`로 이관된 문서입니다.

---

## 0. 이 문서를 읽는 방법

v19 이후로 인수인계 기록은 저장소 `CLAUDE.md`에 **세션마다 최상단에 쌓는 방식**으로 이어졌고, 지금 **약 60만 자**입니다. 통째로 읽을 수 있는 크기가 아닙니다.

**두 문서의 역할이 다릅니다.**

| 문서 | 성격 | 언제 보나 |
|---|---|---|
| `CLAUDE.md` | 세션별 축적 기록 — **왜 그렇게 했는지의 근거**가 전부 여기 있다 | 특정 항목의 이력·판단 근거가 필요할 때 |
| **이 문서(v20)** | 지금 시점의 정돈된 스냅샷 | **처음 붙는 사람·세션이 먼저 읽는 문서** |

🔴 **저장소 코드가 이 문서와 다르면 저장소가 항상 맞습니다.**

⚠️ `CLAUDE.md`를 읽을 때는 **통째로 읽지 말고** 최상단 100줄(최신 세션)과 맨 아래 로드맵만 보고, 나머지는 `grep`으로 찾으십시오.

---

## 1. 서비스 개요

**위캐리 운송** — B2B 화물운송 **주선업** 통합 운영 시스템.

화주 CRM · 견적 · 배차 · 정산을 관리하는 **내부 시스템**, 화주가 직접 발주하고 조회하는 **운송관리 포털**, 그리고 **공개 유입 화면**(랜딩·견적문의·계정신청)까지 하나로 묶여 있습니다.

**포지셔닝은 "정식 물류 파트너"**입니다. 즉시 자동견적·정찰제·완전 셀프가입을 하는 경쟁사와 반대로, **담당자가 직접 응대하는 관계형 영업**을 핵심 차별점으로 둡니다. 운임기준표와 견적 로직은 비공개입니다.

🔴 **서비스 시행일은 2026년 9월 7일**입니다.

---

## 2. 기술 스택 · 배포 · 환경변수

- **Next.js (App Router)** + **Supabase**(Postgres · Auth · Realtime) + **Vercel**
- 저장소 `ttang0825/b2b-cargo-system` (`main`)
- 엑셀은 `xlsx`가 아니라 **`xlsx-js-style`** (스타일링 때문에 교체)

**SQL 변경은 GitHub Actions로 합니다** (v19 이후 신설).

```
migrations/YYYY-MM-DD_무엇.sql  을 추가하고
Actions → "DB 마이그레이션" → dry-run → apply → verify
```

| 규칙 | |
|---|---|
| 🔴 `begin;`/`commit;` 쓰지 말 것 | 워크플로가 `--single-transaction`으로 감쌉니다 |
| 🔴 안전장치는 `select`가 아니라 `do $$ … raise exception … end $$;` | 조건이 틀리면 트랜잭션이 통째로 되돌아갑니다 |
| 🔴 **적용이 끝난 파일의 SQL을 나중에 고치지 말 것** | DB와 파일이 어긋납니다. 값을 바꾸려면 새 파일 |
| 🔴 `migrations/_baseline.txt` 수정 금지 | 과거 INSERT가 중복 실행됩니다 |
| 접속 | **Session pooler**(IPv4). Direct는 IPv6 전용이라 러너에서 실패 |

🟢 **세션이 직접 실행하고 로그까지 읽습니다** — 사용자가 버튼을 누를 필요가 없습니다.

**환경변수(Vercel)** — `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `KAKAO_REST_API_KEY` · `SOLAPI_*`
**미등록(보류)** — `RESEND_API_KEY`(이메일 발송)

---

## 3. 화면 구성

### 공개 — 로그인 없음

`/` 랜딩(8섹션) · `/vehicles` 차량 안내 · `/quote` 견적 문의 · `/apply` 계정 신청 · `/status` 현황 조회

### 화주 운송관리 포털 — 화주 로그인

홈 · 발주 요청 · 견적 확인 · 배차·운송 조회 · 정산·결제내역 · 월별 통계 · 배송지·화물 관리 · 담당자 정보

🔴 **메뉴에는 없지만 살아 있는 라우트 셋** — 캘린더 · 공지사항 · **비밀번호 변경**. **"안 쓰는 라우트"로 보고 지우지 마십시오.** 비밀번호 변경을 지우면 신규 계정의 첫 로그인이 갇힙니다.

- 공지사항 진입로는 **홈의 공지 블록 하나뿐**입니다
- 캘린더는 **월별 통계 화면 하단**으로 들어갈 예정이고, 그때까지는 진입 경로가 없습니다(의도된 공백)

### 내부관리 — 직원 로그인

| 그룹 | 화면 |
|---|---|
| 화주 확보 | 화주관리 · 화주신청 · 공개문의 |
| 화주 관리 | 활성화주 CRM · 화주요청 |
| **운송 운영** | 운임기준표 · 견적 · 오더 · 차주 · 배차 · 정산 |
| 시스템(admin 전용) | 대시보드 · 직원 · 설정 · 문자이력 |

---

## 4. 데이터·정책 구조에서 반드시 아는 것

**설계 원칙은 `CLAUDE.md` §3에 1~56번으로 정리돼 있습니다.** 여기에는 최근에 실제로 사고를 만든 것만 적습니다.

### 4-1. 🔴 화주와 직원이 같은 인증 롤을 씁니다

둘 다 `authenticated`입니다. **컬럼 GRANT로는 구분되지 않습니다.** 두 번 문제가 됐고, 둘 다 **service_role 서버 API**로 옮겨 해결했습니다.

🔴 **RLS 정책은 롤별로 갈립니다** — `TO anon` 정책은 `authenticated` 요청에 적용되지 않습니다. 이걸 모르고 세운 지시서가 실제로 틀렸습니다.

### 4-2. 🔴 anon은 개인정보를 읽지도 쓰지도 못합니다

정책 21개를 지우고 RLS 13개를 켰습니다. 남은 anon 경로는 **INSERT 2개뿐**(`public_quote_requests` · `customer_applications`) — 지우면 견적 문의와 계정 신청이 죽습니다.

⚠️ **RLS on + 정책 0개**인 표들(`consents` 등)은 **service_role 전용이 정상**입니다. 고치지 마십시오.

### 4-3. 견적 계산

```
최종금액 = 기본운임 + 가산 − 혼적할인
```

🔴 **마진율 파라미터가 없습니다.** `rate_distance_tiers.base_fare`가 곧 **화주 청구가**이고, 차주 지급액은 배차 단계에서 담당자가 수동 입력합니다. 마진은 그 차액입니다.

🔴 **가산은 `option_name` 문자열 완전일치로 매칭한 뒤 못 찾으면 `continue` 합니다** — 예외도 경고도 없이 **가산만 조용히 빠집니다.** 그래서 **옵션 라벨을 바꾸는 코드와 `rate_surcharges` 행은 반드시 같은 커밋**에 넣습니다.

### 4-4. 🔴 포털 CSS는 관리자와 거의 전부 공유입니다

포털이 쓰는 클래스 25개 중 **전용은 하나뿐**이고 `.container`·`.card` 등을 관리자 화면 31개가 같이 씁니다. 그래서 포털 개편은 **`.portal-v2` 스코프 격리**로 갔고, 새 컴포넌트는 전부 `.pv2-*`입니다.

🔴 **"중복이니 전역으로 합치자"고 하지 마십시오.** 합치는 순간 관리자가 딸려옵니다.
🔴 **`globals.css`는 순수 추가만** — 삭제된 줄 0이 관리자 무회귀의 근거입니다.

### 4-5. 🔴 `force-dynamic`만으로는 캐시를 못 막습니다

그 지시자는 *라우트 렌더링*만 동적으로 만들고, 라우트 안에서 supabase-js가 쓰는 `fetch`는 그대로 Data Cache를 탑니다. **DB를 바꿨는데 API가 옛 값을 계속 내려주는** 일이 실제로 있었습니다.

🔴 **새 서비스롤 GET 라우트에는 `force-dynamic`과 `createServiceClient()`가 둘 다 필요합니다.**

---

## 5. 확정 사항

### 5-1. 운임

| 항목 | 값 |
|---|---|
| 차급 | **11종** — 1 / 1.4 / 2.5 / 3.5 / 5 / 5톤 플러스·축 / 8 / 11 / 15 / 18 / 25 |
| 거리 구간 | 15구간 · **총 165행** |
| 목표 마진 | **17%** |
| 18톤 | 전 구간 **×0.89** — 🔴 단 `200km 이내`만 **−5.5%**(341,000) |
| 8톤·15톤 | 인접 차급 **보간값**(근거 등급 D) |
| 무료 대기 | **20분** |
| 차량형태 | **21종**(견적용) / 차주 보유차량은 별도 24종 |
| 상하차방식 | **8종** |

🔴 **18톤 `200km 이내` 한 칸만 다른 이유** — ×0.89 하면 321,000이 되어 11톤 325,000보다 낮아집니다(차급 역전). 11톤 대비 +5% 바닥을 걸었습니다. **"왜 이 칸만 다르지" 하고 맞추지 마십시오.**

🔴 **11톤·25톤은 손대지 마십시오** — 11톤은 검증됐고(완료 36건, 마진 +19.6%), 25톤은 완료 3건이라 판단 불가입니다.

### 5-2. 표기

| 항목 | 값 |
|---|---|
| 법인 · 브랜드 | **(주)디자인에그 │ 위캐리 운송** — 가운데는 글자가 아니라 **세로 구분선** |
| 사업자등록번호 | 105-87-66929 |
| 입금 계좌 | 국민은행 069101-04-228121 · 예금주 (주)디자인에그 |
| 고객센터 | 1661-2403 · 평일 09:00–18:00 |
| 시행일 | **2026-09-07** |

🔴 **어느 한쪽도 강조하지 않습니다** — 같은 크기·같은 굵기. 화면·인쇄에는 평문이 아니라 전용 컴포넌트를 씁니다(평문을 그리면 구분선이 글자로 찍힙니다).
⚠️ 대표번호 상수가 **두 개**입니다(고객센터 / 견적서 발행업체 TEL). 지금은 같은 값이지만 **바꿀 때 두 곳을 함께** 고쳐야 합니다.

### 5-3. 🔴 공개 화면 문구

**확정 문구 — 「1톤부터 5톤 이상까지」**

```
검증 정규식   1톤(부터|~)\s*5톤(?!\s*이상)   매치 0건
```

🔴 **"이상" 한 단어가 금지와 허용을 가릅니다.** `1톤부터 5톤까지`는 상한을 못 박는 금지 표현입니다.

| | |
|---|---|
| 🔴 금지 | `25톤` · `전 차종` · `예비 배차처` · `계약 없이` · `기존 거래처를 바꾸` · `이사` |
| 🔴 금지 | 첫 거래 혜택에 **금액·비율·조건 없는 지원 표현** |
| 🔴 금지 | **보험·배상** (약관·처리방침 안은 예외) |

**금지 표현의 적용 범위**

```
광고·유입   /, /vehicles, /quote, /apply     → 차급 6종만
화주 입력   /customer/request 희망 톤수       → 11종 (로그인 화주 대상이라 광고가 아님)
표시        포털 조회 · 견적서 · 엑셀         → 저장값 그대로
관리자·DB                                     → 11종
```

### 5-4. 🔴 용어가 갈리는 두 줄은 의도입니다

| | 화주 화면 | 관리자·DB |
|---|---|---|
| 견적 상태 | 협의 중 · 취소 | `보류` · `실패` |
| 배차 상태 | 3단계 (접수 / 배차완료 / 운송완료) | 6종 |

화주에게 "실패"라고 보여주면 **자기 요청이 문제였던 것으로 오해**하고, 담당자에게는 "실패"가 정확합니다.

🔴 **통일하려 들지 마십시오** — DB와 이력이 전부 걸립니다. **관리자 화면에 화주가 보는 이름을 병기**하는 쪽으로 갑니다.

⚠️ `문제발생`은 3단계 어디에도 자리가 없어 **별도 경고 배지**를 새로 만들어야 합니다.

---

## 6. 진행 현황

| 차수 | 내용 | 상태 |
|---|---|---|
| 15–17 | 거리 매칭 버그 · 운임 기준 교체 · 차급 9종 확장 | 완료 |
| 18–21 | 약관 동의 · anon RLS 정리 ①②④ | 완료 |
| 22 | 운임 매트릭스 보정 + 차급 11종 | 완료 |
| 23 | 화주포털 개편 사전조사 | 완료 |
| 24 | 포털 화면 ① — 셸 · 토큰 · 아이콘 · 홈 | 완료 |
| 25 | 포털 화면 ② — 발주 요청 · 배송지 · 프리셋 | 완료 |
| **26** | **시안 정합 — 서체 · 아이콘 · 드롭다운 · 달력** | **진행 중** |
| 27 | 내부관리시스템 연동 조사 | 지시서 완료 |
| 28 | 포털 견적 흐름 — 목록 · 상세 신규 · 견적서 부가세 | 예정 |
| 29 | 포털 조회 흐름 — 배차 · 정산 · 통계 · 담당자 | 예정 |
| 30~ | 관리자 개편 (범위는 27차 조사가 정함) | 예정 |

**미정** — 랜딩 재설계 + 이미지 5종 · 랜딩 「문의·신청 현황 조회」 진입 경로 · 상차 시각 지정 가산 신설 · 5톤급 운임 재검토 · anon GRANT 회수

🔴 **차수 번호는 예고가 아니라 실제 실행 순서로 붙입니다.** 화면 차수가 지금까지 **일곱 번** 뒤로 밀렸습니다.

---

## 7. 미결·위험

### 🔴 공개를 막는 것 — 1건

**랜딩 이미지 5종이 자리표시자**입니다. 히어로 · 운송 유형 · 차량 4장 · 아이콘 4종 · 운송관리 캡처. 랜딩 재설계 시안이 나온 뒤 넣기로 순서를 미뤘고, 파일이 준비되면 `lib/landingImages.ts` 한 곳만 채우면 됩니다.

### 🔴 법무

| | |
|---|---|
| **변호사 검토 미실시** | 약관·처리방침·동의 문구 전부. 시행일은 검토가 늦어져도 9/7로 간다고 확정했습니다 |
| **처리방침 제4조가 사실과 다름** | *"차주 성명·연락처·차량번호를 화주에게 제공한다"*는 행이 있는데 **실제 결정은 노출하지 않는 것**입니다. 법적 문서라 임의로 고치지 않고 두었습니다 |
| **개인정보 파기 장치 없음** | 견적문의 1년 · 등록신청 6개월로 고지했는데 코드·스케줄러·트리거 어디에도 삭제 장치가 없습니다. 🔴 **공개 후 1년 안에는 실제로 파기해야 합니다.** 지금은 데이터 0건이라 위반 아님 |
| **동의 고지 4항목 미완** | 수집항목·보유기간·거부권. 개인정보보호법 제15조 2항을 아직 다 갖추지 못했습니다 |

### ⚠️ 근거가 약한 값

- **15톤 운임** — 완료 실거래 1건이라 사실상 미검증
- **차량형태 신규 13종 가산율** — 기존 7종에서 산술로 유도한 값
- 둘 다 **관찰 대상**입니다. 실거래가 쌓이면 다시 봐야 합니다

### ⚠️ 확인 대기

- **무료 대기시간** — 무료 시간은 20분이 됐는데 `"초과 시 30분당"` 단위는 그대로입니다. 가격에 직접 영향
- **화주 입력값을 담당자가 보는가** — 배송지 담당자·연락처, 화물 프리셋, 적재구분, `loading_type`. **27차 조사가 판정합니다**

### ⏸️ 의도적으로 멈춘 것

- **보험** — 미가입. 플래그 off이고 켤 때 **두 값을 함께** 바꿉니다
- **이메일 발송** — 인프라만 구축, 미활성
- **첫 거래 혜택** — 조건·기간 미확정이라 문구를 전부 뺐습니다. 다시 쓰려면 조건부터

### 사용자가 배포본에서 확인할 것

- 운임기준표에 열이 **12개**인가(거리구간 + 차급 11), 18톤 값이 내려갔는가
- 발주 요청 희망 톤수가 **11종**인가
- 거리 `10.4`km · 1톤 → **60,000원** / 5톤 · 대기 90분 → **50,000원**
- `/vehicles` 시작가 **48,000원부터**
- `/apply` 체크박스가 **정확히 2개**이고 `/quote`에는 약관 문구가 없는가
- 공개문의 → 견적 전환 과거 건이 연결돼 있는가

---

## 8. 작업 방식

### 한 차수의 흐름

```
지시서 작성  →  구현 세션  →  실사용 리뷰  →  CLAUDE.md 갱신  →  기록 검증
   (값·완료조건·           (착수 전 확인      (배포본을          (되돌릴 위험이
    금지사항 확정)          부터)             눈으로)            적혔는지)
```

### 지켜서 효과를 본 규칙

| | |
|---|---|
| **한 번에 하나씩** | 값 변경과 구조 변경을 같은 차수에 섞지 않습니다 |
| **파일이 겹치지 않게 쪼갠다** | 차수를 나누는 기준입니다 |
| 🔴 **착수 전 확인** | 지시서 전제를 코드로 대조. **여덟 번** 사고를 막았습니다 |
| 🔴 **의도한 것은 적는다** | 안 적으면 다음 세션이 "정리"하다 되돌립니다 |
| **커밋 분리** | 원인 추적용. 버그 수정은 반드시 따로 뗍니다 |

### 지시서에 반드시 넣는 것

- **왜 하는가** — 사유가 없으면 판단이 필요한 순간에 임의로 갑니다
- **완료조건은 기계로 검증되게** — `"문구가 통일됐다"`가 아니라 `grep 0건`
- **하지 말 것** — 특히 **이전 차수가 의도적으로 해둔 것**
- **적용 순서** — DB가 먼저인지 코드가 먼저인지는 **매번 따집니다**. 한 방향으로 굳은 규칙이 아닙니다

### 검증

- **렌더링으로 확인합니다.** 목 Supabase 서버 + 세션 주입으로 포털·관리자 모두 띄울 수 있습니다
- **관리자 회귀는 `origin/main`을 같은 목으로 띄워 대조**합니다(before 스냅샷이 아니라)
- 🔴 **목은 실제 DB보다 헐거우면 안 됩니다** — `order`·`limit`·`gt`를 흉내내지 않으면 검증이 통째로 무의미해집니다
- **시안 대조는 소스에서 값을 뽑아** 합니다

---

## 9. 되풀이된 함정

| # | 함정 | 대응 |
|---|---|---|
| 1 | 🔴 **요약 문서·스크린샷을 근거로 삼기** | 시안 핸드오프 문서가 **네 곳** 실제와 달랐고, 스크린샷이 뷰포트로 잘려 오판이 두 번. **소스가 정본** |
| 2 | 🔴 **자동검증만 믿기** | 27개가 전부 통과했는데 시안과 다른 곳이 **여섯**. 구조·수치만 재고 "같은가"를 안 쟀습니다 |
| 3 | 🔴 **대상 위치를 적게 세기** | 2곳→4곳, 3곳→5곳, 1개→3개. **표로 못 박지 말고 전수 grep** |
| 4 | 🔴 **`pkill`·`kill`이 자기 자신을 죽임** | **다섯 번.** 대괄호 트릭으로도 안 막힙니다 — **PID 파일**을 쓰십시오 |
| 5 | 🔴 **dev 서버가 뜬 채로 빌드** | `.next`가 깨집니다. 빌드 전에 죽이고 지울 것 |
| 6 | 🔴 **`git checkout <파일>`로 시험 되돌리기** | 미커밋 편집을 통째로 날렸습니다. 원본을 `cp`로 복사해 **사본으로 복원** |
| 7 | 🔴 **정규식으로 소스를 긁어 배열 읽기** | 주석 안 문자열을 원소로 오인. **실제 모듈을 평가**해서 잴 것 |
| 8 | 🔴 **주석에 상수 이름을 문자로 쓰기** | 완료조건 `grep`에 주석 자신이 걸립니다. **세 번** |
| 9 | 🔴 **`error`를 버리고 빈 목록으로 두기** | 조회 실패가 "데이터 없음"으로 보입니다 |
| 10 | 🔴 **`.order()`를 없는 컬럼에 걸기** | 정렬은 불러온 뒤 JS로 하십시오 |
| 11 | **`npx tsc … \| tail`** | `tail`의 종료코드를 읽어 오판합니다. **파일로** 받으십시오 |
| 12 | **`<br />`로 한글 줄바꿈 고정** | 폰트가 CDN이라 폭이 달라지면 어색해집니다. `word-break: keep-all` |

---

## 10. 새 세션에서 이어가는 방법

1. 이 문서를 먼저 읽습니다
2. `CLAUDE.md` **최상단 100줄**(최신 세션)과 **맨 아래 로드맵**을 봅니다
3. 해당 차수의 **작업지시서**를 받습니다 — 값·완료조건·금지사항이 거기 있습니다
4. 🔴 **착수 전 확인부터** 합니다. 지시서 전제가 틀린 적이 여러 번입니다
5. 다르면 **임의로 판단하지 말고 보고 후 대기**합니다

⚠️ **사용자는 개발 초보자입니다.** 용어를 풀어 설명하고, 무엇을 왜 하는지 먼저 말한 뒤 진행하십시오.

⚠️ **`CLAUDE.md`만 바뀌는 커밋은 PR 없이 `main`에 바로 반영합니다**(사용자가 정한 규칙). 코드가 한 줄이라도 섞이면 이 규칙은 적용되지 않습니다.

---

## 부록 — v19 이후 무엇이 바뀌었나

v19는 **claude.ai 채팅에서 Claude Code로 전환하던 시점**(2026-07-23)의 문서입니다. 그 뒤 한 달 남짓 사이의 변화입니다.

| 영역 | v19 | v20 |
|---|---|---|
| 관리자 접근 | 공유 비밀번호 · 직원별 계정 없음 | **직원 계정·권한·이력** 완료 |
| 운임기준표 | 6차급 90행 | **11차급 165행** · 마진 17% 기준 · 실거래로 검증 |
| SQL 반영 | 사용자가 SQL Editor에 붙여넣기 | **GitHub Actions 자동화** — 세션이 직접 실행 |
| anon 정책 | 핵심 표에 전체 허용 | **잠금 완료** — anon이 개인정보를 못 읽습니다 |
| 랜딩 | 개편 전 | **8섹션 전면 개편** · 공개 헤더 옐로 |
| 화주포털 | 기능은 있으나 디자인 미개편 | **셸·홈·발주 개편 완료**, 견적·조회는 진행 중 |
| 약관 동의 | 없음 | **이용약관·제3자 제공 동의** 저장 구조 완료 |
| 시행일 | 미정 | **2026-09-07 확정** |
| 법인 표기 | 자리표시자 | **(주)디자인에그 │ 위캐리 운송** 확정 |

🔴 **v19에 있던 "다음 예정 작업"은 대부분 소진됐거나 순서가 바뀌었습니다.** 지금 유효한 로드맵은 위 §6이고, 원본은 `CLAUDE.md` 맨 아래에 있습니다.

---

*이 문서는 `CLAUDE.md`를 대체하지 않습니다. 근거와 이력은 그쪽에 있고, 이 문서는 지금 필요한 것만 추린 것입니다.*
