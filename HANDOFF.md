# WeCarry 운송 통합 운영 시스템 — 인수인계 문서

**버전** v21.5 · 2026-09-04
**기준** 세션 70차 + 네이버 등록 + SEO 메타 + 카피라이트·랜딩 이미지 + 무료 대기시간 단위 확정 + 프리셋 이름 표시(전부 차수 없음) / 저장소 `ttang0825/b2b-cargo-system` / 마지막 코드 커밋 `77e8f29`

> 🔴 **이 파일은 이름을 고정해서 씁니다.**
> 갱신할 때 `HANDOFF_v22.md` 같은 새 파일을 만들지 말고 **이 파일의 내용만 교체**한 뒤 위 버전 줄을 올리십시오.
> 옛 버전은 `git log -- HANDOFF.md`로 언제든 꺼내 볼 수 있고, 파일명을 바꾸면 `CLAUDE.md`의 안내가 깨집니다.

---

## 0. 이 문서를 읽는 방법

**두 문서의 역할이 다릅니다.**

| 문서 | 성격 | 언제 보나 |
|---|---|---|
| `CLAUDE.md` | 세션별 축적 기록 — **왜 그렇게 했는지의 근거**가 전부 여기 있다 (약 65만 자) | 특정 항목의 이력·판단 근거가 필요할 때 |
| **이 문서(v21)** | 지금 시점의 정돈된 스냅샷 | **처음 붙는 사람·세션이 먼저 읽는 문서** |

🔴 **저장소 코드가 이 문서와 다르면 저장소가 항상 맞습니다.**

⚠️ `CLAUDE.md`를 읽을 때는 **통째로 읽지 말고** 최상단(최신 세션 기록)과 §3 설계 원칙, §5 로드맵, §7 함정만 보고 나머지는 `grep`으로 찾으십시오.

---

## 1. 서비스 개요

**위캐리 운송** — B2B 화물운송 **주선업** 통합 운영 시스템.

화주 CRM · 견적 · 배차 · 정산을 관리하는 **내부 시스템**, 화주가 직접 발주하고 조회하는 **운송관리 포털**, 그리고 **공개 유입 화면**(랜딩 · 차량 안내 · 견적문의 · 계정신청)까지 하나로 묶여 있습니다.

**포지셔닝은 "정식 물류 파트너"**입니다. 즉시 자동견적·정찰제·완전 셀프가입을 하는 경쟁사와 반대로, **담당자가 직접 응대하는 관계형 영업**을 핵심 차별점으로 둡니다. 운임기준표와 견적 로직은 비공개이고, 공개하는 것은 **10km 이내 기준가 6행**뿐입니다.

🔴 **서비스 시행일은 2026년 9월 7일**입니다.
🔴 **도메인은 `wecarrylogis.co.kr`** — **`www` 없는 짧은 쪽이 메인**입니다(명함·문자·전화 안내용). `www`는 308 리다이렉트.

---

## 2. 기술 스택 · 배포 · 환경변수

- **Next.js (App Router)** + **Supabase**(Postgres · Auth · Realtime) + **Vercel**(함수 리전 **서울 `icn1`**)
- 저장소 `ttang0825/b2b-cargo-system` (`main`)
- 엑셀은 `xlsx`가 아니라 **`xlsx-js-style`** (스타일링 때문에 교체)
- SMS는 **솔라피**. 발신번호는 **로그인한 담당자 기준**으로 서버가 정합니다

⚠️ **네임서버는 가비아**, 웹은 Vercel, **메일만 카페24 웹메일**입니다. 🔴 **네임서버를 카페24로 옮기지 마십시오** — `A(@)`·`CNAME(www)`가 함께 넘어가 사이트가 통째로 죽습니다.

**SQL 변경은 GitHub Actions로 합니다.**

```
migrations/YYYY-MM-DD_무엇.sql  을 추가하고
Actions → "DB 마이그레이션" → dry-run → apply → verify
```

| 규칙 | |
|---|---|
| 🔴 `begin;`/`commit;` 쓰지 말 것 | 워크플로가 `--single-transaction`으로 감쌉니다 |
| 🔴 안전장치는 `select`가 아니라 `do $$ … raise exception … end $$;` | 조건이 틀리면 트랜잭션이 통째로 되돌아갑니다 |
| 🔴 **적용이 끝난 파일의 SQL을 나중에 고치지 말 것** | DB와 파일이 어긋납니다. 값을 바꾸려면 새 파일 |
| 🔴 `migrations/_baseline.txt` 수정 금지 | 과거 INSERT가 중복 실행됩니다 |
| 접속 | **Session pooler**(IPv4). Direct는 IPv6 전용이라 러너에서 실패 |

🟢 **세션이 직접 실행하고 로그까지 읽습니다** — 사용자가 버튼을 누를 필요가 없습니다.
🟢 `verify` 모드(읽기 전용)로 운임·동의·RLS 상태를 언제든 확인할 수 있습니다.

**환경변수(Vercel)** — `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `KAKAO_REST_API_KEY` · `SOLAPI_*`
**미등록(보류)** — `RESEND_API_KEY`(이메일 발송)

---

## 3. 화면 구성

### 공개 — 로그인 없음

`/` 랜딩(8섹션) · `/about` 회사소개 · `/vehicles` 차량·요금 안내 · `/quote` 견적 문의 · `/apply` 운송관리 계정 신청 · 법적 문서 3종(`/terms` · `/privacy` · `/email-policy`)

🔴 **`/status`(문의·신청 현황 조회)는 62차에 라우트·API·처리방침 행까지 통째로 삭제됐습니다.** 되살리지 마십시오. **지금 비회원이 접수 결과를 확인할 방법은 없습니다**(사용자가 그렇게 확정).

⚠️ 법적 문서 3종은 **페이지 + 모달 하이브리드**입니다 — 푸터 링크는 모달로 열리지만 URL도 살아 있습니다(분쟁 시 시점 특정용). 조문을 고칠 때는 `lib/legal/*.ts`만 고치면 양쪽에 동시 반영됩니다.

### 화주 운송관리 포털 — 화주 로그인

홈 · 발주 요청 · 견적 확인 · 배차·운송 조회 · 정산·결제내역 · 월별 통계 · 배송지·화물 관리 · 담당자 정보

🔴 **메뉴에는 없지만 살아 있는 라우트 셋** — 캘린더 · 공지사항 · **비밀번호 변경**. **"안 쓰는 라우트"로 보고 지우지 마십시오.** 비밀번호 변경을 지우면 신규 계정의 첫 로그인이 갇힙니다.

- 공지사항 진입로는 **홈의 공지 블록 하나뿐**입니다
- 🟢 캘린더는 **월별 통계 화면 하단**에 들어갔습니다(60차). `/customer/calendar` 라우트와 같은 컴포넌트를 씁니다

🔴 **포털 로그인은 이메일이 아니라 시스템 발급 아이디**입니다(`we` + 발급일 YYMMDD + 2자리). 내부적으로만 합성 이메일로 Supabase Auth에 등록합니다.

### 내부관리 — 직원 로그인

| 그룹 | 화면 |
|---|---|
| 화주 확보 | 화주관리 · 화주신청 · 공개문의 · 개인고객 |
| 화주 관리 | 활성화주 CRM · 화주요청 |
| **운송 운영** | 운임기준표(+가산기준·산재보험료 탭) · 견적 · 오더 · 차주 · 배차 · 정산 |
| 시스템(admin 전용) | 운영 대시보드 · 직원 계정 · 지원접속 이력 · 문자 이력 |

---

## 4. 데이터·정책 구조에서 반드시 아는 것

**설계 원칙은 `CLAUDE.md` §3에 1~58번으로 정리돼 있습니다.** 여기에는 최근에 실제로 사고를 만든 것만 적습니다.

### 4-1. 🔴 화주와 직원이 같은 인증 롤을 씁니다

둘 다 `authenticated`입니다. **컬럼 GRANT로는 구분되지 않습니다.** 두 번 문제가 됐고, 둘 다 **service_role 서버 API**로 옮겨 해결했습니다.

🔴 **RLS 정책은 롤별로 갈립니다** — `TO anon` 정책은 `authenticated` 요청에 적용되지 않습니다. 이걸 모르고 세운 지시서가 실제로 틀렸습니다.

### 4-2. 🔴 anon은 개인정보를 읽지도 쓰지도 못합니다

정책 21개를 지우고 RLS 13개를 켰습니다. 남은 anon 경로는 **INSERT 2개뿐**(`public_quote_requests` · `customer_applications`) — 지우면 견적 문의와 계정 신청이 죽습니다.

⚠️ **RLS on + 정책 0개**인 표들(`consents` 등)은 **service_role 전용이 정상**입니다. 고치지 마십시오.
⚠️ **③(anon GRANT 회수)는 아직입니다** — RLS가 이미 막고 있어 추가 방어이며 급하지 않습니다.

### 4-3. 견적 계산

```
최종금액 = 기본운임 + 가산 − 혼적할인
```

🔴 **마진율 파라미터가 없습니다.** `rate_distance_tiers.base_fare`가 곧 **화주 청구가**이고, 차주 지급액은 배차 단계에서 담당자가 수동 입력합니다. 마진은 그 차액입니다.

🔴 **가산은 `option_name` 문자열 완전일치로 매칭한 뒤 못 찾으면 `continue` 합니다** — 예외도 경고도 없이 **가산만 조용히 빠집니다.** 그래서 **옵션 라벨을 바꾸는 코드와 `rate_surcharges` 행은 반드시 같은 커밋**에 넣습니다.

🔴 **거리 구간 매칭은 "상한 이하 첫 구간"이고 `distance_from_km`을 보지 않습니다** — 조건에 다시 넣으면 정수 사이 빈틈이 살아나 소수 거리(10.4km 등)에서 자동계산이 조용히 실패합니다.

### 4-4. 🔴 포털 CSS는 관리자와 거의 전부 공유입니다

포털이 쓰는 클래스 25개 중 **전용은 하나뿐**이고 `.container`·`.card` 등을 관리자 화면 31개가 같이 씁니다. 그래서 포털 개편은 **`.portal-v2` 스코프 격리**로 갔고, 새 컴포넌트는 전부 `.pv2-*`입니다.

🔴 **"중복이니 전역으로 합치자"고 하지 마십시오.** 합치는 순간 관리자가 딸려옵니다.
🔴 **`globals.css`는 순수 추가만** — 삭제된 줄 0이 관리자 무회귀의 근거입니다.
🔴 **포털 폼에 네이티브 `<select>`·`<input type="date">`를 쓰지 마십시오**(원칙 57번) — `Pv2Select` · `Pv2DatePicker`를 씁니다. 네이티브는 펼친 목록·달력을 브라우저가 그려서 CSS로 못 바꿉니다.

### 4-5. 🔴 `force-dynamic`만으로는 캐시를 못 막습니다

그 지시자는 *라우트 렌더링*만 동적으로 만들고, 라우트 안에서 supabase-js가 쓰는 `fetch`는 그대로 Data Cache를 탑니다. **DB를 바꿨는데 API가 옛 값을 계속 내려주는** 일이 실제로 있었습니다.

🔴 **새 서비스롤 GET 라우트에는 `force-dynamic`과 `createServiceClient()`가 둘 다 필요합니다.**

### 4-6. ⚠️ CSS 특성도와 인라인 style

이 저장소는 화면 스타일을 **인라인 `style`로 많이 씁니다.** 그래서 —

- 🔴 **클래스 규칙으로는 인라인을 못 이깁니다.** 인라인을 덮어야 하면 `!important`가 필요하고, 그건 우회가 아니라 **유일한 방법**입니다
- 🔴 **특성도가 같으면 선언 순서가 이깁니다** — 레이아웃이 어긋날 때 **규칙 순서를 바꿔 고치지 마십시오**(다음에 누가 정렬하면 조용히 되돌아갑니다). `.a.b`처럼 **두 클래스로 특성도를 올려서** 고칩니다

⚠️ **미디어쿼리는 특성도를 올려주지 않습니다.** `@media` 안의 한 클래스 규칙은 바깥의 두 클래스 규칙을 못 이깁니다 — 모바일 전용 값이 조용히 안 먹는 형태로 나타납니다(2026-09-04에 `.pv2-load-slot`이 모바일에서 100%가 아니라 190px로 남아 있던 것이 이 경우). 🔴 **`@media` 안에도 같은 특성도로 써야 합니다.**

---

## 5. 확정 사항

### 5-1. 운임

| 항목 | 값 |
|---|---|
| 차급 | **11종** — 1 / 1.4 / 2.5 / 3.5 / 5 / 5톤 플러스·축 / 8 / 11 / 15 / 18 / 25 |
| 거리 구간 | 15구간 · **총 165행** |
| 🔴 현재 기준가 | **1톤 10km = 40,000원** — 운영 DB가 v8 원본값(마진 17% 얹기 *전*)입니다 |
| 18톤 | 전 구간 **×0.89** — 🔴 단 `200km 이내`만 **−5.5%**(341,000) |
| 8톤·15톤 | 인접 차급 **보간값**(근거 등급 D) |
| 무료 대기 | **20분** |
| 차량형태 | **21종**(견적용) / 차주 보유차량은 별도 24종 |
| 상하차방식 | **8종** |

🔴 **기준가 ×0.833 인하는 「의도한 인하」로 확정됐습니다**(65차 ①). 45차 마이그레이션 값(48,000)은 **옛 값**이니 "인하됐네" 하고 되돌리지 마십시오.
⚠️ **그 결과 5톤급 역마진(−3.7%)이 남아 있습니다** — 마진을 다시 볼지는 별도 판단입니다.

🔴 **18톤 `200km 이내` 한 칸만 다른 이유** — ×0.89 하면 321,000이 되어 11톤 325,000보다 낮아집니다(차급 역전). 11톤 대비 +5% 바닥을 걸었습니다. **"왜 이 칸만 다르지" 하고 맞추지 마십시오.**

🔴 **무료 20분 / 초과 30분당 단위는 「현행 유지」로 확정됐습니다**(2026-09-04). 25차부터 답을 기다리던 항목이고, **문구와 동작이 이미 일치합니다** — 계산식이 실제로 `ceil((대기분 − 20) ÷ 30) × 단가`라 「무료는 20분인데 왜 30분당이지」 하고 **문구만 고치면 오히려 표시가 틀려집니다.** 단위만 20분으로 줄이면 단가는 그대로인 채 구간이 촘촘해져 **대기료가 최대 2배**가 됩니다(대기 50분 1톤 10,000 → 20,000).
⚠️ **단위 `30`은 `app/admin/quotes/page.tsx` 한 곳에만 하드코딩돼 있고 DB 컬럼이 없습니다** — 무료시간·단가는 담당자가 `/admin/rates`에서 바꿀 수 있는데 단위만 못 바꿉니다. 운영에서 조정할 일이 생기면 그때 컬럼을 하나 더 만드는 것이 맞습니다.

🔴 **11톤·25톤은 손대지 마십시오** — 11톤은 검증됐고(완료 36건, 마진 +19.6%), 25톤은 완료 3건이라 판단 불가입니다.

🟢 **공개 기준가는 DB를 실시간으로 읽습니다**(`lib/startPrices.ts`, 64차). 게시 범위는 **「10km 이내」 × 6차급**(1톤 ~ 5톤 플러스/축)이고, `/admin/rates`에서 그 칸을 고치면 **확인 창**이 뜬 뒤 바로 게시됩니다.
🔴 **폴백 금액을 다시 만들지 마십시오** — 두면 반드시 낡고, 게시가 ≠ 견적가가 되면 그대로 표시가격 분쟁입니다. 조회 실패 시에는 표를 안 그리고 안내 문구만 남깁니다.

### 5-2. 표기

| 항목 | 값 |
|---|---|
| 법인 · 브랜드 | **(주)디자인에그 │ 위캐리 운송** — 가운데는 글자가 아니라 **세로 구분선** |
| 사업자등록번호 | 105-87-66929 |
| 입금 계좌 | 국민은행 069101-04-228121 · 예금주 (주)디자인에그 |
| 고객센터 | 1661-2403 · 평일 09:00–18:00 |
| 대표 메일 | **biz@wecarrylogis.co.kr** (카페24 웹메일) |
| 도메인 | **wecarrylogis.co.kr** (`www` 없는 쪽이 메인) |
| 시행일 | **2026-09-07** |

🔴 **법인·브랜드는 어느 한쪽도 강조하지 않습니다** — 같은 크기·같은 굵기. 화면·인쇄에는 평문이 아니라 **전용 컴포넌트**(`CompanyNameMark`)를 씁니다(평문을 그리면 구분선이 글자로 찍힙니다).

🔴 **메일 주소는 상수 한 줄(`COMPANY_CONTACT_EMAIL`)이 전부입니다** — 처리방침 제11·12조와 사업자 정보가 전부 그 상수를 참조합니다. **개인정보 전용 주소를 따로 만들지 않기로 확정**했고(69차 ③), **상수를 둘로 나누지 마십시오.**
⚠️ `COMPANY_INFO.email`(견적서 PDF 발행업체용)은 **별개 상수이고 빈 값**입니다. 둘을 합치지 마십시오.
⚠️ 대표번호 상수도 **두 개**입니다(고객센터 / 견적서 발행업체 TEL). 지금은 같은 값이지만 **바꿀 때 두 곳을 함께** 고쳐야 합니다.

### 5-3. 🔴 공개 화면 문구

**확정 문구 — 「1톤부터 5톤 이상, 특수차량까지」** (시안 원문, 30차 리뷰 2라운드 확정)

⚠️ **v20의 「1톤부터 5톤 이상까지」에서 바뀌었습니다.** 「특수차량」과 「25톤」은 **금지어에서 풀렸습니다**(차급 11종이 실재해 견적이 실제로 산출되기 때문). 다만 —

| | |
|---|---|
| 🔴 여전히 금지 | `전 차종` · `모든 차량` · `최저가` · `예비 배차처` · `계약 없이` · `기존 거래처를 바꾸` · **`이사`** |
| 🔴 여전히 금지 | 연착 · `보장합니다` · `빠짐없이` · `남김없이` · `염려 없이` 같은 **단정 표현** |
| 🔴 여전히 금지 | 보험 **금액**(약관 제19조가 금액 없는 설계입니다) |
| ⚠️ 조건부 허용 | `월별 이벤트` · `운임할인 · 적립 · 캐시백` — **실제로 운영돼야 하고 조건·기간을 함께** 알려야 합니다(표시광고법 제3조) |

🔴 **「이사」는 65차에 고객 접점에서 다시 뺐습니다**(wecarry24.co.kr로 분리). 30차 기록을 근거로 되살리지 마십시오. 단 `public/landing/service-04-moving.jpg`는 **그 사이트가 쓰므로 지우지 마십시오.**

**금지 표현의 적용 범위**

```
광고·유입   /, /vehicles, /quote, /apply     → 차급 6종만 (VEHICLE_TYPES_PUBLIC)
화주 입력   /customer/request 희망 톤수       → 11종 (로그인 화주 대상이라 광고가 아님)
표시        포털 조회 · 견적서 · 엑셀         → 저장값 그대로
관리자·DB                                     → 11종 (VEHICLE_TYPES_ALL)
```

🔴 **두 배열을 합치지 마십시오** — 특히 `/apply`는 배열을 `join(", ")`으로 본문에 뿌려서, 뚫리면 공개 화면에 `25톤`이 그대로 찍힙니다.
🔴 **검증은 소스가 아니라 렌더링 텍스트에 겁니다** — 소스에 걸면 금지어를 경고하는 주석 자체가 매치됩니다.

### 5-4. 🔴 용어가 갈리는 두 줄은 의도입니다

| | 화주 화면 | 관리자·DB |
|---|---|---|
| 견적 상태 | 협의 중 · 취소 | `보류` · `실패` |
| 배차 상태 | **3단계** (접수 / 배차완료 / 운송완료) + 「문제 발생」 별도 배지 | 6종 |
| 정산방식 | 「운임 수금방식」 위캐리 수금 / 선착불 + **별도 줄** 「청구」 건별 / 월정산 | 2축 4값 |

화주에게 "실패"라고 보여주면 **자기 요청이 문제였던 것으로 오해**하고, 담당자에게는 "실패"가 정확합니다.

🔴 **통일하려 들지 마십시오** — DB와 이력이 전부 걸립니다.
🟢 배차 3단계 매핑의 정의처는 **`lib/dispatchStage.ts` 하나**입니다(배차 화면·홈 공용). 🔴 `lib/dispatchStatusColors.ts`(6종)를 이 매핑으로 바꾸지 마십시오 — 관리자 3화면이 그대로 씁니다.
🔴 **차주 성명·연락처·차량번호는 화주에게 노출하지 않습니다**(마스킹도 안 함).

### 5-5. 🔴 약관 — 「차주·보험자 1차 부담 + 회사 중재」

66차 리뷰 2라운드에 **방향이 뒤집혔습니다.** 34차의 「회사가 배상 주체」 설계는 옛 기록입니다.

| 삭제 금지 | 이유 |
|---|---|
| 제18조 3항 **마지막 문장**(「회사 자신의 배상책임은 제1·2항 및 제19조에 따른다」) | 없으면 전면 면책으로 읽혀 그 항이 무효가 됩니다 |
| 제19조 **8항**(고의·중과실 예외) | 7항 단서를 뺀 뒤로 **유일한 방어선**입니다 |
| 제19조 5항의 **「그로 인하여 발생하거나 확대된」** 인과 한정 | 없으면 유리제품·계란류에 상법 근거가 없어 무효 위험 |

🔴 **되살리기 금지** — 제19조 2항의 「보험금 지급 여부와 관계없이」 · 7항의 과실 경합 「다만」 단서 · 랜딩 「배상해 드립니다」 · 5항에서 뺀 두 호(살아있는 동물 · 법령상 제한 — **제12조와 정면으로 부딪힙니다**).

🔴 **변호사 검수 1순위 — 확정운임 → 상법 제119조 2항 운송인 의제 가능성.** 위캐리는 화주에게 확정 운임을 청구하므로 운송인으로 의제될 수 있고, 그러면 차주가 이행보조자가 됩니다. **그래서 「회사는 책임이 없다」로 더 밀지 않았습니다** — 강행법규에 밀려 무효가 되면 한도·면책까지 같이 잃습니다.

⚠️ **특별약관을 하나도 가입하지 않아 제외 화물 6종은 고지해도 보험금이 안 나옵니다** — 원고류 · 귀금속류 · **시계·담배·모피류** · **유리제품** · **계란류** · **살아있는 동물**. 「유리제품」 정의가 넓어 **유리병에 담긴 음료·소스·주류**가 그대로 들어갑니다.
🔴 **화물이 없어진 사고는 즉시 경찰에 도난신고를 접수해야 보험이 열립니다** — 약관이 그 외의 망실을 전부 「분실」로 간주하고 면책하기 때문입니다. **이 한 줄이 1억과 0을 가릅니다.**
🔴 **그래서 공개 화면에 「분실」이라는 낱말을 쓰지 마십시오**(70차 ②) — 보험이 안 나오는 바로 그 경우를 약속하는 문장이 됩니다. 「없어지거나」로 씁니다.
⚠️ **자기부담금 20만원은 회사가 흡수합니다** — 파손 문의에 「보험으로 처리된다」고 답하지 마십시오.

### 5-6. 랜딩 WHY 카드 (70차)

🔴 **`desc` = 접힘에 보이는 짧은 소개 / `detail` = 펼치면 그것을 이어서 설명.** 되풀이하지 마십시오 — 다섯 장 중 넷이 `detail` 첫 줄에서 `desc`를 그대로 되풀이하고 있어서 "단계가 어색하다"는 지적을 받았습니다. **05(보험)가 본보기**입니다.

🔴 **펼침은 카드가 아니라 이어지는 본문입니다** — 흰 배경·테두리·radius·안쪽여백 0이고 글자 규격이 `desc`와 **완전히 동일**합니다. 2026-09-02의 「부가 설명 카드」 확정을 뒤집은 것이니 되살리지 마십시오.
🔴 **화살표는 제목 옆이 아니라 설명글 바로 아래**입니다. `<summary>` 밖으로 빼면 클릭이 안 먹습니다.
🔴 **보험 카드는 맨 뒤여야 합니다** — 미가입이면 배열에서 빠지는데, 뒤에 있어야 남는 번호가 01~04로 이어집니다.

### 5-7. 모바일 랜딩 (70차)

🔴 **모바일 여백 축소는 전부 `@media (max-width: 700px)` 안입니다 — 데스크탑 변경 0.** 섹션 위아래를 40~45% 줄여 전체 높이가 11,128 → 10,312px이 됐습니다. **여기서 더 줄이지 마십시오**(사용자 단서: 「너무 타이트하면 안 된다」).

🔴 **차량 목록은 모바일에서 자동으로 흐릅니다**(32px/s, `useAutoMarquee`).
- 목록을 **두 벌 이어 붙여** 되감기는 순간이 안 보이게 합니다. 뒤쪽 12장은 데스크탑에서 `display:none`
- 🔴 **정수 픽셀은 `scrollLeft`, 소수 픽셀은 안쪽 줄의 `transform`이 나눠 싣습니다** — 브라우저가 `scrollLeft`를 정수로 반올림해서 덜덜거리기 때문이고, **둘 중 하나만 쓰면 그 증상이 그대로 돌아옵니다**
- 🔴 `.landing-vehicle-track`은 데스크탑에서 `display: contents` — 지우면 그리드가 깨집니다
- 🔴 **스크롤 스냅을 되살리지 마십시오** — 자동 이동을 죽입니다

### 5-8. 🔴 SEO 메타 (PR #118)

**메타는 `lib/pageMetadata.ts`의 `buildPageMetadata` 한 곳에서만 만듭니다.** 화면은 제목·설명·경로 셋만 넘기고, 그 함수가 같은 값을 `<title>`·`description`·`og:*`·`twitter:*`·`canonical`에 넣습니다. **`og:title`·`og:description`을 따로 적지 마십시오** — 두 벌이 되면 조용히 갈립니다.

🔴 **description은 80자 이내입니다.** 네이버 서치어드바이저 「간단체크」 권고이고, 넘으면 **「사이트 설명」·「Open Graph 설명」 두 항목**에 ⚠️ 가 뜹니다. 두 항목이 같은 값이라 **한쪽만 고칠 수 없고, 한 번 줄이면 둘 다 사라집니다.** 넘으면 빌드 로그에 `[metadata]` 경고가 남습니다(빌드를 실패시키지는 않습니다).

🔴 **`lib/structuredData.ts` — Organization JSON-LD를 넣은 이유는 SEO 일반이 아니라 동명 회사 구별입니다.** 네이버에 「위캐리 운송」을 검색하면 **물류업에만 「위캐리」가 셋**입니다((주)위캐리로지스 부천 · (주)위캐리 제주 · (주)위캐리컴퍼니 서울). **사업자등록번호와 운송주선사업 허가번호가 갈라주는 유일한 식별자**라 `identifier` 배열로 내보냅니다 — **그 둘을 지우면 이 파일의 존재 이유가 없어집니다.**

- 🔴 **`name`은 「위캐리 운송」 — 「WeCarry」를 넣지 마십시오.** 경쟁사 블로그가 그 영문 표기를 쓰고 도메인도 `wecarry_logis`로 겹쳐, 브랜드명으로 영문을 쓰면 구별이 오히려 흐려집니다. ⚠️ **화면의 시각 브랜딩이 영문인 것은 별개이고 26차 확정입니다**
- 🔴 **`LocalBusiness`로 바꾸지 마십시오** — 손님이 찾아오는 사업장을 뜻합니다. 주선업이고 스마트플레이스를 쓰지 않기로 한 결정과도 어긋납니다
- 🔴 **값을 그 파일에 문자열로 적지 마십시오** — 전부 기존 상수 참조입니다
- 🔴 **루트 layout에 둡니다** — 공개 화면마다 붙이면 새 경로에서 빠뜨립니다
- ⚠️ **`sameAs`는 비어 있습니다** — 네이버 블로그를 개설하면 그 주소를 넣을 자리입니다
- ⚠️ **랜딩 description에서 「정식 허가업체(제180254호)」를 뺐습니다**(80자 제약). 허가번호는 JSON-LD·`/about`·푸터에 남아 있고, 되살리려면 다른 문장을 그만큼 줄여야 합니다

⏸️ **콘텐츠 페이지 확충 · IndexNow · 파비콘은 미착수입니다.** 🔴 **`keywords` 메타는 일부러 넣지 않았습니다** — 구글은 무시하고 네이버도 랭킹 요소로 밝힌 바 없으며 과하면 스팸 신호가 됩니다.

### 5-9. 🔴 상호 표기와 랜딩 이미지 (PR #119)

**상호는 문자열이 아니라 `components/CompanyNameMark.tsx`로 그립니다** — `(주)디자인에그 │ 위캐리 운송`. 가운데는 **슬래시 글자가 아니라 세로 구분선**입니다(54차 확정). 쓰는 곳은 **일곱**입니다 — 견적서 상세 · print 2종 · 사업자정보 모달 · 랜딩 푸터 상호 줄 · **카피라이트 3곳**(랜딩 푸터 · 공용 푸터 · 운송관리 로그인).

- 🔴 **기본값(`#c4c4c4` · 여백 9px)을 바꾸지 마십시오** — 견적서 4곳이 그 경로를 씁니다. 견적서(14px 굵은 글자·흰 배경) 기준으로 맞춘 값입니다
- 🔴 카피라이트는 12.5px 흐린 글자라 그대로 쓰면 **구분선이 글자보다 진해 보입니다** → 그 세 곳만 `separatorColor="currentColor"` · `separatorGap={7}`로 넘깁니다. **`currentColor`인 것이 요점**입니다(세 곳의 글자색이 서로 다르고, 나중에 푸터 색을 바꿔도 따라옵니다)
- 🔴 **`COMPANY_LEGAL_NAME`(「주식회사 디자인에그」)을 지우지 마십시오** — 사업자 표시사항 「상호」 행 · 브랜드 고지 문장 · 법적 문서가 계속 씁니다. **카피라이트 줄에서만 빠졌습니다**
- 🔴 **「위캐리 운송」을 붙여 쓰지 마십시오** — 실행계획 §0-5의 전략 키워드가 네 글자입니다

**랜딩 이미지 — 두 가지를 되돌리지 마십시오.**

- 🔴 **서비스 카드 첫 장만 `loading="eager"`입니다**(`app/page.tsx`, `fetchPriority="low"` 동반). 모바일에서 그 섹션이 y=2161px인데 브라우저 지연로딩 선행 거리가 약 1250px이라 **보이는 순간에야 받기 시작**했습니다 — 4G 실측 **회색 상자 371ms · 3/3회**. eager로 바꿔 **0ms · 0/3회**가 됐습니다. 🔴 **둘째 카드부터는 `lazy` 유지 — 넷 다 eager로 만들지 마십시오**(첫 화면이 느려집니다)
- 🔴 **히어로·CTA·service-02·03은 품질 90 재인코딩본입니다**(크기·구도 동일). 랜딩 이미지가 **10.9MB → 5.5MB**가 됐습니다. 표시 크기 기준 원본 대비 **평균 차이 0.1~0.7%**. **원본이 필요하면 61차 커밋에서 꺼내십시오** — 다시 인코딩하면 세대 손실만 쌓입니다
- ⚠️ **안 건드린 것**: `service-00`·`service-01`(이득이 19K·11K뿐) · `service-04-moving`(랜딩에서 안 쓰고 `wecarry24.co.kr`이 가져다 씁니다) · TMS PNG 3.6MB(화면 캡처라 PNG가 맞고 WebP 전환은 별도 판단)

---

## 6. 진행 현황

| 차수 | 내용 | 상태 |
|---|---|---|
| 15–17 | 거리 매칭 버그 · 운임 기준 교체 · 차급 9종 확장 | 완료 |
| 18–22 | 약관 동의 · anon RLS 정리 ①②④ · 운임 보정 + 차급 11종 | 완료 |
| 23–27 | 포털 사전조사 · 셸/홈 · 발주 흐름 · 시안 정합 · 견적 흐름 | 완료 |
| 28–29 | 내부관리 연동 조사 · 포털 조회 흐름 | 완료 |
| 30–31 | 랜딩 교체(이미지 32종) · 폼 3화면 | 완료 |
| 차수 없음 | 도메인 확정 · 랜딩 소수정 · 랜딩 2차 개편 + 법적 문서 · 이용약관 개정 · 메일 로고 · 고정화물 사진 · 대표메일 교체 · **랜딩 WHY + 모바일**(70차) | 완료 |
| 차수 없음 | **네이버 서치어드바이저 등록**(2026-09-04) — 사용자가 `main`에 직접 올림(`50d6c8b`). 🔴 **코드 변경 0**, 세션은 서빙 확인만 | 완료 |
| 차수 없음 | **SEO 메타 보강**(PR #118 `887f6ff`) — Organization JSON-LD 신설 · description 4곳 80자 이내 · 길이 가드. 🔴 **DB 0 · 화면 변화 0** | 완료 |
| 차수 없음 | **카피라이트 + 랜딩 모바일 이미지**(PR #119 `4580959`) — 상호 병기 3곳 · 첫 서비스 사진 `eager`로 팝인 371ms → 0 · 랜딩 이미지 10.9MB → 5.5MB. 🔴 **DB 0** | 완료 |
| 차수 없음 | **무료 대기시간 단위 확정**(2026-09-04) — 25차부터 답 대기하던 「초과 시 30분당」을 **현행 유지**로 닫음. 🔴 **코드 0 · DB 0**(`verify` 1회만) | 완료 |
| 차수 없음 | **불러온 프리셋 이름 표시**(PR #120 `77e8f29`) — 26차부터 답 대기하던 항목. 불러오기 드롭다운 옆에 이름을 보조로. 🔴 **DB 0** | 완료 |
| **32** | **내부 정합 ① 견적·오더** | **다음** |
| 33 | 내부 정합 ② 배차·정산 | 예정 |
| 34 | 내부 정합 ③ 화주·차주 | 예정 |

**미정** — 담당자 정보(`customer/profile`) 소수정 · admin 공개문의 배지 · 상차 시각 지정 가산 신설 · 5톤급 운임 재검토 · anon GRANT 회수 · 접수 안내 문자 신설

🔴 **차수 번호는 예고가 아니라 실제 실행 순서로 붙입니다.** 화면 차수가 지금까지 **여덟 번** 뒤로 밀렸고, **차수를 아예 쓰지 않은 세션이 12번** 있었습니다.
🔴 **세션을 병행하면 차수가 겹칩니다** — 번호는 **먼저 merge한 쪽**이 가져갑니다(67·68·69차에서 실제로 발생). `CLAUDE.md`는 **merge 직후에** 쓰십시오. PR 안에 넣으면 반드시 충돌합니다.

---

## 7. 미결·위험

### 🟢 공개를 막는 코드 작업은 없습니다

v20의 유일한 차단 항목이던 **랜딩 이미지가 전부 해소됐습니다** — 30차에 디자인팀 이미지 32종이 들어왔고, 마지막 「고정화물 배차」 카드 사진도 68차에 채워졌습니다.
🔴 **`lib/landingImages.ts`에 자리표시자(`null`)를 다시 허용하지 마십시오.**

### 🔴 법무 — 남은 유일한 선행 조건

| | |
|---|---|
| **변호사 검토 미실시** | 약관·처리방침·동의 문구 전부. 🔴 **이것이 지금 유일한 선행 조건입니다**(보험사 문의는 2026-09-03에 취소 확정). 워드 세 벌을 **새 메일 주소로 갱신해서** 보낼 것 |
| **처리방침 제4조가 사실과 다름** | *"차주 성명·연락처·차량번호를 화주에게 제공한다"*는 행이 있는데 **실제 결정은 노출하지 않는 것**입니다. 법적 문서라 임의로 고치지 않고 두었습니다 |
| **처리방침 제4조에 보험회사 행이 없음** | 사고 시 담당자 성명·연락처와 경위가 DB손해보험으로 넘어갑니다. 추가가 필요해 보이며 **변호사 검토와 함께 물을 항목**입니다 |
| **개인정보 파기 장치 없음** | 견적문의 1년 · 등록신청 6개월로 고지했는데 코드·스케줄러·트리거 어디에도 삭제 장치가 없습니다. 🔴 **공개 후 1년 안에는 실제로 파기해야 합니다** |
| **열람청구 10일 처리 의무** | 개인정보보호법 제35조 3항. 전용 주소가 없어 **일반 문의에 섞여 들어옵니다** — 메일 자동 분류 규칙으로 대비할 것 |

⚠️ 판본은 현재 **약관 `terms-v2` · 처리방침 `privacy-v2`**입니다. 🔴 **본문을 실질적으로 고치면 판본을 반드시 올리십시오** — 판본은 파일 diff가 아니라 **게시되는 문서 내용**을 기준으로 올립니다.

### ⚠️ 근거가 약한 값

- **15톤 운임** — 완료 실거래 1건이라 사실상 미검증
- **장척/중량 물품특성** — 표본 5건(v8 자신이 "5건 미만은 바꾸지 말 것"이라 한 경계선)
- **차량형태 신규 13종 가산율** — 기존 7종에서 산술로 유도한 값
- 전부 **관찰 대상**입니다. 실거래가 쌓이면 다시 봐야 합니다

### ⚠️ 확인 대기

- **「월별 이벤트」·「운임할인/적립/캐시백」** — 실제 운영 여부와 적용 조건·기간(표시광고법)
- **혼적(합짐) 문장을 요금 설명글에 되살릴지**
- **견적서 머리 금액을 부가세 별도로 내릴지**(10차·53차 위계를 뒤집는 일이라 물어둔 상태)

### ⏸️ 의도적으로 멈춘 것 / 하지 않기로 한 것

- **이메일 발송(Resend)** — 인프라만 구축, 미활성
- **개인정보 전용 메일 주소** — 만들지 않기로 확정(69차 ③)
- **보험사 문의 6문항** — 취소 확정(66차 ⑬). 「확인 대기」로 되살리지 마십시오
- **펼친 상태 화살표 흐리게/숨기기** — 제안했고 「지금대로」로 확정(70차)
- **자동 슬라이드를 데스크탑에도** — 모바일 한정이 의도입니다

### 사용자가 코드 밖에서 할 일

- 🔴 **배포 후 모바일에서 랜딩을 한 번 내려보십시오**(PR #119) — ① 「고정화물 배차」에 회색 상자가 스치는지 ② **히어로·마감CTA 배경이 예전보다 흐릿해 보이지 않는지**(수치로는 차이가 0.1~0.7%지만 눈으로 봐야 확실합니다) ③ 맨 아래 카피라이트 작대기. ⚠️ **파일명이 그대로라 캐시가 남습니다** — 안 바뀐 것 같으면 새로고침을 강하게 하십시오
- 🔴 **변호사 검수 의뢰**(약관 + 처리방침) — **유일한 선행 조건**
- 🔴 `biz@` 기본환경에서 발신자명을 「위캐리 운송」으로 · 서명 켜기
- 🟡 메일 자동 분류 규칙(제목에 「열람」·「개인정보」·「정정」·「삭제」)
- ⚠️ 카페24에 **POP3S(995) 지원 여부 문의** — 지금은 POP3(110) 평문뿐이라 처리방침 제9조와 어긋납니다. 그래서 POP3·SMTP를 꺼 두고 웹메일로만 씁니다
- 🔴 **카카오톡 OG 캐시 초기화** — `https://developers.kakao.com/tool/clear/og`
- ⚠️ 증권 만기 알림(일반화물 2027-09-01 · 이사화물 2027-04-05)
- ⚠️ 원격 임시 브랜치 정리(`claude/*` — 이 환경은 삭제가 막힙니다)
- 🟢 **네이버 서치어드바이저 등록은 2026-09-04에 끝났습니다** — 사이트 등록 · 소유확인 · 사이트맵 제출 · 수집요청. ⚠️ **구글 서치콘솔은 아직입니다**
- 🔴 **배포 후 「간단체크」를 한 번 더 돌리십시오** — 「사이트 설명」·「Open Graph 설명」 두 ⚠️ 가 ✅ 로 바뀌어 **8개 항목 전부 ✅** 가 되어야 정상입니다(PR #118에서 description을 80자 이내로 줄였습니다). ⚠️ 진단은 **랜딩 하나만** 보므로 다른 화면은 URL 칸에 `https://wecarrylogis.co.kr/quote` 처럼 넣어 각각 돌리십시오
- ⚠️ 구글 [리치 결과 테스트](https://search.google.com/test/rich-results)로 Organization JSON-LD 인식 확인(이 환경은 프록시가 막혀 확인 못 했습니다)
- 🔴 **「위캐리」 제39류(운송·보관업) 상표 선등록 확인**(kipris.or.kr, 10분) — 출원은 안 하기로 확정했지만 **선등록 확인은 별개**이고 아직 안 됐습니다
- 🔴 **네이버 블로그 개설 + 주 1회** — 스마트플레이스를 포기(`(주)디자인에그`로 이미 등록돼 있고 주소가 같음)해서 **블로그가 첫 화면을 잡는 유일한 무료 수단**이 됐습니다. 🔴 **개설하면 알려 주십시오 — JSON-LD `sameAs`에 그 주소를 더합니다**(「이 블로그가 우리 것」임을 검색엔진에 알리는 값입니다). ⚠️ 아이디를 `wecarrylogis`로, 블로그명을 「위캐리 물류」로 하지 마십시오 — 경쟁사 (주)위캐리로지스가 `wecarry_logis` / 「위캐리 물류 이야기」를 씁니다
- ⚠️ 잡코리아·사람인 기업정보 등록 · 「위캐리」 **단독 키워드에는 돈·시간을 쓰지 마십시오**(국제물류·제주 짐배송이 점유해 되찾을 수 없습니다)

---

## 8. 작업 방식

### 한 차수의 흐름

```
지시서 작성  →  구현 세션  →  실사용 리뷰  →  CLAUDE.md 갱신  →  기록 검증
   (값·완료조건·           (착수 전 확인      (배포본을          (되돌릴 위험이
    금지사항 확정)          부터)             눈으로)            적혔는지)
```

⚠️ **실사용 리뷰가 여러 라운드로 가는 것이 정상입니다** — 27차 7라운드 · 62차 3라운드 · 65차 4라운드 · 66차 2라운드 · 70차 3라운드. 리뷰에서 **확정이 뒤집히는 일이 자주 있고**, 그때는 반드시 "옛 기록으로 되돌리지 말 것"을 `CLAUDE.md`에 적습니다.

### 지켜서 효과를 본 규칙

| | |
|---|---|
| **한 번에 하나씩** | 값 변경과 구조 변경을 같은 차수에 섞지 않습니다 |
| **파일이 겹치지 않게 쪼갠다** | 차수를 나누는 기준입니다 |
| 🔴 **착수 전 확인** | 지시서 전제를 코드로 대조. **여덟 번** 사고를 막았습니다 |
| 🔴 **의도한 것은 적는다** | 안 적으면 다음 세션이 "정리"하다 되돌립니다 |
| **커밋 분리** | 원인 추적용. 버그 수정은 반드시 따로 뗍니다 |
| 🔴 **PR 본문은 merge 직전에 다시 쓴다** | 리뷰로 범위가 넓어지면 처음 본문이 사실과 달라집니다(45차·70차) |

### 지시서에 반드시 넣는 것

- **왜 하는가** — 사유가 없으면 판단이 필요한 순간에 임의로 갑니다
- **완료조건은 기계로 검증되게** — `"문구가 통일됐다"`가 아니라 `grep 0건`
- **하지 말 것** — 특히 **이전 차수가 의도적으로 해둔 것**
- **적용 순서** — DB가 먼저인지 코드가 먼저인지는 **매번 따집니다**. 한 방향으로 굳은 규칙이 아닙니다

### 검증

- **렌더링으로 확인합니다.** 목 Supabase 서버 + 세션 주입으로 포털·관리자 모두 띄울 수 있습니다
- **관리자 회귀는 `origin/main`을 같은 목으로 띄워 대조**합니다(before 스냅샷이 아니라)
- 🔴 **목은 실제 DB보다 헐거우면 안 됩니다** — `order`·`limit`·`gt`·`not.`을 흉내내지 않으면 검증이 통째로 무의미해집니다
- **시안 대조는 소스에서 값을 뽑아** 합니다(24차가 자동검증 27개를 다 통과하고도 시안과 여섯 곳이 달랐습니다)
- ⚠️ **이 환경은 프록시가 `vercel.app`과 `wecarrylogis.co.kr`을 막습니다** — Preview를 볼 수 없어 **로컬 프로덕션 빌드로 잽니다**

---

## 9. 되풀이된 함정

| # | 함정 | 대응 |
|---|---|---|
| 1 | 🔴 **요약 문서·스크린샷을 근거로 삼기** | 시안 핸드오프 문서가 **네 곳** 실제와 달랐고, 스크린샷이 뷰포트로 잘려 오판이 두 번. **소스가 정본** |
| 2 | 🔴 **자동검증만 믿기** | 27개가 전부 통과했는데 시안과 다른 곳이 **여섯**. 구조·수치만 재고 "같은가"를 안 쟀습니다 |
| 3 | 🔴 **거짓 ❌ 를 코드 결함으로 읽기** | **아홉 차수**에서 났고 **전부 시험 자산 결함**이었습니다. "❌가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 마십시오 |
| 4 | 🔴 **대상 위치를 적게 세기** | 2곳→4곳, 3곳→5곳, 1개→3개. **표로 못 박지 말고 전수 grep** |
| 5 | 🔴 **`pkill`·`kill`이 자기 자신을 죽임** | **여덟 번.** 대괄호 트릭으로도, `/proc` 훑기로도 걸립니다 — `comm`이 대상 프로세스인 것만 고르거나 **PID 파일**을 쓰십시오 |
| 6 | 🔴 **낡은 서버가 포트를 잡아 옛 빌드를 잼** | **일곱 번.** `next start`가 죽어도 `curl`은 200을 돌려줍니다. **포트를 바꿔 새로 띄우고 응답으로 확인** |
| 7 | 🔴 **dev 서버가 뜬 채로 빌드** | `.next`가 깨집니다. 빌드 전에 죽이고 지울 것 |
| 8 | 🔴 **`git checkout <파일>`로 시험 되돌리기** | 미커밋 편집을 통째로 날렸습니다. 원본을 `cp`로 복사해 **사본으로 복원** |
| 9 | 🔴 **정규식으로 소스를 긁어 배열 읽기** | 주석 안 문자열을 원소로 오인. **실제 모듈을 평가**해서 잴 것 |
| 10 | 🔴 **주석에 상수·금지어를 문자로 쓰기** | 완료조건 `grep`에 주석 자신이 걸립니다. **여섯 번** |
| 11 | 🔴 **`error`를 버리고 빈 목록으로 두기** | 조회 실패가 "데이터 없음"으로 보입니다 |
| 12 | 🔴 **`.order()`를 없는 컬럼에 걸기** | 정렬은 불러온 뒤 JS로 하십시오 |
| 13 | 🔴 **`scrollLeft`만으로 천천히 밀기** | 브라우저가 **정수로 반올림**해 덜덜거립니다. 정수는 `scrollLeft`, 소수는 `transform`이 나눠 싣습니다 |
| 14 | 🔴 **여백을 줄이려다 늘리기** | 선택자가 넓으면 `margin: 0`인 요소에 값을 **붙입니다**. 전후 높이를 반드시 재십시오 |
| 15 | **`npx tsc … \| tail`** | `tail`의 종료코드를 읽어 오판합니다. **파일로** 받으십시오 |
| 16 | **`<br />`로 한글 줄바꿈 고정** | 폰트가 CDN이라 폭이 달라지면 어색해집니다. `word-break: keep-all` |
| 17 | **`<details name>`에 `open=true`를 한꺼번에 걸기** | 배타 아코디언이라 **마지막 하나만** 열립니다. 하나씩 열어 재십시오 |
| 18 | 🔴 **`public/`의 "참조 0건" 파일을 정리하기** | 넷은 **외부가 절대 URL로 가져다 씁니다** — 지우면 회수가 안 됩니다. 아래 표 참고 |
| 19 | 🔴 **「이미지가 늦게 뜬다」를 용량 문제로 짐작하기** | 먼저 **그 섹션의 y 좌표**를 재십시오. 지연로딩 선행 거리(약 1250px) 안팎이면 **보이는 순간 받기 시작**합니다 — 용량과 무관. 5.4MB를 줄여도 팝인은 371 → 339ms로 그대로였고, **그 한 장만 `eager`로** 바꿔야 0이 됐습니다 |
| 20 | 🔴 **스크롤 타이밍을 `waitUntil:"load"`나 rAF로 재기** | 전자는 대역폭이 이미 비었고, 후자는 CPU 스로틀에 1.5초가 15초로 늘어납니다 — 둘 다 「전·후 똑같음」이라는 무의미한 결과가 나옵니다. `domcontentloaded` → 1.5초 대기 → **`setTimeout`으로 벽시계 1.5초** 스크롤 |
| 21 | 🔴 **이 환경에서 FCP·LCP로 성능을 판단하기** | 프록시가 폰트 CDN을 막아 그 대기가 그대로 섞입니다(실측 FCP 13.2초). 믿을 것은 **전송 바이트**와 **요소 타이밍**뿐입니다 |

🔴 **`public/`에서 지우면 안 되는 파일 넷** — `grep` 참조 0건이 근거가 되지 않습니다(원래 코드가 참조하지 않는 것이 정상입니다).

```
  naverd2a4fd031fc1d5c8e06b2ca3aace4eb8.html   네이버 소유확인 — 지우면 소유확인이 풀리고
                                               제출한 사이트맵·수집요청이 무효가 됩니다
  email/wecarry-logo-400.png / -white.png      직원 메일 서명 — 이미 발송된 메일이 깨집니다
  landing/service-04-moving.jpg                wecarry24.co.kr(이사)이 씁니다
  og-wecarry-1200x630.jpg                      카톡·검색 미리보기
```

---

## 10. 새 세션에서 이어가는 방법

1. 이 문서를 먼저 읽습니다
2. `CLAUDE.md` **최상단**(최신 세션 기록)과 **§3 설계 원칙 · §5 로드맵 · §7 함정**을 봅니다
3. 해당 차수의 **작업지시서**를 받습니다 — 값·완료조건·금지사항이 거기 있습니다
4. 🔴 **착수 전 확인부터** 합니다. 지시서 전제가 틀린 적이 여러 번입니다
5. 다르면 **임의로 판단하지 말고 보고 후 대기**합니다

⚠️ **사용자는 개발 초보자입니다.** 용어를 풀어 설명하고, 무엇을 왜 하는지 먼저 말한 뒤 진행하십시오.

⚠️ **`CLAUDE.md`·`HANDOFF.md`만 바뀌는 커밋은 PR 없이 `main`에 바로 반영합니다**(사용자가 정한 규칙). 코드가 한 줄이라도 섞이면 이 규칙은 적용되지 않습니다.

---

## 부록 — v20 이후 무엇이 바뀌었나

v20은 **세션 55차 / 지시서 26차 진행 중**(2026-08-27) 시점의 문서입니다.

| 영역 | v20 | v21 |
|---|---|---|
| 포털 | 셸·홈·발주까지 개편, 견적·조회는 진행 중 | **전 화면 개편 완료**(견적·배차·정산·통계·캘린더). 담당자 정보만 남음 |
| 랜딩 | 8섹션 개편 · **이미지 5종 자리표시자**(공개 차단) | **이미지 32종 전부 실물** · WHY 아코디언 · 사업자정보 모달 · 모바일 여백·자동 슬라이드 |
| 폼 3화면 | 미개편 | `/quote`·`/apply`·`/customer/login` **시안으로 교체** |
| `/status` | 존재 | 🔴 **라우트까지 삭제** |
| 도메인 | 미확정 | **wecarrylogis.co.kr** 확정 · OG·sitemap·canonical 완료 |
| 대표 메일 | 개인 도메인 | **biz@wecarrylogis.co.kr**(전용 도메인 웹메일) |
| 약관 | 34차 개정본(회사가 배상 주체) | **66차 개정본** — 보험 보통약관 대조 · 차주·보험자 1차 부담 |
| 보험 | 미가입 · 플래그 off | **일반화물 증권 수령**(1사고당 1억) · 플래그 on · 값은 화면에 안 나감 |
| 기준가 | 코드 하드코딩 | **DB 실시간 조회** · 게시 6행 · 저장 전 확인 창 |
| 공개 문구 | 「1톤부터 5톤 이상까지」 | **「1톤부터 5톤 이상, 특수차량까지」**(25톤·특수차량 금지 해제, 「이사」는 다시 금지) |
| 공개 차단 | 1건(랜딩 이미지) | **0건** — 남은 선행 조건은 **변호사 검수**뿐 |

🔴 **v20의 "다음 예정 작업"은 대부분 소진됐거나 순서가 바뀌었습니다.** 지금 유효한 로드맵은 위 §6이고, 원본은 `CLAUDE.md` §5에 있습니다.

---

*이 문서는 `CLAUDE.md`를 대체하지 않습니다. 근거와 이력은 그쪽에 있고, 이 문서는 지금 필요한 것만 추린 것입니다.*
