# WeCarry 운송 통합 운영 시스템 — 인수인계 문서

**버전** v81 · 2026-09-18
**기준** 세션 70차 + 차수 없는 작업 20건(네이버 등록 · SEO 메타 · 카피라이트/랜딩 이미지 · 무료 대기시간 단위 · 프리셋 이름 표시 · 담당자 정보 화면 · 보유기간 만료 파기 · 랜딩 모션 · 월별 통계 기간 + 엑셀 · 이용약관 면책 보강 · 설치형 앱(PWA) 전환 + 후속 수정 · 모바일 로그인 첫 화면 + 카톡 브라우저 · 모바일 입력창 확대 · 안드로이드 설치 원인 · 법적 문서 3종 화면 정리 · 회사소개·차량요금 화면 삭제 · 이용약관 추가 개정 terms-v4 · 운임기준표 v11 A장(운임 165칸) + B장(혼적 3구간) · 문서 정합 정리) **+ 32차 내부관리 아이디 로그인 + 직원 비밀번호 재발급 + 이용약관 제19조 개정 terms-v5 + 33차 화주 항목 정합 + 정기계약 + 정기계약 배지 후속 + 34차 견적관리 개편(포털 발주요청과 정합 · 실사용 리뷰 5라운드) + 다마스·라보 차급 신설(11차급 → 13차급) + 견적관리 폼 시안 정합(라벨·필수 강조·개인고객 필수 항목) + 화주 목록 페이지 나누기 + 모바일 카드 + 견적 폼 블록 번호 배지 옐로 + 35차 내부 정합 ① 오더·배차·정산 + 운영 대시보드(선착불 정산 모델 · 부가세 구분 되살리기 · 마진 정의 통일 · 실사용 리뷰 8라운드) + 랜딩 히어로 「운송관리 계정 신청」 버튼 + **거래처 온보딩 안내 페이지 `/guide` 신설**(공개 화면 6 → **7개** · 프리렌더 44 → **45**) + 문서 정합 정리 3차(HANDOFF §5-12 선착불 정산 모델 신설) + **화주포털 이용가이드 `/customer/guide` 신설**(본문 정의처 `lib/guideContent.ts` 하나 · 포털 메뉴 8 → **9종** · 포털 홈 링크 제거 · 프리렌더 45 → **46**)** + **36차 A·B장 화주 거래조건 + 발주요청 청구주기 「요청」**(거래 조건 신설 · 정산 마감일을 전 화주 월말로 통일 · 「계약과 다름」 배지 · 상단바 sticky · `_migrations` 29 → **32행** · 실사용 리뷰 3라운드 · 🔴 **세금계산서 발행 방식이 화면에서 빠진 채 merge 됨 — PR 2 에서 복구**)** + **36차 PR 2 C·D·E장 + A장 복구 + 견적서 공유 링크**(선착불은 화주 미수금 0 · 미수금을 **표시 시점 계산**으로 · 하차 = 상차 +30분 · 견적서 「조정」 줄 · 계산창이 sticky 상단바에 가리던 것(`--admin-topnav-h`) · 일정 칩 「지금·당착·내착」 · **`/q/[token]` 견적서 공유 링크 신설**(공개 화면 **+1**, 단 noindex) · 견적안내 문자가 **87byte SMS** 로 대체 · `_migrations` 32 → **33행** · 실사용 리뷰 2라운드)** + **견적서 공유 링크 후속**(PR #152 — 🔴 **뒤 4자리 확인 제거**(사용자 지시) · 없는 컬럼을 select 에 적어 조회 실패가 **404 로 둔갑하던 버그** 수정(원칙 55번) · **DB 0**)** + **운송오더 3건**(PR #153 — 견적 → 오더 **금액·차량형태 프리필**(35차가 만든 칸을 프리필이 몰랐습니다) · 하차 하한을 **다섯 화면 한 정의처**로(오더 2시간 → 30분 · **오더 상세는 하한이 아예 없었습니다**) + 일정 칩 · 등록 오류를 **버튼 옆으로**(원칙 33번 버그를 오더 상세에서 하나 더 발견해 같이 수정) · **DB 0**)** + **월정산 묶음 개편**(PR #154 — 🔴 **「담기가 안 된다」의 진짜 원인은 `add_item_to_billing_batch` 가 `record` 로 선언한 변수를 `is_billing_batch_candidate(invoices)` 에 넘겨 **매 호출마다 예외로 죽던 것**이었습니다(`cannot cast type record to invoices`) — `invoices%rowtype` 으로 **선언 한 줄**만 고쳤고 **묶음 DB 함수가 처음으로 저장소에 들어왔습니다** · 운송완료 시 **묶음 자동 생성** · 묶음 화면을 **아코디언 목록 하나로** · 결제일을 화주 거래조건에서 **자동 계산** · 마감 지난 draft 표시 + **연체 자동 판정**(매일 03:20 KST) · `_migrations` 33 → **34행** · 실사용 리뷰 **5라운드**)** + **추가 수정 6건 + 리뷰 2건**(PR #155 — 🔴 **상호가 배송지 별칭 칸을 쓰고 있던 것**을 `customer_locations.company_name` 으로 갈랐습니다(포털 발주 폼이 `location_name` 에 밀어 넣어 관리자 화주 상세에서는 **상호가 어디에도 안 보였습니다**) · 관리자 화주 상세의 「저장된 주소」를 **화주포털과 같은 구조로**(상세주소·담당자·특이사항을 읽지도 쓰지도 않고 있었습니다) · 견적관리 칩이 **상호·담당자·상세주소까지 프리필** · 화주포털 3화면에 **조회기간**(정의처 `Pv2PeriodFilter` 하나) · 견적관리 **품목 필수** · 견적 펼침에 **부가세 포함가 병기** + 「합계」 → **「공급가액」** · 묶음 목록 **가독성**(상태 배지 · 금액 15.5px · 「납부기한」) · `_migrations` 34 → **35행**) + **운임기준표 v12**(PR #156 A장 · #157 B장 · #158 C장 — 🔴 **견적금액을 만원 격자로**(다마스·라보만 5,000) · 운임 **184칸** UPDATE · 가산을 **요율제 → 정액**으로(차량형태 21 + 운송시간 6행) · 가산 **소계**를 반올림 · 견적서에서 **가산을 한 줄로 합산**((가)안 · 이름만 적고 요율은 뗀다) · 공개 게시가 **5칸 인하** · `_migrations` 35 → **37행** · 실사용 리뷰 1라운드)  + **37차 24시콜 ⓐ — 배차된 차주 정보 붙여넣기**(PR #159 — 접수중 배차의 「외부 배정」 분기에 붙여넣기 칸 · `홍길동 서울12가3456 010-1234-5678` 한 줄로 차주 세 칸을 채웁니다 · 🔴 **범위가 작업 중에 차주 셋으로 줄어 `dispatches.external_order_no` 신설이 없어졌고 DB 변경이 0**입니다 · 라벨이 없어 **순서가 아니라 모양으로** 가릅니다 · **채우기만 하고 저장은 기존 「배차확정」이** 합니다 · **서버에 보내지도 저장하지도 않습니다** · **DB 0 · +328 / −0 · 리뷰 라운드 0**)** + **38차 접수 알림 — 관리자 A·B장 + 화주포털 A장**(PR #160 — 고객 접수 셋이 들어오면 **사무실 밖에서도** 압니다 · A장 화면 안 알림(배너 · 탭 제목 · 소리) · B장 **웹 푸시**(VAPID + aes128gcm 을 **Node `crypto` 만으로** · **신규 의존성 0** · `push_subscriptions` 표 1건) · 리뷰로 **알림음 볼륨 3단계 · 소리 3종** · 🔴 **그 뒤 사용자 요청으로 화주포털에도 A장**(지시서는 「포털은 한 줄도 건드리지 않는다」였습니다 — **사용자가 범위를 넓힌 것**입니다) · 🔴 **새 폴링도 새 구독도 0개**(배지 `counts` 가 늘어난 순간만 봅니다) · `_migrations` 37 → **38행** · **+1,884 / −1**) + **화주포털 B장 — 웹 푸시**(PR #161 — 화주도 **브라우저를 닫은 채** 견적·배차·정산을 알 수 있습니다 · 🔴 **직원용과 표를 갈랐습니다**(`customer_push_subscriptions` — 화주와 직원이 **둘 다 `authenticated` 롤**이라 한 표에 두면 정책 하나에 함께 색니다) · 🔴 **`company_id` 를 구독 표에 복사해 두지 않습니다** · 🔴 **서버 안에서는 `await` · 브라우저에서는 금지** · 구독 단추를 **관리자·포털 공용**으로 · `_migrations` 38 → **39행** · **+1,079 / −211** · 리뷰 라운드 0)**  + **알림 4건 — 창내 배너 자동 닫기 · 화주 견적 승인 · 포털 알림 범위 축소**(PR #162 — 창내 배너가 **그 화면에 들어가면 스스로 꺼집니다**(끄는 신호가 **둘**이고 둘 다 필요합니다) · **화주 견적 승인**에도 알림(38차 금지를 뒤집은 것입니다) · 🔴 **「포털에 배너가 안 뜬다」는 실측 결과 고장이 아니었습니다**(A장은 `updated_at` · B장은 사건 목록이라 갈립니다) · 사용자가 **좁히는 쪽**을 골라 **포털 알림 넷 → 둘 · 푸시 다섯 → 셋**(정산·공지·상차완료·하차완료를 뺐습니다 · **배지는 넷 그대로**) · **DB 0 · +240 / −55 · 리뷰 라운드 0**) + **견적번호 중복 수정**(PR #163 — 🔴 **「저장이 안 된다」와 「배지가 안 사라진다」가 한 뿌리였습니다**. 번호를 **「그날 만들어진 행 수 + 1」**로 만들어서 그날 건을 하나라도 지우면 **영영 저장이 실패**했고, 발주요청은 **「견적이 저장되는 순간」에 승인됨**이 되므로 요청이 `대기중` 으로 남아 배지가 그대로였습니다 · **「그날 최대 번호 + 1」 + 유니크 위반 시 재시도**로 고쳤습니다 · **DB 0 · +219 / −34 · 리뷰 라운드 0**) + **포털 알림 4건**(PR #164 — 🔴 **사용자 지시 네 건 중 앞 셋이 한 뿌리였습니다**. 화면 안 배너의 신호가 `updated_at` **하나**라 **무엇이 바뀌었는지 모른 채** 울렸습니다(담당자 메모 한 줄 · 화주 **본인**의 견적 승인 · 상차완료가 전부 같은 신호입니다) — 신호를 **바뀐 뒤의 상태**로 바꿔 셋을 한꺼번에 닫았습니다 · **배지(넷)와 배너(둘)를 갈랐습니다** · 「상담중」을 **화면 글자만** 「확인중」으로(🔴 **DB 값·CHECK 는 그대로**) · 목록 안 **「업데이트」 표시** 신설(배너와 **별개로 넓습니다**) · **DB 0 · +313 / −30 · 리뷰 라운드 0**) + **39차 견적문의 폼 정합 + 운송시간 자동선택**(PR #165 — 🔴 **「왕복,편도 드롭메뉴 안됨」의 정체는 카테고리 키 한 글자**였습니다(`surcharge["왕복편도"]` — 슬래시가 없어 그 칸이 **빈 배열**이었고 예외도 경고도 안 났습니다 · 다른 화면 넷은 전부 맞았습니다) · **필수 항목 표시**를 넣었고 리뷰에서 **빨간 `*`** 가 됐습니다(🔴 **31차가 확정했던 회색 「필수」 글자를 뒤집은 것**이고 **화주포털 발주요청까지** 넓어졌습니다 — 포털에는 **검정 `*` 가 placeholder 안에 문자로** 박혀 있었습니다) · **일정을 발주요청과 맞췄습니다**(「지금」·「당착」·「내착」 칩 · 하차 **+30분 하한** · 🔴 **포털 부품을 끌어오지 않고 같은 동작을 랜딩 생김새로** 옮겼습니다) · **운송시간 자동선택**(정의처 `lib/transportTimeAuto.ts` 하나를 **세 화면**이 씁니다 · 🔴 **관리자 견적 등록에 이미 있었고 틀렸습니다** — `출퇴근/혼잡`·`새벽`을 아예 안 썼고 「평일」 **부분일치**였습니다 · ⚠️ **18:00~19:59 상차 건이 0원 → +10,000원으로 오릅니다**(사용자 확정)) · 🔴 **리뷰 — 칩이 1280px 에서도 두 줄이던 원인은 칩 줄이 `DatePicker` 안이라 가용 폭이 날짜 칸(154px)뿐이던 것**입니다(밖으로 꺼내 **여섯 폭 전부 한 줄**) · **DB 0 · +547 / −65 · 실사용 리뷰 1라운드**) + **39차 D장 — 주소칸 엔터 검색**(PR #166 — 주소 칸에 「역삼동」처럼 치고 **엔터를 누르면 그 검색 결과를 이미 보여주는 상태로** 팝업이 열립니다 · 🔴 **39차 지시서의 D장이고 PR #165 에는 안 들어갔습니다**(전달된 판본이 D장 추가 전) · 🔴 **주소 부품이 둘이라 둘 다 고쳤습니다**(`AddressSearch` / `Pv2AddressField` — 25차가 인라인 style 때문에 갈랐습니다) · 규칙은 **`lib/useDaumPostcode.ts` 하나** · 🔴 **`preventDefault()` 를 `ready` 보다 먼저**(주소 칸을 쓰는 화면 여덟이 `<form>` 안이고 `/quote`·`/apply` 는 anon INSERT 경로입니다) · 🔴 **곁다리로 원래 있던 버그를 고쳤습니다** — 두 부품이 각자 `roadAddress || jibunAddress` 라 **지번 선택이 통째로 무시**되고 있었습니다(사용자 신고 · `userSelectedType` 으로 가릅니다) · 🔴 **「동까지만」은 이 팝업으로 안 됩니다**(사용자 실물 스크린샷으로 확인 — 우편번호가 항목마다 다릅니다) · **DB 0 · +115 / −20 · 리뷰 라운드 0**)** + **내부시스템 소수정 8건**(PR #167 — 사용자가 배포본을 쓰며 낸 요청 8건입니다 · 🔴 **①「수동 승인 처리」는 삭제하면 안 되는 버튼이었습니다** — 견적 저장이 실패해 발주요청이 `대기중` 으로 남았을 때 푸는 **유일한 탈출구**라(PR #163 이 그 증상) 이름·생김새만 **「전화로 처리함」**으로 낮췄습니다 · ② 「확인중」 견적 행을 **옅은 노랑**으로(🔴 **판정은 DB 값 `상담중`**) · ③ 견적 폼 상세를 **접었습니다**(목록 머리 y **2005 → 635px** · 🔴 `<details>` 가 아니라 state + 조건부 렌더이고 **저장 실패 시 접지 않습니다** · 손잡이 화살표는 **글자가 아니라 SVG** — `▸` 가 폭 6px 로 그려져 알아볼 수 없었습니다) · ④ 상태 **컬러 캡**(🔴 **관리자 전용 `quoteStatusAdminStyle()` 신설** — 관리자에서 옐로는 「할 일」, 화주에서는 「성사」라 합칠 수 없습니다) · ⑤ 운송오더 목록에 **「+ 배차 등록」**(🔴 `status` 가 아니라 `dispatches` 에 묻습니다) · 🔴 **⑦ 배차 중복등록은 원인이 둘이었습니다** — 후보 조건이 `배차중` 을 남기던 것 + `?from_order=` effect 가 **닫은 폼을 다시 열던 것**(등록을 마치면 이제 **그 배차 상세로** 넘어갑니다) · ⑥ **차주 지급운임 필수**(🔴 **배차확정에서만** 막습니다) · 🔴 **⑧ 「새로운 페이지에서 오류가 뜬다」의 정체는 `alert` 이 아니라 원칙 33번 전체화면 가드였습니다** — PR #153 이 오더 상세에서 고친 그 버그가 배차 상세에 남아 있었습니다 · **DB 0 · +561 / −63 · 리뷰 라운드 0** · 🟢 **사용자 확인 완료**)** + **관리자 목록 기간 「직접지정」**(PR #168 — 화주포털에만 있던 조회기간 직접지정을 **관리자 네 화면**(견적·오더·배차·정산)에 넣었습니다 · 🔴 **정의처는 `components/DateRangeFilter.tsx` 이고 `lib/` 에 없습니다** · 🔴 **끝날은 그 날을 포함합니다** — `to` 는 **종료일 다음 날 자정**이고 호출부는 `.lt()` 입니다 · 🔴 **`"YYYY-MM-DD"` 를 `new Date(문자열)` 로 읽지 마십시오**(UTC 라 KST 에서 하루가 밀립니다) · 🔴 **`Pv2PeriodFilter` 를 끌어오지 않았습니다**(원칙 57번) · 🔴 **opt-in 이라 같은 부품을 쓰는 나머지 네 화면은 프리셋 넷 그대로**입니다 · **DB 0 · +285 / −54 · 리뷰 라운드 0**)** + **랜딩 히어로 사진·헤더 로고 선명도**(PR #169 — 히어로 **축소본 4장 + `image-set()`**(1배율 전송 1,554 → **502KB** −68% · 소스는 **61차 커밋 `29f849b` 의 원본** · **크로마 4:4:4**) · 헤더 흐림을 **배경이 생겼을 때만** · 🔴 **헤더 로고의 원인은 렌더링 결함이 아니라 「픽셀 수」였습니다** — 배경을 같게 맞춰 재니 `fixed`·`backdrop-filter`·`will-change` 가 **평범한 div 와 차이 0** 이었고 고해상도 PNG 마스크는 **크기·배율마다 승패가 뒤집혀** 못 씁니다 · 🔴 **520px 이하에서 한글을 떼고 「WeCarry」만**(사용자 확정 (A)안 · `viewBox` 만 잘라 쓰고 **새 이미지 파일 0개** · 글자 **17.7 → 28.8px**) · 🟢 **521px 이상은 한 글자도 안 바뀝니다** · **DB 0 · +121 / −8 · 커밋 4개 · 실사용 리뷰 2라운드**)** + **마감 CTA 배경 축소본 + 운송시간 자동선택 새벽 우선**(PR #170 — 사용자 결정 **다섯 건 중 코드가 필요한 둘**입니다 · 마감 CTA 배경에 **축소본 2장 + `image-set()`**(1배율 전송 437 → **112KB** −74% · PR #169 이 「같은 상황이고 안 고쳤다」로 남긴 자리 · 소스는 **61차 커밋 `29f849b` 의 원본** · 조건은 히어로와 같습니다) · ⚠️ **이름을 둘 바꿨습니다** — `heroImageSet()` → **`bgImageSet()`** · `--hero-fallback` → **`--bg-fallback`**(옛 이름은 두 파일 주석에 적어 뒀습니다 · 🟢 히어로 동작 무변) · 🔴 **운송시간 자동선택에서 새벽을 요일보다 먼저** 봅니다(사용자 확정 「새벽우선」 — 39차가 「먼저 물을 것」으로 열어 둔 미결을 닫았습니다 · ⚠️ **토·일 00:00~05:59 상차 건이 +10,000원 오릅니다** · 🟢 168칸 중 바뀐 칸이 **정확히 12칸**) · 🔴 **코드 없이 닫은 결정 셋** — 컴팩트 워드마크를 **521~1120px 로 넓히지 않습니다** · 주소 「동까지만」은 **지금대로** · 견적서 가산 줄은 **「외 N건」으로 축약하지 않습니다** · **DB 0 · +48 / −24 · 커밋 3개 · 리뷰 라운드 0**)** + **배차 취소 · 문제발생 사유**(PR #171 — 운송이 **정상 경로를 벗어났을 때** 무엇을 남기고 누구에게 어떻게 알릴지입니다 · **A장** `dispatch_status` 6종 → **7종**(`취소`)과 사유 7종(🔴 **「차주 책임」 칸이 표의 존재 이유**) · 🚨 **재배차가 막히는 자리가 한 곳이 아니라 둘이었습니다**(배차 목록 · **오더 목록**) · **B장** 문제발생 사유 8종 + **사고 대응 체크리스트**(관리자 전용) · 🚨 **`lost` 의 화주 라벨은 「화물 확인 중」입니다** — 보험 약관이 **도난신고 없는 망실을 「분실」로 간주해 면책**하므로 그 낱말을 화주 화면에 쓰면 **보험이 안 나오는 경우를 회사가 스스로 적은 것**이 됩니다 · 🔴 **리뷰 1라운드에 하루 전 확정 (A)「화주 화면에서 감춘다」가 뒤집혔습니다** — 지금은 **「접수」 + 「배차 취소 · 재배차 접수 중」**이고 중복은 `lib/portalCancelledDispatches.ts` 가 가릅니다 · 🚨 **내부 목록에서 취소 행이 「배차확정」으로 보이고 누르면 진짜 그렇게 저장되던 결함**을 덮었습니다(그 값이 화면 상수 6종에 없어 브라우저가 첫 항목을 골랐습니다) · 🔴 **리뷰 2라운드 「바로 안 바뀐다」는 코드가 아니라 「DB 는 반영됐고 코드는 merge 전」인 중간 상태였습니다**(운영 `main` 을 worktree 로 띄워 같은 목으로 대조) · 곁다리로 **`lib/portalRefresh.ts`** 신설(포털에는 폴링이 0개라 Realtime 이 끊기면 화면이 옛 값에 멈춥니다) · `_migrations` 39 → **40행** · **+1,665 / −45 · 커밋 7개 · 실사용 리뷰 2라운드**)**  + **배차취소 후속 3건 + 견적 금액 → 오더 반영**(PR #172 — PR #171 을 실제로 써 보고 나온 수정 셋 + 새 기능 하나입니다 · 🔴 **포털 취소 배지가 옅은 레드**(하루 전 제가 쓴 「문제발생과 색을 같게 하지 말 것」 주석을 **사용자가 뒤집었습니다**) · 🔴 **배지 규칙이 「취소된 순간부터 배차확정까지」로 바뀌었습니다** — 재배차가 접수되면 **새 카드가 취소 이력을 이어받습니다**(그전에는 접수되는 순간 사라졌습니다) · 🔴 **오더 상세 「배차 상세보기」가 살아 있는 배차로** 갑니다(원인은 조회에 **정렬도 상태 조건도 없던 것** — `.limit(1)` 만이라 PostgREST 가 순서를 보장하지 않았습니다) · 🔴 **견적 금액을 고치면 연결된 오더 청구금액이 따라옵니다**(`customer_charge_vat_included: false` 를 같이 씁니다 — `final_amount` 는 공급가액입니다 · **정산·배차는 안 따라옵니다**) · **DB 0 · +419 / −83 · 커밋 4개 · 리뷰 라운드 0**)** + **수정견적 배지 + 견적·오더 수정 이력 + 리뷰 5건**(PR #173 — 승인 뒤 금액을 조정하는 것이 **정식 경로**가 되면서(PR #172) 화주가 알 자리·담당자가 되짚을 자리가 한꺼번에 필요해졌습니다 · 화주포털 견적 목록 금액 앞 **「수정견적」 배지**(신호는 **`quotes.revised_at`** 이고 `updated_at` 이 아닙니다 · 소멸 판정은 **배차 단계**) · 견적·오더 상세에 **수정 이력**(🚨 **표를 새로 만들지 않고 `activity_logs` 를 되살렸습니다** — 21차가 「쓸 일이 생기면 정책을 만들면 된다」로 남긴 표인데 **그 표의 `user_id` 가 죽은 `profiles` 를 참조하고 있어** 그대로 뒀으면 **화면에 아무 증상 없이 이력이 한 줄도 안 쌓였습니다**) · 견적 상세 **「해당 운송오더 이동」 + 「+ 추가 운송오더 생성」** · 🚨 **리뷰에서 결함 하나가 드러났습니다** — 이력을 오더 상세에만 붙였는데 금액을 바꾸는 자리는 **견적 쪽이 더 많아** 절반만 남고 있었습니다(고친 자리는 화면이 아니라 `syncQuoteAmountToOrders()` 안입니다) · 발주요청 대기중 버튼 **세 줄** · ⚠️ **「최종금액 직접 입력」 차액이 「조정」이 아니라 기본운임으로 흡수됩니다**(36차 E장을 뒤집은 것이고 그때의 사용자 원문도 「기본운임에서 + - 되어서」였습니다) · 취소 사유 **「운임료 조정 필요」**(코드는 유지 · **`driverFault` 를 `false` 로**) + 그 사유면 **견적 금액 수정으로 이동·옅은 빨강 강조** · 취소 배차만 남은 오더에 **「배차 상세보기」+「+ 배차 등록」** · 배차 목록 **「취소건 숨기기」**(기본 꺼짐 + 감춘 건수 표시) · `_migrations` 40 → **41행** · **+1,554 / −81 · 커밋 8개 · 실사용 리뷰 2라운드**)** + **공지사항 간이 서식 + 미리보기 + 수정**(PR #174 — 공지가 **제목 한 줄 + 평문 네 줄짜리 `<textarea>`** 뿐이라 **올리면 고칠 수도 없고 올리기 전에 어떻게 보이는지도 알 수 없었습니다** · 🚨 **지시서가 통째로 뒤집혔습니다** — v1 은 **정식 편집기 도입**(Tiptap + sanitize-html)이었고 0-2 가 *「이것은 「신규 의존성 0」을 처음으로 깨는 작업입니다」* 로 시작했는데, 착수 전 보고를 받은 **사용자가 작업량을 보고 범위를 줄였습니다**(*「너무 작업량이 과해지는게 아닌지 우려스럽다. 최소한의 수정으로 글자볼드, 글자크기, 글자컬러, 이모티콘을…」*) → **ⓐ 간이 서식** · 🔴 **그래서 「신규 의존성 0」이 그대로 지켜졌습니다** · 🚨 **안전 구조가 정식 편집기와 반대입니다** — HTML 을 **받아서 골라내는** 것이 아니라 **전부 이스케이프한 뒤 허락한 표기만 태그로 만들어 냅니다**(통과시키는 HTML 이 애초에 없어 정화 라이브러리가 필요 없습니다 · 시험 29개로 `<script>`·`javascript:` 링크·따옴표 탈출이 전부 글자로 남는 것을 확인) · 도구 모음 8개(굵게 · 밑줄 · 크기 2단계 · 색 5종 · 이모지 12종 · 링크 · 목록 · 구분선) · **[편집]/[미리보기] 탭**(🔴 **화주와 같은 부품**) · **목록 각 행에 [수정]** + **「화주에게 다시 알림」**(기본 꺼짐) · 본문 칸 **약 90px → 360px** · 🔴 **`content_format`(`plain`|`markup`)이 옛 공지의 줄바꿈을 지킵니다**(기존 1행에 실재 · 없었으면 275자가 한 줄로 뭉개졌습니다) · 🚨 **「새 공지」 판정이 `created_at` → `announced_at` 으로 옮겨졌습니다**(쓰는 곳 **셋**) · 🔴 **이미지 올리기(지시서 C장)는 안 했습니다**(사용자 「사진은 나중에」) · `_migrations` 41 → **42행** · **+1,215 / −39 · 커밋 4개 · 리뷰 라운드 0**)** + **이용가이드 재작성 + 시각 개편**(PR #175 — 🔴 **PR #149 이후 아홉 차수가 가이드에 하나도 반영돼 있지 않았고**, 더 나쁜 것은 **「지금 화면에 없는 말」**(「상담 중」 — PR #164 가 「확인중」으로 바꿨습니다)**을 적고 있던 것**입니다 · 항목 **14 → 21개**(알림 받기 · 견적 확인·승인 · 배차·운송 진행 · 정산·결제내역 · 기간 골라 보기 · 공지사항 · 화면에 붙는 작은 표시들) · 🔴 **기존 항목 id 14개를 한 글자도 안 바꿨습니다**(`?topic=` 이 곧 URL) · 🟢 **첫 커밋은 화면 파일 0줄**(있는 블록만 조합) · 리뷰로 **시각 개편** — `GuideIcon` **20종 인라인 SVG**(이미지 파일 0건 · 이름 목록은 **정의처**에 둡니다) · 새 블록 **`steps`·`cards`·`figure`** + `note` `tone` · 공개 화면 **바로가기 칩 줄** · 🚨 **화주포털 캡처 6장**(PR #148 의 「캡처 0건이 의도」를 뒤집었지만 **실계정이 아닙니다** — `scripts/guide-shots.mjs` 가 Supabase 응답을 가로채 **가짜 값**을 물려 찍습니다 · 🔴 **실계정으로 다시 찍지 마십시오 — git 이력에서 지워지지 않습니다**) · 🚨 **고객센터 시간이 전 화면 09:00 ~ 19:00** 이 됐습니다(리뷰 2·3라운드 — 상수를 둘로 갈랐다가 **같은 날 도로 합쳤습니다** · 🔴 **다시 둘로 가르지 마십시오**) · 🚨 **로그인 화면의 시간 줄은 둘이고 뜻이 다릅니다**(「접수 기준 당일 발급」은 **발급 약속**이고 31차가 **랜딩 FAQ 와 일부러 맞춰 둔 값**입니다 — **셋을 함께** 올렸습니다) · **DB 0 · `_migrations` 42행 유지 · 신규 의존성 0 · `globals.css` 순수 추가(+391 / −0) · +1,636 / −100 · 커밋 4개 · 실사용 리뷰 3라운드**)** + **문자 발송 정리**(PR #176 — 문자 **8종 → 7종**이고 **배차확정이 두 통**입니다(차주 「화물정보 안내」 + 고객 「배차확정 안내」 신설) · 상차완료·하차완료 문자 **폐지**(사용자 *「알림으로만 충분하다」*) · 머리말 **`[위캐리운송]` 하나** · 🚨 **지시서의 「DB 변경 없을 것으로 봄」이 틀렸습니다** — `sms_logs_template_type_check` 가 **8종 고정**이라 DB 가 먼저여야 했습니다(**8종 → 9종** · 폐지 두 종을 빼면 옛 행이 제약을 어깁니다) · `dispatches.external_vehicle_type` 신설 · 🚨 **곁다리로 날짜가 9시간 어긋나던 것**을 고쳤습니다(Vercel 함수가 UTC 인데 `getHours()` 를 썼습니다) · 🚨 **재발송이 87byte 견적 단문에 제목을 붙여 LMS 로 올리던 결함**을 `smsSubjectFor()` 한 함수로 모았습니다 · `_migrations` 42 → **43행** · **+1,204 / −203 · 리뷰 라운드 0**) + **포털 「접수」 카드 + `/apply`·`/quote` 모바일 정리**(PR #177 — 🔴 **화주가 견적을 승인한 순간부터 포털에 그 건이 보입니다**(그전에는 담당자가 오더·배차를 만들기 전까지 배차·운송 조회와 홈이 **통째로 비어 있었습니다**) · 🔴 **코드를 열기 전에 운영 DB 를 재서**(`_verify.sql` ㉛) 막힌 데이터가 아니라 **시간의 문제**임을 확인했고 그래서 **DB 변경이 0**입니다 · 🔴 **오더를 자동 생성하지 않습니다**(27차 확정) — **DB 에 아무것도 만들지 않는 가상 카드**이고 정의처는 `lib/portalPendingDispatches.ts` 하나입니다 · `/apply` 문구 **「전화로도 신청 가능합니다」** · 이메일 **`[아이디] @ [도메인]` + 고르기 11종** · 🚨 **모바일 동의 설명글을 두 화면에서 감췄습니다**(제 판단이 리뷰에서 뒤집혔습니다 — 🔴 **「전문 보기」와 거부권 문단은 반드시 남깁니다**) · 🔴 **`/quote` 거부권 한 줄은 이번에 처음 생겼습니다** · 전화 카드 **271 → 165px** · 모바일 폼 항목 제목 **15px/700** · **DB 0 · +680 / −51 · 실사용 리뷰 3라운드 7건**)** + **라보 운임을 다마스 기준으로 통일**(PR #178 — 사용자 지시 *「다마스, 라보는 기본 30000원 · 그 이후거리 금액도 모두 다마스기준으로 통일」* · 🔴 **코드를 열기 전에 운영 DB 를 쟀더니 다마스 10km 는 이미 30,000 이었습니다**(v12 A장이 그렇게 넣었습니다) — 그래서 **바뀐 것은 라보 15칸뿐이고 다마스는 0칸**입니다 · 🚨 **곁다리로 100원 오타 두 칸을 찾았습니다**(라보 110km `85,100` · 400km `225,100` — v12 A장은 85,000/225,000 을 넣었고 그 뒤 `/admin/rates` 에서 잘못 저장된 값입니다. **그 화면은 클릭이 곧 저장이고 되돌리기가 없습니다**) · 🔴 **차급 역전 검사에서 다마스↔라보 한 쌍만 동가를 허용하도록 좁혔습니다**(`<=` 를 전부 `<` 로 바꾸면 다른 11쌍의 동가까지 통과합니다) · **제품 코드 0줄**(게시가 6종에 둘 다 없고 견적은 DB 를 매번 읽습니다) · `_migrations` 43 → **44행**)** + **견적관리 전화응대 매뉴얼**(PR #179 — `/admin/quotes` 오른쪽 열, 자동 계산 결과 **아래**에 전화응대 매뉴얼 패널 · **전 직원 공유 · 관리자만 편집**(사용자 확정) · 🚨 **쓰기 정책을 아예 안 만들고** 읽기만 RLS 정책(조건은 `public.is_active_staff()`)을 열었습니다 · **행은 하나뿐**이고 줄 목록은 jsonb 배열 · 번호는 **CSS 카운터**라 저장하지 않습니다 · sticky 를 계산 카드에서 **`.quote-side`** 로 올렸습니다 · 모바일(≤700px)은 안 그립니다 · `_migrations` 44 → **45행**)** / 저장소 `ttang0825/b2b-cargo-system` / 마지막 코드 커밋 **`4bc1c76`**(PR #179 — 전화응대 매뉴얼) · 마지막 DB 반영 **`4bc1c76`**(PR #179 · `2026-09-18_call_script.sql` · `_migrations` **45행**) · 마지막 문서 커밋 **전화응대 매뉴얼 기록**(2026-09-18)

🟢 **운임기준표 v12 는 PR 셋이 모두 끝났습니다**(A #156 → B #157 → C #158, 2026-09-15). 🔴 **차수를 소비하지 않았습니다.** 자세한 것은 **§5-1 운임**과 `docs/history/current.md` 맨 위를 보십시오.

🟢 **37차가 끝났습니다**(PR #159, 2026-09-16) — **24시콜 ⓐ(배차된 차주 정보 붙여넣기)**이고, 옛 37차 「CRM 항목 신설」은 **뒤로 미뤘습니다**(근거는 **§6 표**).

🟢 **38차도 끝났습니다**(PR #160, 2026-09-16) — **접수 알림**입니다. 🔴 **알림은 두 겹이고 성격이 다릅니다**: **A장**은 그 화면을 **열어 두고 있을 때** 뜨는 배너·탭 제목·소리이고, **B장**은 **브라우저를 닫아도·폰에서도** 오는 웹 푸시입니다. 🔴 **B장은 담당자가 기기마다 한 번씩 「이 기기로 알림 받기」를 눌러야 옵니다**(누르지 않은 기기에는 영영 안 옵니다 — 「안 온다」는 신고를 받으면 **그것부터** 물어보십시오). 🟢 **화주포털도 두 겹이 다 끝났습니다**(PR #161, 2026-09-16) — ⚠️ **이 줄이 적고 있던 「배차 SMS 와 겹친다」는 틀렸습니다** — 배차확정 문자는 **차주**에게 가고 상차·하차완료는 **자동 발송이 없습니다**(PR #73 이후 수동 버튼뿐). **겹치는 자리가 없습니다.** 🔴 **화주도 기기마다 한 번씩 눌러야 옵니다**(아이폰은 홈 화면 추가가 먼저). §5-17.

🔴 **알림 범위가 2026-09-16 에 한 번 더 좁아졌습니다**(PR #162) — 화주포털에서 **정산·공지·상차완료·하차완료**를 뺐고 남은 것은 **견적 · 배차확정 · 운송완료** 셋입니다(**배지 숫자는 넷 그대로**). 관리자에는 **화주 견적 승인**이 늘었습니다. 창내 배너는 이제 **그 화면에 들어가면 스스로 꺼집니다.** §5-17.

🟢 **헤더 워드마크가 520px 이하에서 「WeCarry」만 나옵니다**(PR #169, 2026-09-17) — 🔴 **없어진 것이 아니라 그렇게 정한 것입니다.** 그 폭에서 로고가 27px 까지 줄어 한글 글자가 **약 16px** 이라 계단처럼 깨져 보였고, 가로로 짧아진 만큼 세로를 키웠습니다(글자 **17.7 → 28.8px**). 🔴 **521px 이상은 그대로 「WeCarry 위캐리 운송」입니다.** §7.

🔴 **다음은 24시콜 ⓑ(견적 폼)이고, 그것은 목록 행 복사만으로는 안 됩니다** — 상하차 방법·시점·특이사항·상세주소·거리·금액이 통째로 안 옵니다(2026-09-16 샘플 실측 · §7 · `CLAUDE.md` §5 의 v4 블록).

> 🔴 **이 파일은 이름을 고정해서 씁니다.**
> 갱신할 때 `HANDOFF_v22.md` 같은 새 파일을 만들지 말고 **이 파일의 내용만 교체**한 뒤 위 버전 줄을 올리십시오.
> 옛 버전은 `git log -- HANDOFF.md`로 언제든 꺼내 볼 수 있고, 파일명을 바꾸면 `CLAUDE.md`의 안내가 깨집니다.

---

## 0. 이 문서를 읽는 방법

**두 문서의 역할이 다릅니다.**

| 문서 | 성격 | 언제 보나 |
|---|---|---|
| `CLAUDE.md` | 설계 원칙 · 로드맵 · 함정 (**약 37만 자** · 실측 365,053자 · 2026-09-18) | 규칙과 다음 차수를 볼 때 |
| `docs/history/` | 세션별 축적 기록 — **왜 그렇게 했는지의 근거**가 전부 여기 있다 (**약 71만 자 · 15개 파일** · 실측 715,128자) | 특정 항목의 이력·판단 근거가 필요할 때 |
| **이 문서** | 지금 시점의 정돈된 스냅샷 | **처음 붙는 사람·세션이 먼저 읽는 문서** |

🔴 **저장소 코드가 이 문서와 다르면 저장소가 항상 맞습니다.**

⚠️ **2026-09-09 에 세션 기록을 `docs/history/` 로 옮겼습니다**(한 글자도 지우지 않았습니다). `CLAUDE.md` 는 §3 설계 원칙 · §5 로드맵 · §7 함정만 남았으니 그대로 읽으면 되고, **이력은 `CLAUDE.md` §0 「이력 찾는 법」 표**를 보고 `grep -rn "…" docs/history/ CLAUDE.md` 로 찾으십시오.

🔴 **위 표의 자수·파일 수는 2026-09-16(문서 정합 정리 4차 직후)에 전수 실측한 값입니다.** ⚠️ 그 전에는 「약 14만 자」·「약 53만 자 · 8개 파일」로 적혀 있었는데, **2026-09-09 분리 시점의 값이 그대로 남아 있던 것**입니다(그 뒤로 `CLAUDE.md` 는 다시 불어났고 `docs/history/` 는 8 → 15개가 됐습니다). 🔴 **「줄어들었다」가 아니라 표기가 낡았던 것이니 아카이브를 복원하려 들지 마십시오.** 🔴 **파일을 가를 때마다 이 두 칸도 같이 고치십시오.**

---

## 1. 서비스 개요

**위캐리 운송** — B2B 화물운송 **주선업** 통합 운영 시스템.

화주 CRM · 견적 · 배차 · 정산을 관리하는 **내부 시스템**, 화주가 직접 발주하고 조회하는 **운송관리 포털**, 그리고 **공개 유입 화면**(랜딩 · 견적문의 · 계정신청 · 법적 문서 3종)까지 하나로 묶여 있습니다.

**포지셔닝은 "정식 물류 파트너"**입니다. 즉시 자동견적·정찰제·완전 셀프가입을 하는 경쟁사와 반대로, **담당자가 직접 응대하는 관계형 영업**을 핵심 차별점으로 둡니다. 운임기준표와 견적 로직은 비공개이고, 공개하는 것은 **10km 이내 기준가 6행**뿐입니다.

🔴 **서비스 시행일은 2026년 9월 7일**입니다.
🔴 **도메인은 `wecarrylogis.co.kr`** — **`www` 없는 짧은 쪽이 메인**입니다(명함·문자·전화 안내용). `www`는 308 리다이렉트.

---

## 2. 기술 스택 · 배포 · 환경변수

- **Next.js (App Router)** + **Supabase**(Postgres · Auth · Realtime) + **Vercel**(함수 리전 **서울 `icn1`**)
- 저장소 `ttang0825/b2b-cargo-system` (`main`) — 🔴 **public 입니다.**
  **직원·화주의 실명·이메일·연락처·로그인 아이디·사업자등록번호를 커밋하지 마십시오.**
  마이그레이션·`_verify.sql` 출력·PR 본문도 같습니다(**Actions 로그는 누구나 봅니다**).
  값을 채워야 하면 Supabase SQL 편집기에서 손으로 하고, 검증은 **마스킹하거나 건수만** 셉니다.
  🔴 되돌릴 수 없는 종류입니다 — git 이력에서 지워지지 않습니다(32차에 실제로 막았습니다).
- 엑셀은 `xlsx`가 아니라 **`xlsx-js-style`** (스타일링 때문에 교체)
- SMS는 **솔라피**. 발신번호는 **로그인한 담당자 기준**으로 서버가 정합니다.
  문자는 **7종**이고 머리말이 `[위캐리운송]` 하나이며 **견적만 SMS**(87byte 링크)입니다 — §5-25

⚠️ **네임서버는 가비아**, 웹은 Vercel, **메일만 카페24 웹메일**입니다. 🔴 **네임서버를 카페24로 옮기지 마십시오** — `A(@)`·`CNAME(www)`가 함께 넘어가 사이트가 통째로 죽습니다.

**SQL 변경은 GitHub Actions로 합니다.**

```
migrations/YYYY-MM-DD_무엇.sql  을 추가하고
Actions → "DB 마이그레이션" → dry-run → apply → verify
```

| 규칙 | |
|---|---|
| 🔴 `begin;`/`commit;` 쓰지 말 것 | 워크플로가 `--single-transaction`으로 감쌉니다 |
| 🔴 안전장치는 `select`가 아니라 `do $$ … raise exception … end $$;` | 조건이 틀리면 트랜잭션이 통째로 되돌아갑니다 |
| 🔴 **적용이 끝난 파일의 SQL을 나중에 고치지 말 것** | DB와 파일이 어긋납니다. 값을 바꾸려면 새 파일 |
| 🔴 `migrations/_baseline.txt` 수정 금지 | 과거 INSERT가 중복 실행됩니다 |
| 접속 | **Session pooler**(IPv4). Direct는 IPv6 전용이라 러너에서 실패 |

🟢 **세션이 직접 실행하고 로그까지 읽습니다** — 사용자가 버튼을 누를 필요가 없습니다.
🟢 `verify` 모드(읽기 전용)로 운임·동의·RLS 상태를 언제든 확인할 수 있습니다.

**환경변수(Vercel)** — `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `KAKAO_REST_API_KEY` · `SOLAPI_*`
**미등록(보류)** — `RESEND_API_KEY`(이메일 발송)

---

## 3. 화면 구성

### 공개 — 로그인 없음

`/` 랜딩(8섹션) · `/quote` 견적 문의 · `/apply` 운송관리 계정 신청 · **`/guide` 운송관리 이용안내** · 법적 문서 3종(`/terms` · `/privacy` · `/email-policy`) — **7개**(= `sitemap.xml`의 7 URL) **+ `/q/[token]` 견적서 공유 링크**(색인 대상이 아님)

🔴 **`/q/[token]` 은 2026-09-15 신설입니다**(36차 PR 2 · PR #151). 문자로 받은 견적서를 **로그인 없이** 여는 화면이고, **주소를 열면 바로 내용이 뜹니다**(뒤 4자리 확인은 같은 날 PR #152 에 사용자 지시로 없앴습니다 — 🔴 **링크를 아는 사람이 곧 열람 권한자입니다**). 🔴 **`sitemap.xml` 에는 넣지 않습니다** — 특정 화주의 상호·구간·금액이 담기므로 색인되면 안 됩니다. 차단은 **두 겹**(`app/q/layout.tsx` 의 `robots:{index:false}` + `app/robots.ts` 의 `disallow`)이고, `app/sitemap.ts` 는 동적 세그먼트를 건너뛰어 **저절로 빠집니다**(손댈 것 없음). 🔴 `components/TopNav.tsx` 숨김 조건에 `/q/` 가 등록돼 있습니다(원칙 11번). 자세한 것은 **§5-15**.

🔴 **`/guide` 는 2026-09-14 신설입니다**(PR #148 — 차수 없음). 계정을 받은 거래처가 첫 로그인에서 멈추던 것을 막는 온보딩 안내입니다. ⚠️ **진입 링크는 2곳입니다** — 랜딩 「운송관리 소개」 카드 · **포털 로그인 화면**. 🔴 **포털 홈 링크는 같은 날 오후(PR #149)에 뺐습니다 — 되살리지 마십시오**(로그인한 화주는 포털 메뉴의 「이용가이드」로 갑니다). 🔴 ~~`/customer/guide` 를 따로 만들지 마십시오 — 페이지는 하나이고 포털에 별도 안내 화면이 없는 것이 의도입니다~~ → 🔴 **2026-09-14 에 뒤집혔습니다**(PR #149). 사유는 **로고**입니다: 이 화면은 `PublicPageHeader` 를 쓰는 공개 화면이라 포털에서 들어가면 **로고가 랜딩으로 나가** 돌아올 길이 없습니다. **「페이지는 하나」를 근거로 되돌리지 마십시오** — 「두 벌이면 한쪽만 낡는다」는 걱정은 **`lib/guideContent.ts` 한 정의처 + 노출범위(`scope`)**로 풀었습니다(§5-13). 🔴 **이 화면의 한 장 스크롤 형태는 그대로입니다** — 처음 오는 사람이 네 단계를 순서대로 읽는 자리입니다. 🔴 `components/TopNav.tsx` 숨김 조건에 등록돼 있습니다(원칙 11번 — 빼면 관리자 메뉴가 얹힙니다). 🔴 이 화면에서 `/customer/login` 으로 가는 링크만 `<a href>` 입니다(PR #130 — 서비스워커 구역 밖이라 `next/link` 로 가면 「앱」이 아니라 「바로가기」가 만들어집니다).

🔴 **`/about`(회사소개) · `/vehicles`(차량·요금 안내)는 2026-09-08에 삭제됐습니다**(PR #132). 없어진 것이 아니라 **없애기로 확정한 것**이니 「왜 없지」 하고 되살리지 마십시오 — 필요해지면 **신규 제작**입니다. 진입 경로가 먼저 사라져(30차 리뷰가 랜딩에서, PR #131이 법적 문서 푸터에서) 이미 고아였던 화면을 정리한 것입니다.

⚠️ 공개 헤더는 **두 종류뿐**입니다 — `components/landing/LandingHeader.tsx`(31차 시안 · `/`·`/quote`·`/apply`)와 `components/PublicPageHeader.tsx`(로고만 · 법적 문서 3종 + `/customer/login`·`/customer/support-verify`). `components/LandingHeader.tsx`(37차)는 **파일째 없어졌습니다 — 두 파일을 헷갈리지 마십시오.**

🔴 **`/status`(문의·신청 현황 조회)는 62차에 라우트·API·처리방침 행까지 통째로 삭제됐습니다.** 되살리지 마십시오. **지금 비회원이 접수 결과를 확인할 방법은 없습니다**(사용자가 그렇게 확정).

⚠️ 법적 문서 3종은 **페이지 + 모달 하이브리드**입니다 — 푸터 링크는 모달로 열리지만 URL도 살아 있습니다(분쟁 시 시점 특정용). 조문을 고칠 때는 `lib/legal/*.ts`만 고치면 양쪽에 동시 반영됩니다.

### 화주 운송관리 포털 — 화주 로그인

홈 · 발주 요청 · 견적 확인 · 배차·운송 조회 · 정산·결제내역 · 월별 통계 · 배송지·화물 관리 · 담당자 정보 · **이용가이드** — **9종**

🔴 **「이용가이드」(`/customer/guide`)는 2026-09-14 신설입니다**(PR #149 — 차수 없음). 맨 아래 혼자 한 그룹이고(5그룹 9항목), 라벨이 공개 화면의 「운송관리 이용안내」와 **일부러 다릅니다**(같은 말이면 같은 화면으로 오해합니다). 🔴 **캘린더·공지사항·비밀번호 변경은 여전히 메뉴 밖입니다 — 이 김에 넣지 마십시오.** ⚠️ 메뉴 배열은 `NAV_GROUPS`(사이드바 **+ 모바일 「전체」 바텀시트**가 같은 `NavList` 를 씁니다)와 `MOBILE_TABS`(**고정 4탭**, 전체 메뉴가 아닙니다) 둘인데, **`NAV_GROUPS` 한 곳만 고치면 데스크탑·모바일이 함께 바뀝니다.**

🔴 **메뉴에는 없지만 살아 있는 라우트 셋** — 캘린더 · 공지사항 · **비밀번호 변경**. **"안 쓰는 라우트"로 보고 지우지 마십시오.** 비밀번호 변경을 지우면 신규 계정의 첫 로그인이 갇힙니다.

- 공지사항 진입로는 **홈의 공지 블록 하나뿐**입니다
- 🟢 캘린더는 **월별 통계 화면 하단**에 들어갔습니다(60차). `/customer/calendar` 라우트와 같은 컴포넌트를 씁니다

🔴 **포털 로그인은 이메일이 아니라 시스템 발급 아이디**입니다(`we` + 발급일 YYMMDD + 2자리). 내부적으로만 합성 이메일로 Supabase Auth에 등록합니다.

### 설치형 앱(PWA) — 포털과 내부관리 둘 다

**화주 운송관리와 내부관리는 홈 화면에 설치되는 앱입니다**(PR #126). 🔴 **랜딩·공개 화면은 설치 대상이 아닙니다** — manifest 링크 0건이 정상이고, 랜딩에 설치 안내를 넣지 않은 것도 사용자 확정입니다.

| | |
|---|---|
| manifest | `public/manifest-{customer,admin}.webmanifest` 🔴 **세그먼트별 manifest 는 Next 가 지원하지 않습니다**(루트 전용). 그래서 정적 파일 + `metadata.manifest` |
| 구역(scope) | **`/admin` · `/customer`** 🔴 **끝 슬래시를 붙이지 마십시오** — Next 가 `/admin/` 을 `/admin` 으로 308 로 보내는데 구역 판정이 문자열 접두 비교라, 슬래시를 붙이면 **앱이 켜지자마자 구역 밖으로 나가** 창에 주소 띠가 남습니다(PR #127에서 실제로 겪었습니다) |
| 서비스워커 | `public/{customer,admin}/sw.js` 🔴 스크립트 위치가 **최대** 구역을 정합니다 — 루트로 옮기면 한 워커가 두 시스템과 랜딩을 삼킵니다. 구역을 `/admin` 으로 넓히려고 `next.config.mjs` 의 `Service-Worker-Allowed` 헤더 + 등록 `scope` 를 씁니다(🔴 **둘은 한 벌**) |
| 캐시 | `offline.html` · `/_next/static/` · `/icons/` **셋뿐** 🔴 **화면(HTML)·API 0건** — 넓히면 화주가 낡은 배차 상태·정산 금액을 봅니다 |
| 설치 버튼 | 로그인 화면 + 홈, 네 곳. **홈에서는 제목 줄 오른쪽 끝**입니다. 문구는 **모바일에서 「홈 화면에 추가」, 데스크탑에서 「앱으로 설치」**(PR #128 — 실제로 하는 일이 그것이고 폰 메뉴에 적힌 말과도 같습니다). 설치 신호는 🔴 **인라인 스크립트가 HTML 을 읽는 순간 먼저 잡습니다**(`useEffect` 로 들으면 놓칩니다). 신호가 없어도 **주소창 설치 아이콘 위치를 안내**합니다 — 크롬은 앱을 지운 뒤 한동안 신호를 안 쏘는데, 전에는 그때 버튼이 사라져 다시 설치할 길이 없었습니다 |
| 🔴 앱 안 브라우저 | **카톡으로 링크를 열면 카톡 자체 브라우저가 뜨는데 거기에는 「홈 화면에 추가」가 없습니다.** 그런데 UA 는 아이폰/크로미움으로 잡혀서, 전에는 **있지도 않은 메뉴를 안내하고 있었습니다**(PR #128). 지금은 **in-app 판정을 맨 앞에서** 하고(카카오톡·네이버·라인·인스타/페북) 카톡은 `kakaotalk://web/openExternal` 로 **기본 브라우저에 넘깁니다**. 다른 앱은 표준이 없어 **주소 복사 + 메뉴 위치 안내**입니다. 🔴 **페이지를 열자마자 자동으로 넘기지 마십시오** |
| iOS | 🔴 **`beforeinstallprompt` 자체가 없습니다** — 「공유 → 홈 화면에 추가」 안내가 유일한 방법이고 버그가 아닙니다 |

🔴 **`middleware.ts` 의 `PUBLIC_PATHS` 3개(`icon.svg`·`apple-icon.png`·`sw.js`)를 줄이지 마십시오** — matcher 가 `/admin/` 아래 정적 파일을 전부 잡아서, 빼면 **화면에 아무 증상 없이**(307 리다이렉트라 콘솔도 조용합니다) 아이콘·설치가 조용히 망가집니다. 반대로 **화면·API 경로는 절대 넣지 마십시오.**

⚠️ **Vercel Preview 에서는 앱 창 위에 주소 띠가 남습니다** — Vercel 로그인 벽을 한 번 들렀다 오기 때문이고 **실제 도메인에는 생기지 않습니다.** 스코프 문제로 오해하지 마십시오.

### 내부관리 — 직원 로그인

🔴 **로그인은 이메일이 아니라 아이디입니다**(32차, PR #137). `/api/admin/staff-login`(service_role)이 `login_id` 로 행을 찾아 **그 행의 `email` 로** Supabase Auth 에 로그인시킵니다 — 🔴 **Auth 에 저장된 이메일은 한 글자도 안 바꿉니다.** 포털처럼 합성 이메일로 갈아치우면 **나중에 Resend 를 켰을 때의 유일한 복구 경로**가 사라집니다. 🔴 **`staff_accounts.email` 은 이제 로그인 자격이 아니라 연락처**이고, 화면 라벨도 「담당자 이메일(연락용)」입니다.

🔴 **메뉴에 없지만 살아 있는 라우트 하나 — `/admin/change-password`**(임시 비밀번호를 받은 직원의 **강제 변경** 화면, 포털과 같은 취급). middleware 가 `must_change_password` 를 보고 여기로 보내므로 **지우면 재발급받은 직원이 404에 갇힙니다.** 🔴 **`PUBLIC_PATHS` 에 넣지도 마십시오**(로그인이 필요한 화면입니다). 평소의 비밀번호 변경은 `/admin/my-account` 그대로입니다.

⚠️ **로그인 실패 문구는 한 종류뿐입니다** — 「없는 아이디」·「비밀번호 틀림」·「퇴사 처리됨」이 **문구·상태코드·응답시간 모두 같습니다.** 갈라 보여주면 아이디를 넣어보는 것만으로 누가 재직 중인지 새어 나갑니다. 🔴 **「사용자에게 친절하게」를 이유로 나누지 마십시오.**

| 그룹 | 화면 |
|---|---|
| 화주 확보 | 화주관리 · 화주신청 · 공개문의 · 개인고객 |
| 화주 관리 | 활성화주 CRM · 화주요청 |
| **운송 운영** | 견적 · 오더 · **배차 · 정산 · 차주** · 외부 정보망 · **운임기준표**(+가산기준·산재보험료 탭) |
| 시스템(admin 전용) | 운영 대시보드 · 직원 계정 · 지원접속 이력 · 문자 이력 |

⚠️ **운송 운영 순서가 35차 리뷰 4라운드(PR #146 `1d0b14b`)에 바뀌었습니다** — 사용자 지시 *「차주관리는 정산관리 아래에 위치하게」*. 그전에는 「…오더·**차주**·배차·정산」이었습니다. 견적 → 오더 → 배차 → 정산으로 **운송 한 건이 위에서 아래로 흐르는 순서**를 끊지 않으려고 차주를 뒤로 뺀 것입니다. 🔴 **옛 순서로 「정렬」하지 마십시오.** ⚠️ **「외부 정보망 관리」가 이 표에서 빠져 있었습니다**(코드에는 있습니다 — 2026-09-14 문서 정합 3차에 실측해 추가). 🔴 **메뉴는 여기와 `CLAUDE.md` 원칙 14번 두 곳에 적혀 있습니다 — 고칠 때 같이 고치십시오.**

---

## 4. 데이터·정책 구조에서 반드시 아는 것

**설계 원칙은 `CLAUDE.md` §3에 1~58번으로 정리돼 있습니다.** 여기에는 최근에 실제로 사고를 만든 것만 적습니다.

### 4-1. 🔴 화주와 직원이 같은 인증 롤을 씁니다

둘 다 `authenticated`입니다. **컬럼 GRANT로는 구분되지 않습니다.** 두 번 문제가 됐고, 둘 다 **service_role 서버 API**로 옮겨 해결했습니다.

🔴 **RLS 정책은 롤별로 갈립니다** — `TO anon` 정책은 `authenticated` 요청에 적용되지 않습니다. 이걸 모르고 세운 지시서가 실제로 틀렸습니다.

### 4-2. 🔴 anon은 개인정보를 읽지도 쓰지도 못합니다

정책 21개를 지우고 RLS 13개를 켰습니다. 남은 anon 경로는 **INSERT 2개뿐**(`public_quote_requests` · `customer_applications`) — 지우면 견적 문의와 계정 신청이 죽습니다.

⚠️ **RLS on + 정책 0개**인 표들(`consents` 등)은 **service_role 전용이 정상**입니다. 고치지 마십시오.
⚠️ **③(anon GRANT 회수)는 아직입니다** — RLS가 이미 막고 있어 추가 방어이며 급하지 않습니다.

### 4-3. 견적 계산

```
가산소계  = roundToUnit(요율분 + 정액 + 대기료 + 경유지비, 차급)
최종금액  = (기본운임 + 가산소계) − roundToUnit(혼적할인액, 차급)
```

🔴 **반올림은 v12 C장(PR #158)에 생겼습니다** — 격자는 **다마스·라보 5,000 / 나머지 10,000** 이고 정의처는 **`lib/roundToUnit.ts` 하나**입니다. 🔴 **개별 가산 줄이 아니라 「소계」에 겁니다**(줄마다 걸면 작은 요율 줄이 통째로 올라가 **과다 청구**). 자세한 것은 §5-1.

🔴 **마진율 파라미터가 없습니다.** `rate_distance_tiers.base_fare`가 곧 **화주 청구가**이고, 차주 지급액은 배차 단계에서 담당자가 수동 입력합니다. 마진은 그 차액입니다.

🔴 **그것은 주선사정산 기준입니다 — 선착불의 마진은 차액이 아니라 주선수수료입니다**(§5-12). 마진의 계산은 `lib/marginCalc.ts` **한 곳**입니다.

🔴 **가산은 `option_name` 문자열 완전일치로 매칭한 뒤 못 찾으면 `continue` 합니다** — 예외도 경고도 없이 **가산만 조용히 빠집니다.** 그래서 **옵션 라벨을 바꾸는 코드와 `rate_surcharges` 행은 반드시 같은 커밋**에 넣습니다. 🔴 **가산이 0원인 행도 지우면 안 됩니다**(행이 없으면 드롭다운에서 고를 수 없게 됩니다 — §5-1).

🔴 **차량형태·운송시간 가산은 v12 B장부터 정액입니다**(`rate_pct = 0`). 요율이 남은 것은 **`왕복/편도` 3행뿐**입니다.

🔴 **거리 구간 매칭은 "상한 이하 첫 구간"이고 `distance_from_km`을 보지 않습니다** — 조건에 다시 넣으면 정수 사이 빈틈이 살아나 소수 거리(10.4km 등)에서 자동계산이 조용히 실패합니다.

### 4-4. 🔴 포털 CSS는 관리자와 거의 전부 공유입니다

포털이 쓰는 클래스 25개 중 **전용은 하나뿐**이고 `.container`·`.card` 등을 관리자 화면 31개가 같이 씁니다. 그래서 포털 개편은 **`.portal-v2` 스코프 격리**로 갔고, 새 컴포넌트는 전부 `.pv2-*`입니다.

🔴 **"중복이니 전역으로 합치자"고 하지 마십시오.** 합치는 순간 관리자가 딸려옵니다.
🔴 **`globals.css`는 순수 추가만** — 삭제된 줄 0이 관리자 무회귀의 근거입니다.
🔴 **포털 폼에 네이티브 `<select>`·`<input type="date">`를 쓰지 마십시오**(원칙 57번) — `Pv2Select` · `Pv2DatePicker`를 씁니다. 네이티브는 펼친 목록·달력을 브라우저가 그려서 CSS로 못 바꿉니다.

### 4-5. 🔴 `force-dynamic`만으로는 캐시를 못 막습니다

그 지시자는 *라우트 렌더링*만 동적으로 만들고, 라우트 안에서 supabase-js가 쓰는 `fetch`는 그대로 Data Cache를 탑니다. **DB를 바꿨는데 API가 옛 값을 계속 내려주는** 일이 실제로 있었습니다.

🔴 **새 서비스롤 GET 라우트에는 `force-dynamic`과 `createServiceClient()`가 둘 다 필요합니다.**

### 4-6. ⚠️ CSS 특성도와 인라인 style

이 저장소는 화면 스타일을 **인라인 `style`로 많이 씁니다.** 그래서 —

- 🔴 **클래스 규칙으로는 인라인을 못 이깁니다.** 인라인을 덮어야 하면 `!important`가 필요하고, 그건 우회가 아니라 **유일한 방법**입니다
- 🔴 **특성도가 같으면 선언 순서가 이깁니다** — 레이아웃이 어긋날 때 **규칙 순서를 바꿔 고치지 마십시오**(다음에 누가 정렬하면 조용히 되돌아갑니다). `.a.b`처럼 **두 클래스로 특성도를 올려서** 고칩니다

⚠️ **미디어쿼리는 특성도를 올려주지 않습니다.** `@media` 안의 한 클래스 규칙은 바깥의 두 클래스 규칙을 못 이깁니다 — 모바일 전용 값이 조용히 안 먹는 형태로 나타납니다(2026-09-04에 `.pv2-load-slot`이 모바일에서 100%가 아니라 190px로 남아 있던 것이 이 경우). 🔴 **`@media` 안에도 같은 특성도로 써야 합니다.**

---

## 5. 확정 사항

### 5-1. 운임

| 항목 | 값 |
|---|---|
| 차급 | **13종** — 다마스 / 라보 / 1 / 1.4 / 2.5 / 3.5 / 5 / 5톤 플러스·축 / 8 / 11 / 15 / 18 / 25<br>🔴 **DB 정본 표기는 `5톤 플러스/축`** — 위 「·」는 `/` 가 이 줄의 구분자와 겹쳐서 쓴 표기이고, 견적 매칭은 **문자열 완전일치**라 값으로 쓸 때는 반드시 `/` 입니다<br>🔴 **다마스·라보는 2026-09-11 신설이고 배열 맨 앞입니다**(PR #142) — 배열 순서가 곧 `/admin/rates` 열 순서이자 드롭다운 순서라 **끝에 붙이지 마십시오.** 🔴 **어디에 나오고 어디에 안 나오는가 (사용자 확정 2026-09-11)**<br>　화주포털 발주요청(`/customer/request`) **✅ 화주가 직접 고릅니다**<br>　공개 폼(`/quote` · `/apply`) ❌ `VEHICLE_TYPES_PUBLIC` 에 없습니다<br>　랜딩 「차량 · 요금 가이드」 ❌ `PUBLISHED_START_PRICE_TONS` 에 없습니다<br>🔴 **「내부 전용」이라고 적지 마십시오** — 그 표현을 근거로 포털에서 빼면 사용자 확정과 정반대가 됩니다. 지금 동작이 확정된 동작이고 **코드는 한 줄도 바꾸지 않았습니다**<br><br>🔴 **접수 기준 (사용자 확정 2026-09-11)**<br>　무게　　둘 다 **500kg 이내**<br>　다마스　높이 **110cm 이내** · 비를 맞으면 안 되는 짐 (밀폐형 밴 · 적재함 160×110×110cm)<br>　라보　　높이 **160cm 까지** · 길거나 높은 짐 (개방형 · 적재함 190×130×160cm)<br>🔴 **무게로는 둘이 같습니다 — 갈리는 것은 부피와 형태입니다.** 「다마스가 더 작은 차」로 요약하지 마십시오<br>🔴 **파렛트 화물은 둘 다 받지 않습니다** — 표준 파렛트(1,100×1,100mm)가 다마스 폭에 안 들어가고, 라보는 폭은 되지만 파렛트 무게까지 더하면 500kg 를 넘깁니다. **파렛트는 1톤부터**입니다 |
| 거리 구간 | 15구간 · **총 195행** (13차급 × 15구간) |
| 🔴 현재 기준가 | **v12**(2026-09-15 · A장 PR #156 · B장 PR #157 · C장 PR #158) — 195칸이 **만원 격자**에 올라가 있습니다(다마스·라보만 5,000). 게시 6칸은 **40,000 / 50,000 / 80,000 / 90,000 / 110,000 / 140,000** |
| 🔴 격자가 **둘**이다 | 다마스·라보 **5,000 배수** / 1톤 이상 **10,000 배수**. 🔴 **한 격자로 합치지 마십시오** — 전 차급 만원 반올림은 **역전**을 만듭니다(11·15·18톤 200km 342·344·345천이 셋 다 340,000 이 됩니다). ⚠️ **예전에 같이 적던 「라보 35,000 과 1톤 40,000 이 둘 다 40,000」은 낡았습니다** — 라보 `10km 이내` 는 2026-09-18 부터 **30,000** 입니다(아래) |
| 🔴 라보 = 다마스 | **15칸 전부 같은 값입니다**(2026-09-18 · PR #178 · `2026-09-18_rate_labo_unify.sql`). 사용자 지시 *「다마스, 라보는 기본 30000원으로 정하자. 그 이후거리 금액도 모두 다마스기준으로 통일하자」*. 🔴 **다마스는 한 칸도 안 바꿨습니다** — `10km 이내` 가 v12 A장부터 이미 **30,000** 이었습니다(코드를 열기 전에 `verify` 로 재서 확인했고, 안 쟀으면 **아무것도 안 바뀌는 UPDATE 를 써 놓고 「바꿨다」고 보고**할 뻔했습니다). 값은 **30,000 / 40,000 / 50,000 / 60,000 / 70,000 / 80,000 / 90,000 / 100,000 / 110,000 / 120,000 / 135,000 / 155,000 / 185,000 / 215,000 / 240,000**. 🔴 **수식으로 다시 만들지 마십시오** — 마이그레이션이 다마스 행을 **그대로 복사**하고, 그 전에 다마스 15칸이 위 값과 같은지 **먼저 단언**합니다(다르면 아무것도 안 바꾸고 멈춥니다). 🚨 **곁다리로 100원 오타 두 칸이 같이 씻겼습니다** — 라보 `110km 이내` **85,100** · `400km 초과` **225,100** 이 5,000 배수가 아니었습니다(v12 A장은 85,000 / 225,000 을 넣었으니 그 뒤 `/admin/rates` 에서 손으로 저장된 값입니다). 🔴 **그 화면은 클릭이 곧 저장이고 되돌리기가 없습니다** — 운임을 화면에서 고치면 저장소에 안 남으니 **문서에 한 줄 남기십시오**(혼적 30km 20% · 다마스·라보 적재 한도에 이어 **문서와 DB 가 갈린 세 번째**입니다). 되돌리기는 `_bak_rate_distance_tiers_before_labo_unify` 하나이고 **라보만 되돌립니다** |
| 🔴 역전 검사 예외 한 쌍 | 다마스 = 라보가 되면서 **차급 역전 검사(`_verify.sql` ⑥-b)가 다마스↔라보에서만 동가를 허용**합니다(`ob.rk = oa.rk + 1` 일 때 앞이 다마스면 `<`, 아니면 `<=`). 🔴 **`<=` 를 전부 `<` 로 바꾸지 마십시오** — 나머지 **11쌍의 동가까지 조용히 통과**해 검사가 헐거워집니다. 🟢 라보가 다마스보다 **낮아지면 여전히 걸립니다**(실측: 동가 0건 · 라보를 낮추면 1건) |
| 🔴 앵커는 하나 | **1톤 `10km 이내` = 40,000** — 반올림 대상이 아니라 사용자가 확정한 공개 게시가입니다. 마이그레이션 단언이 못박습니다 |
| ⚠️ v11 계산식 | `ROUND(차주지급 × 1.15, -3)` 이었습니다 — **v12 가 덮어썼습니다.** `base_fare` 가 **화주 청구가**라는 것은 그대로이고, 🔴 **마진율 파라미터는 여전히 시스템에 없습니다** |
| 🔴 `200km 이내` | **차급 간격이 가장 빡빡한 구간**입니다 — 11톤 340,000 · 15톤 350,000 · 18톤 360,000 · 25톤 370,000 으로 **10쌍 중 7쌍이 격자 한 칸(10,000)** 입니다. 값을 만질 때 여기부터 역전을 보십시오. ⚠️ v11 시절 표기(342·344·345천)는 낡았습니다 |
| 8톤·15톤 | 인접 차급 **보간값**(근거 등급 D) |
| 무료 대기 | **20분** |
| 차량형태 | **21종**(견적용) / 차주 보유차량은 별도 24종. 🔴 **가산은 v12 B장부터 정액입니다**(아래) |
| 상하차방식 | **8종** — 🟢 이미 사실상 정액입니다(요율 0건 · `수작업` 10,000 뿐) |
| 🔴 가산은 **정액** | 차량형태 **21행** + 운송시간 **6행**의 `rate_pct` 가 전부 **0** 이고 `flat_amount` 만 씁니다(v12 B장 · PR #157). 🔴 **`왕복/편도` 3행만 요율을 남겼습니다** — 거리 비례라 정액으로 담을 수 없습니다(1톤 10km +4만 vs 25톤 400km +91만) |
| 🔴 최종금액 **반올림** | 가산 **소계**와 혼적 할인액을 그 차급의 격자에 올립니다(v12 C장 · PR #158 · `lib/roundToUnit.ts` **유일 정의처**). 🔴 **줄마다 걸지 마십시오** — 작은 요율 줄이 통째로 올라가 **과다 청구**가 됩니다 |
| 🔴 혼적 표준 할인율 | 거리 **3구간**(2026-09-09 · PR #136) — 30km 이내 **20%** / 250km 이내 **35%** / 250km 초과 **55%**. ⚠️ **한동안 「15%」로 적혀 있었습니다 — DB 실측값은 20% 입니다**(2026-09-11 `verify` ⑩-e 조회). PR #136 마이그레이션은 15 로 넣었고 그 뒤 담당자가 `/admin/rates` 에서 20 으로 올렸습니다(세 행의 `updated_at` 이 `2026-09-09 03:10:47` 로 같고 `updated_by` 가 직원 계정입니다). 🔴 **마이그레이션 파일의 15 를 근거로 되돌리지 마십시오 — DB 가 정본입니다.** 🔴 **계산값이 아니라 입력창 기본값**이고 담당자가 건별로 고칠 수 있습니다 |

🔴 **v11 은 v10 매트릭스가 아닙니다.** 근거 자료 v10 은 **차주 지급 기준**이고 우리 `base_fare` 는 **화주 청구가**입니다 — v10 을 그대로 넣으면 1톤 10km 이 36,000 이 되어 **마진이 0** 이 됩니다.
🔴 **마진 15% 는 시스템 파라미터가 아닙니다.** DB 에는 청구가만 있고 차주 지급가는 배차 담당자가 건별로 입력합니다. **마진율 컬럼·설정 화면을 만들지 마십시오** — 만드는 순간 `base_fare` 의 의미가 뒤집혀 **공개 게시가에 원가가 나갑니다.** 차주 지급 상한 가이드(= 청구가 ÷ 1.15)는 운영 규칙이고 코드에 없습니다.

🔴 **공개 게시가 6칸 (v12 · 사용자 승인 2026-09-15 — 인상 0칸)**

| 차급 | v11 | **v12** | 증감 |
|---|---:|---:|---:|
| 1톤 | 40,000 | **40,000** | 🟢 ±0 (앵커) |
| 1.4톤 | 52,000 | **50,000** | −2,000 |
| 2.5톤 | 87,000 | **80,000** | −7,000 |
| 3.5톤 | 100,000 | **90,000** | −10,000 |
| 5톤 | 115,000 | **110,000** | −5,000 |
| 5톤 플러스/축 | 150,000 | **140,000** | −10,000 |

⚠️ **v11 의 「×1.15 예외 세 칸」 기록은 v12 가 덮어썼습니다**(5톤 115,000 → 110,000 · 5톤+/축 150,000 → 140,000). 🔴 **그 표를 근거로 되돌리지 마십시오** — 지금은 **전 칸이 격자값**이고 ×1.15 예외라는 개념 자체가 없습니다.
🔴 **10·20·30km 만 내렸고 60km 이상은 격자 정렬만 했습니다** — 실측 마진이 근거리는 33~56% 인데 **중·장거리는 −4.2% ~ 14%** 입니다. 사용자 원문의 *「그 뒤로 운행구간도 유사한 비율로」*를 그대로 적용하면 **역마진이 번집니다.** 🔴 **60km 이상을 같은 비율로 내리지 마십시오.**
🔴 **18톤 `200km 이내` 만 +15,000** 입니다(345,000 → 360,000) — 15톤 350,000 과 벌리려고 올린 것이니 **「왜 이 칸만」 하고 맞추지 마십시오.**
🔴 **v10 매트릭스로 마진을 재지 마십시오** — 근거리 값이 「완료·독차·30km 초과 527건」 계수의 **외삽**이라 실측보다 40% 이상 높습니다. 기획 세션이 그것으로 재서 *「3.5톤 90,000 은 마진 3.4%」*라는 틀린 경고를 냈고, 실측으로 다시 재니 **50%** 였습니다. 마진은 **24시콜 실거래(완료·독차) 중앙값**으로 잽니다.
⚠️ **중·장거리 역마진 두 칸은 이번에 안 올렸습니다** — 5톤+ `130km 이내` −4.2%(표본 8건) · 5톤 `90km 이내` −3.0%(표본 6건). 표본이 얇고 인접 차급 역전이 나므로 **실거래가 더 쌓인 뒤 별도 차수**입니다.
⚠️ **되돌리는 수단은 `_bak_rate_distance_tiers_before_v12` 스냅샷 하나뿐**이고(운임 이력 표가 없습니다), **되돌리면 공개 게시가도 같이 돌아갑니다.** 🔴 **B장·C장이 들어간 뒤에 A장만 되돌리면** 가산은 정액인데 기본운임만 옛 값이 되어 **최종금액이 만원에서 벗어납니다.**

⚠️ **22차의 「18톤 전 구간 ×0.89 · 200km 이내만 −5.5%(341,000)」는 v11 이 덮어썼습니다** — 그 값은 이제 없습니다. 다만 **그 구간이 차급 간격이 가장 좁다는 성질은 그대로**여서 위 표에 옮겨 적었습니다.

⚠️ **직전 구간 대비 +25% 를 넘는 칸이 여섯 있고 전부 정상입니다**(v12 실측) — 다마스 10→20km +33% · 라보 10→20km **+33%**(⚠️ v12 시절에는 +29% 였고 **PR #178 로 다마스와 같아졌습니다**) · 3.5톤 30→60km +27% · 2.5/3.5톤 250→300km +33%/+32% · 25톤 200→250km +30%. 앞의 셋은 **격자가 성긴 소형·근거리**라 한 칸이 크게 보이는 것이고, 뒤의 셋은 **외부 실거래표의 실제 모양**입니다. 🔴 **「급등이니 완만하게 펴자」고 하지 마십시오.** ⚠️ v11 시절 목록(1톤 10→20km · 15/18톤 200→250km 등)은 낡았습니다.

🔴 **무료 20분 / 초과 30분당 단위는 「현행 유지」로 확정됐습니다**(2026-09-04). 25차부터 답을 기다리던 항목이고, **문구와 동작이 이미 일치합니다** — 계산식이 실제로 `ceil((대기분 − 20) ÷ 30) × 단가`라 「무료는 20분인데 왜 30분당이지」 하고 **문구만 고치면 오히려 표시가 틀려집니다.** 단위만 20분으로 줄이면 단가는 그대로인 채 구간이 촘촘해져 **대기료가 최대 2배**가 됩니다(대기 50분 1톤 10,000 → 20,000).
⚠️ **단위 `30`은 `app/admin/quotes/page.tsx` 한 곳에만 하드코딩돼 있고 DB 컬럼이 없습니다** — 무료시간·단가는 담당자가 `/admin/rates`에서 바꿀 수 있는데 단위만 못 바꿉니다. 운영에서 조정할 일이 생기면 그때 컬럼을 하나 더 만드는 것이 맞습니다.

🔴 **11톤·25톤은 손대지 마십시오** — 11톤은 검증됐고(완료 36건, 마진 +19.6%), 25톤은 완료 3건이라 판단 불가입니다.

🔴 **가산 — 요율제에서 정액으로 (v12 B장 · PR #157 · 2026-09-15)**

차량형태 **21행** + 운송시간 **6행**의 `rate_pct` 를 전부 **0** 으로 바꾸고 `flat_amount` 만 남겼습니다(UPDATE 27행).

```
  요소 정액 — 가법 구조                       시간 할증
    윙 · 호로 (개폐·덮개)   10,000             새벽   20,000      주말   10,000
    탑 (밀폐)              20,000             야간   10,000      공휴일 10,000
    리프트 (파워게이트)      20,000             이른아침 10,000    심야   10,000
    초장축                 20,000
    냉장 40,000 · 냉동 50,000 · 무진동 100,000
    🔴 조합형(카고/윙 · 윙바디/탑)은 **낮은 쪽**을 따릅니다
```

🔴 **근거는 실측 「절대 차액(원)」입니다** — 같은 차급 × 같은 거리구간의 카고 중앙값 대비 윙바디 **10,000**(145건) · 리프트 **15,000**(35건) · 냉동탑 **50,000**(10건) · 카고계열 **0**(722건). 요율제는 대형·장거리에서 실측을 크게 넘었습니다(윙바디 15% 가 1톤 10km 에서 6,000원인데 **25톤 400km 에서 136,500원**).
🔴 **가산 0원 4종의 행을 지우지 마십시오** — `카고`(428건) · `카고/윙`(69건) · `차종무관`(225건) · `특수/협의`. **전체 오더의 71%** 입니다. 행이 없으면 `option_name` **문자열 완전일치**에 실패해 **드롭다운에서 고를 수 없게 됩니다**(20차 `도크` 와 같은 자리). 마이그레이션 단언이 지킵니다.
🔴 **대형은 윙바디 프리미엄이 없습니다** — 차급군별 실측 차액이 소형 5,000 · 중형 25,000 · **준대형 −5,000 · 대형 −10,000** 입니다(큰 차는 윙바디가 표준입니다). 🔴 **「대형인데 왜 가산이 같지」로 요율을 되살리지 마십시오.**
🔴 **새벽은 20,000 입니다** — 기획 세션이 30,000 으로 유도했으나 사용자가 낮췄습니다(*"새벽의 비율이 너무 높다"*).
⚠️ **운송시간 6종은 실측 표본 0건입니다**(수집 오더 1,860건이 전부 06~19시) — **근거 등급 D** 이고 🔴 **「실측으로 확인됐다」고 적지 마십시오.**
⚠️ **가산 수입이 건당 12,082 → 4,077원(−66%)** 이 됩니다 · 견적 총액 −5.9% · 마진 34.5% → **26.7%**. **의도한 것이고**, 배차가 오히려 쉬워지는지 운영해 보고 판단합니다.
🟢 **범위 밖은 한 칸도 안 바뀝니다** — `물품특성` 8행 · `상하차방식` 8행 · `긴급여부` 4행(폐지된 항목) · `특별할인` 1행 · `rate_vehicle_extra_fees` 13행 · `mixed_loading_discount_settings` 3행.
⚠️ 되돌리기는 `_bak_rate_surcharges_before_v12` 하나뿐입니다.

🔴 **반올림과 견적서 가산 한 줄 (v12 C장 · PR #158 · DB 0)**

A·B 가 들어간 뒤에도 **만원을 깨뜨리는 것이 넷** 남습니다 — **왕복/편도**(유일하게 남은 요율) · **대기료** · **경유지비**(각 13행 중 **4행이 5,000 단위**) · **혼적 할인**(율 방식).
🔴 **그 표들을 고쳐서 반올림을 없애려 하지 마십시오** — 담당자가 `/admin/rates` 에서 정한 값이고 사용자가 **현행 유지로 확정**했습니다(2026-09-15). **반올림이 그것을 흡수하는 것이 `lib/roundToUnit.ts` 의 존재 이유입니다.**

```
  surchargeTotal = roundToUnit(요율분 + 정액 + 대기료 + 경유지비, 차급)
  rawFinal       = max(기본운임 + surchargeTotal, 0)           ← 격자값
  혼적할인        = roundToUnit(rawFinal − applyMixedDiscount(rawFinal, …), 차급)
  final          = max(rawFinal − 혼적할인, 0)                  ← 격자값
```

🔴 **개별 가산 줄이 아니라 「소계」에 겁니다** · 🔴 **대기료·경유지비를 소계 밖으로 빼지 마십시오**(밖에 두면 최종금액이 만원에서 벗어납니다) · 🔴 **`roundToUnit` 은 차급을 인자로 받습니다**(호출부마다 다마스·라보 예외를 적으면 반드시 한 곳이 빠집니다 — 35차 `lib/marginCalc.ts` 와 같은 결). 격자 상수는 `lib/constants.ts` 한 곳입니다.

**🔴 화주가 받는 견적서는 가산을 한 줄로 합산합니다**((가)안 · 사용자 확정). 정의처는 **`lib/quoteFareLines.ts` 하나**이고 **여섯 산출물**(화주 견적 펼침 · 화주 견적서 상세 · print 2종 · 공유 견적서 `/q/[token]` · 엑셀)이 같이 씁니다.

```
  기본운임                                   130,000
  가산 (윙바디 · 새벽 · 왕복 · 대기료(47분))    90,000   ← 개별 4줄이 한 줄로
  혼적 할인(10%)                             -20,000   ← 🔴 할인 줄은 그대로 남깁니다
  공급가액                                   200,000   ← 합이 정확히 맞습니다 (조정 줄 없음)
```

🔴 **왜 합치는가** — C장이 **소계**를 반올림하는데 `quote_items` 에는 **반올림 전 개별 줄**이 그대로 남습니다. 개별 줄을 그리면 합이 최종금액과 어긋나 **거의 모든 견적에 「조정」 줄이 붙습니다.** 🟢 그리고 **28차 확정**(가산 **금액**은 화주에게 비공개 — `/api/customer/surcharge-options` 가 **이름만** 내려줍니다)**과도 맞습니다.**
🔴 **할인(음수) 줄은 합치지 마십시오** — 혼적 할인처럼 화주가 **동의해서 받은 것**이라 감추면 안 됩니다.
🔴 **「조정」 줄은 이제 수동 조정 전용입니다** — 반올림 차액은 거기 오지 않습니다(가산 줄이 이미 반올림된 `surcharge_amount` 라서).
🔴 **관리자 견적 상세(`/admin/quotes/[id]`)는 개별 줄을 그대로 봅니다**(내부 화면 · 사용자 확정) — 담당자가 「왜 이 금액인가」를 되짚을 수 있어야 합니다. ⚠️ **그래서 그 화면의 조정 줄에는 반올림 차액이 나타납니다 — 고장이 아닙니다.** ⚠️ 그 화면은 **원래부터 「공급가액 (부가세 별도)」 줄이 없습니다**(「최종 견적금액」 + 캡션 구조) — v12 가 만든 차이가 아닙니다.
🔴 **가산 줄에는 이름을 적고 금액·요율은 적지 않습니다**(리뷰 1라운드 · *"그냥 「가산」으로만 되어 있어 고객입장에서 오해할수 있다"*). 🔴 **요율 표기 `(80%)` 는 떼어냅니다**(`stripRateSuffix`) — 기본운임이 **바로 윗줄**이라 요율을 보면 그 줄의 금액이 그대로 계산됩니다(왕복 80% × 130,000 = 104,000). **금액을 감추기로 한 결정이 요율 한 글자로 무너집니다. 되살리지 마십시오.** ⚠️ `(47분)`·`2곳` 처럼 **금액이 계산되지 않는 괄호는 남깁니다.**

🔴 **회귀 기준선 (2026-09-16 실측 · v12 C장 merge 직후)**

```
  「공급가액 (부가세 별도)」 라벨    6곳
     화주 견적 펼침 · 화주 견적서 상세 · 화주 print · 관리자 print ·
     공유 견적서 /q/[token] · 엑셀(lib/quoteExcel.ts)
     🔴 3곳이 아닙니다 — PR #155 가 「맞춘」 곳이 셋이고(그 PR 본문의 「세 곳이
        맞았다」는 그 PR 이 바꾼 범위입니다), 라벨이 있는 곳은 그보다 많습니다.
     🔴 「합계 (부가세 별도)」로 되돌아간 곳은 0건이어야 합니다.
     🟢 다섯 화면 모두 조건부가 아닌 무조건 렌더링입니다(금액만 priceless 로 갈림).
     ⚠️ 관리자 견적 상세(/admin/quotes/[id])는 원래부터 이 줄이 없습니다
        (「최종 견적금액」 + 캡션 구조) — 빠진 것이 아닙니다.

  프리렌더 실패                     46      ← v12 전후 동일 · 46개 항목 diff 0
     🔴 환경변수 없이 `npm run build` 를 돌렸을 때의 값입니다. 더미
        NEXT_PUBLIC_SUPABASE_* 를 넣고 빌드하면 실패가 0건이라 비교가 안 됩니다.
     🟢 /q/[token] 은 동적 세그먼트라 이 목록에 들어오지 않습니다(실측 확인).

  `_migrations`                     37행
```


🟢 **공개 기준가는 DB를 실시간으로 읽습니다**(`lib/startPrices.ts`, 64차). 게시 범위는 **「10km 이내」 × 6차급**(1톤 ~ 5톤 플러스/축)이고, `/admin/rates`에서 그 칸을 고치면 **확인 창**이 뜬 뒤 바로 게시됩니다.
🔴 **그래서 운임 마이그레이션은 merge 를 기다리지 않고 게시됩니다** — A장(PR #156)은 `apply` 가 끝난 순간 랜딩 숫자가 바뀌었습니다. **제품 코드는 0줄**입니다.
🔴 **폴백 금액을 다시 만들지 마십시오** — 두면 반드시 낡고, 게시가 ≠ 견적가가 되면 그대로 표시가격 분쟁입니다. 조회 실패 시에는 표를 안 그리고 안내 문구만 남깁니다.

### 5-2. 표기

| 항목 | 값 |
|---|---|
| 법인 · 브랜드 | **(주)디자인에그 │ 위캐리 운송** — 가운데는 글자가 아니라 **세로 구분선** |
| 사업자등록번호 | 105-87-66929 |
| 입금 계좌 | 국민은행 069101-04-228121 · 예금주 (주)디자인에그 |
| 고객센터 | 1661-2403 · 평일 09:00–19:00 |
| 대표 메일 | **biz@wecarrylogis.co.kr** (카페24 웹메일) |
| 도메인 | **wecarrylogis.co.kr** (`www` 없는 쪽이 메인) |
| 시행일 | **2026-09-07** |

🔴 **법인·브랜드는 어느 한쪽도 강조하지 않습니다** — 같은 크기·같은 굵기. 화면·인쇄에는 평문이 아니라 **전용 컴포넌트**(`CompanyNameMark`)를 씁니다(평문을 그리면 구분선이 글자로 찍힙니다).

🔴 **메일 주소는 상수 한 줄(`COMPANY_CONTACT_EMAIL`)이 전부입니다** — 처리방침 제11·12조와 사업자 정보가 전부 그 상수를 참조합니다. **개인정보 전용 주소를 따로 만들지 않기로 확정**했고(69차 ③), **상수를 둘로 나누지 마십시오.**
⚠️ `COMPANY_INFO.email`(견적서 PDF 발행업체용)은 **별개 상수이고 빈 값**입니다. 둘을 합치지 마십시오.
⚠️ 대표번호 상수도 **두 개**입니다(고객센터 / 견적서 발행업체 TEL). 지금은 같은 값이지만 **바꿀 때 두 곳을 함께** 고쳐야 합니다.

### 5-3. 🔴 공개 화면 문구

**확정 문구 — 「1톤부터 5톤 이상, 특수차량까지」** (시안 원문, 30차 리뷰 2라운드 확정)

⚠️ **v20의 「1톤부터 5톤 이상까지」에서 바뀌었습니다.** 「특수차량」과 「25톤」은 **금지어에서 풀렸습니다**(차급 11종이 실재해 견적이 실제로 산출되기 때문). 다만 —

| | |
|---|---|
| 🔴 여전히 금지 | `전 차종` · `모든 차량` · `최저가` · `예비 배차처` · `계약 없이` · `기존 거래처를 바꾸` · **`이사`** |
| 🔴 여전히 금지 | 연착 · `보장합니다` · `빠짐없이` · `남김없이` · `염려 없이` 같은 **단정 표현** |
| 🔴 여전히 금지 | 보험 **금액**(약관 제19조가 금액 없는 설계입니다) |
| ⚠️ 조건부 허용 | `월별 이벤트` · `운임할인 · 적립 · 캐시백` — **실제로 운영돼야 하고 조건·기간을 함께** 알려야 합니다(표시광고법 제3조) |

🔴 **「이사」는 65차에 고객 접점에서 다시 뺐습니다**(wecarry24.co.kr로 분리). 30차 기록을 근거로 되살리지 마십시오. 단 `public/landing/service-04-moving.jpg`는 **그 사이트가 쓰므로 지우지 마십시오.**

**금지 표현의 적용 범위**

```
광고·유입   /, /quote, /apply                 → **11종** (VEHICLE_TYPES_PUBLIC)
                                              ⚠️ 이 줄은 오래 「6종」이라 적혀 있었는데
                                                 31차부터 실제는 11종입니다(실측 2026-09-11)
            /guide (거래처 온보딩 · PR #148)   → 차급을 **하나도 열거하지 않습니다**(실측)
                                              🔴 그래도 **공개 화면이라 위 금지 표현은 그대로 걸립니다**
                                                 — 이 줄이 오래 빠져 있었습니다(2026-09-15 보완).
                                                 🔴 차급을 적을 일이 생기면 `VEHICLE_TYPES_PUBLIC`(11종)
화주 입력   /customer/request 희망 톤수       → **13종** (로그인 화주 대상이라 광고가 아님)
                                              🔴 다마스·라보를 화주가 직접 고릅니다 (사용자 확정 2026-09-11)
표시        포털 조회 · 견적서 · 엑셀         → 저장값 그대로
관리자·DB                                     → **13종** (VEHICLE_TYPES_ALL)
기준가 게시  랜딩 「차량 · 요금 가이드」        → **6종** (PUBLISHED_START_PRICE_TONS)
```

🔴 **두 배열을 합치지 마십시오.** 🟢 **2026-09-11 에 값이 처음으로 갈렸습니다** — 다마스·라보가 `ALL`(13) 에만 있고 `PUBLIC`(11) 에는 없습니다. 46차가 배열을 나눈 이유가 실현된 첫 사례이니 **같은 값으로 맞추지 마십시오.**
⚠️ 「`/apply` 가 배열을 `join(", ")` 으로 본문에 뿌린다」는 옛 근거는 **낡았습니다** — 31차에 그 툴팁을 없앴습니다. 🔴 **다시 만들지 마십시오**(그것이 46차가 배열을 나눈 직접적인 이유였습니다).
🔴 **검증은 소스가 아니라 렌더링 텍스트에 겁니다** — 소스에 걸면 금지어를 경고하는 주석 자체가 매치됩니다.

### 5-4. 🔴 용어가 갈리는 두 줄은 의도입니다

| | 화주 화면 | 관리자·DB |
|---|---|---|
| 견적 상태 | 협의 중 · 취소 | `보류` · `실패` |
| 배차 상태 | **3단계** (접수 / 배차완료 / 운송완료) + 「문제 발생」 별도 배지 | 6종 |
| 정산방식 | 「운임 수금방식」 위캐리 수금 / 선착불 + **별도 줄** 「청구」 건별 / 월정산 | 2축 4값 |

화주에게 "실패"라고 보여주면 **자기 요청이 문제였던 것으로 오해**하고, 담당자에게는 "실패"가 정확합니다.

🔴 **통일하려 들지 마십시오** — DB와 이력이 전부 걸립니다.
🟢 배차 3단계 매핑의 정의처는 **`lib/dispatchStage.ts` 하나**입니다(배차 화면·홈 공용). 🔴 `lib/dispatchStatusColors.ts`(6종)를 이 매핑으로 바꾸지 마십시오 — 관리자 3화면이 그대로 씁니다.
🔴 **차주 성명·연락처·차량번호는 화주에게 노출하지 않습니다**(마스킹도 안 함).
🚨 **이것은 화면 기준입니다 — 배차확정 문자로는 보냅니다**(2026-09-18 · 사용자 확정 · §5-25).
고객이 배차 내용을 다시 확인하고 잘못된 상하차지·차량 배차를 미리 발견하게 하려는 것이고,
처리방침 제4조가 이미 그렇게 적고 있어 **그 조가 사실과 맞게 됐습니다**.
🔴 **이 줄을 근거로 고객 문자에서 기사 정보를 빼지 마십시오** ·
🔴 **반대로 화주포털 화면에 기사 정보를 띄우지도 마십시오**(화면은 그대로입니다).

### 5-5. 🔴 약관 — 「책임은 고객과 차주에게 있다 + 회사는 중재한다」 (terms-v5)

방향이 **두 번** 뒤집혔습니다. 34차의 「회사가 배상 주체」 → 66차의 「차주·보험자 **1차** 부담」 → **PR #125의 「배상책임은 차주 및 그 보험자에게 **있습니다**」**(「1차적」이 빠졌습니다). 🔴 **옛 기록으로 되돌리지 마십시오.**

**PR #133이 v4 로 한 번 더 고쳤습니다**(사용자 지시 3건). ① 제19조 1항 조건절 **앞에** 「배상책임은 실제 운송을 수행한 차주 및 그 보험자에게 **있으며**」를 세웠습니다 — 조건절만 있으면 **어떤 경우에 회사가 지는지를 이 조가 스스로 말하지 않아** 애매합니다. 🔴 **제18조 1항과 중복이 아닙니다**(누가 지는가 / 그럼에도 회사가 질 때 얼마까지인가) — 한쪽을 「정리」로 지우지 마십시오. ② **제20조가 4개 항이 됐습니다** — 🔴 **「14일 이내 통지」 호와 「1년 소멸시효」 항은 삭제됐습니다.** 옛 기록이 「제20조 1항 2호」·「5항」을 가리키면 그건 낡은 것입니다. 🔴 **2항의 악의 인도 예외(상법 제146조 2항)는 남았고, 14일 호를 뺀 지금 2항에 남은 유일한 예외입니다.** 🔴 **제17조에 통지 기간을 새로 만들지 마십시오**(지우기로 한 것을 다른 조로 옮기는 셈입니다). 🟢 1년 시효는 약관에 없어도 상법 제147조·제121조로 그대로 1년이라 회사 지위는 안 바뀝니다.

**PR #138이 v5 로 제19조 1·2·7항을 다시 고쳤습니다**(사용자 지시 + 보고한 우려 셋을 확정 반영, 2026-09-09). **옛 2항(가액 신고제)이 1항으로 올라가고, 옛 1항 자리의 문안이 통째로 교체**됐습니다.

| | 지금 |
|---|---|
| **1항** | 가액 신고제 + 「**가액을 신고하지 아니한 경우에는 상법 등 관계 법령이 정하는 바에 따릅니다**」 |
| **2항** | 「배상책임은 **실제 운송을 수행한 차주 및 그 보험자에게 있으며**, 그럼에도 … 회사가 배상책임을 지게 되는 경우 그 배상한도는 **제1항**을 따릅니다」 + 요청 시 보험 안내 |
| **7항** | 「**제1항부터** 제6항까지는 …」 (전에는 「제2항부터」) |
| 3~6항 | **한 글자도 안 바뀌었습니다** |

🔴 **1·2항의 순서를 되돌리지 마십시오** — 2항이 「제1항을 따릅니다」로 1항을 가리키므로 되돌리면 그 참조가 **자기 자신을 가리키게** 됩니다.
🔴 **「인도할 날의 도착지 가격」을 되살리지 마십시오** — 상법 제137조 1항의 법문 그대로이지만 *"일반적이지 않고 애매하다"*(사용자)로 뺐습니다. 🟢 **빼도 회사 지위는 같습니다**(약관에 없어도 상법은 그대로 적용되고, 1항 둘째 문장이 그 자리를 가리킵니다). 🔴 **거래명세서·세금계산서 기준으로 바꾸는 안은 사용자가 안 골랐습니다** — 법정 기준을 약관으로 깎는 것이라 검수 항목이 늘어납니다.
🔴 **7항의 「제1항부터」를 「제2항부터」로 되돌리지 마십시오** — 1항이 **가액 신고 한도**(전형적인 책임제한 조항)라 고의·중과실 예외 밖에 두면 *"고의로 훼손해도 신고 가액까지만"* 으로 읽혀 **그 항만 약관규제법 제7조로 무효**가 될 수 있습니다.
🔴 **2항 앞 문장의 굵게 표시를 지우지 마십시오**(뒤의 조건절에는 붙이지 않습니다 — 2026-09-07 확정).

**PR #125의 방향** — *"우리가 배상해야 하는 내용은 배제하자 … **상세할수록 불리해지는 것 같다**"*(사용자). 회사가 스스로 배상을 약속하던 문장을 걷어냈습니다. 🟢 **지운 문장들은 상법 제115조를 옮겨 적은 것이라 빼도 법적 지위가 나빠지지 않습니다** — 두면 「약관으로 배상을 약속했다」는 근거를 상대에게 줄 뿐입니다.

| 삭제 금지 | 이유 |
|---|---|
| 제19조 **2항의 「회사가 배상책임을 지는 경우」 조건절**(PR #138 전에는 1항) | 지우면 제18조 1항(「책임은 차주에게 있다」)과 **약관 안에서 모순**이 되고, 모순은 **작성자 불이익 원칙**(약관규제법 제5조 2항)으로 고객에게 유리하게 해석됩니다 |
| 제19조 **7항**(고의·중과실 예외, 「**제1항부터** 제6항까지」) | **유일한 방어선**입니다. 지우면 면책 9개 호를 통째로 잃습니다 |
| 제19조 4항의 **「그로 인하여 발생하거나 확대된」** 인과 한정 | 없으면 유리제품·계란류에 상법 근거가 없어 무효 위험 |
| 면책 8호의 **「회사가 배차한 차주가 수행한 작업은 제외」** 단서 · 9호의 **「고객이 접수 시 알린」** 한정 | 전부 무효 방어선입니다 |
| 제18조 **2항과 3항은 쌍** | 3항의 「그 업무에 관한 주의」가 2항의 「업무」를 받습니다 — 사이에 다른 항을 끼우지 마십시오 |

🔴 **되살리기 금지** — **「1차적으로」**(66차 주석의 「반드시 남길 것」이 PR #125 지시로 뒤집혔습니다) · 제19조 2항의 「보험금 지급 여부와 관계없이」 · 과실 경합 「다만」 단서 · 랜딩 「배상해 드립니다」 · 취급 협의 항에서 뺀 두 호(살아있는 동물 · 법령상 제한 — **제12조와 정면으로 부딪힙니다**).

🔴 **「회사는 일체의 책임을 지지 않습니다」류로 더 밀지 마십시오** — 지금 형태(**책임이 누구에게 있는지를 적는 진술**)는 무효 위험이 낮지만, 면책 선언은 **약관규제법 제7조 2호**에 정면으로 걸려 **그 항이 통째로 무효가 되고 지금 얻은 문장까지 같이 잃습니다.** 지금이 한계선입니다.

⚠️ **항 번호가 또 밀렸습니다(세 번째)** — 제19조 **8항 → 7항**(1항 삭제) · 제18조 **3항→1항 · 1항→2항 · 2항→3항** · **PR #138에 제19조 1항과 2항이 서로 자리를 바꿨습니다.** 옛 기록의 「제19조 8항」·「제19조 1항의 조건절」·「제18조 3항 마지막 문장」 표기는 전부 낡았습니다. 🔴 **번호가 아니라 내용으로 찾으십시오.** 🟢 제4조·제18조·제19조를 본문에서 참조하는 곳은 **0건**이라 깨진 상호참조는 없습니다.

⚠️ **영구크린 등 중개 플랫폼 약관을 본떴지만 사업 구조가 다릅니다** — 그쪽은 순수 중개이고 우리는 **운송주선사업자 허가 + 확정 운임**이라 같은 문구여도 효력 판단이 다를 수 있습니다. 🔴 **사용자가 그 위험을 알고 선택했습니다**(*"법적소송으로 갔을 때 따질지언정"*) — **「무효 위험이 있다」를 근거로 되돌리지 마십시오.**

🔴 **변호사 검수 1순위 — 확정운임 → 상법 제119조 2항 운송인 의제 가능성.** 위캐리는 화주에게 확정 운임을 청구하므로 운송인으로 의제될 수 있고, 그러면 차주가 이행보조자가 됩니다. **그래서 「회사는 책임이 없다」로 더 밀지 않았습니다** — 강행법규에 밀려 무효가 되면 한도·면책까지 같이 잃습니다.

⚠️ **특별약관을 하나도 가입하지 않아 제외 화물 6종은 고지해도 보험금이 안 나옵니다** — 원고류 · 귀금속류 · **시계·담배·모피류** · **유리제품** · **계란류** · **살아있는 동물**. 「유리제품」 정의가 넓어 **유리병에 담긴 음료·소스·주류**가 그대로 들어갑니다.
🔴 **화물이 없어진 사고는 즉시 경찰에 도난신고를 접수해야 보험이 열립니다** — 약관이 그 외의 망실을 전부 「분실」로 간주하고 면책하기 때문입니다. **이 한 줄이 1억과 0을 가릅니다.**
🔴 **그래서 공개 화면에 「분실」이라는 낱말을 쓰지 마십시오**(70차 ②) — 보험이 안 나오는 바로 그 경우를 약속하는 문장이 됩니다. 「없어지거나」로 씁니다.
⚠️ **자기부담금 20만원은 회사가 흡수합니다** — 파손 문의에 「보험으로 처리된다」고 답하지 마십시오.

### 5-6. 랜딩 WHY 카드 (70차)

🔴 **`desc` = 접힘에 보이는 짧은 소개 / `detail` = 펼치면 그것을 이어서 설명.** 되풀이하지 마십시오 — 다섯 장 중 넷이 `detail` 첫 줄에서 `desc`를 그대로 되풀이하고 있어서 "단계가 어색하다"는 지적을 받았습니다. **05(보험)가 본보기**입니다.

🔴 **펼침은 카드가 아니라 이어지는 본문입니다** — 흰 배경·테두리·radius·안쪽여백 0이고 글자 규격이 `desc`와 **완전히 동일**합니다. 2026-09-02의 「부가 설명 카드」 확정을 뒤집은 것이니 되살리지 마십시오.
🔴 **화살표는 제목 옆이 아니라 설명글 바로 아래**입니다. `<summary>` 밖으로 빼면 클릭이 안 먹습니다.
🔴 **보험 카드는 맨 뒤여야 합니다** — 미가입이면 배열에서 빠지는데, 뒤에 있어야 남는 번호가 01~04로 이어집니다.

### 5-7. 모바일 랜딩 (70차)

🔴 **모바일 여백 축소는 전부 `@media (max-width: 700px)` 안입니다 — 데스크탑 변경 0.** 섹션 위아래를 40~45% 줄여 전체 높이가 11,128 → 10,312px이 됐습니다. **여기서 더 줄이지 마십시오**(사용자 단서: 「너무 타이트하면 안 된다」).

🔴 **차량 목록은 모바일에서 자동으로 흐릅니다**(32px/s, `useAutoMarquee`).
- 목록을 **두 벌 이어 붙여** 되감기는 순간이 안 보이게 합니다. 뒤쪽 12장은 데스크탑에서 `display:none`
- 🔴 **정수 픽셀은 `scrollLeft`, 소수 픽셀은 안쪽 줄의 `transform`이 나눠 싣습니다** — 브라우저가 `scrollLeft`를 정수로 반올림해서 덜덜거리기 때문이고, **둘 중 하나만 쓰면 그 증상이 그대로 돌아옵니다**
- 🔴 `.landing-vehicle-track`은 데스크탑에서 `display: contents` — 지우면 그리드가 깨집니다
- 🔴 **스크롤 스냅을 되살리지 마십시오** — 자동 이동을 죽입니다

### 5-8. 🔴 SEO 메타 (PR #118)

**메타는 `lib/pageMetadata.ts`의 `buildPageMetadata` 한 곳에서만 만듭니다.** 화면은 제목·설명·경로 셋만 넘기고, 그 함수가 같은 값을 `<title>`·`description`·`og:*`·`twitter:*`·`canonical`에 넣습니다. **`og:title`·`og:description`을 따로 적지 마십시오** — 두 벌이 되면 조용히 갈립니다.

🔴 **description은 80자 이내입니다.** 네이버 서치어드바이저 「간단체크」 권고이고, 넘으면 **「사이트 설명」·「Open Graph 설명」 두 항목**에 ⚠️ 가 뜹니다. 두 항목이 같은 값이라 **한쪽만 고칠 수 없고, 한 번 줄이면 둘 다 사라집니다.** 넘으면 빌드 로그에 `[metadata]` 경고가 남습니다(빌드를 실패시키지는 않습니다).

🔴 **`lib/structuredData.ts` — Organization JSON-LD를 넣은 이유는 SEO 일반이 아니라 동명 회사 구별입니다.** 네이버에 「위캐리 운송」을 검색하면 **물류업에만 「위캐리」가 셋**입니다((주)위캐리로지스 부천 · (주)위캐리 제주 · (주)위캐리컴퍼니 서울). **사업자등록번호와 운송주선사업 허가번호가 갈라주는 유일한 식별자**라 `identifier` 배열로 내보냅니다 — **그 둘을 지우면 이 파일의 존재 이유가 없어집니다.**

- 🔴 **`name`은 「위캐리 운송」 — 「WeCarry」를 넣지 마십시오.** 경쟁사 블로그가 그 영문 표기를 쓰고 도메인도 `wecarry_logis`로 겹쳐, 브랜드명으로 영문을 쓰면 구별이 오히려 흐려집니다. ⚠️ **화면의 시각 브랜딩이 영문인 것은 별개이고 26차 확정입니다**
- 🔴 **`LocalBusiness`로 바꾸지 마십시오** — 손님이 찾아오는 사업장을 뜻합니다. 주선업이고 스마트플레이스를 쓰지 않기로 한 결정과도 어긋납니다
- 🔴 **값을 그 파일에 문자열로 적지 마십시오** — 전부 기존 상수 참조입니다
- 🔴 **루트 layout에 둡니다** — 공개 화면마다 붙이면 새 경로에서 빠뜨립니다
- ⚠️ **`sameAs`는 비어 있습니다** — 네이버 블로그를 개설하면 그 주소를 넣을 자리입니다
- ⚠️ **랜딩 description에서 「정식 허가업체(제180254호)」를 뺐습니다**(80자 제약). 허가번호는 JSON-LD·푸터에 남아 있고, 되살리려면 다른 문장을 그만큼 줄여야 합니다

⏸️ **콘텐츠 페이지 확충 · IndexNow · 파비콘은 미착수입니다.** 🔴 **`keywords` 메타는 일부러 넣지 않았습니다** — 구글은 무시하고 네이버도 랭킹 요소로 밝힌 바 없으며 과하면 스팸 신호가 됩니다.

### 5-9. 🔴 상호 표기와 랜딩 이미지 (PR #119)

**상호는 문자열이 아니라 `components/CompanyNameMark.tsx`로 그립니다** — `(주)디자인에그 │ 위캐리 운송`. 가운데는 **슬래시 글자가 아니라 세로 구분선**입니다(54차 확정). 쓰는 곳은 **여덟**입니다(2026-09-09 전수 실측) — 견적서 상세 · print 2종 · 사업자정보 모달 · 랜딩 푸터 상호 줄 · **카피라이트 3곳**(랜딩 푸터 · 공용 푸터 · 운송관리 로그인).

- 🔴 **기본값(`#c4c4c4` · 여백 9px)을 바꾸지 마십시오** — 견적서 4곳이 그 경로를 씁니다. 견적서(14px 굵은 글자·흰 배경) 기준으로 맞춘 값입니다
- 🔴 카피라이트는 12.5px 흐린 글자라 그대로 쓰면 **구분선이 글자보다 진해 보입니다** → 그 세 곳만 `separatorColor="currentColor"` · `separatorGap={7}`로 넘깁니다. **`currentColor`인 것이 요점**입니다(세 곳의 글자색이 서로 다르고, 나중에 푸터 색을 바꿔도 따라옵니다)
- 🔴 **`COMPANY_LEGAL_NAME`(「주식회사 디자인에그」)을 지우지 마십시오** — 사업자 표시사항 「상호」 행 · 브랜드 고지 문장 · 법적 문서가 계속 씁니다. **카피라이트 줄에서만 빠졌습니다**
- 🔴 **「위캐리 운송」을 붙여 쓰지 마십시오** — 실행계획 §0-5의 전략 키워드가 네 글자입니다

**랜딩 이미지 — 두 가지를 되돌리지 마십시오.**

- 🔴 **서비스 카드 첫 장만 `loading="eager"`입니다**(`app/page.tsx`, `fetchPriority="low"` 동반). 모바일에서 그 섹션이 y=2161px인데 브라우저 지연로딩 선행 거리가 약 1250px이라 **보이는 순간에야 받기 시작**했습니다 — 4G 실측 **회색 상자 371ms · 3/3회**. eager로 바꿔 **0ms · 0/3회**가 됐습니다. 🔴 **둘째 카드부터는 `lazy` 유지 — 넷 다 eager로 만들지 마십시오**(첫 화면이 느려집니다)
- 🔴 **히어로·CTA·service-02·03은 품질 90 재인코딩본입니다**(크기·구도 동일). 랜딩 이미지가 **10.9MB → 5.5MB**가 됐습니다. 표시 크기 기준 원본 대비 **평균 차이 0.1~0.7%**. **원본이 필요하면 61차 커밋에서 꺼내십시오** — 다시 인코딩하면 세대 손실만 쌓입니다
- ⚠️ **안 건드린 것**: `service-00`·`service-01`(이득이 19K·11K뿐) · `service-04-moving`(랜딩에서 안 쓰고 `wecarry24.co.kr`이 가져다 씁니다) · TMS PNG 3.6MB(화면 캡처라 PNG가 맞고 WebP 전환은 별도 판단)

### 5-10. 🔴 랜딩 모션 (PR #123)

**스코프는 `.landing-page`입니다** — `.landing-root`라는 클래스는 이 저장소에 없습니다(지시서가 그렇게 적었지만 틀렸습니다). 토큰(`--mo-*` 11개)도 그 스코프 안에만 있습니다. 🔴 **`globals.css`에 두지 마십시오** — 관리자 31화면이 공유합니다(이번 차수 변경 **0**).

**리빌은 클래스 기반입니다** — 마크업이 `.landing-reveal`을 들고 시작하고(CSS가 처음부터 숨김) 훅이 보이는 순간 `.is-in`만 붙입니다. 🔴 **인라인 스타일로 되돌리지 마십시오** — 61차 방식이 그랬는데 (a) 인라인이 스타일시트를 이겨 토큰이 안 먹고 (b) stagger를 JS로 계산해야 하고 (c) `<noscript>`가 닿지 않고 (d) **첫 페인트에 내용이 보였다가 JS가 숨겨 화면이 깜빡였습니다.**

🟢 **그래서 리빌 대상을 늘리는 데 코드가 필요 없습니다 — 클래스만 붙이면 됩니다.**

**재생 규칙은 C안입니다**(사용자 확정 2026-09-07). 🔴 **지시서 원칙 「한 번만 재생」은 이 지시로 뒤집혔습니다 — 그 원칙을 근거로 되돌리지 마십시오.**

```
enterObs  좁은 상자(화면 아래 8% 제외)에 들어오면  .is-in 을 붙인다
farObs    위아래로 한 화면(100%) 넓힌 상자에서 벗어나면  .is-in 을 뗀다
```

두 조건은 동시에 참이 될 수 없습니다(좁은 상자 안이면 넓힌 상자 안이기도 하니까요). 🔴 **`farObs`의 `rootMargin`을 줄이지 마십시오** — 작아질수록 리셋 지점이 화면에 가까워지고, 0에 가까워지면 **읽던 글자가 눈앞에서 사라집니다.**

**되돌리면 안 되는 것**

- 🔴 **JS 실패 대비가 두 겹입니다** — `<noscript>` 스타일(JS 자체가 꺼진 경우)과 훅의 `try/catch`(`IntersectionObserver`가 없거나 던지는 경우). **막는 경우가 서로 달라서**, 하나를 「중복」으로 지우면 다른 경우에 랜딩이 백지가 됩니다
- 🔴 **차량 트랙(`.landing-vehicle-track`) 안쪽에 리빌·`--i`·`transition`을 걸지 마십시오** — 70차 자동 슬라이드가 매 프레임 **소수 `transform`**을 씁니다. 덮어쓰면 「덜덜거림」이 그대로 돌아옵니다. 리빌은 **바깥 상자에만** 있습니다
- 🔴 **아코디언 래퍼는 두 겹입니다**(`.landing-acc-body` 그리드 > `.landing-acc-clip` overflow hidden) — 안쪽 clip이 없으면 `.landing-reason-detail`의 `margin`이 클리핑 밖에 남아 **닫힌 상태에도 32px 틈**이 생깁니다
- 🔴 **`--mo-rise` 26px은 지시서 상한(20px)을 넘긴 의도한 값입니다**(리뷰 1라운드 — 「모션이 너무 티가 나지 않는다」). 지시서의 「눈치채지 못할 정도」를 근거로 14px로 되돌리지 마십시오. **다만 30px은 넘기지 마십시오**
- 🔴 **`--i`는 5를 넘기지 마십시오** — 마지막 것이 나타나기까지 너무 오래 걸립니다
- 🔴 **reduced-motion 블록은 `app/landing.css` 맨 끝이어야 합니다**(원래 파일 중간에 있었습니다)

⚠️ **모션은 랜딩 한정입니다** — 폼 3화면·법적 문서 3종에는 리빌이 없습니다(토큰 `--mo-*`도 `.landing-page` 스코프 안에만 있습니다). 넣으려면 스코프부터 정해야 합니다.

### 5-11. 🔴 정기계약 (33차 PR #139 · 후속 PR #140)

**`companies` 의 별도 컬럼 5개입니다** — `is_recurring_contract` · `recurring_contract_started_on` · `recurring_contract_ended_on` · `recurring_contract_frequency` · `recurring_contract_note`. 🔴 **`companies.status`(거래 상태)에 「정기계약」을 값으로 넣지 마십시오 — 축이 다릅니다.** 상태는 「지금 어느 단계인가」이고 정기계약은 「어떤 관계인가」라, 같은 칸에 넣으면 **「정기계약 화주인데 지금 새 견적을 협의 중」**을 표현할 수 없습니다. 🟢 **마이그레이션 안의 단언이 이것을 막습니다**(`status = '정기계약'` 행이 있으면 실패).

🔴 **기존 `repeat_customer`(재거래 여부)를 재사용하지 마십시오** — 정산 저장이 `총 오더수 > 1` 이면 **자동으로 덮어쓰는 실적 파생값**입니다. 계약 여부를 거기 담으면 담당자가 켠 값이 다음 정산에서 조용히 사라집니다. **두 항목이 화면에 나란히 보이는 것이 정상입니다.**

**종료일 규칙** — 🔴 **종료일이 지난 계약에는 배지가 붙지 않습니다**(체크는 기록으로 남고 화주 상세에만 「종료됨」이 뜹니다). 🔴 **종료일 당일은 유효**입니다(날짜만 비교 — 시각이 섞이면 종료일 당일이 빠집니다). **「체크가 켜져 있는데 왜 배지가 없지」는 의도된 동작입니다.**

**정의처는 하나입니다** — `lib/companyFields.ts` 의 `isRecurringContractActive()` 와 `RECURRING_CONTRACT_BADGE_LABEL`. 🔴 **판정을 SQL 로 다시 쓰거나 화면에 따로 적지 마십시오** — 종료일 규칙이 두 벌이 되어 「배지는 없는데 필터에는 걸리는」 건이 생깁니다.

**배지가 붙는 곳은 다섯입니다** — 화주 관리 · 활성 화주(CRM) · 견적 · 오더 · 배차 목록. 자리는 **회사명 위 줄**입니다(`block` prop). 🔴 **호출부에서 `<div>` 로 감싸 올리지 마십시오** — 배지가 안 붙는 화주는 컴포넌트가 `null` 을 돌려주는데 바깥 `<div>` 는 그대로 남아 **빈 줄로 칸이 벌어집니다**(그래서 감싸는 일까지 컴포넌트가 합니다).

🔴 **`<select>` 의 `<option>` 안에는 붙일 수 없습니다** — 브라우저가 내용을 평문으로 그립니다(배차 등록의 오더 선택 드롭다운이 그 자리입니다).

🔴 **화주에게는 보이지 않습니다** — 화주포털·견적서·엑셀 **0줄**. 배차 담당자가 우선순위를 판단하는 내부 정보입니다. `RecurringContractBadge` 를 `app/customer/**` 에 쓰지 마십시오.

### 5-12. 🔴 선착불 정산 모델 (35차 PR #146)

🔴 **수금방식이 「받을 돈」과 「줄 돈」을 정합니다.** 그전에는 수금방식과 무관하게 화주 청구액을 받을 돈으로 잡아서, **위캐리를 거치지도 않는 운임이 미수금**으로 잡혀 있었습니다.

```
[선착불  collection_method = driver_direct]
   받을 돈   = **주선수수료**      🔴 화주 청구액이 아닙니다 — 되돌리지 마십시오
   줄 돈     = **0**
   운송완료  = 입금·지급 **완료**
   세금계산서  화주에게 ❌  /  차주에게 **수수료분** ⭕

[주선사정산  collection_method = broker]
   지금과 같습니다 (회귀 0)
```

🔴 **화주 기준 미수금은 선착불이면 0입니다**(36차 PR 2 PR #151). 운임은 위캐리를 거치지 않고, **주선수수료는 차주가 냅니다.** 🔴 **`invoices.receivable_amount` 를 그대로 더하지 마십시오** — 그 칸은 「위캐리가 **누구에게든** 받을 돈」이라 선착불이면 차주가 낼 수수료가 들어 있습니다. 정의처는 **`lib/receivableCalc.ts`** 하나입니다.

🔴 **미수금은 저장값이 아니라 「표시 시점 계산」입니다.** `companies.outstanding_amount` 는 저장 시점 스냅샷이고 다시 쓰이는 경로가 **둘뿐**이라(새 정산 등록 / 입금완료 토글), **선착불 화주는 입금완료를 누를 일이 없어** 한 번 잘못 적힌 값이 영영 안 고쳐집니다. 그래서 **활성 화주 목록과 화주 상세 「실적」이 매번 다시 셉니다**(35차 마진과 같은 수법 — *"옛 건도 자동으로 맞는다"*). 🔴 **`won(c.outstanding_amount)` 로 되돌리지 마십시오** · 🔴 **옛 값을 SQL 로 때우지 마십시오**(고칠 것은 값이 아니라 출처입니다).

🔴 **받을 수수료는 `brokerage_fee_paid` 로 따로 남습니다 — 「입금 완료」와 한 칸으로 뭉개지 마십시오.** 선착불 정산 건의 **유일한 미수금**입니다.

🔴 **화면에서 화주 세금계산서 칸을 감추지 않습니다** — 자리는 남기고 「해당 없음 — 선착불은 화주가 차주에게 직접 지급합니다」로 **문구를 바꿔 그립니다.** 차주 수수료분은 선착불 블록 안의 **다른 칸**이고 컬럼도 다릅니다(`driver_tax_invoice_issued`/`_date` vs 화주쪽 `tax_invoice_issued`). 🔴 **「빈 항목이니 살리자」로 화주 칸을 선착불에 되돌리지 마십시오.**

⚠️ **주선수수료는 부가세 포함가로 기입합니다** — 🔴 **코드가 강제하지 못하는 운영 규칙**이라 배차 담당자에게 전달해야 합니다(§7 「사용자가 코드 밖에서 할 일」).

🔴 **마진의 정의처는 `lib/marginCalc.ts` 하나입니다.** 정산 화면 **한 곳에만 공식이 셋**이었고(등록 미리보기 / 저장값 / 대시보드) 셋 다 다른 숫자를 냈습니다.
- 🔴 **저장값 `commission_total` 에서 읽지 마십시오** — 표시 시점 계산이라 **옛 건도 자동으로 맞습니다.**
- 🔴 `dispatches.margin`(DB 생성 컬럼, 부가세 환산 없음)·`settlementCalc` 의 `realMargin` 과 **섞지 마십시오.**

🔴 **부가세는 건별로 확정합니다.** 합계에 ×1.1 을 하면 **25,000원 3건이 74,999원**이 됩니다(세금계산서 단위로 원 단위가 확정되므로 **건별 부가세의 합**이 맞습니다). 🔴 **`lib/vat.ts` 의 `splitVat` 에서 `inclusive` 를 `supply × 1.1` 로 되돌리지 마십시오** — **11,000원마다 1원**이 어긋나고(6,000 → 6,001), 이것이 **24시콜 세금계산서 대조가 갈리던 원인**입니다.

🔴 **A-7 「배차 기준으로 금액 다시 맞추기」는 원칙 47번 위반이 아닙니다** — 틀리게 얼려진 기준 금액을 바로잡는 경로입니다. 추가비는 `created_at <= invoice.created_at` 인 것만 넣습니다(빼면 **두 번 청구**). 🔴 **`invoice.created_at` 을 건드리지 마십시오** — 그 필터의 기준선입니다.

🔴 **「주선수수료 면제」 체크는 없앴지만 게이트는 0원 확인 창으로 남겼습니다 — 지우지 마십시오.** 지우면 「정말 0원」과 「아직 안 적었다」를 가릴 장치가 없어져 입력 누락 건이 **미수금 없이 조용히 확정**됩니다.

⚠️ **이 절은 2026-09-14 문서 정합 3차에 신설됐습니다** — 그전에는 이 확정이 **§6 진행 현황 표의 한 칸에만** 있었습니다. §6은 「무엇을 했나」이고 §5는 「무엇이 확정인가」라, 새 세션이 §5를 다 읽어도 알 수 없었습니다. 🔴 **앞으로 확정이 생기면 §6 이력 한 줄과 §5 절 하나를 같이 만드십시오.**

### 5-13. 🔴 이용가이드 — 한 정의처, 두 화면 (PR #149 · PR #175 에 크게 늘었습니다)

🔴 **화면은 둘, 글은 한 곳입니다 — `lib/guideContent.ts`**

```
  /guide           공개 · 한 장 스크롤 · 처음 오는 사람이 순서대로 읽는 자리   public + both  12항목
  /customer/guide  포털 · 항목 선택   · 이미 쓰는 사람이 필요한 것만 찾는 자리  portal + both  18항목
```

🚨 **화면을 고치는 차수는 이 글도 같이 보십시오.** PR #175 가 재작성할 때 **PR #149 이후
아홉 차수가 하나도 반영돼 있지 않았고**, 발주 항목이 **「상담 중」**이라 적고 있었는데
PR #164 가 그 글자를 **「확인중」**으로 바꾼 뒤였습니다. 메뉴 표도 8종에 멈춰 있었습니다.
**가이드는 화면을 고칠 때 저절로 따라오지 않습니다 — 안 보면 조용히 거짓말이 됩니다.**

🔴 **블록 타입은 아홉입니다** — `p`·`ol`·`note`(`plain`/`tone`)·`dl`·`table`·`faq`·`pills`·
`mock`·`tel` 에 PR #175 가 **`steps`**(번호 원 + 제목 + 설명) · **`cards`**(아이콘 타일) ·
**`figure`**(캡처 + 캡션)를 더했습니다. 🔴 **새 항목을 더할 때는 먼저 있는 블록으로 되는지부터
보십시오** — 블록 타입을 더하면 **렌더러 둘 + CSS** 를 함께 고쳐야 하고, 그러면 「정의처만
고치면 된다」는 성질이 깨집니다.

🔴 **정의처를 쓸 때 걸리는 함정 넷**(전부 구조 단언이 잡습니다)
```
  ① table 은 묶음(group)에 든 항목에 넣지 말 것 — 공개 화면은 표를 **묶음 밖**에서만
     그려서 **조용히 사라집니다**(항목당 하나까지)
  ② table 행 · faq 답 · steps 제목 · cards 라벨 · figure 캡션에 `**` 강조 금지 —
     그 자리는 두 화면 모두 **원문 그대로** 그려 **별표가 글자로** 보입니다
  ③ titleSub 은 묶음에 든 항목에만 — 밖에 달면 공개 화면에서 조용히 사라집니다
  ④ 항목 id 는 URL 입니다
```

🔴 **아이콘은 `components/GuideIcon.tsx` 20종(인라인 SVG · 이미지 파일 0건)이고, 이름
목록(`GuideIconName`)은 `lib/guideContent.ts` 에 있습니다.** 🔴 **방향을 뒤집지 마십시오** —
컴포넌트 쪽에 두면 정의처가 화면 부품을 import 하게 됩니다. 🔴 **`PortalIcon` 과 합치지
마십시오**(그쪽은 포털 메뉴 전용이고 10종뿐입니다).

🚨 **캡처 6장은 실계정이 아닙니다 — PR #148 의 「캡처 0건이 의도」를 뒤집되 그 이유는
살렸습니다.** PR #148 이 캡처를 안 넣은 이유 셋 중 **셋째(실계정이면 화주 상호·연락처가
public 저장소에 박힌다)**를 없애는 방식으로 풀었습니다 — **`scripts/guide-shots.mjs` 가
Supabase 응답을 통째로 가로채 가짜 값을 물려 찍습니다**(상호 「시험상사」 · 연락처
`010-0000-0000`). 🔴 **실계정으로 다시 찍지 마십시오 — git 이력에서 지워지지 않습니다.**
🔴 **DSF 2 로 찍어 절반 크기로 저장**하고 `width`·`height` 를 **구조 단언이 파일과 대조**합니다.
⚠️ 알림 버튼이 나오게 하려면 **`NEXT_PUBLIC_VAPID_PUBLIC_KEY` 를 주고 dev 를 띄워야 합니다.**
규칙은 `public/guide/README.md` 입니다.

🔴 **CSS 는 공개(`.guide-*` + 전역 토큰)와 포털(`.pv2-guide-*` + `--pv2-*`) 두 벌을 일부러
따로 적었습니다 — 한쪽으로 몰지 마십시오.** 포털 토큰은 `.portal-v2` 안에만 있어 공개
화면에서는 **색이 아예 안 나옵니다**(PR #145 실측).

⚠️ **PR #175 에 잠깐 블록마다 `only?: "public" | "portal"` 을 두었다가 같은 날 지웠습니다**
(고객센터 시간이 하루 동안 공개 18시 / 포털 19시로 갈렸던 그때뿐입니다).
🔴 **다시 만들지 마십시오** — 화면을 가르는 일은 항목의 `scope` 가 합니다.

🔴 **본문을 화면에 따로 적지 마십시오 — 그러면 한쪽만 낡습니다**(PR #148 이 「페이지는 하나」로 둔 이유가 바로 그 걱정이었고, 없앤 것이 아니라 이렇게 푼 것입니다). 항목마다 `scope`(`public`/`portal`/`both`)가 어느 화면에 나갈지 정합니다.

🔴 **정의처에 JSX·className 을 넣지 마십시오** — 넣는 순간 공개 화면 CSS 와 `.pv2-*`(포털 전용 스코프)가 한 파일에서 섞이고 두 화면이 같은 마크업을 강제로 쓰게 됩니다. **그리는 일은 각 화면이 합니다.**

🔴 **블록 타입이 렌더러가 아니라 정의처에 있습니다.** `lib/legal/*.ts` 는 **렌더러가 하나**(`components/LegalDoc.tsx` 를 페이지와 모달이 공유)라 타입을 그 컴포넌트에 둘 수 있었지만, 이용가이드는 **렌더러가 둘이고 CSS 스코프가 서로 다릅니다.** 타입을 한쪽에 두면 반대쪽 화면이 그 컴포넌트를 import 하게 됩니다. 🔴 **「법적 문서처럼 렌더러로 옮기자」로 되돌리지 마십시오.**

🔴 **`pills` 블록은 글자를 담지 않습니다** — `{ type:"pills", set:"quote"|"dispatch" }` 뿐이고 라벨·색은 **화면이** `lib/quoteStatusLabels.ts`·`lib/dispatchStage.ts` 에서 가져옵니다. 정의처에 적으면 **DB 값(`보류`·`실패`)이 그 파일에 박히고** 라벨이 바뀔 때 안내만 조용히 낡습니다.

🔴 **항목 `id` 는 URL 입니다**(`/customer/guide?topic=<id>`) — 바꾸면 밖에서 보낸 링크가 깨집니다. 영문 소문자·하이픈만 씁니다.

🔴 **항목 선택은 `?topic=` 쿼리스트링이고 라우트는 하나입니다 — 항목마다 라우트를 만들지 마십시오**(프리렌더가 항목 수만큼 늘고 항목을 더할 때마다 파일이 늡니다). 🔴 **항목 이동은 `<Link>`(history push)입니다** — `replace` 로 바꾸면 뒤로가기가 목록이 아니라 **포털 홈으로** 튑니다.

🔴 **모바일은 목록과 본문을 같이 그리지 않습니다** — 합치면 없애려던 긴 스크롤로 돌아갑니다. 🔴 **가르는 일은 CSS 가 합니다**(`.pv2-guide-open`, **≤700px** — 포털이 이미 쓰는 분기점이라 새 값을 만들지 않았습니다). 화면 폭을 JS 로 재면 첫 그림에서 잘못된 쪽이 한 번 번쩍입니다. 🔴 **알 수 없는 `topic` 에 빈 화면을 내지 마십시오** — 첫 항목(데스크탑)·목록(모바일)으로 떨어뜨립니다.

🟢 **돌아가기는 상단 「←」(PR #129)가 이미 맡습니다** — 이 화면에 그려지고 `router.back()` 입니다. 🔴 **「목록으로」 버튼을 따로 만들지 마십시오**(같은 줄에 뒤로가기가 둘이 됩니다).

🔴 **포털 홈에는 링크를 두지 않습니다 — 일부러 뺐습니다.** 공개 `/guide` 로 보내면 포털 셸 밖으로 나가 **로고가 랜딩으로** 갑니다. 포털 안에서는 메뉴의 「이용가이드」가 그 일을 합니다. 🟢 **포털 로그인 화면의 링크는 남깁니다** — 로그인 전이라 셸 밖인 것이 자연스럽고, 계정을 막 받은 사람이 가장 안내가 필요합니다.

🔴 **항목 제목은 `/guide` 의 h2·h3 문자열과 한 글자도 다르지 않습니다**(「견적 상태」·「배차·운송 상태」·「금액 표시」). 지시서 제안(「견적 확인과 승인」 등)을 쓰면 그 화면의 섹션 제목이 바뀝니다. ⚠️ **포털 전용 `invoice` 항목은 만들지 않았습니다** — `amount`(금액 표시)와 메뉴 표의 「정산·결제내역」 행이 답합니다. 사용자가 「정산을 찾는 사람에게 읽히는가」를 보고 정합니다.

⚠️ **`/customer/guide` 는 `noindex` 이고 `sitemap.xml` 에 넣지 않습니다** — `app/customer/layout.tsx` 를 상속하고 `app/sitemap.ts` 가 `/customer` 를 걸러냅니다. `sitemap.xml` 은 **7 URL 그대로**입니다.

---

### 5-14. 🔴 화주 거래조건 (36차 A·B장 PR #150)

🔴 **화주 상세의 「거래 조건」은 이 모양입니다** — 정의처는 `lib/companyFields.ts` 하나입니다(33차).

```
  청구주기      건별 / 월정산                      ← 오더·발주요청의 **기본값(제안)**
  정산 마감     🔴 **읽기 전용 안내 — 입력칸이 아닙니다**
                  월정산 → 「월말 기준 (모든 화주 공통)」
                  건별   → 「해당 없음 — 운송 완료 시 즉시지급」
  결제일        익월 말일 · 익월 1일 … 익월 31일 · 협의   ← **월정산일 때만**
  세금계산서    ⭕ 정발행 / 역발행 / 발행 안 함
  특이사항      자유 서술 (구 `payment_terms` 를 이름만 바꿔 계속 씁니다)
```

🟢 **세금계산서 발행 방식은 36차 PR 2(PR #151)에서 복구됐습니다.** A·B장 merge 시점에는 컬럼·상수만 있고 **화면 입력칸이 빠져 있었는데**, 리뷰 2라운드에 `credit_limit`(여신 한도)을 지우면서 **배열에서 바로 아래 붙어 있던 것이 같이** 지워진 것이었습니다. 🔴 **교훈 — 지운 뒤에는 「무엇을 지웠나」가 아니라 「무엇이 남았나」를 세십시오**(`grep -c 'key: "..."'` 한 줄이면 잡혔습니다). 인접한 항목이 같이 지워지는 것은 diff 를 읽어도 눈에 잘 안 들어옵니다.

🔴 **전부 내부 전용입니다** — 화주포털·견적서·엑셀 **0줄**(33차 정기계약 배지와 같은 취급). 포털은 청구주기·결제일을 **기본값으로만** 읽고 **화면에 글자로 그리지 않습니다.** 🔴 `app/customer/**` 에서 이 값들을 읽지 마십시오.

🔴 **기본값이지 강제가 아닙니다.** 오더·발주요청을 만들 때 복사해 채우고, 그 뒤로는 **그 건의 값이 이깁니다.** 강제로 만들면 화주 설정을 바꾸는 순간 **진행 중인 건의 정산방식이 조용히 바뀝니다.** 🔴 **역방향 동기화 금지**(원칙 45번) — 오더 → 화주로 되돌리지 마십시오.

🔴 **정산 마감일은 전 화주 월말입니다 — 입력칸을 되살리지 마십시오.**

```
  건별     정산 마감 개념이 없다  →  즉시지급
  월정산   항상 월말             →  결제일 = 정산 익월 며칠
```

사용자 확정입니다 — *"업체마다 정산마감일이 상이하면 복잡해질것 같다"* · *"월정산을 했을때 모두 월말기준으로 바꾸자. 정산마감은 월말기준이라는 표시는 두자."* 🔴 **화면만 숨기면 다음 세션이 되살립니다** — `buildCompanyPayload` 가 이 키를 payload 에서 **아예 지웁니다.** 🔴 **「월말 기준」 표시는 사용자가 남기라고 한 것입니다**(발주요청 화면에도 같은 문장이 있습니다).

🔴 **`billing_cutoff_day` 컬럼은 남깁니다 — `null` 이 곧 「월말」이라는 뜻입니다.** 월정산 묶음 계산이 「비어 있으면 달력월(1일~말일)」이라 컬럼을 지우면 그 계산이 통째로 깨집니다. 🔴 **값을 지우는 마이그레이션이라 `_bak_billing_cutoff_day_20260914` 에 백업했고 백업이 비면 멈추는 단언을 걸었습니다 — 그 표를 먼저 지우지 마십시오.**

🟢 **과거 월정산 묶음은 안 바뀝니다 — 돌리기 전에 코드로 확인했습니다.** `MonthlyBillingBatchPanel` 이 ① 기존 묶음을 **`period_end` 가 그 달력월 안에 있는지**로 찾고(마감일로 역산하지 않습니다) ② 찾은 묶음은 **저장된 `period_start`/`period_end` 를 그대로** 씁니다. 마감일은 **새 묶음의 기간 계산에만** 쓰입니다. 🔴 **이 성질이 깨지면(= 묶음을 마감일로 역산해 찾도록 되돌리면) 이 마이그레이션이 소급 사고가 됩니다** — 원칙 46번이 그래서 있습니다.

🔴 **결제일은 화면 값과 DB 값이 1:1 이 아닙니다.** 「협의」를 고르면 값 칸이 아니라 **`payment_due_basis`** 가 바뀝니다 — 변환은 **`paymentDueToForm`/`paymentDueFromForm` 두 함수만** 합니다. 🔴 **`payment_due_basis` 와 `PAYMENT_DUE_BASIS_OPTIONS` 를 지우지 마십시오**(화면에서 안 고를 뿐 저장은 계속 하고, 기준이 여럿이 되면 **화면만** 되살리면 됩니다). 🔴 **건별로 바꾸면 값을 비웁니다** — 숨기기만 하면 월정산으로 되돌렸을 때 **옛 날짜가 되살아납니다.**

🔴 **여신 한도는 화면에서 뺐습니다**(사용자 *"여신한도는 지금 뺀다"*) — **컬럼 `credit_limit` 은 남겼습니다**(채워진 행 0건이라 남기는 비용이 없고, 되살릴 때 마이그레이션이 또 필요해집니다). ⚠️ **착수 전 입력에 ⭕ 로 적혀 있다고 되살리지 마십시오.**

🔴 **포털의 「요청」은 확정이 아닙니다.** `portal_order_requests.requested_billing_cycle`·`requested_payment_due_value` — **`requested_` 를 떼지 마십시오**(27차 확정: *「월정산은 화주별 계약이라 담당자가 정한다」*). 🔴 **화면이 칸을 숨겨도 서버가 다시 봅니다**(원칙 25번) — 잘못된 값은 400 이 아니라 **`null` 로 떨굽니다**(발주 자체는 통과시킵니다). 🔴 **계약이 「협의」인 화주에게는 결제일 칸을 그리지 않습니다.**

🔴 **「계약과 다름」 배지는 알리기만 하고 막지 않습니다** — 막으면 계약과 다르게 합의한 건을 아예 못 넣습니다. 🟢 `contractBillingCycle` prop 을 안 넘기는 화면(배차·정산·사유 모달)은 **한 글자도 안 바뀝니다.**

🔴 **기존 화주 539건이 전부 비어 있는 것이 맞습니다** — 🔴 **일괄로 「건별」을 채우지 마십시오**(「안 정했다」와 「건별로 정했다」가 구분되지 않습니다).

### 5-15. 🔴 견적서 공유 링크 (36차 PR 2 · PR #151 · **뒤 4자리 제거 PR #152**)

사용자 원문: *"견적문자 발송에서 간단한 멘트와(견적서발송,위캐리운송포함) 견적서 url을 보낼수 있나? 가급적 (SMS) 문자이면 좋겠다."* → 확정 3건: ~~① 연락처 뒤 4자리 확인~~ **② 유효기간 지나면 차단 ③ 기존 견적안내 문자 대체**

🔴 **①은 하루도 안 돼 사용자 지시로 없어졌습니다**(2026-09-15 · PR #152 — *"뒤4자리 확인을 빼고 바로 링크를 확인할수 있으면 좋겠다"*). ⚠️ **그 신고의 원인은 4자리 확인이 아니라 API 의 잘못된 select 이었고**(없는 컬럼 다섯 · `error` 를 안 받아 `42703` 이 404 로 둔갑 — 원칙 55번) 그것도 같은 PR 에서 고쳤습니다. 🔴 **지금은 링크를 아는 사람이 곧 열람 권한자입니다** — 문자를 전달받은 사람도 그대로 열립니다. **되살리려면 사용자에게 먼저 물으십시오.**

🔴 **왜 만들었나** — 견적서를 볼 수 있는 곳이 `/admin/quotes/[id]`(직원만)와 `/customer/quotes/[id]`(포털 계정 있는 화주만) 둘뿐이라, **전화로 받은 게스트 견적은 계정이 없어** 링크를 줘도 열 수 없었습니다.

```
[문자]  [위캐리운송] 견적서를 보내드립니다.
        https://wecarrylogis.co.kr/q/<토큰 22자>        실측 87byte → **SMS**

[화면]  /q/[token]   주소를 열면 **바로** 견적서가 뜬다 (확인 단계 없음)
```

🔴 **토큰이 곧 열쇠이고, 이제 유일한 열쇠입니다** — 이 값을 아는 사람은 그 견적서(상호·구간·금액)를 봅니다. 22자 base62(≈131비트)이고 **길이를 줄이지 마십시오.** 생성과 유효기간 판정은 **`lib/quoteShare.ts`** 하나가 합니다.

⚠️ **뒤 4자리가 막던 것은 「문자가 가족·동료에게 전달됐을 때 무심코 열리는 것」이었고, 그것은 이제 막지 않습니다.** 피해 범위를 가두는 것은 **응답 필드**입니다 — 차주 지급액·마진·연락처가 한 칸도 없어서, 새어 나가는 것은 「그 화주에게 낸 견적 내용」뿐입니다.

**되돌리면 안 되는 것**

| | |
|---|---|
| **주소만으로는 안 보인다** | 화면이 뜬 **뒤에** 브라우저가 따로 받아옵니다(서버가 HTML 에 실어 보내지 않습니다 — 30차 푸터 이메일 함정). 그래서 **링크 미리보기 봇·크롤러가 내용을 가져가지 못합니다.** 🔴 **서버 컴포넌트로 바꾸면 이 성질이 통째로 무너집니다** — 4자리가 없어진 뒤에도 그대로인 이유입니다 |
| **API 는 POST** | GET 이면 토큰이 **주소창·서버 로그·리퍼러**에 남습니다. 🔴 「4자리가 없어졌으니 GET 으로」로 가지 마십시오 — 이유가 4자리와 무관합니다 |
| **`quotes` anon SELECT 금지** | 만드는 순간 **토큰과 무관하게 모든 견적이 열립니다.** service_role 로만 읽습니다 |
| **응답 필드 고정** | 차주 지급액·마진은 한 칸도 없고 **연락처도 담지 않습니다**(링크만으로 열리는 지금은 담는 순간 전화번호가 링크와 함께 퍼집니다 — select 에서부터 뺐습니다). 🔴 `select("*")` 로 바꾸지 마십시오 |
| 🔴 **컬럼을 짐작하지 말 것** | `quotes` 는 조건값을 **`selected_options`(jsonb)** 에 한글 키로 담습니다 — `body_type`·`trip_type`·`transport_time`·`waiting_minutes`·`waypoint_count` 같은 컬럼은 **없습니다.** 실제 목록은 `_verify.sql` ⑯ 이 찍습니다. 🔴 **두 조회 모두 `error` 를 받습니다**(원칙 55번 — 버리면 조회 실패가 「없는 견적」 404 로 둔갑합니다. 실제로 겪었습니다) |
| **만료 컬럼 금지** | 유효기간은 **견적서 자신의 것**(발행일 + 7일 · `created_at` 기준)을 씁니다. 따로 두면 **문서에 인쇄된 날짜와 링크가 막히는 날이 갈립니다** |
| **토큰은 한 번만** | 보낼 때마다 새로 만들면 **먼저 받은 문자의 링크가 죽습니다.** 기존 견적에 **백필하지 않습니다** |
| **실패 사유를 가르지 않음** | 「없는 토큰이다/조회가 실패했다」로 가르면 링크를 주운 사람에게 *"이 토큰은 맞다"*를 알려주는 셈입니다(서버 로그에는 남깁니다) |
| **문자 87byte** | 🔴 「문의 1661-2403」을 넣으면 **102byte 로 LMS** 가 됩니다 — 넣지 마십시오(발신번호가 담당자 본인 번호라 회신되고 대표번호는 견적서 화면 안에 있습니다). 🔴 **35차 「90byte 압축 금지」와 부딪히지 않습니다** — 그때는 품목이 잘려 문제였고 지금은 **링크가 본문을 대신합니다** |
| **줄임주소 금지** | 외부 서비스가 죽으면 이미 보낸 문자가 통째로 죽습니다. 경로가 `/q/` 로 짧은 이유입니다 |

⚠️ **클라이언트 컴포넌트가 `crypto` 를 쓰는 모듈을 import 하면 안 됩니다** — 유효기간 상수는 **`lib/quoteValidity.ts`**(의존성 0)에 따로 있고 🔴 **`quoteShare` 에서 재수출하지 않았습니다**(재수출하면 같은 일이 벌어집니다). ⚠️ 같은 「발행일 + 7일」이 **세 곳에 숫자로 박혀 있습니다**(견적서 print 2종 · `lib/quoteExcel.ts`) — 유효기간을 바꿀 일이 생기면 **그 셋도 같이 보십시오.**

---

### 5-16. 🔴 배송지 — 상호와 별칭은 다른 칸이다 (PR #155)

`customer_locations` 에 **`company_name`(상호)** 가 있습니다(2026-09-15 신설).
**`location_name` 은 배송지 별칭**(「가산 본사 창고」)이고 **같은 것이 아닙니다.**

🔴 **그전에는 포털 발주 폼이 상호를 `location_name` 에 밀어 넣고 있었습니다** —
`location_name: form.origin_company_name.trim() || form.origin.trim()`.
`quotes`·`orders`·`portal_order_requests` 에는 상호가 **제 컬럼**으로 있는데
(`origin_company_name`/`destination_company_name`) 이 표에만 없어서 생긴 일이고,
그래서 ① 상호와 별칭이 **한 칸을 다퉜고** ② 관리자 화주 상세는 그 칸을 「주소 이름」으로만
읽어 **상호가 어디에도 안 보였습니다.**

| 칸 | 뜻 | 예 |
|---|---|---|
| `company_name` | **상호**(업체명) | `(주)가산테크 1공장` |
| `location_name` | **별칭**(그 화주가 부르는 이름) | `가산 본사 창고` |
| `address` + `address_detail` | 도로명주소 + 상세주소 — **합치지 않습니다** | `서울 금천구 …` + `3층 물류센터` |

- 🔴 **`location_name` 을 없애지 마십시오** — 20차가 같은 자리에서 *「`name` 을 새로
  만들지 말 것」* 이라고 못박았고, 별칭으로 쓰던 화주의 표시가 통째로 바뀝니다.
- 🔴 **카드 제목은 별칭 → 상호 → 주소 순**입니다(`locLabel()`). **상호를 맨 앞에 두지
  마십시오** — 별칭을 일부러 적은 화주는 그것으로 구분합니다. 제목이 이미 상호면 아래
  「상호」 줄을 **두 번 그리지 않습니다.**
- 🔴 **상세주소를 주소에 합치지 마십시오.** `fullOrigin` 관례(원칙 37번)는 상세주소 칸이
  **없는** 표(`quotes`·`orders`)에 적용되는 것입니다. 관리자 화주 상세가 실제로 합쳐 넣고
  있어서 포털과 모양이 갈려 있었습니다.
- 🔴 **읽고 쓰는 화면이 넷입니다** — 화주포털 배송지·화물 관리 / 화주포털 발주요청 /
  관리자 화주 상세 / 견적관리(칩 프리필·체크 저장). **칸을 도로 줄이면** 화주가 포털에
  적은 담당자·특이사항이 담당자 화면에서 다시 사라집니다.
- ⚠️ **백필은 「옮겨 적기」였습니다** — `location_name` 이 **주소와 다를 때만** 상호로 보고
  **원본을 지우지 않았습니다**(운영 실측 14건 중 **2건 이관** · 재실행 0건). 값을 지우지
  않아 **백업표가 없습니다**(36차 `billing_cutoff_day` 와 다릅니다).
- 🟢 **`/api/admin/approve-application` 은 `company_name` 을 안 넣습니다** — 화주등록신청에
  **상호 항목 자체가 없어서**이고 `location_name` 도 원래 안 넣습니다. **정상입니다.**

---

### 5-17. 🔴 알림 — 두 겹이고 성격이 다르다 (38차 PR #160 · 화주포털 B장 PR #161)

**A장과 B장은 「같은 기능의 강약」이 아니라 서로 다른 것입니다.**

```
  A장  화면 안 알림    그 화면을 열어 두고 있을 때만        배너 · 탭 제목 · 소리
  B장  웹 푸시         브라우저를 닫아도 · 폰에서도          윈도우/폰 알림 배너
```

🔴 **B장은 받는 사람이 기기마다 한 번씩 「이 기기로 알림 받기」를 눌러야 옵니다**
(담당자도, **화주도** 마찬가지입니다). 누르지 않은 기기에는 **영영 안 옵니다** —
「알림이 안 온다」는 신고를 받으면 **그것부터** 물어보십시오(그다음이 `_verify.sql` **㉓**
로 구독이 실제로 들어왔는지, 그다음이 VAPID 환경변수, 그다음이 코드입니다).
⚠️ **확인은 Preview 가 아니라 실제 도메인에서** 하십시오 — 서비스워커 구역과 구독이
**도메인마다 별개**라 Preview 에서 등록한 구독은 운영에서 쓰이지 않습니다.
⚠️ **아이폰은 홈 화면에 추가한 뒤에만** 단추가 나옵니다(iOS 16.4+). **앱을 지우면 구독이
사라지고** 다시 설치하면 다시 눌러야 합니다 — **버그가 아닙니다.**

**알리는 것** — 🔴 **PR #162(2026-09-16)에 화주포털이 좁아졌습니다.**

```
  관리자   견적문의 · 화주신청 · 발주요청 · **화주 견적 승인**      A장 + B장
  화주포털  견적 `견적제출` · 배차 `배차확정`·`운송완료`            A장 + B장   ← PR #164
  화주포털  배지(숫자)만 — 정산 · 공지                            🔴 **알림 없음**
```

🔴 **PR #164(2026-09-16)에 화주포털 A장이 「종류」가 아니라 「상태」로 좁아졌습니다.**
그전에는 신호가 `updated_at > 마지막으로 본 시각` **하나**라 **무엇이 바뀌어도** 울렸고,
그래서 담당자가 메모 한 줄만 고쳐도, **화주 본인이 견적 승인을 눌러도**, 상차완료를
체크해도 배너가 떴습니다(사용자 지시 세 건이 전부 이 한 뿌리였습니다).
**이제 A장도 B장과 같은 상태 목록을 봅니다** — 정의처는 `lib/portalAlert.ts` 의
`PORTAL_ALERT_QUOTE_STATUSES`·`PORTAL_ALERT_DISPATCH_STATUSES` 입니다.
🔴 **`updated_at` 만 보는 쪽으로 되돌리지 마십시오** — 세 지시가 한꺼번에 뒤집힙니다.
🔴 **배지(`counts` 넷)와 배너(`alertSignals` 둘)는 이제 값도 다릅니다** — 배너 수 ≤ 배지 수
가 항상 성립하므로 **「배지는 늘었는데 배너가 안 뜬다」는 고장이 아닙니다.** 무엇이
바뀌었는지는 목록 안의 **「업데이트」 표시**가 알려줍니다(§5-18).

🔴 **화주포털에서 정산·공지·상차완료·하차완료를 뺀 것은 사용자 확정입니다**
(2026-09-16 — *「화주포털 알림중 정산관련해서는 알림이 안뜨는게 좋겠다」* ·
*「화주포탈은 상차완료, 하차완료, 공지사항 알림도 빼자. 견적을 받을때, 배차완료,
운송완료 시에만 알림이 가게 설정」*). **되살리지 마십시오.**
🔴 **정산은 「종류째」 뺐습니다** — 사용자가 든 것은 셋(세금계산서 발행완료 · 화주입금
완료 · 차주지급완료)인데 포털 알림의 신호가 **`invoices.updated_at` 하나**라 **그 셋을
골라낼 수가 없습니다.** 되살리려면 **사건별 신호를 새로 만들어야** 합니다.
🔴 **공지를 안 넣는 이유는 그대로입니다** — 전체 공지라 **모든 화주 폰이 한꺼번에**
울립니다. 넣으려면 「누구에게 보낼지」를 먼저 정해야 합니다.
🟢 **배지(숫자)는 넷 그대로입니다** — 「볼 것이 생겼다」와 「불러야 한다」가 다른 것이라
`PortalCountKind`(넷) / `PortalAlertKind`(둘)로 갈랐습니다. 🔴 **다시 합치지 마십시오** —
합치면 배지를 지우거나 알림을 되살리거나 둘 중 하나가 됩니다.
🔴 **화면 안 알림과 푸시는 항상 같이 움직입니다** — 한쪽만 끄면 **「폰으로는 오는데
화면에는 안 뜨는」** 상태가 됩니다(그 반대도 마찬가지입니다).

🔴 **`하차완료` 와 `운송완료` 는 DB 에서 다른 값입니다**(`DISPATCH_STATUS_OPTIONS` 6종)
— 그런데 **화주 화면은 둘 다 「운송완료」로 보여줍니다**(`lib/dispatchStage.ts` 2단계 ·
29차). 푸시는 사용자가 쓴 말 그대로 **DB 값 `운송완료` 에만** 걸었으므로,
**담당자가 `하차완료` 에서 멈추면 화주에게 완료 알림이 가지 않습니다**(화면에는
「운송완료」로 보이는데 알림만 없습니다). 단계(`getDispatchStage`)로 바꿔 걸면
`하차완료` 에서도 울리는데 **그것은 이번 확정과 반대입니다 — 바꾸려면 먼저 물으십시오.**

🔴 **창내 배너는 그 화면에 들어가면 스스로 꺼집니다**(PR #162) — 끄는 신호가 **둘**이고
**둘 다 있어야 합니다**: ① 경로가 바뀌었을 때 ② **서비스워커가 보내는 쪽지**.
②가 없으면 **이미 그 화면이 열려 있는 경우**를 못 잡습니다(열린 탭을
`client.navigate()` 로 옮기는데 같은 주소면 경로가 안 바뀝니다).
정의처는 `lib/alertCore.ts` 의 **`dropToastsForPath()` 하나**입니다.

🚨 **2026-09-18 부터 배차확정은 문자와 푸시가 둘 다 갑니다 — 사용자가 고른 겹침입니다.**
고객용 배차확정 문자가 생겼기 때문입니다(§5-25). **문자는 확인용 상세 정보**(주소·일시·
기사·차량)이고 **푸시는 알림**(종류와 건수뿐)이라 하는 일이 다릅니다.
🔴 **「겹친다」를 근거로 푸시 사건을 줄이지 마십시오.**
⚠️ **그 전(2026-09-16 실측)에는 겹치는 자리가 없었습니다** — 배차확정 문자가 **차주에게만**
갔고 상차·하차완료는 자동 발송이 아예 없었습니다. 🔴 **그 시절 기록을 근거로 고객 문자를
빼지 마십시오.** 🟢 **상차·하차완료 문자는 아예 폐지됐으므로**(§5-25) 「나중에 자동 발송으로
되돌리면 겹친다」는 걱정도 함께 사라졌습니다.

**되돌리면 안 되는 것**

- 🔴 **알림 본문에 고객 정보를 넣지 마십시오** — 폰 **잠금화면**과 데스크탑 **배너**에
  그대로 뜨고 옆사람이 봅니다. 담는 것은 **종류와 건수**뿐이고 실측으로 키가
  **`body,tag,title,url` 넷뿐**임을 확인했습니다. 🔴 **「담당자가 편하도록」을 이유로
  넓히지 마십시오** — 이 저장소는 public 이고 화면 캡처가 돌아다닙니다.
- 🔴 **구독 표는 둘이고 합치면 안 됩니다** — `push_subscriptions`(직원) /
  `customer_push_subscriptions`(화주). **둘 다 RLS on + 정책 0개**입니다. 화주포털 계정과
  직원 계정이 **둘 다 `authenticated` 롤**이라(19차) 정책을 하나라도 만들면 **화주가
  직원 기기의 구독 정보를 읽습니다** — 그래서 이 표들은 정책이 아니라 **표를 가르는 것**으로
  지켜집니다. 한 표에 두 종류를 담아 두면 언젠가 누가 「조회 편하게」 정책을 하나 열 때
  **양쪽이 한꺼번에** 샙니다. 마이그레이션 단언이 정책 0개와 `staff_id` 컬럼의 **부재**까지
  확인합니다.
- 🔴 **화주 쪽 구독 표에 `company_id` 를 복사해 두지 않았습니다.** 담당자 계정이 다른
  화주로 옮겨지면 낡은 값으로 **남의 회사 소식이 그 폰으로 갑니다** — 발송할 때마다
  `customer_accounts` 를 조인해 회사를 정합니다. ⚠️ **그래서 그 표만 보고는 「어느
  화주인가」를 알 수 없습니다 — 그것이 의도입니다.**
- 🔴 **서버 안에서는 `await` 하고 브라우저에서는 하지 않습니다 — 방향이 반대입니다.**
  서버리스 함수는 응답을 돌려준 뒤 **얼어붙어서** fire-and-forget 으로 두면 푸시가
  **아예 안 나갑니다**(정산확정이 그 경우입니다). 반대로 견적·배차는 **브라우저가** 서버
  API 를 부르는 구조(원칙 53번)라 `await` 하면 담당자의 상태 변경이 알림을 기다리며
  멈춥니다. ⚠️ 이름이 헷갈립니다 — **`notify-` 로 시작하는 `lib/notifyPortalPush.ts` 가
  브라우저용**이고 `lib/portalPushNotify.ts` 가 서버 발송 정의처입니다.
- 🔴 **배차 상태를 바꾸는 자리가 넷이라**(배차 상세의 확정 버튼·상태 드롭다운·상차/하차
  체크박스 + 배차 목록) 상태 → 사건 매핑을 `notifyPortalPushForDispatchStatus()` **한 곳**에
  뒀습니다. 🔴 **수동 재발송 버튼(배차 상세 「문자 발송」 셋)에서는 부르지 마십시오** —
  거기는 담당자가 **문자**를 다시 보내는 자리라 며칠 뒤 눌러도 푸시가 또 갑니다.
  ⚠️ `fetchDispatchSmsPreview()` 안에 넣으면 딱 그 사고입니다(**그 함수를 여섯 곳이
  부르는데 그중 셋이 수동 재발송**입니다).
- 🔴 **`staff_id` FK 는 cascade 입니다 — 원칙 32번과 반대로 잡은 것입니다.**
  `support_access_logs` 는 **이력**이라 원본이 지워져도 남아야 하지만, 이것은 **살아 있는
  발송 대상 목록**이라 퇴사자 기기로 알림이 계속 가면 안 됩니다.
- 🔴 **role 을 가르지 않습니다**(사용자 확정: 직원 전원 같은 알림) — `role === "admin"` 을
  넣으면 **staff 가 알림을 못 받습니다.** 🔴 **수신 설정 화면을 만들지 마십시오.**
- 🔴 **새 폴링도 새 구독도 만들지 마십시오.** 관리자·포털 둘 다 **이미 있던 배지 건수
  (`counts`)가 늘어난 순간**만 봅니다. 🔴 **「15초 폴링에 얹으라」는 지시서 2-1 로 되돌리면
  가장 급한 발주요청을 못 잡습니다**(그것은 폴링이 아니라 **Realtime** 입니다).
- 🔴 **소리는 `localStorage` 에 기억하고 관리자와 포털이 같은 칸을 씁니다.**
  `staff_accounts` 컬럼으로 옮기지 마십시오 — 수신 설정 화면을 안 만들기로 한 것과
  어긋나고, DB 변경이 0 이던 A장이 DB 변경을 갖게 됩니다.
- 🔴 **윈도우·폰 알림 소리는 OS 가 정하므로 우리가 못 바꿉니다**(Windows 설정 → 시스템 →
  알림 → 브라우저). 「푸시 소리도 화면에서 고르게 하자」로 넓히지 마십시오 —
  **만들 수 없는 것을 있는 것처럼 그리게 됩니다.**
- 🔴 **포털 라벨은 「견적 업데이트」이지 「새 견적」이 아닙니다** — 신호가
  `updated_at > 마지막으로 본 시각` 이라 **새로 생긴 것과 고쳐진 것을 가리지 않습니다**
  (담당자가 메모 한 줄만 고쳐도 뜹니다). 공지만 `created_at` 기준이라 「새」가 맞습니다.

**정의처**

```
  lib/alertCore.ts        소리 · 늘어난 것 고르기 · 탭 제목      ← 관리자·포털이 같이 쓴다
    ├ lib/adminIntakeAlert.ts   관리자 말·경로
    └ lib/portalAlert.ts        포털 말·경로
```

🔴 **코어에 라벨·경로를 넣지 마십시오** — 넣는 순간 반대편이 남의 말을 쓰게 됩니다.
🔴 **코어에 CSS·JSX 가 0건인 것이 중요합니다** — 그래야 `.portal-v2` 스코프와 관리자
화면이 서로를 끌고 오지 않습니다(원칙 57번 · `lib/arrivalType.ts` 와 같은 자리).

---

### 5-18. 🔴 견적 상태 라벨 · 포털 목록 「업데이트」 표시 (PR #164)

**「상담중」은 화면에 「확인중」으로 찍습니다 — DB 값은 그대로입니다.**

```
                  쓰는 함수                        상담중    보류      실패
  화주 화면       quoteStatusStyle()      (기존)    확인중   협의 중    취소
  관리자 화면     quoteStatusAdminLabel() (신규)    확인중   보류      실패
```

🔴 **`quotes.status` 도 `quotes_status_check`(다섯 값)도 안 건드렸습니다.** 값을 바꾸려면
CHECK 를 갈고 기존 행을 UPDATE 해야 하는데, **그 사이 배포 순서가 어긋나면 견적 저장이
통째로 막힙니다** — 바로 앞 차수(PR #163)가 겪은 바로 그 증상입니다.
🔴 **DB 값을 바꾸는 쪽으로 되돌리지 마십시오.**
🔴 **관리자에 화주용 순화 라벨을 끌어오지 마십시오** — 담당자에게 `보류`·`실패` 는 정확한
말이고, 그 화면에서 「취소」는 무엇이 취소된 것인지 흐려집니다(27차 확정 그대로입니다).
🔴 **`<option value>` 는 DB 값 그대로** 두십시오 — 값까지 바꾸면 CHECK 위반으로 상태
변경이 실패합니다.
🟢 **이용가이드 2화면**(`/guide`·`/customer/guide`)은 `quoteStatusStyle()` 에서 라벨을
가져오므로 **코드 0줄로** 같이 바뀌었습니다.
🟢 **모르는 값은 그대로 돌려줍니다** — 상태가 늘어도 화면에서 조용히 사라지지 않습니다.

**포털 목록의 「업데이트」 표시는 배너와 별개이고 넓습니다.**

견적 확인 · 배차·운송 조회 카드의 번호 옆에 파란 알약이 붙습니다. 판정은
**마지막으로 본 뒤 `updated_at` 이 바뀌었는가** 하나입니다.
🔴 **배너와 같은 조건으로 좁히지 마십시오** — 사용자 원문이 **「알림배너와 별개로」**이고,
배너를 §5-17 처럼 좁힌 만큼 **무엇이 바뀌었는지는 목록이 말해야** 합니다. 그래서
「상차완료 알림은 빼되 화면에서는 보이게」가 둘 다 성립합니다.
🔴 **셸이 화면 진입 때 「마지막으로 본 시각」을 밀기 때문에**(배지를 0으로 만들려고)
목록이 그 뒤에 읽으면 **언제나 「바뀐 것 없음」**입니다 — `lib/usePortalSeenAt.ts` 가
**밀리기 전 값을 붙잡습니다.** 🔴 **화면에서 `getLastSeen()` 을 직접 읽지 마십시오.**
🔴 **첫 방문에는 아무것도 표시하지 않습니다**(기준이 없으면 목록이 통째로 표시됩니다).
🔴 **발주 요청 행에는 안 붙입니다** — 반려는 빨간 「접수 반려」 알약으로 이미 눈에 띕니다.
🔴 **시각 비교는 `Date.parse` 입니다 — 문자열로 비교하면 뒤집힙니다.** Postgres 는
`…+00:00`(µs), `toISOString()` 은 `…Z`(ms)라 형식이 다릅니다. 실측:
`"2026-09-16T12:00:00.500+00:00" > "2026-09-16T12:00:00Z"` 가 **false** 입니다(`.` < `Z`).
⚠️ **밀리초 아래는 버립니다** — 기준값과 같은 밀리초에 바뀐 건은 표시되지 않는데, 그
건은 **화주 본인이 방금 일으킨 것**이라 실무에서 문제가 되지 않습니다.

🟢 **실제 도메인에서 확인 완료입니다**(사용자 2026-09-16) — 저장 직후 관리자·포털 둘 다
「확인중」 · 포털에서 견적 승인 시 **포털에는 안 뜨고 내부시스템에는 뜨는 것** · 목록의
「업데이트」가 다시 들어가면 사라지는 것까지 전부 확인받았습니다.

### 5-19. 🔴 필수 항목 표시 — 빨간 `*`, 정의처는 둘 (39차 PR #165)

🔴 **필수 항목 표시는 빨간 `*`(`#B4423A`)입니다.** 공개 폼 3화면(`/quote`·`/apply`·
`/customer/login` 계열)과 **화주포털 발주요청**이 같은 모양을 씁니다.

⚠️ **31차 리뷰가 확정했던 「빨간 `*` 가 아니라 회색 「필수」 글자」를 뒤집은 것입니다**
(사용자 지시 2026-09-16 — *"필수항목은 🔴 빨간 * 으로 표시하자. 이부분은 화주포털
발주요청도 마찬가지로 수정하자"*). 🔴 **그 옛 결정을 근거로 글자로 되돌리지 마십시오.**

```
  공개 폼   components/landing/form/Fields.tsx   requiredMark · REQUIRED_MARK_COLOR
  화주포털   app/globals.css                      .pv2-req
```
🔴 **정의처가 둘인 것은 의도입니다** — 공개 폼 부품을 포털에 import 하지 않으려고
가른 것입니다(원칙 57번). 🔴 **값은 같아야 합니다**(실측 `rgb(180,66,58)` · 15px · 700) —
**한쪽을 고치면 반대쪽도 같이 고치십시오.**
🔴 **`--pv2-*` 토큰을 새로 만들지 마십시오** — 만들면 포털 전용이 되어 공개 폼과 값을
맞출 수 없습니다(PR #145 가 같은 판단을 했습니다).

⚠️ **포털에 표시가 없던 게 아니었습니다** — **검정 `*` 가 부제와 placeholder 안에
문자로** 박혀 있었습니다(`"상차지 정보 *"` · `"출발지 주소 검색 또는 직접 입력 *"`).
🔴 **placeholder 는 값을 입력하면 사라져서 표시 역할을 못 합니다** — 제목 옆으로 옮겼고
문자 `*` 는 지웠습니다. **문자로 되돌리지 마십시오.**

🔴 **표시는 안내일 뿐 제출을 막지 않습니다** — 막는 것은 각 화면의 **제출 직전 검사**이고,
네이티브 `required` 는 일부러 안 씁니다(React 핸들러보다 먼저 걸려 우리 오류 문구가
안 뜹니다 · PR #121). **둘이 어긋나지 않게 하십시오.**

```
  /quote            출발지 · 도착지 · 성함/업체명 · 연락처   (+ 개인정보 동의)
  /customer/request 출발지 · 도착지 · 품목                  (+ 제3자 제공 동의)
  /apply            회사명 · 담당자명 · 담당자 연락처         (+ 약관 · 개인정보 동의)
```
🔴 **동의 카드(`LandingConsentCard`)의 자체 「필수」 배지는 별개입니다**(12px `#8B8A85`) —
라벨 옆 표시가 아니라 카드 제목 옆 배지라 건드리지 않았습니다.

### 5-20. 🔴 운송시간 자동선택 (39차 C장 PR #165)

희망 상차일시를 정하면 **운송시간이 자동으로 맞춰집니다**(담당자·화주가 직접 바꿀 수
있고, **상차일시를 다시 고치면 또 맞춰집니다**). 정의처는 **`lib/transportTimeAuto.ts`
하나**이고 **세 화면**이 씁니다 — 견적문의 `/quote` · 화주포털 발주요청 · 관리자 견적 등록.

```
  토·일                주말            +10,000   🔴 시간대보다 먼저다
  00:00 ~ 05:59       새벽            +20,000
  06:00 ~ 07:59       출퇴근/혼잡      +10,000
  08:00 ~ 17:59       평일 주간            0
  18:00 ~ 19:59       출퇴근/혼잡      +10,000
  20:00 ~ 23:59       평일 야간        +10,000
  공휴일               (자동 선택 대상이 아님 — 달력이 없다)
```

⚠️ **그전에는 관리자 견적 등록 화면 안에만 규칙이 있었고 틀렸습니다** —
① `출퇴근/혼잡`·`새벽`을 **아예 안 썼습니다**(`hour < 8 || hour >= 20` 을 전부 「평일
야간」으로 봐서 **새벽 3시도 야간**이었습니다) ② `includes("평일")` **부분일치**라
`평일 주간`·`평일 야간` 중 **배열 순서로 먼저 오는 쪽**이 잡혔습니다(둘 다 「평일」을
포함하고 글자수까지 같습니다). 🔴 **부분일치로 되돌리지 마십시오.**

⚠️ **그래서 18:00~19:59 상차 건의 견적이 0원 → +10,000원으로 올랐습니다** —
**사용자 확정**(「여섯 종 전부 자동」)이고, 06:00~07:59 는 지금도 +10,000 이라 **이름만**
바뀝니다. 🔴 **값을 올린 것이 아니라 고장을 고친 결과입니다.**

🔴 **`autoTransportTime()` 은 실제로 내려온 선택지 목록 안에 있을 때만 값을 줍니다** —
`option_name` 완전일치라 이름이 바뀌면 틀린 값을 넣는 대신 **아무것도 안 고릅니다.**
**`available` 인자를 생략 가능하게 만들지 마십시오.**
🔴 **주말 우선순위와 공휴일은 §7 「39차가 남긴 것」에 열려 있습니다.**

### 5-21. 🔴 주소 입력 — 부품은 둘, 규칙은 훅 하나 (39차 D장 PR #166)

**주소 칸에 「역삼동」처럼 치고 엔터를 누르면 팝업이 그 검색 결과를 이미 보여주는
상태로 열립니다.** 고른 것은 **도로명이든 지번이든 고른 그대로** 들어갑니다.

🔴 **주소를 받는 부품은 둘입니다 — 고칠 때 반드시 둘 다 보십시오.**

```
  components/AddressSearch.tsx        공개 2(/quote · /apply) · 관리자 6 · CompanyFieldInput
  components/pv2/Pv2AddressField.tsx  화주포털 2(/customer/locations · /customer/request)
```
**25차가 인라인 style 때문에 일부러 갈랐습니다**(주소검색 버튼·상세주소의 `padding`·
`borderRadius`·`fontSize`·`marginTop` 을 CSS 로 덮을 수 없어서). 🟢 **주소를 받는데 이
둘을 안 쓰는 칸은 0건**입니다(`daum` 을 직접 부르는 곳도 이 둘뿐).

🔴 **규칙은 `lib/useDaumPostcode.ts` 하나이고 부품에 다시 적지 마십시오.**

```
  open(oncomplete, q?)          q 를 주면 그 결과를 보여주며 연다. 빈 검색어는 안 넘긴다
  enterToSearch(value, search)  엔터 처리 — preventDefault 를 먼저, 조건 없이
  pickSelectedAddress(data)     사용자가 고른 타입(도로명/지번) 그대로
```
⚠️ **지번 선택이 통째로 무시되던 버그가 정확히 「두 부품이 각자 적고 있던 것」이었습니다**
(`roadAddress || jibunAddress`). 정의처를 하나로 모은 이유입니다.

🔴 **옵션 이름은 `q` 이고 `open()` 의 인자입니다**(생성자 옵션이 아닙니다).
⚠️ **공식 문서를 못 봤습니다**(이 환경의 프록시가 403). `react-daum-postcode@4.0.0` 의
`OpenOptions` 타입·구현으로 확인했고 **1차 출처가 아니라고 주석에 적어 뒀습니다.**

🔴 **`preventDefault()` 를 `ready` 검사보다 먼저, 조건 없이 부릅니다** — 주소 칸을 쓰는
화면 **여덟이 `<form>` 안**이고 `/quote`·`/apply` 는 **anon INSERT 경로**라 **주소를
치다가 문의가 그대로 접수됩니다.** ⚠️ 폼 쪽 `handleFormKeyDown`(원칙 34번)이 이미
막지만 **그것에 기대지 않습니다** — 그 핸들러가 없는 화면이 셋입니다.

🔴 **주소 칸은 원래부터 자유 입력이고 그 기능을 없애지 마십시오** — 직접 치면 그대로
저장되고 그때 `sido`/`sigungu` 가 빈 값으로 갑니다(원칙 37번·두 부품 주석).
**「고르지 않고 닫으면 친 글자를 되돌린다」를 넣으면 그 기능이 통째로 사라집니다.**

#### 🔴 「동까지만」은 이 팝업으로 안 됩니다 (실물 확인 2026-09-16)

사용자 질문 — *「인계동」이라고 적고 엔터한 다음 팝업창에서 「경기 수원시 팔달구
인계동」 표시되게 할수 있는 방법은 없는 건가?*

다음(카카오) 우편번호 서비스는 **건물·지번 단위만** 돌려줍니다. 🟢 **사용자 실물
스크린샷으로 확인됐습니다** — 「인계동」 결과가 전부 `경수대로 419 (우리병원)` /
`인계동 1014` 처럼 번지·건물번호까지 붙어 있고, 🔴 **우편번호가 항목마다 다릅니다**
(16480 / 16490). **우편번호가 건물·구역 단위라 「동까지만」으로는 정해지지 않습니다.**

```
  (A) 직접 타이핑           🟢 이미 됩니다(엔터를 안 누르면 팝업도 안 뜹니다) ·
                            🟢 거리 계산도 됩니다(`/api/distance` 가 그 문자열을 그대로
                               카카오 로컬 API 에 넘깁니다) ·
                            ⚠️ `sido`/`sigungu` 가 비지만 **금액 영향 0**(저장·프리필 전용)
  (B) 카카오 로컬 API 목록   🟢 **키·엔드포인트가 이미 있습니다** — `app/api/distance/
                               route.ts` 가 `dapi.kakao.com/v2/local/search/address.json`
                               을 `KAKAO_REST_API_KEY` 로 부릅니다 ·
                            🔴 **D장이 「안 고르는 길」로 못박았습니다**(저장값 출처가
                               바뀝니다) · 별도 차수 크기입니다
  (C) 고른 뒤 번지 떼어내기  🔴 **금지.** 「테헤란로 152」에서 152 를 떼면 동이 아니라
                               도로명이고 우편번호도 그 건물 것이라 어긋납니다
```
🟢 **2026-09-17 에 (A) 「지금대로」로 확정됐습니다**(사용자) — **닫혔습니다.**
🔴 **「열려 있습니다」로 적힌 옛 문장을 근거로 (B) 를 착수하지 마십시오** — 키가 이미
있어 손쉬워 보이지만 **저장값 출처가 바뀌고 D장 확정과 부딪힙니다.** 되돌리려면
**사용자에게 먼저 물어야 합니다.**

### 5-22. 🔴 「전화로 처리함」은 지우면 안 되는 탈출구다 (PR #167)

화주 발주 요청 목록(`/admin/portal-requests`)의 **「전화로 처리함」** 버튼(옛 이름
「수동 승인 처리」)은 **없애도 되는 버튼이 아닙니다.**

발주요청은 **「견적이 저장되는 순간」에 `승인됨`** 이 됩니다
(`app/admin/quotes/page.tsx` 의 `if (fromRequestId && newQuote)`). 그래서 **견적 저장이
실패하면 요청이 `대기중` 으로 남고 배지가 영영 안 사라집니다** — **PR #163 이 정확히 그
증상**이었고(견적번호가 겹쳐 insert 가 계속 실패했습니다), 그때 사용자가 **이 버튼을 손으로
눌러** 풀었습니다. 코드가 자동으로 고쳐주는 경로는 없습니다.

```
  🔴 지우지 마십시오          `handleManualApprove()` 와 그 버튼
  🔴 `display: none` 도 안 됩니다  숨기면 탈출구가 없는 것과 같습니다
  🟢 대신 무게만 낮췄습니다      이름 「전화로 처리함」 · `.btn-quiet`(밑줄 회색 글자) ·
                            버튼 줄 맨 끝 · 확인 창에 「견적서는 만들어지지 않습니다」
```

⚠️ **사용자 원문은 「이는 불필요해 보인다. 상관없으면 삭제하자」였습니다**(2026-09-16).
조사해 보니 상관이 **있어서** 이름·생김새만 바꾸고 보고했고, 사용자가 그대로 확정했습니다.
🔴 **「불필요해 보인다」는 신고를 받으면 먼저 재십시오** — 이 버튼은 **평소에 쓸 일이 없는
것이 정상**이라 그렇게 보였던 것이고, **쓰는 빈도가 낮은 것과 없어도 되는 것은 다릅니다.**

### 5-23. 🔴 배차 취소 · 문제발생 사유 (PR #171)

운송이 **정상 경로를 벗어났을 때** 무엇을 남기고 화주에게 무엇을 보여줄지의 확정입니다.

**① 취소는 삭제가 아닙니다.** `dispatch_status` 에 **`취소`** 가 늘었습니다(DB CHECK **7종**).
🔴 **화면 상수 `DISPATCH_STATUS_OPTIONS` 는 6종 그대로이고 거기에 `취소` 를 넣지 마십시오** —
드롭다운에서 고를 수 있게 되면 **사유를 못 받습니다**(취소는 배차 상세의 「배차 취소」 버튼 +
사유 모달로만 갑니다). 🔴 **되살리는 경로를 만들지 마십시오**(상태가 `취소` 면 읽기 전용이고,
되살리면 `cancelled_at` 이 남은 채 살아나 집계가 어긋납니다 — **새 배차가 정상 경로**입니다).
🔴 **`운송완료` 에서는 취소로 갈 수 없습니다**(정산이 이미 만들어졌을 수 있습니다).
🔴 **「차주 책임」(`driverFault`) 칸이 사유 표의 존재 이유입니다** — 차주별 집계가 이 칸으로
가릅니다. **화주 요청 취소를 차주 이력에 세지 마십시오.**

**② 화주 화면에서 감추지 않습니다**(사용자 확정 2026-09-17 · 리뷰 1라운드).

```
  화면          「접수」 + 「배차 취소 · 재배차 접수 중」 + 설명 한 줄
  배지 색        옅은 레드 #FDF3F2 / #B4423A            (PR #172 · 사용자 지시)
  뜨는 구간      🔴 취소된 순간부터 **배차확정까지**      (PR #172)
                 재배차가 접수되면 **새 카드가 이력을 이어받습니다**
  화주가 취소    「배차 취소 · 고객 요청 취소」 (재배차하지 않으므로 그 말을 안 붙입니다)
  중복 처리      살아 있는 배차가 있는 오더의 취소 건만 감춥니다
                 같은 오더에 취소가 여럿이면 가장 최근 하나만  (lib/portalCancelledDispatches.ts)
  엑셀(월별 통계) 취소는 계속 뺍니다 — 그것은 「운송 실적」이라 성격이 다릅니다
```

⚠️ **위 두 줄(배지 색 · 뜨는 구간)은 PR #172 에 바뀐 것입니다.**
🔴 **배지 색은 옅은 레드입니다** — 하루 전 `.pv2-dcancel-tag` 주석이 *「`.pv2-dissue`
(문제발생)와 색을 같게 하지 말 것 — 취소는 사고가 아니다」* 라고 적고 있었고 **사용자가
그것을 뒤집었습니다**(같은 커밋에서 그 문장을 고쳤습니다). **회색으로 되돌리지 마십시오.**
🔴 **새 색을 만들지 않았습니다**(`DISPATCH_ISSUE_STYLE` 과 같은 값) — 두 배지가 한 카드에
같이 떠도 **가르는 것은 색이 아니라 말입니다.** ⚠️ **값이 `lib/dispatchCancel.ts` 의
`DISPATCH_CANCEL_CUSTOMER_STYLE`(홈)과 `app/globals.css` 의 `.pv2-dcancel-tag`(조회) 두 곳에
리터럴로 있습니다** — CSS 는 상수를 못 읽습니다. **한쪽을 고치면 반대쪽도 고치십시오.**
🔴 **배지는 재배차가 접수된 뒤에도 배차확정 전까지 계속 뜹니다**(사용자 지시 2026-09-17 —
*「이때까지는 배차취소 뱃지가 남아있고 배차완료가 됐을때 사라지게 하자」*). 그전에는 재배차가
접수되는 순간 사라졌습니다(취소 카드를 감추는데 **새 카드는 이력을 몰랐습니다**).
판정의 정의처는 **`getPortalCancelNotice()` 하나**이고 홈·조회 두 화면이 씁니다 —
🔴 **화면에서 `isDispatchCancelled` 로 다시 적지 마십시오**(고치기 전 동작으로 되돌아갑니다).
🔴 **단계 판정은 `lib/dispatchStage.ts` 가 합니다**(`"접수중"` 문자열을 적지 마십시오) ·
🔴 **형제 배차를 화면이 든 목록에서 찾지 마십시오** · 🔴 **취소 카드는 DB 가 아니라 `rows`
안에서 최근 것을 고릅니다**(DB 기준이면 그 행이 목록 밖일 때 **통째로 사라집니다**).

⚠️ **이것은 하루 전 확정 (A)「감춘다」를 사용자가 배포본을 보고 뒤집은 것입니다.**
🔴 **그 옛 확정을 근거로 되돌리지 마십시오.** 🔴 **화주 라벨에 차주를 탓하는 말·회사 책임을
시사하는 말·「지연됩니다」류 결과 단정을 넣지 마십시오**(약관 제19조가 배상책임을 차주와
그 보험자에게 둡니다).

🚨 **차주 정보 비노출은 이 절에서도 화면 기준입니다** — 배차확정 문자로는 고객에게
기사 이름·연락처·차량번호·차종을 보냅니다(2026-09-18 · §5-25). 🔴 **화주 화면은 그대로**
이고, 🔴 **취소된 배차에서는 문자가 나가지 않습니다**(취소는 읽기 전용이라
`취소 → 배차확정` 전환이 없고, 재배차는 새 배차 행이라 그 배차의 확정에서 나갑니다).
⚠️ **다만 고객이 이미 받은 문자에는 옛 기사 번호가 남아 있습니다** — §7 의 열린 항목입니다.

**③ 🚨 「분실」을 화주 화면에 쓰지 마십시오.** 보험 약관이 **도난신고 없는 망실을 전부
「분실」로 간주해 면책**합니다. 화주 화면에 그 낱말이 뜨면 **보험이 안 나오는 바로 그 경우를
회사가 스스로 적은 것**이 됩니다. `lost` 의 화주 라벨은 **「화물 확인 중」**이고 「없어졌」도
같은 이유로 피했습니다. 🔴 **사고 체크리스트(`lib/incidentGuide.ts`)는 관리자 전용입니다** —
화주에게 보이면 보험 조건이 **협상 카드**가 됩니다. 🔴 **「확인함」 체크박스를 달지 마십시오**
(누른 기록이 곧 「알고도 안 했다」의 증거입니다). 🔴 **보험 금액(1억)을 적지 마십시오**
(자기부담금 20만원은 내부 화면이라 적었습니다).

**④ 내부 화면은 글자만 「배차취소」입니다** — **DB 값은 `취소` 그대로**입니다
(`dispatchStatusAdminLabel()` · 배차 목록·배차 상세·활성 화주 목록 셋). 🔴 **CHECK 를 가는
쪽으로 되돌리지 마십시오**(배포 순서가 어긋나면 배차 저장이 통째로 막힙니다 · PR #164 와 같은
판단). 🚨 **그전에는 취소 행에 `<select value="취소">` 가 그려졌는데 그 값이 6종 목록에 없어
브라우저가 첫 항목을 골랐습니다** — 화면에는 **「배차확정」으로 보이고 누르면 진짜 그렇게
저장**됐습니다. 지금은 데스크탑·모바일 **둘 다 고정 배지**이고 저장 경로에도 가드가 있습니다.

**⑤ 화주포털에는 되풀이 폴링이 0개입니다.** 화면이 스스로 갱신되는 길은 **Realtime 하나뿐**
이고 ① 탭이 뒤로 갔다 오면 **놓친 이벤트가 다시 오지 않고** ② 알림을 눌렀는데 이미 그
화면이면 주소가 안 바뀌어 다시 마운트되지 않습니다. **`lib/portalRefresh.ts`** 가 그 둘을
메웁니다(포털 네 화면 + 배지가 같은 신호). 🔴 **되풀이 타이머를 만들지 마십시오**(38차 원칙).


### 5-24. 🔴 견적 금액은 연결된 오더까지 간다 (PR #172)

**배차가 안 잡혀 금액을 올릴 때** 담당자가 두 화면에서 같은 숫자를 두 번 고치지 않게 한
것입니다(사용자 지시 2026-09-17). 그전에는 견적 금액이 오더로 넘어가는 길이 **오더 등록 때
한 번**(`?from_quote=` 프리필)뿐이었습니다.

```
  견적 상세에서 최종 견적금액을 고쳐 저장하면
    → 연결된 오더의  customer_charge               = 그 금액
                     customer_charge_vat_included  = false   🔴 빼지 마십시오
    → 배차(dispatches.customer_charge)  안 따라갑니다
    → 정산(invoices)                    안 따라갑니다        (원칙 47번)
  확인 창은 오더가 배차완료·운송중·운송완료일 때만        (원칙 39번과 같은 목록)
```

🔴 **`customer_charge_vat_included: false` 를 빼지 마십시오** — `quotes.final_amount` 는
**공급가액(부가세 별도)**이라, 금액만 덮어쓰면 오더가 포함가로 읽혀 **공급가액이 10% 줄어든
채로** 배차·정산까지 승계됩니다(**PR #153 이 프리필에서 겪은 그 자리**입니다).

🔴 **정산은 따라오지 않습니다** — 정산 건의 금액은 생성 시점 스냅샷이고(원칙 47번) 맞추는
자리는 정산 상세의 **「배차 기준으로 금액 다시 맞추기」**(35차 A-7)입니다. **여기서
`invoices` 를 같이 고치지 마십시오 — 그것이 원칙 47번이 막는 바로 그 일입니다.**

🔴 **배차도 따라오지 않습니다** — 배차의 화주 청구금액은 **배차 담당자가 배차 상세에서**
정합니다(혼적 할인·현장 조정이 거기서 붙습니다). **조용히 같이 고치지 마십시오** — 돈이고
고치는 사람이 다릅니다. 대신 화면이 **세 자리**(입력칸 아래 안내 · 저장 후 결과 문구 ·
확인 창 본문)에서 「배차·정산 금액은 따로 확인하라」고 말합니다.

🔴 **묻지 않고 바로 반영하는 것이 기본입니다**(그것이 지시입니다). 오더가 이미
`배차완료`·`운송중`·`운송완료`일 때만 확인 창이 한 번 뜹니다 — 🔴 **그 목록은 원칙 39번이
오더의 정산방식을 잠그는 것과 같은 목록이니 갈라 적지 마십시오**
(`lib/quoteAmountSync.ts` 의 `ORDER_AMOUNT_SYNC_CONFIRM_STATUSES`).

🔴 **견적 금액이 비면 아무것도 하지 않습니다**(오더 금액을 지우면 배차·정산이 0원으로
흐릅니다) · 🔴 **금액이 그대로면 오더를 건드리지 않습니다**(괜한 `updated_at` 은 화주포털
목록에 **「업데이트」 알약**으로 뜹니다) · 🔴 **견적 저장이 성공한 뒤에만** 고칩니다.

🔴 **안 한 것 — 배차 금액 자동 반영.** 바로 따라오는 생각이지만 사용자가 말한 것은 오더
뿐입니다. **하게 된다면 「어느 상태까지 덮어쓸 것인가」를 먼저 정하십시오**(배차확정 이후의
금액은 차주와 합의된 값이 얽힙니다).

---

### 5-25. 🔴 문자 7종 — 누가 언제 무엇을 받나 (2026-09-18)

문자 **8종이 7종**이 됐고 **배차확정이 두 통**이 됐습니다.

```
  키                            라벨              받는 사람  발송 지점                    형식
  ───────────────────────────────────────────────────────────────────────────────────────
  dispatch_confirmed            화물정보 안내      차주      배차확정(확인창 1/2)·수동     LMS
  dispatch_confirmed_customer   배차확정 안내 🆕   고객      배차확정(확인창 2/2)·수동     LMS
  application_approved          승인 안내         신청자     화주신청 승인                LMS
  application_rejected          거절 안내         신청자     화주신청 거절                LMS
  portal_account_issued         계정발급 안내      고객      화주 상세 계정 발급           LMS
  portal_password_reissued      비밀번호 재발급    고객      화주 상세 재발급              LMS
  quote_summary                 견적 안내         고객      견적 상세 「견적서 문자 발송」  SMS
  ───────────────────────────────────────────────────────────────────────────────────────
  pickup_completed              상차완료 안내      —        🔴 폐지 (라벨·타입은 남김)
  delivery_completed            하차완료 안내      —        🔴 폐지 (라벨·타입은 남김)
```

**① 상차완료·하차완료 문자를 없앴습니다**(사용자 확정: *「알림으로만 충분하다」*).
화주는 **운송관리 알림**(화면 배너 + 웹 푸시)으로 받습니다(§5-17). 이미 PR #73 이후
자동 발송은 없고 수동 버튼만 남아 있었고 실제 발송 이력도 **0건**이었습니다.
🔴 **되살리지 마십시오** — 되살리려면 그 확정부터 뒤집어야 합니다.
🔴 **`SMS_TEMPLATE_LABELS` 의 두 라벨과 `SmsTemplateType` 의 두 값, DB CHECK 의 두 값은
남겨 뒀습니다** — 지우면 `/admin/sms-logs` 와 각 상세 이력 칸에 **영문 코드가 그대로
노출**되고(그 상수를 순회해 종류 필터를 그립니다) 실패 건 **재발송 경로가 타입에서
막힙니다.** 🔴 **재발송은 일부러 막지 않았습니다** — 원문 그대로 다시 보내는 것이라
틀린 문자가 아니고, **새로 만들어지는 경로가 없을 뿐**입니다.

**② 배차확정은 두 통이고 차주가 먼저입니다**(고객 문의 전화에 담당자가 응대가 되려면).

```
  배차확정 저장 성공
    → 확인창 (1/2) 화물정보 안내 — 차주      [건너뛰기] [발송]
    → 확인창 (2/2) 배차확정 안내 — 고객      [건너뛰기] [발송]
```

🔴 **큐는 화면이 듭니다.** 라우트(`/api/admin/notify-dispatch-status`)는 **한 번에 한 통**만
돌려주고, 두 통을 차례로 띄우는 일은 `lib/notifyDispatchSms.ts` 가 합니다 —
**수동 버튼이 한 통만 띄워야** 해서 응답을 배열로 만들지 않았습니다.
🔴 **공용 `SmsConfirmModal` 을 두 통 동시 편집 창으로 뜯어고치지 마십시오** — 그 창을 쓰는
**다른 다섯 곳**(화주신청 승인·거절 · 계정발급 · 비밀번호 재발급 · 견적)이 같이 흔들립니다.
더한 것은 **선택 prop `step` 하나**뿐이고, 안 주면 지금까지와 한 글자도 다르지 않습니다.
🚨 **호출부는 `key={preview.templateType}` 를 같이 주어야 합니다** — 모달이
`useState(preview.message)` 로 초기값을 잡아서, 없으면 **둘째 창에 첫 통의 본문과
수신번호가 그대로 남습니다.** 실제로 `key` 를 빼고 렌더링해 확인했더니 **고객에게 차주
문구가 차주 번호로** 나갔습니다.
🔴 **`취소` 에서는 문자가 나가지 않습니다**(§5-23 — 취소는 읽기 전용이라
`취소 → 배차확정` 전환이 없고, 재배차는 **새 배차 행**이라 그 배차의 확정에서 나갑니다).
🔴 **웹 푸시 호출을 문자 흐름 안으로 옮기지 마십시오** — 확인창 안으로 넣으면
「건너뛰기」에 푸시까지 안 가고, `fetchDispatchSmsPreview()` 안으로 넣으면 **수동 재발송에
푸시가 또 갑니다**(§5-17).

**③ 🚨 차주 정보는 화면에서는 감추고 문자로는 보냅니다.**
**§5-4·§5-23 의 「차주 성명·연락처·차량번호는 화주에게 노출하지 않습니다」는 화면 기준**
입니다. 사용자 확정(2026-09-17): *「고객에게 배차내용을 다시 확인할수 있고 잘못된 상하차지나
차량배차를 사전에 발견할수 있으며 기사와 화주간 연락 착오를 줄일 수 있다.」*
🟢 처리방침 **제4조**가 이미 *「운송을 의뢰한 고객 — 배차 확정 정보 안내 — 차주 성명,
연락처, 차량번호, 차량 종류」* 를 적고 있어서 **그 조가 이제 사실과 맞습니다**(§7).
🔴 **「비노출이라고 적혀 있다」를 근거로 고객 문자에서 기사 정보를 빼지 마십시오.**
🔴 **반대로 화주포털 화면에 기사 정보를 띄우지도 마십시오** — 그쪽은 그대로입니다.

**④ 두 문구의 빈 칸 규칙이 정반대입니다.**

```
  차주 「화물정보 안내」   빈 칸은 줄째 뺀다     「미정」·「-」를 찍지 않는다
  고객 「배차확정 안내」   기사 정보가 비면 「미등록」
```

🔴 차주용은 사용자 원문이 *「기입되어 있는 선안에서」* 이고, 없는 정보를 있는 것처럼 적으면
차주가 그 글자를 보고 전화를 걸 수 없습니다. 🔴 고객용은 **이 문자의 목적이 기사 정보
확인**이라, 줄째 빠지면 담당자가 빠진 줄 모르고 보냅니다 — 확인창에서 「미등록」이 보여야
채우고 보냅니다. **둘을 같게 만들지 마십시오.**
🔴 **차주 문자에 화주 대표 연락처를 넣지 마십시오** — PR #73 이 뺀 이유(*「오더 1건에 화주
연락처가 하나뿐인데 실제로는 상·하차지 담당자가 서로 다르다」*)는 34차가 **상·하차지 담당자
칸을 따로 만들면서 해소됐고**, 들어가는 것은 **그 칸**입니다(배차 값 → 비었으면 오더 값).
🔴 **두 문자 모두에 운임·수수료·마진을 넣지 마십시오** — 분쟁 때 그 숫자가 기준이 됩니다.
🔴 **고객 문자에는 품목·특이사항도 넣지 않습니다**(특이사항에 내부 메모가 섞입니다).

**⑤ 🔴 차종은 「실제로 온 차」가 먼저입니다.**
`dispatches.external_vehicle_type` 을 신설했습니다(배차 상세 외부 배정 분기의 입력칸).
내부 배정은 `drivers → vehicles(vehicle_type)`, 외부 배정은 이 칸, **둘 다 비었을 때만**
오더의 **요청 차종**으로 떨어집니다. 🔴 **요청 차종을 먼저 쓰면 그 문자가 스스로를 확인해
「요청대로 왔다」고 항상 말하게 되어** *「잘못된 차량배차를 사전에 발견」* 한다는 목적이
무너집니다. ⚠️ **운영 배차는 전부 외부 배정입니다**(22건 · `_verify.sql` ㉚-f) — 이쪽이
기본 경로입니다.

**⑥ 🚨 날짜는 한국 시각으로 뽑습니다 — `getHours()`·`getDate()` 를 쓰지 마십시오.**
문구 함수는 **서버 라우트**에서 돌고 Vercel 함수의 시간대는 **UTC** 입니다. 실측:

```
  KST 2026-09-18 00:00  →  getHours() 15      9시간 어긋남
  KST 2026-09-18 09:00  →  getHours()  0      **날짜까지 하루 앞으로 밀립니다**
```

🟢 배차확정 문자 발송 이력이 **0건**이라 실피해는 없었고(`_verify.sql` ㉚-c), 배차확정 두 통을
만들면서 **같이 고친 기존 결함**입니다. 견적 폴백 LMS 도 같은 함수를 써서 함께 맞아졌습니다.
🔴 `Intl` 에 `timeZone: "Asia/Seoul"` 을 주는 방식을 되돌리지 마십시오.

**⑦ 🔴 하차 시각 `23:59` 는 시각이 아니라 자리 채움입니다**(당착·내착 · `lib/arrivalType.ts`).
운영 오더 **3건**이 그렇습니다(㉚-g). 두 문자 모두 그런 건은 **날짜만** 찍습니다.
🔴 **그 시각을 문자열로 다시 적지 마십시오** — 정의처는 `ARRIVAL_FILLER_TIME` 하나입니다.

**⑧ 머리말은 `SMS_HEADER` 하나입니다**(사용자 확정: *「머리말은 [위캐리운송]으로 통일.」*).
7종이 전부 이 상수를 쓰고 **견적 LMS 제목**도 같습니다(그 제목에는 띄어쓰기가 있었습니다).
🔴 **이메일 2건의 영문 머리말은 범위 밖입니다**(Resend 미가동).
🔴 **이미 나간 문자의 본문·제목은 고치지 않습니다** — `sms_logs` 를 UPDATE 하지 마십시오.

**⑨ 🔴 견적만 SMS 이고 여유가 3byte 입니다.**
「8종이 전부 LMS」는 **낡았습니다** — 견적안내가 2026-09-15 에 **견적서 링크 문자
(87byte SMS)**로 대체됐습니다(§5-15). 🔴 **문구를 고치면 반드시 byte 를 다시 재십시오** —
넘으면 에러가 아니라 **요금과 알림창 표기만 조용히 달라집니다.**
🚨 **제목 판정은 `smsSubjectFor()` 한 함수입니다.** 그전에는 `send-sms` 와
`sms-logs/resend` 두 곳에 따로 적혀 있었고 **재발송 쪽이 길이를 안 봐서**, 87byte 링크
문자를 재발송하면 제목이 붙어 **LMS 로 나갔습니다**(솔라피는 제목이 있으면 단문을 올립니다).
🔴 **`template_type` 만 보는 쪽으로 되돌리지 마십시오** — 담당자가 확인창에서 본문을 길게
고치면 그때는 LMS 가 맞으므로, 판정 기준은 **종류가 아니라 본문 길이**입니다.

**⑩ 견적 상세 버튼은 「견적서 문자 발송」입니다.**
옆 두 버튼이 「견적서 출력 (PDF)」·「(Excel)」이라 **무엇을 어떻게 보내는지**가 한 형식으로
읽혀야 합니다. 🔴 **「링크 문자 발송」으로 바꾸지 마십시오** — 링크 생성이 실패하면 **옛 요약
LMS 로 내려가서**(`send-quote-sms`) 그때는 틀린 이름이 됩니다.

**정의처**

```
  lib/sms/templates.ts       문구 · SMS_HEADER · 제목 판정(smsSubjectFor) · KST 포맷터
  lib/smsLogLabels.ts        라벨(9종 — 폐지 2종 포함) · 받는 사람 · 상태
  lib/sendSms.ts             SmsTemplateType(9종) · 발송 + sms_logs 기록
  lib/notifyDispatchSms.ts   상태 → 문자 매핑 · 자동은 배열, 수동은 단건
```

### 5-26. 🔴 화주포털 「접수」 카드 · 공개 폼 모바일 (PR #177, 2026-09-18)

**화주가 견적을 승인한 순간부터 화주포털에 그 건이 보입니다.** 그전에는 담당자가 오더·배차를
만들기 전까지 배차·운송 조회와 홈이 **통째로 비어 있었습니다**(두 화면이 `dispatches` 만
읽는데 그 행이 아직 없기 때문입니다).

🔴 **오더를 자동으로 만들어서 메우지 마십시오** — 27차 확정이고 사유가
`app/api/customer/approve-quote/route.ts` 에 적혀 있습니다(같은 견적에 오더가 **두 번**
생기고, 담당자가 화주 조건을 확인하는 흐름을 화주가 건너뜁니다). **DB 에 아무것도 만들지
않는 「가상 카드」**입니다 — 그래서 이 작업의 **DB 변경이 0**입니다.

```
  정의처   lib/portalPendingDispatches.ts   🔴 배차·운송 조회와 홈이 같이 씁니다
  세는 것  ① 수주 견적인데 오더가 없다   ② 오더는 있는데 배차가 없다
  가상 행  dispatch_status·pickup_confirmed·delivery_confirmed = null → 단계 0(접수)
           order_id = null          채우면 취소 배지가 붙을 길이 생깁니다
           updated_at = null        화주 본인의 승인에 「업데이트」 알약이 뜹니다
           created_at               approved_by_customer_at (없으면 created_at)
  번호     오더번호가 없으면 「견적 20260918007」 — 🔴 접두어를 빼지 마십시오
```

🔴 **①만 만들지 마십시오** — 오더가 생기는 순간 카드가 사라졌다가 배차 후 다시 나타나
화주 눈에는 **건이 없어졌다 돌아온 것**으로 보입니다.
🔴 **②의 상태 조건(`접수`·`배차중`)을 지우지 마십시오**(관리자 「+ 배차 등록」 후보와 같은
두 단계 판정 — 지우면 운송이 끝난 오더까지 「접수」로 되살아납니다).
🔴 **취소 배차가 있는 오더는 안 걸립니다**(취소도 `dispatches` 행입니다) —
`lib/portalCancelledDispatches.ts` 와 **합치지 마십시오**(저쪽은 「있는 배차를 어떻게
보여줄까」, 이쪽은 「없는 배차를 어떻게 메울까」입니다).

**공개 폼 두 화면(`/apply`·`/quote`)의 모바일 표시 — 확정된 것**

```
  동의 카드 회색 설명글   ≤700px 에서 감춘다 (두 화면 모두)
  🔴 반드시 남기는 둘     카드마다 「전문 보기」 · 카드 아래 거부권 문단
  데스크탑                그대로 보인다
```

🚨 **처음에는 약관 요약만 감췄습니다** — 개인정보 카드의 설명이 **수집·이용 목적
고지**(개인정보보호법 제15조 2항 1호)라서입니다. 보고했더니 **사용자가 두 카드 모두 +
`/quote` 까지 빼기로 확정**했습니다. 🔴 **그 옛 판단을 근거로 되살리지 마십시오.**
🔴 **「전문 보기」와 거부권 문단까지 감추면 그때는 정말로 법정 고지가 사라집니다.**
⚠️ `/quote` 에서 감춰지는 문장에는 **보유기간(1년)도** 들어 있습니다.

🔴 **`/quote` 거부권 한 줄은 이번에 처음 생겼습니다**(그 화면만 카드를 직접 그려서 통째로
없었습니다) — 부품은 **`ConsentRefusalNote` 하나**, 문구 정의처는
**`lib/legalInfo.ts` 의 `CONSENT_REFUSAL_NOTICE` 하나**입니다(`TERMS_CONSENT.refusal` 은
그 값을 가리키는 별명). 🔴 **감춤 대상이 아닙니다.**

**그 밖에 되돌리면 안 되는 것**

- 🔴 `/apply` 전화 카드 문구는 **「전화로도 신청 가능합니다」**입니다 — 권유형(「…하세요」)
  으로도, 중간 판본(「전화로 신청도 가능합니다」)으로도 되돌리지 마십시오.
- 🔴 담당자 이메일은 **`[아이디] @ [도메인]` + 고르기 11종**(`EmailField`)이고 **저장값은
  여전히 문자열 하나**입니다. **별도 state 를 만들지 말고**(값에서 갈라 읽습니다)
  **도메인 칸을 잠그지 마십시오**(고르기는 바로가기입니다).
- 🔴 `/apply` 전화 카드는 모바일에서 **271 → 165px** 입니다. **문구는 한 글자도 안 지웠고**
  데스크탑은 그대로입니다.
- 🔴 모바일 폼 항목 제목은 **15px · 700 · `#4A4945`** 이고 손잡이는
  **`landing-field-label`** 입니다(`fieldLabel` 이 인라인 style 이라 CSS 로는 못 이깁니다).
  **새 라벨에도 같이 다십시오. 16px 로 올리지 마십시오**(입력값과 같아집니다).
- 🔴 **포털에서 `lib/unlinkedWonQuotes.ts` 를 import 하지 마십시오** — 맨 위에서 관리자
  클라이언트를 import 해서 포털 번들에 관리자 세션이 딸려 들어갑니다(원칙 1번). 그래서
  `WON_QUOTE_STATUS` 정의처를 의존성 0인 **`lib/quoteStatusLabels.ts`** 로 옮겼습니다.


## 6. 진행 현황

| 차수 | 내용 | 상태 |
|---|---|---|
| 15–17 | 거리 매칭 버그 · 운임 기준 교체 · 차급 9종 확장 | 완료 |
| 18–22 | 약관 동의 · anon RLS 정리 ①②④ · 운임 보정 + 차급 11종 | 완료 |
| 23–27 | 포털 사전조사 · 셸/홈 · 발주 흐름 · 시안 정합 · 견적 흐름 | 완료 |
| 28–29 | 내부관리 연동 조사 · 포털 조회 흐름 | 완료 |
| 30–31 | 랜딩 교체(이미지 32종) · 폼 3화면 | 완료 |
| 차수 없음 | 도메인 확정 · 랜딩 소수정 · 랜딩 2차 개편 + 법적 문서 · 이용약관 개정 · 메일 로고 · 고정화물 사진 · 대표메일 교체 · **랜딩 WHY + 모바일**(70차) | 완료 |
| 차수 없음 | **이용약관 면책 보강 terms-v3**(PR #125 `cd20bfb`) — 회사의 배상 선언 제거 + 책임 귀속을 첫 문장으로 · 면책은 개별 호 문구만 조임. 🔴 **DB 0 · 화면 코드 0** · 실사용 리뷰 **4라운드**. 🔴 **merge = 게시가 아님**(§5-5 · 아래 「코드 밖 할 일」) | 완료 |
| 차수 없음 | **네이버 서치어드바이저 등록**(2026-09-04) — 사용자가 `main`에 직접 올림(`50d6c8b`). 🔴 **코드 변경 0**, 세션은 서빙 확인만 | 완료 |
| 차수 없음 | **SEO 메타 보강**(PR #118 `887f6ff`) — Organization JSON-LD 신설 · description 4곳 80자 이내 · 길이 가드. 🔴 **DB 0 · 화면 변화 0** | 완료 |
| 차수 없음 | **카피라이트 + 랜딩 모바일 이미지**(PR #119 `4580959`) — 상호 병기 3곳 · 첫 서비스 사진 `eager`로 팝인 371ms → 0 · 랜딩 이미지 10.9MB → 5.5MB. 🔴 **DB 0** | 완료 |
| 차수 없음 | **무료 대기시간 단위 확정**(2026-09-04) — 25차부터 답 대기하던 「초과 시 30분당」을 **현행 유지**로 닫음. 🔴 **코드 0 · DB 0**(`verify` 1회만) | 완료 |
| 차수 없음 | **불러온 프리셋 이름 표시**(PR #120 `77e8f29`) — 26차부터 답 대기하던 항목. 불러오기 드롭다운 옆에 이름을 보조로. 🔴 **DB 0** | 완료 |
| 차수 없음 | **담당자 정보 화면 시안 정합**(PR #121 `ae8ddfb`) — 포털에서 마지막까지 남아 있던 화면. 🟢 **이것으로 화주포털 8개 화면이 전부 pv2 스코프다.** 🔴 **DB 0 · 관리자·공개 화면 0** | 완료 |
| 차수 없음 | **보유기간 만료 파기 장치**(PR #122 `4010940`) — 처리방침 제3조가 고지한 「견적문의 1년 · 미승인 신청 6개월」을 실제로 지우는 경로. 🔴 **DB 변경 있음**(`processed_at` + `retention_purge_logs`, `_migrations` **23행**) · 매일 03:10 KST `purge.yml` | 완료 |
| 차수 없음 | **랜딩 스크롤 진입·상태 전환 모션**(PR #123 `1375d3d`) — 리빌 34개 · 아코디언 높이 전환 · 모달 등장 · 헤더 상태. 🔴 **DB 0 · `globals.css` 0 · `lib/` 0 · 관리자·포털 0** · 실사용 리뷰 **2라운드** | 완료 |
| 차수 없음 | **화주포털 월별 통계 기간 버그 + 엑셀 보기 개선**(PR #124 `06880e1`) — 「최근 3/6/12개월·전체」가 전부 빈 화면이던 것을 고침(하한이 **시작월**에 걸려 있었다) · 엑셀 열 폭을 데이터 기준으로 · 본문 10pt · 다운로드 버튼에 엑셀 아이콘. 🔴 **DB 0 · 견적서 엑셀 무변경(md5 동일)** | 완료 |
| 차수 없음 | **설치형 앱(PWA) 전환**(PR #126 `e0be57c`) — 포털·내부관리를 홈 화면 앱으로. 🔴 **DB 0 · 신규 의존성 0 · 랜딩 0줄** · 41화면 × 323지표 회귀 0. 🔴 **middleware 가 `/admin/` 아래 정적 파일을 가로채던 것**을 이때 발견·수정 | 완료 |
| 차수 없음 | **모바일 로그인 첫 화면 + 카톡 브라우저**(PR #128 `7a850f0`) — 🔴 **「확장되서 움직인다」의 원인은 `100vh`**(모바일에서 주소창 숨김 기준 높이) → `.screen-fit`(`100vh` 폴백 + `100dvh`) · 🔴 **운송관리 로그인에 같은 로고가 두 번 그려지고 있었음**(공용 헤더 + 자체 헤더) · 모바일 한 화면 맞춤 1,316 → **726px** · 🔴 **카톡 자체 브라우저 갈래 신설**. 🔴 **DB 0 · 신규 파일 0** | 완료 |
| 차수 없음 | **설치형 앱 후속 수정**(PR #127 `48c14e5`) — PR #126 의 결함 셋. 🔴 **구역 끝 슬래시**(주소 띠 원인) · 🔴 **서비스워커가 홈을 못 맡아 홈에서 오프라인 안내가 안 뜸** · 🔴 **설치 신호를 하이드레이션 뒤에 들어 놓침** + 지운 뒤 다시 설치할 길이 없던 것 · 버튼을 제목 줄 오른쪽 끝으로. 🔴 **DB 0 · 랜딩 0줄** | 완료 |
| 차수 없음 | **모바일 입력창 확대 + 상단 뒤로가기·로그아웃**(PR #129 `a71ceae`) — 🔴 **확대의 원인은 16px 미만**이고 아이폰 사파리는 **되돌리지 않아 로그인 뒤에도 커진 채 남습니다**. `≤700px` 입력요소를 16px `!important`(인라인 style 을 이겨야 해서). 설치형 앱엔 브라우저 뒤로가기가 없어 상단 「←」 신설(홈에서는 안 그림). 🔴 **DB 0** · 실사용 리뷰 **3라운드** | 완료 |
| 차수 없음 | **안드로이드 설치 원인 + 탭 하이라이트**(PR #130 `9f269ce`) — 🔴 **「설치가 안 된다」와 「아이콘이 노랗다」는 한 뿌리**였습니다: 앱이 아니라 **「바로가기」**가 만들어지고 있었고(바로가기는 manifest 를 안 보고 파비콘을 씁니다), 원인은 랜딩이 서비스워커 구역 밖이라 `next/link` 로 들어간 문서가 **영영 제어를 못 받는 것**. 진입 링크 3곳만 `<a>` 로(원칙 31번의 의도된 예외). 🔴 **아이콘 파일을 고치러 가지 마십시오** | 완료 |
| 차수 없음 | **법적 문서 3종 화면 정리**(PR #131 `44e1b61`) — 옐로 헤더(37차)의 메뉴가 이 세 화면에만 남아 있던 것을 로고만 있는 `PublicPageHeader` 로 교체 + 푸터 페이지 링크도 끔. 🔴 **`/terms`·`/privacy`·`/email-policy` 라우트는 살려 둡니다**(분쟁 시 시점 지목 · `robots.ts` · `/apply` 전문 보기). 🔴 **DB 0** | 완료 |
| 차수 없음 | **회사소개·차량요금 안내 화면 삭제**(PR #132 `811424b`) — `/about`·`/vehicles` 를 라우트째. 진입 경로가 이미 다 끊겨 고아였던 것을 정리한 것입니다(사용자 확정: *"나중에 필요하면 새로 다시 만들면 된다"*). 🔴 **`components/LandingHeader.tsx`(37차)가 파일째 없어졌습니다** — `components/landing/LandingHeader.tsx` 와 **다른 파일**입니다. 🟢 프리렌더 실패 44 → **43** | 완료 |
| 차수 없음 | **이용약관 추가 개정 terms-v4**(PR #133 `6e35f54`) — 제19조 1항에 「배상책임은 차주 및 그 보험자에게 있으며」를 앞세우고, 제20조에서 **14일 통지 호와 1년 소멸시효 항을 삭제**(4개 항). 🔴 **DB 0 · 화면 코드 0** · ⚠️ **변호사 검수 지점이 여섯이 됐습니다**(§5-5) | 완료 |
| 차수 없음 | **CLAUDE.md 세션 기록을 `docs/history/` 로 분리**(PR #134 `6b70a5a`) — 기록 56건 + §4 를 8개 파일로. **한 글자도 안 지웠고 md5 로 대조**(`51e5e1d0…`). 루트 **654,627 → 138,993자**. 🔴 **코드 0 · DB 0 · 화면 변화 0.** 🔴 **`/merge` 규칙이 본체입니다** — 새 기록은 `docs/history/current.md` 맨 위, 이 문서도 **매 차수에 머리 버전 줄 + 이 표**를 갱신합니다 | 완료 |
| 차수 없음 | **운임기준표 v11 — 화주 청구가 165칸 교체(A장)**(PR #135 `1da2663`) — 🔴 **DB 변경 있음**(`_migrations` **24행**) · 🟢 **제품 코드 0.** 165칸 중 **164칸 인상**(평균 ×1.22 · 인하 0). 🔴 **×1.15 예외 세 칸은 공개 게시가**이고 근거 엑셀과 다른 것이 정상입니다(§5-1). 🔴 **B장(혼적 3단계)은 아직**입니다 | 완료 |
| 차수 없음 | **운임기준표 v11 — 혼적 할인 거리 3구간(B장)**(PR #136 `fceb11f`) — 🔴 **DB 변경 있음**(`mixed_loading_discount_settings` 1행 → **3행** · `_migrations` **25행**). 🟢 **이것으로 v11 지시서가 A·B 둘 다 끝났습니다.** 🔴 **지시서 3-2 「거리가 계수를 고르게」를 C안으로 대체**(사용자 확정) — 표준값은 **계산에 안 쓰이는 입력창 기본값**이고 `orders` 에 거리가 없어, 거리가 계수를 정하면 담당자 건별 조정값을 덮어쓰고 견적과 오더가 갈립니다. 담당자가 표준과 다른 값을 넣으면 **막지 않고 「표준과 다름」으로 보이기만** 합니다 | 완료 |
| 차수 없음 | **문서 정합 정리**(`CLAUDE.md`·`HANDOFF.md`) — 🔴 **코드 0 · DB 0 · `docs/history/` 0.** v11 이 해소한 「5톤급 재검토」가 `CLAUDE.md` 에만 「아직 안 한 것」으로 남아 두 문서가 갈려 있던 것 · 「원칙 NN번」 오참조 **2건**(전수 확인으로 1건을 더 찾았습니다) · 이 문서 다섯 곳. 🔴 **판본은 이제 머리 줄 한 곳에만 적습니다** — §0 표·부록 헤더에 적었더니 세 판본째 갈렸습니다 | 완료 |
| **32** | **내부관리 로그인·계정 개편**(PR #137 `1f97fc7`) — 직원 로그인을 **이메일 → 아이디**로 바꾸고 **관리자가 직원 비밀번호를 재발급**할 수 있게 했습니다. 🔴 **DB 변경 있음**(`staff_accounts` 에 `login_id`·`must_change_password` + `lower(login_id)` 유니크 · `_migrations` **26행**). 🔴 **진짜 이유는 편의가 아니라 「비밀번호를 잊으면 아무도 풀어줄 수 없었다」**입니다(`RESEND_API_KEY` 미등록 · SMTP 미연결). 🔴 **Auth 에 저장된 직원 이메일은 안 바꿉니다** — 포털의 합성 이메일 방식으로 갈아치우면 나중에 Resend 를 켰을 때의 유일한 복구 경로가 사라집니다 | 완료 |
| 차수 없음 | **이용약관 제19조 1·2·7항 개정 terms-v5**(PR #138 `a9b5ccc`) — 옛 2항(가액 신고제)을 1항으로 올리고 옛 1항 문안을 교체, 고의·중과실 예외를 「제1항부터」로. 🔴 **DB 0 · 화면 코드 0** · 🔴 **「인도할 날의 도착지 가격」을 되살리지 마십시오**(§5-5) · ⚠️ **변호사 검수 지점이 일곱이 됐습니다** | 완료 |
| **33** | **화주 항목 정합 + 정기계약 (A장 + B장)**(PR #139 `5fef0d1`) — 화주가 만들어지는 입구 **셋**(등록·수정·승인)이 항목 목록을 각자 들고 있어 갈려 있던 것을(**등록 25 · 수정 54 · 승인 19**) `lib/companyFields.ts` **한 정의처**로 모으고, 정기계약 5개 컬럼을 신설해 배지를 화주·견적·오더·배차 목록과 운영 대시보드로 확산했습니다. 🔴 **DB 변경 있음**(`companies` 정기계약 5컬럼 + 부분 인덱스 · `_migrations` **27행**). 🔴 **`companies.status` 에 「정기계약」을 넣지 않았습니다 — 축이 다릅니다**(상태 = 지금 어느 단계인가 / 정기계약 = 어떤 관계인가). 🔴 **기존 `repeat_customer` 를 재사용하지 않았습니다** — 정산이 `오더수 > 1` 이면 **자동으로 덮어쓰는 실적 파생값**이라 담당자가 켠 값이 조용히 사라집니다. 🟢 **화주포털 0줄 · 공개 화면 0줄** · 실사용 리뷰 **4라운드** | 완료 |
| 차수 없음 | **정기계약 배지 후속**(PR #140 `079dd3b`) — 33차가 빠뜨린 **활성 화주 목록**(`/admin/customers`)에 배지를 붙이고, 실사용 지적으로 **배지를 회사명 위 줄로**(화주 관리·활성 화주·견적·오더·배차 **다섯 목록 전부**) + 활성 화주 목록에 **「정기계약만 보기」**. 🔴 **호출부에서 `<div>` 로 감싸 올리지 마십시오** — 배지가 없는 화주는 컴포넌트가 `null` 을 돌려주는데 바깥 `<div>` 는 남아 **빈 줄로 칸이 벌어집니다**(그래서 `block` prop 이 감싸는 일까지 합니다). 🔴 **DB 0 · 화주포털 0줄 · 공개 화면 0줄** | 완료 |
| **34** | **견적관리 개편 — 화주포털 발주요청과 정합**(PR #141 `8ddb59a`) — 견적 입력을 포털과 같은 네 블록·**출발지 열 / 도착지 열**로, 견적서 출력을 화면 안 모달로, 대기 중 발주요청을 견적목록 맨 위 고정 + 중복 전환 차단. 🔴 **맞춘 것은 배치이지 부품이 아닙니다** — 포털 부품(`Pv2Select`·`.pv2-*`)을 **하나도 안 가져왔습니다**(`.portal-v2` 스코프 전용이라 끌어오면 관리자 31화면 CSS 가 딸려옵니다). 🔴 **「견적관리 알림이 안 없어진다」의 원인은 오더가 견적과 안 이어진 것**이었습니다(실측 오더 6건 중 **3건이 `quote_id` 없음**) — 세는 규칙을 `lib/unlinkedWonQuotes.ts` **한 정의처**로 모으고 **오더 상세에 「견적 연결」**을 신설했습니다(없으면 배지를 지우려고 **중복 오더**가 생깁니다). 🔴 **DB 0 · `_migrations` 27행 유지 · 화주포털 0줄 · 공개 화면 0줄** · 실사용 리뷰 **5라운드** | 완료 |
| 차수 없음 | **다마스·라보 차급 신설 (11차급 → 13차급)**(PR #142 `8c0f251`) — 다마스·라보로 실을 짐도 담당자가 **1톤으로 견적**을 내던 것을 없앴습니다. 🔴 **DB 변경 있음**(`rate_distance_tiers` 165 → **195행** · `rate_vehicle_extra_fees` 11 → **13행** · `_migrations` **28행**). 🔴 **값은 표가 정본이고 수식으로 재생성하면 안 됩니다** — 라보 `400km 초과` 가 285,000 × 0.90 = 256,500 이라 반올림 방식에 따라 **256,000 / 257,000** 으로 갈립니다(확정값 **256,000**). 🔴 **`VEHICLE_TYPES_ALL`(13) 과 `VEHICLE_TYPES_PUBLIC`(11) 의 값이 처음으로 갈렸습니다** — 다마스·라보는 **공개 폼(`/quote`·`/apply`)과 랜딩 게시가에 넣지 않습니다** — 🔴 **화주포털 발주요청에서는 화주가 직접 고릅니다**(사용자 확정 2026-09-11). **「내부 전용」이 아니니 그렇게 적지 마십시오.** **같은 값으로 맞추지 마십시오.** 🔴 **`DEFAULT_VEHICLE_TYPE` 신설**(지시서에 없던 것) — 맨 앞에 넣으면 `VEHICLE_TYPES_ALL[0]` 이 「다마스」가 되는데 그 자리를 쓰던 **일곱 군데 중 둘이 화주포털**이라 **톤수를 안 건드린 발주가 전부 다마스로 접수**될 뻔했습니다. 🟢 **화주포털 화면 0 · 공개 화면 0 · `globals.css` 0 · 랜딩 0** · 실사용 리뷰 **1라운드**(운임기준표 줄바꿈) | 완료 |
| 차수 없음 | **견적관리 폼 시안 정합**(PR #143 `5fcb9fa`) — 34차가 맞춘 것은 **배치**였고 입력칸 생김새가 남아 있던 것을 좁혔습니다. 🟢 **두 폼을 나란히 재 봤더니 다른 것은 라벨과 배경 둘뿐**이라, 포털 부품을 끌어오지 않고 그 둘만 맞췄습니다(**「폼이 다르다」는 지적을 받으면 먼저 재십시오**). 🔴 **`.quote-form` 스코프를 신설했습니다** — `.field` 는 관리자 **31화면**이 공유하니 전역으로 고치지 마십시오(34차 `.admin-wide` 와 같은 결의 장치입니다). 🔴 **CSS 색이 리터럴인 것은 의도입니다** — `.pv2-*`·`--pv2-*` 를 끌어오면 포털 CSS 가 통째로 딸려옵니다. 값은 포털 **실측치를 옮겨 적은 것**이고 포털 토큰이 바뀌어도 따라가지 않습니다. 🔴 **필수항목 옅은 강조가 사라졌던 것은 「없던 기능」이 아니라 제가 덮어쓴 것이었습니다** — 새 스코프 규칙(0,0,3,1)이 기존 `.req-marks .field-required input`(0,0,2,1)을 이겼습니다. **순서로 고치지 말고**(원칙 58번) `.quote-form.req-marks`(0,0,4,1)로 **특성도를 올려** 고쳤습니다. 🔴 **스코프 클래스를 새로 만들 때는 그것이 무엇을 덮어쓰는지 먼저 보십시오.** 🔴 **개인·신규 고객의 필수는 고객명이 아니라 연락처입니다**(사용자 확정) — 이름이 비면 `guest_name` 에 빈 문자열이 아니라 **`null`** 을 넣습니다(목록·상세가 `guest_name ||` 로 대체 표기합니다). ⚠️ **블록 번호 배지 색은 그때 답을 못 받아 파랑으로 남겨뒀습니다** — 🟢 **바로 아래 차수(PR #145)에 옐로가 됐습니다.** 이 줄을 근거로 파랑으로 되돌리지 마십시오. 🟢 **DB 0 · `_migrations` 28행 유지 · 화주포털 0줄 · 공개 화면 0줄 · `globals.css` 순수 추가(삭제 0)** · 실사용 리뷰 **1라운드(지적 3건)** | 완료 |
| 차수 없음 | **화주 목록 페이지 나누기 + 모바일 카드**(PR #144 `143e55d`) — 화주 **539건**을 한 번에 그리던 두 목록을 **15행씩**으로 줄였습니다. 🔴 **지시서 전제 다섯 개가 실제와 달랐고 그중 3번이 「보고 후 대기」 조건이라 실제로 멈추고 보고했습니다** — `/admin/crm` 은 없고(`/admin/customers` 입니다) · 필터·검색·정렬이 **전부 클라이언트** · **두 화면 다 모바일 카드가 없었습니다**. 🔴 **서버 전환이 아니라 클라이언트 나누기입니다**(사용자 (C)안 확정) — 정렬 키 6개 중 셋이 DB 컬럼이 아니라 JS 계산값(`formatRegion`·`formatIndustry`·`STATUS_OPTIONS.indexOf`)이라 PostgREST `.order()` 로 옮기려면 **DB 변경**이 필요하고 검색도 결과가 달라집니다. **되돌리지 마십시오.** ⚠️ 그래서 지시서 완료조건 2(페이지마다 새 요청)만 **설계상 불충족**입니다. 🔴 **실측이 계획 하나를 지웠습니다** — `/admin/customers` 의 `dispatches` 전수조회를 고칠 계획이었는데 `verify` ⑮ 가 **배차 6행 · 오더 10행**(전체화주 539 · 활성화주 7)을 보여줘 **안 고쳤습니다.** 「무거워 보인다」로 착수하지 말고 재고 시작하십시오. 🔴 **페이지당 건수는 `LIST_PAGE_SIZE` 한 곳(15)** 이고, **페이지 줄은 카드의 푸터**입니다(`border-top` 을 빼지 말 것 · 좌우는 표 칸 여백에 맞춤 — `.table-compact` 화면은 `<ListPagination compact />`). 🔴 **「폼 열림 시 목록 미렌더」 분기를 없앴습니다 — 다시 만들지 마십시오.** 🟢 **DB 0 · `_migrations` 28행 유지 · 화주포털 0줄 · 공개 화면 0줄 · `globals.css` 순수 추가(+119 / −0)** · 실사용 리뷰 **2라운드**(50 → 15행 · 카드 푸터) | 완료 |
| 차수 없음 | **견적 폼 블록 번호 배지 옐로**(PR #145 `b0c3f2d`) — PR #143 이 답을 못 받아 파랑으로 남겨둔 **한 줄**입니다(사용자 확정 *"배지 색도 화주포털처럼 옐로우로 바꿔줘"*). 🔴 **`var(--brand-yellow)` 입니다 — `#FFD833` 리터럴로 다시 적지 마십시오**(34차 계산창 손잡이와 같은 규칙). 🔴 **`var(--pv2-yellow)` 를 쓰지 마십시오** — 그 토큰은 `.portal-v2` 스코프 안에만 있어 관리자에서는 **옐로가 나오지 않습니다**(실측: 상속값 `rgb(25,31,40)`). 값은 `--brand-yellow` 와 같습니다. 🟢 **글자색은 안 건드렸습니다** — `#1a1a1a` 가 원래부터 포털 `--pv2-text` 와 같은 값이라 배경만 맞추면 포털과 완전히 같아집니다. 🔴 **같은 화면의 다른 파랑(탭 버튼·라디오·계산 결과 카드)은 그대로입니다** — 지시는 **배지**였고 관리자 전체 강조색을 바꾸는 것은 다른 크기의 작업입니다. 🟢 **DB 0 · `_migrations` 28행 유지 · 화주포털 0줄 · 공개 화면 0줄 · `globals.css` 0줄 · 파일 1개** · 리뷰 라운드 0 | 완료 |
| **35** | **내부 정합 ① 오더·배차·정산 + 운영 대시보드**(PR #146 `9abca84`) — 사용자가 배포본에서 짚은 **13건**을 A·B·C 세 장으로 처리했습니다. 🔴 **DB 변경 있음**(`dispatches`·`invoices`·`orders` 부가세 구분 등 10컬럼 · `_migrations` **29행**). 🔴 **선착불 정산 모델이 틀려 있었습니다** — 종전 코드가 수금방식과 무관하게 「받을 돈 = 화주 청구액」으로 정산 건을 만들어 **위캐리를 거치지도 않는 운임이 미수금**이었습니다. 이제 **받을 돈 = 주선수수료 · 줄 돈 = 0**이고, 받을 수수료는 `brokerage_fee_paid` 로 **따로** 남습니다(한 칸으로 뭉개지 마십시오). 🔴 **마진 공식이 정산 화면 한 곳에만 셋**이었습니다(등록 미리보기 / 저장값 / 대시보드) — `lib/marginCalc.ts` 하나로 모았고 **표시 시점 계산이라 옛 건도 자동으로 맞습니다**(저장값 `commission_total` 에서 읽지 마십시오). 🔴 **옛 정산 건을 고칠 경로가 아예 없었습니다** — 금액 칸이 읽기 전용이고 저장 화이트리스트에도 없어, 배차 기준 재동기화 API 를 신설했습니다(원칙 47번 위반이 아니라 **틀리게 얼려진 기준 금액을 바로잡는 것**이고, 추가비는 `created_at <= invoice.created_at` 인 것만 넣습니다 — 빼면 **두 번 청구**). 🔴 **부가세를 건별로 확정합니다** — 합계에 ×1.1 을 하면 25,000원 3건이 74,999원이 되고, 포함가를 되계산하면 **11,000원마다 1원**이 어긋납니다(**24시콜 세금계산서와 갈리던 원인**). 🟢 **화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `globals.css` 0줄 · `lib/legal/` 0줄** · 실사용 리뷰 **8라운드** | 완료 |
| 차수 없음 | **랜딩 히어로에 「운송관리 계정 신청」 버튼**(PR #147 `2ac879a`) — 전화 문의 버튼 **아래에** 세로로 쌓았습니다. 🔴 **옛 결정 둘을 뒤집은 것입니다** — `app/page.tsx` 주석의 「새 버튼을 만들지 말 것」(2026-09-02)과 41차의 「히어로에 세 번째 선택지를 다시 넣지 말 것」. **그 줄들을 근거로 빼지 마십시오**(주석과 `CLAUDE.md` 두 곳에 뒤집힘 표시를 달아뒀습니다). 🔴 **넷째는 넣지 않습니다** — 견적은 헤더 CTA·마감 CTA 에 있습니다. 🔴 **옐로 `#FFD834`** 는 랜딩의 계정 신청 네 자리가 같은 색이어야 같은 행동으로 읽히기 때문이고, 전화가 검정 채움+그림자로 여전히 1순위입니다. 🔴 **`landing.css` 의 히어로 위 여백 215px(1321px 이상 전용)을 지우지 마십시오** — 히어로가 세로 가운데 정렬이라 버튼이 늘면 제목이 절반만큼 올라가, 1366~1440px 에서 **제목 끝 글자가 차체 로고에 가려 안 읽혔습니다**(원칙 54번과 같은 자리). **버튼을 더하거나 빼거나 크기를 바꾸면 이 값도 같이 고치십시오.** ⚠️ **가로 겹침은 원래 있던 것**이고(1440에서 288px, 전후 동일) 바뀐 것은 세로 47px 뿐입니다 — 스크린샷만 보면 오판하는 자리라 재서 갈랐습니다. 🟢 **DB 0 · `globals.css` 0줄 · 관리자 0줄 · 화주포털 0줄 · `lib/` 0줄** · 파일 2개 | 완료 |
| 차수 없음 | **거래처 온보딩 안내 페이지 `/guide` 신설**(PR #148 `931ec79`) — 계정을 발급받은 거래처가 **첫 로그인에서 멈추던 것**을 막는 공개 안내 화면입니다(지금 안내할 수 있는 것이 담당자 전화뿐이었습니다). 공개 경로 하나 + 진입 링크 **3곳**(랜딩 운송관리 소개 카드 · 포털 로그인 · 포털 홈). ⚠️ **지금은 2곳입니다** — PR #149 가 포털 홈 링크를 뺐습니다(로그인한 화주는 포털 메뉴의 「이용가이드」로 갑니다). 🔴 **차수를 소비하지 않았습니다**(PWA PR #126 과 같은 취급). ~~🔴 **`/customer/guide` 를 따로 만들지 마십시오** — 페이지는 하나이고 **포털에 별도 안내 화면이 없는 것이 의도**입니다. 🔴 **포털 메뉴는 여전히 8종**입니다.~~ → 🔴 **셋 다 같은 날 오후에 뒤집혔습니다**(PR #149 — 아래 행). 사유는 **로고**입니다: `/guide` 는 공개 화면이라 포털에서 들어가면 로고가 **랜딩으로 나가** 돌아올 길이 없습니다. 지금은 **`/customer/guide` 가 있고 포털 메뉴가 9종**이며, 「두 벌이면 한쪽만 낡는다」는 걱정은 **`lib/guideContent.ts` 한 정의처 + `scope`** 로 풀었습니다. 🔴 **이 행의 취소선 문장을 근거로 되돌리지 마십시오.** 🔴 **`/guide` → `/customer/login` 링크만 `<a href>`** 입니다(PR #130 — 서비스워커 구역 밖이라 `next/link` 로 가면 「앱」이 아니라 「바로가기」가 만들어집니다). **왜 한쪽만 다른지 주석에 적어 뒀습니다.** 🔴 **캡처 이미지 0건이 의도입니다** — 낡고, 무겁고, **실계정으로 찍으면 화주 상호·담당자 연락처가 public 저장소에 박힙니다.** 자리표시자도 두지 않고 CSS 도식으로 대신했습니다. 🔴 **아이디 형식을 공개 화면에 적지 마십시오**(리뷰 확정 — 기술적으로 안전한가와 **다른 축의 결정**입니다). 🔴 **로그인 화면의 링크 한 줄과 여백 여섯 곳 축소는 한 벌입니다**(360px **693px** 유지 · 문구는 하나도 안 지웠습니다 · ⚠️ **이제 여백에서 더 뺄 곳이 없습니다**). ⚠️ **지시서 전제 셋이 틀렸습니다** — 로그인 높이 726 → **실측 693px** · 이 화면에 「아이디·비밀번호는 위캐리에서 발급합니다」 문구가 **없습니다** · 랜딩 `<a>` 는 파일상 **한 곳**(그 헤더를 세 화면이 공유). 🟢 **DB 0 · `_migrations` 29행 유지 · 마이그레이션 0 · 신규 의존성 0 · 신규 이미지 0 · `globals.css` 순수 추가(삭제 0)** · 🔴 **프리렌더 44 → 45**(늘어난 항목은 `/guide` 하나) · 실사용 리뷰 **1라운드(지적 2건)** | 완료 |
| 차수 없음 | **문서 정합 정리 3차**(`CLAUDE.md`·`HANDOFF.md`·`docs/history/`) — 🔴 **코드 0 · DB 0 · `migrations/` 0.** 바로 위 `/guide` 차수와 **한 커밋**입니다(두 지시서가 같은 두 파일을 PR 없이 `main` 직접으로 고쳐 따로 하면 충돌합니다). 🔴 **§5-12 「선착불 정산 모델」 신설이 가장 급했습니다** — 그 확정이 이 표의 35차 칸에만 있어서, 새 세션이 §5 를 다 읽어도 「선착불의 받을 돈이 수수료」임을 알 수 없었습니다. 🔴 **앞으로 확정이 생기면 §6 이력 한 줄과 §5 절 하나를 같이 만드십시오.** 그 밖에 §3 메뉴 순서(「외부 정보망」 누락 포함) · §4-3 한 줄 · 원칙 **47번 A-7 예외** · 원칙 **14번 「HANDOFF §3 도 같이」** · §6 24시콜 행에 `lib/autoCreateInvoice.ts`. 🔴 **`CLAUDE.md` §0 표의 `history-07`·`history-08` 자수가 처음부터 틀려 있었습니다**(71,022 → **38,232** · 32,637 → **18,221**) — **데이터 손실이 아니라** 그 값을 써 넣은 커밋(`c37d5b4`)에서 파일을 꺼내 재도 같은 값입니다. **복원하려 들지 마십시오.** 🔴 **`current.md` 가 상한을 넘어 `history-09.md` 로 갈랐습니다**(md5 대조 · 한 글자도 안 지웠습니다) | 완료 |
| 차수 없음 | **화주포털 이용가이드 `/customer/guide` 신설**(PR #149 `45e3179`) — 포털 홈에서 공개 `/guide` 로 들어가면 **로고가 랜딩으로 나가** 돌아올 길이 없던 것을, 포털 셸 **안**의 **항목 선택** 화면으로 대체했습니다. 🔴 **하루 전(PR #148) 확정 셋을 뒤집은 것입니다** — ①「`/customer/guide` 를 만들지 말 것」 ②「메뉴 8종 유지」 ③「포털에 별도 안내 화면 없음」. **「페이지는 하나」를 근거로 되돌리지 마십시오.** 🔴 **「두 벌이면 한쪽만 낡는다」는 걱정은 없앤 것이 아니라 다른 방법으로 푼 것입니다** — `lib/guideContent.ts` **한 정의처 + 노출범위(`scope`)**이고, 자세한 것은 **§5-13** 입니다. 🔴 **정의처에 JSX·className 0건** · 🔴 **`pills` 블록은 글자를 담지 않습니다**(DB 값 `보류`·`실패` 가 박힙니다) · 🔴 **블록 타입이 렌더러가 아니라 정의처에 있습니다**(렌더러가 둘이고 CSS 스코프가 다릅니다) · 🔴 **라우트는 하나이고 항목은 `?topic=`** · 🔴 **모바일은 목록과 본문을 같이 그리지 않습니다**(가르는 일은 **CSS**, ≤700px) · 🟢 **상단 「←」(PR #129)가 이미 `router.back()` 이라 「목록으로」 버튼을 만들지 않았습니다** · 🔴 **아이콘을 10번째로 새로 그렸습니다**(`PortalIcon` 의 `guide` — 이미지 파일 0건, 인라인 path) · 🟢 **포털 로그인 화면의 링크는 일부러 남겼습니다**(로그인 전이라 셸 밖이 자연스럽습니다). ⚠️ **완료조건 셋을 보고 후 확정받고 바꿨습니다** — ①「렌더링 HTML 동일」은 `{" "}` 때문에 바이트 동일이 불가능해 **`<!-- -->` 만 지운 정규화본 md5 일치**로 ②`globals.css` 삭제 0 이 아니라 **죽은 `.pv2-guide-link` 12줄**(다른 선택자 0건) ③ 항목 구성표 13 → **14개**(제목을 `/guide` 의 h2·h3 와 한 글자도 다르지 않게 잡느라). ⚠️ **포털 전용 `invoice` 항목은 안 넣었습니다** — 「금액 표시」로 찾아지는지 보고 정합니다. 🟢 **DB 0 · `_migrations` 29행 유지 · 마이그레이션 0 · 신규 의존성 0 · 신규 이미지 0 · 관리자 0줄 · 랜딩 0줄 · `lib/legal/` 0줄 · 포털 로그인 화면 0줄** · 🔴 **프리렌더 45 → 46**(늘어난 항목은 `/customer/guide` 하나) · 리뷰 라운드 **0** | 완료 |
| **36 (A·B장)** | **화주 거래조건 + 발주요청 청구주기 「요청」**(PR #150 `f338721`) — 화주 상세에 **「거래 조건」**을 신설하고(청구주기 · 정산 마감 안내 · 결제일 · 특이사항), 화주가 발주요청에서 **청구주기·결제일을 「요청」**할 수 있게 했습니다. 🔴 **DB 변경 있음**(마이그레이션 **3건** · `_migrations` 29 → **32행**). 🔴 **지시서 전제 둘이 틀렸습니다** — `companies.billing_cutoff_day` 와 `payment_terms` 가 **이미 있었고 화면에도 있었습니다**(두 벌을 만들면 월정산 묶음이 갈립니다 — 원칙 46번이 기록한 사고 자리). 🔴 **정산 마감일은 전 화주 월말로 통일했습니다** — 입력칸과 **저장 경로**를 함께 막고 값도 비웠습니다(`_bak_billing_cutoff_day_20260914` 백업 · 백업이 비면 멈추는 단언). **컬럼은 남깁니다 — `null` 이 곧 월말**이고 지우면 묶음 계산이 통째로 깨집니다. 🟢 **과거 묶음은 안 바뀝니다**(`MonthlyBillingBatchPanel` 이 `period_end` 로 찾고 **저장된 기간을 그대로** 씁니다) — 🔴 **마감일로 역산해 찾도록 되돌리면 이 마이그레이션이 소급 사고가 됩니다.** 🔴 **`requested_*` 에서 `requested_` 를 떼지 마십시오**(27차 확정: 확정은 담당자가 합니다). 🔴 **「계약과 다름」 배지는 알리기만 하고 막지 않습니다.** 🔴 **거래 조건은 내부 전용**입니다(화주포털·견적서·엑셀 0줄). 🔴 **세금계산서 발행 방식이 화면에서 빠진 채 merge 됐습니다**(merge 직후 발견 — 리뷰 2라운드에 `credit_limit` 을 지우면서 **바로 아래 붙어 있던 것이 같이** 지워졌습니다. 컬럼·상수는 있고 **입력칸만 없습니다** · PR 본문의 「칸이 넷」은 틀렸고 실제는 셋 · **PR 2 의 첫 커밋으로 복구**). 🟢 **화주포털 표시 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `lib/legal/` 0줄** · 실사용 리뷰 **3라운드** · ⚠️ **사용자 Preview 확인 없이 merge**(§7) | 완료 |
| **36 (PR 2)** | **C·D·E장 + A장 복구 + 견적서 공유 링크**(PR #151 `217b3cd` · 커밋 10개 · 파일 32개 · 실사용 리뷰 **2라운드** · 🔴 **`_migrations` 32 → 33행**) — C장 **선착불은 화주 미수금 0**(`lib/receivableCalc.ts` 정의처 · 🔴 리뷰 1라운드에 **식을 고쳐도 화면이 안 바뀌어** **표시 시점 계산**으로 바꿨습니다 §5-12) · D장 **하차 = 상차 +30분**(`lib/dropoffGap.ts` 세 화면) · E장 **견적서 「조정」 줄** · 🔴 **A장 누락 복구**(세금계산서 발행 방식) · 리뷰 2라운드 **계산창 가림**(`--admin-topnav-h`)·**일정 칩 「지금·당착·내착」**(opt-in) · 🔴 **`/q/[token]` 견적서 공유 링크 신설**(§5-15). ⚠️ **착수 시점엔 DB 0 이었고 리뷰 도중 성격이 바뀌었습니다** | 완료 |
| — | **견적서 공유 링크 후속 — 뒤 4자리 제거**(PR #152 `381885e` · 커밋 1개 · 파일 **3개** · 리뷰 라운드 **0** · 🟢 **DB 0**) — 🔴 **뒤 4자리 확인 제거**(사용자 지시 · 이제 **링크를 아는 사람이 곧 열람 권한자**) · 🔴 신고의 진짜 원인은 **없는 컬럼 다섯을 select 에 적은 것**이었고 `error` 를 안 받아 **404 로 둔갑**했습니다(**원칙 55번** · `_verify.sql` ⑯ 실측으로 바로잡음) · 🟢 곁다리로 차량 줄의 `body_type`(없는 컬럼) 제거. 🔴 **차수를 소비하지 않았습니다** | 완료 |
| — | **운송오더 3건 — 견적 프리필·하차 30분·오류 위치**(PR #153 `27fbdf7` · 커밋 1개 · 파일 **2개** · 리뷰 라운드 **0** · 🟢 **DB 0**) — 🔴 **35차가 `orders.customer_charge` 를 만들며 견적 프리필을 같이 안 고쳤습니다**(구간·담당자·차량까지 20여 칸을 채우면서 **금액만** 빠져 있었습니다). 🔴 **`quotes.final_amount` 는 공급가액이라 부가세 구분을 `false` 로** 맞춥니다 — `true` 면 **공급가액이 10% 줄어든 채로** 배차·정산까지 승계됩니다. 🔴 **곁다리로 차량형태가 통째로 떨어지고 있었습니다**(「5톤 윙바디」 견적이 「5톤 카고」 오더가 됐습니다 — `quotes.vehicle_type` 은 톤수뿐이고 차량형태는 `selected_options` 안입니다. **PR #152 와 같은 뿌리를 같은 날 두 번 놓쳤습니다**). 🔴 **하차 하한이 다섯 화면 한 정의처가 됐습니다**(오더 등록 2시간 → 30분 · **오더 상세는 하한이 아예 없었습니다**) + 일정 칩. 🔴 **오더 상세에 원칙 33번의 그 버그가 남아 있어 같이 고쳤습니다**(저장 실패가 채우던 폼을 통째로 덮었습니다). 🔴 **차수를 소비하지 않았습니다** | 완료 |
| — | **월정산 묶음 사전조사**(`74d53e9`·`fc6168b` · 🔴 **코드 0 · DB 0** — `_verify.sql` ⑱ 만) — 사용자 신고 *"정산관리 월 묶음건에 대해 정보나 방식이 애매하다"*. 실측: 월정산 **2건인데 묶음에 담긴 것 0건** · 묶음 1건이 그나마 draft. 결함 넷 — ① **자동 생성 없음** ② **묶음 안에 견적·운송 정보 없음** ③ **결제일이 화주 설정과 안 이어짐** ④ **마감일 이후가 통째로 없음**(`overdue` 를 세팅하는 코드 0곳). 🟢 사용자 확정 **(A) 표시 + 연체 자동 판정**. 자세한 것은 **§7** | 완료 |
| — | **월정산 묶음 개편**(PR #154 `0b77668`) — 🔴 **가장 큰 것은 담기 불가 버그였습니다.** `add_item_to_billing_batch` 가 `declare v_invoice record;` 로 선언한 변수를 `is_billing_batch_candidate(p_invoice invoices)` 에 넘겨 **매 호출마다 예외로 죽고 있었습니다** (`cannot cast type record to invoices`) — 관문을 전부 통과한 건이 마지막 줄에서 죽으므로 「담기」·「모두 담기」·자동 담기가 **전부, 언제나** 실패했습니다. `invoices%rowtype` 으로 **선언 한 줄만** 고쳤고 본문은 운영 DB 에서 떠온 그대로입니다. 🔴 **마이그레이션이 고친 함수를 실제로 불러보고 되돌리는 단언을 포함합니다 — 빼지 마십시오.** 🟢 **묶음 DB 함수가 처음으로 저장소에 들어왔습니다**(나머지 11개는 여전히 밖). ① 운송완료 → **묶음 자동 생성**(호출은 `lib/autoCreateInvoice.ts` 안 · **fire-and-forget** · 확정·해제된 묶음에는 담지 않음) ② 화면을 **아코디언 목록 하나로** + 항목에 상차일시·구간·차량·품목 ③ 결제일을 **`lib/paymentDueDate.ts`** 가 화주 거래조건에서 계산(「협의」·미설정은 **계산 안 함** — 없는 기한을 지어내면 합의하지 않은 날에 연체가 붙습니다) ④ 마감 지난 draft 표시 + **연체 자동 판정**(`scripts/mark-overdue.sql` 매일 03:20 KST). 🔴 **연체는 두 겹입니다 — 화면은 표시 시점 계산, DB 는 기록**(지시와 다르게 판단한 것입니다). 🔴 **연체가 붙으면 「묶음 해제」가 막힙니다**(`release_billing_batch` 가 `('paid','overdue')` 를 거절 — **의도된 동작** · 입금완료는 안 막힘). 🔴 **DB 변경 있음** · `_migrations` 33 → **34행** · 실사용 리뷰 **5라운드** | 완료 |
| — | **추가 수정 6건 + 리뷰 2건**(PR #155 `169c3a5` · 커밋 2개 · 파일 **11개** · 실사용 리뷰 **2건**) — 🔴 **②③ 의 뿌리는 하나였습니다: 상호가 배송지 별칭 칸을 쓰고 있었습니다.** 포털 발주 폼이 `location_name: form.origin_company_name || form.origin` 으로 **상호를 밀어 넣어** 상호와 별칭이 한 칸을 다퉜고, 관리자 화주 상세는 그 칸을 「주소 이름」으로만 읽어 **상호가 어디에도 안 보였습니다** → `customer_locations.company_name` 신설. 🔴 **`location_name` 을 없애지 않았습니다**(별칭은 상호와 다른 것 — 20차 금지). 🔴 **「기존 정보들을 건드려서라도」를 「덮어쓰기」가 아니라 「옮겨 적기」로 읽었습니다**(지시와 다르게 판단한 것) — 백필은 `location_name` 이 **주소와 다를 때만** 상호로 보고 **원본을 지우지 않습니다**(운영 실측 14건 중 **2건 이관** · 재실행 0건). 🔴 **관리자 화주 상세를 포털과 같은 구조로** — 그전에는 `address_detail`·`contact_*`·`notes` 를 **읽지도 쓰지도 않았고** 상세주소를 주소에 합쳐 넣었습니다. 🔴 **합치지 마십시오**(원칙 37번에 예외 줄을 달았습니다). 🔴 **견적관리 칩도 같은 칸을 읽고 씁니다**(상호·담당자·상세주소까지 프리필). ④ **품목 필수**(화면 표시 + 제출 검사 한 벌) ⑤ **조회기간** — 포털 3화면 공용 부품 `components/pv2/Pv2PeriodFilter.tsx` 하나, **끝날은 그 날을 포함** ⑥ 견적 펼침에 **부가세 포함가 병기**. 리뷰 2건 — 묶음 목록 **가독성**(🔴 **원인은 정보량이 아니라 무게**였습니다: 상태를 배지로 · 금액 13.5 → **15.5px** · 「기한」 → **「납부기한」**) · 「합계 (부가세 별도)」 → **「공급가액 (부가세 별도)」**(견적서 상세·엑셀과 **세 곳이 맞았습니다**). 🔴 **DB 변경 있음** · `_migrations` 34 → **35행** · 🟢 **Preview 확인 완료**(사용자 2026-09-15 — merge 시점에는 ②③④⑤ 가 미확인이었습니다) | 완료 |
| 차수 없음 | **운임기준표 v12 A장**(PR #156 `bfa2ff5`) — 화주 청구가 195칸을 **만원 격자**로(다마스·라보만 5,000). UPDATE **184칸** · INSERT 0 · `_migrations` 35 → **36행** · 🔴 **제품 코드 0줄**. 공개 게시가 **5칸 인하 · 인상 0칸**(사용자 승인). 🔴 **격자가 둘이라 한 격자로 합치면 역전 17건** · 🔴 **앵커는 1톤 10km = 40,000 하나** · 🔴 **60km 이상은 일부러 안 내렸습니다**(중·장거리 실측 마진이 −4.2% ~ 14% 라 같은 비율로 내리면 역마진이 번집니다) · ⚠️ **착수 전 확인에서 지시서 전제 넷이 틀렸습니다**(대기료·경유지비에 5,000 단위 **7칸** · `rate_surcharges` 에 모르는 카테고리 둘 · `왕복/편도` 가 2행이 아니라 **3행** · 「공급가액」 라벨이 3곳이 아니라 **6곳**) — **넷 다 손대지 않기로 확정** · 🟢 `_verify.sql` **㉒** 신설 | 완료 |
| 차수 없음 | **운임기준표 v12 B장 — 가산 요율제 → 정액**(PR #157 `95dff88`) — 차량형태 **21행** + 운송시간 **6행**의 `rate_pct` 를 0 으로, `flat_amount` 만 남겼습니다(UPDATE 27행 · `_migrations` 36 → **37행** · 🔴 **제품 코드 0줄**). 🔴 **근거는 실측 「절대 차액(원)」**(윙바디 10,000 / 145건 · 리프트 15,000 / 35건 · 냉동탑 50,000 / 10건 · 카고계열 **0** / 722건) — 요율제는 **25톤 400km 윙바디에서 136,500원**이 붙었습니다. 🔴 **가산 0원 4종의 행을 지우지 마십시오**(`카고`·`카고/윙`·`차종무관`·`특수/협의` = 전체 오더의 **71%** — 행이 없으면 드롭다운에서 고를 수 없습니다). 🔴 **대형은 윙바디 프리미엄이 없습니다**(실측 준대형 −5,000 · 대형 −10,000). 🔴 **새벽은 20,000**(사용자가 30,000 에서 낮췄습니다). 🔴 **`왕복/편도` 3행만 요율을 남겼습니다**(거리 비례). ⚠️ **운송시간 6종은 실측 표본 0건 · 등급 D** · ⚠️ **가산 수입 −66%**(건당 12,082 → 4,077원 · 마진 34.5% → 26.7%) — **의도한 것입니다** | 완료 |
| 차수 없음 | **운임기준표 v12 C장 — 소계 반올림 + 견적서 가산 한 줄**(PR #158 `ef5ca7e` · 파일 **10개** · 실사용 리뷰 **1라운드**) — 🟢 **DB 0 · `_migrations` 37행 그대로.** A·B 뒤에도 만원을 깨는 것이 **넷**(왕복/편도 · 대기료 · 경유지비 · 혼적 할인)이라 **가산 소계**와 **혼적 할인액**을 그 차급 격자에 올립니다(`lib/roundToUnit.ts` 유일 정의처). 🔴 **줄마다 걸지 마십시오**(과다 청구). 🔴 **화주 견적서 여섯 산출물은 가산을 한 줄로 합산합니다**((가)안 · `lib/quoteFareLines.ts`) — `quote_items` 에 **반올림 전 개별 줄**이 남아 그대로 그리면 **거의 모든 견적에 조정 줄**이 붙습니다. 🔴 **할인(음수) 줄은 그대로** · 🔴 **관리자 견적 상세는 개별 줄 그대로**(그 화면의 조정 줄에 반올림 차액이 나타나는 것은 **고장이 아닙니다**). 🔴 **리뷰 — 가산 줄에 이름을 적되 요율 `(80%)` 는 뗍니다**(기본운임이 바로 윗줄이라 요율로 금액이 계산됩니다). ⚠️ **print 2종은 실물 확인 대상**(목이 불완전해 크래시 — `origin/main` 도 똑같이 크래시해서 내 변경 탓이 아님을 확인) | 완료 |
| **37** | **24시콜 ⓐ — 배차된 차주 정보 붙여넣기**(PR #159 `d2c2927` · 파일 **3개** · **+328 / −0** · 리뷰 라운드 **0**) — 🟢 **DB 0 · 마이그레이션 0 · `_migrations` 37행 그대로 · 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `globals.css` 0줄 · 신규 의존성 0.** 접수중 배차의 **「외부 배정」 분기**에 붙여넣기 칸을 두고 `홍길동 서울12가3456 010-1234-5678` 한 줄로 차주 세 칸을 채웁니다. 🔴 **범위가 작업 중에 두 번 줄었습니다** — 사용자가 **차주 셋으로**(*「차주명, 전화번호, 차량번호만」*), 이어서 붙여넣을 글도 **목록 행 40열 → 한 줄**이 됐습니다. **그래서 `dispatches.external_order_no` 신설이 없어지고 DB 변경이 0이 됐습니다.** 🔴 **금액·수수료·화물번호로 넓히지 마십시오**(저장 경로가 다르고 수수료 칸은 **선착불에서만** 그려집니다). 🔴 **자리는 `dispatch_status === "접수중"` + `assignment_type === "external"` 안뿐입니다**(확정 이후에는 읽기 전용 · ⚠️ **차주 기본운임 계산기는 렌더 조건이 정확히 반대**입니다). 🔴 **라벨이 없어 순서가 아니라 「모양」으로 가릅니다**(연락처 = 숫자 10~11자리 + 0 시작 · 차량번호 = `(지역 0~2)(숫자 2~3)(한글 1)(숫자 4)` · 이름 = 나머지 첫 토큰). 🔴 **8토큰을 넘으면 거절합니다** — 목록 행은 전화번호가 여럿이라 잘못 집으면 **엉뚱한 사람에게 배차 문자가 나갑니다.** 🔴 **채우기만 하고 저장은 기존 「배차확정」이 합니다**(그 버튼의 검사·낙관적 잠금을 우회하지 않기 위해). 🔴 **빈 값으로 덮어쓰지 않고**, **읽지 못한 토큰은 화면에 나열합니다**(원칙 55번). 🔴 **서버에 보내지도 저장하지도 않습니다**(처리방침 변경 0 · 개인정보 보관 0) | 완료 |
| **38** | **접수 알림 — 관리자 A·B장 + 화주포털 A장**(PR #160 `caa83d0` · 커밋 4개 · 파일 **17개** · **+1,884 / −1** · 실사용 리뷰 **2건**) — 고객 접수 셋(견적문의·화주신청·발주요청)이 들어오면 **사무실 밖에서도** 압니다. 🔴 **DB 변경 있음**(`push_subscriptions` 표 · `_migrations` 37 → **38행**) · 🟢 공개 화면 0줄 · 랜딩 0줄 · `lib/legal/` 0줄 · **신규 의존성 0** · `public/customer/sw.js` **0줄** · `globals.css` **순수 추가**. **A장** = 화면 안 배너 · 탭 제목 `(3) …` · 소리. **B장** = 웹 푸시(**VAPID + aes128gcm 을 Node `crypto` 만으로** — RFC 8291 §5 시험 벡터와 **바이트 단위 일치** 확인). 🔴 **지시서 전제가 넷 틀렸습니다** — ① 발주요청은 폴링이 아니라 **Realtime** 이라 「폴링이 늘어난 순간」으로는 **가장 급한 것을 못 잡습니다**(배지 `counts` 에 얹었고 **새 되풀이 타이머 0개**) ② 관리자 경로 셋 중 **둘이 없는 경로**였습니다 ③ 암호화까지 `crypto` 만으로 되어 **페이로드 있는 푸시**가 됐습니다 ④ 알림음을 **파일 없이 WebAudio 로 합성**했습니다. 🔴 **`AudioContext.resume()` 은 제스처가 없으면 거절하지 않고 「영영 안 끝납니다」** — 300ms 경주를 빼지 마십시오(화면이 멀쩡해 보여 눈으로는 안 잡힙니다). 🔴 **서버리스 함수는 응답 뒤 얼어붙습니다** — 지시서 금지 10번(fire-and-forget)을 그대로 따르면 **푸시가 아예 안 나갑니다**(PR #154 는 **브라우저**가 부르는 경우라 다릅니다). 3초 상한을 걸고 `await` 하되 **절대 던지지 않습니다**. 🔴 **알림 본문에 고객 정보를 넣지 마십시오**(잠금화면·배너에 그대로 뜹니다 — 키가 `body,tag,title,url` 넷뿐입니다). 🔴 **`push_subscriptions` 는 RLS on + 정책 0개**(화주와 직원이 **같은 `authenticated` 롤**이라 정책을 만들면 화주가 직원 기기 구독을 읽습니다) · `endpoint` **유니크** · `staff_id` FK **cascade**(원칙 32번과 **반대**로 잡은 것 — 이력이 아니라 **살아 있는 발송 대상**입니다) · 🔴 **role 을 가르지 마십시오**(넣으면 staff 가 못 받습니다). 리뷰로 **알림음 볼륨 3단계 · 소리 3종**(🔴 기본값은 A장이 넣은 그 소리 그대로 · 🔴 **`localStorage` 에 기억하고 `staff_accounts` 로 옮기지 마십시오** · 🔴 **윈도우·폰 알림 소리는 OS 가 정합니다**). 🔴 **화주포털 A장은 사용자가 범위를 넓힌 것입니다** — 지시서는 「포털은 한 줄도 건드리지 않는다」였습니다. **그 금지를 근거로 되돌리지 마십시오**(§5-13) | 완료 |
| — | **화주포털 B장 — 웹 푸시**(PR #161 `8d261cc` · 파일 **17개** · 커밋 2개 · **+1,079 / −211** · 리뷰 라운드 **0**) — 화주도 **브라우저를 닫은 채** 견적·배차·정산을 알 수 있습니다(A장은 포털을 **열어 둔 동안에만** 보였습니다). 🔴 **DB 변경 있음**(`customer_push_subscriptions` 표 · `_migrations` 38 → **39행**) · 🟢 공개 화면 0줄 · 랜딩 0줄 · **신규 의존성 0** · `globals.css` **순수 추가**(+47 / −0). 사용자 확정 범위는 **견적·배차·정산 셋**입니다(*「견적과 배차,운송 정산만」*) — 🔴 **공지사항은 안 넣었습니다**(전체 공지라 **모든 화주 폰이 한꺼번에** 울립니다). ⚠️ **이 항목이 「먼저 정해야 한다」고 적고 있던 「배차 SMS 와 겹친다」는 틀렸습니다** — 실측하니 **배차확정 문자는 차주에게** 가고 **상차·하차완료는 자동 발송이 아예 없습니다**(PR #73 이 「팝업이 번거롭다」는 피드백으로 수동 버튼만 남겼습니다). **겹치는 자리가 하나도 없습니다** — 🔴 「SMS 와 겹친다」를 근거로 사건을 줄이지 마십시오. ⚠️ 다만 **나중에 상차·하차 문자를 자동으로 되돌리면 그때는 겹칩니다.** 🔴 **직원용과 표를 갈랐습니다** — 화주포털 계정과 직원 계정이 **둘 다 `authenticated` 롤**이라(19차) 이 표들은 **RLS on + 정책 0개**로만 지켜집니다. 한 표에 두 종류를 담으면 언젠가 누가 「조회 편하게」 정책을 하나 열 때 **화주와 직원 기기가 한꺼번에 새어 나갑니다.** **합치지 마십시오**(단언이 지킵니다). 🔴 **`company_id` 를 구독 표에 복사해 두지 않았습니다** — 담당자 계정이 다른 화주로 옮겨지면 **남의 회사 소식이 그 폰으로 갑니다**(발송 때마다 조인합니다). 🔴 **서버 안에서는 `await` · 브라우저에서는 금지** — 서버리스 함수는 응답 뒤 **얼어붙어** fire-and-forget 이면 푸시가 **아예 안 나갑니다**(정산확정). 반대로 견적·배차는 **브라우저가** 부르는 구조(원칙 53번)라 `await` 하면 담당자의 상태 변경이 멈춥니다. 🔴 **부르는 곳이 넷**(배차 상세 셋 + 배차 목록)이라 상태 → 사건 매핑을 `lib/notifyPortalPush.ts` 한 곳에 뒀습니다 · 🔴 **수동 재발송 버튼에서는 부르지 않습니다**(며칠 뒤 눌러도 푸시가 또 갑니다). 구독 단추는 **관리자·포털 공용**이 됐고(`components/PushSubscribeButton.tsx`) 🟢 **관리자 동작은 한 글자도 안 바뀌었습니다**(기계 비교로 확인). 🟢 `_verify.sql` **㉓** 신설(「알림이 안 온다」 신고의 출발점) | 완료 |
| — | **알림 4건 — 창내 배너 자동 닫기 · 화주 견적 승인 · 포털 알림 범위 축소**(PR #162 `fda178a` · 파일 **12개** · 커밋 1개 · **+240 / −55** · 리뷰 라운드 **0**) — 38차·B장이 나간 뒤 **실제로 써 본 사람의 말** 넷을 반영했습니다. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 랜딩 0줄 · `globals.css` **0줄**. 🔴 **창내 배너 끄는 신호가 둘이고 둘 다 있어야 합니다**(경로 변화 + 서비스워커 쪽지) — 둘째가 없으면 **이미 그 화면이 열려 있는 경우**를 못 잡습니다(`client.navigate()` 로 주소가 안 바뀝니다). 정의처는 `lib/alertCore.ts` 의 **`dropToastsForPath()` 하나**이고 🔴 **뺄 것이 없으면 같은 배열을 돌려줍니다**(새 배열이면 배너 줄이 매번 다시 그려집니다) · 🔴 **`startsWith(href + "/")` 의 `+ "/"` 를 빼지 마십시오**(`/admin/quotes-archive` 가 `/admin/quotes` 배너를 지웁니다) · 🟢 **서비스워커 캐시 목록 무변경이라 `VERSION` 그대로**입니다. 🔴 **화주 견적 승인 알림은 38차 금지를 뒤집은 것입니다** — 「접수가 아니라 담당자가 이어서 할 일이라 소리로 부를 일이 아니다」로 빼 뒀던 자리이고 **그 주석을 같은 커밋에서 고쳤습니다.** 옛 문장을 근거로 지우지 마십시오. ⚠️ **담당자가 직접 「수주」로 바꾸면 배너만 뜨고 푸시는 안 갑니다**(그 경로는 API 를 안 거칩니다 — **고장이 아닙니다**). 🔴 **「포털에 윈도우 배너가 안 뜬다」는 신고는 실측 결과 고장이 아니었습니다** — `_verify.sql` ㉓ 에 화주 쪽 **성공이력 1 · 실패 0** 이었습니다. 갈리는 자리는 **A장은 `updated_at` · B장은 사건 목록**이라는 것이고, 그래서 **「화면에는 뜨는데 폰에는 안 오는」 것이 정상 동작으로도 일어납니다.** 보고했더니 사용자가 **좁히는 쪽**을 골랐습니다. 🔴 **포털 알림 넷 → 둘 · 푸시 다섯 → 셋** · 🔴 **배지는 넷 그대로**(§5-17) | 완료 |
| — | **견적번호 중복 수정**(PR #163 `a8b0aff` · 파일 **4개** · 커밋 2개 · **+219 / −34** · 리뷰 라운드 **0**) — 사용자 신고 *「발주요청을 승인하고 견적관리로 넘어왔는데 알림 표시가 안 사라지고 `duplicate key value violates unique constraint "quotes_quote_no_key"` 가 뜬다」*. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로**. 🔴 **두 신고가 한 뿌리였습니다** — `lib/generateNumber.ts` 가 번호를 **「그날 만들어진 행 수 + 1」**로 만들어서, 그날 건을 **하나라도 지우면** 다음 번호가 이미 쓰인 번호와 겹치고 **다시 눌러도 같은 번호를 뽑아 영영 실패**했습니다. 그리고 발주요청은 **「견적이 저장되는 순간」에 `승인됨`** 이 되므로(`app/admin/quotes/page.tsx`) 저장이 실패하면 `대기중` 으로 남아 배지가 그대로였습니다. 🔴 **코드부터 열지 않고 운영 DB 를 쟀습니다**(`_verify.sql` **㉔** 신설) — 견적번호 20260916 이 **건수 2 · 최대번호 3**, 발주요청 **대기중 1건**으로 신고와 정확히 일치했습니다. 🟢 `orders` 에도 유니크 제약이 이미 있어(㉔-d) **DB 변경이 0** 이 됐습니다. 🔴 **「그날 최대 번호 + 1」입니다** — 지워진 번호는 비워 둡니다(번호는 연속이 아니라 **유일**하기만 하면 됩니다). 🔴 **동시 저장은 번호 계산만으로 못 막습니다** — 유니크 제약을 방어선으로 두고 **위반(`23505`)이면 다시 뽑아 재시도**합니다(`insertWithDailyNumber` 한 곳 · 상한 5회). 🔴 **번호만 뽑아 쓰지 마시고, 호출부에서 `quote_no`·`order_no` 를 직접 넣지 마십시오**(재시도가 같은 번호를 다시 씁니다). 🔴 **번호가 아닌 유니크 위반은 재시도하지 않고 원문을 올립니다** · 🔴 **조회 실패를 `001` 로 때우지 마십시오**(원칙 55번 — 그 자체가 중복이 되어 「중복」이라는 엉뚱한 원인이 뜹니다) · 🔴 **`created_at` 이 아니라 번호 문자열로 그날을 고릅니다**(자정 전후에 어긋납니다) · 🔴 **하루 1,000건을 넘어도 숫자로 비교합니다**(`"1000" < "999"`). 🟢 **`orders` 도 같은 함수라 함께 고쳐졌습니다.** ⚠️ **사용자 몫 — `대기중` 1건은 「견적으로 전환」을 다시 눌러야 풀립니다**(코드가 자동으로 고치지 않습니다) | 완료 |
| — | **포털 알림 4건**(PR #164 `96bec73` · 파일 **10개**(신규 2) · 커밋 1개 · **+313 / −30** · 리뷰 라운드 **0**) — 사용자 지시 4건. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 랜딩 0줄 · `globals.css` **순수 추가**(+33 / −0). 🔴 **네 건이 아니라 둘이었습니다 — 앞 세 건이 한 뿌리입니다.** 화면 안 배너의 신호가 `updated_at > 마지막으로 본 시각` **하나**라 **무엇이 바뀌었는지 모른 채** 울렸습니다(담당자 메모 한 줄 · 화주 **본인**의 견적 승인 · 상차완료가 전부 같은 신호입니다). 🔴 **세 건을 각각 막으러 가면 안 됐습니다** — ②를 「승인 직후 `markSeen`」으로 막으면 Realtime 과 **경합**하고, ③은 `updated_at` 만으로는 **판정 자체가 불가능**합니다. 신호를 **바뀐 뒤의 상태**로 바꾸니 셋이 한꺼번에 닫혔습니다. 🔴 **배지(`counts` 넷)와 배너(`alertSignals` 둘)를 갈랐습니다** — 배너는 **견적 `견적제출` · 배차 `배차확정`·`운송완료`** 뿐이고 정의처는 `lib/portalAlert.ts` 입니다(§5-17). 🔴 **웹 푸시 사건 셋과 같은 뜻이라 한쪽만 고치지 마십시오.** ⚠️ **「배지는 늘었는데 배너가 안 뜬다」는 고장이 아닙니다**(부분집합입니다). 🔴 **「상담중」은 화면 글자만 「확인중」으로 바꿨고 DB 값·`quotes_status_check` 는 그대로입니다** — 값을 바꾸면 배포 순서가 어긋날 때 **견적 저장이 통째로 막힙니다**(바로 앞 PR #163 이 그 증상이었습니다). 관리자는 `quoteStatusAdminLabel()`(신규 · `상담중` 만), 화주는 기존 `quoteStatusStyle()`. 🔴 **관리자에 화주용 순화 라벨을 끌어오지 마시고, `<option value>` 는 DB 값 그대로 두십시오.** 🟢 이용가이드 2화면은 **코드 0줄**로 따라왔습니다. 🔴 **목록 안 「업데이트」 표시는 배너와 별개로 넓습니다**(`updated_at` 이면 전부 · 사용자 원문이 「알림배너와 **별개로**」입니다) — 셸이 화면 진입 때 「마지막으로 본 시각」을 밀기 때문에 **`lib/usePortalSeenAt.ts` 가 밀리기 전 값을 붙잡습니다**(화면에서 `getLastSeen()` 을 직접 읽지 마십시오). 🔴 **시각 비교는 `Date.parse`** 입니다(형식이 달라 사전순이 뒤집힙니다 — 실측 `"…12:00:00.500+00:00" > "…12:00:00Z"` 가 **false**). ⚠️ **`하차완료` 에서 멈추면 완료 배너가 안 뜹니다**(푸시와 같은 기준 · 화면은 둘 다 「운송완료」로 보여줍니다 — 바꾸려면 **먼저 물으십시오**). 🟢 **실제 도메인 확인 완료**(사용자 2026-09-16 — *「모두 확인했다」*) — merge 시점에 열려 있던 ①②④ 를 전부 확인받았습니다. 🔴 **「사용자 몫 · 미확인」으로 적힌 옛 줄에 속지 마십시오.** ⚠️ 다만 위 **`하차완료` 건은 그대로 열려 있습니다**(고장이 아니라 확정과 반대라 안 바꾼 것입니다) | 완료 |
| 39차 | **견적문의 폼 정합 + 운송시간 자동선택 (A·B·C장)**(PR #165 `8c8a1c5` · 파일 **8개**(신규 1) · 커밋 5개 · **+547 / −65** · 실사용 리뷰 **1라운드**) — 사용자 지시 4건. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 랜딩 0줄 · `lib/legal/` 0줄 · `globals.css` **순수 추가**(+15 / −0). 🔴 **「왕복,편도 드롭메뉴 안됨」은 카테고리 키 한 글자**였습니다 — `surcharge["왕복편도"]`(슬래시 없음)라 그 칸이 **빈 배열**이 됐고 **예외도 경고도 안 났습니다**(원칙 55번과 같은 결). **재현했습니다.** `SURCHARGE_KEYS` 상수를 두어 **화면에서 카테고리 문자열을 직접 적지 않게** 했습니다. 🔴 **필수 표시는 빨간 `*`(`#B4423A`)이고 화주포털 발주요청까지 넓어졌습니다** — ⚠️ **31차가 확정했던 회색 「필수」 글자를 뒤집은 것**입니다. 포털에는 표시가 없던 게 아니라 **검정 `*` 가 부제·placeholder 안에 문자로** 박혀 있었습니다(placeholder 는 값을 넣으면 사라져 표시 역할을 못 합니다). **색은 한 값이고 정의처가 둘입니다**(공개 폼 `REQUIRED_MARK_COLOR` / 포털 `.pv2-req` — 원칙 57번). 🔴 **B장 — 포털 부품(`Pv2DateTimeField`)을 끌어오지 않고 같은 동작을 랜딩 생김새로** 옮겼습니다(「지금」·「당착」·「내착」 칩 · 하차 **+30분 하한** · 칩이 켜지면 칸을 잠급니다 · 당착·내착이면 특이사항에 **하차 일시를 적지 않습니다**). 🔴 **C장 — 관리자 견적 등록에 자동선택이 이미 있었고 틀렸습니다**(`출퇴근/혼잡`·`새벽`을 아예 안 썼고 「평일」 **부분일치**였습니다). 정의처 `lib/transportTimeAuto.ts` 하나를 **세 화면**이 씁니다. ⚠️ **18:00~19:59 상차 건이 0원 → +10,000원으로 오릅니다**(사용자 확정 「여섯 종 전부 자동」). 🔴 **주말이 시간대보다 먼저입니다 — 바꾸면 주말 새벽이 또 오르니 먼저 물으십시오.** 🔴 **`공휴일`은 자동으로 못 고릅니다**(달력이 없습니다 — 하드코딩 금지). 🔴 **리뷰 — 칩이 1280px 에서도 두 줄이던 원인은 칩 줄이 `DatePicker` 안이라 가용 폭이 날짜 칸(154px)뿐이던 것**입니다. 밖으로 꺼내 315px 이 됐고 **여섯 폭 전부 한 줄**입니다(360px 에서도 76px 여유 — 글자 크기·여백은 한 글자도 안 줄였습니다). 🟢 `_verify.sql` **㉕** 신설 · ⚠️ **거짓 ❌ 넷 — 전부 제 측정이 틀렸습니다** | ✅ |
| — | **39차 D장 — 주소칸 엔터 검색 + 고른 타입 그대로**(PR #166 `4dc46b0` · 파일 **3개** · 커밋 2개 · **+115 / −20** · 리뷰 라운드 **0**) — 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · `globals.css` **0줄** · **화면 파일(`app/**`) 0줄**. 🔴 **39차 지시서의 D장이고 PR #165 에는 안 들어갔습니다** — 전달된 판본이 D장 추가 전이었습니다(39차 기록에 주소 관련 **0건**인 것이 근거). **「또 있지」로 읽지 마십시오.** 🔴 **지시서 전제 셋이 틀렸습니다** — ① **주소 부품이 둘**(`AddressSearch` / `Pv2AddressField` — 25차가 **인라인 style 때문에** 갈랐습니다 · **둘 다 고쳤습니다**) ② **훅도 고쳐야 했습니다**(검색어는 `open()` 이 받습니다) ③ **주소 칸은 읽기 전용이 아니었습니다**(원래부터 자유 입력). 🔴 **규칙은 `lib/useDaumPostcode.ts` 하나**(`open(oncomplete, q)` · `enterToSearch` · `pickSelectedAddress`) — **부품에 다시 적지 마십시오.** 🔴 **`preventDefault()` 를 `ready` 보다 먼저, 조건 없이** — 주소 칸을 쓰는 화면 **여덟이 `<form>` 안**이고 `/quote`·`/apply` 는 **anon INSERT 경로**입니다. 🔴 **곁다리로 원래 있던 버그를 고쳤습니다** — 두 부품이 각자 `roadAddress || jibunAddress` 라 **지번 선택이 통째로 무시**되고 있었습니다(사용자 신고). 🔴 **「동까지만」은 이 팝업으로 안 됩니다** — 사용자 실물 스크린샷으로 확인했습니다(결과가 전부 번지·건물번호까지 붙고 **우편번호가 항목마다 다릅니다** 16480/16490). ⚠️ **실제 팝업을 못 띄워 `window.daum` 을 목으로 심어 쟀습니다**(25항목 통과) · 🟢 **렌더링 DOM md5 무변**(관리자 회귀 0) | ✅ |
| — | **내부시스템 소수정 8건 — A장(발주요청·견적관리) + B장(운송오더·배차관리)**(PR #167 `ea80364` · 파일 **8개** · 커밋 2개 · **+561 / −63** · 실사용 리뷰 라운드 **0** · 🟢 **사용자 확인 완료** 2026-09-17 *「모두 확인했다」*) — 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `globals.css` **순수 추가**(+114 / −0). 🔴 **차수를 소비하지 않았습니다.** 🔴 **①은 삭제하면 안 되는 버튼이었습니다**(사용자 원문은 「상관없으면 삭제하자」) — 「수동 승인 처리」는 **견적 저장이 실패해 발주요청이 `대기중` 으로 남았을 때 푸는 유일한 탈출구**입니다(PR #163 이 정확히 그 증상이었고 그때 손으로 눌러 풀었습니다). 지우는 대신 **「전화로 처리함」**으로 이름을 바꾸고 `.btn-quiet`(밑줄 회색 글자)로 무게를 낮췄습니다 — **지우지도 숨기지도 마십시오.** 🔴 **② 판정은 DB 값 `상담중` 입니다**(「확인중」은 PR #164 가 화면에만 입힌 글자입니다) · 색은 발주요청 고정 행과 **같은 토큰**(`--admin-row-attention`)이고 **새 색으로 갈라놓지 마십시오** · 🔴 **③ `<details>` 가 아니라 state + 조건부 렌더입니다** — 열림을 코드가 정하는 자리가 **넷**이고, `display:none` 으로 숨기면 `required` 주소 칸이 DOM 에 남아 **제출이 조용히 막힙니다.** 🔴 **저장에 실패했을 때 접지 마십시오** · 🔴 **손잡이 화살표는 SVG 입니다**(`▸` 가 폭 6px 로 그려져 손잡이인지 알아볼 수 없었습니다 — PR #129 의 `⋯` 와 같은 자리) · 🟢 목록 머리 y **2005 → 635px** · 🔴 **④ 관리자 전용 `quoteStatusAdminStyle()` 을 신설했습니다**(화주용은 한 글자도 안 건드렸습니다) — 관리자 목록에서 옐로는 **「내가 할 일」**이고 화주 목록에서는 **「성사」**라 두 매핑을 합칠 수 없습니다 · 대비 4.52~6.37:1 AA 통과 · 🔴 **⑤ `orders.status` 로 가르지 않고 `dispatches` 에 묻습니다**(담당자가 손으로 `배차중` 을 고를 수 있습니다) · 🔴 **⑦ 원인이 둘이었습니다** — `DISPATCH_TO_ORDER_STATUS["접수중"]` 이 `배차중` 인데 후보 조건이 그것을 남겼고, `?from_order=` effect 가 목록을 다시 부를 때마다 **닫은 폼을 다시 열었습니다**. **상태 조건을 지워서 고치지 마십시오**(두 단계라야 합니다) · 등록을 마치면 **그 배차 상세로** 넘어갑니다 · 🔴 **⑥ 배차확정에서만 막습니다**(등록 단계는 협의 중이라 비워도 됩니다) · **`> 0` 이 아니라 「비었는가」**(0원 배차가 실무에 있습니다) · 🔴 **⑧ 원인은 `alert` 이 아니라 원칙 33번 전체화면 가드였습니다** — `if (error || !dispatch)` 가 액션 실패까지 받아 화면을 덮었고 아래 배너는 **한 번도 그려진 적이 없었습니다.** PR #153 이 오더 상세에서 고친 그 버그이고 **이 저장소에서 네 번째**입니다. `actionError` 에 `scope`(page/confirm/save)를 뒀고 **하나로 되돌리지 마십시오** · 🟢 **검증은 임시 라우트 + Supabase REST 목으로 실제로 그려서** 36항목(A 20 · B 16) 전부 통과 · 프리렌더 **46** 동일 · ⚠️ **렌더링이 아니었으면 못 봤을 것 둘**(손잡이 글리프 · 발주요청 행의 「대기중」이 평문으로 남아 있던 것) | ✅ |
| — | **관리자 목록 기간 「직접지정」**(PR #168 `c7b3582` · 파일 **6개** · 커밋 1개 · **+285 / −54** · 리뷰 라운드 **0**) — 사용자 요청 *「내부시스템**에서도** 견적관리, 운송오더, 배차관리, 정산관리에서 목록 정렬할때 날짜 "직접지정" 이 있어야 한다」*. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · **화주포털 0줄** · 공개 화면 0줄 · 랜딩 0줄 · `globals.css` **순수 추가**(+52 / −0). 🔴 **차수를 소비하지 않았습니다.** 🔴 **정의처는 `components/DateRangeFilter.tsx` 안이고 `lib/` 에 없습니다**(`grep -rn "getDateRange" lib/` 가 0건 — **거기서 못 찾고 새로 만들면 정의처가 둘이 됩니다**). 🔴 **끝날은 그 날을 포함합니다** — `getDateRange()` 가 이제 `{ from, to }` 를 돌려주고 **`to` 는 종료일 다음 날 자정**이라 호출부는 **`.lt()`** 입니다. **그 날 자정을 `.lte()` 로 바꾸면 종료일 00:00 이후에 생긴 건이 통째로 빠집니다**(9월 15일까지로 골랐는데 15일 건이 하나도 안 나옵니다 — 화주포털 `Pv2PeriodFilter` 가 PR #155 에서 겪은 그 자리이고 이번엔 **정의처 주석에 못박았습니다**). 🔴 **`"YYYY-MM-DD"` 를 `new Date(문자열)` 로 읽지 마십시오** — **UTC 자정**이라 KST 에서 09:00 이 되어 **하루가 밀립니다**(원칙 41번과 같은 결). 검증도 **`timezoneId: "Asia/Seoul"`** 로 돌렸습니다 — 🔴 **이 컨테이너는 TZ 가 UTC 라 그냥 재면 이 버그가 안 잡힙니다.** 🔴 **`Pv2PeriodFilter` 를 끌어오지 않았습니다**(원칙 57번 — `--pv2-*` 가 `.portal-v2` 안에만 있어 **관리자에서는 색이 안 나옵니다**). **같은 동작을 관리자 생김새로 옮겼고 프리셋 계산은 한 글자도 안 바꿨습니다**(갈리면 같은 「이번주」가 두 값이 됩니다). 🔴 **opt-in 입니다** — `custom`/`onCustomChange` 가 선택 prop 이고 **`onCustomChange` 가 없으면 「직접지정」 버튼 자체를 안 그립니다.** 그래서 같은 부품을 쓰는 **나머지 네 화면**(화주신청 · 발주요청 · 공개문의 · 문자이력)은 **프리셋 넷 그대로**입니다(실제로 렌더링해 확인). **기본으로 켜지 마십시오** — 사용자가 고른 것은 네 화면입니다. 🔴 **빈 구간을 만들지 않습니다**(처음 켜면 이번달 1일~오늘 · 한쪽만 비우면 열린 구간 · `max`/`min` 으로 역전 방지). ⚠️ **범위를 제가 넓힌 것 하나** — 조회 상한 안내가 「전체」일 때만 떴는데 **직접지정으로 긴 구간을 고르면 정확히 거기 걸려서** 모든 기간으로 넓혔고, **견적 목록에는 그 안내가 아예 없어서** 같이 넣었습니다. ⚠️ **날짜 표시 형식은 브라우저·OS 로케일이 정합니다**(`<input type="date">`) — **CSS 로 못 바꿉니다.** ⚠️ **거짓 ❌ 5건 — 전부 제 측정이 틀렸습니다**(하네스의 `<pre>` 가 안 접혀 4건 · `order=created_at.desc` 를 기간 조건으로 센 것 1건) · 🟢 프리렌더 **46** 동일 · ⚠️ **Preview 확인 없이 merge 했습니다**(확인 3건 — 특히 🔴 **종료일 당일 건이 목록에 나오는지**) | ✅ |
| — | **랜딩 히어로 사진·헤더 로고 선명도**(PR #169 `f656abb` · 파일 **9개** · 커밋 **4개** · **+121 / −8** · 실사용 리뷰 **2라운드**) — 사용자 신고 *「데스크탑 Windows Chrome 에서 헤더 워드마크와 히어로 트럭 옆면의 WeCarry 로고가 **지글지글하게 깨져** 보인다. 원본 파일은 깨끗하다」*. 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 문구 변경 0 · 관리자 0줄 · 화주포털 0줄 · 공개 폼 0줄. 🔴 **차수를 소비하지 않았습니다.** 🔴 **지시서는 히어로와 헤더 로고를 「같은 원인」으로 묶어 커밋 셋을 지정했는데 헤더 로고는 그 셋 중 어느 것으로도 안 고쳐졌습니다** — 실제 원인이 완전히 달랐고 밝히는 데 **신고가 두 번 더** 걸렸습니다(「히어로는 개선됐는데 로고는 계단현상이 남아있다」 → 「수정이 된건가? 크게 나아진점이 없다」). **커밋 4번이 그 결과이고 지시서에 없던 것입니다.** ▸ **히어로(커밋 1·2)** — 사진 띠가 1600px 화면에서 **0.41배로 축소**되어 그려지는데 그 위에서 10초 교차 애니메이션이 돕니다. 배율별 축소본 4장을 굽고 `image-set()` 으로 고르게 했습니다(**1배율 전송 1,554 → 502KB, −68%**). 🔴 **소스는 61차 커밋 `29f849b` 의 원본입니다** — 지금 저장소 파일은 PR #119 가 품질 90 으로 재인코딩한 것이라 **그것을 또 줄이면 세대 손실이 겹칩니다**(저장소가 shallow clone 이라 `git fetch --unshallow` 가 필요했습니다). 🔴 **크로마 4:4:4 입니다(지시서에 없던 결정)** — 노리는 것이 **색 있는 로고의 사선 가장자리**라 4:2:0 이면 그 자리가 다시 번집니다(대가 +44KB). 🔴 **원본 2장은 그대로입니다**(2배율·모바일이 씁니다). 🔴 **`--hero-fallback`(인라인)과 `landing.css` 의 `var(--hero-fallback)` 은 한 벌입니다** — `image-set()` 을 모르는 브라우저는 **인라인 선언을 통째로 버려서** 그것이 없으면 **배경이 사라집니다.** ▸ **헤더 흐림(커밋 3)** — 투명 상태에도 `blur(28px)` 가 항상 걸려 있던 것을 **배경이 생겼을 때만**으로. ⚠️ **첫 화면 헤더 띠 뒤 사진이 선명해지는 눈에 보이는 변화**가 있습니다. 🔴 시작이 `transparent` 인 것은 그대로입니다. ▸ 🔴 **헤더 로고의 원인은 「픽셀 수」였습니다(커밋 4)** — ① 배경을 **똑같은 흰색으로 맞춰** 재니 평범한 `div` · `position: fixed` · `backdrop-filter: blur(28px)` · `will-change` 네 경우의 로고 그림이 **서로 완전히 같았습니다**(차이 0). **그래서 커밋 3이 로고를 못 고친 것이고, 사용자가 「하드웨어 가속을 꺼도 똑같다」고 한 것과도 맞습니다.** ② 고해상도 PNG 마스크(슈퍼샘플링)는 **크기·배율마다 승패가 뒤집혀** 못 씁니다(이상적 그림 대비 RMS — 27px 에서 SVG **23.06** / 4배 36.10, 36px 에서 6.70 / **3.50**, 46px 에서 **6.65** / 7.53). ③ 남은 변수는 **창 너비**뿐입니다(1121↑ 46px / 1120↓ 36px / 760↓ 30px / **520↓ 27px**). 🔴 **신고 스크린샷의 단추가 짧은 라벨이라 520px 이하임을 알았습니다**(`.landing-nav-short`). ▸ **사용자 확정 (A)안** — 520px 이하에서 **한글을 떼고 「WeCarry」만**. `viewBox` 만 잘라 쓰므로 **새 이미지 파일 0개 · path 하나**이고, 글자가 **17.7 → 28.8px(+63%)** 로 커집니다. 🔴 **폭은 오히려 175.6 → 165.8px 로 줄어** 360px 화면이 **9.8px 덜 빡빡합니다.** ⚠️ 헤더가 **58 → 69px**(+11) 높아지지만 `fixed` 라 아래는 안 밀리고 히어로 위 여백 96px 대비 27px 남습니다. 🟢 **521px 이상은 한 글자도 안 바뀝니다**(14개 폭 전후 대조 · `git stash` 로 같은 하네스에서 기준선을 다시 쟀습니다). 🟢 `BrandLogo` 를 쓰는 나머지 넷은 무변경. ⚠️ **완료조건 둘을 그대로 쓸 수 없었습니다** — 픽셀 평균차 2/255 는 「선명하게」와 **양립하지 않고**(실제 2.604 · 대신 **1/8 축소 후 0.448** + **정렬 탐색 ±8px 최적 `dx=0,dy=0`** 으로 구도 무변을 보였습니다), `npm run lint` 는 **이 저장소에 ESLint 자체가 없습니다**. ⚠️ **`cta-bg.jpg` 도 같은 상황이고 안 고쳤습니다.** ⚠️ **커밋 4는 Preview 확인 없이 merge 했습니다**(히어로 1~3 은 확인받았습니다) | ✅ |
| — | **마감 CTA 배경 축소본 + 운송시간 자동선택 새벽 우선**(PR #170 `a7dbeec` · 파일 **6개** · 커밋 **3개** · **+48 / −24** · 리뷰 라운드 **0**) — 사용자 결정 다섯 건 중 **코드가 필요한 둘**입니다(나머지 셋은 「지금대로」로 닫혔고 **코드 변경이 0**). 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **39행 그대로** · 신규 의존성 0 · 관리자 화면 0줄 · 화주포털 표시 0줄 · `lib/legal/` 0줄 · `globals.css` 0줄. 🔴 **차수를 소비하지 않았습니다.** ▸ **마감 CTA 배경(커밋 1·2)** — PR #169 이 「같은 상황이고 안 고쳤다」로 남긴 자리입니다. 그 띠는 1600px 화면에서 원본 2912px 을 **약 0.55배로 줄여** 그립니다. 축소본 2장(`cta-bg-1600.jpg` **112KB** · `cta-bg-2400.jpg` **303KB**)을 굽고 `image-set()` 으로 고르게 했습니다 — **1배율 전송 437 → 112KB(−74%)**. 🔴 **소스는 61차 커밋 `29f849b` 의 원본입니다**(저장소 파일은 PR #119 재인코딩본이라 그것을 또 줄이면 세대 손실이 겹칩니다 · shallow clone 이라 `git fetch --unshallow` 필요). 조건은 히어로와 같습니다(Lanczos · 품질 90 · progressive · **크로마 4:4:4** · 샤프닝 없음). 🔴 **원본 `cta-bg.jpg` 를 지우지 마십시오**(2배율 이상이 씁니다). ⚠️ **이름을 둘 바꿨습니다** — `heroImageSet()` → **`bgImageSet()`** · `--hero-fallback` → **`--bg-fallback`**(마감 CTA 가 같이 쓰게 되어 「hero」가 사실과 어긋납니다 · **옛 이름을 두 파일 주석에 적어 뒀습니다**). 🟢 **히어로 동작은 한 글자도 안 바뀌었습니다**(dpr 1 → `-1600` 2장 / dpr 2 → 원본 2장 · 배경 위치 82%/88% 전과 동일). 🔴 **`--bg-fallback` 과 `app/landing.css` 의 `background-image: var(--bg-fallback)` 은 여전히 한 벌이고 선택자가 셋이 됐습니다**(`.landing-hero-band`·`.landing-hero-fade`·**`.landing-cta`**). 🟢 **구도 무변** — 평균차 **0.827 / 255** · 1/8 축소 후 **0.197** · 정렬 탐색(±8px) 최적이 `dx=0, dy=0`(**PR #169 이 세운 방식 그대로**이고 이번에는 지시서 완료조건 2/255 에도 들어갔습니다). ▸ **새벽 우선(커밋 3)** — 39차가 *「주말이 시간대보다 먼저다 — 바꾸면 주말 새벽이 또 오른다. **먼저 물을 것**」*으로 열어 둔 미결입니다. 🔴 **`00:00~05:59` 는 요일과 무관하게 `새벽`** 이고 주말 낮(06:00~)은 그대로 `주말` 입니다. ⚠️ **토·일 00:00~05:59 상차 건이 `주말`(+10,000) → `새벽`(+20,000) 으로 +10,000원 오릅니다.** 🔴 **「주말이 먼저다」로 적힌 옛 기록을 근거로 되돌리지 마십시오 — 코드 주석도 같은 커밋에서 고쳤습니다.** ⚠️ **공휴일 새벽도 `새벽` 으로 잡힙니다**(달력이 없습니다 — 하드코딩 금지). 🟢 정의처 `lib/transportTimeAuto.ts` **한 곳**이라 세 화면이 같은 답을 냅니다 · 🟢 **전수 검증 — 7일 × 24시 168칸 중 바뀐 칸이 정확히 12칸**(나머지 156칸 무변경). ▸ 🔴 **코드 없이 닫은 결정 셋** — ① 컴팩트 워드마크를 **521~1120px 로 넓히지 않습니다**(520px 이하 전용 그대로) ② 주소 「동까지만」은 **지금대로**(🔴 **카카오 로컬 API 로 바꾸지 마십시오**) ③ 견적서 가산 줄은 **「외 N건」으로 축약하지 않습니다**(가로 넘침 0). **셋 다 「보류」가 아니라 확정입니다.** 🟢 프리렌더 **46**(기준선 동일) · 🟢 **거짓 ❌ 0건** · ⚠️ **Preview 확인 없이 merge 했습니다**(확인 2건 열림 — Windows Chrome 선명도 · 토·일 새벽 견적) | ✅ |
| — | **배차 취소 · 문제발생 사유 (A장 + B장)**(PR #171 `6b93ba8` · 파일 **24개** · 커밋 7개 · **+1,665 / −45** · 실사용 리뷰 **2라운드**) — 🔴 **DB 변경 있음**(`_migrations` 39 → **40행**). `dispatch_status` 6종 → **7종**(`취소`) · `dispatches` 컬럼 5개 · 기존 행 UPDATE **0** · 백필 **0**. 🔴 **화면 상수 `DISPATCH_STATUS_OPTIONS` 는 6종 그대로이고 거기에 `취소` 를 넣지 마십시오**(드롭다운에서 고를 수 있게 되면 **사유를 못 받습니다**). 🚨 **재배차 후보 조건이 한 곳이 아니라 둘이었습니다**(배차 목록 · **오더 목록** — 한 곳만 고치면 그 화면에서 재배차 버튼이 영영 안 보입니다). 🔴 **정의처 넷** — `lib/dispatchCancel.ts` · `lib/dispatchIssue.ts` · `lib/incidentGuide.ts`(**관리자 전용**) · `lib/portalCancelledDispatches.ts`. 🚨 **「분실」 금지**(§5-23). 🔴 **리뷰 1라운드에 「화주 화면에서 감춘다」가 뒤집혔습니다** · 🚨 **취소 행이 「배차확정」으로 보이고 누르면 그렇게 저장되던 결함**을 덮었습니다 · 🔴 **리뷰 2라운드 신고는 코드가 아니라 「DB 먼저」의 중간 상태**였습니다(`_verify.sql` ㉗ + `origin/main` worktree 대조로 확인) · 곁다리 **`lib/portalRefresh.ts`** | 완료 |
| — | **배차취소 후속 3건 + 견적 금액 → 오더 반영**(PR #172 `9c7cf3f` · 파일 **8개** · 커밋 4개 · **+419 / −83** · 리뷰 라운드 **0**) — 사용자가 `main` 에서 PR #171 을 확인하고 낸 수정 4건입니다(셋은 후속, 하나는 새 기능). 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **40행 그대로** · 신규 의존성 0 · 랜딩 0줄 · 공개 화면 0줄 · `lib/legal/` 0줄 · 관리자 목록 화면 0줄. 🔴 **차수를 소비하지 않았습니다.** ▸ **① 포털 취소 배지가 옅은 레드**(`#FDF3F2`/`#B4423A`) — ⚠️ **하루 전 제가 쓴 `.pv2-dcancel-tag` 주석이 *「`.pv2-dissue`(문제발생)와 색을 같게 하지 말 것 — 취소는 사고가 아니다」* 라고 적고 있었고 사용자가 그것을 뒤집었습니다**(같은 커밋에서 그 문장을 고쳤습니다 — **회색으로 되돌리지 마십시오**). 🔴 **새 색을 만들지 않았습니다**(`DISPATCH_ISSUE_STYLE` 과 같은 값 — 두 배지를 가르는 것은 **색이 아니라 말**입니다). ⚠️ **값이 두 곳에 리터럴로 있습니다**(`lib/dispatchCancel.ts` 의 `DISPATCH_CANCEL_CUSTOMER_STYLE` / `globals.css` — **CSS 는 상수를 못 읽습니다**). ▸ **② 배지 규칙이 「취소된 순간부터 배차확정까지」로** 바뀌었습니다 — 그전에는 **재배차가 접수되는 순간 사라졌습니다**(취소 카드를 감추는데 **새 카드는 이력을 몰랐습니다**). 이제 **새 카드가 이력을 이어받고** 배차가 확정되면 사라집니다. 정의처는 `lib/portalCancelledDispatches.ts` 의 **`getPortalCancelNotice()` 하나**이고 홈·조회가 같이 씁니다 — 🔴 **화면에서 `isDispatchCancelled` 로 다시 적지 마십시오**(고치기 전 동작으로 정확히 되돌아갑니다) · 🔴 **취소 카드를 감추는 것은 그대로**(감춰도 정보가 안 사라져 **감췄던 이유가 해소됐습니다**) · 🔴 **단계 판정은 `lib/dispatchStage.ts`**(`"접수중"` 문자열 금지) · 🔴 **형제 배차를 화면이 든 목록에서 찾지 마십시오** · 🔴 **취소 카드는 DB 가 아니라 `rows` 안에서** 최근 것을 고릅니다(DB 기준이면 그 행이 목록 밖일 때 **통째로 사라집니다** — 홈은 5건입니다) · 🟢 취소도 단계0 도 없으면 **형제 질의 0회**. ▸ **③ 오더 상세 「배차 상세보기」가 살아 있는 배차로** 갑니다 — 🔴 **원인은 조회에 정렬도 상태 조건도 없던 것**입니다(`.limit(1)` 만 · PostgREST 는 `order` 가 없으면 순서를 보장하지 않습니다). 🔴 **취소 건만 있으면 그 건으로 보냅니다**(사유·경위가 거기에만 있습니다) · 🔴 **조회 실패를 삼키지 않습니다**(실패하면 버튼이 「배차관리로 이동」으로 바뀌어 **배차가 없는 것처럼 보입니다**). ▸ **④ 견적 금액을 고치면 연결된 오더 청구금액이 따라옵니다**(`lib/quoteAmountSync.ts` · §5-24) — 🔴 **`customer_charge_vat_included: false` 를 같이 씁니다**(`final_amount` 는 공급가액 · **PR #153 과 같은 자리**) · 🔴 **정산·배차는 안 따라옵니다**(원칙 47번 / 고치는 사람이 다릅니다 — 화면이 「따로 확인하라」고 세 자리에서 말합니다) · 🔴 **묻지 않고 바로 반영이 기본**이고 `배차완료`·`운송중`·`운송완료`일 때만 확인 창(**원칙 39번과 같은 목록**) · 🔴 **금액이 비면 아무것도 안 합니다** · **금액이 그대로면 안 건드립니다**(괜한 `updated_at` 은 포털에 「업데이트」로 뜹니다) · 🔴 **견적 저장이 성공한 뒤에만** 고칩니다. ⚠️ **목이 `limit` 을 무시하던 탓에 ③ 의 「고치기 전」이 다르게 재현됐습니다** — 목을 고치고 나서야 진짜 증상(`d-cancel`)이 나왔습니다(원칙 56번). 🟢 프리렌더 **46**(항목 `diff` 0) · 🟢 **거짓 ❌ 0건** · ⚠️ **Preview 확인 없이 merge 했습니다**(확인 3건 — 배지 톤 · 재배차→배차확정 흐름 · 견적 금액이 오더에 따라오는지 · 🔴 **merge 직후 화주포털 새로고침**) | 완료 |
| — | **수정견적 배지 + 견적·오더 수정 이력 + 리뷰 5건**(PR #173 `e80f32c` · 파일 **17개** · 커밋 8개 · **+1,554 / −81** · 실사용 리뷰 **2라운드(5건)**) — 사용자 지시 3건으로 시작해 리뷰에서 5건이 더 붙었습니다. 🔴 **DB 변경 있음**(`_migrations` 40 → **41행**) · 신규 의존성 0 · 랜딩 0줄 · 공개 화면 0줄 · `lib/legal/` 0줄 · `globals.css` **순수 추가**(+21 / −0) · 화주포털은 **배지 한 곳뿐**. 🔴 **여덟 건 중 여섯이 한 뿌리입니다** — PR #172 가 「승인 뒤 금액 조정」을 정식 경로로 만들면서 화주가 알 자리·담당자가 되짚을 자리·견적서 표기·조정하러 가는 길이 한꺼번에 필요해졌습니다. 🚨 **표를 새로 만들지 않고 `activity_logs` 를 되살렸습니다**(21차가 「쓸 일이 생기면 정책을 만들면 된다」로 남긴 표 · 원칙 27번) — **그런데 그 표의 `user_id` 가 죽은 `profiles` 를 참조하고 있었습니다**(`quotes.created_by` 사고와 **같은 시대·같은 원인**). 그대로 뒀으면 **화면에 아무 증상 없이 이력이 한 줄도 안 쌓였습니다.** 🔴 **레거시 표를 되살릴 때는 컬럼 이름만 보지 말고 제약·인덱스를 실측하십시오.** 🔴 **배지 신호는 `revised_at` 이고 `updated_at` 이 아닙니다**(되돌리면 거의 모든 견적에 붙습니다 · PR #164 의 교훈) · **`수주` + 금액이 실제로 바뀐 때만** 찍습니다 · **소멸 판정은 배차 단계**입니다(`orders.status` 로 재면 `하차완료` 건이 화주 눈에는 완료인데 배지만 남습니다). 🚨 **리뷰가 이 PR 의 결함을 하나 찾았습니다** — 이력을 오더 상세에만 붙였는데 담당자가 금액을 바꾸는 자리는 **견적 쪽이 더 많습니다**(PR #172 가 그 경로를 만든 이유입니다). 고친 자리는 화면이 아니라 **`syncQuoteAmountToOrders()` 안**입니다(원칙 53번). ⚠️ **「최종금액 직접 입력」 차액이 「조정」 줄이 아니라 기본운임으로 흡수됩니다** — **36차 E장의 「기본운임을 덮어쓰지 않는다」를 뒤집은 것**이고, 그때의 사용자 원문도 **「기본운임에서 + - 되어서」**였습니다(그 세션이 「줄을 더하는」 쪽으로 읽었습니다). 🔴 **E장의 걱정(되짚을 수 없다)은 이 PR 의 수정 이력이 답합니다.** 🔴 취소 사유 **「운임료 조정 필요」**(이름만 · 코드 `fare_disagreement` 는 유지 — CHECK 가 없어 코드를 바꾸면 옛 행이 이름을 잃습니다) · **`driverFault` 를 `true` 로 되돌리지 마십시오**(우리 가격 문제를 차주 이력에 세면 그 숫자를 아무도 못 믿습니다 — §7 이 열어 둔 물음의 답입니다). 🔴 취소 배차만 남은 오더에 **「배차 상세보기」+「+ 배차 등록」 둘 다**(`linkedDispatchId` 하나로 가르면 재배차 버튼이 사라집니다 — 지금까지가 그 상태였습니다). 🔴 배차 목록 **「취소건 숨기기」는 기본 꺼짐 + 감춘 건수 표시**이고 **조회에서 `.neq()` 로 빼지 마십시오**(상한이 취소 건을 못 셉니다). 🔴 발주요청 **「전화로 처리함」을 지우거나 숨기지 마십시오**(PR #167 ① 의 유일한 탈출구). ⚠️ **거짓 ❌ 11건은 전부 하네스가 틀린 것**이었습니다(목에 `customer_accounts` 누락 8 · **쿠키 세션 미주입으로 `isAdmin` false** 2 · middleware 307 1). 🟢 단위 **62** · 렌더 **47** · 프리렌더 **46**(항목 `diff` 0). 🟢 **사용자 확인 완료**(2026-09-17 *「모두 확인했다. 잘적용이 되었다」*) | 완료 |
| — | **공지사항 간이 서식 + 미리보기 + 수정**(PR #174 `94e60d3` · 파일 **11개** · 커밋 4개 · **+1,215 / −39** · 리뷰 라운드 **0**) — 🔴 **DB 변경 있음**(`_migrations` 41 → **42행**) · 🟢 **신규 의존성 0**. 🚨 **지시서 v1 은 정식 편집기 도입**(Tiptap + sanitize-html)**이었고 착수 전 보고를 받은 사용자가 범위를 줄였습니다** → **ⓐ 간이 서식**(*「너무 작업량이 과해지는게 아닌지 우려스럽다」*). 🔴 **「지시서에 정식 편집기라고 적혀 있다」를 근거로 라이브러리를 끌어오지 마십시오.** 🚨 **안전 구조가 정식 편집기와 반대입니다** — HTML 을 받아서 골라내는 것이 아니라 **전부 이스케이프한 뒤 허락한 표기만 태그로 만들어 냅니다**. 🔴 **이스케이프를 미루거나 건너뛰지 말 것 · `[html]` 같은 통과 표기를 만들지 말 것.** 정의처는 **`lib/announcementMarkup.ts`** 하나, 그리는 부품은 **`components/AnnouncementBody.tsx`** 하나(🔴 **미리보기 전용 렌더러 금지**). 🔴 **`content_format`(`plain`|`markup`)이 옛 공지의 줄바꿈을 지킵니다.** 🚨 **「새 공지」 판정이 `created_at` → `announced_at`**(「화주에게 다시 알림」 기본 꺼짐 · 쓰는 곳 **셋**). 🔴 **이미지 올리기(C장)는 안 했습니다** | 완료 |
| — | **이용가이드 재작성 + 시각 개편**(PR #175 `78f5a33` · 파일 **19개** · 커밋 4개 · **+1,636 / −100** · 실사용 리뷰 **3라운드**) — 🟢 **DB 0 · `_migrations` 42행 유지 · 신규 의존성 0**. 🔴 **PR #149 이후 아홉 차수가 가이드에 하나도 반영돼 있지 않았고**, 더 나쁜 것은 **「지금 화면에 없는 말」**(「상담 중」 — PR #164 가 「확인중」으로 바꿨습니다)**을 적고 있던 것**입니다. 항목 **14 → 21개**. 🔴 **기존 항목 id 14개를 한 글자도 안 바꿨습니다**(`?topic=` 이 곧 URL). 🟢 **첫 커밋은 화면 파일 0줄**(있는 블록만 조합). 리뷰로 **시각 개편** — `GuideIcon` **20종 인라인 SVG** · 새 블록 **`steps`·`cards`·`figure`** · **바로가기 칩 줄**. 🚨 **화주포털 캡처 6장** — **실계정이 아닙니다**(`scripts/guide-shots.mjs` 가 가짜 값을 물려 찍습니다 · 🔴 **실계정으로 다시 찍지 마십시오**). 🚨 **고객센터 시간이 전 화면 09:00 ~ 19:00** 이 됐습니다(상수를 둘로 갈랐다가 **같은 날 도로 합쳤습니다** · 🔴 **다시 둘로 가르지 마십시오**). 🚨 **로그인 화면의 시간 줄은 둘이고 뜻이 다릅니다**(「접수 기준 당일 발급」은 **발급 약속**이고 31차가 **랜딩 FAQ 와 맞춰 둔 값** — **셋을 함께** 올렸습니다). 자세한 것은 **§5-13** | 완료 |
| — | **문자 발송 정리**(PR #176 `4f2e1b8` · 파일 **16개** · 커밋 **7개**(조사 2 + 구현 5) · **+1,204 / −203** · 실사용 리뷰 라운드 **0**) — 🔴 **DB 변경 있음**(`_migrations` 42 → **43행**). 🚨 **지시서의 「DB 변경 없을 것으로 봄」이 틀렸습니다** — `sms_logs_template_type_check` 가 **8종 고정**이라 코드를 먼저 올리면 고객 문자가 나가기는 하는데 **이력만 조용히 안 남습니다**(`sendSmsWithLog` 는 예외를 던지지 않아 화면에 증상이 없습니다). **A장** 상차·하차완료 문자 **폐지**(🔴 라벨·타입·CHECK 의 두 값은 **남깁니다** — 옛 이력 표시·재발송). **B장** 배차확정 **두 통**(차주 「화물정보 안내」 + 고객 「배차확정 안내」 **신설** · 확인창 큐 **1/2 → 2/2** · 공용 모달에는 선택 prop **하나만** · `dispatches.external_vehicle_type` 신설). **C장** 머리말 **`[위캐리운송]`** 통일(7종 + LMS 제목) · 견적 버튼 **「견적서 문자 발송」** · 🚨 **재발송이 87byte 견적 링크 문자에 제목을 붙여 LMS 로 올리던 결함** 수정(`smsSubjectFor()` 한 함수로). 🚨 **곁다리로 날짜가 9시간 어긋나던 기존 결함을 고쳤습니다**(Vercel 함수가 UTC 인데 `getHours()` 를 썼습니다 — 배차확정 문자 발송 이력이 0건이라 실피해 없음). 🟢 **화주포털·랜딩·공개 화면·`globals.css`·`lib/legal/` 0줄 · 신규 의존성 0**. 자세한 것은 **§5-25** | 완료 |
| — | **포털 「접수」 카드 + `/apply`·`/quote` 모바일 정리**(PR #177 `f4c0c11` · 파일 **12개** · 커밋 7개 · **+680 / −51** · 실사용 리뷰 **3라운드 7건**) — 🟢 **DB 0** · 마이그레이션 0 · `_migrations` **43행 그대로** · 신규 의존성 0 · `globals.css` **0줄** · `lib/legal/` **0줄** · 관리자 화면 **0줄**. 🔴 **차수를 소비하지 않았습니다.** 🔴 **코드를 열기 전에 운영 DB 를 쟀습니다**(`_verify.sql` **㉛** 신설 · 읽기 전용) — **수주인데 오더 없음 0 · 오더인데 배차 없음 0**. 막힌 데이터가 아니라 **승인부터 담당자가 배차를 등록하기까지 화면이 비어 있던 것**이 원인이라 **DB 변경이 0** 이 됐습니다(PR #154 가 세운 「화면보다 DB 를 먼저」가 또 값어치를 했습니다). 🔴 **오더를 자동 생성해서 메우지 마십시오**(27차 확정) — **DB 에 아무것도 만들지 않는 가상 카드**입니다 · 정의처 **`lib/portalPendingDispatches.ts` 하나**를 조회 화면과 홈이 같이 씁니다 · **두 갈래를 다 셉니다**(①만 만들면 오더가 생기는 순간 카드가 사라졌다 배차 후 다시 나타납니다) · ⚠️ **`orders.quote_id` 가 비면 같은 건이 카드 둘**이 됩니다(관리자 「견적 연결」이 잇는 길 · 실측 0건). 🔴 **곁다리 — `WON_QUOTE_STATUS` 정의처를 `lib/quoteStatusLabels.ts` 로 옮겼습니다**(`lib/unlinkedWonQuotes.ts` 가 관리자 클라이언트를 import 해서 포털이 가져다 쓰면 **포털 번들에 관리자 세션이 딸려 들어갑니다** — 원칙 1번). 🔴 **`/apply` 문구를 두 번 고쳤습니다**(→ **「전화로도 신청 가능합니다」**) · 이메일이 **`[아이디] @ [도메인]` + 고르기 11종**(저장값은 문자열 하나 · 도메인 칸을 잠그지 않습니다) · 🚨 **모바일 동의 설명글은 제 판단이 리뷰에서 뒤집혔습니다**(수집·이용 목적 고지라 약관 요약만 감췄다가 **두 카드 모두 + `/quote` 까지** 확정 — 🔴 **「전문 보기」와 거부권 문단은 반드시 남깁니다**) · 🔴 **`/quote` 거부권 한 줄은 이번에 처음 생겼습니다**(통째로 없었습니다) · 전화 카드 **271 → 165px**(문구 무변 · 데스크탑 불변) · 모바일 폼 항목 제목 **15px/700/`#4A4945`**(손잡이 `landing-field-label` **18곳**). ⚠️ **거짓 ❌ 3건은 전부 제 측정이 틀린 것**이었습니다(뷰포트 비율 · 임의의 절대값 — **같은 화면에서 감춘 것만 되살려 전후를 재는** 방식으로 바꿨습니다). 🟢 프리렌더 **46**(항목 `diff` 0) · 단위 21 · 렌더 117항목. 자세한 것은 **§5-26** | 완료 |
| — | **라보 운임을 다마스 기준으로 통일**(PR #178 `5f0d969` · 파일 **3개** · 커밋 1개 · **마이그레이션 1건 + `_verify.sql` +6 / −1** · 리뷰 라운드 **0**) — 🔴 **DB 변경 있음**(`rate_distance_tiers` UPDATE **15칸** · `_migrations` 43 → **44행**) · 🟢 **제품 코드 0줄**(`app`·`components`·`lib`·`public` 변경 파일 0) · 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄. 🔴 **차수를 소비하지 않았습니다.** 🔴 **코드를 열기 전에 운영 DB 를 쟀고 그것이 지시의 뜻을 바꿨습니다** — 다마스 `10km 이내` 는 **이미 30,000**(v12 A장)이라 **다마스는 0칸 · 라보만 15칸**입니다. 🚨 **곁다리로 100원 오타 두 칸**(라보 110km 85,100 · 400km 225,100)이 같이 씻겼습니다 — `/admin/rates` 는 **클릭이 곧 저장이고 되돌리기가 없습니다.** 🔴 **차급 역전 검사를 다마스↔라보 한 쌍만 동가 허용으로 좁혔습니다**(`<` 로 전부 바꾸면 11쌍이 헐거워집니다) · 검증은 **로컬 Postgres 16 에 운영 표를 복제해 실제로 돌렸고**(함정 31번) 부정 시험 2건이 의도한 단언에서 멈추는 것까지 확인 | 완료 |
| — | **견적관리 전화응대 매뉴얼**(PR #179 `4bc1c76` · 파일 **6개** · 커밋 1개 · **+903 / −12** · 리뷰 라운드 **0**) — 사용자 요청 *「견적관리에서 전화응대 메뉴얼 창이 있으면 좋겠다. 자동계산결과 창아래 따로 창이 있어서 마찬가지로 스크롤 따라 가게(모바일버전은 생략) … 줄칸을 늘릴수 있고 줄일수 있게 설정. 줄칸 앞에 넘버링만 있으면 된다」*. 🔴 **DB 변경 있음**(`call_script` 표 1건 · `_migrations` 44 → **45행**) · 🟢 화주포털 0줄 · 공개 화면 0줄 · 랜딩 0줄 · `lib/legal/` 0줄 · 신규 의존성 0 · `globals.css` **순수 추가**(+231 / −0). 🔴 **사용자 확정 「전 직원 공유 · 관리자만 편집」**(「직원마다 자기 것」을 안 골랐습니다) · 🚨 **쓰기 정책을 아예 안 만들었습니다 — 그것이 안전 구조입니다**: 읽기만 RLS 정책(`staff_read_call_script` · 조건은 **`public.is_active_staff()`** — 화주포털 계정도 `authenticated` 라 빼면 화주가 내부 매뉴얼을 읽습니다)을 열고, 쓰기는 service_role 저장 API 가 `role === "admin"` 을 확인한 뒤에만 합니다(원칙 25번) · 🔴 **행은 하나뿐**(`check (id)`)이고 줄 목록은 **jsonb 배열**입니다(행으로 두면 `sort_order` 재정렬 + 저장이 쪼개져 절반만 반영됩니다 · 원칙 40번 방식은 PR #136 에서 3행이 되며 사고가 났습니다) · 🔴 **번호는 CSS 카운터라 저장하지 않습니다** · 🔴 **sticky 를 계산 카드에서 `.quote-side` 로 올렸습니다**(형제 둘을 같은 `top` 으로 각각 sticky 로 두면 겹칩니다) · ⚠️ **폼이 접혀 있으면 안 따라옵니다 — 고장이 아닙니다**(sticky 는 자기 격자 칸 안에서만 움직이고, 계산 패널도 전부터 같은 조건이었습니다) · 🚨 **`lib/callScript.ts` 에 `lib/supabaseClient` 를 들였다가 `next build` 가 멈췄습니다**(PR #151 과 같은 자리) · 검증은 **로컬 Postgres 16 복제**(함정 31번) 부정 시험 **5건이 전부 의도한 단언에서 멈추고 롤백** · 🚨 **단언 하나가 되돌림을 「10줄인가」로 재서 2회차에 멀쩡한 DB 를 멈춰 세웠습니다**(시험 직전 값과 견주도록 수정) | 완료 |
| — | **24시콜 ⓑ 견적 폼 → ⓒ 정산 엑셀** — ⓐ 다음입니다. 고치는 화면이 달라(배차 상세 / 견적 폼 + 오더·배차 생성 / 정산) 파일이 거의 안 겹칩니다. 🟢 설계서 §8 「35차에 먼저 넣을 것」 2건은 PR #146 에 들어갔습니다 — ① 선착불 받을 돈 ② **정산 자동등록 공용 함수 `lib/autoCreateInvoice.ts`**(ⓒ「정산 엑셀로 운송완료」가 부를 함수입니다). 🔴 **v3 §5 매핑표는 35차와 v12 가 낡게 만들었습니다** — 자세한 것은 `CLAUDE.md` §5 의 「24시콜 가져오기 — 설계 정리 v4」 블록. 🔴 **ⓑ 는 목록 행만으로는 안 됩니다** — 상하차 방법·시점·특이사항·상세주소·거리·금액이 통째로 안 옵니다(2026-09-16 샘플 실측). 🔴 **v4 5장 매핑표도 낡았습니다**(금액·화물번호를 전제로 쓴 표라 ⓐ 실제 구현과 다릅니다) | **다음** |
| — | **내부 정합 ② 화주·차주 — CRM 항목 신설**(업종·잠재등급·주요 노선 등) + 차주. 🔴 **옛 37차이고 2026-09-16 에 뒤로 미뤘습니다** — 실측이 반대를 가리킵니다(화주 등록 **539건**인데 활성 **7건** · 36차가 만든 거래조건이 **539건 전부 비어 있고** · 배차 6건이 전부 외부 배정이라 내부 `drivers` 에 쌓인 차주가 없습니다). 🔴 **값이 동작을 일으키지 않는 칸은 아무도 안 채웁니다** — 36차 거래조건은 「오더 기본값」이라는 동작이 붙어 채울 이유가 있었지만 업종·잠재등급은 넣어도 화면이 달라지지 않습니다. **그 값으로 무엇을 할지가 정해진 뒤**에 만드는 것이 순서입니다. 🟢 **영업 활동 기록(접촉 이력·후속 연락일)도 지금은 아닙니다**(사용자 확인 2026-09-16: *「539건을 아직 거의 안 돌렸다」*). ⚠️ **거래조건은 36차가 가져갔습니다** | 미정 |

**미정** — admin 공개문의 배지 · 상차 시각 지정 가산 신설 · anon GRANT 회수 · 접수 안내 문자 신설(⚠️ **없는 기능을 새로 만드는 것**이라 「소수정」이 아닙니다 — `/quote`·`/apply` API 에 발송 트리거가 붙는 별도 차수입니다) · **직원 로그인 실패 제한의 지속 저장**(지금은 프로세스 메모리라 Vercel 다중 인스턴스에서 완벽하지 않습니다. 🔴 IP 를 저장하려면 **처리방침 제2조도 같이** 고쳐야 합니다)
🟢 **2026-09-17 에 결정 셋이 닫혔습니다**(PR #170) — **컴팩트 워드마크를 521~1120px 로 넓히지 않습니다** · **주소 「동까지만」은 지금대로입니다** · **견적서 가산 줄을 「외 N건」으로 축약하지 않습니다.** 🔴 **셋 다 「보류」가 아니라 확정이니 「미정」에 다시 넣지 마십시오.**
🟢 **「화주 목록 페이지네이션」은 PR #144 에 해소됐습니다**(15행씩 · 모바일 카드 포함) — 「미정」에 다시 넣지 마십시오.
🟢 **「다마스·라보 적재 한도」는 2026-09-11 에 확정됐습니다**(§5-1 — 둘 다 500kg 이내이고 갈리는 것은 높이·형태입니다) — 「미정」에 다시 넣지 마십시오.
🟢 **「5톤급 운임 재검토」는 PR #135(v11)에 해소됐습니다** — 「미정」에 다시 넣지 마십시오.
🟢 **B장(혼적 할인 거리 3단계)은 PR #136에 끝났습니다** — 「미정」에 다시 넣지 마십시오. 🟢 **31~250km 35% 도 2026-09-11 에 「현행 유지」로 확정됐습니다**(사용자) — 🔴 **「맞다고 검증된 값」이 아니라 「지금은 안 바꾸고 실거래를 모은다」는 뜻입니다.** 여전히 **직접 비교 쌍 0쌍인 보간값**이고 하필 가장 자주 걸리는 구간이라, **그 구간의 혼적 실적이 쌓이면 그때 다시 봅니다.** 🔴 담당자가 매번 손으로 고치고 있다면 그게 다시 볼 신호입니다. 바꾸는 자리는 `/admin/rates` → 「가산기준」 탭 → 「표준 혼적 할인율」이고 **화면에서 고치면 저장소 문서에는 안 남습니다**(30km 가 실제로 그렇게 15 → 20 이 됐습니다).
🟢 **담당자 정보(`customer/profile`)는 PR #121에 끝났습니다** — 「미정」에 다시 넣지 마십시오.
🟢 **「화주포털 B장」은 PR #161 에 끝났습니다**(2026-09-16, 같은 날) — 「미정」에 다시 넣지 마십시오. ⚠️ 그때 「먼저 정하라」고 적었던 **배차 SMS 와 겹치는 문제는 실재하지 않았습니다** — 배차확정 문자는 **차주**에게 가고 상차·하차완료는 **자동 발송이 없습니다**(PR #73 이후 수동 버튼뿐). 🔴 **짐작으로 적은 제약이 한 차수를 막고 있던 자리입니다 — 「겹친다」류의 전제는 재고 나서 적으십시오.**

🔴 **차수 번호는 예고가 아니라 실제 실행 순서로 붙입니다.** 화면 차수가 지금까지 **열한 번** 뒤로 밀렸고(가장 최근은 2026-09-10 — 34차 지시서가 「내부 정합 ①」이 아니라 **견적관리 화면 개편**으로 와서 내부 정합이 **35~36차**로 밀렸습니다), **차수를 아예 쓰지 않은 세션이 15번** 있었습니다.
🔴 **세션을 병행하면 차수가 겹칩니다** — 번호는 **먼저 merge한 쪽**이 가져갑니다(67·68·69차에서 실제로 발생). `CLAUDE.md`는 **merge 직후에** 쓰십시오. PR 안에 넣으면 반드시 충돌합니다.

---

## 7. 미결·위험

### 🟢 공개를 막는 코드 작업은 없습니다

v20의 유일한 차단 항목이던 **랜딩 이미지가 전부 해소됐습니다** — 30차에 디자인팀 이미지 32종이 들어왔고, 마지막 「고정화물 배차」 카드 사진도 68차에 채워졌습니다.
🔴 **`lib/landingImages.ts`에 자리표시자(`null`)를 다시 허용하지 마십시오.**

### ⏳ 화주포털 「접수」 카드가 남긴 것 (PR #177)

| | |
|---|---|
| ⚠️ **`orders.quote_id` 가 비면 같은 건이 카드 둘이 됩니다** | 담당자가 `?from_quote=` 를 거치지 않고 오더를 직접 만들면 가상 카드(수주 견적)와 진짜 배차 카드가 **함께** 뜹니다. 🔴 **새 판정 기준을 만들지 마십시오** — 관리자 TopNav 「견적 관리」 배지가 **같은 기준**으로 세고 있고 오더 상세의 **「견적 연결」**이 뒤늦게 잇는 길입니다. 🟢 실측(2026-09-18) 어긋난 건 **0건** |
| ⚠️ **모바일 동의 설명글을 감춘 것이 변호사 검수 항목에 걸립니다** | 감춰지는 문장이 **수집·이용 목적**(`/apply`)과 **목적 + 보유기간**(`/quote`)입니다. 🔴 **사용자 확정이고 되살리지 마십시오.** 남는 것은 **「전문 보기」와 거부권 문단** 둘이며, 제15조 2항을 온전히 채우는 문제는 **원래부터 열려 있던 검수 항목**입니다(아래 「변호사 검토 미실시」). 🟢 이번에 `/quote` 거부권 하나는 **오히려 메웠습니다** |
| ⏳ **`APPLY_CONSENT.detail` 을 원래대로 되돌릴지** | 첫 커밋에서 한 문장으로 줄였는데(뗀 것은 「승인 여부와 무관하게 안전하게 관리됩니다」 — **법이 요구하는 항목이 아닙니다**), 이제 모바일에서는 안 보이므로 **데스크탑 문구를 되돌려도 됩니다**(한 줄). 사용자가 정합니다 |

### ⏳ 배차 취소 · 문제발생 사유가 남긴 것 (PR #171 · 후속 PR #172)

| | |
|---|---|
| 🟢 ~~취소 사유 7종이 실무와 맞는지~~ **닫혔습니다**(PR #173, 2026-09-17) | 「운임 협의 결렬」 → **「운임료 조정 필요」**(사용자 지시)로 이름이 바뀌었고, 그러면서 뜻이 「차주와 협상이 깨졌다」에서 **「우리 운임이 낮아 올려야 한다」**로 옮겨져 **`driverFault` 를 `false` 로** 바꿨습니다. 🔴 **`true` 로 되돌리지 마십시오** — 우리 쪽 가격 문제를 차주 이력에 세면 그 숫자를 아무도 못 믿습니다. 🔴 **코드(`fare_disagreement`)는 그대로**입니다(DB 에 CHECK 가 없어 코드를 바꾸면 이미 저장된 행이 **이름 없는 사유**가 됩니다). 🟢 그 사유로 취소하면 **견적 금액 수정 화면으로 보내고 금액 칸을 옅은 빨강으로 강조**합니다 |
| 🚨 **배차 취소 시 고객에게 문자를 보낼지** | 2026-09-18 부터 고객이 **배차확정 문자로 기사 이름·번호를 받습니다**(§5-25). 그 뒤 **차가 바뀌면 고객이 옛 기사에게 전화**할 수 있는데, 배차 취소(PR #171·#172)는 **배너 + 푸시**만 넣었습니다. 🟢 재배차가 확정되면 **새 고객 문자가 나가므로 결국은 맞춰집니다** — 문제는 **그 사이의 공백**입니다. 🔴 **사용자에게 물어야 합니다**(문자를 한 통 더 보낼지 / 지금대로 둘지). 🔴 **묻기 전에 취소에 문자를 붙이지 마십시오** |
| ⏳ **배차가 없는 오더의 버튼 이름** | 취소 배차만 남은 오더에는 **「+ 배차 등록」**, 배차가 아예 없는 오더에는 **「배차관리로 이동」**입니다 — **가는 곳은 같은데 이름이 둘**이라 헷갈릴 수 있습니다(PR #173 에서 최소 변경으로 뒀습니다). 통일하면 **한 줄**입니다 |
| ⏳ **문제발생 사유 8종에 빠진 것** | 지연 / 파손 / 화물 없어짐 / 차량 사고 / 오배송 / 인수 거부 / 차량 고장 / 기타. 같은 이유로 **한 줄이면 늡니다**(`lib/dispatchIssue.ts`) |
| ⏳ **문제발생에도 웹 푸시를 보낼지** | 지금은 **화면에만** 뜹니다 — 38차가 *「내부 사건이라 성격이 다르다」*로 뺀 자리이고 이번에도 안 넣었습니다. **사고는 전화가 먼저**라는 판단이면 이대로 둡니다 |
| ⚠️ **차주별 취소 건수가 지금은 전부 0** | 운영 배차가 **전부 외부 배정**이라 `driver_id` 가 **0건**입니다(외부 차주는 `external_driver_name` 자유텍스트로만 남습니다). 🔴 **「값이 안 나오니 지우자」로 없애지 마십시오** — 사용자 확정 (A)로 구조만 세워 두고 값이 쌓이기를 기다리는 것입니다 |
| 🔴 **배차 금액은 안 따라갑니다**(PR #172) | 견적 금액을 고치면 **오더까지만** 따라갑니다(§5-24) — 배차의 화주 청구금액은 배차 담당자가 정하고 정산은 스냅샷입니다. ⏳ **「배차 금액도 자동으로」가 필요해지면** 🔴 **「어느 상태까지 덮어쓸 것인가」를 먼저 정하십시오**(배차확정 이후의 금액은 차주와 합의된 값이 얽힙니다) |
| 🔴 **「DB 먼저」의 대가** | 마이그레이션을 merge 전에 반영하는 순서는 **맞습니다**(반대면 CHECK 위반으로 배차 저장이 통째로 막힙니다). ⚠️ **다만 그 창에서 운영 화면이 새 값을 모릅니다** — 이번에 실제로 취소 건이 화주 화면에 **「접수」**로, 관리자 목록에 **「배차확정」**으로 보였고 그것이 신고로 돌아왔습니다. 🔴 **다음에 같은 순서를 쓸 때는 「merge 전까지 운영 화면이 어떻게 보이는지」를 먼저 말하십시오** |

### 🔴 히어로·헤더 로고 선명도가 남긴 것 (PR #169)

| | |
|---|---|
| 🔴 **520px 이하 워드마크에 한글이 없습니다** | **없어진 것이 아니라 그렇게 정한 것입니다**(사용자 확정 (A)안). 그 폭에서 로고가 27px 까지 줄어 한글 글자가 **약 16px** 이라 계단처럼 깨졌습니다. 🔴 **컴팩트 판을 없애고 27px 로 되돌리면 그 신고가 그대로 돌아옵니다.** 값은 `components/BrandLogo.tsx` 의 **`WORDMARK_VIEWBOX_COMPACT`(`9 25 569 151`)** 하나이고 🔴 **`569` 를 줄이지도 늘리지도 마십시오**(아트웍이 `x 571.37` 까지 있고 한글이 `x 609.4` 에서 시작합니다 — 줄이면 **사선 띠 끝이 잘리고** 610 을 넘기면 **한글 획이 비어져 나옵니다**) |
| 🔴 **로고가 거칠다는 신고를 받으면 합성·흐림을 의심하지 마십시오** | 이 자리는 **이미 배제됐습니다** — 배경을 같게 맞춰 재니 `fixed`·`backdrop-filter`·`will-change` 가 **평범한 `div` 와 차이 0** 이었습니다. 남은 변수는 **로고가 받는 픽셀 수**뿐이고 그것은 **창 너비**가 정합니다. ⚠️ 비교할 때 **배경을 반드시 같게** 만드십시오(안 그러면 배경 차이가 지표를 먹습니다) |
| 🔴 **고해상도 PNG 마스크를 되살리지 마십시오** | 크기·배율마다 승패가 뒤집혀 **일관된 개선이 없습니다**(27px 에서는 SVG 가, 36px 에서는 마스크가 낫습니다). 넣으면 `currentColor` 를 잃고 26KB 가 늘 뿐입니다. ⚠️ **「하드 경계 개수」를 지표로 쓰지 마십시오** — **흐리기만 해도 줄어듭니다.** 뜻이 있는 것은 **16배 렌더를 Lanczos 로 줄인 기준 그림과의 RMS** 뿐입니다 |
| 🟢 **521~1120px 구간은 그대로 두기로 확정됐습니다** | 그 구간 로고(30px·36px)도 한글이 **18~21px** 이라 같은 성격의 거칠기가 있지만, **컴팩트 판을 넓히지 않는 것이 사용자 확정입니다**(2026-09-17). 🔴 **「사용자가 정합니다」로 적힌 옛 줄을 보고 다시 묻지 마십시오** — 컴팩트 워드마크는 **520px 이하 전용**입니다 |
| 🟢 **`cta-bg.jpg` 는 PR #170 에 고쳤습니다** | 원본 2912×1632 인데 1600px 화면에서 **약 0.55배**로 축소되던 자리입니다. 히어로와 같은 방식(축소본 2장 + `image-set()` · 4:4:4)으로 **1배율 전송 437 → 112KB(−74%)**. 🔴 **「안 했습니다」로 적힌 옛 줄에 속지 마십시오** |
| ⚠️ **커밋 4는 Preview 미확인입니다** | 히어로(커밋 1~3)는 사용자가 「개선됐다」로 확인했지만 **컴팩트 워드마크는 확인 없이 merge 했습니다**(merge 지시를 받았습니다). 볼 것: ① 좁은 폭에서 「WeCarry」만 나오고 매끈한지 ② **데스크탑 전체화면이 전과 같은지** ③ 좁은 화면에서 **헤더 띠가 11px 높아진 것**이 거슬리지 않는지 |
| 🔴 **`npm run lint` 를 완료조건에 쓰지 마십시오** | 이 저장소에는 **ESLint 설정 파일도 의존성도 없습니다**(`.eslintrc*` 0건 · `package.json` 에 eslint 0건) — `next lint` 가 **설정 마법사에서 멈춥니다.** 검사는 **`tsc --noEmit` + `next build`** 입니다 |
| 🔴 **이미지 차수의 완료조건은 이렇게 쓰십시오** | **원본 대비 픽셀 평균차**는 「선명하게」와 양립하지 않습니다(고주파가 반드시 달라집니다). **축소본 대비 평균차 + 정렬 탐색(±8px)** 으로 **구도가 움직였는가**를 재십시오 |

### 🔴 39차가 남긴 것 (PR #165)

| | |
|---|---|
| ⚠️ **Preview 미확인** | 리뷰 1라운드 반영 직후 사용자가 merge 를 지시해 **눈으로 확인된 것이 없습니다.** 볼 것: ① 빨간 `*` 색 톤(`#B4423A`) ② **화주포털 발주요청**의 표시 자리 ③ 일정 칩 한 줄(실기기 · 특히 360px) |
| ⚠️ **`/apply` 도 빨간 `*` 가 됐습니다** | 사용자가 든 것은 **견적문의와 발주요청 둘**이지만 `/apply` 가 같은 정의처(`requiredMark`)를 써서 함께 바뀌었습니다. 🔴 **`/apply` 만 회색 글자로 남기면 공개 폼 둘이 갈립니다** — 그 이유로 셋 다 바꿨고 merge 보고에 밝혔습니다. **원하지 않으면 되돌립니다**(답은 아직 없습니다) |
| 🟢 **주말 vs 새벽 우선순위는 닫혔습니다** | **새벽이 먼저입니다**(PR #170 · 사용자 확정 「새벽우선」). `00:00~05:59` 는 요일과 무관하게 `새벽`(+20,000)이고 주말 낮은 그대로 `주말` 입니다. ⚠️ **토·일 00:00~05:59 상차 건이 +10,000원 올랐습니다.** 🔴 **「주말이 먼저다」로 적힌 옛 기록을 근거로 되돌리지 마십시오** — 자리는 `lib/transportTimeAuto.ts` 의 `classifyTransportTime()` 한 곳이고 **코드 주석도 같이 고쳤습니다** |
| ⚠️ **18:00~19:59 상차 건의 견적이 올랐습니다** | 0원 → **+10,000원**(`출퇴근/혼잡`). **사용자 확정**(「여섯 종 전부 자동」)이고 06:00~07:59 는 이름만 바뀝니다. 🔴 **그전에는 `출퇴근/혼잡`·`새벽`을 아예 안 쓰고 있었습니다** — 고장을 고친 결과이지 값을 올린 것이 아닙니다 |
| ⚠️ **`공휴일`은 자동으로 못 고릅니다** | 저장소에 공휴일 달력이 없습니다. 목록에는 그대로 있어 담당자·화주가 직접 고릅니다. 🔴 **달력을 하드코딩하지 마십시오**(매년 바뀝니다). 넣으려면 「어디서 받아올지」를 먼저 정해야 합니다 |
| 🟢 **「동까지만」 주소는 「지금대로」로 확정됐습니다** | 다음 우편번호 팝업으로는 **안 됩니다**(실물 확인 — §5-21). 🟢 **직접 타이핑은 이미 되고 거리 계산도 됩니다.** 사용자가 **현행 유지**를 골랐습니다(2026-09-17). 🔴 **카카오 로컬 API 로 바꾸지 마십시오** — 키·엔드포인트가 이미 있어(`/api/distance`) 유혹이 크지만 **D장이 「안 고르는 길」로 못박았고 저장값 출처가 바뀝니다** |
| ⚠️ **주소 저장값 전후 비교가 미확인입니다** | 이 환경에서 **실제 팝업을 못 띄웁니다**(스크립트 CDN·공식 문서 둘 다 프록시 403). 목으로 25항목을 쟀지만 **우편번호·건물명까지 같은지는 사용자가 실물로** 봐야 합니다 |
| ⚠️ **주소칸 placeholder 가 그대로입니다** | 「도로명주소 검색 또는 직접 입력」인데 이제 **지번도 그대로 들어갑니다.** 🔴 **화면마다 placeholder 를 따로 넘기는 구조**라 바꾸면 여러 파일을 건드립니다. 보고했고 답은 아직 없습니다 |
| ⚠️ **운송시간 6종은 여전히 근거 등급 D** | 경계 시간대 실측이 **모수 6건**(주간 4 · 야간 1 · 퇴근 18-20 **1** · 출퇴근 06-08 **0**)이라 비율을 잴 수 없었습니다. v12 B장도 그 6종을 **표본 0건**으로 적습니다. 🔴 **「실측으로 확인됐다」고 적지 마십시오** — 진단은 `_verify.sql` **㉕** 입니다 |

### 🔴 36차가 남긴 것 (PR #150 · #151)

| | |
|---|---|
| 🟢 ~~**세금계산서 발행 방식이 화면에 없음**~~ **해소됨(PR #151)** | A·B장이 컬럼·상수만 두고 merge 됐던 것을 PR 2 첫 커밋에서 복구했습니다(DB 0). 🔴 **교훈은 §5-14 에 남겼습니다 — 지운 뒤에는 「무엇이 남았나」를 세십시오** |
| ⚠️ **PR #150 의 Preview 미확인 6건** | 3라운드 반영분을 올리고 **15시간 무응답** 뒤 사용자가 확인 없이 merge 를 지시했습니다. 아직 눈으로 확인되지 않은 것: ① 거래 조건 칸 구성 ② 「수정」을 안 눌러도 「정산 마감」 줄이 보이는지 ③ 건별 ↔ 월정산 왕복 시 결제일이 비는지 ④ 포털 발주요청의 안내·드롭다운·기본값 ⑤ **기존 확정 월정산 묶음의 기간** ⑥ 상단바 고정 + 모달이 헤더 위로 뜨는지. 🔴 **⑤ 가 가장 급합니다** — 값을 지우는 마이그레이션의 영향이고, 코드로는 안전을 확인했지만 실물 확인은 아직입니다 |
| ⚠️ **PR #151·#152 의 운영 확인** | merge 직후 확인할 것: **문자 링크가 바로 열리는지**(⚠️ **뒤 4자리 확인은 PR #152 에 없앴습니다** — 유효기간 지난 건 차단만 남았습니다) · 차량 줄에 차종 · 계산창 가림 · 일정 칩 · 미수금(**활성 화주 목록 + 화주 상세 「실적」 둘 다**) · 조정 줄 · 세금계산서 발행 방식 칸 |
| 🔴 **merge 전에는 견적 문자를 보내지 말 것** | PR #151 에서 실제로 겪었습니다 — 문자에 실리는 주소는 `lib/siteUrl.ts` 의 **운영 도메인 상수**라, 기능이 운영에 없으면 **링크가 404 입니다**(고객에게 죽은 링크가 나갈 뻔했습니다). 🟢 이미 보낸 문자는 merge 와 함께 살아납니다(토큰·주소가 그대로). 🔴 **`SITE_URL` 을 환경변수로 바꿔 우회하지 마십시오** — 고객 문자에 Preview 주소가 실립니다 |

### 🔴 월정산 묶음 — 개편이 끝났습니다 (PR #154 · 2026-09-15)

사용자 신고: *"정산관리 월 묶음건에 대해 정보나 방식이 애매하다. 월정산건은 운송 완료시 자동으로 월정산 묶음이 만들어지고 이 묶음안에서 견적부분이나 정산부분 결제일 부분이 표시가 되어야 하고 실제 정산마감일이 지나고 어떻게 진행되는지도 알아야 한다."*

| | |
|---|---|
| 🔴 **「담기가 안 된다」의 정체** | **DB 함수가 매 호출마다 예외로 죽고 있었습니다.** `add_item_to_billing_batch` 가 `declare v_invoice record;` 로 선언한 변수를 `is_billing_batch_candidate(p_invoice invoices)` 에 넘겨 **`cannot cast type record to invoices`** — 관문을 전부 통과한 건이 마지막 줄에서 죽으므로 「담기」·「모두 담기」·자동 담기가 **전부, 언제나** 실패했습니다. `invoices%rowtype` 으로 **선언 한 줄만** 고쳤습니다(PR #154). 🔴 **같은 `record` 캐스트 함정이 나머지 11개 함수에도 잠복해 있습니다** — 지금은 그들이 `is_billing_batch_candidate` 를 안 불러서 안 터질 뿐입니다 |
| 🔴 **짐작하지 말고 재십시오** | 신고를 **세 번** 받는 동안 화면 쪽만 고쳤습니다(전부 실재한 결함이지만 원인이 아니었습니다). 🔴 **목(mock)은 이것을 영영 못 잡습니다 — 목에는 그 DB 함수가 없습니다**(원칙 56번). **「담기·저장이 안 된다」류 신고는 화면보다 DB 를 먼저 재십시오.** 🟢 도구는 `_verify.sql` **⑲**(함수 본문) **⑳**(관문별 판정) **㉑**(실제로 불러보고 되돌리기)입니다 |
| 🔴 **로직 11개가 아직 저장소 밖입니다** | 묶음 DB 함수 12개가 **마이그레이션 자동화(47차)보다 먼저인 14차 산출물**이라 DB 안에만 있었고, **그것이 이번에 원인 찾기를 어렵게 만든 바로 그 이유**였습니다. 🟢 PR #154 에 `add_item_to_billing_batch` **하나가 처음으로 들어왔습니다**(목록은 `verify` ⑱-e). 🔴 **나머지도 고쳐야 할 일이 생기면 그때 마이그레이션으로 저장소에 들일 것** — 손으로 DB 에서만 고치면 같은 상황이 반복됩니다 |
| 🟢 **결함 넷은 전부 메워졌습니다** | ① **운송완료 시 자동 생성**(호출은 `lib/autoCreateInvoice.ts` 안 · **fire-and-forget** · 확정·해제된 묶음에는 담지 않음) ② **묶음 안에 상차일시·구간·차량·품목** ③ **결제일을 `lib/paymentDueDate.ts` 가 화주 거래조건에서 계산**(「협의」·미설정은 **계산 안 함** — 없는 기한을 지어내면 합의하지 않은 날에 연체가 붙습니다) ④ **마감 지난 draft 표시 + 연체 자동 판정**(`scripts/mark-overdue.sql` 매일 **03:20 KST** · 파기 03:10 과 10분 띄웠습니다 · 상한 가드 200건) |
| 🔴 **연체는 두 겹입니다** | **화면은 표시 시점 계산 · DB 는 기록.** 지시와 다르게 판단한 것입니다 — 매일 작업만 두면 ⓐ 기한이 지난 뒤 **최대 하루가 비고** ⓑ GitHub 은 저장소가 **60일 조용하면 예약을 스스로 끕니다**(`purge.yml` 머리에 적힌 사실). 🔴 **둘을 반대로 만들지 마십시오.** 대상은 **확정 + 납부기한 있는 것**뿐입니다(draft 는 아직 청구 전) |
| 🔴 **연체가 붙으면 해제가 막힙니다** | `release_billing_batch` 가 `payment_status in ('paid','overdue')` 를 거절합니다(함수 본문 실측). 이미 청구가 나간 묶음을 조용히 되돌리지 않겠다는 뜻이라 **의도된 동작**이고, 관리자 「완전삭제」가 남아 있습니다. 🟢 **입금완료는 안 막힙니다**(`'paid'` 만 거절) |
| 🔴 **아코디언 두 함수** | **`toggleBatch`(접기 판정) / `openBatchRow`(무조건 열기)가 갈려 있어야 합니다.** 핸들러에서 `toggleBatch` 를 부르면 **담고 나서 그 묶음이 접히고 항목이 비워집니다**(오래된 클로저 — `setBatch(null)` 은 그 자리에서 반영되지 않습니다). 실사용 4라운드에 실제로 그랬고 **오류도 안 떴습니다** |
| 🟢 **실측이 근거입니다** | 정산 5건(월정산 **2** · 건별 3) · 묶음 **1건이고 그나마 draft** · 화주 540 중 결제일 설정 2 · 마감일 0. ⚠️ 28차의 「월정산 운영 0건」은 **더 이상 사실이 아닙니다**. ⚠️ **연체 `apply` 는 실제로 돌려본 적이 없습니다**(연체 건이 0이라 dry-run 과 로컬 Postgres 시험만 했습니다) — 첫 연체가 생기는 달에 로그를 한 번 보십시오 |
| 🟢 **사용자 확정 (2026-09-15)** | **(A) 표시 + 연체 자동 판정** — 마감 지난 draft 에 배지, 납부기한 지난 확정 묶음은 매일 도는 작업이 「연체」로. 🔴 **확정·세금계산서·입금은 담당자가 손으로 누릅니다**(자동 확정 아님). 🔴 **(B) 자동 확정을 안 고른 이유** — 마감일에 금액이 굳으면 늦게 등록된 건은 보충 묶음으로만 넣을 수 있습니다. **돈이 걸린 상태를 코드가 마음대로 바꾸지 않는 선**이고 원칙 44번과 같은 결입니다 |
| ⚠️ **컬럼 이름** | 활성 항목은 **`released_at is null`** 이고 **`is_active` 는 없습니다**(진단 SQL 에 그렇게 적었다가 42703 으로 멈췄습니다 — PR #152 와 같은 실수를 같은 날 반복했습니다) |
| 🟢 **화주포털은 묶음을 한 줄도 읽지 않습니다** | 내부 전용입니다(`grep` 0건) |

### 🔴 법무 — 남은 유일한 선행 조건

| | |
|---|---|
| **변호사 검토 미실시** | 약관·처리방침·동의 문구 전부. 🔴 **이것이 지금 유일한 선행 조건입니다**(보험사 문의는 2026-09-03에 취소 확정). 워드 세 벌을 **새 메일 주소로 갱신해서** 보낼 것 |
| 🟢 ~~**처리방침 제4조가 사실과 다름**~~ **→ 2026-09-18 에 사실과 맞게 됐습니다** | 그 행(*"차주 성명·연락처·차량번호를 화주에게 제공한다"*)이 **실제 동작이 됐습니다** — 고객용 배차확정 문자가 그 넷을 보냅니다(§5-25). 🔴 **화주포털 화면은 여전히 비노출**이라 §5-4 와 어긋나지 않습니다. 🔴 **처리방침을 고치지 마십시오** — 고칠 것이 없어졌습니다 |
| **처리방침 제4조에 보험회사 행이 없음** | 사고 시 담당자 성명·연락처와 경위가 DB손해보험으로 넘어갑니다. 추가가 필요해 보이며 **변호사 검토와 함께 물을 항목**입니다 |
| ~~**개인정보 파기 장치 없음**~~ | 🟢 **PR #122에 해소됐습니다.** `scripts/purge-expired.sql`을 `.github/workflows/purge.yml`이 **매일 03:10 KST**에 돌립니다. 🔴 **파기 조건 세 개(`quote_id is null` · `company_id is null` · `status <> '승인됨'`)를 빼지 마십시오** — 빼면 5년 보관 대상인 거래 기록까지 지웁니다. 기산점은 `processed_at`이고 `updated_at`을 쓰면 안 됩니다 |
| **열람청구 10일 처리 의무** | 개인정보보호법 제35조 3항. 전용 주소가 없어 **일반 문의에 섞여 들어옵니다** — 메일 자동 분류 규칙으로 대비할 것 |
| 🟢 ~~운영 신고 — 정산관리에 차주지급액이 안 뜨고 정산확정이 안 됨~~ **해소됨(PR #146)** | 2026-09-11 신고 → 2026-09-14 해소. 🔴 **한 줄의 신고가 실제로는 서로 다른 둘이었습니다** — ① **확정이 막힌 것**은 2026-09 건이 「선착불 · 수수료 0원 · 지급자 빈칸」이라 게이트에 걸린 것이고(같은 조건이 DB 전체에 **딱 1건**), ② **금액이 안 맞는 것**은 스냅샷 문제였습니다(정산 50,000 vs 배차 45,000 — 정산 건이 먼저 만들어졌습니다). 사용자가 의심한 *"지나간 날짜에 운임료를 수정해서"* 는 **원인이 아니었습니다**. ①은 「수수료 지급자」를 없애면서 **확정 직전 0원 확인 창**으로 옮겼고, ②는 **배차 기준 재동기화 API**(A-7)로 담당자가 화면에서 고칠 수 있게 했습니다. 🔴 **기존 정산 건을 SQL 로 일괄 수정하지 않았습니다** — 잘못 잡힌 것은 **선착불 3건**이고 원칙 47번대로 화면 경로만 열었습니다. ⚠️ **비슷한 신고를 받으면 원인을 먼저 가르십시오** — 「안 된다」 한 줄이 둘 이상일 수 있습니다 |

⚠️ 판본은 현재 **약관 `terms-v5` · 처리방침 `privacy-v2`**입니다. 🔴 **v4까지가 「고객에게 불리한 변경」이라 약관 제3조 3항이 적용일 30일 전 공지를 요구하고, `terms-v1`로 저장된 동의가 실제로 3건 있습니다 — merge = 게시가 아닙니다.** ⚠️ **v5(PR #138)가 「불리한 변경」에 해당하는지는 변호사 검수 항목입니다** — 그렇다면 30일 공지는 **v5 기준**으로 계산합니다(v3·v4가 게시 전이라 중간 판본은 따로 공지하지 않습니다). 🔴 **본문을 실질적으로 고치면 판본을 반드시 올리십시오** — 판본은 파일 diff가 아니라 **게시되는 문서 내용**을 기준으로 올립니다.

### 🔴 코드 밖에서 지켜야 하는 것 — 계정 잠김 대비 (32차)

```
🔴 admin 롤 계정을 최소 두 개 두십시오 (대표 + 신뢰 직원)
   — 서로 비밀번호를 풀어줄 수 있습니다. 하나뿐이면 그 사람이 잊는 순간 회사가 잠깁니다
   (2026-09-09 기준 admin 3 · staff 1 로 충분합니다)

🔴 최후 수단: Supabase 대시보드 → Authentication → 해당 사용자 → 비밀번호 재설정
   — 이 경로가 있다는 사실을 아는 것 자체가 대비책입니다
```

🟢 **「직원이 비밀번호를 잊으면 복구 방법이 없다」는 위험은 32차에 해소됐습니다** — 관리자가 `/admin/staff` 에서 임시 비밀번호를 재발급하고, 그 직원은 첫 로그인에서 반드시 바꿉니다. ⚠️ 다만 **관리자 본인이 잊으면 다른 관리자나 위 최후 수단이 필요합니다.**

### ⚠️ 근거가 약한 값

- **15톤 운임** — 완료 실거래 1건이라 사실상 미검증
- **장척/중량 물품특성** — 표본 5건(v8 자신이 "5건 미만은 바꾸지 말 것"이라 한 경계선)
- ~~**차량형태 신규 13종 가산율**~~ → **v12 B장이 실측 절대 차액으로 다시 깔았습니다**(PR #157). 🟡 다만 **리프트 계열 조합형은 실거래 0건**이라 여전히 유도값(등급 D)이고, **탑차·호로는 표본 5건·1건**입니다(카고 오더에 「갑바·호루」로 숨어 있을 가능성)
- 🔴 **운송시간 6종 정액**(새벽 20,000 · 나머지 10,000) — **실측 표본 0건 · 등급 D.** 수집 오더 1,860건이 전부 **06~19시**라 새벽·야간·주말 건이 없습니다. 🔴 **「실측으로 확인됐다」고 적지 마십시오**
- ⚠️ **중·장거리 역마진 두 칸** — 5톤+ `130km 이내` **−4.2%**(표본 8건) · 5톤 `90km 이내` **−3.0%**(표본 6건). 표본이 얇고 올리면 인접 차급 역전이 나서 **v12 에서 안 올렸습니다** — 실거래가 더 쌓인 뒤 별도 차수입니다
- ⚠️ **소형 차급의 정액 가산이 상대적으로 큽니다** — 장척/중량 40,000 이 다마스 30km(기본 50,000)의 **80%** 입니다
- 🔴 **라보 근거리 마진**(2026-09-18 · PR #178) — 라보 `10km 이내` 가 **30,000** 이 되면서 차주 지급 상한(= 청구가 ÷ 1.15)이 **26,087원**인데 24시콜 실거래 라보 지급 중앙값은 **35,000** 입니다. ⚠️ **라보가 더 큰 차인데**(개방형 · 높이 160cm · 다마스는 밀폐형 110cm) 값이 같아진 것이고, 다마스 지급 중앙값도 32,500 이라 **다마스는 v12 A장부터 이미 같은 자리에 있었습니다 — 새로 생긴 위험이 아닙니다.** 🔴 **보고했고 사용자가 그대로 확정했습니다 — 되돌리려면 먼저 물으십시오.** 다시 볼 신호는 **근거리 라보 배차가 안 잡히거나 담당자가 매번 청구가를 손으로 올릴 때**입니다
- 🚨 **`/admin/rates` 는 클릭이 곧 저장이고 되돌리기가 없습니다** — 라보 110km `85,100` · 400km `225,100` 처럼 **100원 오타가 운영에 그대로 남습니다**(v12 A장은 85,000 / 225,000 을 넣었습니다). 🔴 **화면에서 운임을 고치면 저장소에는 안 남으니 문서에 한 줄 남기십시오** — 혼적 30km 20% · 다마스·라보 적재 한도에 이어 **문서와 DB 가 갈린 세 번째**입니다. 🟢 `_verify.sql` **㉒-b** 의 「격자 배수 아님」 칸 수가 이런 오타를 잡습니다(지금 **0**)
- 전부 **관찰 대상**입니다. 실거래가 쌓이면 다시 봐야 합니다

### ⚠️ 확인 대기

- **「월별 이벤트」·「운임할인/적립/캐시백」** — 실제 운영 여부와 적용 조건·기간(표시광고법)
- **견적서 머리 금액을 부가세 별도로 내릴지**(10차·53차 위계를 뒤집는 일이라 물어둔 상태)

🟢 **PR #155 의 여섯 건은 2026-09-15 에 Preview 확인이 끝났습니다**(배송지 상호 · 화주 상세
구조 · 견적 칩 프리필 · 조회기간 · 묶음 목록 가독성 · 「공급가액」 라벨) — 「확인 대기」에 다시
넣지 마십시오.

🔴 **웹 푸시는 「배포되면 끝」이 아닙니다**(PR #160 관리자 · PR #161 화주포털) —
**받는 사람이 기기마다 한 번씩 「이 기기로 알림 받기」를 눌러야** 알림이 옵니다
(아이폰은 **홈 화면에 추가 먼저**). 그리고 **실제 도메인에서** 문의를 한 건 넣어
(관리자) · 견적을 하나 「견적제출」로 바꿔(화주) 폰·데스크탑에 오는지 봐야 합니다
(🔴 **Preview 가 아닙니다** — 구독이 도메인마다 별개입니다).
🔴 **화주 쪽은 안내가 따로 필요합니다** — 화주는 그 단추가 있는 줄 모릅니다
(포털 왼쪽 아래, 모바일은 「전체」 메뉴).
🟢 Vercel 환경변수 셋은 등록이 끝났고 **화주포털 B장에 추가 등록은 없습니다.**
🟢 구독이 실제로 들어왔는지는 `_verify.sql` **㉓** 로 봅니다(2026-09-16 실측 —
직원 **1건** · 화주 **0건**).

### ⏸️ 의도적으로 멈춘 것 / 하지 않기로 한 것

- **이메일 발송(Resend)** — 인프라만 구축, 미활성
- **개인정보 전용 메일 주소** — 만들지 않기로 확정(69차 ③)
- **보험사 문의 6문항** — 취소 확정(66차 ⑬). 「확인 대기」로 되살리지 마십시오
- **펼친 상태 화살표 흐리게/숨기기** — 제안했고 「지금대로」로 확정(70차)
- **자동 슬라이드를 데스크탑에도** — 모바일 한정이 의도입니다
- **혼적(합짐) 문장을 요금 설명글에 되살리기** — 2026-09-04에 종결된 항목입니다. 🔴 「확인 대기」로 되살리지 마십시오
- **랜딩에 설치 안내 · 푸시 알림 · 오프라인 목록 열람** — 설치형 앱 차수가 **의도적으로 뺀 것**입니다(PR #126). 랜딩 링크는 사용자가 「넣지 않는다」로 확정했고, 🔴 **오프라인 목록 열람은 캐시를 넓히는 것이 아니라 별도 설계**입니다(지금 넓히면 낡은 금액을 보여줍니다)
- **`물품특성` 8행 · `상하차방식` 8행의 정액 전환** — v12 B장이 **범위에서 뺀 것**입니다(PR #157). 🟢 **실측은 이미 끝나 있고 값만 정하면 됩니다.** 🟢 상하차방식은 이미 **사실상 정액**입니다(요율 0건 · `수작업` 10,000 뿐)
- **대기료·경유지비 표를 만원 단위로 고치기** — 🔴 **하지 않기로 확정했습니다**(2026-09-15). 각 13행 중 4행이 5,000 단위인데 **담당자가 `/admin/rates` 에서 정한 값**이고, **C장의 소계 반올림이 그것을 흡수하는 것이 설계**입니다
- **`reverseMixedDiscount()` 정리** — 호출부가 0곳이지만 **정의는 그대로 둡니다**(지우면 다음에 「왜 역연산이 없지」 하고 다시 만듭니다)
- **화주포털 웹 푸시의 「공지사항」** — 견적·배차·정산 셋만 보내고 **공지는 안 넣었습니다**
  (사용자 확정 범위 · PR #161). 전체 공지라 **모든 화주 폰이 한꺼번에** 울립니다 —
  넣으려면 「누구에게 보낼지」를 먼저 정해야 합니다. ⚠️ **A장(화면 안 알림)에는 공지가
  있습니다** — 두 겹의 범위가 다른 것이 정상입니다
- **화주 쪽 알림 수신 설정 화면** — 관리자와 같은 판단으로 **만들지 않습니다.** 켜고 끄는
  것은 그 기기의 단추 하나뿐입니다
- **역할별 알림 분기 · 알림 수신 설정 화면** — 사용자 확정(**직원 전원 같은 알림**)이라
  **만들지 않습니다**(38차 0-3). 🔴 「관리자만 받게 하자」로 되돌리면 staff 가 못 받습니다
- **히어로 리빌 · 차량 카드 stagger · 아코디언 닫힘 애니메이션 · 패럴랙스** — 전부 랜딩 모션 차수가 **의도적으로 뺀 것**입니다(PR #123). 사유는 `CLAUDE.md` 「차수 없음: 랜딩 스크롤 진입·상태 전환 모션」 ⑥⑦⑬

### 사용자가 코드 밖에서 할 일

- 🟢 **24시콜 ⓐ 는 끝났습니다**(PR #159, 2026-09-16) — 차주 **이름·연락처·차량번호** 셋만 붙여넣기로 채웁니다(🔴 **금액·수수료·화물번호는 범위 밖입니다** — 넓히려면 지시가 필요합니다). 착수 전 확정 셋은 그대로 유효합니다 — ① **주선수수료는 24시콜에 적힌 값을 그대로 넣습니다**(*"선착불 오더에서 적힌 수수료는 부가세 포함가이다"* — 35차의 「포함가 기입」과 그대로 맞물립니다. 제가 제안했던 「×1.1 해서 16,500」은 **뒤집혔습니다**) ② **산재는 다루지 않습니다** ③ **접수중 배차는 다루지 않습니다**. 🔴 **다음 차수(ⓑ 견적 폼)를 막는 것이 남아 있습니다** — 복사되는 것은 창이 아니라 **아래쪽 목록의 한 행**이라 **메모·거리·합계·상하차 방법·상차/하차 시점·상세주소가 안 옵니다.** ⓐ 는 차주 셋뿐이라 상관없었지만 **ⓑ 는 그것들이 있어야 합니다** — **ⓑ 지시서는 「목록 행만으로는 안 된다」를 전제로 써야 합니다.** ⚠️ 그리고 **24시콜과 우리가 같은 건의 수수료 공급가액을 다르게 봅니다**(24시콜 15,000 / 우리 13,636) — 세금계산서 대조에서 어긋나는 줄이 나오면 알려 주십시오. 자세한 것은 `CLAUDE.md` §5 의 「24시콜 가져오기 — 설계 정리 v4」 블록에 있습니다
- ⏸️ **CRM 항목 목록은 지금 정하지 않아도 됩니다** — 🔴 **2026-09-16 에 그 차수를 뒤로 미뤘습니다**(§6). 실측이 반대를 가리켰습니다: 화주 539건 중 활성이 **7건**이고, 36차가 만든 거래조건 칸이 **539건 전부 비어 있습니다.** 🔴 **값이 동작을 일으키지 않는 칸은 아무도 안 채웁니다** — **그 값으로 무엇을 할지**(예: 잠재등급으로 목록을 거르기, 주요 노선으로 차주를 추천하기)가 정해진 뒤에 만듭니다. 🟢 영업을 실제로 돌리기 시작하면 그때 알려 주십시오
- 🔴 **견적서 print 2종을 한 번 출력해 보십시오**(PR #158) — 관리자 견적서와 화주 견적서입니다. ⚠️ **이 환경에서 렌더링을 확인하지 못한 유일한 자리**입니다(목 데이터가 불완전해 크래시했고, 🔴 **`origin/main` 판본도 같은 목에서 똑같이 크래시**해서 **변경 탓이 아님을 확인**했습니다). 볼 것 둘 — ① 가산이 **한 줄**로 나오고 이름이 붙는지(`가산 (윙바디 · 새벽 · 왕복)`) ② **기본운임 + 가산 + 할인 = 공급가액**이 정확히 맞고 「조정」 줄이 **안 뜨는지**(뜨면 수동 조정한 건입니다)
- 🟡 **가산 수입이 −66% 가 됩니다**(PR #157) — 건당 12,082 → **4,077원** · 견적 총액 −5.9% · 마진 34.5% → **26.7%**. **의도한 것이고 되돌릴 계획이 없습니다.** 볼 것은 **배차가 실제로 쉬워지는지**입니다(요율제가 대형·장거리에서 실측을 크게 넘어 배차를 어렵게 만들고 있었다는 것이 이번 근거였습니다). 한 달쯤 운영해 보고 판단하십시오
- 🟢 **가산 줄 라벨 길이는 닫혔습니다**(PR #158 에서 열렸던 것 · 2026-09-17 확정) — 조건이 많으면 `가산 (윙바디 · 새벽 · 파손주의 · 왕복 · 상차 수작업 · 하차 수작업 · 대기료(47분) · 경유지 2곳)` 처럼 길어지지만 줄바꿈으로 처리되고 **가로 넘침이 없습니다.** 🔴 **「외 N건」으로 축약하지 않습니다** — 보류가 아니라 확정입니다
- 🔴 **`/guide` 와 `/customer/guide` 를 한 번씩 읽어 보십시오**(PR #148 · #149) — ⚠️ **이 두 차수는 여기에 확인 항목이 한 건도 없었습니다**(2026-09-15 보완). ① 계정을 처음 받은 거래처 입장에서 **첫 로그인까지 막히는 곳이 없는지** — 반복될 문의 다섯 개에 문장 하나씩이 대응하므로 **줄이면 그 문의가 돌아옵니다** ② 🔴 **포털에서 「이용가이드」로 들어갔을 때 로고가 포털 안에 머무는지**(이것이 `/customer/guide` 를 따로 만든 유일한 이유입니다 — 공개 `/guide` 는 로고가 랜딩으로 나갑니다) ③ **모바일에서 목록과 본문이 따로 뜨는지**(≤700px) ④ ⏳ **답 대기** — 정산 금액을 찾는 분에게 「금액 표시」라는 항목명이 읽히는지. 안 읽히면 포털 전용 `invoice` 항목을 하나 더 넣습니다(지금은 없습니다)
- 🟡 **랜딩 히어로의 새 버튼을 눈으로 확인해 주십시오**(PR #147) — ⚠️ **Preview 확인 전에 merge 했으므로 아직 실기기로 본 적이 없습니다.** ① **옐로 톤이 적당한지**(더 조용한 대안은 테두리만 있는 흰 버튼이고 한 줄이면 바뀝니다) ② 🔴 **노트북 폭(1366~1440px)에서 제목 「위캐리가 관리합니다.」의 마지막 글자가 트럭에 안 가리는지** — 이번에 고친 자리입니다(숫자로는 기준선과 동일하지만 실기기 확인은 아직입니다) ③ 휴대폰에서 버튼 두 개가 한 화면에 들어오는지. 🟢 셋 다 **되돌리기 쉬운 표면 변경**입니다
- 🔴 **배차 담당자에게 운영 규칙을 전달하십시오**(PR #146) — **선착불 주선수수료는 이제 「부가세 포함가」로 기입합니다.** 그전 라벨이 「공급가액」이라 **DB 에 두 기준이 섞여 있습니다**(5,000 = 포함가 · 4,545 = 공급가액 실측). 🔴 **코드가 강제하지 못하는 부분입니다** — 섞인 채로 두면 금액이 맞아도 부가세가 안 맞고, 24시콜 세금계산서 대조가 계속 어긋납니다
- 🔴 **선착불 정산 3건을 화면에서 다시 맞추십시오**(PR #146) — 정산 상세의 **「배차 기준으로 금액 다시 맞추기」**(사유 입력 필수). 🔴 **SQL 로 일괄 수정하지 않았습니다**(원칙 47번). 🟢 **마진 표시는 재동기화 없이도 이미 맞습니다** — 표시 시점 계산이라 옛 건도 자동으로 맞고, 재동기화가 필요한 것은 **금액 자체가 배차와 다른 건**뿐입니다
- 🟡 **24시콜 세금계산서와 엑셀 「정산 건별」 시트를 줄 단위로 대조해 보십시오**(PR #146) — 반올림 원인 둘은 없앴지만 **입력 기준이 섞인 건**은 남아 있습니다. 어긋나는 줄이 나오면 **오더번호와 24시콜 쪽 금액**을 알려 주십시오
- 🔴 **화주 등록·승인을 한 번씩 밟아 보십시오**(PR #139) — ① 🔴 **`/apply` 로 신청을 한 건 넣고 승인**해서 신청서에 값이 있는 항목이 **전부 대응 컬럼에 들어갔는지**(특히 「월 예상 운송건수」에 `30` 처럼 숫자만 넣었을 때 `monthly_expected_orders` 로 들어가는지). **이 환경에서 확인할 수 없는 유일한 항목입니다** ② `staff` 롤 계정으로도 화주 **등록·수정이 되는지**(권한 회귀) ③ 신규 등록에서 **접힌 구획에 넣은 값도 저장되는지** ④ 정기계약 **종료일을 과거 날짜**로 넣으면 목록 네 곳(화주·견적·오더·배차)에서 배지가 사라지고 상세에 「종료됨」이 뜨는지 ⑤ 운영 대시보드의 「정기계약 화주 N개」를 눌러 **명단이 펼쳐지고 이름에서 화주 상세로** 가는지. 🟢 **「화주가 540건이라 등록 화면이 무겁다」는 PR #144 에 해소됐습니다**(한 번에 15행). 폼이 열려 있는 동안 목록을 안 그리던 분기도 그때 없앴으니 **폼과 목록이 같이 보이는 것이 정상입니다**
- 🔴 **내부관리 아이디 로그인·비밀번호 재발급을 한 번씩 밟아 보십시오**(PR #137) — 🔴 **32차는 실사용 리뷰가 0회라 아무도 실제로 눌러 보지 않았습니다.** ① `/admin/login` 에서 **아이디**로 로그인되는지(이메일이 아닙니다) ② 없는 아이디·틀린 비밀번호·퇴사자 계정이 **전부 같은 문구**로 거절되는지 ③ 직원 계정 화면에서 **비밀번호 재발급** → 화면에 뜬 임시 비밀번호로 로그인 → **`/admin/change-password` 로 자동으로 끌려가고, 바꾸기 전에는 다른 화면으로 못 가는지** ④ 아이디를 바꾼 뒤에도 **기존 비밀번호 그대로** 로그인되는지 ⑤ role 이 `staff` 인 계정에는 재발급 단추가 **안 보이는지**. 🔴 **관리자(`admin`) 계정을 최소 2개 유지하십시오** — 하나뿐인데 그 계정이 잠기면 **앱 안에서 풀 방법이 없습니다**(Supabase 대시보드 → Authentication 이 유일한 길입니다). ⚠️ 실패 제한은 표가 아니라 프로세스 메모리라 **배포되면 초기화됩니다** — 벽이 아니라 과속방지턱입니다
- 🔴 **폰에서 로그인 화면과 카톡 링크를 확인하십시오**(PR #128) — ① `/admin/login`·`/customer/login` 의 **첫 화면이 더 이상 늘었다 줄었다 하지 않는지** ② 운송관리 로그인에 **로고가 한 번만** 나오고 스크롤 없이 「운송관리 계정 신청」까지 보이는지 ③ 🔴 **카톡으로 링크를 보내 열고** 「홈 화면에 추가」 → **「기본 브라우저로 열기」가 실제로 사파리/크롬을 여는지**(이 환경에 카톡이 없어 UA 로만 확인했습니다 — 유일하게 못 잰 부분). ⚠️ **③ 은 Preview 로는 정확하지 않습니다** — Vercel 로그인 벽 때문에 카톡이 우리 페이지 대신 Vercel 화면을 엽니다. **실제 도메인에서 보십시오**. ⚠️ 작은 화면(360x800·iPhone SE)은 여전히 조금 스크롤됩니다 — 여백은 더 뺄 곳이 없어 **더 줄이려면 문구를 지워야** 하고, 지시를 기다리는 상태입니다
- 🔴 **배포 후 모바일에서 랜딩을 한 번 내려보십시오**(PR #119) — ① 「고정화물 배차」에 회색 상자가 스치는지 ② **히어로·마감CTA 배경이 예전보다 흐릿해 보이지 않는지**(수치로는 차이가 0.1~0.7%지만 눈으로 봐야 확실합니다) ③ 맨 아래 카피라이트 작대기. ⚠️ **파일명이 그대로라 캐시가 남습니다** — 안 바뀐 것 같으면 새로고침을 강하게 하십시오
- 🔴 **이미 설치한 앱은 한 번 지우고 다시 설치하십시오**(PR #127) — `chrome://apps` → 제거 → `wecarrylogis.co.kr/admin`(또는 `/customer`)에서 다시 설치. 그러면 창 위에 **주소 대신 앱 이름만** 남습니다. 🟢 `id` 를 그대로 둬서 지우지 않아도 크롬이 manifest 를 다시 읽는 시점에 저절로 고쳐질 수 있지만, 다시 설치하는 쪽이 확실합니다. ⚠️ **PC 크롬은 정상 설치돼도 얇은 제목줄이 남습니다**(주소가 아니라 앱 이름). ⚠️ **Vercel Preview 에서는 로그인 벽 때문에 주소 띠가 계속 보입니다 — 실제 도메인에는 안 생깁니다**
- 🔴 **설치형 앱을 실제 기기에서 한 번 확인하십시오**(PR #126) — 이 환경에서는 잴 수 없는 것들입니다. ① 안드로이드에서 「앱으로 설치」 → 아이콘이 **포털은 흰 배경 + 검은 W / 내부관리는 검은 배경 + 흰 W** 인지, 주소창 없이 뜨는지, 설치 후 버튼이 사라지는지 ② 아이폰에서 안내 창 3단계가 읽히는지, 「홈 화면에 추가」 후 이름이 **「운송관리」·「내부관리」** 인지 ③ 비행기 모드에서 「연결이 끊겼습니다」가 뜨는지 ④ 🔴 **복귀 후 배차·정산 숫자가 웹과 같은지**(낡은 값이 보이면 알려 주십시오). ⚠️ **Vercel Preview 에서는 앱 창 위에 주소 띠가 남습니다** — Vercel 로그인 벽 때문이고 실제 도메인에는 생기지 않습니다
- 🔴 **약관 `terms-v5` 게시 시점과 30일 공지 절차 결정**(PR #125·#133·#138 — **공지는 v5 기준**입니다. v3 이 게시 전에 v4 가 됐으므로 중간 판본을 따로 공지할 필요가 없습니다) — **merge = 게시가 아닙니다.** 「고객에게 불리한 변경」이라 약관 제3조 3항이 **적용일 30일 전 공지**를 요구하고, `terms-v1`로 저장된 동의가 **실제로 3건** 있습니다. 🟢 기존 동의 3건의 기록은 손대지 않았습니다(판본만 올려 그분들이 본 문서가 특정됩니다)
- 🔴 **변호사 검수 의뢰**(약관 + 처리방침) — **유일한 선행 조건.** 짚어달라고 할 지점 **아홉**: ① **확정운임 → 운송인 의제**(상법 제119조 2항) ② 제4조 3항·제18조 1항의 **책임 귀속 진술**이 약관규제법 제7조 2호를 통과하는지 ③ **제19조 2항의 조건절**(PR #138 전에는 1항)이 배상 회피로 읽히지 않는지 ④ 면책 9개 호가 「상당한 이유」 요건을 넘는지 ⑤ **제19조 7항(고의·중과실 예외)이 유일한 방어선이 된 구조** ⑥ **상법 제146조 1항 단서(2주 통지)를 약관에서 배제한 것**이 제7조 2호를 통과하는지(PR #133 — 법이 준 권리를 약관으로 배제한 것으로 읽히면 **악의 인도 예외를 담은 2항까지 흔들릴 수 있습니다.** 🔴 보고했고 사용자가 그대로 확정한 위험입니다) ⑦ **제19조 1항이 미신고 건의 배상 한도를 「상법 등 관계 법령」에 위임한 것**(PR #138 — 우리 판단은 문제 없음입니다. 법정 기준을 그대로 따르는 것이라 깎지 않았고, 「인도할 날의 도착지 가격」을 적지 않은 것은 화주가 읽고 금액을 가늠할 수 없어서입니다) ⑧ **화주 화면에 띄우는 문제발생 사유 문구**가 제19조(배상책임은 **차주와 그 보험자**)와 어긋나지 않는지(PR #171 — 특히 **「화물 확인 중」처럼 「분실」을 피해 쓴 표현**이 나중에 분쟁에서 불리하게 읽히지 않는지. 🔴 「분실」을 쓰면 **보험이 안 나오는 경우를 회사가 스스로 적은 것**이 되어 피한 것입니다 · §5-23) ⑨ 🚨 **차주 성명·연락처를 고객에게 문자로 제공하는 것**(2026-09-18 · §5-25 — 처리방침 **제4조 3행**이 근거이고 그 조가 이제 사실과 맞습니다. 짚어야 할 것은 ⓐ 그 고지만으로 충분한지 ⓑ **차주 쪽 고지**가 충분한지입니다 — 처리방침 「2) 차주」 절에 제3자 제공 안내가 있는지. 🔴 **차주는 우리와 계약하는 쪽이라 고객과 고지 경로가 다릅니다**)
- 🔴 **파기 워크플로가 아직 도는지 가끔 확인하십시오**(PR #122) — GitHub은 **저장소가 60일간 활동이 없으면 예약 실행을 자동으로 끕니다.** 꺼진 줄 모르면 **고지만 남고 파기가 조용히 멈춥니다.** Actions 탭에서 「보유기간 만료 파기」를 보면 됩니다. ⚠️ 첫 자동 실행(다음 날 03:10 KST) 로그를 한 번 볼 것 + 관리자 화면에서 신청 **거절/보류**를 한 건 처리해 `processed_at`이 찍히는지 확인할 것
- 🔴 `biz@` 기본환경에서 발신자명을 「위캐리 운송」으로 · 서명 켜기
- 🟡 메일 자동 분류 규칙(제목에 「열람」·「개인정보」·「정정」·「삭제」)
- ⚠️ 카페24에 **POP3S(995) 지원 여부 문의** — 지금은 POP3(110) 평문뿐이라 처리방침 제9조와 어긋납니다. 그래서 POP3·SMTP를 꺼 두고 웹메일로만 씁니다
- 🔴 **카카오톡 OG 캐시 초기화** — `https://developers.kakao.com/tool/clear/og`
- ⚠️ 증권 만기 알림(일반화물 2027-09-01 · 이사화물 2027-04-05)
- ⚠️ 원격 임시 브랜치 정리(`claude/*` — 이 환경은 삭제가 막힙니다)
- 🟢 **네이버 서치어드바이저 등록은 2026-09-04에 끝났습니다** — 사이트 등록 · 소유확인 · 사이트맵 제출 · 수집요청. ⚠️ **구글 서치콘솔은 아직입니다**
- 🔴 **배포 후 「간단체크」를 한 번 더 돌리십시오** — 「사이트 설명」·「Open Graph 설명」 두 ⚠️ 가 ✅ 로 바뀌어 **8개 항목 전부 ✅** 가 되어야 정상입니다(PR #118에서 description을 80자 이내로 줄였습니다). ⚠️ 진단은 **랜딩 하나만** 보므로 다른 화면은 URL 칸에 `https://wecarrylogis.co.kr/quote` 처럼 넣어 각각 돌리십시오
- ⚠️ 구글 [리치 결과 테스트](https://search.google.com/test/rich-results)로 Organization JSON-LD 인식 확인(이 환경은 프록시가 막혀 확인 못 했습니다)
- 🔴 **「위캐리」 제39류(운송·보관업) 상표 선등록 확인**(kipris.or.kr, 10분) — 출원은 안 하기로 확정했지만 **선등록 확인은 별개**이고 아직 안 됐습니다
- 🔴 **네이버 블로그 개설 + 주 1회** — 스마트플레이스를 포기(`(주)디자인에그`로 이미 등록돼 있고 주소가 같음)해서 **블로그가 첫 화면을 잡는 유일한 무료 수단**이 됐습니다. 🔴 **개설하면 알려 주십시오 — JSON-LD `sameAs`에 그 주소를 더합니다**(「이 블로그가 우리 것」임을 검색엔진에 알리는 값입니다). ⚠️ 아이디를 `wecarrylogis`로, 블로그명을 「위캐리 물류」로 하지 마십시오 — 경쟁사 (주)위캐리로지스가 `wecarry_logis` / 「위캐리 물류 이야기」를 씁니다
- ⚠️ 잡코리아·사람인 기업정보 등록 · 「위캐리」 **단독 키워드에는 돈·시간을 쓰지 마십시오**(국제물류·제주 짐배송이 점유해 되찾을 수 없습니다)

---

## 8. 작업 방식

### 한 차수의 흐름

```
지시서 작성  →  구현 세션  →  실사용 리뷰  →  CLAUDE.md 갱신  →  기록 검증
   (값·완료조건·           (착수 전 확인      (배포본을          (되돌릴 위험이
    금지사항 확정)          부터)             눈으로)            적혔는지)
```

⚠️ **실사용 리뷰가 여러 라운드로 가는 것이 정상입니다** — 27차 7라운드 · 62차 3라운드 · 65차 4라운드 · 66차 2라운드 · 70차 3라운드 · 랜딩 모션 2라운드. 리뷰에서 **확정이 뒤집히는 일이 자주 있고**, 그때는 반드시 "옛 기록으로 되돌리지 말 것"을 `CLAUDE.md`에 적습니다.

### 지켜서 효과를 본 규칙

| | |
|---|---|
| **한 번에 하나씩** | 값 변경과 구조 변경을 같은 차수에 섞지 않습니다 |
| **파일이 겹치지 않게 쪼갠다** | 차수를 나누는 기준입니다 |
| 🔴 **착수 전 확인** | 지시서 전제를 코드로 대조. **열한 번** 사고를 막았습니다(32차 — anon 정책이 이미 없어져 별도 표가 불필요했던 것 · public 저장소에 실명을 넣을 뻔한 것 / 33차 — 안내가 가리키던 **존재하지 않는 파일** / PR #144 — **지시서 전제 다섯 개가 실제와 달라 멈추고 보고**했습니다. `/admin/crm` 이라는 경로가 없었고 필터·검색·정렬이 전부 클라이언트였습니다) |
| 🔴 **의도한 것은 적는다** | 안 적으면 다음 세션이 "정리"하다 되돌립니다 |
| **커밋 분리** | 원인 추적용. 버그 수정은 반드시 따로 뗍니다 |
| 🔴 **PR 본문은 merge 직전에 다시 쓴다** | 리뷰로 범위가 넓어지면 처음 본문이 사실과 달라집니다(45차·70차) |

### 지시서에 반드시 넣는 것

- **왜 하는가** — 사유가 없으면 판단이 필요한 순간에 임의로 갑니다
- **완료조건은 기계로 검증되게** — `"문구가 통일됐다"`가 아니라 `grep 0건`
- **하지 말 것** — 특히 **이전 차수가 의도적으로 해둔 것**
- **적용 순서** — DB가 먼저인지 코드가 먼저인지는 **매번 따집니다**. 한 방향으로 굳은 규칙이 아닙니다

### 검증

- **렌더링으로 확인합니다.** 목 Supabase 서버 + 세션 주입으로 포털·관리자 모두 띄울 수 있습니다
- **관리자 회귀는 `origin/main`을 같은 목으로 띄워 대조**합니다(before 스냅샷이 아니라)
- 🔴 **목은 실제 DB보다 헐거우면 안 됩니다** — `order`·`limit`·`gt`·`not.`을 흉내내지 않으면 검증이 통째로 무의미해집니다
- **시안 대조는 소스에서 값을 뽑아** 합니다(24차가 자동검증 27개를 다 통과하고도 시안과 여섯 곳이 달랐습니다)
- ⚠️ **이 환경은 프록시가 `vercel.app`과 `wecarrylogis.co.kr`을 막습니다** — Preview를 볼 수 없어 **로컬 프로덕션 빌드로 잽니다**
- ⚠️ **Playwright는 `npm i --no-save playwright-core`로 넣습니다** — `package.json`·`package-lock.json` 변경 **0**을 유지해야 하는 차수가 대부분입니다. 브라우저는 `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`에 이미 있습니다

---

## 9. 되풀이된 함정

| # | 함정 | 대응 |
|---|---|---|
| 1 | 🔴 **요약 문서·스크린샷을 근거로 삼기** | 시안 핸드오프 문서가 **네 곳** 실제와 달랐고, 스크린샷이 뷰포트로 잘려 오판이 두 번. **소스가 정본** |
| 2 | 🔴 **자동검증만 믿기** | 27개가 전부 통과했는데 시안과 다른 곳이 **여섯**. 구조·수치만 재고 "같은가"를 안 쟀습니다 |
| 3 | 🔴 **거짓 ❌ 를 코드 결함으로 읽기** | **열여섯 차수**에서 났고 **전부 시험 자산 결함**이었습니다. ⚠️ PR #144 에서 또 **두 번** 났습니다 — 🔴 하나는 **목 데이터가 실제와 달라서**(활성 화주를 고르게 뿌려 360행이 되는 바람에 「7건이라 페이지가 하나」를 못 쟀습니다. 목은 엄격하기만 하면 되는 것이 아니라 **분포도 실측과 같아야** 합니다), 다른 하나는 🔴 **PR #142 와 똑같이 「줄 수」를 `top` Set 으로 센 것**입니다(`…` 가 `<span>` 이라 top 이 달라 한 줄이 2줄로 잡혔습니다 — **세로로 겹치지 않는 띠**로 셀 것). ⚠️ PR #142 에서 **아홉 번**, PR #143 에서 **두 번** 더 났습니다 — #143 의 하나는 🔴 **회귀를 견적 화면에서 쟀기 때문**이고(폼이 열리면 스코프 **밖** `.field` 가 0개라 비교 대상 자체가 없습니다), 다른 하나는 🔴 **목 가드가 부분문자열로 걸린 것**입니다(`".admin-wide .quote-form" not in s` 가 34차의 `.quote-form-layout` 에 걸려 통과했습니다). "❌가 나왔다"를 곧장 "코드가 틀렸다"로 읽지 마십시오. ⚠️ v12 에서 **세 번** 더 났습니다 — ① 🔴 **역전 단언 시험을 `UPDATE` 대상 칸으로** 해서 마이그레이션이 덮어썼습니다(**증감 0인 칸**으로 해야 단언이 실제로 걸립니다) ② 🔴 **부정 시험의 `sed` 치환이 0건**이라 「깨뜨린 사본」이 원본과 같았습니다(**치환 자리 수를 찍으십시오**) ③ 🔴 관리자 견적서 print 크래시를 내 변경으로 의심했는데 **`origin/main` 판본을 같은 목으로 띄우니 똑같이 크래시**했습니다(목 데이터가 불완전했던 것입니다 — **의심되면 기준 판본을 같은 조건으로 돌려 보십시오**). ⚠️ 34차 한 차수에서만 **다섯 번** 났습니다 — 그중 하나는 🔴 **목이 실제보다 엄격해서**였습니다(PATCH 의 `Access-Control-Allow-Methods` 누락. 원칙 56번은 「헐거우면 안 된다」인데 **너무 엄격해도 검증이 무의미해집니다**). ⚠️ 38차에서 **넷**, 화주포털 B장에서 **하나** 더 났고 **다섯 다 하네스가 틀린 것**이었습니다 — 🔴 38차의 둘은 **`position: absolute` 부품을 실제로 놓이는 자리가 아닌 곳에 두고** 「창이 화면 밖으로 나간다」를 쟀습니다(**그 화면의 진짜 뼈대 클래스로 감싸고 재십시오** — 포털은 `.pv2-shell > .pv2-sidebar > .pv2-sidebar-foot`). 🔴 B장의 하나는 **두 파일을 md5 로 비교하면서 자르는 자리를 틀린 것**입니다(푸시 주석 블록 **안쪽**에서 잘라 「갈렸다」가 났는데 자르는 자리를 옮기니 바이트까지 같았습니다) — **비교 범위를 코드 경계(닫는 `});`)에 맞추고, 주석 안에서 자르지 마십시오**. ⚠️ PR #168 에서 **다섯** 더 났고 **전부 제 측정이 틀린 것**이었습니다 — 🔴 넷은 **하네스가 넘친 것을 부품이 넘친 것으로 읽었습니다**(검사 결과를 찍어 둔 `<pre>` 가 줄바꿈이 안 돼 좁은 폭에서 가로 넘침 166~566px 이 났는데 **제 컴포넌트는 0이었습니다** — **어느 요소가 넘치는지 `getBoundingClientRect()` 로 집어보면 바로 갈립니다**), 하나는 🔴 **`order=created_at.desc` 에 든 `created_at` 을 기간 조건으로 센 것**입니다(`created_at=` 로 재야 했습니다 — **부분문자열로 세지 마십시오**) ⚠️ PR #179 에서 **두 번 더** 났고 둘 다 **내 측정이 틀린 것**이었습니다 — 🔴 하나는 `getComputedStyle(el, "::before").content` 가 Chromium 에서 **`counter(cs)` 문자열 그대로** 나오는 것을 몰라 번호가 안 붙는다고 읽은 것(→ `counterReset`·`counterIncrement`·폭으로 재고 스크린샷으로 확인), 다른 하나는 **`position: sticky` 가 자기 격자 칸 안에서만 움직인다**는 것을 잊고 900px 을 스크롤해 「안 따라온다」고 읽은 것입니다(→ 「따라갈 수 있는 거리」를 계산해서 다시 측정). |
| 4 | 🔴 **대상 위치를 적게 세기** | 2곳→4곳, 3곳→5곳, 1개→3개. **표로 못 박지 말고 전수 grep** |
| 5 | 🔴 **`pkill`·`kill`이 자기 자신을 죽임** | **열세 번.** 대괄호 트릭으로도, `/proc` 훑기로도 걸립니다 — `comm`이 대상 프로세스인 것만 고르거나 **PID 파일**을 쓰십시오. ⚠️ v12 에서 **두 번 더**, 37차에 **한 번 더** 났고, v12 의 둘 중 하나는 🔴 **`pkill` 을 아예 안 썼는데도** 내 셸의 명령줄에 패턴 문자열이 들어 있어 `/proc` 훑기에 걸린 것입니다. `ps -eo pid,args` 로 뽑아 **PID 로 죽이십시오**. ⚠️ PR #168 에서 **한 번 더**(`pkill -f "next dev"` · exit 144) — 🟢 **애초에 죽이지 않아도 됩니다**(포트를 달리 쓰거나 그대로 두고 빌드하면 됩니다). 🟢 **PR #169 에서 처음으로 피했습니다 — `fuser -k <포트>/tcp`** 로 포트에서 찾아 끄면 **패턴 문자열이 내 명령줄에 안 들어가** 걸릴 일이 없습니다. **다음에도 이렇게 하십시오** |
| 6 | 🔴 **낡은 서버가 포트를 잡아 옛 빌드를 잼** | **일곱 번.** `next start`가 죽어도 `curl`은 200을 돌려줍니다. **포트를 바꿔 새로 띄우고 응답으로 확인** |
| 7 | 🔴 **dev 서버가 뜬 채로 빌드** | `.next`가 깨집니다. 빌드 전에 죽이고 지울 것 |
| 8 | 🔴 **`git checkout <파일>`로 시험 되돌리기** | 미커밋 편집을 통째로 날렸습니다. 원본을 `cp`로 복사해 **사본으로 복원** |
| 9 | 🔴 **정규식으로 소스를 긁어 배열 읽기** | 주석 안 문자열을 원소로 오인. **실제 모듈을 평가**해서 잴 것 |
| 10 | 🔴 **주석에 상수·금지어를 문자로 쓰기** | 완료조건 `grep`에 주석 자신이 걸립니다. **여덟 번** — ⚠️ 2026-09-18 문자 발송 정리에서 **한 세션에 두 번** 더 밟았습니다(「되살리지 말 것」을 적으면서 금지 문자열을 그대로 인용). 🟢 **뜻이 같은 다른 말로 적으면 됩니다**(「그 시각을 문자열로 다시 적지 말 것」 · 「그전에는 요약을 보낸다는 말이었다」). 🔴 **소스를 grep 하는 완료조건은 주석을 걷어낸 뒤에 세십시오** |
| 11 | 🔴 **`error`를 버리고 빈 목록으로 두기** | 조회 실패가 "데이터 없음"으로 보입니다 |
| 12 | 🔴 **`.order()`를 없는 컬럼에 걸기** | 정렬은 불러온 뒤 JS로 하십시오 |
| 13 | 🔴 **`scrollLeft`만으로 천천히 밀기** | 브라우저가 **정수로 반올림**해 덜덜거립니다. 정수는 `scrollLeft`, 소수는 `transform`이 나눠 싣습니다 |
| 14 | 🔴 **여백을 줄이려다 늘리기** | 선택자가 넓으면 `margin: 0`인 요소에 값을 **붙입니다**. 전후 높이를 반드시 재십시오 |
| 15 | **`npx tsc … \| tail`** | `tail`의 종료코드를 읽어 오판합니다. **파일로** 받으십시오 |
| 16 | **`<br />`로 한글 줄바꿈 고정** | 폰트가 CDN이라 폭이 달라지면 어색해집니다. `word-break: keep-all` |
| 17 | **`<details name>`에 `open=true`를 한꺼번에 걸기** | 배타 아코디언이라 **마지막 하나만** 열립니다. 하나씩 열어 재십시오 |
| 18 | 🔴 **`public/`의 "참조 0건" 파일을 정리하기** | 일곱 중 넷은 **외부가 절대 URL로 가져다 씁니다** — 지우면 회수가 안 됩니다. 아래 표 참고 |
| 19 | 🔴 **「이미지가 늦게 뜬다」를 용량 문제로 짐작하기** | 먼저 **그 섹션의 y 좌표**를 재십시오. 지연로딩 선행 거리(약 1250px) 안팎이면 **보이는 순간 받기 시작**합니다 — 용량과 무관. 5.4MB를 줄여도 팝인은 371 → 339ms로 그대로였고, **그 한 장만 `eager`로** 바꿔야 0이 됐습니다 |
| 20 | 🔴 **스크롤 타이밍을 `waitUntil:"load"`나 rAF로 재기** | 전자는 대역폭이 이미 비었고, 후자는 CPU 스로틀에 1.5초가 15초로 늘어납니다 — 둘 다 「전·후 똑같음」이라는 무의미한 결과가 나옵니다. `domcontentloaded` → 1.5초 대기 → **`setTimeout`으로 벽시계 1.5초** 스크롤 |
| 21 | 🔴 **이 환경에서 FCP·LCP로 성능을 판단하기** | 프록시가 폰트 CDN을 막아 그 대기가 그대로 섞입니다(실측 FCP 13.2초). 믿을 것은 **전송 바이트**와 **요소 타이밍**뿐입니다 |
| 22 | 🔴 **`<details>`의 높이를 애니메이션하려 하기** | `height: auto`는 보간되지 않습니다. `grid-template-rows: 0fr → 1fr` 트릭을 쓰되 **안쪽에 `overflow: hidden` 겹을 하나 더** 두십시오 — 없으면 안쪽 `margin`이 클리핑 밖에 남아 **닫힌 상태에도 틈**이 생깁니다(랜딩 WHY는 32px). ⚠️ 만들기 전에 그 브라우저에서 실제로 보간되는지 먼저 재보십시오 |
| 23 | 🔴 **스크롤 리빌의 재생 여부를 「화면 안에 opacity 0인 요소가 있는가」로 재기** | 처음 등장하는 요소가 정상적으로 0에서 시작하는 것까지 잡혀서, 「한 번만」과 「반복」을 구분하지 못합니다. 🟢 재야 할 것은 **「보이는 상태의 요소가 다시 숨겨지는 순간」**이고 `MutationObserver`로 클래스가 떼어지는 순간을 잡아 그때 화면 안이었는지 봅니다 |
| 24 | ⚠️ **모션 값을 재생 시간보다 짧게 기다렸다 재기** | 620ms짜리를 900ms 뒤에 쟀더니 `opacity: 0.99`가 나와 거짓 ❌ 가 두 번 났습니다. **`transition-duration` + `delay`의 두 배쯤** 기다리십시오 |
| 25 | 🔴 **`<select>`의 `<option>` 안에 마크업을 넣기** | 브라우저가 내용을 **평문으로** 그려서 배지·아이콘·굵게가 전부 무시됩니다(요소는 DOM에 있는데 화면에 안 보입니다). 33차에서 배차 등록의 오더 선택 드롭다운에 정기계약 배지를 못 붙인 이유이고, **다음에 또 시도할 종류**입니다. 표시가 필요하면 `<option>` **글자 자체**에 넣거나 드롭다운을 커스텀 부품으로 바꿔야 합니다 |
| 26 | 🔴 **「안 된다」는 신고를 화면 쪽부터 고치기** | 「담기가 안 된다」를 **세 번** 받는 동안 화면만 고쳤습니다(오류 위치·빈 렌더·오래된 클로저 — 전부 실재한 결함이지만 **원인이 아니었습니다**). 진짜 원인은 **DB 함수가 매 호출마다 예외로 죽던 것**이었습니다(`cannot cast type record to invoices`). 🔴 **목(mock)은 이것을 영영 못 잡습니다 — 목에는 그 DB 함수가 없습니다.** 「담기·저장이 안 된다」류는 **DB 를 먼저 재십시오**(`_verify.sql` ⑳·㉑ 처럼 관문을 행마다 다시 계산하고, 함수를 **실제로 불러보고 되돌리십시오**). 🟢 **PR #163 에서는 이 순서를 지켜 한 번에 맞혔습니다** — 「견적 저장이 안 된다」에 `_verify.sql` **㉔** 을 만들어 먼저 쟀더니 견적번호가 **건수 2 · 최대번호 3** 으로 신고와 정확히 일치했고, 같이 온 「배지가 안 사라진다」까지 **같은 원인**이라는 것이 그 자리에서 드러났습니다 |
| 27 | 🔴 **오류를 화면 맨 위 한 곳에만 그리기** | 누른 버튼에서 멀면 **「눌렀는데 아무 일도 안 일어난다」로 읽힙니다**(월정산 묶음에서 1000px 위에 떴습니다). 원칙 33번이고 **PR #153 · #154 · #167 에서 세 번** 겪었습니다(⚠️ **PR #167 은 더 나빴습니다** — 배차 상세는 `if (error || !dispatch)` 전체화면 가드가 액션 실패까지 받아 **아래 배너가 한 번도 그려진 적이 없었습니다**. **PR #153 이 오더 상세에서 고친 그 버그를 이웃 화면에 남겨 둔 것**입니다). 오류는 **난 자리**에 띄우고, **같은 패턴을 한 화면에서 고쳤으면 형제 화면도 같이 grep 하십시오** |
| 28 | 🔴 **「지운 뒤에 무엇이 남았나」를 안 세기** | PR #150 이 `credit_limit` 을 지우며 **바로 아래 항목까지** 지웠고, PR #154 가 `_verify.sql` 에 예외 처리를 더하며 **안쪽 블록의 `end;` 를** 같이 지웠습니다. 🔴 **「무엇을 지웠나」가 아니라 「무엇이 남았나」를 `grep -c` 로 세십시오.** SQL 은 워크플로를 태우기 전에 **로컬 Postgres 16 에 그 블록만 넣어 구문을 확인**할 수 있습니다 |
| 29 | 🔴 **`NEXT_PUBLIC_*` 를 런타임 값으로 여기기** | **빌드 시점에 번들에 박힙니다**(서버 코드에서도 그렇습니다). 목 Supabase 를 띄워 놓고 더미 URL 로 빌드한 앱을 돌리면 API 가 **`stale: true` · 0행**을 돌려줍니다 — 「코드가 틀렸다」로 읽기 쉬운 자리입니다. 🔴 **목을 쓸 때는 그 URL 로 다시 빌드하십시오** |
| 30 | 🔴 **「전수로 쟀다」고 적기** | v12 지시서의 「49,140 조합 전수」는 **불가능했습니다** — 실제 축의 곱이 약 **7,500만**입니다(차급 13 × 구간 15 × 차량형태 21 × 운송시간 6 × 물품특성 8 × 왕복편도 3 × 상차 8 × 하차 8 × 대기 × 경유). 🟢 대신 **성질이 구조적으로 보증되는 것**(격자값끼리의 합·차는 격자값)에 **표본**을 얹고 그렇게 적었습니다. 🔴 **표본을 전수라고 적으면 다음 세션이 그 말을 근거로 안 재고 넘어갑니다** |
| 31 | 🔴 **시험이 실제로 실패할 수 있는지 확인하지 않기** | 통과만 보면 **아무것도 안 재는 시험**과 구분되지 않습니다. v12 C장은 **반올림을 일부러 꺼서** 그 시험이 위반 **424건**을 잡는 것까지 확인했고, 마이그레이션 단언은 **13종을 하나씩 깨뜨려** 전부 멈추고 롤백되는 것을 확인했습니다 ⚠️ **PR #179 에서 그 다음 단계를 또 밟았습니다** — 부정 시험 둘이 **멈추기는 했는데 엉뚱한 단언**에서 멈췄습니다(UPDATE 를 통째로 막는 트리거를 걸었더니 앞 시험이 먼저 통과해 버렸습니다). 🔴 **「멈췄다」가 아니라 「의도한 단언이 멈췄다」를 볼 것**(PR #178 이 세운 규칙 그대로입니다). 🚨 그리고 단언 하나가 되돌림을 **「10줄인가」라는 고정 숫자로** 재고 있어서 **2회차에 멀쩡한 DB 에서 마이그레이션이 통째로 멈췄습니다** — 재실행 시점에는 담당자가 이미 줄 수를 바꿔 놨을 수 있습니다. **시험 직전에 떠 둔 값과 견줄 것** |
| 32 | 🔴 **통과한 시험이 「무엇을 쟀는지」 안 보고 넘어가기** | 함정 31 의 다음 단계입니다 — **실패할 수는 있는데 정작 노린 경로를 안 타는** 시험이 있습니다. PR #163 의 첫 목은 「번호가 겹치면 다시 뽑는다」를 잰다고 해놓고, 001·002 가 다 있는 상태라 **첫 시도가 곧 003** 이어서 **재시도 없이도 통과**했습니다. 그대로 뒀으면 「재시도를 확인했다」가 거짓이 될 뻔했습니다. 🟢 **「뽑은 번호를 남이 채간다」 훅**을 목에 넣어 다시 재니 `002 → 003` 두 번 시도가 보였습니다. 🔴 **시도 횟수·호출 순서처럼 「그 경로를 탔다는 증거」를 단언에 함께 넣으십시오** |
| 34 | 🔴 **dev 서버에서 「몇 번 불렸나」를 세기** | Next 14 의 dev 는 **React StrictMode 라 effect 가 두 번 돕니다.** PR #171 에서 「형제 조회가 정확히 한 번」을 단언했다가 **2회가 나와 거짓 ❌** 가 났습니다(코드는 멀쩡했습니다). 🔴 **횟수를 절대값으로 단언하지 말고 「기준이 되는 다른 조회와 같은 횟수인가」로 재십시오** |
| 33 | 🔴 **프로덕션 빌드와 dev 서버를 `.next` 를 안 지우고 번갈아 쓰기** | `npm run build` 를 돌린 뒤 `next dev` 를 띄우면 **첫 페이지 컴파일이 영영 안 끝납니다** — 39차에서 `/quote` 가 **3분을 기다려도 「Compiling」에서 멈췄고 load average 는 0.07** 이었습니다(즉 **일을 하고 있지 않습니다**). 에러도 안 나서 「내 코드가 무한 루프를 만들었나」로 읽기 쉬운 자리입니다. 🟢 **`rm -rf .next` 하고 다시 띄우니 4.7초에 떴습니다.** 🔴 **빌드 ↔ dev 사이에는 항상 `.next` 를 지우십시오** |

🔴 **`public/`에서 지우면 안 되는 파일 일곱** — `grep` 참조 0건이 근거가 되지 않습니다(원래 코드가 참조하지 않는 것이 정상입니다). **앞의 넷은 외부가 절대 URL로 가져다 쓰고, 뒤의 셋은 JSON·서비스워커가 문자열로만 가리킵니다**(그래서 `.ts`/`.tsx`를 grep하면 0건입니다).

```
  naverd2a4fd031fc1d5c8e06b2ca3aace4eb8.html   네이버 소유확인 — 지우면 소유확인이 풀리고
                                               제출한 사이트맵·수집요청이 무효가 됩니다
  email/wecarry-logo-400.png / -white.png      직원 메일 서명 — 이미 발송된 메일이 깨집니다
  landing/service-04-moving.jpg                wecarry24.co.kr(이사)이 씁니다
  og-wecarry-1200x630.jpg                      카톡·검색 미리보기

  icons/*.png (6개)                            설치형 앱 아이콘 — manifest 2종이 JSON
                                               문자열로만 가리킵니다
  offline.html                                 오프라인 안내 — sw.js 가 문자열로만
                                               가리킵니다. 🔴 두 앱이 같이 씁니다
  {customer,admin}/sw.js                       서비스워커 — 🔴 **위치가 곧 담당 구역**이라
                                               루트로 옮기면 한 워커가 두 시스템을 삼킵니다
```

---

## 10. 새 세션에서 이어가는 방법

1. 이 문서를 먼저 읽습니다
2. `CLAUDE.md` 의 **§3 설계 원칙 · §5 로드맵 · §7 함정**을 봅니다
   ⚠️ 세션 기록은 **`docs/history/`** 에 있습니다 — 최신은 `current.md` 최상단(§0 「이력 찾는 법」)
3. 해당 차수의 **작업지시서**를 받습니다 — 값·완료조건·금지사항이 거기 있습니다
4. 🔴 **착수 전 확인부터** 합니다. 지시서 전제가 틀린 적이 여러 번입니다
5. 다르면 **임의로 판단하지 말고 보고 후 대기**합니다

⚠️ **사용자는 개발 초보자입니다.** 용어를 풀어 설명하고, 무엇을 왜 하는지 먼저 말한 뒤 진행하십시오.

⚠️ **`CLAUDE.md`·`HANDOFF.md`만 바뀌는 커밋은 PR 없이 `main`에 바로 반영합니다**(사용자가 정한 규칙). 코드가 한 줄이라도 섞이면 이 규칙은 적용되지 않습니다.

---

## 부록 — v20 이후 무엇이 바뀌었나

v20은 **세션 55차 / 지시서 26차 진행 중**(2026-08-27) 시점의 문서입니다.

🔴 **이 표는 v20 문서를 들고 있는 사람을 위한 안내이고, 본문(§3·§5·§6)이 정본입니다.**
어긋나는 줄이 보이면 **본문을 믿으십시오.** 이 표는 `/merge` 의 매 차수 갱신 대상이
아니라 **반드시 낡습니다.**

| 영역 | v20 | 지금 |
|---|---|---|
| 포털 | 셸·홈·발주까지 개편, 견적·조회는 진행 중 | **전 화면 개편 완료** — 담당자 정보 포함 **8화면 전부 pv2**(PR #121) |
| 랜딩 | 8섹션 개편 · **이미지 5종 자리표시자**(공개 차단) | **이미지 32종 전부 실물** · WHY 아코디언 · 사업자정보 모달 · 모바일 여백·자동 슬라이드 |
| 폼 3화면 | 미개편 | `/quote`·`/apply`·`/customer/login` **시안으로 교체** |
| `/status` | 존재 | 🔴 **라우트까지 삭제** |
| 도메인 | 미확정 | **wecarrylogis.co.kr** 확정 · OG·sitemap·canonical 완료 |
| 대표 메일 | 개인 도메인 | **biz@wecarrylogis.co.kr**(전용 도메인 웹메일) |
| 약관 | 34차 개정본(회사가 배상 주체) | 배상책임은 차주 및 그 보험자에게 **있다** · 제20조 4개 항 · 미신고 건 한도는 법령에 위임 (🔴 **판본은 이 문서 머리 줄에만 적습니다** — 여기에 적었더니 세 판본째 갈렸습니다) |
| 보험 | 미가입 · 플래그 off | **일반화물 증권 수령**(1사고당 1억) · 플래그 on · 값은 화면에 안 나감 |
| 기준가 | 코드 하드코딩 | **DB 실시간 조회** · 게시 6행 · 저장 전 확인 창 · **값은 v11 로 교체**(PR #135 · §5-1) |
| 공개 문구 | 「1톤부터 5톤 이상까지」 | **「1톤부터 5톤 이상, 특수차량까지」**(25톤·특수차량 금지 해제, 「이사」는 다시 금지) |
| 공개 차단 | 1건(랜딩 이미지) | **0건** — 남은 선행 조건은 **변호사 검수**뿐 |
| 설치형 앱 | 없음 | **포털·내부관리가 홈 화면 앱**(PR #126) — manifest 2 · 서비스워커 2 · 오프라인 안내 1 · 설치 버튼 4곳 |
| 설치형 앱 — 구역 | `/admin/`(끝 슬래시, 주소 띠가 남던 상태) | **`/admin`**(PR #127) — 홈까지 구역 안이라 서비스워커가 홈도 맡습니다 |
| 로그인 화면 높이 | `min-height: 100vh` (모바일에서 첫 화면이 늘었다 줄었다) | **`.screen-fit`(`100dvh`)** (PR #128) — 운송관리 로그인은 모바일 **693px** 로 한 화면. ⚠️ **PR #128 시점의 726px 는 낡았습니다** — PR #148 이 이용가이드 링크 한 줄을 넣으면서 여백 여섯 곳을 조여 **693px** 가 됐습니다(실측). 🔴 **여백에서 더 뺄 곳이 없습니다** |
| 카톡으로 받은 링크 | 있지도 않은 메뉴를 안내 | **앱 안 브라우저를 갈라내고 기본 브라우저로 넘김**(PR #128) |

🔴 **v20의 "다음 예정 작업"은 대부분 소진됐거나 순서가 바뀌었습니다.** 지금 유효한 로드맵은 위 §6이고, 원본은 `CLAUDE.md` §5에 있습니다.

---

*이 문서는 `CLAUDE.md`를 대체하지 않습니다. 근거와 이력은 그쪽에 있고, 이 문서는 지금 필요한 것만 추린 것입니다.*
