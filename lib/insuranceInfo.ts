// 적재물배상책임보험 정보의 유일 정의처.
//
// 🟢 **2026-09-01 에 일반화물 증권을 받아 아래 값 5개가 채워졌다**(DB손해보험
// 120261161557, 화물자동차운송주선위험(대물), 1사고당 1억원, 보험기간 2026-09-02 ~
// 2027-09-01). 그래서 `INSURANCE_INFO_IS_PLACEHOLDER` 를 false 로 내렸다.
//
// 🔴 **이 값들을 화면에 렌더링하지 말 것 — 내부 값이다.** 약관 제19조 2항이
// *「요청이 있는 경우 보험 가입 사실과 보상 한도를 안내합니다」* 라고만 적고 있고,
// 34차가 제19조를 **금액을 쓰지 않는 설계**로 전면 개정했다(상한은 3항 가액 신고제와
// 4항 고가물 특칙으로 만든다). 화면에 한도를 적으면 그 설계가 깨진다.
//
// ⚠️ **자기부담금이 1사고당 200,000원이다** — 20만원 이하 손해는 보험금이 나오지 않고,
// 그 이상이어도 20만원이 공제된다. 즉 **소액 사고에는 실질적으로 보험이 작동하지 않는다.**
// 파손 문의에 「보험으로 다 처리된다」는 취지로 답하지 말 것.
//
// ⚠️ **이사화물 증권(120260427278)은 여기에 넣지 않는다** — 2026-09-02 확정으로 「이사」를
// 고객 접점에서 뺐기 때문이다. 🔴 **그 증권 자체는 살아 있다 — 「해지됐다」고 적지 말 것.**
//
// 🔴 **2026-08-20 시점 기록(아래)은 그대로 둔다** — 왜 이 파일이 플래그 두 개로 나뉘어
// 있는지의 근거다. 미가입 상태에서 "보험 가입"이 화면에 보이면 허위·과장 광고이므로,
// 값이 채워질 때까지 `INSURANCE_ENABLED`로 막아두는 구조였다.
//
// ⚠️ 34차에 보험 언급을 고객 접점에서 **전부 제거**한 이력이 있다 — 당시 가입돼 있던 증권이
// 이사화물 운송주선업자 특별약관이라 **일반화물 사고가 담보되는지 자체가 확인되지 않았기**
// 때문이다. 약관 제19조(손해배상)는 보험과 무관하게 성립하도록 짜여 있으므로, 보험을 다시
// 노출하는 것은 **가입이 끝나고 담보 범위가 확인된 뒤**의 별도 판단이다.
//
// ── 이 파일을 쓰는 규칙(13차에 UI가 붙었다 — 계속 지킬 것) ────────────────────────
// 1. `INSURANCE_ENABLED`가 false면 보험 블록을 **DOM에 아예 렌더링하지 않는다.**
//    `visibility: hidden`이나 `display: none`이 아니라 **조건부 렌더링**이어야 한다 —
//    숨기기만 하면 페이지 소스에는 남아서 검색·스크래핑에 잡힌다.
// 2. **보험 문구를 컴포넌트에 하드코딩하지 않는다.** 반드시 이 파일만 참조할 것.
// 3. **히어로 신뢰줄 4번째와 안전·책임 섹션, 두 곳을 이 플래그 하나로 제어한다.**
//    별도 플래그를 만들지 말 것 — 한쪽만 켜지는 사고를 막기 위함이다.
//
// ⚠️ **13차에 실제 UI가 붙었다**(12차에는 참조 UI가 0건이었음). 참조 지점은 `app/page.tsx`
// 두 곳뿐이다 — 히어로 신뢰줄 `TRUST_POINTS`와 ⑦ 안전·책임 `SAFETY_ITEMS`. 둘 다
// 배열에 조건부로 항목을 **넣고 빼는** 방식이라, false면 DOM에 아예 나타나지 않는다.
//
// 🔴 **가입이 끝나 켤 때는 두 플래그를 같이 바꾼다** — `INSURANCE_ENABLED = true`,
// `INSURANCE_INFO_IS_PLACEHOLDER = false`. 한쪽만 바꾸면 빌드 경고가 계속 남거나
// (후자만 내림), 상수가 빈 채로 화면에 노출된다(전자만 올림).

// 🔴 **2026-08-31 (30차 리뷰) 에 사용자 지시로 true 가 됐다** — *"위캐리를 선택해야 하는
// 이유에서 03. 적재물배상책임보험은 추가해줘. 나중에 이용약관만 수정하면 된다."*
// 증권은 같은 날 "오늘·내일 중 전달 예정"으로 확인됐고, 공개일은 2026-09-07 이다.
// ⚠️ **그래서 지금 이 값은 "가입 완료"가 아니라 "공개 시점에는 가입돼 있다"는 전제다.**
// 🔴 증권을 받으면 담보 종류(일반화물/이사화물)와 한도를 확인하고 아래 값 5개를 채운 뒤
//    `INSURANCE_INFO_IS_PLACEHOLDER` 를 false 로 내릴 것. 담보가 확인되지 않은 채로
//    공개일이 오면 이 값을 다시 false 로 내려야 한다.
// 🟢 **2026-09-01 에 일반화물 증권을 받아 그 전제가 해소됐다** — 이제 "가입 완료"가 맞다.
//    🔴 **이 값을 건드리지 말 것**(이미 true 다).
/** 적재물배상책임보험 가입 완료 시 true. false면 관련 UI는 렌더링되지 않는다. */
export const INSURANCE_ENABLED = true;

// 🔴 아래 5개는 **일반화물 증권 한 벌**이다(증권번호 120261161557). 이사화물 증권 값을
// 섞어 넣지 말 것 — 두 벌이 되면 어느 담보로 안내하는 것인지 알 수 없어진다.

/** 상품명 */
export const INSURANCE_PRODUCT_NAME = "적재물배상책임보험";
/** 보험사명 */
export const INSURANCE_INSURER = "DB손해보험";
/** 1사고당 보상 한도. 🔴 화면에 적지 말 것(제19조는 금액을 쓰지 않는 설계다). */
export const INSURANCE_COVERAGE_LIMIT = "1사고당 100,000,000원 (화물자동차운송주선위험 · 대물)";
/** 보험 개시일 (YYYY-MM-DD). 만기는 2027-09-01 — 갱신 시 이 값을 함께 고칠 것. */
export const INSURANCE_EFFECTIVE_DATE = "2026-09-02";
/** 자기부담금·면책 안내. ⚠️ 20만원 이하 손해는 보험금이 나오지 않는다. */
export const INSURANCE_EXCLUSION_NOTE =
  "1사고당 자기부담금 200,000원이 공제됩니다. 고가·특수 화물은 접수 시 미리 알려주시면 조건을 확인해 안내드립니다.";

// 위 값들이 아직 비어 있음을 나타내는 자리표시자 플래그.
// next.config.mjs가 빌드·서버 기동 시 이 파일을 텍스트로 읽어 `= true`이면 콘솔에 경고를
// 출력한다(빌드를 실패시키지는 않음 — 26·29·30차와 동일한 방식).
//
// 🔴 **반드시 리터럴 `true`/`false`로 둘 것.** 경고 로직이 `= true` 문자열을 정규식으로
// 찾으므로, 계산식이나 다른 변수로 유도하면 경고가 조용히 동작하지 않는다.
// 🔴 `INSURANCE_ENABLED`를 경고 대상으로 쓸 수 없어서 이 플래그를 따로 둔 것이다 —
// 그 값은 미가입 상태에서 `false`라 `= true` 검사에 걸리지 않는다.
// 가입이 끝나면 위 값들을 채우고 **이 플래그를 false로, `INSURANCE_ENABLED`를 true로**
// 함께 바꿀 것.
//
// 🟢 **2026-09-02 에 false 로 내렸다** — 증권을 받아 값 5개를 채웠다. 이로써 빌드 경고에서
// 보험 1건이 사라진다. 🔴 **증권이 만기(2027-09-01)되고 갱신이 확인되지 않으면 값을 비우고
// 이 플래그를 다시 true 로 올릴 것.**
export const INSURANCE_INFO_IS_PLACEHOLDER = false;
