// 견적서 유효기간 — 🔴 **의존성이 하나도 없는 파일이어야 한다.**
//
// ⚠️ **왜 `lib/quoteShare.ts` 에서 떼어냈나** — 그 파일은 토큰을 만들려고 `crypto` 를
//    import 한다. 그런데 공유 견적서 화면(`app/q/[token]/page.tsx`)은 **클라이언트
//    컴포넌트**이고 이 숫자 하나 때문에 그 파일을 import 했다. top-level 에 `crypto`
//    가 있는 모듈을 클라이언트가 import 하면 **번들러가 그 노드 모듈을 클라이언트
//    번들에 끌어온다**(폴리필이 붙거나 빌드가 깨진다).
//
// 🔴 **이 파일에 import 를 추가하지 말 것.** 상수만 사는 자리다.
//
// ⚠️ 같은 규칙이 **세 곳에 숫자로 박혀 있다** — 견적서 print 2종과 `lib/quoteExcel.ts`
//    가 각자 `setDate(getDate() + 7)` 를 한다. 이번에 그 셋을 건드리지 않은 것은
//    범위 밖이라서이지 그게 맞아서가 아니다. 🔴 **유효기간을 바꿀 일이 생기면
//    그 셋도 같이 볼 것**(지금은 넷이 우연히 같은 값이다).

/** 견적서 유효기간 — 발행일(`quotes.created_at`)로부터 며칠 */
export const QUOTE_VALID_DAYS = 7;
