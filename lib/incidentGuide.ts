// ─────────────────────────────────────────────────────────────────────────────
// 사고 대응 체크리스트 — **유일 정의처** (2026-09-17)
//
// 왜 — `HANDOFF.md` §5-5 에 적힌 **보험 조건이 문서에만 있고 시스템 어디에도 없었다.**
// 사고가 난 그 순간 담당자가 기억해내야 하는데, **한 줄을 놓치면 보상과 0 이 갈린다**
// (도난신고가 없으면 약관이 「분실」로 간주해 면책한다).
//
// 🔴 **관리자 전용이다 — 화주포털·견적서·엑셀 0줄.**
//    화주에게 보험 조건이 보이면 **협상 카드**가 된다(무엇이 면책인지 알고 청구한다).
//
// 🔴 **누르게 만들지 말 것.** 「확인함」 체크박스를 두면 **누른 기록이 곧
//    「알고도 안 했다」의 증거**가 된다. 보여주기만 한다.
//
// 🔴 **보험 금액(1억)을 적지 말 것** — 증권 갱신 때 낡는다(만기 2027-09-01).
//    자기부담금 20만원은 **내부 화면이라** 적어도 된다.
//
// 🔴 **화면 파일에 이 문장들을 직접 적지 말 것** — 보험 조건이 바뀌면 한 곳만 고친다.
// ─────────────────────────────────────────────────────────────────────────────

/** 한 줄의 무게. `danger` 는 놓치면 보상을 잃는 것, `warn` 은 알아둘 것. */
export type IncidentGuideTone = "danger" | "warn";

export type IncidentGuideLine = { tone: IncidentGuideTone; text: string };

/**
 * 🔴 **보험 제외 화물 6종** — 특별약관을 하나도 가입하지 않아 **고지해도 보험이
 *    안 나온다**(66차 실측). 🔴 「고지하면 된다」고 적지 말 것.
 *    ⚠️ 「유리제품」에 **유리병에 담긴 음료·소스·주류**가 들어간다 — 실무에서 가장
 *    자주 걸리는 자리라 한 줄로 따로 적는다.
 */
const EXCLUDED_CARGO_LINES: IncidentGuideLine[] = [
  {
    tone: "danger",
    text:
      "보험 제외 화물인지 확인하십시오 — 원고류 · 귀금속류 · 시계/담배/모피류 · " +
      "유리제품 · 계란류 · 살아있는 동물.",
  },
  {
    tone: "warn",
    text: "「유리제품」에 유리병에 담긴 음료·소스·주류가 들어갑니다.",
  },
];

/**
 * 사유 코드별로 띄울 문장.
 * 🔴 여기에 없는 코드는 **아무것도 띄우지 않는다** — 할 말이 없는데 상자만 그리면
 *    다음부터 담당자가 이 상자를 안 읽는다.
 */
export const INCIDENT_GUIDE: Record<string, IncidentGuideLine[]> = {
  lost: [
    {
      tone: "danger",
      text:
        "화물이 없어진 건은 즉시 경찰에 도난신고를 접수하십시오. " +
        "신고가 없으면 약관이 「분실」로 간주해 보험이 나오지 않습니다.",
    },
    ...EXCLUDED_CARGO_LINES,
    {
      tone: "warn",
      text:
        "자기부담금은 회사가 흡수합니다(20만원). " +
        "화주에게 「보험으로 처리된다」고 미리 답하지 마십시오.",
    },
  ],
  accident: [
    {
      tone: "danger",
      text:
        "사람이 다쳤으면 그것이 먼저입니다. 화물 처리는 그다음입니다.",
    },
    {
      tone: "danger",
      text: "사진을 확보하십시오 — 현장 · 화물 · 차량.",
    },
    ...EXCLUDED_CARGO_LINES,
    {
      tone: "warn",
      text:
        "자기부담금은 회사가 흡수합니다(20만원). " +
        "화주에게 「보험으로 처리된다」고 미리 답하지 마십시오.",
    },
  ],
  damage: [
    {
      tone: "danger",
      text: "사진을 확보하십시오 — 현장 · 화물 · 차량.",
    },
    ...EXCLUDED_CARGO_LINES,
  ],
};

export function getIncidentGuide(code: string | null | undefined): IncidentGuideLine[] {
  if (!code) return [];
  return INCIDENT_GUIDE[code] || [];
}

/**
 * ⚠️ **사진 업로드 칸은 이 화면에 없다** — 29차가 조회 화면에서 사진 자리를 뺐고
 *    (PR #107 리뷰) 이번 범위도 아니다. 🔴 **만들지 말고 이 문장만 보여줄 것.**
 */
export const INCIDENT_PHOTO_NOTE =
  "사진 보관 칸은 아직 없습니다 — 담당자가 따로 보관하십시오.";
