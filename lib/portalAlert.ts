// ─────────────────────────────────────────────────────────────────────────────
// 화주포털 알림 — **포털 쪽 정의처** (2026-09-16 · 사용자 요청 「화주포털에도 적용」)
//
// 🔴 **38차 지시서는 「화주포털은 한 줄도 건드리지 않는다」였다.** 그 뒤에 사용자가
//    *「이 알림기능을 화주포털에도 적용할 수 있나?」* 로 범위를 넓혔고 **A장만 먼저**로
//    확정했다(2026-09-16). 🔴 **지시서의 그 금지를 근거로 이 파일을 지우지 말 것.**
//
// 🔴 **배지와 배너의 신호가 갈렸다**(2026-09-16 저녁 · 사용자 지시 세 건).
//
//      배지(`counts`)      `updated_at > 마지막으로 본 시각` — **무엇이 바뀌어도** 센다.
//                          「이 화면에 볼 것이 있다」는 뜻이라 넓은 것이 맞다.
//      배너(`signals`)     **아래 두 상태 목록에 든 것만** 센다. 「폰·화면을 울릴 일인가」다.
//
//    🔴 **한동안 둘이 같은 수였고**(「따로 세면 배지는 1인데 배너는 2가 된다」고 적혀
//    있었다) 그래서 담당자가 메모 한 줄만 고쳐도 배너가 떴다. 사용자가 **세 번에 걸쳐**
//    좁히라고 했다 — ①「확인중 상태에서는 알림이 가지 않아도 된다」 ②「화주가 본인이
//    누른 견적승인인데 왜 알림이 오나」 ③「상차완료시에는 알림이 가지 않아도 된다」.
//    🔴 **`updated_at` 하나로는 그 셋을 하나도 걸러낼 수 없다** — 무엇이 바뀌었는지를
//    모르기 때문이다. 그래서 **바뀐 뒤의 상태**로 판정한다.
//    🔴 **배지와 같은 수로 되돌리지 말 것** — 세 지시가 한꺼번에 뒤집힌다.
//    🟢 배너 수 ≤ 배지 수 가 항상 성립한다(부분집합이라) — 「배지는 3인데 배너는 1」은
//    「셋이 바뀌었고 그중 하나가 새 견적서」라는 뜻이라 어긋난 것이 아니다.
//
// 🔴 **라벨은 여전히 「견적 업데이트」다** — 「새 견적」으로 바꾸지 말 것.
//    좁히긴 했지만 `견적제출` 로 **다시** 바뀌는 경우(담당자가 금액을 고쳐 재제출)가
//    있어서 「새」는 여전히 거짓말이 될 수 있다.
// ─────────────────────────────────────────────────────────────────────────────

import { collectRises, type AlertRise } from "@/lib/alertCore";

/**
 * 🔴 `app/customer/CustomerPortalShell.tsx` 의 `counts` 키와 **같은 이름**이다.
 *
 * ⚠️ **배지에 세는 것과 알림으로 부르는 것이 갈렸다**(2026-09-16) — 아래 두 타입을
 *    보라. `invoices`·`announcements` 는 **배지에는 남고 알림에서만 빠졌다.**
 */
export type PortalCountKind = "quotes" | "dispatches" | "invoices" | "announcements";

/**
 * 🔴 **알림으로 부르는 종류 — 둘뿐이다**(사용자 확정 2026-09-16).
 *
 *    ① `invoices` 를 뺀 이유 — *「화주포털 알림중 정산관련해서는 알림이 안뜨는게
 *       좋겠다」*(예로 든 것이 **세금계산서 발행완료 · 화주입금완료 · 차주지급완료**).
 *       포털 알림의 신호는 `invoices.updated_at` 하나라 **그 셋을 골라낼 수가 없다** —
 *       담당자가 정산 건의 어느 칸을 만져도 똑같이 울린다. 그래서 종류째 뺐다.
 *    ② `announcements` 를 뺀 이유 — *「화주포탈은 상차완료, 하차완료, 공지사항 알림도
 *       빼자. 견적을 받을때, 배차완료, 운송완료 시에만 알림이 가게 설정」*(같은 날).
 *       ⚠️ **그 앞 줄에 「공지는 유일하게 새로 생기기만 하는 것이라 「새」가 맞다」고
 *       적혀 있었는데 그것은 「라벨이 맞다」는 말이지 「알려야 한다」는 말이 아니다.**
 *       🔴 그 문장을 근거로 되살리지 말 것.
 *
 *    🔴 **되살리지 말 것** — 정산을 되살리려면 「어떤 변경일 때만 부를지」를 먼저
 *    정해야 하고, 그것은 `updated_at` 이 아니라 사건별 신호를 새로 만드는 일이다.
 *    ⚠️ **웹 푸시에서도 같은 것들을 함께 없앴다**(`lib/portalPushNotify.ts`) —
 *    화면 안 배너만 끄고 푸시를 남기면 **폰으로는 오는데 화면에는 안 뜨는** 상태가 된다.
 *    🟢 **배지는 넷 그대로다** — 정산·공지 화면에 볼 것이 생긴 것은 사실이라 수는 보여준다.
 */
export type PortalAlertKind = "quotes" | "dispatches";

/**
 * 🔴 경로는 **포털 메뉴의 실제 경로**다. 「발주 요청」(`/customer/request`)은 여기 없다 —
 *    그 화면은 폼 하나이고 담당자가 무엇을 하든 결과는 **「견적 확인」에** 뜬다
 *    (27차 리뷰 3라운드 확정, 그래서 배지도 안 단다). 🔴 더하지 말 것 — 눌러도 볼 것이 없다.
 */
export const PORTAL_ALERTS: Record<PortalAlertKind, { title: string; href: string }> = {
  // 🔴 견적이 맨 앞이다 — 화주가 답해야 진행되는 유일한 것이라(견적 승인) 가장 급하다.
  quotes: { title: "견적 업데이트", href: "/customer/quotes" },
  dispatches: { title: "배차·운송 업데이트", href: "/customer/dispatches" },
};

export const PORTAL_ALERT_KINDS: PortalAlertKind[] = ["quotes", "dispatches"];

/**
 * 🔴 **배지가 세는 값이라 `invoices`·`announcements` 가 들어 있다** —
 * 알림 종류(`PortalAlertKind`)와 다르다. **둘을 같은 타입으로 합치지 말 것.**
 */
export type PortalAlertCounts = Record<PortalCountKind, number>;

/**
 * 🔴 **배너를 띄울지 판정하는 수 — 배지(`PortalAlertCounts`)와 다른 값이다.**
 *    키가 둘뿐인 것이 곧 「정산·공지는 배너를 안 띄운다」이고, 그 둘의 **값**이
 *    좁은 것이 「확인중·수주·상차완료로는 안 띄운다」다.
 */
export type PortalAlertSignals = Record<PortalAlertKind, number>;

/** 늘어난 것만 고른다. 규칙 셋은 `lib/alertCore.ts` 의 `collectRises()` 한 곳에 있다. */
export function collectPortalRises(
  prev: Partial<PortalAlertSignals>,
  next: PortalAlertSignals
): AlertRise<PortalAlertKind>[] {
  return collectRises(PORTAL_ALERT_KINDS, prev, next);
}

// ── 배너를 띄울 상태 ─────────────────────────────────────────────────────────
//
// 🔴 **웹 푸시(B장)의 사건 넷과 같은 뜻이다** — `lib/portalPushNotify.ts` 의
//    `quote_submitted` · `dispatch_confirmed` · `transport_completed` ·
//    `dispatch_cancelled`(2026-09-17 신설).
//    🔴 **한쪽만 고치지 말 것** — 갈리면 「폰으로는 오는데 화면에는 안 뜨는」(또는 그
//    반대) 상태가 된다. 그 둘을 한 파일로 합치지도 말 것(푸시 모듈은 서버 전용이고
//    이 모듈은 소리(`alertCore`)를 끌고 온다).

/**
 * 견적 — **「견적서를 받았다」일 때만** 울린다.
 *
 * 🔴 `상담중`(화면 글자는 「확인중」)을 넣지 말 것 — 담당자가 견적을 **저장한 순간**의
 *    상태라 화주가 할 일이 아직 없다(사용자 지시 ①).
 * 🔴 `수주` 를 넣지 말 것 — 그 값으로 바꾸는 사람이 **화주 본인**이다
 *    (`/api/customer/approve-quote`). 본인이 누른 일로 본인 화면이 울리면 안 된다
 *    (사용자 지시 ②). ⚠️ 담당자가 관리자 화면에서 손으로 `수주` 로 바꾸는 경우에도
 *    안 울리는데, 그 건은 **화주가 이미 구두로 확정한 것**이라 새 소식이 아니다.
 * 🔴 `보류`·`실패` 도 없다 — 담당자가 전화로 할 이야기다.
 */
export const PORTAL_ALERT_QUOTE_STATUSES = ["견적제출"] as const;

/**
 * 배차 — **배차가 잡혔을 때와 운송이 끝났을 때만** 울린다.
 *
 * 🔴 `상차완료` 를 넣지 말 것(사용자 지시 ③). `하차완료`·`접수중`·`문제발생` 도 없다.
 * ⚠️ **`하차완료` 와 `운송완료` 는 DB 에서 다른 값인데 화주 화면은 둘 다 「운송완료」로
 *    보여준다**(`lib/dispatchStage.ts` 2단계). 여기는 푸시와 같이 **`운송완료` 에만**
 *    건다 — 담당자가 `하차완료` 에서 멈추면 완료 배너가 안 뜬다. 🔴 단계로 바꿔 걸면
 *    `하차완료` 에서도 울리는데 **그것은 이 확정과 반대다 — 바꾸려면 먼저 물을 것.**
 *
 * 🔴 **`취소` 가 셋째다**(2026-09-17 · 사용자 확정 「화면 배너 + 웹 푸시까지」).
 *    ⚠️ 바로 위가 「상차완료를 넣지 말 것」이라 적고 있는데 **취소는 그 결이 아니다** —
 *    운송 도중의 중간 보고가 아니라 **이미 나간 배차확정 알림이 무효가 되는 것**이다.
 *    🔴 「알림을 좁혔다」를 근거로 빼지 말 것.
 *    ⚠️ 그리고 화주 화면은 취소된 배차를 **감추므로**(사용자 확정 (A), 2026-09-17)
 *    이 배너와 푸시가 **「그 건이 왜 사라졌는지」를 말해 주는 유일한 경로**다.
 *    🔴 **`lib/notifyPortalPush.ts` 의 매핑과 한 벌이다** — 한쪽만 고치면 「폰으로는
 *    오는데 화면에는 안 뜨는」(또는 그 반대) 상태가 된다.
 */
export const PORTAL_ALERT_DISPATCH_STATUSES = ["배차확정", "운송완료", "취소"] as const;
