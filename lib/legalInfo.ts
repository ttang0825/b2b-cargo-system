// 법적 문서(이용약관 `/terms`, 개인정보처리방침 `/privacy`, 이메일무단수집거부
// `/email-policy`)가 공유하는 상수의 유일 정의처.
//
// 시행일이 세 문서에 걸쳐 여러 번 등장하고 아직 확정되지 않아서, 화면마다 날짜를
// 적지 않고 여기 한 곳만 고치면 전부 갱신되도록 모아둠.
//
// ⚠️ 회사 기본 정보(상호·대표자·주소·사업자등록번호·허가번호)는 이 파일이 아니라
// `lib/companyInfo.ts`가 유일 정의처임. 대표번호는 `lib/contactInfo.ts`.
// **같은 값을 여기에 다시 적지 말 것.**

// ── 시행일 ────────────────────────────────────────────────────────────────────

// 실제 공개일(2026-09-07, 월). 값과 아래 플래그는 **항상 같이** 바꿀 것 —
// 값만 바꾸고 플래그를 true로 두면 빌드 경고가 계속 남는다.
//
// ⚠️ **화면에 나오는 곳은 11곳이다**(예전 주석의 "5곳"은 문서 본문만 센 숫자였음).
// 시행일을 바꾸면 아래를 전부 확인할 것:
//   본문 5개 표시 항목 — `lib/legal/terms.ts`(약관 부칙) /
//     `lib/legal/privacy.ts`(시행일자·최종 개정일이 **한 줄에 2항목** + 부칙) /
//     이메일무단수집거부 게시일
//   페이지 헤더 3곳 — `app/terms|privacy|email-policy/page.tsx`
//   모달 헤더 3곳 — `components/LegalLinks.tsx`
// 🔴 이메일무단수집거부 게시일은 `lib/legal/emailPolicy.ts`에 참조가 **0건**이고
//    `app/email-policy/page.tsx`가 `effectiveLabel` prop으로 넘긴다 —
//    내용 파일만 보고 찾으면 놓친다.
export const LEGAL_EFFECTIVE_DATE = "2026-09-07";

// 위 시행일이 아직 미확정 자리표시자임을 나타내는 플래그.
// next.config.mjs가 빌드·서버 기동 시 이 파일을 읽어 true이면 콘솔에 경고를 출력함
// (빌드를 실패시키지는 않음 — 26차 대표번호와 동일한 방식).
// 12차에 공개일이 2026-09-07로 확정되어 false로 내림.
export const LEGAL_EFFECTIVE_DATE_IS_PLACEHOLDER = false;

// ── 문서 버전 ─────────────────────────────────────────────────────────────────

// 개인정보처리방침과 약관은 개정 주기가 다를 수 있어 항목별로 나눠 정의함 —
// 두 문서 중 한쪽만 개정될 때 다른 쪽 동의까지 무효가 되면 안 되기 때문.
//
// 🔴 **문서 본문(`lib/legal/privacy.ts`·`lib/legal/terms.ts`)을 실질적으로 고치면 해당 버전
// 문자열을 반드시 올릴 것.** 이 값이 `consents.version`에 그대로 저장되므로(14차), 본문이
// 바뀌었는데 버전이 그대로면 **서로 다른 문서에 동의한 사람이 같은 버전으로 기록된다.**
// 그러면 "이 사람이 무엇에 동의했는가"를 되짚을 수 없어 기록의 의미가 사라진다.
//
// ⚠️ 14차에 `/quote` 화면 동의 문구의 사실 오류를 고쳤지만 **버전은 올리지 않았다** —
// 처리방침·약관 **본문은 바뀌지 않았고**, 화면 문구는 그 문서를 요약한 UI 텍스트인데
// 요약이 원문(제3조 보유기간)과 어긋나 있던 것을 맞춘 작업이기 때문이다.
// 당시 기존 동의 데이터가 0건이라 "같은 v1인데 다른 문구에 동의한 사람"도 생기지 않았다.
//
// 🔴 **2026-09-03 에 `terms-v1` → `terms-v2` 로 올렸다.** 보험 보통약관 대조로
// `lib/legal/terms.ts` 본문을 실질 개정했기 때문이다 — 제19조에 「취급 협의 대상 화물」
// 항이 신설되고(항 7 → 8), 5항에 특별손해가 들어가고, 2항에 보험·배상책임 관계
// 문장이 붙고, 면책 호가 2개 늘고, 제20조 2항에 「알면서 인도한 경우」 예외가 생겼다.
// ⚠️ 14차와 달리 이번엔 **`terms-v1` 로 저장된 동의가 실제로 있다** — 그 사람들이 본
// 약관과 지금 약관이 다르므로 버전을 안 올리면 기록이 뒤섞인다.
// 🟢 `PRIVACY_POLICY_VERSION` 은 그대로다 — 처리방침 본문은 한 글자도 안 고쳤다.
export const PRIVACY_POLICY_VERSION = "privacy-v1";
export const TERMS_VERSION = "terms-v2";

// ── 화면 동의 문구 ────────────────────────────────────────────────────────────
//
// 🔴 **화면 JSX에 하드코딩하지 말 것.** 14차 전까지 두 문장이 각 화면에 직접 적혀 있어서
// 버전과 함께 관리할 수 없었다. 문구를 고칠 때 위 버전 상수를 같이 보게 하려고 이 파일에 둔다.
//
// ⚠️ 두 문구 모두 **개인정보 수집·이용만** 말하고 이용약관은 언급하지 않는다. 그래서
// `lib/consent.ts`가 `privacy` 한 항목만 저장한다 — **문구에 없는 동의를 기록하면 안 된다.**
//
// ⚠️ 두 문구 모두 개인정보보호법 제15조 2항의 고지 4항목(목적·수집항목·보유기간·거부권)을
// 아직 다 갖추지 못했다. **고지 항목 개편은 변호사 검토와 함께 별도 차수**이며, 14차는
// 아래 `/quote` 문구의 **사실 오류 1건만** 고쳤다.

/**
 * `/quote` 견적 문의 동의 문구.
 * 🔴 예전 문구는 상담이 끝나면 보관 기간 없이 처리된다고 적혀 있었는데, 실제로는
 * `public_quote_requests`에 저장되고 관리자·방문자가 조회하며 파기 로직도 없었다.
 * 일반인은 그 문장을 "보관하지 않는다"로 읽는다 — **처리방침 제3조("견적 문의는 문의일로부터
 * 1년")에 맞춰 정정한 것**이며, 새로운 법적 판단이 아니라 원문과의 정합성 수정이다.
 * ⚠️ 문구가 1년을 약속하므로 **1년 뒤에는 실제로 파기해야 한다**(파기 로직은 아직 없음).
 */
/**
 * 🔴 **한 문장을 「제목 / 설명」 두 조각으로 나눠 둔 것이다 — 내용은 한 글자도 안 바뀌었다.**
 * 31차 리뷰(2026-09-01)에 사용자가 *"시안대로 「개인정보 수집·이용에 동의합니다.」가 먼저
 * 오고 다음 줄에 「입력하신 정보는 …」 이 회색톤으로 작게"* 로 확정해서, 화면이 두 줄로
 * 그리려면 조각이 필요해졌다. `QUOTE_CONSENT_TEXT` 는 그 둘을 다시 이어 붙인 값이라
 * **기존 문자열과 동일**하다(옛 호출부가 그대로 돌아간다).
 */
export const QUOTE_CONSENT = {
  label: "개인정보 수집·이용에 동의합니다.",
  detail: "입력하신 정보는 견적 상담 목적으로만 이용되며, 문의일로부터 1년간 보관 후 파기됩니다.",
} as const;

export const QUOTE_CONSENT_TEXT = `[필수] ${QUOTE_CONSENT.detail} ${QUOTE_CONSENT.label}`;

/**
 * `/apply` 운송관리 계정 신청 동의 문구.
 * ⚠️ 14차에서 **내용은 그대로 두고 상수로 옮기기만** 했다. 기간이 적혀 있지 않지만
 * `/quote`와 달리 **사실과 어긋나지는 않는다**(처리방침 제3조는 미승인·거절 건을 6개월로 둠).
 */
/** 🔴 `QUOTE_CONSENT` 와 같은 이유로 나눠 둔 것이다 — 이어 붙이면 기존 문자열과 동일하다. */
export const APPLY_CONSENT = {
  label: "개인정보 수집·이용에 동의합니다.",
  detail: "입력하신 정보는 고객 등록 심사 목적으로만 이용되며, 승인 여부와 무관하게 안전하게 관리됩니다.",
} as const;

export const APPLY_CONSENT_TEXT = `[필수] ${APPLY_CONSENT.detail} ${APPLY_CONSENT.label}`;

// ── 국외 이전(처리방침 제6조) ──────────────────────────────────────────────────

// Vercel Inc.의 개인정보 문의처. 29차·30차에는 이 실행 환경에서 vercel.com 접근이
// 차단되어(프록시 403) 확인하지 못했고, **12차에 사용자가 Vercel 공식 개인정보처리방침
// 에서 확인해 전달한 값**으로 채웠다(법인 주소가 필요해지면
// 440 N Barranca Avenue #4133, Covina, CA 91723, United States).
// ⚠️ 값이 비면 `lib/legal/privacy.ts`가 "확인 중"으로 표시한다 — 폴백은 그대로 두되
// **추측으로 다른 주소를 적지 말 것.**
export const VERCEL_PRIVACY_CONTACT = "privacy@vercel.com";
export const VERCEL_PRIVACY_CONTACT_IS_PLACEHOLDER = false;

// ── 표시용 헬퍼 ───────────────────────────────────────────────────────────────

/**
 * "2026-09-01" -> "2026년 9월 1일"
 * 법적 문서는 숫자 날짜(2026-09-01)보다 한글 표기가 관례라 화면에서는 이 형태로 보여줌.
 */
export function formatLegalDate(isoDate: string): string {
  const [y, m, d] = isoDate.split("-");
  // 앞자리 0을 떼서 "9월 1일"로 (Number 변환이 "09" -> 9를 처리)
  return `${y}년 ${Number(m)}월 ${Number(d)}일`;
}

export const LEGAL_EFFECTIVE_DATE_LABEL = formatLegalDate(LEGAL_EFFECTIVE_DATE);

// ── 화주포털 발주요청 제3자 제공 동의 ─────────────────────────────────────────
//
// 🔴 처리방침 제4조가 **"제3자 제공은 실제 운송 접수(발주) 시점에 별도의 동의를 받은 후에만
// 이루어집니다"**라고 약속하고 있는데, `/customer/request`는 상·하차지 담당자의 성명·연락처를
// **필수로 받으면서 동의 절차가 없었다.** 20차가 그 어긋남을 맞춘 것이다.
//
// ⚠️ **이 문구는 초안이며 변호사 검토 대상이다.** 개인정보보호법 제15조 2항의 고지 4항목
// (제공받는 자·목적·항목·보유기간)과 거부권 고지를 형식상 갖췄으나 법적 완결성은 별개다.
// 마지막 확인 문장("제3자의 개인정보를 입력하시는 경우…")은 실무 관행이지 법적 면책이 아니다.
//
// 🔴 **화면 JSX에 다시 적지 말 것.** 21차가 이 화면을 새 디자인으로 갈아엎을 때
// 문구·저장 로직·DB는 그대로 재사용한다 — 상수로 두어야 옮기면서 문구가 어긋나지 않는다.
export const PORTAL_ORDER_THIRD_PARTY_CONSENT = {
  title: "[필수] 상·하차지 담당자 정보 제3자 제공 동의",
  intro:
    "입력하신 상·하차지 담당자의 성명·연락처는 운송 수행을 위해 배차된 차주에게 제공됩니다.",
  items: [
    "제공받는 자: 해당 운송 건에 배차된 차주",
    "제공 목적: 상·하차 현장 연락",
    "제공 항목: 담당자 성명, 연락처, 현장 주소",
    "보유·이용 기간: 해당 운송 건 종료 시까지",
  ],
  confirm:
    "제3자의 개인정보를 입력하시는 경우, 해당 정보주체로부터 제공에 대한 동의를 받으셨음을 확인합니다.",
  refusal: "동의를 거부하실 수 있으나, 거부 시 운송 접수가 제한됩니다.",
} as const;

// ── /apply 현장 담당자 정보 동의 (31차 리뷰) ────────────────────────────────
//
// 🔴 **왜 조건부인가.** 현장 상호·담당자명·연락처는 **선택** 칸이고, 안 적으면 제3자의
//    개인정보 자체가 없으므로 받을 동의도 없다. 그래서 이 카드는 **현장 담당자 연락처를
//    입력했을 때만** 화면에 나타나고, 그때만 `consents` 에 기록된다(사용자 지시 2026-09-01
//    — "제3자 동의는 현장 담당자 연락처를 적을시 활성화되고 동의체크를 유도하면 어떤가").
//
// 🔴 **처리방침 제4조와 어긋나지 않는다.** 제4조는 *"견적 문의 또는 고객 등록 신청 단계
//    에서는 어떠한 정보도 제3자에게 제공되지 않습니다"* 라고 못박고 있고 그 말은 **여전히
//    사실이다** — 신청 단계에서는 **수집만** 하고, 차주에게 실제로 넘어가는 것은 발주·배차
//    시점이며 그때 47차가 만든 별도 동의를 다시 받는다. 그래서 이 문구는 「지금 제공한다」가
//    아니라 「나중에 배차 시 전달되는 것에 미리 동의한다」로 쓰여 있다. **바꾸지 말 것.**
//
// ⚠️ **이 문구는 초안이며 변호사 검토 대상이다.** 특히 `confirm` — 신청인이 제3자를 대신해
//    동의할 수는 없으므로 「동의를 받으셨음을 확인」하는 형태이고, 이는 47차가 화주포털에
//    쓴 것과 같은 실무 관행이지 법적 면책이 아니다.
export const APPLY_SITE_CONTACT_CONSENT = {
  title: "[필수] 현장 담당자 정보 수집·제공 동의",
  intro:
    "입력하신 현장 담당자의 성명·연락처는 계정 발급 후 발주·배차 시 해당 운송 건의 차주에게 전달됩니다.",
  items: [
    "수집·제공 항목: 현장 상호, 현장 담당자 성명·연락처",
    "이용 목적: 상·하차 현장 연락 및 배차 안내",
    "제공받는 자: 해당 운송 건에 배차된 차주 (실제 제공은 발주 시점에 별도 동의 후)",
    "보유·이용 기간: 거래 종료 후 관계 법령이 정한 기간까지",
  ],
  confirm:
    "제3자의 개인정보를 입력하시는 경우, 해당 정보주체로부터 수집·제공에 대한 동의를 받으셨음을 확인합니다.",
  refusal:
    "동의를 거부하실 수 있으며, 이 경우 현장 담당자 정보를 비워두시면 계정 신청은 그대로 접수됩니다.",
} as const;

// ── 이용약관 동의 (18차) ──────────────────────────────────────────────────────
//
// 🔴 **왜 이제야 생겼나.** `TERMS_VERSION`은 5차에 정의된 뒤 계속 미사용이었다. 원인은
// `/quote`·`/apply`의 동의 문구가 **"개인정보 수집·이용"으로만 끝나고 이용약관을 언급하지
// 않았기 때문**이다. 14차가 "문구에 없는 동의를 기록하지 않는다"를 원칙으로 세웠으므로,
// **문구를 먼저 고쳐야 `terms` 동의를 기록할 수 있었다.** 18차가 그 문구를 넣은 것이다.
//
// 🔴 **체크박스를 하나로 합치지 말 것.** 개인정보보호법 제22조 1항이 **각각의 동의 사항을
// 구분해 알리고 각각 동의를 받도록** 하고 있다. 나중에 "UI 정리"로 묶지 말 것.
// 14차가 `PRIVACY_POLICY_VERSION`과 `TERMS_VERSION`을 나눠 둔 것과 같은 취지다 —
// 두 문서는 개정 주기가 달라서 5차에 분리했다.
//
// ⚠️ **이 문구는 초안이며 변호사 검토 대상이다.**
export const TERMS_CONSENT = {
  /**
   * `/apply` — 운송관리 계정 신청. **약관 동의를 받는 화면은 지금 여기 하나뿐이다.**
   *
   * 🔴 **`/quote`(공개 견적 문의)에는 넣지 않는다 — 사용자 결정(2026-08-26).**
   * 견적 문의 자체는 계약이 아니다. 18차가 처음에 "그 견적으로 그대로 운송이 진행될 수
   * 있다"는 이유로 넣었다가 되돌린 것이니, **다시 넣지 말 것.** 넣으려면 그 화면 문구부터
   * 고쳐야 하고(문구에 없는 동의는 기록하지 않는다 — 14차), 그건 별도 판단이다.
   */
  applyLabel: "[필수] 이용약관에 동의합니다.",
  /** 시안 폼(31차)처럼 「필수」를 별도 배지로 그리는 화면이 쓰는 라벨. */
  label: "이용약관에 동의합니다.",
  /**
   * 🔴 이 요약은 `lib/legal/terms.ts`의 **실제 조 제목에서 파생된 것**이다.
   *   운송계약 성립 → 제8조 / 운임 산정·추가비 → 제10·11조 /
   *   손해배상·면책 → 제19조(면책은 34차에 통합됐고 2026-09-03 개정으로 7항이다)
   * 🔴 **조문이 개정되면 이 요약도 같이 볼 것.** 어긋나면 **요약을 고치고 조문은 두라.**
   *
   * ⚠️ 31차 리뷰(2026-09-01)에 사용자가 *"아래 회색 설명글이 좀더 간략했으면 좋겠고"* 라고
   *    해서 줄였다. 시안 원문은 「운송계약 성립, 운임 산정, 손해배상과 면책에 관한
   *    내용입니다.」인데 **「추가비」만 되살려 두었다** — 제11조 추가비가 현장 분쟁이
   *    가장 잦은 항목이라 요약에서 빠지면 화주가 "그런 말 없었다"고 읽는다.
   *    🔴 상·하차·인도(제15·17조)는 이번에 요약에서 빠졌다 — 조문은 그대로다.
   */
  summary: "운송계약 성립, 운임 산정과 추가비, 손해배상과 면책에 관한 내용입니다.",
  refusal: "동의를 거부하실 수 있으나, 거부 시 서비스 이용이 제한됩니다.",
} as const;
